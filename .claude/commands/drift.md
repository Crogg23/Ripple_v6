---
description: How often the rules got broken lately — a number, not a feeling. Usage /drift [days]
---
Run `python scripts/drift_audit.py --days ${ARGUMENTS:-7}` and show its table as-is. One line under it: the single rule that broke most, and whether that's a wording problem or a hook problem.
