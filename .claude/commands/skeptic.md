---
description: Fresh-context adversarial pass on the last "done" claim. Usage /skeptic [what to check]
---
Run the `skeptic` subagent (Agent tool, subagent_type "skeptic") on the most recent claim of done / works / fine / fixed — or on this if given: $ARGUMENTS

Hand it three things, verbatim: (1) Chris's request in his own words, (2) what was built — files, diff, numbers, output, (3) the exact claim sentence.

When it returns: show its verdict and yours side by side in a short table. If they disagree, stop — Chris decides. Do not fix anything before he answers.
