# Engineering read — would a hiring manager say "hire him"?
2026-08-31. Live scan of the repo, CI, and warehouse. Receipts below.

## Method
- Repo shape: `git ls-files`, `git count-objects -vH`, `git verify-pack`.
- CI truth: `gh run list`, `gh run view --log-failed`.
- Warehouse: `connect/db.py`, ACCOUNTADMIN, INFORMATION_SCHEMA + LIBRARY_META.REGISTRY.CATALOG.

## Receipts

### Repo
| Thing | Value |
|---|---|
| Tracked files | ~6,300 |
| Tracked .py | 568 |
| Tests | 79 files, 2,896 passing |
| dbt models | 2,367 |
| dbt schema.yml | 1,770 |
| dbt not_null tests | 2,967 |
| dbt unique tests | 1,050 |
| CI workflows | tests.yml, dbt.yml |
| .git pack | 6.95 GiB, 26,776 objects, 0 garbage |

Largest blobs in history: 2,760 MB / 2,087 MB / 1,502 MB / 463 MB / 460 MB.
All reachable. `git clone` downloads ~7 GB.

### CI
`tests` workflow red on the last 3 pushes to main.
Run 33343969948: `2896 passed, 3 skipped, 345 deselected, 2 errors`.
Both errors identical:
```
ERROR tests/test_typing_layer.py::test_ruled_columns_are_really_typed
FileNotFoundError: .keys/ripple_dbt.p8
ERROR tests/test_typing_layer.py::test_cast_dates_stay_inside_range_guard
FileNotFoundError: .keys/ripple_dbt.p8
```
Cause: two live-warehouse tests missing `@pytest.mark.snowflake`.
`dbt` workflow green on all 3.

### Missing tooling
No `pyproject.toml`, `setup.py`, `Makefile`, `tox.ini`.
No `.pre-commit-config.yaml`, no `ruff.toml`, no linter config.
No formatter, no import-sort, no type checker in CI.
`scripts/` holds 234 loose .py files, 13 with dates in the filename.

### Secrets — clean
Only `scripts/secrets_check.py` matches a secret-ish name.
`.gitignore` covers `.env`, `*.env`, `.keys/`, `*.p8`.
CI dbt role is `RIPPLE_TRANSFORM_RW`, not ACCOUNTADMIN. Least privilege, documented inline.

### Warehouse
| Measure | Value |
|---|---|
| Landing rows | 1,317,592,444 |
| Landing tables | 2,213 |
| Marts rows | 1,150,100,812 |
| Marts size | 53.2 GB |
| Catalog sources | 3,288 |
| Modeled sources | 590 |
| Sampled sources | 1,567, 3.5M rows total |
| Failed sources | 265 |
| Hard-key sources | 217 of 590 modeled |

Dead weight in the warehouse:
- `LIBRARY_MARTS._RESTORE_20260731` — 28 tables, 240M rows, 9.18 GB, 17% of marts.
- `LIBRARY_MARTS_PREDBT_20260729` — full shadow database.
- `LIBRARY_META.REGISTRY` — 8 `SOURCE_REGISTRY` backups beside the live one.
- `LIBRARY_META.CONNECT_BAK_20260730` and `CONNECT_PRESPINE_20260730`.

## Fix order
1. Green the `tests` badge. Add `@pytest.mark.snowflake` to the two tests. 5 minutes.
2. Shrink `.git`. Rewrite history to drop the multi-GB blobs, force-push. Ask first.
3. Add `pyproject.toml` + `ruff` + a lint job in CI.
4. Fold `scripts/` one-offs into `_JUNK_DRAWER/` or a dated subfolder.
5. Drop the restore/shadow copies in Snowflake. Gated work — ask first.
