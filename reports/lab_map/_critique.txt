===== CRITIQUE =====

[CROSS_CUTTING_BLOCKER] THE big one: there is no ZIP centroid and no ZIP population anywhere, and ZIP is the warehouse's dominant geography. Deduped to finished marts (excluding TIMELINE views, _RESTORE and UNCATEGORIZED), 163 tables carry a ZIP against 102 with a county, 104 with a FIPS and only 56 with lat/lon — ZIP beats coordinates roughly 3 to 1. I read CORE.XWALK_ZCTA_COUNTY column by column: ZCTA5, COUNTY_FIPS, STATE_FIPS, COUNTY_NAME, STATE_USPS, XWALK_TYPE, VINTAGE. No latitude, no longitude, no population. A grep of every POPULATION* column in LIBRARY_MARTS returns county (DIM_COUNTY), tract (DIM_TRACT and HMDA_HISTORIC), water-system-served and density — never ZIP. So the biggest tables in the building can be counted by ZIP and never mapped or rated: ARCOS 178,598,026, FEC individual contributions 84,172,112, CMS facility-level MDS 31,403,215, USASpending assistance 19,902,879, CFPB complaints 17,168,287, Open Payments 15,385,047 + 14,700,786 + 13,250,000, NPPES 9,606,683. One ~33,000-row table (ZCTA, lat, lon, population) unblocks at once: Flow maps (that is literally its stated blocker), Mass rates at ZIP, KDE at ZIP grain, Hotspot and Moran's at ZIP grain, Aggregate-first below the state line, and the #1 item on the census-grid parking lot ('per person served', 1,426 models).
   tables: LIBRARY_MARTS.CORE.XWALK_ZCTA_COUNTY, LIBRARY_MARTS.HEALTH.HEALTH__FED_DEA_ARCOS, LIBRARY_MARTS.FINANCE.FINANCE__FED_FEC_INDIV_CONTRIBUTIONS, LIBRARY_MARTS.CONSUMER_PROTECTION.CONSUMER_PROTECTION__FED_CFPB_COMPLAINTS, LIBRARY_MARTS.HEALTH.HEALTH__FED_CMS_OPEN_PAYMENTS

[CROSS_CUTTING_BLOCKER] Second blocker, same family: zero boundary polygons, which caps the RENDERING half of six techniques at once. Measured across all 162,750 columns, exactly 2 are typed GEOGRAPHY and both are the same NOAA AIS point column (POSITION_GEOGRAPHY on the mart and on its TIMELINE view). Zero GEOMETRY-typed columns exist. I checked the two tables whose NAMES promise boundaries and both dropped the shape at ingest: ENVIRONMENT__FED_EPA_SUPERFUND_SITE_BOUNDARIES (2,114 rows) carries GIS_AREA, GIS_AREA_UNITS and PROJECTION but no shape column; ENVIRONMENT__FED_USGS_WBD_HUC8 (2,456) carries SHAPE_LENGTH and SHAPE_AREA but no shape. That is why the geom name-signal's 1,212 hits are noise — they are area metrics and WKT-in-TEXT. One TIGER ingest (state/county/tract/ZCTA polygons) keyed on the FIPS codes DIM_STATE, DIM_COUNTY and DIM_TRACT already carry lights up: zoom tiering (its blocker), Voronoi's drawn cells, choropleth for Aggregate-first, and the map surface under Hotspot, Moran's and KDE.
   tables: LIBRARY_MARTS.CORE.DIM_COUNTY, LIBRARY_MARTS.CORE.DIM_TRACT, LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__FED_EPA_SUPERFUND_SITE_BOUNDARIES, LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__FED_USGS_WBD_HUC8

[UNCLAIMED_SHAPE] Free-text harm narratives — roughly 29 million rows of people describing what happened to them, and not one of the 22 techniques touches text at all. Verified columns and row counts: CFPB complaints carries COMPLAINT_NARRATIVE (TEXT) alongside HAS_NARRATIVE (BOOLEAN) on 17,168,287 rows; CPSC NEISS carries NARRATIVE on 9,794,971 ER injury records; FDA MAUDE 2,743,561 device adverse events; FRA CASUALTIES NARRATIVE on 1,150,788; OSHA ITA case detail carries NEW_INCIDENT_DESCRIPTION and JOB_DESCRIPTION on 890,934 (2023) + 688,649 (2024); MSHA ACCIDENTS NARRATIVE on 273,623. Every one of those tables also carries a clock and a place, so the text is already joinable to the maps the catalog does cover. This is the single largest capability nobody claimed, and it is the closest thing in the warehouse to a human voice — which is exactly what the mission says the map is for.
   tables: LIBRARY_MARTS.CONSUMER_PROTECTION.CONSUMER_PROTECTION__FED_CFPB_COMPLAINTS, LIBRARY_MARTS.CONSUMER_SAFETY.CONSUMER_SAFETY__FED_CPSC_NEISS, LIBRARY_MARTS.HEALTH.HEALTH__FED_FDA_MAUDE, LIBRARY_MARTS.TRANSPORT.TRANSPORT__FED_FRA_CASUALTIES, LIBRARY_MARTS.LABOR.LABOR__FED_OSHA_ITA_CASE_DETAIL_2023

[UNCLAIMED_SHAPE] A whole duration/lifecycle layer is already MEASURED on disk and no technique claims it. reports/time_index/spans_ranked.csv holds 56 tables with a start+end pair already scored for median duration, closure rate, cohort comparability, share-open and backwards-dated rows. Biggest: EPA SDWA violations 14,430,187 span rows (COMPL_PER_BEGIN_DATE to NON_COMPL_PER_END_DATE), USASpending contracts full 19,999,869, USASpending assistance full 11,189,048, NIH RePORTER 1,993,071, FJC IDB criminal fugitive spans 1,427,191, EPA SDWA lead-and-copper samples 927,415, CourtListener judicial positions 50,420. Alongside it, cohorts_ranked.csv holds 142 entity-cohort tables and lag_ranked.csv holds 70 happened-to-reported lag tables (FAERS 4,778,271 rows; EPA ECHO inspection-to-action 565,447; a table literally called ENVIRONMENT__EPA_PENALTY_GAP). The catalog has no survival, hazard, or time-to-event technique anywhere — the closest is contact tracing, which uses ICE stints for co-presence and ignores the duration.
   tables: LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__FED_EPA_SDWA_SDWA_VIOLATIONS_ENFORCEMENT, LIBRARY_MARTS.ECONOMICS.ECONOMICS__FED_USASPENDING_CONTRACTS_FULL, LIBRARY_MARTS.SCIENCE_RESEARCH.SCIENCE_RESEARCH__FED_NIH_REPORTER, LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__FED_EPA_ECHO, LIBRARY_MARTS.IMMIGRATION.IMMIGRATION__FED_ICE_DETENTION_STINTS

[UNCLAIMED_SHAPE] Demographic disparity columns at scale — the most on-mission shape in the building, claimed by nothing. HOUSING__FED_CFPB_HMDA_HISTORIC (19,136,434 rows) carries five APPLICANT_RACE slots plus five CO_APPLICANT_RACE slots, APPLICANT_ETHNICITY, APPLICANT_SEX, APPLICANT_INCOME_000S, ACTION_TAKEN, DENIAL_REASON_1/2/3, RATE_SPREAD, and — the part every entry missed — its own tract denominators: POPULATION, MINORITY_POPULATION, HUD_MEDIAN_FAMILY_INCOME, NUMBER_OF_OWNER_OCCUPIED_UNITS, on the same row as STATE_CODE + COUNTY_CODE + CENSUS_TRACT_NUMBER. The technique is not hypothetical here: JUSTICE.JUSTICE__RACIAL_JAIL_DISPARITY already exists at 128,507 county-year rows with BLACK_JAIL_RATE, WHITE_JAIL_RATE, LATINX_JAIL_RATE, BW_RATIO, BW_RATE_GAP, per-race working-age population denominators and an IS_MEASURABLE_COHORT flag. Someone already built the disparity mart; the catalog does not know the family exists.
   tables: LIBRARY_MARTS.HOUSING.HOUSING__FED_CFPB_HMDA_HISTORIC, LIBRARY_MARTS.JUSTICE.JUSTICE__RACIAL_JAIL_DISPARITY, LIBRARY_MARTS.JUSTICE.JUSTICE__XC_VERA_INCARCERATION_TRENDS, LIBRARY_MARTS.HEALTH.HEALTH__FED_CMS_MEDICARE_PROVIDER

[UNCLAIMED_SHAPE] Denominators that are not resident population, and time-varying ones — every scored entry reached for DIM_COUNTY.POPULATION_2020, a single 2020 snapshot, and missed the better ones. HEALTH__FED_CDC_DRUG_POISONING_COUNTY is 53,387 rows of county x YEAR with its own POPULATION (FLOAT) and an age-adjusted death rate — a per-year county denominator, which is what any rate over a 2006-2012 or 2011-2026 span actually needs. LABOR__FED_OSHA_ITA_300A_SUMMARY_2024 (398,620 establishments) carries TOTAL_HOURS_WORKED and ANNUAL_AVERAGE_EMPLOYEES next to TOTAL_DEATHS and TOTAL_INJURIES — an exposure denominator, not a population one (note both hours and injuries are stored TEXT and need casting). EPA SDWA public water systems carries POPULATION_SERVED_COUNT on 434,040 systems; EPA ECHO carries POPULATION_DENSITY as FLOAT. CDC injury/violence county (132,000 rows) even ships RATE_M_CI, a confidence interval.
   tables: LIBRARY_MARTS.HEALTH.HEALTH__FED_CDC_DRUG_POISONING_COUNTY, LIBRARY_MARTS.LABOR.LABOR__FED_OSHA_ITA_300A_SUMMARY_2024, LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__FED_EPA_SDWA_SDWA_PUB_WATER_SYSTEMS, LIBRARY_MARTS.HEALTH.HEALTH__FED_CDC_INJURY_VIOLENCE_COUNTY

[UNCLAIMED_SHAPE] The shape of money itself. In finished marts (excluding TIMELINE) there are 656 numerically typed amount columns — 352 FLOAT and 304 NUMBER — against 347 still TEXT. The biggest carriers: SEC 13F positions 101,261,252 rows, FEC individual contributions 84,172,112, Medicare Part D prescribers 25,869,521, USASpending assistance 19,902,879, Open Payments 15,385,047 + 14,700,786 + 13,250,000 (43.3M rows of industry payments to named doctors), USASpending contracts 6,325,622, HRSA NPDB malpractice payouts 1,911,185. Every technique that touches these uses the amount as a weight to sum. Nothing in the catalog looks at the DISTRIBUTION of the amounts — digit tests, round-number and just-under-threshold bunching, concentration curves, tail fitting. Worth noting the 2026-07-27 sweep already found one of these by hand ('a tiny donor cluster generates ~1/5 of all rows at $23 each') — that is a concentration finding produced with no technique behind it.
   tables: LIBRARY_MARTS.FINANCE.FINANCE__FED_FEC_INDIV_CONTRIBUTIONS, LIBRARY_MARTS.HEALTH.HEALTH__FED_CMS_OPEN_PAYMENTS, LIBRARY_MARTS.FINANCE.FINANCE__FED_SEC_13F_POSITIONS, LIBRARY_MARTS.HEALTH.HEALTH__FED_HRSA_NPDB

[UNCLAIMED_SHAPE] A full calendar dimension nobody claimed. REFERENCE.REFERENCE__CALENDAR is 155,593 rows and 33 columns including FEDERAL_FISCAL_YEAR, FEDERAL_FISCAL_MONTH, FEDERAL_FISCAL_QUARTER, ELECTION_CYCLE, IS_ELECTION_YEAR, CONGRESS_NUMBER, IS_WEEKEND, IS_MONTH_END, IS_YEAR_END plus month/quarter/year start-and-end anchors; CORE.DIM_DATE adds 31,411 rows with US_FISCAL_YEAR and DAY_OF_WEEK_ISO. Bolt that onto the 403 tables now sitting on the RIPPLE_TS clock and you get calendar-effect analysis for free: the September fiscal-year-end contract dump, money timed to election cycles, weekday-only reporting artifacts, month-end filing spikes. No technique in the catalog names seasonality or calendar effects at all — the only time technique listed is correlation matrices.
   tables: LIBRARY_MARTS.REFERENCE.REFERENCE__CALENDAR, LIBRARY_MARTS.CORE.DIM_DATE, LIBRARY_MARTS.TIMELINE.TIMELINE__WAREHOUSE

[UNCLAIMED_SHAPE] Status-span lists with hard identifiers on both sides — the 'banned but still operating' machinery. PROCUREMENT__FED_SAM_EXCLUSIONS (167,928 rows) carries UEI, CAGE_CODE and NPI together with ACTIVATION_DATE and TERMINATION_DATE as real DATE columns plus IS_CURRENTLY_EXCLUDED (BOOLEAN); HEALTH__FED_HHS_OIG_LEIE 83,369; JUSTICE__INTL_OPENSANCTIONS_DEFAULT 1,281,846. UEI joins straight to USASpending, NPI joins straight to NPPES/Medicare/Part D. The capability is proven — LIBRARY_MARTS.FINDINGS already holds EXCLUDED_PROVIDER_PAID_AFTER_EXCLUSION, EXCLUDED_PROVIDER_AT_FACILITY and REVOKED_BUT_DEDUCTIBLE as built views — but no technique in the catalog names the shape. Caveat to carry: the 2026-07-27 sweep found the federal debarment list ≤9% loaded with fabricated status flags, so the span dates are usable and the flags are not.
   tables: LIBRARY_MARTS.PROCUREMENT.PROCUREMENT__FED_SAM_EXCLUSIONS, LIBRARY_MARTS.HEALTH.HEALTH__FED_HHS_OIG_LEIE, LIBRARY_MARTS.JUSTICE.JUSTICE__INTL_OPENSANCTIONS_DEFAULT, LIBRARY_MARTS.FINDINGS.EXCLUDED_PROVIDER_PAID_AFTER_EXCLUSION

[UNCLAIMED_SHAPE] Industry classification: 916 tables carry a NAICS or SIC column (195 named NAICS_CODE, 137 NAICS, 121 SIC, 105 INDUSTRY_SECTOR), and not one technique uses industry as an axis. Biggest carriers: Open Payments 15,385,047, EPA NPDES QNCR history 7,951,656, UK Companies House PSC 7,000,000, USASpending contracts 6,325,622, BLS QCEW 3,619,437, EPA FRS NAICS codes 2,179,702, SBA loans 2,174,502, FRA casualties 1,150,788, EPA FRS SIC codes 1,085,211, OSHA ITA 890,934. That is the natural peer-group axis for 'is this employer unusually bad compared with others doing the same work.' Gap worth stating: a grep of every table name for naics/sic/industry finds no dimension or lookup table anywhere — nothing decodes a code or nests a 6-digit code up to its 2-digit sector, so the hierarchy has to be built or the inline *_DESC columns trusted.
   tables: LIBRARY_MARTS.HEALTH.HEALTH__FED_CMS_OPEN_PAYMENTS, LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__FED_EPA_FRS_FRS_NAICS_CODES, LIBRARY_MARTS.ECONOMICS.ECONOMICS__FED_BLS_QCEW, LIBRARY_MARTS.LABOR.LABOR__FED_OSHA_ITA_CASE_DETAIL_2023

[UNCLAIMED_SHAPE] A 6.5-million-row labelled training set sits unused. LIBRARY_META.CONNECT.GOLD_PAIRS holds 6,538,936 rows of NPI, L_LAST, L_FIRST, R_LAST, R_FIRST, PLACE, LABEL (BOOLEAN) and SCORE — human-or-rule-labelled name-match pairs. No technique in the catalog is supervised; every one is descriptive geometry or graph math. This is the substrate for a learned record-linkage model, which is also the thing that would start dissolving the island problem two other entries call a blocker. Related and equally unclaimed: NICKNAME_MAP (102 name variants) and ENTITY_LINKS (2,324 rows with METHOD, SCORE, BAND).
   tables: LIBRARY_META.CONNECT.GOLD_PAIRS, LIBRARY_META.CONNECT.ENTITY_LINKS, LIBRARY_META.CONNECT.NICKNAME_MAP

[MISSING_TECHNIQUE] Survival / time-to-event analysis (Kaplan-Meier curves, hazard rates, cumulative-incidence, cohort funnels). The Laboratory has no duration technique in any of its five sections — the closest is 'Ripples,' which animates spread, not persistence. The substrate is not just present, it is pre-measured: 56 span tables in spans_ranked.csv and 70 lag tables in lag_ranked.csv, already scored for censoring, backwards dates and cohort comparability. The questions it answers are the mission's questions: how long does a water system stay in violation before anyone acts (EPA SDWA, 14,430,187 span rows), how long is someone held (ICE detention stints, 2,571,975 with BOOK_IN_AT/BOOK_OUT_AT as TIMESTAMP_NTZ), how long between an EPA inspection and any formal action (ECHO, 565,447 measured lag rows). The repo's own time-censoring memo already warns that raw durations shrink toward the present — meaning the trap is documented and the technique to handle it is not on the menu.
   tables: LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__FED_EPA_SDWA_SDWA_VIOLATIONS_ENFORCEMENT, LIBRARY_MARTS.IMMIGRATION.IMMIGRATION__FED_ICE_DETENTION_STINTS, LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__FED_EPA_ECHO, LIBRARY_MARTS.PROCUREMENT.PROCUREMENT__FED_SAM_EXCLUSIONS

[MISSING_TECHNIQUE] Group-disparity testing — rate ratios between demographic groups with a denominator per group, and the regression-adjusted version that asks whether the gap survives controls. This is the most direct answer to 'who gets hurt, and does the data show it,' and it is absent from all five sections of The Laboratory. Ready substrate: HMDA_HISTORIC 19,136,434 applications with race/ethnicity/sex/income/denial-reason and tract-level POPULATION and MINORITY_POPULATION on the same row; JUSTICE__RACIAL_JAIL_DISPARITY 128,507 county-years already carrying per-race rates, ratio and gap; CMS MEDICARE_PROVIDER 1,296,739 providers with beneficiary age and race counts among its 49 numeric columns. Note the one real limit: HMDA_HISTORIC has no DATE or TIMESTAMP column, only AS_OF_YEAR stored as TEXT, so disparity trends are year-grain and no finer.
   tables: LIBRARY_MARTS.HOUSING.HOUSING__FED_CFPB_HMDA_HISTORIC, LIBRARY_MARTS.JUSTICE.JUSTICE__RACIAL_JAIL_DISPARITY, LIBRARY_MARTS.HEALTH.HEALTH__FED_CMS_MEDICARE_PROVIDER

[MISSING_TECHNIQUE] Text analytics on the narrative corpus — topic modelling, embedding-space maps, phrase-shift over time, near-duplicate detection on complaint text. Nothing in The Laboratory mentions text in any form. ~29M free-text rows exist (CFPB COMPLAINT_NARRATIVE 17.2M, CPSC NEISS NARRATIVE 9.79M, MAUDE 2.74M, FRA 1.15M, OSHA ITA 1.58M across two years, MSHA 273k), each already carrying a place, a clock and a company or facility id — so a topic map is joinable to every geographic technique in the catalog on day one. One hard limit to state plainly: a full-column dtype census finds zero VECTOR-typed columns in all 162,750, so any embedding must be computed and stored, not read.
   tables: LIBRARY_MARTS.CONSUMER_PROTECTION.CONSUMER_PROTECTION__FED_CFPB_COMPLAINTS, LIBRARY_MARTS.CONSUMER_SAFETY.CONSUMER_SAFETY__FED_CPSC_NEISS, LIBRARY_MARTS.LABOR.LABOR__FED_MSHA_ACCIDENTS

[MISSING_TECHNIQUE] Changepoint / structural-break detection on the shared clock. The catalog's only time-comparison technique is correlation matrices ('what moves together'); it has nothing for 'when did this line break.' TIMELINE.TIMELINE__WAREHOUSE is already the exact input at 1,161,122 rows of RIPPLE_SOURCE x RIPPLE_CLOCK x RIPPLE_GRAIN x RIPPLE_DAY x N_ROWS across 403 sources, with 953 series listed in TRENDABLE.csv and 137 substantial ones pre-ranked in series_ranked_substantial.csv. The caveat is inherited from the 2026-08-20 trend sweep and applies to changepoints even harder than to correlations: most breaks will be collection artifacts (a crawl schedule changing, a bulk backfill) rather than the world changing, so the technique needs a publication-artifact filter built alongside it.
   tables: LIBRARY_MARTS.TIMELINE.TIMELINE__WAREHOUSE, LIBRARY_MARTS.TIMELINE.RIPPLE_TIME_REGISTRY

[MISSING_TECHNIQUE] Funnel plots / uncertainty-aware institutional benchmarking — plotting each facility's harm rate against its size so small-denominator noise is separated from genuine outliers. Absent from The Laboratory, and it is the standard fix for the failure mode the mission most invites (naming a tiny hospital as 'worst in America' because it had two bad months). Substrate: CMS HCRIS 6,103 hospitals x 107 numeric columns, CMS POS_OTHER 44,429 facilities x 204 numeric, CMS NURSING_HOME 14,700 x 61 with lat/lon, CMS NURSING_HOME_DEFICIENCIES 418,479, OSHA 300A 398,620 establishments with hours-worked as the exposure denominator. CDC injury/violence county already ships RATE_M_CI, proving the uncertainty column pattern exists in the warehouse.
   tables: LIBRARY_MARTS.HEALTH.HEALTH__FED_CMS_HCRIS, LIBRARY_MARTS.HEALTH.HEALTH__FED_CMS_POS_OTHER, LIBRARY_MARTS.HEALTH.HEALTH__FED_CMS_NURSING_HOME_DEFICIENCIES, LIBRARY_MARTS.LABOR.LABOR__FED_OSHA_ITA_300A_SUMMARY_2024

[MISSING_TECHNIQUE] Sankey / alluvial / chord diagrams — flow WITHOUT geography. The catalog's only movement technique is Flow maps, which the scoring downgraded to PARTIAL solely because there is no ZIP centroid to draw an arc between. A Sankey needs no coordinates at all, so every one of those flows is drawable today: ARCOS reporter-to-buyer across 178,598,026 shipments with TOTAL_MME as the ribbon width and REPORTER_BUSINESS_ACTIVITY / BUYER_BUSINESS_ACTIVITY as the tiers; FEMA IA damaged-ZIP to rental-resource-ZIP on 3,080,000 rows with RENTAL_ASSISTANCE_AMOUNT; FJC IDB criminal DISTRICT to TRANSFER_DISTRICT on the same row across 6,299,908 cases (court districts have no geography anywhere, which kills the map and not the Sankey). This is a whole visual family blocked by an assumption, not by data.
   tables: LIBRARY_MARTS.HEALTH.HEALTH__FED_DEA_ARCOS, LIBRARY_MARTS.HOUSING.HOUSING__FED_FEMA_IA_HOUSING_REGISTRATIONS, LIBRARY_MARTS.JUSTICE.JUSTICE__FED_FJC_IDB_CRIMINAL

[MISSING_TECHNIQUE] Compositional / mix-shift analysis — what share of a total each category holds and how that share moves, including categories that appear or vanish. Not in the catalog (entropy measures how spread a distribution is, never which parts moved). And it is already measured: reports/time_index/mix_ranked.csv holds 687 table-column rulings with mix_shift, dominance_flipped, n_appeared, n_vanished, pct_blank_early/late and explicit snapshot_risk and survivorship_risk flags. Pair it with the category-rich big tables — CFPB complaints (PRODUCT, SUB_PRODUCT, ISSUE, SUB_ISSUE, COMPANY_RESPONSE, TAGS on 17,168,287 rows), EPA SDWA violations (VIOLATION_CODE, CONTAMINANT_CODE, 15,432,737) — and you get 'what changed in the mix' with the collection-artifact risk already scored.
   tables: LIBRARY_MARTS.CONSUMER_PROTECTION.CONSUMER_PROTECTION__FED_CFPB_COMPLAINTS, LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__FED_EPA_SDWA_SDWA_VIOLATIONS_ENFORCEMENT

[INCONSISTENCY] Two entries contradict each other on whether polygons exist, and the metadata settles it. Sample-or-tier-by-zoom: 'There is no boundary-shape table anywhere in the warehouse... Exactly two columns out of 162,750 carry a real GEOGRAPHY type and both are the same ship-position point column.' Spatial autocorrelation: 'One absolute claim from the prior read is also wrong: a polygon layer does exist — LIBRARY_MARTS.HOUSING.HOUSING__FED_MAPPING_INEQUALITY carries a GEOMETRY column with HOLC_GRADE, FIPS and LAT/LON.' Measured: that column's dtype is TEXT, not GEOMETRY, in the mart, in staging and in raw landing. A full dtype census confirms 2 GEOGRAPHY columns (both NOAA AIS POSITION_GEOGRAPHY) and 0 GEOMETRY columns warehouse-wide. Zoom is right; Moran's overturned a correct claim on a column name. It matters because Moran's I was raised to READY partly on that sentence.
   tables: LIBRARY_MARTS.HOUSING.HOUSING__FED_MAPPING_INEQUALITY, LIBRARY_MARTS.MARITIME.MARITIME__FED_NOAA_AIS

[INCONSISTENCY] Flow maps says ARCOS is broken in ways four other entries say it is not. Flow maps: 'its BUYER_COUNTY, REPORTER_COUNTY and TRANSACTION_DATE are all bad casts in the Aug-10 review.' Aggregate-first: 'TRANSACTION_DATE is a real DATE.' Ecology/food-web: 'verified happened/day, HIGH confidence, 2006-01-01 to 2012-12-31.' Measured: in the live mart HEALTH__FED_DEA_ARCOS, BUYER_COUNTY is TEXT (ordinal 17) — it is only NUMBER in the _RESTORE_20260731 backup copy, which the brief says must never be cited. And reports/time_index/clock_index.csv rates health__fed_dea_arcos.transaction_date as happened / day / is_primary / HIGH with the note 'try_to_date(trim(TRANSACTION_DATE),\'MMDDYYYY\') with an explicit format... the cleanest big event clock in this batch.' The flow-maps entry appears to have read a backup-schema row as the live one.
   tables: LIBRARY_MARTS.HEALTH.HEALTH__FED_DEA_ARCOS, LIBRARY_MARTS._RESTORE_20260731.UNCATEGORIZED__FED_DEA_ARCOS_FULL

[INCONSISTENCY] Percolation treats CONNECT_EDGES as a complete network while Distance explicitly warns it is not — and both are READY on the same substrate. Distance: 'a missing pair there means never measured, not no overlap — treating missing as zero would make the warehouse look far more disconnected than it is.' Percolation: 'TIER, MATCH_RATE and CONFIDENCE confirmed on every edge — three ready-made threshold dials on a graph small enough to sweep instantly,' with no such caveat, as its #1 candidate. The arithmetic: 4,910 edges over 3,033 base tables is about 0.1% of the ~4.6M possible pairs. Percolation's entire output is the threshold at which islands snap into one blob — on a 0.1% sample that number measures the sampler, not the warehouse. Distance's caveat is not optional for percolation; it is fatal to it. Same pattern one level down: Astronomy correctly DROPPED MATCH_PAIRS ('a row says one key value appeared in two TABLES, not that entity A attracts entity B') and Connectome flags it too, yet Percolation still lists it as candidate #3.
   tables: LIBRARY_META.CONNECT.CONNECT_EDGES, LIBRARY_META.CONNECT.CONNECT_EDGES_INC, LIBRARY_META.CONNECT.MATCH_PAIRS

[INCONSISTENCY] Three entries are caveated or downgraded on a defect that the same report they cite says was FIXED. Hotspot's closing note: 'the blocker is... that 88 mart models ran their county and FIPS columns through a number cast, so the key you would join on is the thing most likely to be broken.' Flow maps' blocker leans on the same thing for USASpending. But reports/mart_defect_verdicts_2026-08-10.md section 1 records the repair — 'un-wrapped 173 columns across 101 model files... All 101 models rebuilt. 143 of 150 checked columns now carry data' — and states the outcome in the same words: 'county/state FIPS columns are now text, not numbers. That is the fix, not a regression — casting a FIPS to a number strips the leading zero.' Live metadata agrees: PRIME_AWARD_TRANSACTION_PLACE_OF_PERFORMANCE_COUNTY_FIPS_CODE and PRIME_AWARD_TRANSACTION_RECIPIENT_COUNTY_FIPS_CODE are both TEXT today, and every county/FIPS column on HRSA_SHORTAGE_AREAS is TEXT. The join key is intact; two techniques carry a stale warning.
   tables: LIBRARY_MARTS.ECONOMICS.ECONOMICS__FED_USASPENDING_ASSISTANCE_FULL, LIBRARY_MARTS.HEALTH.HEALTH__FED_HRSA_SHORTAGE_AREAS, LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__FED_EPA_FRS_FACILITIES

[OVERSOLD] Flow maps names the wrong defect on its own third candidate. It blames the two county-FIPS columns on ECONOMICS__FED_USASPENDING_ASSISTANCE_FULL (19,902,879 rows). The real problem is far worse and unmentioned: that table has ZERO numeric columns. All 112 are TEXT — FEDERAL_ACTION_OBLIGATION, TOTAL_OBLIGATED_AMOUNT, TOTAL_OUTLAYED_AMOUNT_FOR_OVERALL_AWARD, FACE_VALUE_OF_LOAN, NON_FEDERAL_FUNDING_AMOUNT and ACTION_DATE included. Its twin ECONOMICS__FED_USASPENDING_CONTRACTS_FULL has the identically-named money columns as FLOAT, so this is a per-table modelling miss, not a source limitation. Anyone weighting a flow, a Sankey or a concentration curve on federal assistance dollars gets nothing until those columns are cast. Same pattern worth flagging on two other giants: CMS NPPES is 333 columns with 1 numeric on 9,606,683 rows, and GLEIF is 338 columns with 0 numeric on 3,382,301 — which is the same root cause as the separately-noted 'GLEIF period dates are all TEXT.'
   tables: LIBRARY_MARTS.ECONOMICS.ECONOMICS__FED_USASPENDING_ASSISTANCE_FULL, LIBRARY_MARTS.ECONOMICS.ECONOMICS__FED_USASPENDING_CONTRACTS_FULL, LIBRARY_MARTS.HEALTH.HEALTH__FED_CMS_NPPES, LIBRARY_MARTS.ECONOMICS.ECONOMICS__INTL_GLEIF

[UNDERSOLD] Three geographic reference assets exist that no entry named. REFERENCE.REF__DIM_GEOGRAPHY (6,988 rows) carries FIPS_CODE, STATE_FIPS, COUNTY_FIPS_SUFFIX, COUNTY_NAME, STATE_ABBR, EPA_REGION and CENTROID_LATITUDE / CENTROID_LONGITUDE as FLOAT — a second county centroid table, with an EPA region rollup DIM_COUNTY lacks. REFERENCE__FED_DHS_HIFLD (500 rows, flagged elsewhere as a sample stub) pairs LATITUDE/LONGITUDE with FIPS, ZIP and NAICS_CODE — small, but it is the only table in the warehouse that carries all three keys with a coordinate, i.e. a worked example of the crosswalk shape the ZIP blocker needs. And HOUSING__FED_CFPB_HMDA_HISTORIC carries POPULATION per census tract on 19.1M rows, a second tract denominator beside DIM_TRACT that nobody mentioned.
   tables: LIBRARY_MARTS.REFERENCE.REF__DIM_GEOGRAPHY, LIBRARY_MARTS.REFERENCE.REFERENCE__FED_DHS_HIFLD, LIBRARY_MARTS.HOUSING.HOUSING__FED_CFPB_HMDA_HISTORIC

[UNCLAIMED_SHAPE] Bipartite two-mode structure — entities on one side, source files on the other. LIBRARY_META.CONNECT.ENTITY_MAP holds 33,312,349 entities each with MEMBER_TABLES (ARRAY) and SOURCE_COUNT (NUMBER). Two entries reach into it (centrality reads SOURCE_COUNT as degree, entropy reads MEMBER_TABLES as a distribution), but nobody claims the shape itself: projecting the two-mode graph onto either side. Project onto tables and you get a co-occurrence map of which sources actually describe the same population — the honest version of the schema graph that CONNECT_EDGES only samples at 0.1%. Project onto entities and you get 'which things are visible in an unusual combination of files,' which is a visibility-anomaly detector rather than a picture. Neither projection needs anything built or ingested.
   tables: LIBRARY_META.CONNECT.ENTITY_MAP, LIBRARY_META.CONNECT.ENTITY_INDEX


===== OPPORTUNITIES =====

* Flow maps  (now: PARTIAL, effort: Hours to one day. One dbt model over two tables that already exist, plus a tie-break decision on multi-county ZIPs. Optional follow-on of about a day: derive true per-ZIP centroids by averaging coordinates on the 12.9M geocoded facility/branch rows, which buys finer arcs but is facility-weighted rather than population-weighted and will have holes in ZIPs with no regulated site.)
   why close: The scoring's blocker is 'the clean same-row flows are ZIP-to-ZIP and nothing in the warehouse can place a ZIP on a map.' That is measurably not true. LIBRARY_MARTS.CORE.XWALK_ZCTA_COUNTY (46,960 rows, verified columns ZCTA5, COUNTY_FIPS, STATE_FIPS, XWALK_TYPE, VINTAGE) already maps every ZIP to a county, and LIBRARY_MARTS.CORE.DIM_COUNTY (3,222 rows) already carries POP_CENTER_LAT and POP_CENTER_LON as FLOAT plus POPULATION_2020. ZIP -> county -> centroid is a two-hop join over tables that both exist today, no ingest. For finer grain there is a second in-warehouse route: 23 mart tables carry a ZIP/POSTAL column alongside a numerically typed latitude, and four of them are large enough to average into per-ZIP centroids on their own -- ENVIRONMENT__FED_EPA_FRS_FACILITIES 5,300,149 (POSTAL_CODE + LATITUDE/LONGITUDE FLOAT + FIPS_CODE, all verified), ENVIRONMENT__FED_EPA_ECHO 3,135,554, FINANCE__FED_FDIC_SOD_BRANCH_DEPOSITS 2,823,000, ENVIRONMENT__FED_EPA_RCRA_FACILITIES 1,613,224 -- roughly 12.9M already-geocoded ZIP-stamped rows. And one end of the flagship displacement flow is already drawable with zero work: LIBRARY_MARTS.HOUSING.HOUSING__FED_FEMA_IA_HOUSING_REGISTRATIONS carries CENSUS_GEOID, which joins straight to LIBRARY_MARTS.CORE.DIM_TRACT's TRACT_GEOID and its POP_CENTER_LAT/LON.
   one step: Write one dbt model, DIM_ZIP_POINT: ZCTA5 from XWALK_ZCTA_COUNTY joined to DIM_COUNTY's POP_CENTER_LAT/LON, picking the dominant county row per ZIP (XWALK_ZCTA_COUNTY holds 46,960 rows for ~33k ZCTAs, so some ZIPs span counties and need a tie-break or an allocation weight). Every ZIP-to-ZIP flow in the warehouse becomes drawable the moment that model lands -- ARCOS's 178,598,026 reporter-ZIP-to-buyer-ZIP shipments and FEMA's 3,080,000 damaged-ZIP-to-rental-ZIP moves first.

* Ripples (diffusion/propagation animation)  (now: PARTIAL, effort: Hours. One cast in one dbt model, reusing a pattern already in that file, then a rebuild of a 484,142-row table. Add a day if you also want to sanity-check the span, since the clock index measures the period range running 1832 to 2035, so future-dated and junk-dated rows are present and need a filter.)
   why close: The named fix -- cast GLEIF's TEXT period dates -- is smaller than described, and the 'nowhere to propagate to' half of the blocker is wrong. Two verifications. First, reports/time_index/clock_index.csv already RULED relationship_period_1_startdate and _enddate as span_start/span_end at day grain, medium confidence, and the same file records that the SAME dbt model already applies a guarded to_date(try_to_timestamp_tz(...)) cast to registration_initialregistrationdate, registration_lastupdatedate and registration_nextrenewaldate -- all three confirmed as real DATE dtype in ALL_COLUMNS.csv, at HIGH confidence. So the cast is a copy-paste of a cast living in the same file, and a crude ripple animation actually runs TODAY off the already-typed registration date. Second, the claim that a ripple has nowhere to go misses LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__XC_EPA_CORPORATE_CROSSWALK -- 5,300,149 rows, one per EPA facility, with EPA_REGISTRY_ID, MATCHED_LEI, ULTIMATE_PARENT_LEI, PARENT_CIK, PARENT_UEI, MATCH_METHOD and MATCH_CONFIDENCE. That is a built bridge from GLEIF's LEI namespace to 5.3M geocoded facilities, to 20+ SEC tables via CIK, and to USASpending's 19.9M assistance and 20M contract rows via UEI.
   one step: In the existing economics__intl_gleif_relationships dbt model, apply the guarded to_date(try_to_timestamp_tz(...)) cast already used there on the registration dates to RELATIONSHIP_PERIOD_1_STARTDATE and _ENDDATE, filtered to RELATIONSHIP_PERIOD_1_PERIODTYPE = 'RELATIONSHIP_PERIOD' (the clock index writes that trap down explicitly -- the five period slots are a flattened repeating group and slot 1 is not the same kind of period on every row).

* Neuroscience -> connectome mapping  (now: PARTIAL, effort: Days, not hours, and the cost is the rebuild rather than the SQL. Prior sessions measured a full spine rebuild at roughly 4.5 hours and 10-15 dollars on X-Small, and memory records that flipping the registration flag freezes incremental until that rebuild finishes. Honest caveat: MATCH_METHOD, MATCH_CONFIDENCE and REVIEW_FLAG say this crosswalk is a fuzzy name match, and what share of the 5.3M rows carry a non-null MATCHED_LEI cannot be read from metadata -- check the fill rate before trusting it. The separate DEA island is a much bigger job: ARCOS carries BUYER_NAME/CITY/STATE/ZIP but no street address, and BUYER_DEA_NO and REPORTER_DEA_NO appear in no other table in the warehouse, so that bridge can only reach the name-at-ZIP corroborated tier.)
   why close: The scoring's own distance-to-ready is 'build one crosswalk per island.' One of those crosswalks is already built and was never mentioned: LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__XC_EPA_CORPORATE_CROSSWALK, 5,300,149 rows, verified columns EPA_REGISTRY_ID, FACILITY_NAME, MATCHED_LEI, MATCHED_LEGAL_NAME, MATCH_METHOD, MATCH_CONFIDENCE, ULTIMATE_PARENT_LEI, PARENT_LEGAL_NAME, PARENT_CIK, PARENT_UEI, REVIEW_FLAG. And there is live proof the chain traverses: LIBRARY_MARTS.FINDINGS.FEDERAL_CONTRACTOR_EPA_VIOLATOR is an existing view keyed on PARENT_UEI that already carries VIOLATING_FACILITIES, TOTAL_EPA_PENALTIES, FEDERAL_DOLLARS_OBLIGATED, N_AWARDS and N_AGENCIES on the same row -- an EPA-facility-to-federal-award join that is running now. So the corporate island (GLEIF 3,382,301 entities + 484,142 ownership edges, EPA FRS 5,300,149 facilities, USASpending 19.9M assistance + 20M contracts, the SEC CIK family) is already stitched; what is missing is that the spine does not know about it.
   one step: Register ENVIRONMENT__XC_EPA_CORPORATE_CROSSWALK as an edge source in the spine's key families (LEI, CIK, UEI) so LIBRARY_META.CONNECT sees EPA-facility-to-corporate-parent as edges, rather than the join living only inside one hand-written FINDINGS view. That fuses the largest corporate island into one connected component you can run centrality across instead of inside.

* Circulation (network / force-directed graphs) -- the politics lane specifically  (now: READY overall, but the politics domain inside it is effectively unwired, effort: Hours. One FLATTEN and four re-pointed models. Unverifiable from metadata alone: how many of the 12,794 rows actually carry a populated FEC_IDS array -- if it is as thin as the existing 1,715-row table the ceiling does not move, so measure the fill rate first.)
   why close: Prior sessions recorded that politics rides zero verified cross-family joins. The Rosetta stone that fixes it already exists and no scoring row mentions it: LIBRARY_MARTS.POLITICS.POLITICS__MEMBER_CROSSWALK, 12,794 rows, verified columns MEMBER_KEY, BIOGUIDE, ICPSR, GOVTRACK, OPENSECRETS, VOTESMART, LIS, THOMAS, CSPAN, WIKIDATA, BALLOTPEDIA, MAPLIGHT, GOOGLE_ENTITY_ID and FEC_IDS as a VARIANT. That single table connects the money side (LIBRARY_MARTS.FINANCE.FINANCE__FED_FEC_INDIV_CONTRIBUTIONS at 84,172,112 rows, keyed CMTE_ID, plus POLITICS__FEC_CAND_CMTE_LINK 16,229) to the behaviour side (POLITICS__VOTEVIEW_VOTES 945,523 keyed ICPSR, POLITICS__BILL_COSPONSORS 367,735 keyed BIOGUIDE). The evidence that the join is currently starved: POLITICS__MEMBER_FEC_ID holds only 1,715 rows and POLITICS__MEMBER_INDIV_DONATIONS, MEMBER_MONEY_RAISED, MEMBER_PAC_MONEY and MEMBER_VOTING_RECORD sit at 1,057, 1,050, 1,258 and 1,105 rows -- roughly a tenth of the 12,794 members the crosswalk covers.
   one step: LATERAL FLATTEN the FEC_IDS VARIANT array on POLITICS__MEMBER_CROSSWALK into a row-per-member-per-FEC-candidate-id bridge table, then re-point the four MEMBER_* money tables at it instead of the 1,715-row POLITICS__MEMBER_FEC_ID.

* Sample or tier by zoom  (now: PARTIAL, effort: One to two days: a download-and-land ingest of the kind this platform runs routinely, plus a WKT-to-GEOGRAPHY cast and a join test against the existing FIPS keys. Ranked last here purely because it is the only step on this list that needs a file from outside the warehouse -- but it has by far the widest blast radius, since the same polygons turn Mass, KDE, hotspot analysis, Moran's I and Voronoi from dots-and-surfaces into filled areas in one go.)
   why close: Every rung of the ladder is verified present and the only missing piece is a single reference file. LIBRARY_MARTS.CORE holds DIM_STATE (56), DIM_COUNTY (3,222 with COUNTY_FIPS, STATE_FIPS, POPULATION_2020, POP_CENTER_LAT/LON), DIM_TRACT (85,391 with TRACT_GEOID nesting up through COUNTY_FIPS and STATE_FIPS, same centroid and population columns) and XWALK_ZCTA_COUNTY (46,960). The FIPS keys a boundary file would join on are already sitting there. What is absent is confirmed absolute: a dtype sweep of all 162,750 columns returns exactly two GEOGRAPHY columns (both the same NOAA AIS point column, exposed twice) and zero GEOMETRY-typed columns anywhere, and a name sweep of ALL_TABLES.csv for tiger/boundary/shapefile/gazetteer/cartographic/zcta/polygon returns only FED_EPA_SUPERFUND_SITE_BOUNDARIES (2,114) and FED_BIA_TRIBAL_GEO (100).
   one step: Ingest the Census TIGER cartographic boundary files for state, county, tract and ZCTA -- free public downloads, keyed on the exact FIPS codes DIM_STATE, DIM_COUNTY and DIM_TRACT already carry -- and cast them into Snowflake GEOGRAPHY. That is roughly 89,000 polygon rows across all four rungs.


===== OPP FINDINGS =====

[UNDERSOLD] Flow maps is scored PARTIAL on the blocker 'nothing in the warehouse can place a ZIP on a map,' but the pieces to place a ZIP are both in LIBRARY_MARTS.CORE and were checked column by column: XWALK_ZCTA_COUNTY (46,960 rows) carries ZCTA5, COUNTY_FIPS and STATE_FIPS, and DIM_COUNTY (3,222 rows) carries POP_CENTER_LAT and POP_CENTER_LON as FLOAT. ZIP to county to centroid is a two-hop join with no ingest. The scoring is right that no ZIP-level centroid exists, but wrong that ZIP flows therefore cannot be drawn -- they can be drawn at county resolution today. On top of that, FEMA's origin end needs no crosswalk at all: HOUSING__FED_FEMA_IA_HOUSING_REGISTRATIONS carries CENSUS_GEOID, which joins directly to DIM_TRACT.TRACT_GEOID and its centroid. This changes the opportunity picture because it moves flow maps from 'needs a data source we do not have' to 'needs one dbt model over two tables we do have.'
   tables: LIBRARY_MARTS.CORE.XWALK_ZCTA_COUNTY, LIBRARY_MARTS.CORE.DIM_COUNTY, LIBRARY_MARTS.CORE.DIM_TRACT, LIBRARY_MARTS.HOUSING.HOUSING__FED_FEMA_IA_HOUSING_REGISTRATIONS, LIBRARY_MARTS.HEALTH.HEALTH__FED_DEA_ARCOS

[MISSING_TECHNIQUE] No scoring row mentions LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__XC_EPA_CORPORATE_CROSSWALK, and it is the single most consequential omission in the whole set. Verified: 5,300,149 rows -- exactly one per row of ENVIRONMENT__FED_EPA_FRS_FACILITIES -- with EPA_REGISTRY_ID, FACILITY_NAME, MATCHED_LEI, MATCHED_LEGAL_NAME, MATCH_METHOD, MATCH_CONFIDENCE (NUMBER), ULTIMATE_PARENT_LEI, PARENT_LEGAL_NAME, PARENT_CIK, PARENT_UEI, REVIEW_FLAG (BOOLEAN). This is a pre-built bridge from GLEIF's LEI namespace onto 5.3M geocoded, FIPS-stamped facilities, and onward to the SEC family through PARENT_CIK (CIK appears in 20+ FINANCE and ECONOMICS mart tables) and to federal money through PARENT_UEI (RECIPIENT_UEI / RECIPIENT_PARENT_UEI on USASpending contracts 20,000,000 and assistance 19,902,879). Three separate scoring rows -- Circulation, Ripples and connectome mapping -- describe the corporate lane as sealed or as reaching only 'nine other mart tables,' and all three are working from an incomplete picture. Caveat that must travel with this: MATCH_METHOD/MATCH_CONFIDENCE/REVIEW_FLAG mark it as a fuzzy name match, and the non-null fill rate of MATCHED_LEI is unverified from metadata.
   tables: LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__XC_EPA_CORPORATE_CROSSWALK, LIBRARY_MARTS.ECONOMICS.ECONOMICS__INTL_GLEIF_RELATIONSHIPS, LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__FED_EPA_FRS_FACILITIES, LIBRARY_MARTS.ECONOMICS.ECONOMICS__FED_USASPENDING_CONTRACTS_FULL, LIBRARY_MARTS.ECONOMICS.ECONOMICS__FED_USASPENDING_ASSISTANCE_FULL

[UNDERSOLD] The connectome row calls the islands untouchable and BRIDGE_ENTITIES empty, but LIBRARY_MARTS.FINDINGS holds 13 pre-built cross-domain views that already traverse islands, and one of them proves the corporate chain works end to end: FEDERAL_CONTRACTOR_EPA_VIOLATOR carries PARENT_UEI, COMPANY_NAME, VIOLATING_FACILITIES, ANY_SNC, TOTAL_EPA_PENALTIES, TOTAL_FORMAL_ACTIONS, FEDERAL_DOLLARS_OBLIGATED, N_AWARDS, N_AGENCIES and MATCH_CONFIDENCE on one row. Its siblings do the same for other lanes -- EXCLUDED_PROVIDER_PAID_AFTER_EXCLUSION_IN_SHORTAGE, OPIOID_PRESCRIBER_PAID_HIGH_RX, PAC_FUNDS_BOTH_SIDES, REVOKED_BUT_DEDUCTIBLE, HOSPITAL_CLOSURE_RISK. These are views, so ALL_TABLES.csv reports no row count for them and I cannot say how many rows each returns -- but their existence contradicts the flat claim that cross-island joins are not running. Note the honest counterweight: TOTAL_EPA_PENALTIES on that view is drawing from the EPA penalty column the 2026-07-27 sweep flagged for stamping one settlement onto hundreds of facilities.
   tables: LIBRARY_MARTS.FINDINGS.FEDERAL_CONTRACTOR_EPA_VIOLATOR, LIBRARY_MARTS.FINDINGS.PAC_FUNDS_BOTH_SIDES, LIBRARY_MARTS.FINDINGS.OPIOID_PRESCRIBER_PAID_HIGH_RX, LIBRARY_META.CONNECT.BRIDGE_ENTITIES

[UNDERSOLD] The Ripples row states GLEIF RELATIONSHIPS 'carries five period start/end pairs but all ten are TEXT.' That is true and I confirmed it, but the row omits that the same table already carries three real DATE columns -- REGISTRATION_INITIALREGISTRATIONDATE, REGISTRATION_LASTUPDATEDATE and REGISTRATION_NEXTRENEWALDATE -- and reports/time_index/clock_index.csv rates all three at HIGH confidence, noting they come from a guarded to_date(try_to_timestamp_tz(...)) parse already applied inside that dbt model. So a dated ownership animation runs today off the registration clock, and the outstanding cast is a copy-paste of a cast sitting in the same file rather than new work. The clock index also already ruled the period columns as span_start/span_end at day grain and wrote down the trap in writing: the five slots are a flattened repeating group, so you must filter RELATIONSHIP_PERIOD_1_PERIODTYPE = 'RELATIONSHIP_PERIOD' first.
   tables: LIBRARY_MARTS.ECONOMICS.ECONOMICS__INTL_GLEIF_RELATIONSHIPS

[MISSING_TECHNIQUE] LIBRARY_MARTS.POLITICS.POLITICS__MEMBER_CROSSWALK (12,794 rows) appears in no scoring row, and it is the identifier Rosetta stone for a domain prior sessions recorded as having zero verified cross-family joins. Verified columns: MEMBER_KEY, BIOGUIDE, ICPSR, GOVTRACK, OPENSECRETS, VOTESMART, LIS, THOMAS, CSPAN, WIKIDATA, BALLOTPEDIA, WIKIPEDIA, HOUSE_HISTORY, MAPLIGHT, GOOGLE_ENTITY_ID, FEC_IDS (VARIANT), plus name, party, state and term fields. The downstream tables that should be consuming it are starved: POLITICS__MEMBER_FEC_ID holds 1,715 rows and MEMBER_INDIV_DONATIONS / MEMBER_MONEY_RAISED / MEMBER_PAC_MONEY / MEMBER_VOTING_RECORD hold 1,057 / 1,050 / 1,258 / 1,105 against a 12,794-member spine. Flattening the FEC_IDS array is a hours-long job that could scale the money-to-votes graph roughly tenfold, wiring FEC_INDIV_CONTRIBUTIONS (84,172,112) to VOTEVIEW_VOTES (945,523) and BILL_COSPONSORS (367,735).
   tables: LIBRARY_MARTS.POLITICS.POLITICS__MEMBER_CROSSWALK, LIBRARY_MARTS.POLITICS.POLITICS__MEMBER_FEC_ID, LIBRARY_MARTS.POLITICS.POLITICS__VOTEVIEW_VOTES, LIBRARY_MARTS.FINANCE.FINANCE__FED_FEC_INDIV_CONTRIBUTIONS

[INCONSISTENCY] Two scoring rows contradict each other on whether the warehouse holds any polygon. 'Sample or tier by zoom' says exactly two of 162,750 columns are real GEOGRAPHY and there is no boundary shape anywhere; 'Spatial autocorrelation (Moran's I)' says 'One absolute claim from the prior read is also wrong: a polygon layer does exist -- LIBRARY_MARTS.HOUSING.HOUSING__FED_MAPPING_INEQUALITY carries a GEOMETRY column.' Measured from ALL_COLUMNS.csv: that column is named GEOMETRY but its dtype is TEXT, and a dtype sweep of the full column census returns exactly two GEOGRAPHY columns (both MARITIME__FED_NOAA_AIS.POSITION_GEOGRAPHY, once in MARITIME and once in TIMELINE) and zero GEOMETRY-typed columns in the entire warehouse. Whether that TEXT column holds real WKT or GeoJSON cannot be determined from metadata -- call it unverified. The zoom row is right; the Moran's row overstates. This matters because the polygon gap is the one cross-cutting blocker whose fix lights up five techniques at once, and a reader who believes a polygon layer already exists will not go get one.
   tables: LIBRARY_MARTS.HOUSING.HOUSING__FED_MAPPING_INEQUALITY, LIBRARY_MARTS.MARITIME.MARITIME__FED_NOAA_AIS

[CROSS_CUTTING_BLOCKER] The absence of any national boundary polygon is the single widest constraint in the set, and it is confirmed twice over: zero GEOMETRY-typed columns and two GEOGRAPHY columns across 162,750, and a name sweep of ALL_TABLES.csv for tiger/boundary/shapefile/gazetteer/cartographic/zcta/polygon returning only LIBRARY_RAW.LANDING.FED_EPA_SUPERFUND_SITE_BOUNDARIES (2,114 rows) and LIBRARY_RAW.LANDING.FED_BIA_TRIBAL_GEO (100 rows). Every geometry-named column outside those is TEXT, and the bulk of them are city ArcGIS portal crawls in LIBRARY_STAGING.DBT_CROGERS (Harris County, Atlanta, Fort Worth, Columbus). This one gap caps 'Sample or tier by zoom' at PARTIAL and quietly degrades the rendered output of Mass, KDE, hotspot analysis, Moran's I and Voronoi from filled areas to dots and surfaces, even though all five are correctly scored READY on the statistics. One TIGER ingest keyed on the FIPS codes DIM_STATE, DIM_COUNTY and DIM_TRACT already carry lifts all of them together.
   tables: LIBRARY_MARTS.CORE.DIM_COUNTY, LIBRARY_MARTS.CORE.DIM_TRACT, LIBRARY_MARTS.CORE.DIM_STATE, LIBRARY_RAW.LANDING.FED_EPA_SUPERFUND_SITE_BOUNDARIES

[CROSS_CUTTING_BLOCKER] The shape of nearly every cheap unlock on this list is the same: the value is present but stored as text. 91.6% of all columns are TEXT (149,003 of 162,750) against 7,558 NUMBER, 2,150 DATE and 2,064 FLOAT. 805 tables have a latitude-named column and only 122 store it as a number; 4,977 money columns exist and 3,478 are TEXT; 12,562 date-ish columns exist and 9,385 are TEXT. The pattern the verified-facts file records is that the mart layer is where casts happened and the raw landing copies are still text -- so the generic form of a small-effort unlock in this warehouse is 'add a guarded cast to an existing dbt model,' which is exactly what the GLEIF period-date step is. Anyone ranking opportunities should expect the cheapest wins to be casts, and the expensive ones to be ingests and spine rebuilds.
   tables: LIBRARY_MARTS.ECONOMICS.ECONOMICS__INTL_GLEIF_RELATIONSHIPS, LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__FED_EPA_AIR_EMISSIONS_POLL_RPT_COMBINED_EMISSIONS

[UNCLAIMED_SHAPE] LIBRARY_MARTS.HOUSING.HOUSING__FED_CFPB_HMDA_ARID2017_LEI_XREF (5,399 rows, columns ARID_2017, RESPONDENT_NAME, LEI_2018, LEI_2019, LEI_2020) is an unused bridge that could pull the warehouse's largest tract-level table into the corporate ownership graph. HOUSING__FED_CFPB_HMDA_HISTORIC holds 19,136,434 loan records carrying RESPONDENT_ID and AGENCY_CODE plus STATE_CODE, COUNTY_CODE and CENSUS_TRACT_NUMBER; the xref maps the pre-2018 respondent identifier to the LEI used from 2018 on, and LEI reaches GLEIF's 3,382,301 entities and 484,142 ownership edges. That would put lender ownership on 19.1M mortgage decisions at census-tract grain -- the redlining lane. Two honest caveats: whether ARID_2017 is simply AGENCY_CODE concatenated with RESPONDENT_ID is an assumption I could not verify from metadata, and HMDA_HISTORIC has no DATE column at all -- its only clock is AS_OF_YEAR stored as TEXT, so the joined result is year-grain at best.
   tables: LIBRARY_MARTS.HOUSING.HOUSING__FED_CFPB_HMDA_ARID2017_LEI_XREF, LIBRARY_MARTS.HOUSING.HOUSING__FED_CFPB_HMDA_HISTORIC, LIBRARY_MARTS.ECONOMICS.ECONOMICS__INTL_GLEIF_RELATIONSHIPS

[UNCLAIMED_SHAPE] The contact-tracing row treats ICE detention as a single sealed table, but LIBRARY_MARTS.IMMIGRATION.IMMIGRATION__FED_ICE_DETENTION_FACILITY_CODES (1,490 rows) carries DETENTION_FACILITY_CODE, DETENTION_FACILITY_NAME, ADDRESS, CITY, COUNTY, STATE, ZIP and LATITUDE/LONGITUDE as NUMBER -- and IMMIGRATION__FED_ICE_DETENTION_STINTS (2,571,975 rows) carries the matching DETENTION_FACILITY_CODE. That is a one-hop join putting 2.57M person-level detention stints, each with real TIMESTAMP_NTZ book-in and book-out columns, onto a map with a county key and a distance-band neighbour rule. It makes the detention data eligible for hotspot analysis, KDE and flow maps, not just contact tracing, and no scoring row claims it.
   tables: LIBRARY_MARTS.IMMIGRATION.IMMIGRATION__FED_ICE_DETENTION_STINTS, LIBRARY_MARTS.IMMIGRATION.IMMIGRATION__FED_ICE_DETENTION_FACILITY_CODES

[OVERSOLD] The flow-maps row states that both USASpending county FIPS columns were 'pushed through try_to_number in the Aug-10 review, along with ACTION_DATE, so the claim that it joins straight to DIM_COUNTY centroids with no name matching is false as written.' Two things soften that. The stored dtype of PRIME_AWARD_TRANSACTION_RECIPIENT_COUNTY_FIPS_CODE and PRIME_AWARD_TRANSACTION_PLACE_OF_PERFORMANCE_COUNTY_FIPS_CODE in the mart is TEXT, not NUMBER -- so whatever the model SQL does mid-stream, the landed values are strings and may still carry their leading zeros. I cannot settle it from metadata and did not run SQL, so the honest reading is 'suspect, check the values,' not 'confirmed broken.' The distinction matters: if those two columns survived intact, the county-to-county flow lane over 19,902,879 assistance rows is READY today with no repair at all.
   tables: LIBRARY_MARTS.ECONOMICS.ECONOMICS__FED_USASPENDING_ASSISTANCE_FULL, LIBRARY_MARTS.CORE.DIM_COUNTY

[UNDERSOLD] The Circulation row's closing note says GLEIF is 'the only one that can [cross a source boundary]' and grounds that in LEI appearing in nine other mart tables. I counted the LEI-carrying mart tables and get twelve column hits across nine distinct non-backup tables -- ECONOMICS__INTL_GLEIF, ECONOMICS__INTL_GLEIF_REPEX, ENVIRONMENT__XC_EPA_CORPORATE_CROSSWALK (MATCHED_LEI and ULTIMATE_PARENT_LEI), FINANCE__FED_FDIC_BANK_DATA, FINANCE__INTL_ISO_MIC_REGISTRY, HOUSING__FED_CFPB_HMDA, HOUSING__FED_CFPB_HMDA_DC_ONLY, HOUSING__FED_CFPB_HMDA_LAR and HOUSING__FED_CFPB_HMDA_ARID2017_LEI_XREF -- so the count is roughly right but the weighting is badly off. Three of the nine are HMDA stubs at 28,301 / 28,301 / 17,474 rows. The one that carries almost all the mass is the EPA crosswalk at 5,300,149, and it is also the only one that hands LEI a further hop to CIK and UEI. Reading 'nine tables' as nine comparable destinations overstates the breadth and badly understates the depth.
   tables: LIBRARY_MARTS.ENVIRONMENT.ENVIRONMENT__XC_EPA_CORPORATE_CROSSWALK, LIBRARY_MARTS.HOUSING.HOUSING__FED_CFPB_HMDA_LAR, LIBRARY_MARTS.FINANCE.FINANCE__FED_FDIC_BANK_DATA
