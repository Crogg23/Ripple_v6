# Road to 95 — written 2026-09-21

Chris: "get me to 95 - plan it out - that is your purpose now."

## The ruler first

Every score so far was a judgement: 58 audit, 80 eyeballed, 83 mine, 77 skeptic. A plan to 95 needs a number
that does not move with whoever holds the pen. So the score is pinned to the audit's own findings:

- The 2026-09-20 audit lost 42 points across 23 findings. Fitted weights reproduce the 58 exactly:
  severe 3.5, moderate 1.2, minor 0.3. 9 x 3.5 + 7 x 1.2 + 7 x 0.3 = 42.
- Closed and proven live = full points back. Label-only or half-fixed = half. Open = none.
- New problems found along the way are booked as debt, so the score can go down as well as up.
- Step 0 turns this into a script. Each finding becomes one live query with a pass or fail. After that,
  "how close to 95" is a command, not an opinion.

## Where it stands tonight, strict count

| Finding | Weight | Back so far | State |
|---|---|---|---|
| S1 Bare casts in marts | 3.5 | 2.6 | source-reading marts closed live; 78 casts in staging-reading marts unchecked |
| S2 Staffing counts rounding | 3.5 | 3.5 | closed live |
| S3 FEC memo double-count | 3.5 | 1.75 | marts flag it; public aggregate untouched since 2026-07-12 |
| S4 USASpending 1M-per-year cap | 3.5 | 1.75 | contracts on the uncapped reload; assistance still capped |
| S5 BJS and HUDOC capped samples | 3.5 | 3.5 | closed live today, 68,852 and 211,778 |
| S6 Part B floor of 11 | 3.5 | 1.75 | label only |
| S7 DME family undercount | 3.5 | 1.75 | label only |
| S8 Public views erroring | 3.5 | 3.5 | closed, 254 of 254 |
| S9 ARCOS has no row key | 3.5 | 1.75 | label only |
| M1 Seven renamed-away staging views | 1.2 | 0 | still live |
| M2 512 hand-written staging files outside the cast sweep | 1.2 | 0 | open |
| M3 Part D vintage | 1.2 | 0.6 | label only |
| M4 CHOW flag constant | 1.2 | 0.6 | label only |
| M5 Six unique tests that could not fail | 1.2 | 1.2 | closed |
| M6 Two junk public views | 1.2 | 0 | still live |
| M7 SDWA violation id grain | 1.2 | 0 | open |
| Seven minors | 2.1 | 0 | none re-checked |
| Debt booked 2026-09-21 | | -2.0 | 32 tests moved to warn, loader stamps time wrong, shelf refresh unsafe, rollback files are not undo |

58 + 21.85 + 2.4 - 2.0 = **80**. Ceiling if everything below lands: 100. Ceiling without the grants reload: 98.

## The steps

### Step 0 — the ruler and a save point. 0 points, makes every later point provable.
- `scripts/score_warehouse.py`: 23 findings plus the debt items, one read-only live query each, prints
  pass, half or fail and the total. Writes a dated TSV to `reports/`.
- Commit today's uncommitted work: 33 marts, 3 staging files, 1 macro, 1 script, 2 reports.
- Needs Chris: a yes to commit. No warehouse write.

### Step 1 — delete the dead weight. +3.0, about an hour.
- Drop the 7 renamed-away staging views (M1, 1.2).
- Drop or replace the 2 junk public views, FRAUD_SETTLEMENTS and BENEFICIAL_OWNERSHIP_REGISTRY (M6, 1.2).
- Drop the frozen `DBT_CROGERS_RIPPLE` schema copy, 1,454 objects, read by nothing (minor, 0.3).
- Drop the dead capped SAM exclusions copy and the two `__PREV_20260906` backup tables (minor, 0.3).
- Before each drop: prove zero readers, save the DDL. Needs Chris: `greenlight destroy`, after the list is shown.

### Step 2 — turn labels into locks. +6.45, the biggest block, half a day.
A label stops a careful reader. A lock stops a careless query. Each of these gets a lock plus a test.
- Part B (S6): the source deletes rows under 11 patients, so the money cannot be restored. Lock: add
  `provider_total_paid` and `service_rows_cover_pct` beside every service row, so any sum shows its own gap.
  Test: coverage column is filled on every row.
- DME (S7): family columns undercount by 21 to 40 percent. Lock: null the family columns on suppressed rows and
  add `is_suppressed`. Test: no suppressed row carries a family value.
- ARCOS (S9): build a real row key. Hash of the 7-column compound plus an occurrence number inside each of the
  3,841 collisions. Test: unique and not null on all 178,598,026 rows. One ARCOS build, about 5 minutes.
- Part D vintage (M3): add a `data_year` column to the 3 general Part D marts. Test: one value per mart.
- CHOW (M4): drop the constant column from the nursing-home mart, keep the real one in nursinghome411. Test:
  accepted values.

### Step 3 — the public campaign money totals. +1.75, a couple of hours.
- `build_giant_aggs.py`: filter memo rows, cover all 14 cycles, rebuild from 283.8M records.
- The rollup is 163,388 rows against the script's own 100,000 cap. Needs Chris, one call: I would raise the
  cap, since 163k rows is tiny. Other ways: coarser grain (loses detail), split by cycle (more tables).
- Proof: aggregate total equals landing total with memo rows excluded, to the dollar, per cycle.
- Warehouse write, so a price first.

### Step 4 — finish the cast job everywhere. +2.7, half a day.
- The 78 same-shape casts in marts that read staging: check what the staging view under each already does,
  guard the ones that are truly bare, old-vs-new on live rows by printed value (S1 remainder, 0.9).
- Point `check_staging_casts.py` at all 1,402 staging files, not just the generated ones (M2, 1.2).
- The 42 staging views that hide a row or two, mostly headerless EIA tables: one root cause, one fix (minor 0.3).
- A test that the cast script and the macro file agree, instead of a comment asking them to (minor 0.3).

### Step 5 — pay the debt and the leftovers. +4.1, half a day.
- Fix the loader that writes epoch microseconds into a seconds timestamp, so the next full reload is clean.
- Replace the 32 warn-only `not_null` tests with threshold tests: "no more than N percent blank." Coverage
  comes back and a real regression still fails the build.
- Shelf refresh: make the builder keep a live view's body, not just its shelf, then prove it with a dry run
  that changes 0 of 254. Until then the refresh stays off limits.
- Rollback files: qualify the names so they run as written.
- SDWA violation id (M7, 1.2): document the violation-by-enforcement grain in the schema and test the real key.
- Remaining minors (0.9): tests on the 11 untested marts, join-trap notes on Part D NPI and UK company number,
  facility affiliation label.
- ECHR public comment still says "2,000 row sample". Chris owns the wording.

### Step 6 — the grants reload. +1.75, the only big spend.
- USASpending assistance is cut at exactly 1,000,000 rows a year. Reload it uncapped through the fast path,
  one PUT and one COPY, the way the contracts R2 reload was done. Repoint mart and timeline. Correct the
  stale registry notes.
- Needs Chris: price first, from what the contracts R2 load cost, then `greenlight spend`.
- 95 is reachable without this step. It is the margin.

### Step 7 — the re-audit.
- Run the ruler. Then a fresh skeptic pass over the whole thing with Chris's words verbatim and the claim
  "95 or better." Both numbers go to Chris. He decides whether it is done.

## Running total

| After step | Strict score |
|---|---|
| Tonight | 80 |
| 1 deletes | 83 |
| 2 locks | 89.5 |
| 3 FEC totals | 91.2 |
| 4 casts everywhere | 93.9 |
| 5 debt and leftovers | 98 |
| 6 grants reload | 99.8 |

Expect the skeptic to shave 2 to 4 points off any of these. That is why the plan aims past 95, not at it.

## What needs Chris, all in one place
1. Yes to commit today's work.
2. `greenlight destroy` for step 1, after the drop list is shown.
3. The FEC cap call in step 3. I would raise it.
4. `greenlight spend` for step 6, after a real price.
5. ECHR comment wording.
