You are onboarding a data source into Ripple, an investigative-journalism data
Library. Read the documentation page below and extract a precise profile that
will become a row in the SOURCE_REGISTRY and drive ingestion.

SOURCE
  Name:        {name}
  URL:         {url}
  Layer hint:  {layer}
  Known IDs:   {identifiers}

FOREMAN FEEDBACK (apply this if not "(none)"):
  {feedback}

ACCESS NOTE (how the page below was fetched -- this drives access_pattern):
  {access_note}

DOCUMENTATION PAGE (truncated):
---
{page_text}
---

Return STRICT JSON only (no prose, no code fences) with exactly these keys:

{
  "jurisdiction": "one of: federal | international | cross-cutting | local | state",
  "source_id": "lowercase snake_case id, e.g. fed_usgs_earthquakes (omit to auto-derive)",
  "category": "high-level domain, e.g. Economy, Healthcare, Spending & Budget",
  "subcategory": "finer topic",
  "publisher": "the issuing agency/org",
  "description": "one-line human description",
  "unit_of_observation": "one row = one WHAT",
  "temporal_coverage": "e.g. 1947-present",
  "geographic_scope": "e.g. US (county-level), global",
  "access_pattern": "one of: bulk_csv | bulk_zip | bulk_json | paginated_api | scrape | scrape_js. Use scrape for plain HTML you can parse from the raw response. Use scrape_js ONLY when the data needs a real browser: a JS-rendered SPA (the data is not in the page source) OR a bot wall (a 'Just a moment...' / Cloudflare / 'enable JavaScript' challenge instead of content). scrape_js is heavier (drives a headless browser) -- prefer a documented file/API, then scrape, before scrape_js",
  "access_method": "registry vocab: API | bulk download | API + bulk download | portal-only | scrape | mixed",
  "auth": {"type": "none | free API key | registration | paid/license", "env_var": "the CONVENTIONAL environment-variable name for the key, e.g. CENSUS_API_KEY, FRED_API_KEY, BLS_API_KEY -- use the name the docs give, else <PUBLISHER>_API_KEY; empty string \"\" when type is none", "notes": "where to get a key, cost"},
  "format": "CSV | JSON | XML | ...",
  "cost": "free | paid | freemium",
  "update_cadence": "real-time | daily | monthly | annual | irregular | static",
  "est_volume": "rough row/record volume",
  "license_terms": "e.g. public domain, CC-BY",
  "key_identifiers": ["join keys present, e.g. FIPS, EIN, NPI, lat_lon, CIK"],
  "accountability_relevance": "1 sentence: why this matters for following money/power",
  "priority_tier": "1 (high) | 2 | 3",
  "load_mode": "snapshot | incremental | chunked -- DEFAULT snapshot. incremental = huge / daily-growing sources (millions of rows, a date-bounded search API) where re-mirroring everything each run is wrong. chunked = a SINGLE very large bulk FILE (hundreds of MB to multiple GB -- e.g. NPPES ~9GB, a national CSV/ZIP) that won't fit in memory: the fetch STREAMS it and yields row-batches. Pick chunked when est_volume/format implies a multi-GB download; cursor_field/primary_key are not needed for it",
  "cursor_field": "incremental only: the sortable column to advance on (a date/timestamp, e.g. date_received); else \"\"",
  "primary_key": "incremental only: the unique record id used to dedupe in staging (e.g. complaint_id); else \"\"",
  "schema_fields": [{"name": "col", "type": "TEXT|INT|FLOAT|DATE|...", "description": "..."}],
  "entity": "snake_case name for the primary table, e.g. earthquakes",
  "domain": "dbt mart domain, e.g. economics, health, seismic",
  "joins_to": [{"source": "other_source_id", "on": "SHARED_KEY"}],
  "notes": "caveats, gotchas, snapshot-vs-append behaviour"
}

Be honest about uncertainty -- use "unknown" rather than inventing values.
Prefer a documented bulk download over scraping when both exist.
Note: the raw landing table stores every column as TEXT (an exact mirror);
type casting happens later in dbt staging.
