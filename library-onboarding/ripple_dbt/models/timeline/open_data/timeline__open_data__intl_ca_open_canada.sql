{{ config(materialized='view', schema='TIMELINE', alias='OPEN_DATA__INTL_CA_OPEN_CANADA') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for OPEN_DATA.OPEN_DATA__INTL_CA_OPEN_CANADA.
--   column : METADATA_MODIFIED   (TEXT)
--   means  : reported -- Uncast TEXT — when the portal last edited the catalog record; record maintenance by the publisher, always later than creation.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_date('src."METADATA_MODIFIED"')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'OPEN_DATA.OPEN_DATA__INTL_CA_OPEN_CANADA'::varchar as ripple_source,
    src.*
from {{ ref('open_data__intl_ca_open_canada') }} as src
