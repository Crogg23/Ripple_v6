{{ config(materialized='view', schema='TIMELINE', alias='CORPORATE_REGISTRY__UK_COMPANIES_HOUSE_PSC') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for CORPORATE_REGISTRY.CORPORATE_REGISTRY__UK_COMPANIES_HOUSE_PSC.
--   column : DOB_YEAR   (NUMBER)
--   means  : happened -- try_to_number(DOB_YEAR) - the beneficial owner's birth year, the only clock among the two columns offered; note the staging model also carries notified_on and c
--   grain  : year
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."DOB_YEAR"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_year('src."DOB_YEAR"'), 'happened') }} as ripple_clock,
    'CORPORATE_REGISTRY.CORPORATE_REGISTRY__UK_COMPANIES_HOUSE_PSC'::varchar as ripple_source,
    src.*
from {{ ref('corporate_registry__uk_companies_house_psc') }} as src
