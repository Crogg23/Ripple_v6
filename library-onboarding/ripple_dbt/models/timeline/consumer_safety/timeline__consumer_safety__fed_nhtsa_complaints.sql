{{ config(materialized='view', schema='TIMELINE', alias='CONSUMER_SAFETY__FED_NHTSA_COMPLAINTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for CONSUMER_SAFETY.CONSUMER_SAFETY__FED_NHTSA_COMPLAINTS.
--   column : FAIL_DATE   (DATE)
--   means  : happened -- try_to_date(C8,'YYYYMMDD') - the day the component failed or the incident occurred; the year-0003 minimum is source typos, not the cast.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."FAIL_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'CONSUMER_SAFETY.CONSUMER_SAFETY__FED_NHTSA_COMPLAINTS'::varchar as ripple_source,
    src.*
from {{ ref('consumer_safety__fed_nhtsa_complaints') }} as src
