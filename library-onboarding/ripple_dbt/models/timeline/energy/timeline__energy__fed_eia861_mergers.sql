{{ config(materialized='view', schema='TIMELINE', alias='ENERGY__FED_EIA861_MERGERS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENERGY.ENERGY__FED_EIA861_MERGERS.
--   column : EFFECTIVE_DATE   (DATE)
--   means  : happened -- try_to_date(trim(C_03_01_2024),'MM/DD/YYYY') - the day the utility merger/acquisition took legal effect; a real corporate event at day grain, and the only true 
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."EFFECTIVE_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ENERGY.ENERGY__FED_EIA861_MERGERS'::varchar as ripple_source,
    src.*
from {{ ref('energy__fed_eia861_mergers') }} as src
