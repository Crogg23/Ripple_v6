{{ config(materialized='view', schema='TIMELINE', alias='ENERGY__FED_EIA861_DYNAMIC_PRICING') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENERGY.ENERGY__FED_EIA861_DYNAMIC_PRICING.
--   column : DATA_YEAR   (NUMBER)
--   means  : happened -- try_to_number(nullif(trim(UNNAMED_0),'.')) - NUMBER year recovered positionally; the 2024 program year the enrollment counts describe. Only clock on the table.
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."DATA_YEAR"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ENERGY.ENERGY__FED_EIA861_DYNAMIC_PRICING'::varchar as ripple_source,
    src.*
from {{ ref('energy__fed_eia861_dynamic_pricing') }} as src
