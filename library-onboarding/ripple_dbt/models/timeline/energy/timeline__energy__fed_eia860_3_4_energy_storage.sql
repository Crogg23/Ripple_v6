{{ config(materialized='view', schema='TIMELINE', alias='ENERGY__FED_EIA860_3_4_ENERGY_STORAGE') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENERGY.ENERGY__FED_EIA860_3_4_ENERGY_STORAGE.
--   column : OPERATING_YEAR   (NUMBER)
--   means  : happened -- try_to_number(trim(OPERATING_YEAR)) - the year the battery/storage unit entered commercial service; the only clock on this 2024-snapshot table.
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."OPERATING_YEAR"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ENERGY.ENERGY__FED_EIA860_3_4_ENERGY_STORAGE'::varchar as ripple_source,
    src.*
from {{ ref('energy__fed_eia860_3_4_energy_storage') }} as src
