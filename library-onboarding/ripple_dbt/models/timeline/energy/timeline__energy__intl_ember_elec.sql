{{ config(materialized='view', schema='TIMELINE', alias='ENERGY__INTL_EMBER_ELEC') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENERGY.ENERGY__INTL_EMBER_ELEC.
--   column : DATE   (DATE)
--   means  : happened -- Staging casts try_to_date(trim(DATE),'YYYY'), so every value is a bare year snapped to Jan-1; real resolution is YEAR not day, and the census range 2000-01-01..
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ENERGY.ENERGY__INTL_EMBER_ELEC'::varchar as ripple_source,
    src.*
from {{ ref('energy__intl_ember_elec') }} as src
