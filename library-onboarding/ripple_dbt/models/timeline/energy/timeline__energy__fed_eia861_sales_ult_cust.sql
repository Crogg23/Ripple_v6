{{ config(materialized='view', schema='TIMELINE', alias='ENERGY__FED_EIA861_SALES_ULT_CUST') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENERGY.ENERGY__FED_EIA861_SALES_ULT_CUST.
--   column : DATA_YEAR   (NUMBER)
--   means  : happened -- try_to_number(nullif(trim(UNNAMED_0),'.')) - NUMBER year recovered positionally; the 2024 year the sales and revenue occurred in.
--   grain  : year
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."DATA_YEAR"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_year('src."DATA_YEAR"'), 'happened') }} as ripple_clock,
    'ENERGY.ENERGY__FED_EIA861_SALES_ULT_CUST'::varchar as ripple_source,
    src.*
from {{ ref('energy__fed_eia861_sales_ult_cust') }} as src
