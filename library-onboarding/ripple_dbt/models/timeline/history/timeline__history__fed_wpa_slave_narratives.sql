{{ config(materialized='view', schema='TIMELINE', alias='HISTORY__FED_WPA_SLAVE_NARRATIVES') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HISTORY.HISTORY__FED_WPA_SLAVE_NARRATIVES.
--   column : INTERVIEW_DATE   (DATE)
--   means  : happened -- try_to_date(interview_date,'YYYY-MM-DD') in staging with an explicit format: when the WPA interview actually took place; census CLEAN with a 1937-01-01 minimum,
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."INTERVIEW_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."INTERVIEW_DATE"::timestamp_ntz'), 'happened') }} as ripple_clock,
    'HISTORY.HISTORY__FED_WPA_SLAVE_NARRATIVES'::varchar as ripple_source,
    src.*
from {{ ref('history__fed_wpa_slave_narratives') }} as src
