{{ config(materialized='view', schema='TIMELINE', alias='SCIENCE_RESEARCH__XC_RETRACTION_WATCH_DATABASE') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for SCIENCE_RESEARCH.SCIENCE_RESEARCH__XC_RETRACTION_WATCH_DATABASE.
--   column : ORIGINAL_PAPER_DATE   (DATE)
--   means  : happened -- Same explicit 'MM/DD/YYYY' cast: the original paper's publication day, the antecedent whose distance from retraction_date is the response lag; the 1753 floor is
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."ORIGINAL_PAPER_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'SCIENCE_RESEARCH.SCIENCE_RESEARCH__XC_RETRACTION_WATCH_DATABASE'::varchar as ripple_source,
    src.*
from {{ ref('science_research__xc_retraction_watch_database') }} as src
