{{ config(materialized='view', schema='TIMELINE', alias='SCIENCE_RESEARCH__FED_RETRACTION_WATCH') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for SCIENCE_RESEARCH.SCIENCE_RESEARCH__FED_RETRACTION_WATCH.
--   column : ORIGINAL_PAPER_DATE   (DATE)
--   means  : happened -- Same explicit 'MM/DD/YYYY' cast: the day the original paper was published, the antecedent event — the gap to retraction_date is the response lag, and genuinely 
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."ORIGINAL_PAPER_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'SCIENCE_RESEARCH.SCIENCE_RESEARCH__FED_RETRACTION_WATCH'::varchar as ripple_source,
    src.*
from {{ ref('science_research__fed_retraction_watch') }} as src
