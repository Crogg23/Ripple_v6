{{ config(materialized='view', schema='TIMELINE', alias='SCIENCE_RESEARCH__XC_BIORXIV_MEDRXIV') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for SCIENCE_RESEARCH.SCIENCE_RESEARCH__XC_BIORXIV_MEDRXIV.
--   column : PREPRINT_POSTED_DATE   (DATE)
--   means  : reported -- try_to_date on the API's 'date' field: the day the preprint was posted to bioRxiv/medRxiv, the row's own public-disclosure event and the first term in the mart'
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."PREPRINT_POSTED_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'SCIENCE_RESEARCH.SCIENCE_RESEARCH__XC_BIORXIV_MEDRXIV'::varchar as ripple_source,
    src.*
from {{ ref('science_research__xc_biorxiv_medrxiv') }} as src
