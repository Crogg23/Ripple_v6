{{ config(materialized='view', schema='TIMELINE', alias='SCIENCE_RESEARCH__FED_SBIR_STTR_AWARDS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for SCIENCE_RESEARCH.SCIENCE_RESEARCH__FED_SBIR_STTR_AWARDS.
--   column : AWARD_YEAR   (NUMBER)
--   means  : happened -- try_to_number on the award year, which the staging model also uses as part of the surrogate key: the year the award was made, at year grain.
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."AWARD_YEAR"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'SCIENCE_RESEARCH.SCIENCE_RESEARCH__FED_SBIR_STTR_AWARDS'::varchar as ripple_source,
    src.*
from {{ ref('science_research__fed_sbir_sttr_awards') }} as src
