{{ config(materialized='view', schema='TIMELINE', alias='SCIENCE_RESEARCH__XC_OSF_REGISTRATIONS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for SCIENCE_RESEARCH.SCIENCE_RESEARCH__XC_OSF_REGISTRATIONS.
--   column : DATE_REGISTERED   (TIMESTAMP_NTZ)
--   means  : reported -- try_to_timestamp_ntz on attributes.date_registered: the moment the preregistration was frozen and posted to the public registry — the event this row IS; note th
--   grain  : day
--
-- reported cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'reported'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."DATE_REGISTERED"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."DATE_REGISTERED"::timestamp_ntz'), 'reported') }} as ripple_clock,
    'SCIENCE_RESEARCH.SCIENCE_RESEARCH__XC_OSF_REGISTRATIONS'::varchar as ripple_source,
    src.*
from {{ ref('science_research__xc_osf_registrations') }} as src
