{{ config(materialized='view', schema='TIMELINE', alias='LABOR__FED_OSHA_ITA_CASE_DETAIL_2023') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for LABOR.LABOR__FED_OSHA_ITA_CASE_DETAIL_2023.
--   column : DATE_OF_INCIDENT   (DATE)
--   means  : happened -- try_to_date(DATE_OF_INCIDENT) in the mart: the day the injury or illness incident happened to a named worker - the true event clock for this case-grain table.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DATE_OF_INCIDENT"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'LABOR.LABOR__FED_OSHA_ITA_CASE_DETAIL_2023'::varchar as ripple_source,
    src.*
from {{ ref('labor__fed_osha_ita_case_detail_2023') }} as src
