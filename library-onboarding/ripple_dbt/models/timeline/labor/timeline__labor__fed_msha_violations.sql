{{ config(materialized='view', schema='TIMELINE', alias='LABOR__FED_MSHA_VIOLATIONS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for LABOR.LABOR__FED_MSHA_VIOLATIONS.
--   column : VIOLATION_OCCUR_DATE   (DATE)
--   means  : happened -- try_to_date(violation_occur_dt,'MM/DD/YYYY') in the mart with an explicit format: when the violating condition actually existed at the mine, which is earlier th
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."VIOLATION_OCCUR_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."VIOLATION_OCCUR_DATE"::timestamp_ntz'), 'happened') }} as ripple_clock,
    'LABOR.LABOR__FED_MSHA_VIOLATIONS'::varchar as ripple_source,
    src.*
from {{ ref('labor__fed_msha_violations') }} as src
