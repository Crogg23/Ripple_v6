{{ config(materialized='view', schema='TIMELINE', alias='LABOR__FED_OSHA_ITA_300A_SUMMARY_2024') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for LABOR.LABOR__FED_OSHA_ITA_300A_SUMMARY_2024.
--   column : YEAR_FILING_FOR   (TEXT)
--   means  : happened -- raw passthrough integer naming the recordkeeping year the 300A summary covers; the counts are that year's injuries, and the file's created_timestamp range (2025
--   grain  : year
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."YEAR_FILING_FOR"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_year('src."YEAR_FILING_FOR"'), 'happened') }} as ripple_clock,
    'LABOR.LABOR__FED_OSHA_ITA_300A_SUMMARY_2024'::varchar as ripple_source,
    src.*
from {{ ref('labor__fed_osha_ita_300a_summary_2024') }} as src
