{{ config(materialized='view', schema='TIMELINE', alias='LABOR__FED_MSHA_ACCIDENTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for LABOR.LABOR__FED_MSHA_ACCIDENTS.
--   column : ACCIDENT_DATE   (DATE)
--   means  : happened -- try_to_date(accident_dt,'MM/DD/YYYY') in the mart - an explicit format, no epoch risk - dating the moment the mining accident occurred; census graded CLEAN, 200
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."ACCIDENT_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'LABOR.LABOR__FED_MSHA_ACCIDENTS'::varchar as ripple_source,
    src.*
from {{ ref('labor__fed_msha_accidents') }} as src
