{{ config(materialized='view', schema='TIMELINE', alias='LABOR__FED_PBGC_TRUSTEED_PLANS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for LABOR.LABOR__FED_PBGC_TRUSTEED_PLANS.
--   column : DATE_OF_PLAN_TERMINATION   (DATE)
--   means  : happened -- try_to_date(left(trim(DATE_OF_PLAN_TERMINATION),10)) in staging: the day the pension plan terminated - the real-world failure this one-row-per-case table exists
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DATE_OF_PLAN_TERMINATION"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'LABOR.LABOR__FED_PBGC_TRUSTEED_PLANS'::varchar as ripple_source,
    src.*
from {{ ref('labor__fed_pbgc_trusteed_plans') }} as src
