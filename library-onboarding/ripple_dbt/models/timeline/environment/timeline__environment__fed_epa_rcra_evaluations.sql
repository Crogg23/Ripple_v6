{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_EPA_RCRA_EVALUATIONS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_EPA_RCRA_EVALUATIONS.
--   column : EVALUATION_START_DATE   (DATE)
--   means  : happened -- try_to_date(trim(EVALUATION_START_DATE)) - the day the RCRA compliance evaluation (inspection) was conducted. No end column exists, so it is the event date, not
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."EVALUATION_START_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_EPA_RCRA_EVALUATIONS'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_epa_rcra_evaluations') }} as src
