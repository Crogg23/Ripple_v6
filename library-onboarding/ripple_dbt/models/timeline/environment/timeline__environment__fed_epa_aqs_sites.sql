{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_EPA_AQS_SITES') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_EPA_AQS_SITES.
--   column : SITE_ESTABLISHED_DATE   (DATE)
--   means  : span_start -- Staging casts try_to_date(SITE_ESTABLISHED_DATE,'YYYY-MM-DD'); with site_closed_date it bounds the monitoring site's operating tenure, so it is the span start a
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."SITE_ESTABLISHED_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_EPA_AQS_SITES'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_epa_aqs_sites') }} as src
