{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_EPA_SUPERFUND_SITE_BOUNDARIES') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_EPA_SUPERFUND_SITE_BOUNDARIES.
--   column : ORIGINALLY_CREATED_AT   (TIMESTAMP_NTZ)
--   means  : reported -- Staging does to_timestamp_ntz(try_to_number(ORIGINAL_CREATION_DATE)/1000) - an epoch-MILLISECONDS string, decoded correctly here: when EPA first created the bou
--   grain  : day
--
-- reported cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'reported'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."ORIGINALLY_CREATED_AT"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."ORIGINALLY_CREATED_AT"::timestamp_ntz'), 'reported') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_EPA_SUPERFUND_SITE_BOUNDARIES'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_epa_superfund_site_boundaries') }} as src
