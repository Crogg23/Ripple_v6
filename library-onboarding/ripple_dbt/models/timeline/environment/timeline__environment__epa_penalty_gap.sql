{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__EPA_PENALTY_GAP') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__EPA_PENALTY_GAP.
--   column : DATE_LAST_INSPECTION   (DATE)
--   means  : happened -- try_to_date of ECHO FAC_DATE_LAST_INSPECTION: the day the most recent inspection actually took place at the facility.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DATE_LAST_INSPECTION"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__EPA_PENALTY_GAP'::varchar as ripple_source,
    src.*
from {{ ref('environment__epa_penalty_gap') }} as src
