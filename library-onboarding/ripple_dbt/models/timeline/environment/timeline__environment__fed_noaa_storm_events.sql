{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_NOAA_STORM_EVENTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_NOAA_STORM_EVENTS.
--   column : YEAR   (NUMBER)
--   means  : happened -- try_to_number(YEAR): the year the storm event occurred - a real but coarse duplicate of begin_date_time, so it is not the primary.
--   grain  : year
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."YEAR"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_year('src."YEAR"'), 'happened') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_NOAA_STORM_EVENTS'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_noaa_storm_events') }} as src
