{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_EPA_SDWA_SDWA_VIOLATIONS_ENFORCEMENT') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_EPA_SDWA_SDWA_VIOLATIONS_ENFORCEMENT.
--   column : PWS_DEACTIVATION_DATE   (DATE)
--   means  : happened -- try_to_date(PWS_DEACTIVATION_DATE): the day the water system shut down - denormalized from the system record onto every violation row, so it describes the syste
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."PWS_DEACTIVATION_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_EPA_SDWA_SDWA_VIOLATIONS_ENFORCEMENT'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_epa_sdwa_sdwa_violations_enforcement') }} as src
