{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_EPA_NPDES_NPDES_CS_VIOLATIONS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_EPA_NPDES_NPDES_CS_VIOLATIONS.
--   column : ACTUAL_DATE   (DATE)
--   means  : happened -- try_to_date(ACTUAL_DATE): the day the scheduled compliance event actually occurred; the violation is the gap between this and schedule_date.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."ACTUAL_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_EPA_NPDES_NPDES_CS_VIOLATIONS'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_epa_npdes_npdes_cs_violations') }} as src
