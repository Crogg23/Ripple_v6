{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_PHMSA_FLAGGED_INCIDENTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_PHMSA_FLAGGED_INCIDENTS.
--   column : INCIDENT_YEAR   (NUMBER)
--   means  : happened -- try_to_number(trim(IYEAR)): the year the pipeline incident occurred. A coarse integer duplicate of local_datetime.
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."INCIDENT_YEAR"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_PHMSA_FLAGGED_INCIDENTS'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_phmsa_flagged_incidents') }} as src
