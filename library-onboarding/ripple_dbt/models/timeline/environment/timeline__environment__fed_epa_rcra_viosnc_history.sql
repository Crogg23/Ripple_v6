{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_EPA_RCRA_VIOSNC_HISTORY') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_EPA_RCRA_VIOSNC_HISTORY.
--   column : YRMONTH   (TEXT)
--   means  : happened -- trim(YRMONTH) kept as text: the YYYYMM month whose violation / significant-noncompliance status the row reports. Month grain - do not date-parse.
--   grain  : month
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_yyyymm('src."YRMONTH"')) }} as ripple_ts,
    {{ ripple_grain('month') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_EPA_RCRA_VIOSNC_HISTORY'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_epa_rcra_viosnc_history') }} as src
