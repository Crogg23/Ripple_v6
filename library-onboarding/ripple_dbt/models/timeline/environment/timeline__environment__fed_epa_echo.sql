{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_EPA_ECHO') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_EPA_ECHO.
--   column : DATE_LAST_INSPECTION   (DATE)
--   means  : happened -- try_to_date(FAC_DATE_LAST_INSPECTION): the day the facility's most recent inspection occurred; the best whole-table anchor for a facility row.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DATE_LAST_INSPECTION"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_EPA_ECHO'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_epa_echo') }} as src
