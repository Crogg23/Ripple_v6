{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_EPA_SDWA_SDWA_SITE_VISITS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_EPA_SDWA_SDWA_SITE_VISITS.
--   column : VISIT_DATE   (DATE)
--   means  : happened -- try_to_date(VISIT_DATE): the day the sanitary survey / site visit actually took place. Census min 1900-01-01 with 32 epoch-1970 rows = a small pocket of junk, n
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."VISIT_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."VISIT_DATE"::timestamp_ntz'), 'happened') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_EPA_SDWA_SDWA_SITE_VISITS'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_epa_sdwa_sdwa_site_visits') }} as src
