{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_EPA_NPDES_NPDES_INFORMAL_ENFORCEMENT_ACTIONS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_EPA_NPDES_NPDES_INFORMAL_ENFORCEMENT_ACTIONS.
--   column : ACHIEVED_DATE   (DATE)
--   means  : decided -- try_to_date(ACHIEVED_DATE): the day the informal enforcement action was achieved by the agency. Census 0001-01-01 to 8202-06-10 with 3 far-future rows = source 
--   grain  : day
--
-- decided cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'decided'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."ACHIEVED_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."ACHIEVED_DATE"::timestamp_ntz'), 'decided') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_EPA_NPDES_NPDES_INFORMAL_ENFORCEMENT_ACTIONS'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_epa_npdes_npdes_informal_enforcement_actions') }} as src
