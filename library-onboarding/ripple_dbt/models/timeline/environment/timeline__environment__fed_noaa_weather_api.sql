{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_NOAA_WEATHER_API') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_NOAA_WEATHER_API.
--   column : EFFECTIVE   (TEXT)
--   means  : span_start -- Raw passthrough of EFFECTIVE (no cast): the moment a National Weather Service alert takes effect, paired with expires. Chosen as primary because it is alert-sid
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_date('src."EFFECTIVE"') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_NOAA_WEATHER_API'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_noaa_weather_api') }} as src
