{{ config(materialized='view', schema='TIMELINE', alias='ENVIRONMENT__FED_EPA_RCRA_ENFORCEMENTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ENVIRONMENT.ENVIRONMENT__FED_EPA_RCRA_ENFORCEMENTS.
--   column : ENFORCEMENT_ACTION_DATE   (DATE)
--   means  : decided -- Staging casts try_to_date(trim(ENFORCEMENT_ACTION_DATE)) and it is part of the record key: the day the hazardous-waste enforcement action was taken. Census min 
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."ENFORCEMENT_ACTION_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('decided') }} as ripple_clock,
    'ENVIRONMENT.ENVIRONMENT__FED_EPA_RCRA_ENFORCEMENTS'::varchar as ripple_source,
    src.*
from {{ ref('environment__fed_epa_rcra_enforcements') }} as src
