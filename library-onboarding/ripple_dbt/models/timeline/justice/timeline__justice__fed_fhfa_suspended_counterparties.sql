{{ config(materialized='view', schema='TIMELINE', alias='JUSTICE__FED_FHFA_SUSPENDED_COUNTERPARTIES') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for JUSTICE.JUSTICE__FED_FHFA_SUSPENDED_COUNTERPARTIES.
--   column : EFFECTIVE_DATE   (DATE)
--   means  : span_start -- try_to_date(EFFECTIVE_DATESORT_ASCENDING): the day the suspension order takes effect -- the opening bound of the ban period.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."EFFECTIVE_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'JUSTICE.JUSTICE__FED_FHFA_SUSPENDED_COUNTERPARTIES'::varchar as ripple_source,
    src.*
from {{ ref('justice__fed_fhfa_suspended_counterparties') }} as src
