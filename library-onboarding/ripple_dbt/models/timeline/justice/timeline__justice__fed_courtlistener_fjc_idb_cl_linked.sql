{{ config(materialized='view', schema='TIMELINE', alias='JUSTICE__FED_COURTLISTENER_FJC_IDB_CL_LINKED') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for JUSTICE.JUSTICE__FED_COURTLISTENER_FJC_IDB_CL_LINKED.
--   column : DATE_FILED   (DATE)
--   means  : reported -- try_to_date(DATE_FILED): when the case was filed with the district court -- the only date populated on every linked IDB record, so it is the timeline anchor.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DATE_FILED"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'JUSTICE.JUSTICE__FED_COURTLISTENER_FJC_IDB_CL_LINKED'::varchar as ripple_source,
    src.*
from {{ ref('justice__fed_courtlistener_fjc_idb_cl_linked') }} as src
