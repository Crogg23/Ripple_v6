{{ config(materialized='view', schema='TIMELINE', alias='JUSTICE__FED_COURTLISTENER_FINANCIAL_DISCLOSURES') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for JUSTICE.JUSTICE__FED_COURTLISTENER_FINANCIAL_DISCLOSURES.
--   column : YEAR_COL   (NUMBER)
--   means  : span_start -- try_to_number("YEAR") -- the calendar year the disclosure report covers, so it opens a one-year reporting period. Stored as a NUMBER: a bare date-parse would re
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."YEAR_COL"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'JUSTICE.JUSTICE__FED_COURTLISTENER_FINANCIAL_DISCLOSURES'::varchar as ripple_source,
    src.*
from {{ ref('justice__fed_courtlistener_financial_disclosures') }} as src
