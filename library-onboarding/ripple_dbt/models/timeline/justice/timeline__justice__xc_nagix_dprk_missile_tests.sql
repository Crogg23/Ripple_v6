{{ config(materialized='view', schema='TIMELINE', alias='JUSTICE__XC_NAGIX_DPRK_MISSILE_TESTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for JUSTICE.JUSTICE__XC_NAGIX_DPRK_MISSILE_TESTS.
--   column : DATE   (TEXT)
--   means  : happened -- "DATE" raw text passthrough on a missile-test row -- the day of the launch. Real event clock, but uncast text.
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_date('src."DATE"') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_date('src."DATE"'), 'happened') }} as ripple_clock,
    'JUSTICE.JUSTICE__XC_NAGIX_DPRK_MISSILE_TESTS'::varchar as ripple_source,
    src.*
from {{ ref('justice__xc_nagix_dprk_missile_tests') }} as src
