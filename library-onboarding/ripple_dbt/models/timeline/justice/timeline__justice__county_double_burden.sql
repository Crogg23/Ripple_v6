{{ config(materialized='view', schema='TIMELINE', alias='JUSTICE__COUNTY_DOUBLE_BURDEN') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for JUSTICE.JUSTICE__COUNTY_DOUBLE_BURDEN.
--   column : JAIL_RATE_YEAR   (NUMBER)
--   means  : happened -- The compiled mart selects j.jail_year (Vera county-year panel year) as jail_rate_year -- the year the jail-rate measurement refers to.
--   grain  : year
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."JAIL_RATE_YEAR"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_year('src."JAIL_RATE_YEAR"'), 'happened') }} as ripple_clock,
    'JUSTICE.JUSTICE__COUNTY_DOUBLE_BURDEN'::varchar as ripple_source,
    src.*
from {{ ref('justice__county_double_burden') }} as src
