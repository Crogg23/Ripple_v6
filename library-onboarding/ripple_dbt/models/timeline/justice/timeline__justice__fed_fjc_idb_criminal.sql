{{ config(materialized='view', schema='TIMELINE', alias='JUSTICE__FED_FJC_IDB_CRIMINAL') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for JUSTICE.JUSTICE__FED_FJC_IDB_CRIMINAL.
--   column : PROCEEDING_DATE   (DATE)
--   means  : happened -- try_to_date(trim(PROCDATE)), paired with PROCCD naming which proceeding: the day that proceeding took place. Shares the format-string latent risk flagged in the
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."PROCEEDING_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."PROCEEDING_DATE"::timestamp_ntz'), 'happened') }} as ripple_clock,
    'JUSTICE.JUSTICE__FED_FJC_IDB_CRIMINAL'::varchar as ripple_source,
    src.*
from {{ ref('justice__fed_fjc_idb_criminal') }} as src
