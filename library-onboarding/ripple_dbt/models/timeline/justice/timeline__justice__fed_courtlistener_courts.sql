{{ config(materialized='view', schema='TIMELINE', alias='JUSTICE__FED_COURTLISTENER_COURTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for JUSTICE.JUSTICE__FED_COURTLISTENER_COURTS.
--   column : START_DATE   (DATE)
--   means  : span_start -- try_to_date(START_DATE) on a court record: the date the court was established, paired with END_DATE (abolished) -- a period, not a point. The census's 1200-01-0
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."START_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'JUSTICE.JUSTICE__FED_COURTLISTENER_COURTS'::varchar as ripple_source,
    src.*
from {{ ref('justice__fed_courtlistener_courts') }} as src
