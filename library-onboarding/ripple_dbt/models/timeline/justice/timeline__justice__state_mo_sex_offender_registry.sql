{{ config(materialized='view', schema='TIMELINE', alias='JUSTICE__STATE_MO_SEX_OFFENDER_REGISTRY') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for JUSTICE.JUSTICE__STATE_MO_SEX_OFFENDER_REGISTRY.
--   column : DATE_OF_BIRTH   (DATE)
--   means  : happened -- Staging: try_to_date(nullif(trim(DATE_OF_BIRTH),'')) with a header note that the source is an ISO timestamp string -- the registrant's birth date. It is the onl
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."DATE_OF_BIRTH"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."DATE_OF_BIRTH"::timestamp_ntz'), 'happened') }} as ripple_clock,
    'JUSTICE.JUSTICE__STATE_MO_SEX_OFFENDER_REGISTRY'::varchar as ripple_source,
    src.*
from {{ ref('justice__state_mo_sex_offender_registry') }} as src
