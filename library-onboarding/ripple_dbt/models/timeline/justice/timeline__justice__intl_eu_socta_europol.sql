{{ config(materialized='view', schema='TIMELINE', alias='JUSTICE__INTL_EU_SOCTA_EUROPOL') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for JUSTICE.JUSTICE__INTL_EU_SOCTA_EUROPOL.
--   column : PUBLISH_DATE   (DATE)
--   means  : reported -- try_to_date(PUBLISH_DATE) on a row that IS a published report -- the day Europol published it.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."PUBLISH_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'JUSTICE.JUSTICE__INTL_EU_SOCTA_EUROPOL'::varchar as ripple_source,
    src.*
from {{ ref('justice__intl_eu_socta_europol') }} as src
