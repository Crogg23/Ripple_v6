{{ config(materialized='view', schema='TIMELINE', alias='JUSTICE__XC_WAPO_FATAL_FORCE') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for JUSTICE.JUSTICE__XC_WAPO_FATAL_FORCE.
--   column : INCIDENT_DATE   (DATE)
--   means  : happened -- try_to_date(date,'YYYY-MM-DD') with an explicit format -- the day of the fatal police encounter.
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."INCIDENT_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."INCIDENT_DATE"::timestamp_ntz'), 'happened') }} as ripple_clock,
    'JUSTICE.JUSTICE__XC_WAPO_FATAL_FORCE'::varchar as ripple_source,
    src.*
from {{ ref('justice__xc_wapo_fatal_force') }} as src
