{{ config(materialized='view', schema='TIMELINE', alias='JUSTICE__FED_COURTLISTENER_JUDGE_POLITICAL_AFFILIATIONS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for JUSTICE.JUSTICE__FED_COURTLISTENER_JUDGE_POLITICAL_AFFILIATIONS.
--   column : DATE_START   (DATE)
--   means  : span_start -- try_to_date(DATE_START): when the judge's affiliation with that party began -- paired with DATE_END it is a tenure span.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."DATE_START"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'JUSTICE.JUSTICE__FED_COURTLISTENER_JUDGE_POLITICAL_AFFILIATIONS'::varchar as ripple_source,
    src.*
from {{ ref('justice__fed_courtlistener_judge_political_affiliations') }} as src
