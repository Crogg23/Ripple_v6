{{ config(materialized='view', schema='TIMELINE', alias='JUSTICE__INTL_OPENSANCTIONS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for JUSTICE.JUSTICE__INTL_OPENSANCTIONS.
--   column : BIRTH_DATE   (DATE)
--   means  : happened -- The mart's guarded CASE only lets exact 'YYYY-MM-DD', non-future values become dates -- the sanctioned person's date of birth. The only real-world clock among t
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."BIRTH_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'JUSTICE.JUSTICE__INTL_OPENSANCTIONS'::varchar as ripple_source,
    src.*
from {{ ref('justice__intl_opensanctions') }} as src
