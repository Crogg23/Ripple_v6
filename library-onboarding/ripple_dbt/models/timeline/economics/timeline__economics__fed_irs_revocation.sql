{{ config(materialized='view', schema='TIMELINE', alias='ECONOMICS__FED_IRS_REVOCATION') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ECONOMICS.ECONOMICS__FED_IRS_REVOCATION.
--   column : REVOCATION_POSTING_DATE   (TEXT)
--   means  : reported -- trim() of an unparsed TEXT column; when the IRS published the revocation to the list.
--   grain  : day
--
-- reported cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'reported'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_date('src."REVOCATION_POSTING_DATE"') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_date('src."REVOCATION_POSTING_DATE"'), 'reported') }} as ripple_clock,
    'ECONOMICS.ECONOMICS__FED_IRS_REVOCATION'::varchar as ripple_source,
    src.*
from {{ ref('economics__fed_irs_revocation') }} as src
