{{ config(materialized='view', schema='TIMELINE', alias='ECONOMICS__FED_FOREIGNASSISTANCE') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ECONOMICS.ECONOMICS__FED_FOREIGNASSISTANCE.
--   column : FISCAL_YEAR   (TEXT)
--   means  : happened -- Raw passthrough of FISCAL_YEAR and the only time-shaped column; it is the fiscal year the assistance transaction occurred in, so it places the row at year resol
--   grain  : year
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."FISCAL_YEAR"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_year('src."FISCAL_YEAR"'), 'happened') }} as ripple_clock,
    'ECONOMICS.ECONOMICS__FED_FOREIGNASSISTANCE'::varchar as ripple_source,
    src.*
from {{ ref('economics__fed_foreignassistance') }} as src
