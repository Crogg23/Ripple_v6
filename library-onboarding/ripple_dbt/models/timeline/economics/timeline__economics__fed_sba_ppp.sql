{{ config(materialized='view', schema='TIMELINE', alias='ECONOMICS__FED_SBA_PPP') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ECONOMICS.ECONOMICS__FED_SBA_PPP.
--   column : DATE_APPROVED   (DATE)
--   means  : decided -- try_to_date(DATEAPPROVED); SBA approval of the PPP loan, present on every row and used as the dedup ordering key in the model, and it matches the census window 
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DATE_APPROVED"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('decided') }} as ripple_clock,
    'ECONOMICS.ECONOMICS__FED_SBA_PPP'::varchar as ripple_source,
    src.*
from {{ ref('economics__fed_sba_ppp') }} as src
