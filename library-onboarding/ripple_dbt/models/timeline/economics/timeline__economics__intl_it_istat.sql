{{ config(materialized='view', schema='TIMELINE', alias='ECONOMICS__INTL_IT_ISTAT') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ECONOMICS.ECONOMICS__INTL_IT_ISTAT.
--   column : DATE   (DATE)
--   means  : happened -- Staging builds it with a regex-guarded parse: 'YYYY' rows become Jan-1 of that year and 'YYYY-MM' rows become the 1st of that month, so it is the observation-pe
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ECONOMICS.ECONOMICS__INTL_IT_ISTAT'::varchar as ripple_source,
    src.*
from {{ ref('economics__intl_it_istat') }} as src
