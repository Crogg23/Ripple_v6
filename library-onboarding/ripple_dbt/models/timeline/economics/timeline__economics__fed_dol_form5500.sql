{{ config(materialized='view', schema='TIMELINE', alias='ECONOMICS__FED_DOL_FORM5500') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ECONOMICS.ECONOMICS__FED_DOL_FORM5500.
--   column : PLAN_EFF_DATE   (DATE)
--   means  : happened -- try_to_date(PLAN_EFF_DATE) is when the benefit plan came into existence, a real-world event, but it predates the filing by decades so it must not be used to pla
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."PLAN_EFF_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'ECONOMICS.ECONOMICS__FED_DOL_FORM5500'::varchar as ripple_source,
    src.*
from {{ ref('economics__fed_dol_form5500') }} as src
