{{ config(materialized='view', schema='TIMELINE', alias='ECONOMICS__FED_SEC_EDGAR') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ECONOMICS.ECONOMICS__FED_SEC_EDGAR.
--   column : REPORTDATE   (TEXT)
--   means  : span_end -- Raw TEXT passthrough of the data.sec.gov 'reportDate', the period the filing reports on (period end), and the only usable clock in the candidate list; note the 
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_date('src."REPORTDATE"') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_end') }} as ripple_clock,
    'ECONOMICS.ECONOMICS__FED_SEC_EDGAR'::varchar as ripple_source,
    src.*
from {{ ref('economics__fed_sec_edgar') }} as src
