{{ config(materialized='view', schema='TIMELINE', alias='ECONOMICS__INTL_GLEIF_RELATIONSHIPS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ECONOMICS.ECONOMICS__INTL_GLEIF_RELATIONSHIPS.
--   column : RELATIONSHIP_PERIOD_1_STARTDATE   (TEXT)
--   means  : span_start -- Opening bound of period slot 1 on a parent/child relationship row and the best available anchor for when a corporate ownership link began. TRAP: the slots are a
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_date('src."RELATIONSHIP_PERIOD_1_STARTDATE"') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'ECONOMICS.ECONOMICS__INTL_GLEIF_RELATIONSHIPS'::varchar as ripple_source,
    src.*
from {{ ref('economics__intl_gleif_relationships') }} as src
