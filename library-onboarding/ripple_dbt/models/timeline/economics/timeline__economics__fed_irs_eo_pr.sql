{{ config(materialized='view', schema='TIMELINE', alias='ECONOMICS__FED_IRS_EO_PR') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ECONOMICS.ECONOMICS__FED_IRS_EO_PR.
--   column : TAX_PERIOD   (TEXT)
--   means  : span_end -- Raw TEXT passthrough of the BMF YYYYMM period-ending code; it is the only candidate on this Puerto Rico exempt-org extract, so it is the primary by default even
--   grain  : month
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_yyyymm('src."TAX_PERIOD"') }} as ripple_ts,
    {{ ripple_grain('month') }} as ripple_grain,
    {{ ripple_clock('span_end') }} as ripple_clock,
    'ECONOMICS.ECONOMICS__FED_IRS_EO_PR'::varchar as ripple_source,
    src.*
from {{ ref('economics__fed_irs_eo_pr') }} as src
