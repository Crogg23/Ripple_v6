{{ config(materialized='view', schema='TIMELINE', alias='ECONOMICS__FED_FAC_SINGLE_AUDIT') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ECONOMICS.ECONOMICS__FED_FAC_SINGLE_AUDIT.
--   column : FAC_ACCEPTED_DATE   (DATE)
--   means  : reported -- try_to_date(FAC_ACCEPTED_DATE) is when the Federal Audit Clearinghouse accepted the submission; it dates the filing event, is present on essentially every row, 
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."FAC_ACCEPTED_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'ECONOMICS.ECONOMICS__FED_FAC_SINGLE_AUDIT'::varchar as ripple_source,
    src.*
from {{ ref('economics__fed_fac_single_audit') }} as src
