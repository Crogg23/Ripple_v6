{{ config(materialized='view', schema='TIMELINE', alias='ECONOMICS__FED_IRS_AUTO_REVOCATIONS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for ECONOMICS.ECONOMICS__FED_IRS_AUTO_REVOCATIONS.
--   column : REVOCATION_POSTING_DATE   (DATE)
--   means  : reported -- try_to_date(REVOCATION_POSTING_DATE); when the IRS published the revocation to the public list, always later than the revocation itself — the gap is measurable 
--   grain  : day
--
-- reported cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'reported'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."REVOCATION_POSTING_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."REVOCATION_POSTING_DATE"::timestamp_ntz'), 'reported') }} as ripple_clock,
    'ECONOMICS.ECONOMICS__FED_IRS_AUTO_REVOCATIONS'::varchar as ripple_source,
    src.*
from {{ ref('economics__fed_irs_auto_revocations') }} as src
