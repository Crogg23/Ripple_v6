{{ config(materialized='view', schema='TIMELINE', alias='REFERENCE__FED_ITIS_OTHER_SOURCES') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for REFERENCE.REFERENCE__FED_ITIS_OTHER_SOURCES.
--   column : ACQUISITION_DATE   (DATE)
--   means  : happened -- try_to_date(trim(ACQUISITION_DATE),'YYYY-MM-DD') in staging: when ITIS acquired the cited database or website - a real acquisition event on a row that IS a sour
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."ACQUISITION_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'REFERENCE.REFERENCE__FED_ITIS_OTHER_SOURCES'::varchar as ripple_source,
    src.*
from {{ ref('reference__fed_itis_other_sources') }} as src
