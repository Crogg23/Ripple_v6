{{ config(materialized='view', schema='TIMELINE', alias='REFERENCE__FED_ITIS_TAXONOMIC_UNITS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for REFERENCE.REFERENCE__FED_ITIS_TAXONOMIC_UNITS.
--   column : INITIAL_TIME_STAMP   (TIMESTAMP_NTZ)
--   means  : reported -- try_to_timestamp_ntz(trim(INITIAL_TIME_STAMP)) in staging: when ITIS first recorded this taxon; the taxon itself long predates the record, so this is the regist
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."INITIAL_TIME_STAMP"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'REFERENCE.REFERENCE__FED_ITIS_TAXONOMIC_UNITS'::varchar as ripple_source,
    src.*
from {{ ref('reference__fed_itis_taxonomic_units') }} as src
