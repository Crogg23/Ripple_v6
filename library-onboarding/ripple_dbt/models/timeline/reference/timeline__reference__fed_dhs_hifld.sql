{{ config(materialized='view', schema='TIMELINE', alias='REFERENCE__FED_DHS_HIFLD') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for REFERENCE.REFERENCE__FED_DHS_HIFLD.
--   column : SOURCE_DATE   (DATE)
--   means  : reported -- try_to_date(left(nullif(trim(SOURCE_DATE),''),10)) in staging: the date of the underlying source the facility record was compiled from, i.e. the vintage of the 
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."SOURCE_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'REFERENCE.REFERENCE__FED_DHS_HIFLD'::varchar as ripple_source,
    src.*
from {{ ref('reference__fed_dhs_hifld') }} as src
