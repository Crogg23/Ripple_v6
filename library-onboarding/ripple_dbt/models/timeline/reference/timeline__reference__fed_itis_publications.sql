{{ config(materialized='view', schema='TIMELINE', alias='REFERENCE__FED_ITIS_PUBLICATIONS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for REFERENCE.REFERENCE__FED_ITIS_PUBLICATIONS.
--   column : ACTUAL_PUB_DATE   (DATE)
--   means  : happened -- try_to_date(trim(ACTUAL_PUB_DATE),'YYYY-MM-DD') in staging: the real publication date of the cited work - on a bibliographic table the publication IS the event,
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."ACTUAL_PUB_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'REFERENCE.REFERENCE__FED_ITIS_PUBLICATIONS'::varchar as ripple_source,
    src.*
from {{ ref('reference__fed_itis_publications') }} as src
