{{ config(materialized='view', schema='TIMELINE', alias='REFERENCE__FED_ITIS_TU_COMMENTS_LINKS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for REFERENCE.REFERENCE__FED_ITIS_TU_COMMENTS_LINKS.
--   column : UPDATE_DATE   (DATE)
--   means  : reported -- the ITIS UPDATE_DATE pattern on the taxon-to-comment bridge: the curator edit date, the table's only clock.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."UPDATE_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'REFERENCE.REFERENCE__FED_ITIS_TU_COMMENTS_LINKS'::varchar as ripple_source,
    src.*
from {{ ref('reference__fed_itis_tu_comments_links') }} as src
