{{ config(materialized='view', schema='TIMELINE', alias='REFERENCE__XC_OWID_FERTILITY') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for REFERENCE.REFERENCE__XC_OWID_FERTILITY.
--   column : YEAR   (NUMBER)
--   means  : happened -- try_to_number("YEAR") in the mart: the year the fertility rate describes, one row per entity-year - an integer, not a date, so a bare date-parse would collapse 
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."YEAR"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'REFERENCE.REFERENCE__XC_OWID_FERTILITY'::varchar as ripple_source,
    src.*
from {{ ref('reference__xc_owid_fertility') }} as src
