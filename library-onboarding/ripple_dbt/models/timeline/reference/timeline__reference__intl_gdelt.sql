{{ config(materialized='view', schema='TIMELINE', alias='REFERENCE__INTL_GDELT') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for REFERENCE.REFERENCE__INTL_GDELT.
--   column : SQLDATE   (TEXT)
--   means  : happened -- raw passthrough with no cast: GDELT's event date in YYYYMMDD form; it is the true event clock and the finest one here, but it is NOT a date type - parsing it ne
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_date('src."SQLDATE"')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'REFERENCE.REFERENCE__INTL_GDELT'::varchar as ripple_source,
    src.*
from {{ ref('reference__intl_gdelt') }} as src
