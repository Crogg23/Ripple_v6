{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__FED_CONGRESS_LEGISLATORS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__FED_CONGRESS_LEGISLATORS.
--   column : TERM_END   (TEXT)
--   means  : span_end -- End of the person's service span (staging names it last_term_end); for sitting members this is a scheduled future date, so far-future values here are legitimate
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_date('src."TERM_END"') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_end') }} as ripple_clock,
    'POLITICS.POLITICS__FED_CONGRESS_LEGISLATORS'::varchar as ripple_source,
    src.*
from {{ ref('politics__fed_congress_legislators') }} as src
