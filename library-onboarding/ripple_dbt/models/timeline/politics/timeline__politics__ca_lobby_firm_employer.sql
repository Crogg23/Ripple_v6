{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__CA_LOBBY_FIRM_EMPLOYER') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__CA_LOBBY_FIRM_EMPLOYER.
--   column : RPT_START   (DATE)
--   means  : span_start -- try_to_date(...,'MM/DD/YYYY') — opens the billing period the firm-employer line covers; the grain is literally firm-filing-employer-period, so this is the row's
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."RPT_START"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'POLITICS.POLITICS__CA_LOBBY_FIRM_EMPLOYER'::varchar as ripple_source,
    src.*
from {{ ref('politics__ca_lobby_firm_employer') }} as src
