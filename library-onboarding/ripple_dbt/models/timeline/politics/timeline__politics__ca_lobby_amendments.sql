{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__CA_LOBBY_AMENDMENTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__CA_LOBBY_AMENDMENTS.
--   column : EXEC_DATE   (DATE)
--   means  : reported -- try_to_date(split_part(EXEC_DATE,' ',1),'MM/DD/YYYY') — the date the Form 605 amendment was executed and submitted; the row's own filing event.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."EXEC_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'POLITICS.POLITICS__CA_LOBBY_AMENDMENTS'::varchar as ripple_source,
    src.*
from {{ ref('politics__ca_lobby_amendments') }} as src
