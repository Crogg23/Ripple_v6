{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__CA_LOBBY_FIRM') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__CA_LOBBY_FIRM.
--   column : SESSION_YR_1   (TEXT)
--   means  : span_start -- Left as trimmed text while every neighbouring amount got try_to_number(18,2) — the first calendar year of the two-year session, and the only real clock on this 
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."SESSION_YR_1"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'POLITICS.POLITICS__CA_LOBBY_FIRM'::varchar as ripple_source,
    src.*
from {{ ref('politics__ca_lobby_firm') }} as src
