{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__MEMBER_CROSSWALK') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__MEMBER_CROSSWALK.
--   column : FIRST_TERM_START   (TEXT)
--   means  : span_start -- Start of the member's congressional career on a one-row-per-member table - the only forward-looking anchor here; raw trimmed TEXT in staging, never cast to date
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_date('src."FIRST_TERM_START"') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'POLITICS.POLITICS__MEMBER_CROSSWALK'::varchar as ripple_source,
    src.*
from {{ ref('politics__member_crosswalk') }} as src
