{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__VOTEVIEW_ROLLCALLS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__VOTEVIEW_ROLLCALLS.
--   column : VOTE_DATE   (DATE)
--   means  : happened -- The day the roll-call vote was taken; staging parses it with an explicit try_to_date(...,'YYYY-MM-DD') after sampling the landed format on 2026-08-11.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."VOTE_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'POLITICS.POLITICS__VOTEVIEW_ROLLCALLS'::varchar as ripple_source,
    src.*
from {{ ref('politics__voteview_rollcalls') }} as src
