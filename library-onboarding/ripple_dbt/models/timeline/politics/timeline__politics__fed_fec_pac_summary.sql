{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__FED_FEC_PAC_SUMMARY') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__FED_FEC_PAC_SUMMARY.
--   column : COVERAGE_END_DATE   (DATE)
--   means  : span_end -- Last day of the committee's reporting period - cast try_to_date(trim(C27),'MM/DD/YYYY') in staging and part of the table's grain key, so it is the honest anchor
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."COVERAGE_END_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_end') }} as ripple_clock,
    'POLITICS.POLITICS__FED_FEC_PAC_SUMMARY'::varchar as ripple_source,
    src.*
from {{ ref('politics__fed_fec_pac_summary') }} as src
