{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__TX_LOBBY_TRANSPORTATION') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__TX_LOBBY_TRANSPORTATION.
--   column : APPLICABLEYEAR   (TEXT)
--   means  : span_start -- Trimmed text year the report applies to; a real year label, uncast.
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."APPLICABLEYEAR"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'POLITICS.POLITICS__TX_LOBBY_TRANSPORTATION'::varchar as ripple_source,
    src.*
from {{ ref('politics__tx_lobby_transportation') }} as src
