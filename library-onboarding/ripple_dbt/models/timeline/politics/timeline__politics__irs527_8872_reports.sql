{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__IRS527_8872_REPORTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__IRS527_8872_REPORTS.
--   column : ORG_FORMATION_DATE   (DATE)
--   means  : happened -- try_to_date(...,'YYYYMMDD') for when the organization was formed — a real event, but it dates the org, not this report row; the 1869 minimum and 123 epoch rows 
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."ORG_FORMATION_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."ORG_FORMATION_DATE"::timestamp_ntz'), 'happened') }} as ripple_clock,
    'POLITICS.POLITICS__IRS527_8872_REPORTS'::varchar as ripple_source,
    src.*
from {{ ref('politics__irs527_8872_reports') }} as src
