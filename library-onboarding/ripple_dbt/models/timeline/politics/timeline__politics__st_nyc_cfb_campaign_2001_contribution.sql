{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__ST_NYC_CFB_CAMPAIGN_2001_CONTRIBUTION') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__ST_NYC_CFB_CAMPAIGN_2001_CONTRIBUTION.
--   column : ELECTION_CYCLE   (TEXT)
--   means  : span_end -- trim(ELECTION) — the election year the contribution was raised for; the cycle closes in that year, and on this single-cycle table it is effectively constant.
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."ELECTION_CYCLE"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('span_end') }} as ripple_clock,
    'POLITICS.POLITICS__ST_NYC_CFB_CAMPAIGN_2001_CONTRIBUTION'::varchar as ripple_source,
    src.*
from {{ ref('politics__st_nyc_cfb_campaign_2001_contribution') }} as src
