{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__ST_NYC_CFB_CAMPAIGN_2009_CONTRIBUTION') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__ST_NYC_CFB_CAMPAIGN_2009_CONTRIBUTION.
--   column : CONTRIBUTION_DATE   (DATE)
--   means  : happened -- try_to_date(trim(DATE)) — when the contribution was made; the census's 2000-05-02 minimum shows the permissive parse does land real values here, unlike the 2001
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."CONTRIBUTION_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."CONTRIBUTION_DATE"::timestamp_ntz'), 'happened') }} as ripple_clock,
    'POLITICS.POLITICS__ST_NYC_CFB_CAMPAIGN_2009_CONTRIBUTION'::varchar as ripple_source,
    src.*
from {{ ref('politics__st_nyc_cfb_campaign_2009_contribution') }} as src
