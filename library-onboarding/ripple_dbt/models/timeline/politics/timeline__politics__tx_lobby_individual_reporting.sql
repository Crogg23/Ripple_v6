{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__TX_LOBBY_INDIVIDUAL_REPORTING') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__TX_LOBBY_INDIVIDUAL_REPORTING.
--   column : PERIODSTARTDT   (DATE)
--   means  : span_start -- try_to_date(...,'YYYYMMDD') — opens the reporting period the on-behalf-of line falls in; the finest clock on this table.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."PERIODSTARTDT"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'POLITICS.POLITICS__TX_LOBBY_INDIVIDUAL_REPORTING'::varchar as ripple_source,
    src.*
from {{ ref('politics__tx_lobby_individual_reporting') }} as src
