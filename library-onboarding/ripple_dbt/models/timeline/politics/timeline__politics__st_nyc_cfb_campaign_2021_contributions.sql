{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__ST_NYC_CFB_CAMPAIGN_2021_CONTRIBUTIONS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__ST_NYC_CFB_CAMPAIGN_2021_CONTRIBUTIONS.
--   column : CONTRIBUTION_DATE   (DATE)
--   means  : happened -- try_to_date(trim(DATE)) — when the contribution was made; census minimum 2011-03-11 shows real values land here.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."CONTRIBUTION_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'POLITICS.POLITICS__ST_NYC_CFB_CAMPAIGN_2021_CONTRIBUTIONS'::varchar as ripple_source,
    src.*
from {{ ref('politics__st_nyc_cfb_campaign_2021_contributions') }} as src
