{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__CA_LOBBY_COVER') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__CA_LOBBY_COVER.
--   column : RPT_DATE   (TIMESTAMP_NTZ)
--   means  : reported -- try_to_timestamp(RPT_DATE,'MM/DD/YYYY HH12:MI:SS AM') — the report date on the disclosure cover page; the time part is midnight padding so the real resolution i
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."RPT_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'POLITICS.POLITICS__CA_LOBBY_COVER'::varchar as ripple_source,
    src.*
from {{ ref('politics__ca_lobby_cover') }} as src
