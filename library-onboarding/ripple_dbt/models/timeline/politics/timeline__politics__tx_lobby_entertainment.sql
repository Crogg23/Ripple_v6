{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__TX_LOBBY_ENTERTAINMENT') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__TX_LOBBY_ENTERTAINMENT.
--   column : ACTIVITYDATE   (DATE)
--   means  : happened -- try_to_date(ACTIVITYDATE,'YYYYMMDD') — the day the entertainment was actually provided to the official; the true event clock.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."ACTIVITYDATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'POLITICS.POLITICS__TX_LOBBY_ENTERTAINMENT'::varchar as ripple_source,
    src.*
from {{ ref('politics__tx_lobby_entertainment') }} as src
