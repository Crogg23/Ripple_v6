{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__BILLS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__BILLS.
--   column : INTRODUCED_DATE   (DATE)
--   means  : happened -- The day the bill was introduced - the row's own birth event; try_to_date cast in staging and used as the dedup ordering key.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."INTRODUCED_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'POLITICS.POLITICS__BILLS'::varchar as ripple_source,
    src.*
from {{ ref('politics__bills') }} as src
