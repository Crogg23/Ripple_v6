{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__FEC_CANDIDATE_SUMMARY') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__FEC_CANDIDATE_SUMMARY.
--   column : COVERAGE_END_DATE   (TEXT)
--   means  : span_end -- Last day of the financial reporting period the dollar totals cover; HAZARD - staging leaves it as a trimmed MM/DD/YYYY TEXT string (no try_to_date), so any bare
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_date('src."COVERAGE_END_DATE"') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_end') }} as ripple_clock,
    'POLITICS.POLITICS__FEC_CANDIDATE_SUMMARY'::varchar as ripple_source,
    src.*
from {{ ref('politics__fec_candidate_summary') }} as src
