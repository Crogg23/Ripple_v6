{{ config(materialized='view', schema='TIMELINE', alias='POLITICS__XC_JCS_SCOTUS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for POLITICS.POLITICS__XC_JCS_SCOTUS.
--   column : TERM   (TEXT)
--   means  : happened -- Supreme Court term the justice's ideology score belongs to (782 rows = justice x term) - the only time-shaped column on the table; raw uncast passthrough year n
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."TERM"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'POLITICS.POLITICS__XC_JCS_SCOTUS'::varchar as ripple_source,
    src.*
from {{ ref('politics__xc_jcs_scotus') }} as src
