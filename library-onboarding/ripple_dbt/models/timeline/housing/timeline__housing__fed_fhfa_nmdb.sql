{{ config(materialized='view', schema='TIMELINE', alias='HOUSING__FED_FHFA_NMDB') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HOUSING.HOUSING__FED_FHFA_NMDB.
--   column : PERIOD_VALUE   (FLOAT)
--   means  : happened -- try_to_double(PERIOD_VALUE) - the period the mortgage statistic covers, but its resolution is defined by period_type on the same row, so grain is year or quarte
--   grain  : month
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_yyyymm('src."PERIOD_VALUE"') }} as ripple_ts,
    {{ ripple_grain('month') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_yyyymm('src."PERIOD_VALUE"'), 'happened') }} as ripple_clock,
    'HOUSING.HOUSING__FED_FHFA_NMDB'::varchar as ripple_source,
    src.*
from {{ ref('housing__fed_fhfa_nmdb') }} as src
