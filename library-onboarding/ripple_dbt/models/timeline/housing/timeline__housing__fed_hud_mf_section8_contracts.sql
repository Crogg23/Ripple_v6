{{ config(materialized='view', schema='TIMELINE', alias='HOUSING__FED_HUD_MF_SECTION8_CONTRACTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HOUSING.HOUSING__FED_HUD_MF_SECTION8_CONTRACTS.
--   column : TRACS_EFFECTIVE_DATE   (DATE)
--   means  : span_start -- try_to_date(left(trim(...),10),'YYYY-MM-DD') - the day the Section 8 contract took effect. The table has no happened/reported column, so the span start is the a
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."TRACS_EFFECTIVE_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'HOUSING.HOUSING__FED_HUD_MF_SECTION8_CONTRACTS'::varchar as ripple_source,
    src.*
from {{ ref('housing__fed_hud_mf_section8_contracts') }} as src
