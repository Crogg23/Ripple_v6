{{ config(materialized='view', schema='TIMELINE', alias='HOUSING__FED_CFPB_HMDA') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HOUSING.HOUSING__FED_CFPB_HMDA.
--   column : ACTIVITY_YEAR   (TEXT)
--   means  : happened -- nullif(trim(ACTIVITY_YEAR),'') keeps it as TEXT year; HMDA's activity year is the calendar year the action on the application was taken. This slice is a single 
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."ACTIVITY_YEAR"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'HOUSING.HOUSING__FED_CFPB_HMDA'::varchar as ripple_source,
    src.*
from {{ ref('housing__fed_cfpb_hmda') }} as src
