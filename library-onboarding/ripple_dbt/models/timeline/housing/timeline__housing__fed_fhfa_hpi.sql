{{ config(materialized='view', schema='TIMELINE', alias='HOUSING__FED_FHFA_HPI') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HOUSING.HOUSING__FED_FHFA_HPI.
--   column : YR   (TEXT)
--   means  : happened -- Raw YR passthrough from LANDING with no cast in the mart - the calendar year of the index observation. On its own it is year grain; only `period` plus `frequenc
--   grain  : year
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."YR"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_year('src."YR"'), 'happened') }} as ripple_clock,
    'HOUSING.HOUSING__FED_FHFA_HPI'::varchar as ripple_source,
    src.*
from {{ ref('housing__fed_fhfa_hpi') }} as src
