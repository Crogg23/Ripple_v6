{{ config(materialized='view', schema='TIMELINE', alias='HOUSING__FED_FEMA_IA_HOUSING_REGISTRATIONS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HOUSING.HOUSING__FED_FEMA_IA_HOUSING_REGISTRATIONS.
--   column : APPLIED_DATE   (DATE)
--   means  : reported -- try_to_date(trim(APPLIEDDATE)); schema.yml: 'Date the applicant registered for assistance' - the moment a person told FEMA. There is no incident/damage date on 
--   grain  : day
--
-- reported cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'reported'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."APPLIED_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."APPLIED_DATE"::timestamp_ntz'), 'reported') }} as ripple_clock,
    'HOUSING.HOUSING__FED_FEMA_IA_HOUSING_REGISTRATIONS'::varchar as ripple_source,
    src.*
from {{ ref('housing__fed_fema_ia_housing_registrations') }} as src
