{{ config(materialized='view', schema='TIMELINE', alias='TRANSPORT__FED_NTSB_AVIATION_EVENTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for TRANSPORT.TRANSPORT__FED_NTSB_AVIATION_EVENTS.
--   column : EV_DATE   (DATE)
--   means  : happened -- try_to_date on the left 10 characters of an ISO string: the date the aviation accident or incident occurred, one row per event, which is the table's true clock.
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."EV_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."EV_DATE"::timestamp_ntz'), 'happened') }} as ripple_clock,
    'TRANSPORT.TRANSPORT__FED_NTSB_AVIATION_EVENTS'::varchar as ripple_source,
    src.*
from {{ ref('transport__fed_ntsb_aviation_events') }} as src
