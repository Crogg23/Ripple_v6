{{ config(materialized='view', schema='TIMELINE', alias='TRANSPORT__FED_FRA_CASUALTIES') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for TRANSPORT.TRANSPORT__FED_FRA_CASUALTIES.
--   column : DATE   (DATE)
--   means  : happened -- try_to_date with an explicit 'MM/DD/YYYY' format — the full calendar date the casualty occurred, and the one column that puts one row (one reported casualty) on
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'TRANSPORT.TRANSPORT__FED_FRA_CASUALTIES'::varchar as ripple_source,
    src.*
from {{ ref('transport__fed_fra_casualties') }} as src
