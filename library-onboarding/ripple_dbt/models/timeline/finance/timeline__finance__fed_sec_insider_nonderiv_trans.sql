{{ config(materialized='view', schema='TIMELINE', alias='FINANCE__FED_SEC_INSIDER_NONDERIV_TRANS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for FINANCE.FINANCE__FED_SEC_INSIDER_NONDERIV_TRANS.
--   column : TRANSACTION_DATE   (DATE)
--   means  : happened -- try_to_date(trans_date,'DD-MON-YYYY') - the day the non-derivative insider transaction occurred; the explicit format rules out the epoch trap and the year-0022 
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."TRANSACTION_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'FINANCE.FINANCE__FED_SEC_INSIDER_NONDERIV_TRANS'::varchar as ripple_source,
    src.*
from {{ ref('finance__fed_sec_insider_nonderiv_trans') }} as src
