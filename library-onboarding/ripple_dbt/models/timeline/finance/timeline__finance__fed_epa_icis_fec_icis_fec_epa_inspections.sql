{{ config(materialized='view', schema='TIMELINE', alias='FINANCE__FED_EPA_ICIS_FEC_ICIS_FEC_EPA_INSPECTIONS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for FINANCE.FINANCE__FED_EPA_ICIS_FEC_ICIS_FEC_EPA_INSPECTIONS.
--   column : ACTUAL_BEGIN_DATE   (DATE)
--   means  : span_start -- try_to_date(ACTUAL_BEGIN_DATE): the day the federal inspection started; with actual_end_date it bounds the inspection. Census min 0201-08-01 is try_to_date junk
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."ACTUAL_BEGIN_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'FINANCE.FINANCE__FED_EPA_ICIS_FEC_ICIS_FEC_EPA_INSPECTIONS'::varchar as ripple_source,
    src.*
from {{ ref('finance__fed_epa_icis_fec_icis_fec_epa_inspections') }} as src
