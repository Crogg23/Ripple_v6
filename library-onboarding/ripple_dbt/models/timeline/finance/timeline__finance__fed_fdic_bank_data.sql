{{ config(materialized='view', schema='TIMELINE', alias='FINANCE__FED_FDIC_BANK_DATA') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for FINANCE.FINANCE__FED_FDIC_BANK_DATA.
--   column : INSURANCE_DROPPED_DATE   (DATE)
--   means  : span_end -- try_to_date(left(INSDROPDATE,10)) - the day insurance coverage ended, closing the coverage period; a likely home for the census's 9999-12-31 sentinel rows.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."INSURANCE_DROPPED_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_end') }} as ripple_clock,
    'FINANCE.FINANCE__FED_FDIC_BANK_DATA'::varchar as ripple_source,
    src.*
from {{ ref('finance__fed_fdic_bank_data') }} as src
