{{ config(materialized='view', schema='TIMELINE', alias='FINANCE__INTL_ISO_MIC_REGISTRY') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for FINANCE.FINANCE__INTL_ISO_MIC_REGISTRY.
--   column : CREATION_DATE   (DATE)
--   means  : reported -- try_to_date(CREATION_DATE,'YYYYMMDD') - when the market identifier code was first entered in the ISO 10383 registry.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."CREATION_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'FINANCE.FINANCE__INTL_ISO_MIC_REGISTRY'::varchar as ripple_source,
    src.*
from {{ ref('finance__intl_iso_mic_registry') }} as src
