{{ config(materialized='view', schema='TIMELINE', alias='FINANCE__FED_FDIC_SOD_BRANCH_DEPOSITS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for FINANCE.FINANCE__FED_FDIC_SOD_BRANCH_DEPOSITS.
--   column : SURVEY_YEAR   (NUMBER)
--   means  : happened -- try_to_number(YEAR) - the SOD survey year the branch-deposit figures describe and half the table's own grain; a number, so it must never be bare date-parsed.
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."SURVEY_YEAR"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'FINANCE.FINANCE__FED_FDIC_SOD_BRANCH_DEPOSITS'::varchar as ripple_source,
    src.*
from {{ ref('finance__fed_fdic_sod_branch_deposits') }} as src
