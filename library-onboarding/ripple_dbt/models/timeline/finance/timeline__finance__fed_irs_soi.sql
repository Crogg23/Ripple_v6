{{ config(materialized='view', schema='TIMELINE', alias='FINANCE__FED_IRS_SOI') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for FINANCE.FINANCE__FED_IRS_SOI.
--   column : TAX_YEAR   (TEXT)
--   means  : happened -- Raw TAX_YEAR passthrough - the tax year whose income/return statistics the aggregate row describes; a bare year number and exactly the shape that collapses to 1
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."TAX_YEAR"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'FINANCE.FINANCE__FED_IRS_SOI'::varchar as ripple_source,
    src.*
from {{ ref('finance__fed_irs_soi') }} as src
