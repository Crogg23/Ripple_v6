{{ config(materialized='view', schema='TIMELINE', alias='FINANCE__FED_NCUA_CALL_REPORTS_FOICU') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for FINANCE.FINANCE__FED_NCUA_CALL_REPORTS_FOICU.
--   column : YEAR_OPENED   (NUMBER)
--   means  : happened -- try_to_number(YEAR_OPENED) - the year the credit union opened, the one per-row real-world event on this roster that spreads across decades; a number, never date
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."YEAR_OPENED"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'FINANCE.FINANCE__FED_NCUA_CALL_REPORTS_FOICU'::varchar as ripple_source,
    src.*
from {{ ref('finance__fed_ncua_call_reports_foicu') }} as src
