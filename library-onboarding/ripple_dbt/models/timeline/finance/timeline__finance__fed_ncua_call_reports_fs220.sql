{{ config(materialized='view', schema='TIMELINE', alias='FINANCE__FED_NCUA_CALL_REPORTS_FS220') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for FINANCE.FINANCE__FED_NCUA_CALL_REPORTS_FS220.
--   column : CYCLE_DATE   (DATE)
--   means  : span_end -- Dual-format try_to_timestamp cast to date - the quarter-end (2026-03-31) the financial statement covers; the only world-facing clock on the table.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."CYCLE_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_end') }} as ripple_clock,
    'FINANCE.FINANCE__FED_NCUA_CALL_REPORTS_FS220'::varchar as ripple_source,
    src.*
from {{ ref('finance__fed_ncua_call_reports_fs220') }} as src
