{{ config(materialized='view', schema='TIMELINE', alias='FINANCE__FED_SEC_13F_FILERS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for FINANCE.FINANCE__FED_SEC_13F_FILERS.
--   column : DATEREPORTED   (TEXT)
--   means  : reported -- Raw uncast DATEREPORTED - when previously-confidential holdings were finally reported; sparse, confidential-treatment rows only, so a poor table clock.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_date('src."DATEREPORTED"')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'FINANCE.FINANCE__FED_SEC_13F_FILERS'::varchar as ripple_source,
    src.*
from {{ ref('finance__fed_sec_13f_filers') }} as src
