{{ config(materialized='view', schema='TIMELINE', alias='FINANCE__FED_SEC_MONEY_MARKET_FUND_INFORMATION') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for FINANCE.FINANCE__FED_SEC_MONEY_MARKET_FUND_INFORMATION.
--   column : REPORTMONTH   (TEXT)
--   means  : span_end -- Raw REPORTMONTH passthrough - the month of money-market-fund filings this fund/class listing summarizes; uncast text at month grain and the table's only time co
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_date('src."REPORTMONTH"') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_end') }} as ripple_clock,
    'FINANCE.FINANCE__FED_SEC_MONEY_MARKET_FUND_INFORMATION'::varchar as ripple_source,
    src.*
from {{ ref('finance__fed_sec_money_market_fund_information') }} as src
