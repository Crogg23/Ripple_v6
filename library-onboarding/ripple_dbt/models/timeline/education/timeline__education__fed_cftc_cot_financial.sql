{{ config(materialized='view', schema='TIMELINE', alias='EDUCATION__FED_CFTC_COT_FINANCIAL') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for EDUCATION.EDUCATION__FED_CFTC_COT_FINANCIAL.
--   column : REPORT_DATE_AS_YYYY_MM_DD   (DATE)
--   means  : happened -- try_to_date(REPORT_DATE_AS_YYYY_MM_DD) - the same COT as-of date in ISO form, a safer cast than the YYMMDD twin, and the day the open-interest positions existed
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."REPORT_DATE_AS_YYYY_MM_DD"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'EDUCATION.EDUCATION__FED_CFTC_COT_FINANCIAL'::varchar as ripple_source,
    src.*
from {{ ref('education__fed_cftc_cot_financial') }} as src
