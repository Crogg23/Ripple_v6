{{ config(materialized='view', schema='TIMELINE', alias='EDUCATION__FED_SENATE_LDA_FILINGS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for EDUCATION.EDUCATION__FED_SENATE_LDA_FILINGS.
--   column : DT_POSTED   (TEXT)
--   means  : reported -- When the Senate posted the filing to the public LDA database — the classic reported clock; note the mart applies no cast, so its stored type is whatever landed,
--   grain  : day
--
-- reported cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'reported'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_date('src."DT_POSTED"') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_date('src."DT_POSTED"'), 'reported') }} as ripple_clock,
    'EDUCATION.EDUCATION__FED_SENATE_LDA_FILINGS'::varchar as ripple_source,
    src.*
from {{ ref('education__fed_senate_lda_filings') }} as src
