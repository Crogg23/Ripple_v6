{{ config(materialized='view', schema='TIMELINE', alias='EDUCATION__FED_GOOGLE_POLADS_CREATIVE_STATS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for EDUCATION.EDUCATION__FED_GOOGLE_POLADS_CREATIVE_STATS.
--   column : DATE_RANGE_START   (DATE)
--   means  : span_start -- try_to_date(DATE_RANGE_START) — the reported first day of the window this ad's stats cover; the only properly typed DATE on the table.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."DATE_RANGE_START"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'EDUCATION.EDUCATION__FED_GOOGLE_POLADS_CREATIVE_STATS'::varchar as ripple_source,
    src.*
from {{ ref('education__fed_google_polads_creative_stats') }} as src
