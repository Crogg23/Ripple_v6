{{ config(materialized='view', schema='TIMELINE', alias='REGULATORY__FED_FEDERAL_REGISTER_DOCUMENTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for REGULATORY.REGULATORY__FED_FEDERAL_REGISTER_DOCUMENTS.
--   column : PUBLICATION_DATE   (DATE)
--   means  : reported -- try_to_date(publication_date) in staging - the day the document appeared in the Federal Register. Publication is a reporting clock, and it is the anchor every d
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."PUBLICATION_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'REGULATORY.REGULATORY__FED_FEDERAL_REGISTER_DOCUMENTS'::varchar as ripple_source,
    src.*
from {{ ref('regulatory__fed_federal_register_documents') }} as src
