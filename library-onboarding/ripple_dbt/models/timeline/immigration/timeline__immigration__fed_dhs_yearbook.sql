{{ config(materialized='view', schema='TIMELINE', alias='IMMIGRATION__FED_DHS_YEARBOOK') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for IMMIGRATION.IMMIGRATION__FED_DHS_YEARBOOK.
--   column : FISCAL_YEAR   (NUMBER)
--   means  : reported -- try_to_number in the mart and carries both not_null and unique tests across the 27 rows, so it is a fully populated one-row-per-year key — a federal fiscal year
--   grain  : year
--
-- reported cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'reported'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."FISCAL_YEAR"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_year('src."FISCAL_YEAR"'), 'reported') }} as ripple_clock,
    'IMMIGRATION.IMMIGRATION__FED_DHS_YEARBOOK'::varchar as ripple_source,
    src.*
from {{ ref('immigration__fed_dhs_yearbook') }} as src
