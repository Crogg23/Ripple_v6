{{ config(materialized='view', schema='TIMELINE', alias='IMMIGRATION__FED_DOL_OFLC') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for IMMIGRATION.IMMIGRATION__FED_DOL_OFLC.
--   column : DECISION_DATE   (DATE)
--   means  : decided -- try_to_date on DOL's decision date: the day the Office of Foreign Labor Certification ruled on the case, which is this table's own dated event and matches the c
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DECISION_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('decided') }} as ripple_clock,
    'IMMIGRATION.IMMIGRATION__FED_DOL_OFLC'::varchar as ripple_source,
    src.*
from {{ ref('immigration__fed_dol_oflc') }} as src
