{{ config(materialized='view', schema='TIMELINE', alias='IMMIGRATION__FED_CMS_HOSPICE_ENROLLMENTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for IMMIGRATION.IMMIGRATION__FED_CMS_HOSPICE_ENROLLMENTS.
--   column : INCORPORATION_DATE   (DATE)
--   means  : happened -- try_to_date of incorporation; 6 epoch-1970 rows and an 1848 floor are sentinels. Note this CMS hospice table is mis-filed under the IMMIGRATION schema.
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."INCORPORATION_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."INCORPORATION_DATE"::timestamp_ntz'), 'happened') }} as ripple_clock,
    'IMMIGRATION.IMMIGRATION__FED_CMS_HOSPICE_ENROLLMENTS'::varchar as ripple_source,
    src.*
from {{ ref('immigration__fed_cms_hospice_enrollments') }} as src
