{{ config(materialized='view', schema='TIMELINE', alias='IMMIGRATION__FED_EPA_SDWA_SDWA_SERVICE_AREAS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for IMMIGRATION.IMMIGRATION__FED_EPA_SDWA_SDWA_SERVICE_AREAS.
--   column : FIRST_REPORTED_DATE   (DATE)
--   means  : reported -- try_to_date(FIRST_REPORTED_DATE): the day the state first submitted this water-system service-area row. A link table with no event of its own, so the reporting 
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."FIRST_REPORTED_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'IMMIGRATION.IMMIGRATION__FED_EPA_SDWA_SDWA_SERVICE_AREAS'::varchar as ripple_source,
    src.*
from {{ ref('immigration__fed_epa_sdwa_sdwa_service_areas') }} as src
