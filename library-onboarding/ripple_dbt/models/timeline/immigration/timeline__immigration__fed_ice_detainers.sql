{{ config(materialized='view', schema='TIMELINE', alias='IMMIGRATION__FED_ICE_DETAINERS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for IMMIGRATION.IMMIGRATION__FED_ICE_DETAINERS.
--   column : BIRTH_YEAR   (NUMBER)
--   means  : happened -- try_to_number on the anonymized birth year: a real event at year grain but a demographic attribute of the person, never the record's clock.
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."BIRTH_YEAR"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'IMMIGRATION.IMMIGRATION__FED_ICE_DETAINERS'::varchar as ripple_source,
    src.*
from {{ ref('immigration__fed_ice_detainers') }} as src
