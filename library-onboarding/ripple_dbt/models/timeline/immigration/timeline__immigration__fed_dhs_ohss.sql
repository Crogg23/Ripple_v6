{{ config(materialized='view', schema='TIMELINE', alias='IMMIGRATION__FED_DHS_OHSS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for IMMIGRATION.IMMIGRATION__FED_DHS_OHSS.
--   column : FISCAL_YEAR   (TEXT)
--   means  : happened -- Uncast passthrough of the multi-sheet spreadsheet's fiscal-year label: a federal fiscal year (Oct-Sep), so it is offset about three months from calendar time an
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."FISCAL_YEAR"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'IMMIGRATION.IMMIGRATION__FED_DHS_OHSS'::varchar as ripple_source,
    src.*
from {{ ref('immigration__fed_dhs_ohss') }} as src
