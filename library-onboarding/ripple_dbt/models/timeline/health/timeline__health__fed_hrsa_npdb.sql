{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_HRSA_NPDB') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_HRSA_NPDB.
--   column : GRAD_YEAR   (NUMBER)
--   means  : happened -- try_to_number(GRAD) - year the practitioner graduated; a person attribute, useless as the row's event clock.
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."GRAD_YEAR"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'HEALTH.HEALTH__FED_HRSA_NPDB'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_hrsa_npdb') }} as src
