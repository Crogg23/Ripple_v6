{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_NURSINGHOME411') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_NURSINGHOME411.
--   column : DATE_FIRST_APPROVED_TO_PROVIDE_MEDICARE_AND_MEDICAID_SERVICES   (DATE)
--   means  : decided -- try_to_date (bare) - CMS approving the facility; 153 epoch-1970 rows on this table.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DATE_FIRST_APPROVED_TO_PROVIDE_MEDICARE_AND_MEDICAID_SERVICES"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('decided') }} as ripple_clock,
    'HEALTH.HEALTH__FED_NURSINGHOME411'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_nursinghome411') }} as src
