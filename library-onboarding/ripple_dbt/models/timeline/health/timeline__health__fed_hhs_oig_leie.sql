{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_HHS_OIG_LEIE') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_HHS_OIG_LEIE.
--   column : EXCLUSION_DATE   (TEXT)
--   means  : decided -- When OIG excluded the provider - but the mart exposes trim(exclusion_date_raw), i.e. RAW 'YYYYMMDD' TEXT, discarding the parsed date staging builds; staging war
--   grain  : day
--
-- decided cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'decided'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_date('src."EXCLUSION_DATE"') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_date('src."EXCLUSION_DATE"'), 'decided') }} as ripple_clock,
    'HEALTH.HEALTH__FED_HHS_OIG_LEIE'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_hhs_oig_leie') }} as src
