{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_CMS_OPIOID_TREATMENT_PROGRAM_PROVIDERS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_CMS_OPIOID_TREATMENT_PROGRAM_PROVIDERS.
--   column : MEDICARE_ID_EFFECTIVE_DATE   (DATE)
--   means  : span_start -- try_to_date - when the provider's Medicare ID became effective, the start of enrollment; no paired end column.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."MEDICARE_ID_EFFECTIVE_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'HEALTH.HEALTH__FED_CMS_OPIOID_TREATMENT_PROGRAM_PROVIDERS'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_cms_opioid_treatment_program_providers') }} as src
