{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__PHARMA_MEAL_CAP_FINGERPRINT') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__PHARMA_MEAL_CAP_FINGERPRINT.
--   column : PROGRAM_YEAR   (NUMBER)
--   means  : happened -- try_to_number(program_year) and half the declared grain key - the Open Payments program year the food-and-beverage payments were made in.
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."PROGRAM_YEAR"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'HEALTH.HEALTH__PHARMA_MEAL_CAP_FINGERPRINT'::varchar as ripple_source,
    src.*
from {{ ref('health__pharma_meal_cap_fingerprint') }} as src
