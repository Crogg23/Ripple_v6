{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_CMS_OPEN_PAYMENTS_2023') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_CMS_OPEN_PAYMENTS_2023.
--   column : DATE_OF_PAYMENT   (TEXT)
--   means  : happened -- Payment date, uncast text from landing (14.7M rows).
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_date('src."DATE_OF_PAYMENT"')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'HEALTH.HEALTH__FED_CMS_OPEN_PAYMENTS_2023'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_cms_open_payments_2023') }} as src
