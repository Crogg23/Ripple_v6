{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_FDA_DEVICE_ENFORCEMENT') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_FDA_DEVICE_ENFORCEMENT.
--   column : RECALL_INITIATION_DATE   (DATE)
--   means  : happened -- try_to_date 'YYYYMMDD' - the firm actually started pulling product on this date.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."RECALL_INITIATION_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'HEALTH.HEALTH__FED_FDA_DEVICE_ENFORCEMENT'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_fda_device_enforcement') }} as src
