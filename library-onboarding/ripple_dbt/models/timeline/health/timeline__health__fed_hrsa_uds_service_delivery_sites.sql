{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_HRSA_UDS_SERVICE_DELIVERY_SITES') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_HRSA_UDS_SERVICE_DELIVERY_SITES.
--   column : RECORD_CREATE_DATE   (DATE)
--   means  : reported -- HRSA's data-warehouse record-create stamp - publisher bookkeeping.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."RECORD_CREATE_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'HEALTH.HEALTH__FED_HRSA_UDS_SERVICE_DELIVERY_SITES'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_hrsa_uds_service_delivery_sites') }} as src
