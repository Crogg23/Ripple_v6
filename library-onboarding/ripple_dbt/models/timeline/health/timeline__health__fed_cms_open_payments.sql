{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_CMS_OPEN_PAYMENTS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_CMS_OPEN_PAYMENTS.
--   column : DATE_OF_PAYMENT   (TEXT)
--   means  : happened -- The date the manufacturer paid the doctor, but passed through UNCAST from landing - 15.4M rows of text, which is why the census measured nothing.
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_date('src."DATE_OF_PAYMENT"') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_date('src."DATE_OF_PAYMENT"'), 'happened') }} as ripple_clock,
    'HEALTH.HEALTH__FED_CMS_OPEN_PAYMENTS'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_cms_open_payments') }} as src
