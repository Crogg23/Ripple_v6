{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__ST_OEHHA_PROPOSITION_65_LIST') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__ST_OEHHA_PROPOSITION_65_LIST.
--   column : DATE_LISTED   (DATE)
--   means  : decided -- try_to_date on the trimmed value (staging notes an explicit MM/DD/YYYY format nulled 100% of rows, so the bare parse is deliberate) - the state listing the chem
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DATE_LISTED"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('decided') }} as ripple_clock,
    'HEALTH.HEALTH__ST_OEHHA_PROPOSITION_65_LIST'::varchar as ripple_source,
    src.*
from {{ ref('health__st_oehha_proposition_65_list') }} as src
