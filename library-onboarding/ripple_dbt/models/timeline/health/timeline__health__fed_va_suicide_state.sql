{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_VA_SUICIDE_STATE') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_VA_SUICIDE_STATE.
--   column : YEAR_OF_DEATH   (NUMBER)
--   means  : happened -- coalesce(try_to_number(YEAR_OF_DEATH), try_to_number(YEAR)) in staging so every row carries a year of death.
--   grain  : year
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_year('src."YEAR_OF_DEATH"')) }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_clock('happened') }} as ripple_clock,
    'HEALTH.HEALTH__FED_VA_SUICIDE_STATE'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_va_suicide_state') }} as src
