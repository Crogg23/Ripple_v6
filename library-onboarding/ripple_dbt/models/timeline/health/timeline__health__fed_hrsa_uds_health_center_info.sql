{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_HRSA_UDS_HEALTH_CENTER_INFO') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_HRSA_UDS_HEALTH_CENTER_INFO.
--   column : REPORTINGYEAR   (TEXT)
--   means  : reported -- nullif(trim(REPORTINGYEAR)) - TEXT year of the UDS reporting cycle; likely one constant across all 1,356 rows, so it anchors the snapshot but cannot trend.
--   grain  : year
--
-- reported cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'reported'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_ts_from_year('src."REPORTINGYEAR"') }} as ripple_ts,
    {{ ripple_grain('year') }} as ripple_grain,
    {{ ripple_row_clock(ripple_ts_from_year('src."REPORTINGYEAR"'), 'reported') }} as ripple_clock,
    'HEALTH.HEALTH__FED_HRSA_UDS_HEALTH_CENTER_INFO'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_hrsa_uds_health_center_info') }} as src
