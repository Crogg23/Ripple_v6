{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_FDA_DEVICE_PMA') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_FDA_DEVICE_PMA.
--   column : DATE_RECEIVED   (DATE)
--   means  : reported -- try_to_date 'YYYY-MM-DD' - when the PMA application or supplement was filed.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."DATE_RECEIVED"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'HEALTH.HEALTH__FED_FDA_DEVICE_PMA'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_fda_device_pma') }} as src
