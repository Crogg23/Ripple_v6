{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_FDA_FAERS_REAC') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_FDA_FAERS_REAC.
--   column : SRC_QUARTER   (TEXT)
--   means  : reported -- FDA's quarterly release tag - the only time column on 20.6M rows; the header notes a 2004Q1-2012Q3 legacy-layout era, so quarter mix is not uniform.
--   grain  : quarter
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_ts_from_yearquarter('src."SRC_QUARTER"')) }} as ripple_ts,
    {{ ripple_grain('quarter') }} as ripple_grain,
    {{ ripple_clock('reported') }} as ripple_clock,
    'HEALTH.HEALTH__FED_FDA_FAERS_REAC'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_fda_faers_reac') }} as src
