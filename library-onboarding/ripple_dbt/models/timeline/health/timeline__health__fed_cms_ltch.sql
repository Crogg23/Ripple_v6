{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_CMS_LTCH') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_CMS_LTCH.
--   column : CERTIFICATION_DATE   (DATE)
--   means  : decided -- try_to_date of the Medicare certification date; 1 epoch-1970 row.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_event_ts(ripple_window('src."CERTIFICATION_DATE"::timestamp_ntz')) }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('decided') }} as ripple_clock,
    'HEALTH.HEALTH__FED_CMS_LTCH'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_cms_ltch') }} as src
