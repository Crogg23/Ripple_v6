{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_CLINICALTRIALS') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_CLINICALTRIALS.
--   column : FIRST_POSTED_DATE   (DATE)
--   means  : reported -- try_to_date 'YYYY-MM-DD' - when the registration first appeared publicly.
--   grain  : day
--
-- reported cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'reported'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."FIRST_POSTED_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."FIRST_POSTED_DATE"::timestamp_ntz'), 'reported') }} as ripple_clock,
    'HEALTH.HEALTH__FED_CLINICALTRIALS'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_clinicaltrials') }} as src
