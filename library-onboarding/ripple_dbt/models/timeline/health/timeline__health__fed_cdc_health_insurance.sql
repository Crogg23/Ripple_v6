{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_CDC_HEALTH_INSURANCE') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_CDC_HEALTH_INSURANCE.
--   column : TIME_PERIOD_START_DATE   (TIMESTAMP_NTZ)
--   means  : span_start -- try_to_timestamp of the survey window start; census 2020-2024 is sane.
--   grain  : day
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."TIME_PERIOD_START_DATE"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_clock('span_start') }} as ripple_clock,
    'HEALTH.HEALTH__FED_CDC_HEALTH_INSURANCE'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_cdc_health_insurance') }} as src
