{{ config(materialized='view', schema='TIMELINE', alias='HEALTH__FED_FDA_DEVICE_510K') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for HEALTH.HEALTH__FED_FDA_DEVICE_510K.
--   column : DATE_RECEIVED   (DATE)
--   means  : reported -- try_to_date 'YYYY-MM-DD' - when the submitter filed the 510(k) with FDA.
--   grain  : day
--
-- reported cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'reported'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."DATE_RECEIVED"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."DATE_RECEIVED"::timestamp_ntz'), 'reported') }} as ripple_clock,
    'HEALTH.HEALTH__FED_FDA_DEVICE_510K'::varchar as ripple_source,
    src.*
from {{ ref('health__fed_fda_device_510k') }} as src
