{{ config(materialized='view', schema='TIMELINE', alias='PROCUREMENT__INTL_EC_SERCOP') }}

-- GENERATED by scripts/census/gen_time_views.py -- do not hand-edit.
-- Regenerate after any change to reports/time_index/clock_index.csv.
--
-- Canonical clock for PROCUREMENT.PROCUREMENT__INTL_EC_SERCOP.
--   column : CONTRACT_DATE_SIGNED   (DATE)
--   means  : happened -- Day the contract was actually signed - the real-world event the row exists to record; try_to_date(CONTRACT_DATESIGNED) in staging; note the census's 2026-12-17 
--   grain  : day
--
-- happened cannot be in the future -- if this row's value is, ripple_clock
-- reads 'planned' instead, not 'happened'. See ripple_row_clock in
-- macros/ripple_time.sql.
--
-- The original columns pass through untouched; the canonical four sit in front
-- of them. Rule 8 of the datetime standard: the raw column is never overwritten,
-- so a mis-parse is always recoverable.

select
    {{ ripple_window('src."CONTRACT_DATE_SIGNED"::timestamp_ntz') }} as ripple_ts,
    {{ ripple_grain('day') }} as ripple_grain,
    {{ ripple_row_clock(ripple_window('src."CONTRACT_DATE_SIGNED"::timestamp_ntz'), 'happened') }} as ripple_clock,
    'PROCUREMENT.PROCUREMENT__INTL_EC_SERCOP'::varchar as ripple_source,
    src.*
from {{ ref('procurement__intl_ec_sercop') }} as src
