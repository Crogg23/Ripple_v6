{{ config(materialized='view', schema='LANDING_CLEAN', alias='FED_SEC_DERA_SUB_2025Q4') }}

-- GENERATED by scripts/gen_landing_date_views.py -- do not hand-edit.
-- Regenerate after any change to reports/recon/date_cast_inventory_*.csv or
-- reports/recon/date_derive_rules_*.csv.
--
-- Clean date/time columns for LIBRARY_RAW.LANDING.FED_SEC_DERA_SUB_2025Q4, from the
-- 2026-09-03 warehouse-wide date/time inventory. Every original column
-- passes through untouched via source.*; the clean columns sit beside them.
-- See macros/landing_date_casts.sql for how each is built, and
-- macros/ripple_time.sql for the separate mart-level canonical-clock standard
-- this does not replace.

select
    source.*,
    {{ landing_parse_date('"CHANGED"', 'ymd8', 'TEXT') }} as changed_clean_ts,
    {{ landing_parse_date('"PERIOD"', 'ymd8', 'TEXT') }} as period_clean_ts,
    {{ landing_parse_date('"FILED"', 'ymd8', 'TEXT') }} as filed_clean_ts,
    {{ landing_parse_date('"ACCEPTED"', 'iso', 'TEXT') }} as accepted_clean_ts,
    {{ landing_parse_audit_epoch('"_INGESTED_AT"') }} as ingested_at_clean_ts
from {{ source('ripple_raw', 'FED_SEC_DERA_SUB_2025Q4') }} as source
