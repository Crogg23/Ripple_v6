{{ config(materialized='view', schema='LANDING_CLEAN', alias='FED_GOOGLE_POLADS_CREATIVE_STATS') }}

-- GENERATED by scripts/gen_landing_date_views.py -- do not hand-edit.
-- Regenerate after any change to reports/recon/date_cast_inventory_*.csv or
-- reports/recon/date_derive_rules_*.csv.
--
-- Clean date/time columns for LIBRARY_RAW.LANDING.FED_GOOGLE_POLADS_CREATIVE_STATS, from the
-- 2026-09-03 warehouse-wide date/time inventory. Every original column
-- passes through untouched via source.*; the clean columns sit beside them.
-- See macros/landing_date_casts.sql for how each is built, and
-- macros/ripple_time.sql for the separate mart-level canonical-clock standard
-- this does not replace.

select
    source.*,
    {{ landing_parse_date('"DATE_RANGE_START"', 'iso', 'TEXT') }} as date_range_start_clean_ts,
    {{ landing_parse_date('"DATE_RANGE_END"', 'iso', 'TEXT') }} as date_range_end_clean_ts,
    {{ landing_parse_date('"FIRST_SERVED_TIMESTAMP"', 'iso', 'TEXT') }} as first_served_timestamp_clean_ts,
    {{ landing_parse_date('"LAST_SERVED_TIMESTAMP"', 'iso', 'TEXT') }} as last_served_timestamp_clean_ts,
    {{ landing_parse_audit_epoch('"INGESTED_AT"') }} as ingested_at_clean_ts
from {{ source('ripple_raw', 'FED_GOOGLE_POLADS_CREATIVE_STATS') }} as source
