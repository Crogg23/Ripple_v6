{{ config(materialized='view', schema='LANDING_CLEAN', alias='PORTAL_ARC_ATLANTA_DATAATLA_17C43E4BA3') }}

-- GENERATED by scripts/gen_landing_date_views.py -- do not hand-edit.
-- Regenerate after any change to reports/recon/date_cast_inventory_*.csv or
-- reports/recon/date_derive_rules_*.csv.
--
-- Clean date/time columns for LIBRARY_RAW.LANDING.PORTAL_ARC_ATLANTA_DATAATLA_17C43E4BA3, from the
-- 2026-09-03 warehouse-wide date/time inventory. Every original column
-- passes through untouched via source.*; the clean columns sit beside them.
-- See macros/landing_date_casts.sql for how each is built, and
-- macros/ripple_time.sql for the separate mart-level canonical-clock standard
-- this does not replace.

select
    source.*,
    {{ landing_parse_date('"APPR_DATE"', 'us', 'TEXT') }} as appr_date_clean_ts,
    {{ landing_parse_date('"CREATED_DATE"', 'epochms', 'TEXT') }} as created_date_clean_ts,
    {{ landing_parse_date('"DATE_APP"', 'epochms', 'TEXT') }} as date_app_clean_ts,
    {{ landing_parse_date('"LAST_EDITED_DATE"', 'epochms', 'TEXT') }} as last_edited_date_clean_ts,
    {{ landing_parse_date('"INGESTED_AT"', 'iso', 'TEXT') }} as ingested_at_clean_ts
from {{ source('ripple_raw', 'PORTAL_ARC_ATLANTA_DATAATLA_17C43E4BA3') }} as source
