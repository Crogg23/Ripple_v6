{{ config(materialized='view', schema='LANDING_CLEAN', alias='FED_FARA_BULK') }}

-- GENERATED by scripts/gen_landing_date_views.py -- do not hand-edit.
-- Regenerate after any change to reports/recon/date_cast_inventory_*.csv or
-- reports/recon/date_derive_rules_*.csv.
--
-- Clean date/time columns for LIBRARY_RAW.LANDING.FED_FARA_BULK, from the
-- 2026-09-03 warehouse-wide date/time inventory. Every original column
-- passes through untouched via source.*; the clean columns sit beside them.
-- See macros/landing_date_casts.sql for how each is built, and
-- macros/ripple_time.sql for the separate mart-level canonical-clock standard
-- this does not replace.

select
    source.*,
    {{ landing_parse_date('"REGISTRATION_DATE"', 'us', 'TEXT') }} as registration_date_clean_ts,
    {{ landing_parse_date('"TERMINATION_DATE"', 'us', 'TEXT') }} as termination_date_clean_ts,
    {{ landing_parse_date('"DATE_STAMPED"', 'us', 'TEXT') }} as date_stamped_clean_ts,
    {{ landing_parse_date('"SHORT_FORM_TERMINATION_DATE"', 'us', 'TEXT') }} as short_form_termination_date_clean_ts,
    {{ landing_parse_date('"SHORT_FORM_DATE"', 'us', 'TEXT') }} as short_form_date_clean_ts,
    {{ landing_parse_date('"FOREIGN_PRINCIPAL_TERMINATION_DATE"', 'us', 'TEXT') }} as foreign_principal_termination_date_clean_ts,
    {{ landing_parse_date('"FOREIGN_PRINCIPAL_REGISTRATION_DATE"', 'us', 'TEXT') }} as foreign_principal_registration_date_clean_ts,
    {{ landing_parse_date('"REGISTRANT_DATE"', 'us', 'TEXT') }} as registrant_date_clean_ts,
    {{ landing_parse_audit_epoch('"_INGESTED_AT"') }} as ingested_at_clean_ts
from {{ source('ripple_raw', 'FED_FARA_BULK') }} as source
