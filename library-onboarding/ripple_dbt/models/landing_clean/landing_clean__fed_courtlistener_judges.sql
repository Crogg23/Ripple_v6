{{ config(materialized='view', schema='LANDING_CLEAN', alias='FED_COURTLISTENER_JUDGES') }}

-- GENERATED by scripts/gen_landing_date_views.py -- do not hand-edit.
-- Regenerate after any change to reports/recon/date_cast_inventory_*.csv or
-- reports/recon/date_derive_rules_*.csv.
--
-- Clean date/time columns for LIBRARY_RAW.LANDING.FED_COURTLISTENER_JUDGES, from the
-- 2026-09-03 warehouse-wide date/time inventory. Every original column
-- passes through untouched via source.*; the clean columns sit beside them.
-- See macros/landing_date_casts.sql for how each is built, and
-- macros/ripple_time.sql for the separate mart-level canonical-clock standard
-- this does not replace.

select
    source.*,
    {{ landing_parse_date('"DATE_CREATED"', 'iso', 'TEXT') }} as date_created_clean_ts,
    {{ landing_parse_date('"DATE_MODIFIED"', 'iso', 'TEXT') }} as date_modified_clean_ts,
    {{ landing_parse_date('"DATE_COMPLETED"', 'iso', 'TEXT') }} as date_completed_clean_ts,
    {{ landing_parse_date('"DATE_DOB"', 'iso', 'TEXT') }} as date_dob_clean_ts,
    {{ landing_parse_date('"DATE_DOD"', 'iso', 'TEXT') }} as date_dod_clean_ts,
    {{ landing_parse_audit_epoch('"INGESTED_AT"') }} as ingested_at_clean_ts,
    {{ landing_translate_granularity('"DATE_GRANULARITY_DOB"') }} as date_granularity_dob_label,
    {{ landing_translate_granularity('"DATE_GRANULARITY_DOD"') }} as date_granularity_dod_label
from {{ source('ripple_raw', 'FED_COURTLISTENER_JUDGES') }} as source
