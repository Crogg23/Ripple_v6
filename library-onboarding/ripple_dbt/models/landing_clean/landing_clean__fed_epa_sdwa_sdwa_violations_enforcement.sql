{{ config(materialized='view', schema='LANDING_CLEAN', alias='FED_EPA_SDWA_SDWA_VIOLATIONS_ENFORCEMENT') }}

-- GENERATED by scripts/gen_landing_date_views.py -- do not hand-edit.
-- Regenerate after any change to reports/recon/date_cast_inventory_*.csv or
-- reports/recon/date_derive_rules_*.csv.
--
-- Clean date/time columns for LIBRARY_RAW.LANDING.FED_EPA_SDWA_SDWA_VIOLATIONS_ENFORCEMENT, from the
-- 2026-09-03 warehouse-wide date/time inventory. Every original column
-- passes through untouched via source.*; the clean columns sit beside them.
-- See macros/landing_date_casts.sql for how each is built, and
-- macros/ripple_time.sql for the separate mart-level canonical-clock standard
-- this does not replace.

select
    source.*,
    {{ landing_parse_date('"COMPL_PER_BEGIN_DATE"', 'us', 'TEXT') }} as compl_per_begin_date_clean_ts,
    {{ landing_parse_date('"COMPL_PER_END_DATE"', 'us', 'TEXT') }} as compl_per_end_date_clean_ts,
    {{ landing_parse_date('"NON_COMPL_PER_BEGIN_DATE"', 'us', 'TEXT') }} as non_compl_per_begin_date_clean_ts,
    {{ landing_parse_date('"NON_COMPL_PER_END_DATE"', 'us', 'TEXT') }} as non_compl_per_end_date_clean_ts,
    {{ landing_parse_date('"PWS_DEACTIVATION_DATE"', 'us', 'TEXT') }} as pws_deactivation_date_clean_ts,
    {{ landing_parse_date('"CALCULATED_RTC_DATE"', 'us', 'TEXT') }} as calculated_rtc_date_clean_ts,
    {{ landing_parse_date('"VIOL_FIRST_REPORTED_DATE"', 'us', 'TEXT') }} as viol_first_reported_date_clean_ts,
    {{ landing_parse_date('"VIOL_LAST_REPORTED_DATE"', 'us', 'TEXT') }} as viol_last_reported_date_clean_ts,
    {{ landing_parse_date('"ENFORCEMENT_DATE"', 'us', 'TEXT') }} as enforcement_date_clean_ts,
    {{ landing_parse_date('"ENF_FIRST_REPORTED_DATE"', 'us', 'TEXT') }} as enf_first_reported_date_clean_ts,
    {{ landing_parse_date('"ENF_LAST_REPORTED_DATE"', 'us', 'TEXT') }} as enf_last_reported_date_clean_ts,
    {{ landing_parse_audit_epoch('"_INGESTED_AT"') }} as ingested_at_clean_ts
from {{ source('ripple_raw', 'FED_EPA_SDWA_SDWA_VIOLATIONS_ENFORCEMENT') }} as source
