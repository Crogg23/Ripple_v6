{{ config(materialized='view', schema='LANDING_CLEAN', alias='FED_NID_DAMS') }}

-- GENERATED by scripts/gen_landing_date_views.py -- do not hand-edit.
-- Regenerate after any change to reports/recon/date_cast_inventory_*.csv or
-- reports/recon/date_derive_rules_*.csv.
--
-- Clean date/time columns for LIBRARY_RAW.LANDING.FED_NID_DAMS, from the
-- 2026-09-03 warehouse-wide date/time inventory. Every original column
-- passes through untouched via source.*; the clean columns sit beside them.
-- See macros/landing_date_casts.sql for how each is built, and
-- macros/ripple_time.sql for the separate mart-level canonical-clock standard
-- this does not replace.

select
    source.*,
    {{ landing_parse_date('"DATA_LAST_UPDATED"', 'us', 'TEXT') }} as data_last_updated_clean_ts,
    {{ landing_parse_date('"LAST_INSPECTION_DATE"', 'us', 'TEXT') }} as last_inspection_date_clean_ts,
    {{ landing_parse_date('"CONDITION_ASSESSMENT_DATE"', 'us', 'TEXT') }} as condition_assessment_date_clean_ts,
    {{ landing_parse_date('"OPERATIONAL_STATUS_DATE"', 'us', 'TEXT') }} as operational_status_date_clean_ts,
    {{ landing_parse_date('"EAP_LAST_REVISION_DATE"', 'us', 'TEXT') }} as eap_last_revision_date_clean_ts,
    {{ landing_parse_audit_epoch('"_INGESTED_AT"') }} as ingested_at_clean_ts
from {{ source('ripple_raw', 'FED_NID_DAMS') }} as source
