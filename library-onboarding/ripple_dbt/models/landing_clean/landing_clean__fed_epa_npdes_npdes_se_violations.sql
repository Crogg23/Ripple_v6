{{ config(materialized='view', schema='LANDING_CLEAN', alias='FED_EPA_NPDES_NPDES_SE_VIOLATIONS') }}

-- GENERATED by scripts/gen_landing_date_views.py -- do not hand-edit.
-- Regenerate after any change to reports/recon/date_cast_inventory_*.csv or
-- reports/recon/date_derive_rules_*.csv.
--
-- Clean date/time columns for LIBRARY_RAW.LANDING.FED_EPA_NPDES_NPDES_SE_VIOLATIONS, from the
-- 2026-09-03 warehouse-wide date/time inventory. Every original column
-- passes through untouched via source.*; the clean columns sit beside them.
-- See macros/landing_date_casts.sql for how each is built, and
-- macros/ripple_time.sql for the separate mart-level canonical-clock standard
-- this does not replace.

select
    source.*,
    {{ landing_parse_date('"SINGLE_EVENT_VIOLATION_DATE"', 'us', 'TEXT') }} as single_event_violation_date_clean_ts,
    {{ landing_parse_date('"SINGLE_EVENT_END_DATE"', 'us', 'TEXT') }} as single_event_end_date_clean_ts,
    {{ landing_parse_date('"RNC_DETECTION_DATE"', 'us', 'TEXT') }} as rnc_detection_date_clean_ts,
    {{ landing_parse_date('"RNC_RESOLUTION_DATE"', 'us', 'TEXT') }} as rnc_resolution_date_clean_ts,
    {{ landing_parse_audit_epoch('"_INGESTED_AT"') }} as ingested_at_clean_ts
from {{ source('ripple_raw', 'FED_EPA_NPDES_NPDES_SE_VIOLATIONS') }} as source
