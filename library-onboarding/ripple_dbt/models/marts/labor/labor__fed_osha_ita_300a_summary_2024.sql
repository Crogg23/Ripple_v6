{{ config(materialized='table', schema='LABOR') }}

-- Source: FED_OSHA_ITA_300A_SUMMARY_2024 (398620 rows)
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: TOTAL_DEATHS was a bare try_to_number() (silently rounds
-- decimals) and CREATED_TIMESTAMP a bare try_to_timestamp() (pure-digit values
-- misread as epoch seconds). Live-checked: CREATED_TIMESTAMP is a plain
-- M/D/YYYY date string with no time-of-day (e.g. '2/28/2025'), and
-- TOTAL_DEATHS is 100% whole numbers today across all 398,620 rows -- both
-- clean, but unguarded. Now reads the already-guarded staging model (same 32
-- columns, same order): total_deaths and 17 sibling count columns get
-- stg_int, created_timestamp gets stg_date (matches what the data actually
-- is), establishment_id/total_hours_worked get the ID-shaped stg_id_text
-- ('.0' tail stripped) instead of staying fully raw.

select
    id,
    establishment_name,
    establishment_id,
    ein,
    company_name,
    street_address,
    city,
    state,
    zip_code,
    naics_code,
    naics_year,
    industry_description,
    establishment_type,
    size,
    annual_average_employees,
    total_hours_worked,
    no_injuries_illnesses,
    total_deaths,
    total_dafw_cases,
    total_djtr_cases,
    total_other_cases,
    total_dafw_days,
    total_djtr_days,
    total_injuries,
    total_skin_disorders,
    total_respiratory_conditions,
    total_poisonings,
    total_hearing_loss,
    total_other_illnesses,
    change_reason,
    created_timestamp,
    year_filing_for
from {{ ref('stg_fed_osha_ita_300a_summary_2024__records') }}
