{{ config(materialized='table', schema='SCIENCE') }}

-- Source: USGS Earthquake Catalog (M2.5+ full history) (443274 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_USGS_EARTHQUAKES') }}
)

select
    "TIME" as time,
    try_to_double(LATITUDE) as latitude,
    try_to_double(LONGITUDE) as longitude,
    DEPTH as depth,
    MAG as mag,
    MAGTYPE as magtype,
    NST as nst,
    GAP as gap,
    DMIN as dmin,
    RMS as rms,
    NET as net,
    ID as id,
    try_to_date(UPDATED) as updated,
    PLACE as place,
    TYPE as type,
    HORIZONTALERROR as horizontalerror,
    DEPTHERROR as deptherror,
    MAGERROR as magerror,
    MAGNST as magnst,
    STATUS as status,
    LOCATIONSOURCE as locationsource,
    MAGSOURCE as magsource
from source
