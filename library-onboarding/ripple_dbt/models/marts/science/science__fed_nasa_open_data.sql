{{ config(materialized='table', schema='SCIENCE') }}

-- Source: NASA Open APIs & Data Portal (54 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_NASA_OPEN_DATA') }}
)

select
    API_NAME as api_name,
    REQUEST_URL as request_url,
    try_to_date(RESPONSE_DATE) as response_date,
    TITLE as title,
    DESCRIPTION as description,
    URL as url,
    MEDIA_TYPE as media_type,
    RAW_JSON as raw_json
from source
