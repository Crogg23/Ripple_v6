{{ config(materialized='table', schema='SCIENCE') }}

-- Source: Annual reported AI incidents & controversies (AIID via AI Index) (14 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_xc_owid_ai_incidents_annual__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    entity,
    code,
    year,
    annual_reported_artificial_intelligence_incidents_and_controversies
from {{ ref('stg_xc_owid_ai_incidents_annual__records') }}
