{{ config(materialized='table', schema='UNCATEGORIZED') }}

-- Source: XC_ICIJ_OFFSHORE_NODES_INTERMEDIARIES (26768 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_ICIJ_OFFSHORE_NODES_INTERMEDIARIES') }}
)

select
    NODE_ID as node_id,
    NAME as name,
    STATUS as status,
    INTERNAL_ID as internal_id,
    ADDRESS as address,
    COUNTRIES as countries,
    COUNTRY_CODES as country_codes,
    SOURCEID as sourceid,
    VALID_UNTIL as valid_until,
    NOTE as note
from source
