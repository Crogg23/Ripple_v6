{{ config(enabled=false, materialized='table', schema='UNCATEGORIZED') }}
-- DISABLED 2026-08-26: not a duplicate (no categorized twin exists), but the raw
-- landing table's header was never parsed — the two columns are a run timestamp
-- and UNNAMED_1, so this mart is unusable as built. The source needs a RELOAD
-- with proper header parsing before any mart is generated (file under finance
-- via the new "dtcc" ID_HINT when it is). On drop list.

-- Source: FED_DTCC_DTC_PARTICIPANTS (904 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_DTCC_DTC_PARTICIPANTS') }}
)

select
    RUN_TIME_6_30_2026_9_50_31_AM as run_time_6_30_2026_9_50_31_am,
    UNNAMED_1 as unnamed_1
from source
