{{ config(enabled=false, materialized='table', schema='UNCATEGORIZED') }}

-- DISABLED 2026-08-26 (uncategorized-bucket dedup): duplicate of the categorized
-- mart HEALTH.HEALTH__FED_DEA_ARCOS (178,598,026 rows, identical count). Was
-- enabled-but-unbuilt; a full dbt build would have recreated 178.6M duplicate rows.
-- Root cause: ID_HINTS fallback in scripts/gen_mart_models.py — fixed same day.
-- Source: FED_DEA_ARCOS_FULL (178598026 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_dea_arcos_full__organizations) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    reporter_dea_no,
    reporter_bus_act,
    reporter_name,
    reporter_addl_co_info,
    reporter_address1,
    reporter_address2,
    reporter_city,
    reporter_state,
    reporter_zip,
    reporter_county,
    buyer_dea_no,
    buyer_bus_act,
    buyer_name,
    buyer_addl_co_info,
    buyer_address1,
    buyer_address2,
    buyer_city,
    buyer_state,
    buyer_zip,
    buyer_county,
    transaction_code,
    drug_code,
    ndc_no,
    drug_name,
    quantity,
    unit,
    action_indicator,
    order_form_no,
    correction_no,
    strength,
    transaction_date,
    calc_base_wt_in_gm,
    dosage_unit,
    transaction_id,
    product_name,
    ingredient_name,
    measure,
    mme_conversion_factor,
    combined_labeler_name,
    revised_company_name,
    reporter_family,
    dos_str
from {{ ref('stg_fed_dea_arcos_full__organizations') }}
