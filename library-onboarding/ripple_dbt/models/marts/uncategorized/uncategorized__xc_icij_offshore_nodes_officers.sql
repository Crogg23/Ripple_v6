{{ config(enabled=false, materialized='table', schema='UNCATEGORIZED') }}
-- DISABLED 2026-08-26 (uncategorized-bucket dedup): third landing copy of the same
-- ICIJ nodes — duplicate of CORPORATE_REGISTRY__FED_ICIJ_OFFSHORELEAKS_OFFICERS.
-- This copy stores missing values as true NULLs (its LEAKS_ twin uses '' sentinels);
-- the FED_ set backing corporate-registry is still the keeper. On drop list.

-- Source: XC_ICIJ_OFFSHORE_NODES_OFFICERS (771315 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_ICIJ_OFFSHORE_NODES_OFFICERS') }}
)

select
    NODE_ID as node_id,
    NAME as name,
    COUNTRIES as countries,
    COUNTRY_CODES as country_codes,
    SOURCEID as sourceid,
    VALID_UNTIL as valid_until,
    NOTE as note
from source
