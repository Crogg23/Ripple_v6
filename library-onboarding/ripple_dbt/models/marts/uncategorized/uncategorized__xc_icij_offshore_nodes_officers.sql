{{ config(materialized='table', schema='UNCATEGORIZED') }}

-- Source: XC_ICIJ_OFFSHORE_NODES_OFFICERS (771315 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_ICIJ_OFFSHORE_NODES_OFFICERS') }}
)

select
    NODE_ID as node_id,
    NAME as name,
    COUNTRIES as countries,
    COUNTRY_CODES as country_codes,
    SOURCEID as sourceid,
    VALID_UNTIL as valid_until,
    NOTE as note
from source
