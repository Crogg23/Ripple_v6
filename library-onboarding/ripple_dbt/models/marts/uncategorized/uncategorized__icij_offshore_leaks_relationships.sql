{{ config(enabled=false, materialized='table', schema='UNCATEGORIZED') }}
-- DISABLED 2026-08-26 (uncategorized-bucket dedup): duplicate of the categorized
-- mart CORPORATE_REGISTRY.CORPORATE_REGISTRY__FED_ICIJ_OFFSHORELEAKS_RELATIONSHIPS
-- (3,339,267 rows). On drop list.

-- Source: ICIJ_OFFSHORE_LEAKS_RELATIONSHIPS (3339267 rows)
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: START_DATE/END_DATE were bare try_to_date() calls -- a
-- pure-digit value would misread as epoch seconds instead of failing safely.
-- No staging model exists for this source at all (only the entities side of
-- ICIJ offshore leaks has one), so wrapped the two bare casts in place. This
-- model stays disabled (drop-list duplicate, see above) -- fixed anyway since
-- it's a landmine either way.

with source as (
    select * from {{ source('ripple_raw', 'ICIJ_OFFSHORE_LEAKS_RELATIONSHIPS') }}
)

select
    NODE_ID_START as node_id_start,
    NODE_ID_END as node_id_end,
    REL_TYPE as rel_type,
    LINK as link,
    STATUS as status,
    {{ stg_date('START_DATE') }} as start_date,
    {{ stg_date('END_DATE') }} as end_date,
    SOURCEID as sourceid
from source
