{{ config(enabled=false, materialized='table', schema='UNCATEGORIZED') }}
-- DISABLED 2026-08-26 (uncategorized-bucket dedup): duplicate of the categorized
-- mart CORPORATE_REGISTRY.CORPORATE_REGISTRY__FED_ICIJ_OFFSHORELEAKS_INTERMEDIARIES
-- (26,768 rows). On drop list.

-- Source: ICIJ_OFFSHORE_LEAKS_INTERMEDIARIES (26768 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'ICIJ_OFFSHORE_LEAKS_INTERMEDIARIES') }}
)

select
    NODE_ID as node_id,
    NAME as name,
    STATUS as status,
    INTERNAL_ID as internal_id,
    ADDRESS as address,
    COUNTRIES as countries,
    COUNTRY_CODES as country_codes,
    SOURCEID as sourceid,
    VALID_UNTIL as valid_until,
    NOTE as note
from source
