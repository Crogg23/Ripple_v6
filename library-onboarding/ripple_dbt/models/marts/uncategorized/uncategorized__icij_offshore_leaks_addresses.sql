{{ config(enabled=false, materialized='table', schema='UNCATEGORIZED') }}
-- DISABLED 2026-08-26 (uncategorized-bucket dedup): duplicate of the categorized
-- mart CORPORATE_REGISTRY.CORPORATE_REGISTRY__FED_ICIJ_OFFSHORELEAKS_ADDRESSES
-- (402,246 rows, richer columns). Three raw landing copies of this ICIJ family
-- exist; the FED_ set backing the corporate-registry marts is the keeper. On drop list.

-- Source: ICIJ_OFFSHORE_LEAKS_ADDRESSES (402246 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'ICIJ_OFFSHORE_LEAKS_ADDRESSES') }}
)

select
    NODE_ID as node_id,
    ADDRESS as address,
    NAME as name,
    COUNTRIES as countries,
    COUNTRY_CODES as country_codes,
    SOURCEID as sourceid,
    VALID_UNTIL as valid_until,
    NOTE as note
from source
