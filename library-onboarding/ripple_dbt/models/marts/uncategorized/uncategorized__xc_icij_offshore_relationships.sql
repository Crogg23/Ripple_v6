{{ config(materialized='table', schema='UNCATEGORIZED') }}

-- Source: XC_ICIJ_OFFSHORE_RELATIONSHIPS (3339267 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_ICIJ_OFFSHORE_RELATIONSHIPS') }}
)

select
    NODE_ID_START as node_id_start,
    NODE_ID_END as node_id_end,
    REL_TYPE as rel_type,
    LINK as link,
    STATUS as status,
    try_to_date(START_DATE) as start_date,
    try_to_date(END_DATE) as end_date,
    SOURCEID as sourceid
from source
