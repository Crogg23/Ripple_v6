{{ config(enabled=false, materialized='table', schema='UNCATEGORIZED') }}
-- DISABLED 2026-08-26 (uncategorized-bucket dedup): duplicate of the categorized
-- mart CORPORATE_REGISTRY.CORPORATE_REGISTRY__FED_ICIJ_OFFSHORELEAKS_OFFICERS
-- (771,315 rows, 9 cols vs 7). NOTE: this landing copy stores missing values as
-- sentinel '' blanks where the XC_ copy stores true NULLs — the recurring
-- sentinel-vs-NULL trap. Neither is the keeper; the FED_ set is. On drop list.

-- Source: ICIJ_OFFSHORE_LEAKS_OFFICERS (771315 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'ICIJ_OFFSHORE_LEAKS_OFFICERS') }}
)

select
    NODE_ID as node_id,
    NAME as name,
    COUNTRIES as countries,
    COUNTRY_CODES as country_codes,
    SOURCEID as sourceid,
    VALID_UNTIL as valid_until,
    NOTE as note
from source
