{{ config(materialized='table', schema='IMMIGRATION') }}

-- Source: OWID ? Refugee population by country of origin (7442 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_xc_owid_refugees__places) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    entity,
    code,
    year,
    refugees_by_country_of_origin,
    world_region_according_to_owid
from {{ ref('stg_xc_owid_refugees__places') }}
