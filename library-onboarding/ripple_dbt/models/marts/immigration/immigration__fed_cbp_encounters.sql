{{ config(materialized='table', schema='IMMIGRATION') }}

-- Source: CBP Nationwide Encounters Data (9 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_cbp_encounters__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    monthly_totals,
    oct_25,
    nov_25,
    dec_25,
    jan_26,
    feb_26,
    mar_26,
    apr_26,
    may_26,
    jun_26,
    jul_26,
    aug_26,
    sep_26,
    source_url,
    data_as_of_date
from {{ ref('stg_fed_cbp_encounters__records') }}
