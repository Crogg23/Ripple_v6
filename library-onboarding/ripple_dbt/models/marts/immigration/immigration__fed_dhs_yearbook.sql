{{ config(materialized='table', schema='IMMIGRATION') }}

-- Source: DHS Yearbook of Immigration Statistics (27 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_dhs_yearbook__places) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    fiscal_year,
    country_of_birth,
    country_of_last_residence,
    category,
    state,
    count,
    table_name,
    yearbook_edition,
    enforcement_action_type,
    visa_class,
    source_pdf_url
from {{ ref('stg_fed_dhs_yearbook__places') }}
