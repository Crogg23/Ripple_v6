{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: OWID ? Fossil fuel share of primary energy (6379 rows)
-- COUNT vs PUBLISHER (verified 2026-08-11): current grapher CSV has ~4,914 records; our 6,379 comes from an older/wider grapher version (entity-set drift), not a double-load.
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_xc_owid_fossil_share__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    entity,
    code,
    year,
    fossil_fuels
from {{ ref('stg_xc_owid_fossil_share__records') }}
