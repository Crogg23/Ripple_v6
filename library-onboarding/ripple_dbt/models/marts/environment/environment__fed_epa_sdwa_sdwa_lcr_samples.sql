{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_SDWA_SDWA_LCR_SAMPLES (927,415 rows)  [row count re-verified against the live table 2026-08-11; header had said 500000]
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_SDWA_SDWA_LCR_SAMPLES') }}
)

select
    SUBMISSIONYEARQUARTER as submissionyearquarter,
    PWSID as pwsid,
    SAMPLE_ID as sample_id,
    try_to_date(SAMPLING_END_DATE) as sampling_end_date,
    try_to_date(SAMPLING_START_DATE) as sampling_start_date,
    RECONCILIATION_ID as reconciliation_id,
    try_to_date(SAMPLE_FIRST_REPORTED_DATE) as sample_first_reported_date,
    try_to_date(SAMPLE_LAST_REPORTED_DATE) as sample_last_reported_date,
    SAR_ID as sar_id,
    CONTAMINANT_CODE as contaminant_code,
    RESULT_SIGN_CODE as result_sign_code,
    SAMPLE_MEASURE as sample_measure,
    UNIT_OF_MEASURE as unit_of_measure,
    try_to_date(SAR_FIRST_REPORTED_DATE) as sar_first_reported_date,
    try_to_date(SAR_LAST_REPORTED_DATE) as sar_last_reported_date
from source
