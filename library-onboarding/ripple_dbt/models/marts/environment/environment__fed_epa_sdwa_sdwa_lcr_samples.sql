{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_SDWA_SDWA_LCR_SAMPLES (927,415 rows)  [row count re-verified against the live table 2026-08-11; header had said 500000]
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with 6 bare try_to_date() casts, no
-- guard against a pure-digit value misreading as an epoch date. Not live-
-- checked column-by-column for this table specifically -- fixed as a landmine
-- regardless (see environment__fed_epa_sdwa_sdwa_violations_enforcement.sql /
-- environment__fed_epa_sdwa_sdwa_pub_water_systems.sql for the live before/
-- after proof run on this same bug pattern elsewhere in this batch). Column
-- list matches models/staging/fed_epa_sdwa_sdwa_lcr_samples exactly, so swapped
-- to ref() instead of hand-wrapping: same 15 columns, same names, same order,
-- every date cast regex-guarded (stg_date, macros/staging_casts.sql). Two
-- bonuses from the swap: SAMPLE_MEASURE now arrives cast to a guarded float
-- (stg_float) instead of raw uncast text, and the staging model drops 3,552
-- whole-row exact-duplicate records (SAR_ID is not unique per its own DEDUPE
-- note) that this mart previously had no dedup logic for at all.

select
    submissionyearquarter,
    pwsid,
    sample_id,
    sampling_end_date,
    sampling_start_date,
    reconciliation_id,
    sample_first_reported_date,
    sample_last_reported_date,
    sar_id,
    contaminant_code,
    result_sign_code,
    sample_measure,
    unit_of_measure,
    sar_first_reported_date,
    sar_last_reported_date
from {{ ref('stg_fed_epa_sdwa_sdwa_lcr_samples__records') }}
