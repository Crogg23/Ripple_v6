{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_AIR_EMISSIONS_POLL_RPT_COMBINED_EMISSIONS (500000 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_AIR_EMISSIONS_POLL_RPT_COMBINED_EMISSIONS') }}
)

select
    REPORTING_YEAR as reporting_year,
    REGISTRY_ID as registry_id,
    PGM_SYS_ACRNM as pgm_sys_acrnm,
    PGM_SYS_ID as pgm_sys_id,
    POLLUTANT_NAME as pollutant_name,
    ANNUAL_EMISSION as annual_emission,
    UNIT_OF_MEASURE as unit_of_measure,
    NEI_TYPE as nei_type,
    NEI_HAP_VOC_FLAG as nei_hap_voc_flag
from source
