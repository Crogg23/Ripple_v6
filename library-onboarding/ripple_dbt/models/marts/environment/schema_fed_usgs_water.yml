version: 2

models:
  - name: environment__fed_usgs_water
    columns:
      - name: site_no
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - site_no
              - parameter_cd
              - datetime
              - data_type
