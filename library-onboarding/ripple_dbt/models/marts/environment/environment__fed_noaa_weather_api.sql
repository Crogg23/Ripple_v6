{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: Weather.gov API (National Weather Service) (287 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_NOAA_WEATHER_API') }}
)

select
    ID as id,
    TYPE as type,
    EVENT as event,
    STATUS as status,
    MESSAGE_TYPE as message_type,
    SEVERITY as severity,
    CERTAINTY as certainty,
    URGENCY as urgency,
    AREA_DESC as area_desc,
    SENT as sent,
    EFFECTIVE as effective,
    ONSET as onset,
    try_to_date(EXPIRES) as expires,
    ENDS as ends,
    HEADLINE as headline,
    DESCRIPTION as description,
    INSTRUCTION as instruction,
    SENDER_NAME as sender_name,
    ZONE_UGC as zone_ugc,
    GEOMETRY as geometry,
    STATION_ID as station_id,
    TEMPERATURE as temperature,
    try_to_number(WIND_SPEED) as wind_speed,
    WIND_DIRECTION as wind_direction,
    TIMESTAMP as timestamp,
    FORECAST_OFFICE as forecast_office,
    GRID_X as grid_x,
    GRID_Y as grid_y,
    try_to_double(LATITUDE) as latitude,
    try_to_double(LONGITUDE) as longitude,
    FIPS_CODE as fips_code,
    ZIP_CODE as zip_code
from source
