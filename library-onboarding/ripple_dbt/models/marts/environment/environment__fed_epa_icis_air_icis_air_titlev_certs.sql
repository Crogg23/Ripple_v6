{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_ICIS_AIR_ICIS_AIR_TITLEV_CERTS (499,113 rows)  [row count re-verified against the live table 2026-08-11; header had said 500000]
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: ACTUAL_END_DATE was a bare try_to_date() -- no format, no
-- guard, so a pure-digit string would misread as epoch seconds (checked live:
-- try_to_date('2015') = 1970-01-01). Live-checked this column specifically:
-- 493,116 of 493,116 filled rows already parse clean via Snowflake's auto
-- date detection (slash format, e.g. '01/30/2017') and zero rows are
-- pure-digit -- a landmine today, not an active bug. Swapped to the
-- already-guarded staging model instead of hand-wrapping the cast: same 7
-- columns, same names, same order. The mart's own whole-row DEDUP
-- (2026-08-11 fix, single-load exact duplicates) moves to the staging
-- model's identical whole-row dedup -- verified same output count either
-- way (499,113 rows both before and after, so this table currently carries
-- no true whole-row duplicates; the qualify is a no-op safeguard on both
-- sides, not a behavior change).

select
    pgm_sys_id,
    activity_id,
    comp_monitor_type_code,
    comp_monitor_type_desc,
    state_epa_flag,
    actual_end_date,
    facility_rpt_deviation_flag
from {{ ref('stg_fed_epa_icis_air_icis_air_titlev_certs__records') }}
