version: 2

models:
  - name: environment__fed_epa_envirofacts
    description: "SAMPLE ONLY -- NOT the full dataset. EPA Envirofacts API: a 5,000-row slice of ONE program table (TRI facility names); FRS/site/handler id columns are blank in this slice. Use for shape/testing only."
    columns:
      - name: facility_name
        data_tests: [not_null]
