{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_RCRA_RCRA_EVALUATIONS (1166410 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_RCRA_RCRA_EVALUATIONS') }}
)

select
    ID_NUMBER as id_number,
    ACTIVITY_LOCATION as activity_location,
    EVALUATION_IDENTIFIER as evaluation_identifier,
    EVALUATION_TYPE as evaluation_type,
    EVALUATION_DESC as evaluation_desc,
    EVALUATION_AGENCY as evaluation_agency,
    try_to_date(EVALUATION_START_DATE) as evaluation_start_date,
    FOUND_VIOLATION as found_violation
from source
