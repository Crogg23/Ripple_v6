{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_ICIS_AIR_ICIS_AIR_STACK_TESTS (620,302 rows)  [row count re-verified against the live table 2026-08-11; header had said 500000]
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: ACTUAL_END_DATE was a bare try_to_date() -- no format, no
-- guard, so a pure-digit string would misread as epoch seconds (checked live:
-- try_to_date('2015') = 1970-01-01). Live-checked this column specifically:
-- 620,298 of 620,298 filled rows already parse clean via Snowflake's auto
-- date detection (slash format, e.g. '06/20/2024') and zero rows are
-- pure-digit -- a landmine today, not an active bug. Swapped to the
-- already-guarded staging model instead of hand-wrapping the cast: same 10
-- columns, same names, same order, and its whole-row dedup live-verified as
-- a no-op on this table today (620,302 raw = 620,302 deduped, no rows lost).

select
    pgm_sys_id,
    activity_id,
    comp_monitor_type_code,
    comp_monitor_type_desc,
    state_epa_flag,
    actual_end_date,
    pollutant_codes,
    pollutant_descs,
    air_stack_test_status_code,
    air_stack_test_status_desc
from {{ ref('stg_fed_epa_icis_air_icis_air_stack_tests__records') }}
