{{ config(materialized='table', schema='FINANCE') }}

-- Source: FED_EPA_ICIS_FEC_CASE_FACILITIES (204019 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_ICIS_FEC_CASE_FACILITIES') }}
)

select
    ACTIVITY_ID as activity_id,
    CASE_NUMBER as case_number,
    REGISTRY_ID as registry_id,
    FACILITY_NAME as facility_name,
    LOCATION_ADDRESS as location_address,
    CITY as city,
    STATE_CODE as state_code,
    ZIP as zip,
    PRIMARY_SIC_CODE as primary_sic_code,
    PRIMARY_NAICS_CODE as primary_naics_code
from source
