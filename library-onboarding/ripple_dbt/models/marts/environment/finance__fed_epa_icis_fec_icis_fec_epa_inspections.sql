{{ config(materialized='table', schema='FINANCE') }}

-- Source: FED_EPA_ICIS_FEC_ICIS_FEC_EPA_INSPECTIONS (260556 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_ICIS_FEC_ICIS_FEC_EPA_INSPECTIONS') }}
)

select
    REGISTRY_ID as registry_id,
    PGM_SYS_ACRNM as pgm_sys_acrnm,
    PGM_SYS_ID as pgm_sys_id,
    FACILITY_NAME as facility_name,
    EPA_REGION_CODE as epa_region_code,
    ACTIVITY_ID as activity_id,
    ACTIVITY_TYPE_CODE as activity_type_code,
    ACTIVITY_TYPE_DESC as activity_type_desc,
    COMP_MONITOR_UID as comp_monitor_uid,
    COMP_MONITOR_TYPES as comp_monitor_types,
    STATUTE_CODE as statute_code,
    LAW_SECTION_CODE as law_section_code,
    try_to_date(ACTUAL_BEGIN_DATE) as actual_begin_date,
    try_to_date(ACTUAL_END_DATE) as actual_end_date,
    AGENCY as agency,
    COMP_MONITOR_AGENCY_TYPES as comp_monitor_agency_types
from source
