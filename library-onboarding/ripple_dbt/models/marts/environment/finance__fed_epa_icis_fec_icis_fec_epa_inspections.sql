{{ config(materialized='table', schema='FINANCE') }}

-- Source: FED_EPA_ICIS_FEC_ICIS_FEC_EPA_INSPECTIONS (260556 rows)
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with 2 bare try_to_date() casts, no
-- guard against a pure-digit value misreading as an epoch date. Not live-
-- checked column-by-column for this table specifically -- fixed as a landmine
-- regardless (see environment__fed_epa_sdwa_sdwa_violations_enforcement.sql /
-- environment__fed_epa_sdwa_sdwa_pub_water_systems.sql for the live before/
-- after proof run on this same bug pattern elsewhere in this batch). Column
-- list matches models/staging/fed_epa_icis_fec_icis_fec_epa_inspections
-- exactly, so swapped to ref() instead of hand-wrapping: same 16 columns, same
-- names, same order, both date casts regex-guarded (stg_date,
-- macros/staging_casts.sql). Bonus from the swap: the staging model drops 695
-- whole-row exact-duplicate records (ACTIVITY_ID is not unique per its own
-- DEDUPE note) that this mart previously had no dedup logic for at all.

select
    registry_id,
    pgm_sys_acrnm,
    pgm_sys_id,
    facility_name,
    epa_region_code,
    activity_id,
    activity_type_code,
    activity_type_desc,
    comp_monitor_uid,
    comp_monitor_types,
    statute_code,
    law_section_code,
    actual_begin_date,
    actual_end_date,
    agency,
    comp_monitor_agency_types
from {{ ref('stg_fed_epa_icis_fec_icis_fec_epa_inspections__records') }}
