{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_NPDES_NPDES_SICS (500000 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_NPDES_NPDES_SICS') }}
)

select
    NPDES_ID as npdes_id,
    SIC_CODE as sic_code,
    SIC_DESC as sic_desc,
    PRIMARY_INDICATOR_FLAG as primary_indicator_flag
from source
