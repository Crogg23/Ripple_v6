version: 2

models:
  - name: immigration__fed_epa_sdwa_sdwa_service_areas
    columns:
      - name: service_area_type_code
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - pwsid
              - service_area_type_code
              - submissionyearquarter
