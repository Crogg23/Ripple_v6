{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: Global Witness ? Land and Environmental Defenders Database (232 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'INTL_GLOBAL_WITNESS_DEFENDERS') }}
)

select
    DEFENDER_NAME as defender_name,
    try_to_number(COUNTRY) as country,
    REGION as region,
    try_to_number("YEAR") as year,
    INCIDENT_TYPE as incident_type,
    SECTOR as sector,
    INDIGENOUS_STATUS as indigenous_status,
    GENDER as gender,
    PERPETRATOR as perpetrator,
    SOURCE_REPORT as source_report,
    NOTES_CONTEXT as notes_context
from source
