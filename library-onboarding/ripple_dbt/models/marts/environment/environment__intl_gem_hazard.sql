{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: GEM Global Seismic Hazard Model (OpenQuake Mosaic) (12 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_intl_gem_hazard__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    model_version,
    country,
    fips,
    latitude,
    longitude,
    pga_10pct_50yr,
    vs30,
    hazard_model_id,
    doi
from {{ ref('stg_intl_gem_hazard__records') }}
