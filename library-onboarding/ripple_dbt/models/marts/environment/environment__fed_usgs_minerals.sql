{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: USGS Mineral Resources Data (304632 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_usgs_minerals__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    country,
    fips,
    commodity,
    year,
    production_quantity,
    production_unit,
    reserves,
    rank,
    import_reliance_pct,
    us_state,
    value_usd,
    data_source_notes,
    dep_id,
    url,
    mrds_id,
    mas_id,
    site_name,
    latitude,
    longitude,
    region,
    state,
    county,
    com_type,
    commod1,
    commod2,
    commod3,
    oper_type,
    dep_type,
    prod_size,
    dev_stat,
    ore,
    gangue,
    other_matl,
    orebody_fm,
    work_type,
    model,
    alteration,
    conc_proc,
    names,
    ore_ctrl,
    reporter,
    hrock_unit,
    hrock_type,
    arock_unit,
    arock_type,
    structure,
    tectonic,
    ref,
    yfp_ba,
    yr_fst_prd,
    ylp_ba,
    yr_lst_prd,
    dy_ba,
    disc_yr,
    prod_yrs,
    discr,
    score
from {{ ref('stg_fed_usgs_minerals__records') }}
