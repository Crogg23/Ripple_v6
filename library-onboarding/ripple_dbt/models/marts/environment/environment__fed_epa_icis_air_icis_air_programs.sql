{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_ICIS_AIR_ICIS_AIR_PROGRAMS (457333 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_ICIS_AIR_ICIS_AIR_PROGRAMS') }}
)

select
    PGM_SYS_ID as pgm_sys_id,
    PROGRAM_CODE as program_code,
    PROGRAM_DESC as program_desc,
    AIR_OPERATING_STATUS_CODE as air_operating_status_code,
    AIR_OPERATING_STATUS_DESC as air_operating_status_desc,
    try_to_date(BEGIN_DATE) as begin_date,
    try_to_date(UPDATED_DATE) as updated_date
from source
