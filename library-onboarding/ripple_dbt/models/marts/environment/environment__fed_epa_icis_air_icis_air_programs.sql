{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_ICIS_AIR_ICIS_AIR_PROGRAMS (457333 rows)
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: BEGIN_DATE and UPDATED_DATE were both bare try_to_date()
-- calls -- no format, no guard, so a pure-digit string would misread as
-- epoch seconds (checked live: try_to_date('2015') = 1970-01-01).
-- Live-checked both columns: all 457,581 filled rows on each already parse
-- clean via Snowflake's auto date detection (slash format, e.g.
-- '10/19/2014') and zero rows on either column are pure-digit -- a landmine
-- today, not an active bug. Swapped to the already-guarded staging model
-- instead of hand-wrapping the casts: same 7 columns, same names, same
-- order, and its whole-row dedup live-verified as a no-op on this table
-- today (457,581 raw = 457,581 deduped, no rows lost).

select
    pgm_sys_id,
    program_code,
    program_desc,
    air_operating_status_code,
    air_operating_status_desc,
    begin_date,
    updated_date
from {{ ref('stg_fed_epa_icis_air_icis_air_programs__records') }}
