{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_RCRA_RCRA_FACILITIES (1613224 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_RCRA_RCRA_FACILITIES') }}
)

select
    ID_NUMBER as id_number,
    FACILITY_NAME as facility_name,
    ACTIVITY_LOCATION as activity_location,
    FULL_ENFORCEMENT as full_enforcement,
    HREPORT_UNIVERSE_RECORD as hreport_universe_record,
    STREET_ADDRESS as street_address,
    CITY_NAME as city_name,
    STATE_CODE as state_code,
    ZIP_CODE as zip_code,
    try_to_double(LATITUDE83) as latitude83,
    try_to_double(LONGITUDE83) as longitude83,
    FED_WASTE_GENERATOR as fed_waste_generator,
    TRANSPORTER as transporter,
    ACTIVE_SITE as active_site,
    OPERATING_TSDF as operating_tsdf
from source
