{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_RCRA_RCRA_VIOSNC_HISTORY (2675581 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_RCRA_RCRA_VIOSNC_HISTORY') }}
)

select
    ID_NUMBER as id_number,
    ACTIVITY_LOCATION as activity_location,
    YRMONTH as yrmonth,
    VIO_FLAG as vio_flag,
    SNC_FLAG as snc_flag
from source
