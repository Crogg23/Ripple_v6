{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: OWID ? Global temperature anomaly (531 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_OWID_TEMP_ANOMALY') }}
)

select
    ENTITY as entity,
    CODE as code,
    try_to_number("YEAR") as year,
    AVERAGE as average,
    LOWER_BOUND as lower_bound,
    UPPER_BOUND as upper_bound
from source
