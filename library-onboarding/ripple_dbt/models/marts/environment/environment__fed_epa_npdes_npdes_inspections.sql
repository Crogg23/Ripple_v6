{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_NPDES_NPDES_INSPECTIONS (1,900,067 rows)  [row count re-verified against the live table 2026-08-11; header had said 500000]
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: ACTUAL_BEGIN_DATE/ACTUAL_END_DATE were bare try_to_date()
-- calls, no guard against a pure-digit value misreading as an epoch date. Not
-- live-checked column-by-column for this table specifically -- fixed as a
-- landmine regardless (see environment__fed_epa_sdwa_sdwa_violations_
-- enforcement.sql / environment__fed_epa_sdwa_sdwa_pub_water_systems.sql for
-- the live before/after proof run on this same bug pattern elsewhere in this
-- batch). No staging model exists yet for FED_EPA_NPDES_NPDES_INSPECTIONS, so
-- kept on source() and wrapped both calls in place with stg_date
-- (macros/staging_casts.sql).

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_NPDES_NPDES_INSPECTIONS') }}
)

select
    REGISTRY_ID as registry_id,
    NPDES_ID as npdes_id,
    ACTIVITY_ID as activity_id,
    ACTIVITY_TYPE_CODE as activity_type_code,
    COMP_MONITOR_TYPE_CODE as comp_monitor_type_code,
    COMP_MONITOR_TYPE_DESC as comp_monitor_type_desc,
    STATE_EPA_FLAG as state_epa_flag,
    {{ stg_date('"ACTUAL_BEGIN_DATE"') }} as actual_begin_date,
    {{ stg_date('"ACTUAL_END_DATE"') }} as actual_end_date,
    ACTIVITY_OUTCOME_CODE as activity_outcome_code,
    ACTIVITY_OUTCOME_DESC as activity_outcome_desc
from source
