{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_NPDES_NPDES_INSPECTIONS (500000 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_NPDES_NPDES_INSPECTIONS') }}
)

select
    REGISTRY_ID as registry_id,
    NPDES_ID as npdes_id,
    ACTIVITY_ID as activity_id,
    ACTIVITY_TYPE_CODE as activity_type_code,
    COMP_MONITOR_TYPE_CODE as comp_monitor_type_code,
    COMP_MONITOR_TYPE_DESC as comp_monitor_type_desc,
    STATE_EPA_FLAG as state_epa_flag,
    try_to_date(ACTUAL_BEGIN_DATE) as actual_begin_date,
    try_to_date(ACTUAL_END_DATE) as actual_end_date,
    ACTIVITY_OUTCOME_CODE as activity_outcome_code,
    ACTIVITY_OUTCOME_DESC as activity_outcome_desc
from source
