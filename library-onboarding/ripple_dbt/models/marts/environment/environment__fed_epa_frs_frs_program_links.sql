{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_FRS_FRS_PROGRAM_LINKS (500000 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_FRS_FRS_PROGRAM_LINKS') }}
)

select
    PGM_SYS_ACRNM as pgm_sys_acrnm,
    PGM_SYS_ID as pgm_sys_id,
    REGISTRY_ID as registry_id,
    PRIMARY_NAME as primary_name,
    LOCATION_ADDRESS as location_address,
    SUPPLEMENTAL_LOCATION as supplemental_location,
    CITY_NAME as city_name,
    COUNTY_NAME as county_name,
    FIPS_CODE as fips_code,
    STATE_CODE as state_code,
    STATE_NAME as state_name,
    COUNTRY_NAME as country_name,
    POSTAL_CODE as postal_code
from source
