{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_RCRA_RCRA_ENFORCEMENTS (383519 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_RCRA_RCRA_ENFORCEMENTS') }}
)

select
    ID_NUMBER as id_number,
    ACTIVITY_LOCATION as activity_location,
    ENFORCEMENT_IDENTIFIER as enforcement_identifier,
    ENFORCEMENT_TYPE as enforcement_type,
    ENFORCEMENT_DESC as enforcement_desc,
    ENFORCEMENT_AGENCY as enforcement_agency,
    try_to_date(ENFORCEMENT_ACTION_DATE) as enforcement_action_date,
    try_to_double(PMP_AMOUNT) as pmp_amount,
    try_to_double(FMP_AMOUNT) as fmp_amount,
    try_to_double(FSC_AMOUNT) as fsc_amount,
    try_to_double(SCR_AMOUNT) as scr_amount
from source
