{{ config(materialized='table', schema='FINANCE') }}

-- Source: FED_EPA_ICIS_FEC_EPA_INFORMAL_ENFORCEMENT_ACTIONS (21930 rows)
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: ACHIEVED_DATE was a bare try_to_date() call, no guard
-- against a pure-digit value misreading as an epoch date. Not live-checked for
-- this table specifically -- fixed as a landmine regardless (see
-- environment__fed_epa_sdwa_sdwa_violations_enforcement.sql /
-- environment__fed_epa_sdwa_sdwa_pub_water_systems.sql for the live before/
-- after proof run on this same bug pattern elsewhere in this batch). No
-- staging model exists yet for FED_EPA_ICIS_FEC_EPA_INFORMAL_ENFORCEMENT_
-- ACTIONS, so kept on source() and wrapped the one call in place with stg_date
-- (macros/staging_casts.sql).

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_ICIS_FEC_EPA_INFORMAL_ENFORCEMENT_ACTIONS') }}
)

select
    PGM_SYS_ACRNM as pgm_sys_acrnm,
    PGM_SYS_ID as pgm_sys_id,
    REGISTRY_ID as registry_id,
    AGENCY as agency,
    ACTIVITY_TYPE_CODE as activity_type_code,
    ENF_TYPE_CODE as enf_type_code,
    ENF_TYPE_DESC as enf_type_desc,
    {{ stg_date('"ACHIEVED_DATE"') }} as achieved_date,
    ENF_IDENTIFIER as enf_identifier,
    STATUTE as statute
from source
