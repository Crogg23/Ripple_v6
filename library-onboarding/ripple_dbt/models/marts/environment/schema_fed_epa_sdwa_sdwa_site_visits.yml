version: 2

models:
  - name: environment__fed_epa_sdwa_sdwa_site_visits
    columns:
      - name: visit_id
        data_tests:
          - not_null
      - name: visit_reason_code
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - submissionyearquarter
              - pwsid
              - visit_id
