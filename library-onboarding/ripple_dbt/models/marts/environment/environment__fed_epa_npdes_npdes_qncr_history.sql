{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: FED_EPA_NPDES_NPDES_QNCR_HISTORY (7,951,656 rows)  [row count re-verified against the live table 2026-08-11; header had said 500000]
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_EPA_NPDES_NPDES_QNCR_HISTORY') }}
)

select
    NPDES_ID as npdes_id,
    YEARQTR as yearqtr,
    HLRNC as hlrnc,
    NUME90Q as nume90_q,
    NUMCVDT as numcvdt,
    NUMSVCD as numsvcd,
    NUMPSCH as numpsch,
    NUMD8090Q as numd8090_q
from source
