{{ config(materialized='table', schema='ENVIRONMENT') }}

-- Source: OWID ? Annual CO2 emissions by country (29384 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_OWID_CO2') }}
)

select
    ENTITY as entity,
    CODE as code,
    try_to_number("YEAR") as year,
    ANNUAL_CO_EMISSIONS as annual_co_emissions
from source
