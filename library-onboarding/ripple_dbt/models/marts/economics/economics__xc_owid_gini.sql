{{ config(materialized='table', schema='ECONOMICS') }}

-- Source: OWID ? Gini index (income inequality) (2389 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_OWID_GINI') }}
)

select
    ENTITY as entity,
    CODE as code,
    try_to_number("YEAR") as year,
    GINI_COEFFICIENT as gini_coefficient,
    WORLD_REGION_ACCORDING_TO_OWID as world_region_according_to_owid
from source
