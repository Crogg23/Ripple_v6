version: 2

models:
  - name: economics__fed_irs_auto_revocations
    columns:
      - name: zip_code
        data_tests:
          - not_null
      - name: ein
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - ein
              - revocation_date
