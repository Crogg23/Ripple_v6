version: 2

models:
  - name: economics__fed_dol_form5500
    columns:
      - name: ack_id
        data_tests:
          - not_null
          - unique
      # blank string in source on these rows (type_pension_bnft_code: exactly 16,428, live 2026-09-20);
      # the guarded read now shows it as NULL. Real codes like '2E2G2R3D' are untouched.
      - name: business_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: type_pension_bnft_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: type_welfare_bnft_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: m1_receipt_confirmation_code
        data_tests:
          - not_null:
              config:
                severity: warn
