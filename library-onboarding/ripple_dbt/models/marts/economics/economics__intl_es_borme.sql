{{ config(materialized='table', schema='ECONOMICS') }}

-- Source: Spain BORME (Bolet?n Oficial del Registro Mercantil) (25 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'INTL_ES_BORME') }}
)

select
    "DATE" as date,
    BORME_ISSUE_NUMBER as borme_issue_number,
    SECTION as section,
    COMPANY_ID as company_id,
    COMPANY_NAME as company_name,
    ACT_TYPE as act_type,
    ACT_DESCRIPTION as act_description,
    PROVINCE as province,
    try_to_number(COUNTRY) as country,
    CVE as cve,
    PDF_URL as pdf_url
from source
