{{ config(materialized='table', schema='ECONOMICS') }}

-- Source: IRS 990 e-file Index (filer + XML URL, by year) (5544626 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_IRS_990_EFILE_INDEX') }}
)

select
    RETURN_ID as return_id,
    FILING_TYPE as filing_type,
    EIN as ein,
    TAX_PERIOD as tax_period,
    try_to_date(SUB_DATE) as sub_date,
    TAXPAYER_NAME as taxpayer_name,
    RETURN_TYPE as return_type,
    DLN as dln,
    OBJECT_ID as object_id
from source
