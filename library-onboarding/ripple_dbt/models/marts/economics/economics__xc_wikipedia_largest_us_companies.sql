{{ config(materialized='table', schema='ECONOMICS') }}

-- Source: XC_WIKIPEDIA_LARGEST_US_COMPANIES (100 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_WIKIPEDIA_LARGEST_US_COMPANIES') }}
)

select
    "RANK" as rank,
    NAME as name,
    INDUSTRY as industry,
    {{ ripple_num('REVENUE_USD_MILLIONS') }} as revenue_usd_millions,
    REVENUE_GROWTH as revenue_growth,
    {{ ripple_num('EMPLOYEES') }} as employees,
    HEADQUARTERS as headquarters
from source
