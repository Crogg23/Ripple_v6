{{ config(materialized='table', schema='ECONOMICS') }}

-- Source: Grants.gov Opportunity Search API (100 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_grants_gov__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    opportunity_id,
    opportunity_number,
    opportunity_title,
    agency_code,
    agency_name,
    opportunity_status,
    opportunity_category,
    funding_instrument_type,
    category_of_funding_activity,
    cfda_numbers,
    eligible_applicants,
    award_ceiling,
    award_floor,
    estimated_total_program_funding,
    expected_number_of_awards,
    posted_date,
    close_date,
    last_updated_date,
    archive_date,
    synopsis_desc,
    grantor_contact_email,
    grantor_contact_name,
    version
from {{ ref('stg_fed_grants_gov__records') }}
