{{ config(materialized='table', schema='ECONOMICS') }}

-- Source: FED_IRS_AUTO_REVOCATIONS (500000 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_IRS_AUTO_REVOCATIONS') }}
)

select
    EIN as ein,
    LEGAL_NAME as legal_name,
    DOING_BUSINESS_AS as doing_business_as,
    ORGANIZATION_ADDRESS as organization_address,
    CITY as city,
    STATE as state,
    ZIP_CODE as zip_code,
    COUNTRY as country,
    EXEMPTION_TYPE as exemption_type,
    try_to_date(REVOCATION_DATE) as revocation_date,
    try_to_date(REVOCATION_POSTING_DATE) as revocation_posting_date,
    try_to_date(REINSTATEMENT_DATE) as reinstatement_date
from source
