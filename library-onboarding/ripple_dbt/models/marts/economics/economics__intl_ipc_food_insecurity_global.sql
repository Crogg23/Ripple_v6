{{ config(materialized='table', schema='ECONOMICS') }}

-- Source: IPC Global Acute Food Insecurity Country Data (735 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_intl_ipc_food_insecurity_global__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    date_of_analysis,
    country,
    total_country_population,
    validity_period,
    c_from,
    c_to,
    phase,
    number,
    percentage
from {{ ref('stg_intl_ipc_food_insecurity_global__records') }}
