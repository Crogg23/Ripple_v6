{{ config(materialized='table', schema='ECONOMICS') }}

-- Source: FED_IRS_SOI_CHARITIES (2450 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_IRS_SOI_CHARITIES') }}
)

select
    EIN as ein,
    NAME as name,
    ICO as ico,
    STREET as street,
    CITY as city,
    STATE as state,
    ZIP as zip,
    C_GROUP as c_group,
    SUBSECTION as subsection,
    AFFILIATION as affiliation,
    CLASSIFICATION as classification,
    RULING as ruling,
    DEDUCTIBILITY as deductibility,
    FOUNDATION as foundation,
    ACTIVITY as activity,
    C_ORGANIZATION as c_organization,
    STATUS as status,
    TAX_PERIOD as tax_period,
    ASSET_CD as asset_cd,
    INCOME_CD as income_cd,
    FILING_REQ_CD as filing_req_cd,
    PF_FILING_REQ_CD as pf_filing_req_cd,
    ACCT_PD as acct_pd,
    try_to_double(ASSET_AMT) as asset_amt,
    try_to_double(INCOME_AMT) as income_amt,
    try_to_double(REVENUE_AMT) as revenue_amt,
    NTEE_CD as ntee_cd,
    SORT_NAME as sort_name
from source
