{{ config(materialized='table', schema='ECONOMICS') }}

-- Source: IRS Exempt Organizations BMF - Puerto Rico (2587 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_irs_eo_pr__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    ein,
    name,
    ico,
    street,
    city,
    state,
    zip,
    group_exemption,
    subsection,
    affiliation,
    classification,
    ruling,
    deductibility,
    foundation,
    activity,
    organization,
    status,
    tax_period,
    asset_cd,
    income_cd,
    filing_req_cd,
    pf_filing_req_cd,
    acct_pd,
    asset_amt,
    income_amt,
    revenue_amt,
    ntee_cd,
    sort_name
from {{ ref('stg_fed_irs_eo_pr__records') }}
