{{ config(materialized='table', schema='ECONOMICS') }}

-- Source: USAspending Top-Tier Federal Agencies (111 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_usaspending_toptier_agencies__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    agency_id,
    toptier_code,
    abbreviation,
    agency_name,
    congressional_justification_url,
    active_fy,
    active_fq,
    outlay_amount,
    obligated_amount,
    budget_authority_amount,
    current_total_budget_authority_amount,
    percentage_of_total_budget_authority,
    agency_slug
from {{ ref('stg_fed_usaspending_toptier_agencies__records') }}
