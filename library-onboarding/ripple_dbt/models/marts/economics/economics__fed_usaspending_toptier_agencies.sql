{{ config(materialized='table', schema='ECONOMICS') }}

-- Source: USAspending Top-Tier Federal Agencies (111 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_USASPENDING_TOPTIER_AGENCIES') }}
)

select
    AGENCY_ID as agency_id,
    TOPTIER_CODE as toptier_code,
    ABBREVIATION as abbreviation,
    AGENCY_NAME as agency_name,
    CONGRESSIONAL_JUSTIFICATION_URL as congressional_justification_url,
    ACTIVE_FY as active_fy,
    ACTIVE_FQ as active_fq,
    try_to_double(OUTLAY_AMOUNT) as outlay_amount,
    try_to_double(OBLIGATED_AMOUNT) as obligated_amount,
    try_to_double(BUDGET_AUTHORITY_AMOUNT) as budget_authority_amount,
    try_to_double(CURRENT_TOTAL_BUDGET_AUTHORITY_AMOUNT) as current_total_budget_authority_amount,
    try_to_double(PERCENTAGE_OF_TOTAL_BUDGET_AUTHORITY) as percentage_of_total_budget_authority,
    AGENCY_SLUG as agency_slug
from source
