{{ config(materialized='table', schema='ECONOMICS') }}

-- Source: Beneficial Ownership Information Registry (1 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_fincen_boi__organizations) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    fincen_id,
    reporting_company_name,
    jurisdiction_of_formation,
    us_registration_state,
    us_registration_date,
    ein,
    beneficial_owner_full_name,
    beneficial_owner_dob,
    beneficial_owner_address,
    beneficial_owner_id_type,
    beneficial_owner_id_number,
    beneficial_owner_id_issuing_jurisdiction,
    report_type,
    date_filed
from {{ ref('stg_fed_fincen_boi__organizations') }}
