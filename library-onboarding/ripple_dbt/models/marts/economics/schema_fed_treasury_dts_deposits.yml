version: 2

models:
  - name: economics__fed_treasury_dts_deposits
    columns:
      - name: record_date
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - record_date
              - account_type
              - transaction_type
              - transaction_catg
              - table_nbr
              - src_line_nbr
