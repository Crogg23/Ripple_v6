{{ config(materialized='table', schema='TRANSPORT') }}

-- Source: FRA Safety Data API (1 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_fra_safety__places) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    report_id,
    railroad_name,
    event_date,
    state_fips,
    county_fips,
    event_type,
    casualties,
    equipment_type,
    cause_code,
    narrative
from {{ ref('stg_fed_fra_safety__places') }}
