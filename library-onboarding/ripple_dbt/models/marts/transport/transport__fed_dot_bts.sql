{{ config(materialized='table', schema='TRANSPORT') }}

-- Source: BTS TranStats Data Portal (21 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_DOT_BTS') }}
)

select
    DATABASE_NAME as database_name,
    DESCRIPTION as description,
    PROFILE_URL as profile_url,
    MODE as mode,
    SUBJECT as subject,
    try_to_number("YEAR") as year,
    "MONTH" as month,
    CARRIER_CODE as carrier_code,
    ORIGIN as origin,
    DEST as dest,
    STATE_FIPS as state_fips,
    COUNTY_FIPS as county_fips
from source
