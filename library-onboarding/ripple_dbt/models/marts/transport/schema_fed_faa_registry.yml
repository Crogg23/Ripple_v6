# RETIRED 2026-08-22: tests for the disabled stale FAA twin mart
# (transport__fed_faa_registry). Kept as an empty shell so the retirement is
# visible in git; safe to delete along with the model files.
version: 2
