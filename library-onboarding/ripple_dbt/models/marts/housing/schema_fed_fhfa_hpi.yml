version: 2

models:
  - name: housing__fed_fhfa_hpi
    columns:
      - name: place_id
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - hpi_type
              - hpi_flavor
              - frequency
              - level
              - place_id
              - yr
              - period
