version: 2

models:
  - name: housing__fed_hud_assisted_housing_projects
    description: "HUD Picture of Subsidized Households: assisted-housing units and tenant profiles by project/program/geography. NEGATIVE values are HUD suppression/N-A sentinels (kept as published -- filter value >= 0 for analysis)."
    columns:
      - name: quarter
        data_tests: [not_null]
