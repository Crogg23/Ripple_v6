{{ config(materialized='table', schema='HOUSING') }}

-- Source: FHFA House Price Index (HPI) ? Master File (184807 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_FHFA_HPI') }}
)

select
    HPI_TYPE as hpi_type,
    HPI_FLAVOR as hpi_flavor,
    FREQUENCY as frequency,
    LEVEL as level,
    PLACE_NAME as place_name,
    PLACE_ID as place_id,
    YR as yr,
    PERIOD as period,
    try_to_double(INDEX_NSA) as index_nsa,
    try_to_double(INDEX_SA) as index_sa,
    RSTDERR as rstderr,
    NOTE as note
from source
