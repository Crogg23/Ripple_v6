version: 2

models:
  - name: politics__tx_lobby_awards
    description: "Texas lobby award/memento expenditures: one row per activity (lobbyactivityid unique)."
    columns:
      - name: activity_id
        data_tests: [not_null, unique]
      - name: filer_id
        data_tests: [not_null]
