version: 2

models:
  - name: politics__irs527_eain
    description: "IRS Form 8871 election authority identification numbers: one row per EAIN per form (form_id + eain_id unique)."
    columns:
      - name: form_id_number
        data_tests: [not_null]
