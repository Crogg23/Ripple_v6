version: 2

models:
  - name: education__fed_google_polads_advertiser_weekly_spend
    columns:
      - name: advertiser_id
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - advertiser_id
              - election_cycle
              - week_start_date
