version: 2

models:
  - name: politics__tx_lobby_entertainment
    description: "Texas lobby entertainment expenditures: one row per activity (lobbyactivityid unique)."
    columns:
      - name: activity_id
        data_tests: [not_null, unique]
      - name: filer_id
        data_tests: [not_null]
