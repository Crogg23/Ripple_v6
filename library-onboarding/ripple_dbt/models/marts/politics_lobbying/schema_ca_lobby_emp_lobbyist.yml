version: 2

models:
  - name: politics__ca_lobby_emp_lobbyist
    description: "CAL-ACCESS in-house lobbyist roster: one row per lobbyist-employer-session (unique)."
    columns:
      - name: lobbyist_id
        data_tests: [not_null]
      - name: employer_id
        data_tests: [not_null]
