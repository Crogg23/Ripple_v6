{{ config(materialized='table', schema='EDUCATION') }}

-- Source: FED_GOOGLE_POLADS_ADVERTISER_ID_MAPPING (8044 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_GOOGLE_POLADS_ADVERTISER_ID_MAPPING') }}
)

select
    OLDADVERTISERID as oldadvertiserid,
    NEWADVERTISERID as newadvertiserid
from source
