{{ config(materialized='table', schema='EDUCATION') }}

-- Source: EDFacts (33 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_ed_edfacts__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    school_year,
    state_fips,
    lea_id,
    school_id,
    agency_name,
    school_name,
    grade_span,
    total_enrollment,
    subgroup,
    program_participation,
    proficiency_level,
    assessment_subject,
    graduation_rate,
    chronic_absenteeism_rate,
    sea_id
from {{ ref('stg_fed_ed_edfacts__records') }}
