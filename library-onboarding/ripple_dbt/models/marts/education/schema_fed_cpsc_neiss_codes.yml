version: 2

models:
  - name: education__fed_cpsc_neiss_codes
    columns:
      - name: format_name
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - format_name
              - starting_value_for_format
