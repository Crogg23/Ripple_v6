version: 2

models:
  - name: reference__intl_eurostat
    columns:
      - name: dataflow_id
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - raw_xml_series_key
              - time
