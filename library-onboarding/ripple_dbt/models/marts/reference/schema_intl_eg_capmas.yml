version: 2

models:
  - name: reference__intl_eg_capmas
    columns:
      - name: indicator_name
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - indicator_name
              - category
              - year
              - country
              - source
