{{ config(materialized='table', schema='REFERENCE') }}

-- Source: USGS Historical Topographic Map Collection (topoView + TNM API) (250 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_usgs_topoview__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    title,
    mpdesc,
    sourceid,
    sourcename,
    datecreated,
    lastupdated,
    publicationdate,
    downloadurl,
    filesize,
    fileformat,
    datasets,
    boundingbox,
    fips,
    state,
    counties,
    mapscale,
    previewgraphicurl,
    metaurl
from {{ ref('stg_fed_usgs_topoview__records') }}
