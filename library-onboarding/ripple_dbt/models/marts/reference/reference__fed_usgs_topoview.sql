{{ config(materialized='table', schema='REFERENCE') }}

-- Source: USGS Historical Topographic Map Collection (topoView + TNM API) (250 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_USGS_TOPOVIEW') }}
)

select
    TITLE as title,
    MPDESC as mpdesc,
    SOURCEID as sourceid,
    SOURCENAME as sourcename,
    try_to_date(DATECREATED) as datecreated,
    try_to_date(LASTUPDATED) as lastupdated,
    PUBLICATIONDATE as publicationdate,
    DOWNLOADURL as downloadurl,
    FILESIZE as filesize,
    FILEFORMAT as fileformat,
    DATASETS as datasets,
    BOUNDINGBOX as boundingbox,
    FIPS as fips,
    STATE as state,
    COUNTIES as counties,
    MAPSCALE as mapscale,
    PREVIEWGRAPHICURL as previewgraphicurl,
    METAURL as metaurl
from source
