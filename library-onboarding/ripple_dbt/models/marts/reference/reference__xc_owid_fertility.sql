{{ config(materialized='table', schema='REFERENCE') }}

-- Source: OWID ? Fertility rate (children per woman) (19402 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_OWID_FERTILITY') }}
)

select
    ENTITY as entity,
    CODE as code,
    try_to_number("YEAR") as year,
    try_to_double(TOTAL_FERTILITY_RATE) as total_fertility_rate
from source
