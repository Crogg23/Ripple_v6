{{ config(materialized='table', schema='REFERENCE') }}

-- SAMPLE ONLY -- NOT the full dataset. A 1,015-row slice of a corpus that is effectively unbounded (GDELT advertises hundreds of millions of events, growing every 15 minutes). Use for shape/testing only. Labeled 2026-08-11 (verification sweep, defect class 7).
-- Source: GDELT 2.0 Global Events Database (1015 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_intl_gdelt__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    globaleventid,
    sqldate,
    monthyear,
    year,
    fractiondate,
    actor1code,
    actor1name,
    actor1countrycode,
    actor1type1code,
    actor2code,
    actor2name,
    actor2countrycode,
    actor2type1code,
    isrootevent,
    eventcode,
    eventbasecode,
    eventrootcode,
    quadclass,
    goldsteinscale,
    nummentions,
    numsources,
    numarticles,
    avgtone,
    actor1geo_type,
    actor1geo_fullname,
    actor1geo_countrycode,
    actor1geo_lat,
    actor1geo_long,
    actor2geo_type,
    actor2geo_fullname,
    actor2geo_countrycode,
    actor2geo_lat,
    actor2geo_long,
    actiongeo_type,
    actiongeo_fullname,
    actiongeo_countrycode,
    actiongeo_lat,
    actiongeo_long,
    dateadded,
    sourceurl,
    extra_col_0,
    extra_col_1,
    extra_col_2,
    extra_col_3,
    extra_col_4,
    extra_col_5,
    extra_col_6,
    extra_col_7,
    extra_col_8,
    extra_col_9,
    extra_col_10,
    extra_col_11,
    extra_col_12,
    extra_col_13,
    extra_col_14,
    extra_col_15,
    extra_col_16,
    extra_col_17,
    extra_col_18,
    extra_col_19,
    extra_col_20
from {{ ref('stg_intl_gdelt__records') }}
