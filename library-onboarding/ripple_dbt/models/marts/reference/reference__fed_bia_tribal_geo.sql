{{ config(materialized='table', schema='REFERENCE') }}

-- Source: BIA Open Data Geospatial Portal (100 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_BIA_TRIBAL_GEO') }}
)

select
    OBJECTID as objectid,
    LAYER_NAME as layer_name,
    NAME as name,
    FIPS as fips,
    STATE as state,
    AREA_SQMI as area_sqmi,
    GEOMETRY as geometry,
    DATA_SOURCE as data_source,
    try_to_date(LAST_UPDATED) as last_updated
from source
