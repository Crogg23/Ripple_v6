{{ config(materialized='table', schema='REFERENCE') }}

-- Source: Eurostat SDMX Dissemination API (450 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'INTL_EUROSTAT') }}
)

select
    DATAFLOW_ID as dataflow_id,
    GEO as geo,
    "TIME" as time,
    try_to_double(OBS_VALUE) as obs_value,
    OBS_STATUS as obs_status,
    UNIT as unit,
    FREQ as freq,
    INDICATOR as indicator,
    RAW_XML_SERIES_KEY as raw_xml_series_key
from source
