version: 2

models:
  - name: reference__fed_itis_geographic_div
    description: "ITIS (Integrated Taxonomic Information System, USDA/USGS taxonomy) reference data — Geographic divisions (continental/oceanic regions) in which each taxon occurs; one row per TSN x geographic value. Grain: one row per tsn, geographic_value (480,351 rows)."
    columns:
      - name: itis_geographic_div_key
        data_tests: [not_null, unique]
