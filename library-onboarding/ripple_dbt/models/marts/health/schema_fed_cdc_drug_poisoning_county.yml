version: 2

models:
  - name: health__fed_cdc_drug_poisoning_county
    columns:
      - name: fips
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - fips
              - year
