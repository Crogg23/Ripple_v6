{{ config(materialized='table', schema='HEALTH') }}

-- Source: FED_CMS_MEDICARE_DIALYSIS_FACILITIES (500000 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_CMS_MEDICARE_DIALYSIS_FACILITIES') }}
)

select
    STATE as state,
    CCN as ccn,
    PROVIDER_NAME as provider_name,
    CITY as city,
    OWNERSHIP_TYPE as ownership_type,
    ESRD_NETWORK as esrd_network,
    NPI as npi,
    CHAIN as chain,
    MODALITY as modality,
    ALTERNATE_CCNS as alternate_ccns,
    MEASURE as measure,
    MEASURE_SCORE as measure_score,
    try_to_number("YEAR") as year_col,
    MEASURE_ID as measure_id
from source
