version: 2

models:
  - name: health__fed_cdc_health_insurance
    columns:
      - name: quartile_number
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - indicator
              - c_group
              - state
              - subgroup
              - phase
              - time_period
              - time_period_label
