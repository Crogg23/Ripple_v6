{{ config(materialized='table', schema='HEALTH') }}

-- Source: NCHS Drug Poisoning Mortality by County (53387 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_CDC_DRUG_POISONING_COUNTY') }}
)

select
    FIPS as fips,
    try_to_number("YEAR") as year,
    STATE as state,
    ST as st,
    FIPS_STATE as fips_state,
    COUNTY as county,
    try_to_double(POPULATION) as population,
    try_to_double(ESTIMATED_AGE_ADJUSTED_DEATH_RATE_11_CATEGORIES_IN_RANGES) as estimated_age_adjusted_death_rate_11_categories_in_ranges
from source
