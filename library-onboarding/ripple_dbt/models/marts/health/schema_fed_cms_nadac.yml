version: 2

models:
  - name: health__fed_cms_nadac
    columns:
      - name: ndc
        data_tests:
          - not_null
      - name: explanation_code
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - ndc
              - effective_date
