version: 2

models:
  - name: health__fed_cdc_injury_violence_county
    columns:
      - name: geoid
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - geoid
              - intent
              - period
