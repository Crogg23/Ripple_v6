version: 2

models:
  - name: health__fed_cms_medicare_dialysis_facilities
    columns:
      - name: npi
        description: >
          45,002 of 12,456,456 rows (0.36%) are NULL (found after reloading
          the full table 2026-08-06, was truncated at 500,000 before). This
          is a wide, sparse quality-measure table (most cells blank by
          design); a facility-measure row without an individual provider NPI
          is plausible, not clearly a bug. Softened to warn rather than
          treated as broken data.
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: measure_id
        data_tests:
          - not_null
