{{ config(materialized='table', schema='HEALTH') }}

-- Source: FED_CMS_OPT_OUT_AFFIDAVITS (57209 rows)
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_date() calls on
-- OPTOUT_EFFECTIVE_DATE, OPTOUT_END_DATE and LAST_UPDATED -- unguarded
-- against the epoch trap (a pure-digit value silently reads as epoch
-- seconds). Live-checked all three columns today: 57,209 of 57,209 rows are
-- ordinary dashed dates, 0 pure-digit rows -- clean today, but a landmine on
-- the next load, not a live bug. Now reads the already-guarded staging model
-- instead -- same 13 columns the mart uses, same names, same order (the
-- staging model also carries a computed spine_entity_id and load-metadata
-- columns this mart doesn't select); all three date casts run through
-- stg_date (macros/staging_casts.sql), which regex-guards first and reads
-- NULL instead of a wrong date.

select
    first_name,
    last_name,
    npi,
    specialty,
    optout_effective_date,
    optout_end_date,
    first_line_street_address,
    second_line_street_address,
    city_name,
    state_code,
    zip_code,
    eligible_to_order_and_refer,
    last_updated
from {{ ref('stg_fed_cms_opt_out_affidavits__providers') }}
