{{ config(materialized='table', schema='HEALTH') }}

-- Source: FED_CMS_OPT_OUT_AFFIDAVITS (57209 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_CMS_OPT_OUT_AFFIDAVITS') }}
)

select
    FIRST_NAME as first_name,
    LAST_NAME as last_name,
    NPI as npi,
    SPECIALTY as specialty,
    try_to_date(OPTOUT_EFFECTIVE_DATE) as optout_effective_date,
    try_to_date(OPTOUT_END_DATE) as optout_end_date,
    FIRST_LINE_STREET_ADDRESS as first_line_street_address,
    SECOND_LINE_STREET_ADDRESS as second_line_street_address,
    CITY_NAME as city_name,
    STATE_CODE as state_code,
    ZIP_CODE as zip_code,
    ELIGIBLE_TO_ORDER_AND_REFER as eligible_to_order_and_refer,
    try_to_date(LAST_UPDATED) as last_updated
from source
