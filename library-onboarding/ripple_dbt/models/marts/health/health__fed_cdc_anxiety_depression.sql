{{ config(materialized='table', schema='HEALTH') }}

-- Source: CDC Indicators of Anxiety or Depression (16794 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_CDC_ANXIETY_DEPRESSION') }}
)

select
    INDICATOR as indicator,
    C_GROUP as c_group,
    STATE as state,
    SUBGROUP as subgroup,
    PHASE as phase,
    TIME_PERIOD as time_period,
    TIME_PERIOD_LABEL as time_period_label,
    try_to_timestamp(TIME_PERIOD_START_DATE) as time_period_start_date,
    try_to_timestamp(TIME_PERIOD_END_DATE) as time_period_end_date,
    try_to_double("VALUE") as value,
    LOWCI as lowci,
    HIGHCI as highci,
    CONFIDENCE_INTERVAL as confidence_interval,
    QUARTILE_RANGE as quartile_range
from source
