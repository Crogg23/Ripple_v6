{{ config(materialized='table', schema='HEALTH') }}

-- Source: CDC Open Data Portal (data.cdc.gov) (15000 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_CDC_DATA_PORTAL') }}
)

select
    DATASET_ID as dataset_id,
    INDICATOR as indicator,
    try_to_number("YEAR") as year,
    STATE as state,
    FIPS as fips,
    ZIP_CODE as zip_code,
    try_to_double("VALUE") as value,
    UNIT as unit,
    try_to_double(DATA_VALUE_TYPE) as data_value_type,
    TOPIC as topic,
    CATEGORY as category,
    SOURCE as source,
    try_to_date(UPDATED_AT) as updated_at
from source
