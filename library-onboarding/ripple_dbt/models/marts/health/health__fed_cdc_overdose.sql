{{ config(materialized='table', schema='HEALTH') }}

-- Source: CDC VSRR Provisional Drug Overdose Death Counts (83790 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_cdc_overdose__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    state,
    year,
    month,
    period,
    indicator,
    data_value,
    percent_complete,
    percent_pending_investigation,
    state_name,
    footnote,
    footnote_symbol,
    predicted_value
from {{ ref('stg_fed_cdc_overdose__records') }}
