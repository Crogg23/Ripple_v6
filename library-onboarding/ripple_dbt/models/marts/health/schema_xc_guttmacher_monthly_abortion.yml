version: 2

models:
  - name: health__xc_guttmacher_monthly_abortion
    columns:
      - name: state
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - state
              - month
