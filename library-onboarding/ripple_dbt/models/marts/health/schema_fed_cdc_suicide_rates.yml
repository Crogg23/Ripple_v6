version: 2

models:
  - name: health__fed_cdc_suicide_rates
    columns:
      - name: indicator
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - indicator
              - unit
              - stub_name
              - stub_label
              - year
              - age
