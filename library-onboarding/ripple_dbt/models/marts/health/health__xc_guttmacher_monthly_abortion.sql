{{ config(materialized='table', schema='HEALTH') }}

-- Source: xc_guttmacher_monthly_abortion (2040 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_GUTTMACHER_MONTHLY_ABORTION') }}
)

select
    STATE as state,
    "MONTH" as month,
    {{ ripple_num('MEDIAN') }} as median,
    LOWERBOUND as lowerbound,
    UPPERBOUND as upperbound,
    SOURCE as source,
    NOTES as notes,
    PUBLISHDATE as publishdate
from source
