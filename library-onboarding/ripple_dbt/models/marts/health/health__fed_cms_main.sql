{{ config(materialized='table', schema='HEALTH') }}

-- Source: CMS Data Portal (data.cms.gov) (158 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_CMS_MAIN') }}
)

select
    DATASET_ID as dataset_id,
    DATASET_TITLE as dataset_title,
    PUBLISHER as publisher,
    DESCRIPTION as description,
    try_to_date(UPDATE_FREQUENCY) as update_frequency,
    FORMAT as format,
    ACCESS_URL as access_url,
    NPI as npi,
    FIPS as fips,
    ZIP as zip
from source
