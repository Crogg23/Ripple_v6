version: 2

models:
  - name: health__fed_hrsa_uds_table3a_patients
    description: "HRSA UDS Table 3A: federally-funded health center patient counts by age/sex line (bhcmisid unique). Column pairs are (male, female) per age line."
    columns:
      - name: bhcmisid
        data_tests: [not_null, unique]
