{{ config(materialized='table', schema='HEALTH') }}

-- Source: VA National Veteran Suicide Data Appendix 2021-2023 (144 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_va_suicide_appendix__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    veteran_deaths_by_suicide_unadjusted_and_adjusted_rates_by_sex_2001_23,
    col_1,
    col_2,
    col_3,
    col_4,
    col_5,
    col_6,
    col_7,
    col_8,
    col_9,
    col_10,
    col_11,
    col_12
from {{ ref('stg_fed_va_suicide_appendix__records') }}
