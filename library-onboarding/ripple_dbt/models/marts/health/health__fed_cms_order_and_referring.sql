{{ config(materialized='table', schema='HEALTH') }}

-- Source: FED_CMS_ORDER_AND_REFERRING (2,018,354 rows)  [row count re-verified against the live table 2026-08-11; header had said 500000]
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_CMS_ORDER_AND_REFERRING') }}
)

select
    NPI as npi,
    LAST_NAME as last_name,
    FIRST_NAME as first_name,
    PARTB as partb,
    DME as dme,
    HHA as hha,
    PMD as pmd,
    HOSPICE as hospice
from source
