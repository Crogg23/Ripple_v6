{{ config(materialized='table', schema='HEALTH') }}

-- Source: FED_CMS_FISCAL_INTERMEDIARY_SHARED_SYSTEM_ATTENDING_AND_RENDERING (2,047,828 rows)  [row count re-verified against the live table 2026-08-11; header had said 500000]
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_CMS_FISCAL_INTERMEDIARY_SHARED_SYSTEM_ATTENDING_AND_RENDERING') }}
)

select
    NPI as npi,
    LAST_NAME as last_name,
    FIRST_NAME as first_name
from source
