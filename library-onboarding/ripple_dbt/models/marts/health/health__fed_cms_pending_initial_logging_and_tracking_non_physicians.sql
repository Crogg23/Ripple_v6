{{ config(materialized='table', schema='HEALTH') }}

-- Source: FED_CMS_PENDING_INITIAL_LOGGING_AND_TRACKING_NON_PHYSICIANS (6880 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_CMS_PENDING_INITIAL_LOGGING_AND_TRACKING_NON_PHYSICIANS') }}
)

select
    NPI as npi,
    LAST_NAME as last_name,
    FIRST_NAME as first_name
from source
