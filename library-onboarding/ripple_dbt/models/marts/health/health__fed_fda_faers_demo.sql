{{ config(materialized='table', schema='HEALTH') }}

-- Source: FED_FDA_FAERS_DEMO (5811086 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_FDA_FAERS_DEMO') }}
)

select
    ISR as isr,
    C_CASE as c_case,
    I_F_COD as i_f_cod,
    FOLL_SEQ as foll_seq,
    IMAGE as image,
    {{ ripple_dt('EVENT_DT') }} as event_dt,
    {{ ripple_dt('MFR_DT') }} as mfr_dt,
    FDA_DT as fda_dt,
    REPT_COD as rept_cod,
    MFR_NUM as mfr_num,
    MFR_SNDR as mfr_sndr,
    AGE as age,
    AGE_COD as age_cod,
    GNDR_COD as gndr_cod,
    E_SUB as e_sub,
    WT as wt,
    WT_COD as wt_cod,
    REPT_DT as rept_dt,
    OCCP_COD as occp_cod,
    DEATH_DT as death_dt,
    TO_MFR as to_mfr,
    CONFID as confid,
    _SRC_QUARTER as src_quarter,
    REPORTER_COUNTRY as reporter_country,
    PRIMARYID as primaryid,
    CASEID as caseid,
    CASEVERSION as caseversion,
    I_F_CODE as i_f_code,
    {{ ripple_dt('INIT_FDA_DT') }} as init_fda_dt,
    OCCR_COUNTRY as occr_country
from source
