{{ config(materialized='table', schema='IMMIGRATION') }}

-- Source: FED_CMS_MEDICARE_FEE_FOR_SERVICE_PUBLIC_PROVIDER_ENROLLMENT (2,978,925 rows)  [row count re-verified against the live table 2026-08-11; header had said 500000]
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_CMS_MEDICARE_FEE_FOR_SERVICE_PUBLIC_PROVIDER_ENROLLMENT') }}
)

select
    NPI as npi,
    MULTIPLE_NPI_FLAG as multiple_npi_flag,
    try_to_number(PECOS_ASCT_CNTL_ID) as pecos_asct_cntl_id,
    ENRLMT_ID as enrlmt_id,
    PROVIDER_TYPE_CD as provider_type_cd,
    PROVIDER_TYPE_DESC as provider_type_desc,
    STATE_CD as state_cd,
    FIRST_NAME as first_name,
    MDL_NAME as mdl_name,
    LAST_NAME as last_name,
    ORG_NAME as org_name
from source
