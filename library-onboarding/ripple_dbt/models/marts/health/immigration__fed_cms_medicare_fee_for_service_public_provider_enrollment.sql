{{ config(materialized='table', schema='IMMIGRATION') }}

-- Source: FED_CMS_MEDICARE_FEE_FOR_SERVICE_PUBLIC_PROVIDER_ENROLLMENT (2,978,925 rows)  [row count re-verified against the live table 2026-08-11; header had said 500000]
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: PECOS_ASCT_CNTL_ID was a bare try_to_number() -- unguarded,
-- so any non-whole value would silently round instead of reading NULL.
-- Live-checked a 1M-row sample of this table directly: 0 fractional values,
-- 0 non-numeric filled values -- clean today, a landmine not a live bug. A
-- matching staging model exists (stg_fed_cms_medicare_fee_for_service_
-- public_provider_enrollment__records) but its own header says it found "no
-- TEXT column where every filled value parsed as a number, date or boolean"
-- and leaves this column as plain TEXT -- swapping to ref() would silently
-- change this mart's numeric column to text, so this stays on source() and
-- only the bare cast is wrapped, with stg_int (macros/staging_casts.sql),
-- keeping the mart's existing whole-number type.

with source as (
    select * from {{ source('ripple_raw', 'FED_CMS_MEDICARE_FEE_FOR_SERVICE_PUBLIC_PROVIDER_ENROLLMENT') }}
)

select
    NPI as npi,
    MULTIPLE_NPI_FLAG as multiple_npi_flag,
    {{ stg_int('PECOS_ASCT_CNTL_ID') }} as pecos_asct_cntl_id,
    ENRLMT_ID as enrlmt_id,
    PROVIDER_TYPE_CD as provider_type_cd,
    PROVIDER_TYPE_DESC as provider_type_desc,
    STATE_CD as state_cd,
    FIRST_NAME as first_name,
    MDL_NAME as mdl_name,
    LAST_NAME as last_name,
    ORG_NAME as org_name
from source
