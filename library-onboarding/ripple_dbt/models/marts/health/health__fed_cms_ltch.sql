{{ config(materialized='table', schema='HEALTH') }}

-- Source: CMS Care Compare ? Long-Term Care Hospital General Information (311 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_CMS_LTCH') }}
)

select
    CCN as ccn,
    PROVIDER_NAME as provider_name,
    ADDRESS_LINE_1 as address_line_1,
    ADDRESS_LINE_2 as address_line_2,
    CITY_TOWN as city_town,
    STATE as state,
    ZIP_CODE as zip_code,
    COUNTY_PARISH as county_parish,
    TELEPHONE_NUMBER as telephone_number,
    CMS_REGION as cms_region,
    OWNERSHIP_TYPE as ownership_type,
    try_to_date(CERTIFICATION_DATE) as certification_date,
    try_to_double(TOTAL_NUMBER_OF_BEDS) as total_number_of_beds
from source
