{{ config(materialized='table', schema='HEALTH') }}

-- Source: CMS Care Compare ? Long-Term Care Hospital General Information (311 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_cms_ltch__facilities) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    ccn,
    provider_name,
    address_line_1,
    address_line_2,
    city_town,
    state,
    zip_code,
    county_parish,
    telephone_number,
    cms_region,
    ownership_type,
    certification_date,
    total_number_of_beds
from {{ ref('stg_fed_cms_ltch__facilities') }}
