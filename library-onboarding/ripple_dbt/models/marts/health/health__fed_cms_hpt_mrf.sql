{{ config(materialized='table', schema='HEALTH') }}

-- Source: Hospital Price Transparency Machine-Readable Files (1 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_cms_hpt_mrf__providers) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    hospital_name,
    hospital_location,
    hospital_address,
    hospital_licensure_information,
    npi,
    mrf_date,
    cms_template_version,
    attestation_statement,
    attester_name,
    general_description,
    setting,
    billing_code,
    code_type,
    modifiers,
    payer_name,
    plan_name,
    standard_charge_method,
    gross_charge,
    discounted_cash_price,
    payer_specific_negotiated_charge_dollar,
    payer_specific_negotiated_charge_percentage,
    payer_specific_negotiated_charge_algorithm,
    median_allowed_amount,
    tenth_percentile_allowed_amount,
    ninetieth_percentile_allowed_amount,
    count_of_allowed_amounts,
    de_identified_min_negotiated_charge,
    de_identified_max_negotiated_charge,
    drug_unit_of_measurement,
    drug_type_of_measurement,
    additional_generic_notes,
    additional_payer_specific_notes
from {{ ref('stg_fed_cms_hpt_mrf__providers') }}
