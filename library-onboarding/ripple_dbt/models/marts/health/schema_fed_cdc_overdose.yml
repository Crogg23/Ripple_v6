version: 2

models:
  - name: health__fed_cdc_overdose
    columns:
      - name: state
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - state
              - year
              - month
              - indicator
