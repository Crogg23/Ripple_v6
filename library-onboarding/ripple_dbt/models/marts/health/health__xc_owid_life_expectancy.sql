{{ config(materialized='table', schema='HEALTH') }}

-- Source: OWID ? Life expectancy (21565 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_OWID_LIFE_EXPECTANCY') }}
)

select
    ENTITY as entity,
    CODE as code,
    try_to_number("YEAR") as year,
    LIFE_EXPECTANCY as life_expectancy
from source
