{{ config(materialized='table', schema='HEALTH') }}

-- Source: CDC Mapping Injury, Overdose, and Violence - County (132000 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: bare try_to_number()/try_to_double()/try_to_date() casts replaced in place with the project's guarded macros (stg_int/stg_float/stg_date, macros/staging_casts.sql) -- a value that doesn't parse cleanly now reads NULL instead of being silently rounded or misparsed. Kept on source() (not ref()) because the staging model exposes fewer raw columns than this mart needs.

with source as (
    select * from {{ source('ripple_raw', 'FED_CDC_INJURY_VIOLENCE_COUNTY') }}
)

select
    GEOID as geoid,
    NAME as name,
    ST_GEOID as st_geoid,
    ST_NAME as st_name,
    INTENT as intent,
    PERIOD as period,
    {{ stg_int('"COUNT_SUP"') }} as count_sup,
    {{ stg_float('"RATE"') }} as rate,
    {{ stg_float('"RATE_M"') }} as rate_m,
    {{ stg_float('"RATE_M_CI"') }} as rate_m_ci,
    DATA_AS_OF as data_as_of,
    nullif(trim(TTM_DATE_RANGE), '') as ttm_date_range
from source
