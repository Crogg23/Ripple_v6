version: 2

models:
  - name: health__fed_cms_fiscal_intermediary_shared_system_attending_and_rendering
    columns:
      - name: npi
        description: >
          2 of 2,047,828 NPIs repeat (found after reloading the full table
          2026-08-06, was truncated at 500,000 before). Trivial rate
          (0.0001%), likely a provider appearing under two intermediary
          records -- not a bug worth failing the build over.
        data_tests:
          - not_null
          - unique:
              config:
                severity: warn
