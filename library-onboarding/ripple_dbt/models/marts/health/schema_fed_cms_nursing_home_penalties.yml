version: 2

models:
  - name: health__fed_cms_nursing_home_penalties
    columns:
      - name: zip_code
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - cms_certification_number_ccn
              - penalty_date
              - penalty_type
              - fine_id
