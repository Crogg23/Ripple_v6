version: 2

models:
  - name: health__fed_hrsa_uds_health_center_info
    description: "HRSA UDS health center directory: one row per federally-funded health center (bhcmisid unique) with address, director, funding streams, urban/rural flag."
    columns:
      - name: bhcmisid
        data_tests: [not_null, unique]
      - name: healthcentername
        data_tests: [not_null]
