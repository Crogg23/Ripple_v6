{{ config(materialized='table', schema='OPEN_DATA') }}

-- Source: Ghana Open Data Initiative (data.gov.gh) (10 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'INTL_GH_DATAGOVGH') }}
)

select
    DATASET_TITLE as dataset_title,
    CATALOG as catalog,
    MINISTRY_AGENCY as ministry_agency,
    DESCRIPTION as description,
    RESOURCE_URL as resource_url,
    FORMAT as format,
    try_to_date(DATE_PUBLISHED) as date_published,
    LICENSE as license,
    try_to_number(VIEWS_COUNT) as views_count,
    try_to_number(DOWNLOADS_COUNT) as downloads_count
from source
