{{ config(materialized='table', schema='OPEN_DATA') }}

-- Source: Ghana Open Data Initiative (data.gov.gh) (10 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_intl_gh_datagovgh__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    dataset_title,
    catalog,
    ministry_agency,
    description,
    resource_url,
    format,
    date_published,
    license,
    views_count,
    downloads_count
from {{ ref('stg_intl_gh_datagovgh__records') }}
