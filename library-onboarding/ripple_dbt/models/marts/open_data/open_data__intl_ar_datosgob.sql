{{ config(materialized='table', schema='OPEN_DATA') }}

-- Source: Argentina National Open Data Portal (datos.gob.ar) (3556 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_intl_ar_datosgob__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    dataset_id,
    dataset_title,
    c_organization,
    resource_url,
    format,
    last_modified,
    license,
    description,
    tags,
    serie_id,
    serie_value,
    serie_date,
    provincia_id,
    municipio_id,
    departamento_id
from {{ ref('stg_intl_ar_datosgob__records') }}
