{{ config(materialized='table', schema='OPEN_DATA') }}

-- Source: Argentina National Open Data Portal (datos.gob.ar) (3556 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'INTL_AR_DATOSGOB') }}
)

select
    DATASET_ID as dataset_id,
    DATASET_TITLE as dataset_title,
    C_ORGANIZATION as c_organization,
    RESOURCE_URL as resource_url,
    FORMAT as format,
    LAST_MODIFIED as last_modified,
    LICENSE as license,
    DESCRIPTION as description,
    TAGS as tags,
    SERIE_ID as serie_id,
    try_to_double(SERIE_VALUE) as serie_value,
    try_to_date(SERIE_DATE) as serie_date,
    PROVINCIA_ID as provincia_id,
    MUNICIPIO_ID as municipio_id,
    DEPARTAMENTO_ID as departamento_id
from source
