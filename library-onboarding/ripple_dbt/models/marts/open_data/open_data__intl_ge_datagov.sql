{{ config(materialized='table', schema='OPEN_DATA') }}

-- Source: Georgia National Open Data Portal (data.gov.ge) (1 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_intl_ge_datagov__places) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    dataset_id,
    dataset_title,
    publisher_institution,
    policy_area,
    date_published,
    date_updated,
    format,
    download_url,
    description,
    license,
    country,
    ogp_commitment_id
from {{ ref('stg_intl_ge_datagov__places') }}
