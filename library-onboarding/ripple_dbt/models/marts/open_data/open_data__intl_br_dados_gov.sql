{{ config(materialized='table', schema='OPEN_DATA') }}

-- Source: dados.gov.br (10 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_intl_br_dados_gov__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    dataset_id,
    title,
    c_organization,
    theme,
    num_resources,
    download_count,
    last_updated,
    license,
    description,
    resource_urls,
    tags,
    geographic_coverage,
    temporal_coverage
from {{ ref('stg_intl_br_dados_gov__records') }}
