{{ config(materialized='table', schema='OPEN_DATA') }}

-- Source: Wayback CDX Census ? DOJ Epstein Library Listing Pages (1537352 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_WAYBACK_DOJ_EPSTEIN') }}
)

select
    RECORD as record
from source
