{{ config(materialized='table', schema='HISTORY') }}

-- Source: David Rumsey Historical Map Collection (IIIF + LUNA API) (10 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_DAVID_RUMSEY') }}
)

select
    MANIFEST_ID as manifest_id,
    LABEL as label,
    DESCRIPTION as description,
    try_to_date(DATE_PUBLISHED) as date_published,
    AUTHOR as author,
    SUBJECT as subject,
    THUMBNAIL_URL as thumbnail_url,
    IIIF_IMAGE_URL as iiif_image_url,
    LUNA_RECORD_ID as luna_record_id,
    RIGHTS as rights,
    GEO_COVERAGE as geo_coverage,
    SCALE as scale,
    PHYSICAL_DIMENSIONS as physical_dimensions
from source
