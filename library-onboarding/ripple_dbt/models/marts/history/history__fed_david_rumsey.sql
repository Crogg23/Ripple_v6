{{ config(materialized='table', schema='HISTORY') }}

-- SAMPLE ONLY -- NOT the full dataset. A 10-item slice; the publisher advertises over 150,000 items online. Use for shape/testing only. Labeled 2026-08-11 (verification sweep, defect class 7).
-- Source: David Rumsey Historical Map Collection (IIIF + LUNA API) (10 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_david_rumsey__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    manifest_id,
    label,
    description,
    date_published,
    author,
    subject,
    thumbnail_url,
    iiif_image_url,
    luna_record_id,
    rights,
    geo_coverage,
    scale,
    physical_dimensions
from {{ ref('stg_fed_david_rumsey__records') }}
