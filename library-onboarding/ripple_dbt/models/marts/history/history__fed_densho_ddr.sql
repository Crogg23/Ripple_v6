{{ config(materialized='table', schema='HISTORY') }}

-- Source: Densho Digital Repository (DDR) (25 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_DENSHO_DDR') }}
)

select
    OBJECT_ID as object_id,
    COLLECTION_ID as collection_id,
    TITLE as title,
    OBJECT_TYPE as object_type,
    NARRATOR as narrator,
    FACILITY as facility,
    TOPIC as topic,
    "DATE" as date,
    DESCRIPTION as description,
    RIGHTS as rights,
    URL as url,
    FIPS as fips
from source
