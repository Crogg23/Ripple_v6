{{ config(materialized='table', schema='HISTORY') }}

-- Source: Documenting the American South (DocSouth) (144 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_DOCSOUTH') }}
)

select
    TITLE as title,
    AUTHOR as author,
    try_to_date(PUBLICATION_DATE) as publication_date,
    DOCSOUTH_URL as docsouth_url,
    TEXT_URL as text_url,
    COLLECTION as collection
from source
