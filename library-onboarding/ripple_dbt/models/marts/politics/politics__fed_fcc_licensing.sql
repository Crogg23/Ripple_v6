{{ config(materialized='table', schema='POLITICS') }}

-- Source: FCC Universal Licensing System (ULS) (1689338 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_fcc_licensing__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    unique_system_identifier,
    uls_file_number,
    ebf_transaction_id,
    call_sign,
    license_status,
    radio_service_code,
    grant_date,
    expired_date,
    cancellation_date,
    eligibility_rule_num,
    entity_name,
    frn,
    ein,
    address_line1,
    city,
    state,
    zip_code,
    po_box,
    attention_line,
    phone,
    fax,
    email,
    contact_company,
    applicant_type_code,
    fcc_county_code,
    last_action_date
from {{ ref('stg_fed_fcc_licensing__records') }}
