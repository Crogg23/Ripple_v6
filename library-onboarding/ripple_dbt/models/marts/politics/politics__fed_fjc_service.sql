{{ config(materialized='table', schema='POLITICS') }}

-- Source: FJC Biographical Directory of Article III Federal Judges -- Federal Judicial Service (4766 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_fjc_service__people) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    nid,
    sequence,
    judge_name,
    court_type,
    court_name,
    appointment_title,
    appointing_president,
    party_of_appointing_president,
    reappointing_president,
    party_of_reappointing_president,
    aba_rating,
    seat_id,
    statute_authorizing_new_seat,
    recess_appointment_date,
    nomination_date,
    committee_referral_date,
    hearing_date,
    judiciary_committee_action,
    committee_action_date,
    senate_vote_type,
    ayes_nays,
    confirmation_date,
    commission_date,
    service_as_chief_judge_begin,
    service_as_chief_judge_end,
    c_2nd_service_as_chief_judge_begin,
    c_2nd_service_as_chief_judge_end,
    senior_status_date,
    termination,
    termination_date
from {{ ref('stg_fed_fjc_service__people') }}
