{{ config(materialized='table', schema='POLITICS') }}

-- Source: OWID ? Corruption Perceptions Index (TI) (2312 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_OWID_CPI') }}
)

select
    ENTITY as entity,
    CODE as code,
    try_to_number("YEAR") as year,
    try_to_double(CORRUPTION_PERCEPTIONS_INDEX) as corruption_perceptions_index,
    WORLD_REGION_ACCORDING_TO_OWID as world_region_according_to_owid
from source
