version: 2

models:
  - name: politics__fed_fjc_service
    columns:
      - name: seat_id
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - nid
              - sequence
