version: 2

models:
  - name: politics__fed_medsl_president_returns
    columns:
      - name: state_fips
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - election_year
              - state_fips
              - candidate_name
              - party
              - is_writein
