{{ config(materialized='table', schema='POLITICS') }}

-- Source: Voteview -- Roll-Call Votes (member-by-vote matrix) (945523 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_VOTEVIEW_ROLLCALLS') }}
)

select
    try_to_number(CONGRESS) as congress,
    CHAMBER as chamber,
    ROLLNUMBER as rollnumber,
    ICPSR as icpsr,
    CAST_CODE as cast_code,
    PROB as prob
from source
