{{ config(materialized='table', schema='POLITICS') }}

-- Source: United Nations General Assembly Voting Data (Voeten) (1823352 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_intl_voeten_unga_votes__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    col_0,
    session_x,
    ccode1,
    ccode2,
    agree,
    year,
    idealpointfp_x,
    nvotesfp_x,
    idealpointfp_y,
    nvotesfp_y,
    idealpointdistance
from {{ ref('stg_intl_voeten_unga_votes__records') }}
