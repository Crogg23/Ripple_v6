{{ config(materialized='table', schema='POLITICS') }}

-- Source: United Nations General Assembly Voting Data (Voeten) (1823352 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'INTL_VOETEN_UNGA_VOTES') }}
)

select
    COL_0 as col_0,
    SESSION_X as session_x,
    CCODE1 as ccode1,
    CCODE2 as ccode2,
    AGREE as agree,
    try_to_number("YEAR") as year,
    IDEALPOINTFP_X as idealpointfp_x,
    NVOTESFP_X as nvotesfp_x,
    IDEALPOINTFP_Y as idealpointfp_y,
    NVOTESFP_Y as nvotesfp_y,
    IDEALPOINTDISTANCE as idealpointdistance
from source
