{{ config(materialized='table', schema='POLITICS') }}

-- Source: Judicial Common Space -- court/institution ideology medians (102 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_xc_jcs_medians__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    unnamed_0,
    year,
    term,
    congress,
    president,
    house_median,
    senate_median,
    sc_median,
    circuit_median1,
    circuit_sd1,
    circuit_median2,
    circuit_sd2,
    circuit_median3,
    circuit_sd3,
    circuit_median4,
    circuit_sd4,
    circuit_median5,
    circuit_sd5,
    circuit_median6,
    circuit_sd6,
    circuit_median7,
    circuit_sd7,
    circuit_median8,
    circuit_sd8,
    circuit_median9,
    circuit_sd9,
    circuit_median10,
    circuit_sd10,
    circuit_median11,
    circuit_sd11,
    circuit_median12,
    circuit_sd12,
    circuit_median13,
    circuit_sd13
from {{ ref('stg_xc_jcs_medians__records') }}
