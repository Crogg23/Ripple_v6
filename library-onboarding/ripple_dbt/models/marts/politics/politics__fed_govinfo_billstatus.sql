{{ config(materialized='table', schema='POLITICS') }}

-- Source: GovInfo BILLSTATUS -- Bill Status XML (sponsor, cosponsors, actions, laws) (36465 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: bare try_to_number()/try_to_double()/try_to_date() casts replaced in place with the project's guarded macros (stg_int/stg_float/stg_date, macros/staging_casts.sql) -- a value that doesn't parse cleanly now reads NULL instead of being silently rounded or misparsed. Kept on source() (not ref()) because neither of the two staging candidates for this source is the guarded full-staging pattern (one is an unresolved-grain passthrough, one is a hand-written view with its own unguarded casts).

with source as (
    select * from {{ source('ripple_raw', 'FED_GOVINFO_BILLSTATUS') }}
)

select
    {{ stg_int('"CONGRESS"') }} as congress,
    BILL_TYPE as bill_type,
    BILL_NUMBER as bill_number,
    {{ stg_date('"INTRODUCED_DATE"') }} as introduced_date,
    TITLE as title,
    SPONSOR_BIOGUIDE as sponsor_bioguide,
    SPONSOR_NAME as sponsor_name,
    LAW_TYPE as law_type,
    LAW_NUMBER as law_number,
    ACTION_TYPES as action_types,
    N_ACTIONS as n_actions,
    {{ stg_date('"LATEST_ACTION_DATE"') }} as latest_action_date,
    LATEST_ACTION_TEXT as latest_action_text,
    N_COSPONSORS as n_cosponsors
from source
