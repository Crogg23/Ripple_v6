{{ config(materialized='table', schema='POLITICS') }}

-- Source: DOJ Epstein Library ? File Manifest (777 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_DOJ_EPSTEIN_LIBRARY') }}
)

select
    RECORD as record
from source
