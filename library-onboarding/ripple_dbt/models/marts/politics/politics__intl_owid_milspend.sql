{{ config(materialized='table', schema='POLITICS') }}

-- Source: Our World in Data ? Military Spending (SIPRI mirror) (9112 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: bare try_to_number()/try_to_double()/try_to_date() casts replaced in place with the project's guarded macros (stg_int/stg_float/stg_date, macros/staging_casts.sql) -- a value that doesn't parse cleanly now reads NULL instead of being silently rounded or misparsed. Kept on source() (not ref()) because the staging model exposes fewer raw columns than this mart needs.

with source as (
    select * from {{ source('ripple_raw', 'INTL_OWID_MILSPEND') }}
)

select
    ENTITY as entity,
    CODE as code,
    {{ stg_int('"YEAR"') }} as year,
    MILITARY_EXPENDITURE as military_expenditure,
    WORLD_REGION_ACCORDING_TO_OWID as world_region_according_to_owid
from source
