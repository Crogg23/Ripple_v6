{{ config(materialized='table', schema='POLITICS') }}

-- Source: GovInfo BILLSTATUS -- Cosponsor Extract (367742 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: bare try_to_number()/try_to_double()/try_to_date() casts replaced in place with the project's guarded macros (stg_int/stg_float/stg_date, macros/staging_casts.sql) -- a value that doesn't parse cleanly now reads NULL instead of being silently rounded or misparsed. Kept on source() (not ref()) because neither of the two staging candidates for this source is the guarded full-staging pattern.

with source as (
    select * from {{ source('ripple_raw', 'FED_GOVINFO_BILL_COSPONSORS') }}
)

select
    {{ stg_int('"CONGRESS"') }} as congress,
    BILL_TYPE as bill_type,
    BILL_NUMBER as bill_number,
    COSPONSOR_BIOGUIDE as cosponsor_bioguide,
    COSPONSOR_NAME as cosponsor_name,
    COSPONSOR_PARTY as cosponsor_party,
    COSPONSOR_STATE as cosponsor_state,
    IS_ORIGINAL as is_original,
    {{ stg_date('"SPONSORSHIP_DATE"') }} as sponsorship_date,
    {{ stg_date('"SPONSORSHIP_WITHDRAWN_DATE"') }} as sponsorship_withdrawn_date
from source
