version: 2

models:
  - name: politics__st_cannabis_policy_bundles
    columns:
      - name: state
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - state
              - year
