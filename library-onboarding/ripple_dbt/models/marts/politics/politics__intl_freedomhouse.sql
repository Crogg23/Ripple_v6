{{ config(materialized='table', schema='POLITICS') }}

-- Source: Freedom in the World ? All Data (aggregate + category scores) (2723 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_intl_freedomhouse__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    country_territory,
    region,
    c_t,
    edition,
    status,
    pr_rating,
    cl_rating,
    a1,
    a2,
    a3,
    a,
    b1,
    b2,
    b3,
    b4,
    b,
    c1,
    c2,
    c3,
    c,
    add_q,
    add_a,
    pr,
    d1,
    d2,
    d3,
    d4,
    d,
    e1,
    e2,
    e3,
    e,
    f1,
    f2,
    f3,
    f4,
    f,
    g1,
    g2,
    g3,
    g4,
    g,
    cl,
    total
from {{ ref('stg_intl_freedomhouse__records') }}
