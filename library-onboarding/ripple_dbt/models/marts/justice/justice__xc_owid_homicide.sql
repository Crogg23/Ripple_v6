{{ config(materialized='table', schema='JUSTICE') }}

-- Source: OWID ? Homicide rate (UNODC) (4912 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_OWID_HOMICIDE') }}
)

select
    ENTITY as entity,
    CODE as code,
    try_to_number("YEAR") as year,
    try_to_double(HOMICIDE_RATE_PER_100_000_POPULATION) as homicide_rate_per_100_000_population,
    WORLD_REGION_ACCORDING_TO_OWID as world_region_according_to_owid
from source
