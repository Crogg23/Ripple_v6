{{ config(materialized='table', schema='JUSTICE') }}

-- Source: FED_COURTLISTENER_COURTS (3361 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_COURTS') }}
)

select
    ID as id,
    PACER_COURT_ID as pacer_court_id,
    PACER_HAS_RSS_FEED as pacer_has_rss_feed,
    PACER_RSS_ENTRY_TYPES as pacer_rss_entry_types,
    try_to_date(DATE_LAST_PACER_CONTACT) as date_last_pacer_contact,
    FJC_COURT_ID as fjc_court_id,
    try_to_date(DATE_MODIFIED) as date_modified,
    IN_USE as in_use,
    HAS_OPINION_SCRAPER as has_opinion_scraper,
    HAS_ORAL_ARGUMENT_SCRAPER as has_oral_argument_scraper,
    POSITION as position,
    CITATION_STRING as citation_string,
    SHORT_NAME as short_name,
    FULL_NAME as full_name,
    URL as url,
    try_to_date(START_DATE) as start_date,
    try_to_date(END_DATE) as end_date,
    JURISDICTION as jurisdiction,
    NOTES as notes,
    PARENT_COURT_ID as parent_court_id
from source
