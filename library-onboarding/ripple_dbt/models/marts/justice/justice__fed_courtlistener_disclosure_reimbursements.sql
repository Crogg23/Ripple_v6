{{ config(materialized='table', schema='JUSTICE') }}

-- Source: FED_COURTLISTENER_DISCLOSURE_REIMBURSEMENTS (33472 rows)
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: DATE_CREATED/DATE_MODIFIED/DATE_RAW were bare try_to_date()
-- casts, replaced in place with the guarded stg_date macro
-- (macros/staging_casts.sql) -- a value that doesn't parse cleanly now reads
-- NULL instead of a silent epoch-seconds misread. Unlike the sibling
-- disclosure marts, this table's own DATE_RAW is cast to a date here (not
-- passed through as text) -- that choice is unchanged, only guarded. No
-- matching staging model exists for this landing table, so kept on source().

with source as (
    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_DISCLOSURE_REIMBURSEMENTS') }}
)

select
    ID as id,
    {{ stg_date('DATE_CREATED') }} as date_created,
    {{ stg_date('DATE_MODIFIED') }} as date_modified,
    SOURCE as source,
    {{ stg_date('DATE_RAW') }} as date_raw,
    LOCATION as location,
    PURPOSE as purpose,
    ITEMS_PAID_OR_PROVIDED as items_paid_or_provided,
    REDACTED as redacted,
    FINANCIAL_DISCLOSURE_ID as financial_disclosure_id
from source
