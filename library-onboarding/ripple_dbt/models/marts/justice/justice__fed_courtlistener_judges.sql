{{ config(materialized='table', schema='JUSTICE') }}

-- Source: CourtListener ? Judge & Financial Disclosure Database (16191 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_JUDGES') }}
)

select
    ID as id,
    try_to_date(DATE_CREATED) as date_created,
    try_to_date(DATE_MODIFIED) as date_modified,
    try_to_date(DATE_COMPLETED) as date_completed,
    FJC_ID as fjc_id,
    SLUG as slug,
    NAME_FIRST as name_first,
    NAME_MIDDLE as name_middle,
    NAME_LAST as name_last,
    NAME_SUFFIX as name_suffix,
    try_to_date(DATE_DOB) as date_dob,
    try_to_date(DATE_GRANULARITY_DOB) as date_granularity_dob,
    try_to_date(DATE_DOD) as date_dod,
    try_to_date(DATE_GRANULARITY_DOD) as date_granularity_dod,
    DOB_CITY as dob_city,
    DOB_STATE as dob_state,
    try_to_number(DOB_COUNTRY) as dob_country,
    DOD_CITY as dod_city,
    DOD_STATE as dod_state,
    try_to_number(DOD_COUNTRY) as dod_country,
    GENDER as gender,
    RELIGION as religion,
    FTM_TOTAL_RECEIVED as ftm_total_received,
    FTM_EID as ftm_eid,
    HAS_PHOTO as has_photo,
    IS_ALIAS_OF_ID as is_alias_of_id
from source
