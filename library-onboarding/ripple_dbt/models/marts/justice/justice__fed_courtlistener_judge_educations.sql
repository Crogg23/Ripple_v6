{{ config(materialized='table', schema='JUSTICE') }}

-- Source: FED_COURTLISTENER_JUDGE_EDUCATIONS (12777 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_JUDGE_EDUCATIONS') }}
)

select
    ID as id,
    try_to_date(DATE_CREATED) as date_created,
    try_to_date(DATE_MODIFIED) as date_modified,
    DEGREE_LEVEL as degree_level,
    DEGREE_DETAIL as degree_detail,
    DEGREE_YEAR as degree_year,
    PERSON_ID as person_id,
    SCHOOL_ID as school_id
from source
