{{ config(materialized='table', schema='JUSTICE') }}

-- Source: FED_COURTLISTENER_JUDGE_EDUCATIONS (12777 rows)
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: bare try_to_date() casts replaced in place with the
-- project's guarded stg_date macro (macros/staging_casts.sql) -- a value that
-- doesn't parse cleanly now reads NULL instead of a silent epoch-seconds
-- misread. DEGREE_YEAR was already on the guarded ripple_num macro, untouched.
-- No matching staging model exists for this landing table, so kept on
-- source().

with source as (
    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_JUDGE_EDUCATIONS') }}
)

select
    ID as id,
    {{ stg_date('DATE_CREATED') }} as date_created,
    {{ stg_date('DATE_MODIFIED') }} as date_modified,
    DEGREE_LEVEL as degree_level,
    DEGREE_DETAIL as degree_detail,
    {{ ripple_num('DEGREE_YEAR') }} as degree_year,
    PERSON_ID as person_id,
    SCHOOL_ID as school_id
from source
