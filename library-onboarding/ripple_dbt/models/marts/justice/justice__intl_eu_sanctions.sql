{{ config(materialized='table', schema='JUSTICE') }}

-- Source: EU Consolidated Financial Sanctions List (42347 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'INTL_EU_SANCTIONS') }}
)

select
    try_to_date(DATE_FILE) as date_file,
    ENTITY_LOGICAL_ID as entity_logical_id,
    SUBJECT_TYPE as subject_type,
    LEBA_NUMTITLE as leba_numtitle,
    try_to_date(LEBA_PUBLICATION_DATE) as leba_publication_date,
    LEBA_URL as leba_url,
    PROGRAMME as programme,
    ENTITY_REMARK as entity_remark,
    NAAL_LOGICAL_ID as naal_logical_id,
    ENTITY_LOGICAL_ID_1 as entity_logical_id_1,
    NAAL_LEBA_NUMTITLE as naal_leba_numtitle,
    try_to_date(NAAL_LEBA_PUBLICATION_DATE) as naal_leba_publication_date,
    NAAL_LEBA_URL as naal_leba_url,
    NAAL_PROGRAMME as naal_programme,
    NAAL_LASTNAME as naal_lastname,
    NAAL_FIRSTNAME as naal_firstname,
    NAAL_MIDDLENAME as naal_middlename,
    NAAL_WHOLENAME as naal_wholename,
    NAAL_GENDER as naal_gender,
    NAAL_TITLE as naal_title,
    NAAL_FUNCTION as naal_function,
    NAAL_LANGUAGE as naal_language,
    ADDR_LOGICAL_ID as addr_logical_id,
    ENTITY_LOGICAL_ID_2 as entity_logical_id_2,
    ADDR_LEBA_NUMTITLE as addr_leba_numtitle,
    try_to_date(ADDR_LEBA_PUBLICATION_DATE) as addr_leba_publication_date,
    ADDR_LEBA_URL as addr_leba_url,
    ADDR_PROGRAMME as addr_programme,
    ADDR_NUMBER as addr_number,
    ADDR_STREET as addr_street,
    ADDR_ZIPCODE as addr_zipcode,
    ADDR_CITY as addr_city,
    try_to_number(ADDR_COUNTRY) as addr_country,
    ADDR_OTHER as addr_other,
    BIRT_LOGICAL_ID as birt_logical_id,
    ENTITY_LOGICAL_ID_3 as entity_logical_id_3,
    BIRT_LEBA_NUMTITLE as birt_leba_numtitle,
    try_to_date(BIRT_LEBA_PUBLICATION_DATE) as birt_leba_publication_date,
    BIRT_LEBA_URL as birt_leba_url,
    BIRT_PROGRAMME as birt_programme,
    try_to_date(BIRT_DATE) as birt_date,
    BIRT_PLACE as birt_place,
    try_to_number(BIRT_COUNTRY) as birt_country,
    IDEN_LOGICAL_ID as iden_logical_id,
    ENTITY_LOGICAL_ID_4 as entity_logical_id_4,
    IDEN_LEBA_NUMTITLE as iden_leba_numtitle,
    try_to_date(IDEN_LEBA_PUBLICATION_DATE) as iden_leba_publication_date,
    IDEN_LEBA_URL as iden_leba_url,
    IDEN_PROGRAMME as iden_programme,
    IDEN_NUMBER as iden_number,
    try_to_number(IDEN_COUNTRY) as iden_country,
    CITI_LOGICAL_ID as citi_logical_id,
    ENTITY_LOGICAL_ID_5 as entity_logical_id_5,
    CITI_LEBA_NUMTITLE as citi_leba_numtitle,
    try_to_date(CITI_LEBA_PUBLICATION_DATE) as citi_leba_publication_date,
    CITI_LEBA_URL as citi_leba_url,
    CITI_PROGRAMME as citi_programme,
    try_to_number(CITI_COUNTRY) as citi_country,
    EU_REF_NUM as eu_ref_num
from source
