{{ config(materialized='table', schema='JUSTICE') }}

-- Source: FED_COURTLISTENER_JUDGE_POLITICAL_AFFILIATIONS (8486 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_JUDGE_POLITICAL_AFFILIATIONS') }}
)

select
    ID as id,
    try_to_date(DATE_CREATED) as date_created,
    try_to_date(DATE_MODIFIED) as date_modified,
    POLITICAL_PARTY as political_party,
    SOURCE as source,
    try_to_date(DATE_START) as date_start,
    try_to_date(DATE_GRANULARITY_START) as date_granularity_start,
    try_to_date(DATE_END) as date_end,
    try_to_date(DATE_GRANULARITY_END) as date_granularity_end,
    PERSON_ID as person_id
from source
