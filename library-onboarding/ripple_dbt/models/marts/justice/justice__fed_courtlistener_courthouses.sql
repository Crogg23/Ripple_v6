{{ config(materialized='table', schema='JUSTICE') }}

-- Source: FED_COURTLISTENER_COURTHOUSES (3361 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_COURTHOUSES') }}
)

select
    ID as id,
    COURT_SEAT as court_seat,
    BUILDING_NAME as building_name,
    ADDRESS1 as address1,
    ADDRESS2 as address2,
    CITY as city,
    COUNTY as county,
    STATE as state,
    ZIP_CODE as zip_code,
    COUNTRY_CODE as country_code,
    COURT_ID as court_id
from source
