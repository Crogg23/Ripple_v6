version: 2

models:
  - name: justice__fed_ftc_datasets
    columns:
      - name: action_name
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - action_name
              - respondent
              - date_filed
