{{ config(materialized='table', schema='JUSTICE') }}

-- Source: FED_COURTLISTENER_PARENTHETICALS (6408887 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_PARENTHETICALS') }}
)

select
    ID as id,
    TEXT as text,
    SCORE as score,
    DESCRIBED_OPINION_ID as described_opinion_id,
    DESCRIBING_OPINION_ID as describing_opinion_id,
    GROUP_ID as group_id
from source
