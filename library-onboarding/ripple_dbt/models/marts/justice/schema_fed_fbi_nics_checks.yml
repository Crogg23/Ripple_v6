version: 2

models:
  - name: justice__fed_fbi_nics_checks
    columns:
      - name: month
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - month
              - state
