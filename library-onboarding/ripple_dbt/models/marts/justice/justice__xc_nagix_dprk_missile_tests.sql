{{ config(materialized='table', schema='JUSTICE') }}

-- Source: nagix/nk-missile-tests ? North Korea missile flight tests (JSON, CNS-derived + landing geocoding) (340 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_NAGIX_DPRK_MISSILE_TESTS') }}
)

select
    "DATE" as date,
    "TIME" as time,
    SERIES as series,
    MISSILE as missile,
    FACILITY as facility,
    LANDING as landing,
    APOGEE as apogee,
    DISTANCE as distance,
    BEARING as bearing,
    OUTCOME as outcome,
    DESCRIPTION as description,
    GLIDE as glide
from source
