{{ config(materialized='table', schema='JUSTICE') }}

-- Source: FED_COURTLISTENER_POSITIONS (51290 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_POSITIONS') }}
)

select
    ID as id,
    try_to_date(DATE_CREATED) as date_created,
    try_to_date(DATE_MODIFIED) as date_modified,
    POSITION_TYPE as position_type,
    JOB_TITLE as job_title,
    SECTOR as sector,
    ORGANIZATION_NAME as organization_name,
    LOCATION_CITY as location_city,
    LOCATION_STATE as location_state,
    try_to_date(DATE_NOMINATED) as date_nominated,
    try_to_date(DATE_ELECTED) as date_elected,
    try_to_date(DATE_RECESS_APPOINTMENT) as date_recess_appointment,
    try_to_date(DATE_REFERRED_TO_JUDICIAL_COMMITTEE) as date_referred_to_judicial_committee,
    try_to_date(DATE_JUDICIAL_COMMITTEE_ACTION) as date_judicial_committee_action,
    JUDICIAL_COMMITTEE_ACTION as judicial_committee_action,
    try_to_date(DATE_HEARING) as date_hearing,
    try_to_date(DATE_CONFIRMATION) as date_confirmation,
    try_to_date(DATE_START) as date_start,
    try_to_date(DATE_GRANULARITY_START) as date_granularity_start,
    try_to_date(DATE_TERMINATION) as date_termination,
    TERMINATION_REASON as termination_reason,
    try_to_date(DATE_GRANULARITY_TERMINATION) as date_granularity_termination,
    try_to_date(DATE_RETIREMENT) as date_retirement,
    NOMINATION_PROCESS as nomination_process,
    VOTE_TYPE as vote_type,
    VOICE_VOTE as voice_vote,
    VOTES_YES as votes_yes,
    VOTES_NO as votes_no,
    try_to_double(VOTES_YES_PERCENT) as votes_yes_percent,
    try_to_double(VOTES_NO_PERCENT) as votes_no_percent,
    HOW_SELECTED as how_selected,
    HAS_INFERRED_VALUES as has_inferred_values,
    APPOINTER_ID as appointer_id,
    COURT_ID as court_id,
    PERSON_ID as person_id,
    PREDECESSOR_ID as predecessor_id,
    SCHOOL_ID as school_id,
    SUPERVISOR_ID as supervisor_id
from source
