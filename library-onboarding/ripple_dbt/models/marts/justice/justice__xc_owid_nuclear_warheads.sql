{{ config(materialized='table', schema='JUSTICE') }}

-- Source: Our World in Data ? Estimated nuclear warhead stockpiles (FAS Nuclear Notebook) (902 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_OWID_NUCLEAR_WARHEADS') }}
)

select
    ENTITY as entity,
    CODE as code,
    try_to_number("YEAR") as year,
    try_to_number(NUMBER_OF_NUCLEAR_WARHEADS) as number_of_nuclear_warheads
from source
