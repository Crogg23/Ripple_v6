{{ config(materialized='table', schema='JUSTICE') }}

-- Source: Ransomware.live ? global ransomware victim tracker (29193 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_RANSOMWARELIVE_VICTIMS') }}
)

select
    POST_TITLE as post_title,
    GROUP_NAME as group_name,
    DISCOVERED as discovered,
    PUBLISHED as published,
    WEBSITE as website,
    COUNTRY as country,
    ACTIVITY as activity,
    DESCRIPTION as description,
    POST_URL as post_url
from source
