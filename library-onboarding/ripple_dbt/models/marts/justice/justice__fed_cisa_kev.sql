{{ config(materialized='table', schema='JUSTICE') }}

-- Source: CISA Known Exploited Vulnerabilities (KEV) Catalog (1631 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_CISA_KEV') }}
)

select
    CVE_ID as cve_id,
    VENDOR_PROJECT as vendor_project,
    PRODUCT as product,
    VULNERABILITY_NAME as vulnerability_name,
    try_to_date(DATE_ADDED) as date_added,
    SHORT_DESCRIPTION as short_description,
    REQUIRED_ACTION as required_action,
    try_to_date(DUE_DATE) as due_date,
    KNOWN_RANSOMWARE_CAMPAIGN_USE as known_ransomware_campaign_use,
    NOTES as notes,
    CWES as cwes
from source
