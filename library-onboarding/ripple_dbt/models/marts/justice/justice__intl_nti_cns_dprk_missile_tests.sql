{{ config(materialized='table', schema='JUSTICE') }}

-- Source: CNS North Korea Missile Test Database (Nuclear Threat Initiative / James Martin Center for Nonproliferation Studies) (303 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_intl_nti_cns_dprk_missile_tests__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    f1,
    date,
    date_entered_updated,
    launch_time_utc,
    missile_name,
    missile_type,
    launch_agency_authority,
    facility_name,
    facility_location,
    other_name,
    facility_latitude,
    facility_longitude,
    landing_location,
    apogee,
    distance_travelled,
    confirmation_status,
    test_outcome,
    additional_information,
    source_s
from {{ ref('stg_intl_nti_cns_dprk_missile_tests__records') }}
