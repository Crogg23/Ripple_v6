{{ config(materialized='table', schema='JUSTICE') }}

-- Source: FBI Crime Data Explorer (NIBRS/UCR) (1 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_FBI_CDE') }}
)

select
    INCIDENT_ID as incident_id,
    ORI as ori,
    AGENCY_NAME as agency_name,
    STATE_ABBR as state_abbr,
    FIPS_STATE_CODE as fips_state_code,
    try_to_number(FIPS_COUNTY_CODE) as fips_county_code,
    DATA_YEAR as data_year,
    try_to_date(INCIDENT_DATE) as incident_date,
    OFFENSE_CODE as offense_code,
    OFFENSE_NAME as offense_name,
    OFFENSE_CATEGORY as offense_category,
    CLEARED as cleared,
    try_to_number(VICTIM_COUNT) as victim_count,
    try_to_number(OFFENDER_COUNT) as offender_count,
    try_to_number(ARRESTEE_COUNT) as arrestee_count,
    LOCATION_TYPE as location_type,
    WEAPON_TYPE as weapon_type,
    BIAS_MOTIVATION as bias_motivation,
    REPORT_TYPE as report_type,
    try_to_double(POPULATION_GROUP) as population_group,
    NOTE as note
from source
