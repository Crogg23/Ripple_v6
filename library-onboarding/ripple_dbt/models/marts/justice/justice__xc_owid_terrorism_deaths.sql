{{ config(materialized='table', schema='JUSTICE') }}

-- Source: OWID ? Terrorism deaths (10481 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'XC_OWID_TERRORISM_DEATHS') }}
)

select
    ENTITY as entity,
    CODE as code,
    try_to_number("YEAR") as year,
    FATALITIES as fatalities,
    WORLD_REGION_ACCORDING_TO_OWID as world_region_according_to_owid
from source
