version: 2

models:
  - name: justice__fed_courtlistener_financial_disclosures
    columns:
      - name: id
        description: "PROBED LIVE 2026-09-22 (scripts/probe_untested_marts.py): 1 null in source, measured 2026-09-22; threshold, fails above 2."
        tests:
          - not_null:
              config:
                warn_if: ">1"
                error_if: ">2"
