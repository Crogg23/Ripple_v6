{{ config(materialized='table', schema='JUSTICE') }}

-- Source: U.S. Courts Caseload Statistics Data Tables (50 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_USCOURTS_STATS') }}
)

select
    TABLE_NUMBER as table_number,
    TITLE as title,
    PUBLICATION_NAME as publication_name,
    REPORTING_PERIOD as reporting_period,
    TOPIC as topic,
    SUBTOPIC as subtopic,
    DISTRICT as district,
    CIRCUIT as circuit,
    try_to_number(COUNTY) as county,
    FIPS_CODE as fips_code,
    CHAPTER_CODE as chapter_code,
    CASE_TYPE as case_type,
    FILINGS as filings,
    TERMINATIONS as terminations,
    PENDING as pending,
    DOWNLOAD_URL_PDF as download_url_pdf,
    DOWNLOAD_URL_XLSX as download_url_xlsx
from source
