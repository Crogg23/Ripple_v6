{{ config(materialized='table', schema='JUSTICE') }}

-- Source: EU-SOCTA and IOCTA ? Organized and Cybercrime Assessments (26 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_intl_eu_socta_europol__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    report_title,
    report_type,
    publish_date,
    update_date,
    report_year,
    language,
    file_format,
    file_size_mb,
    download_url,
    document_variant,
    isbn,
    doi,
    serial_number,
    publisher,
    geographic_scope,
    source_url
from {{ ref('stg_intl_eu_socta_europol__records') }}
