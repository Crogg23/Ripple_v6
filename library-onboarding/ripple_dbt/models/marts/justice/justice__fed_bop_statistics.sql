{{ config(materialized='table', schema='JUSTICE') }}

-- Source: BOP Inmate Statistics (50 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_BOP_STATISTICS') }}
)

select
    try_to_date(REPORT_DATE) as report_date,
    CATEGORY as category,
    METRIC_NAME as metric_name,
    try_to_double(METRIC_VALUE) as metric_value,
    METRIC_UNIT as metric_unit,
    FACILITY_TYPE as facility_type,
    STATE_FIPS as state_fips,
    SOURCE_URL as source_url
from source
