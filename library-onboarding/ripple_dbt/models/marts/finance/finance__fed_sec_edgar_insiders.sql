{{ config(materialized='table', schema='FINANCE') }}

-- Source: SEC EDGAR Forms 3/4/5 ? Insider Trading Reports (69259 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_SEC_EDGAR_INSIDERS') }}
)

select
    ACCESSION_NUMBER as accession_number,
    try_to_date(FILING_DATE) as filing_date,
    PERIOD_OF_REPORT as period_of_report,
    try_to_date(DATE_OF_ORIG_SUB) as date_of_orig_sub,
    NO_SECURITIES_OWNED as no_securities_owned,
    NOT_SUBJECT_SEC16 as not_subject_sec16,
    FORM3_HOLDINGS_REPORTED as form3_holdings_reported,
    FORM4_TRANS_REPORTED as form4_trans_reported,
    DOCUMENT_TYPE as document_type,
    CIK as cik,
    ISSUERNAME as issuername,
    ISSUERTRADINGSYMBOL as issuertradingsymbol,
    REMARKS as remarks,
    AFF10B5ONE as aff10b5one
from source
