version: 2

models:
  - name: finance__fed_fdic_bank_data
    description: "SAMPLE ONLY -- NOT the full dataset. FDIC insured-institution directory: a 10,000-row API slice (cert number unique within it) of the full multi-decade institution universe. Use for shape/testing only; full pull needs an offset-paginated loader."
    columns:
      - name: cert
        data_tests: [not_null, unique]
      - name: name
        data_tests: [not_null]
