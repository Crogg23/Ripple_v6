{{ config(materialized='table', schema='FINANCE') }}

-- Source: FED_SEC_MONEY_MARKET_FUND_INFORMATION (1206 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_SEC_MONEY_MARKET_FUND_INFORMATION') }}
)

select
    REPORTMONTH as reportmonth,
    REGISTRANT_CIK as registrant_cik,
    REGISTRANT as registrant,
    SERIES_NAME as series_name,
    SERIES_ID as series_id,
    SERIES_CATEGORY as series_category,
    CLASS_NAME as class_name,
    CLASS_ID as class_id,
    INVESTMENT_ADVISOR as investment_advisor,
    CLASS_TICKER_SYMBOL as class_ticker_symbol,
    SUB_ADVISOR as sub_advisor
from source
