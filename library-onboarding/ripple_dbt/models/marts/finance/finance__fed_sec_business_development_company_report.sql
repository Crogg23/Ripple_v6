{{ config(materialized='table', schema='FINANCE') }}

-- Source: FED_SEC_BUSINESS_DEVELOPMENT_COMPANY_REPORT (212 rows)
-- Generated by scripts/gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with a bare try_to_date() cast on
-- FILING_DATE, unguarded against a bad load. Now reads the already-guarded
-- staging model instead (stg_fed_sec_business_development_company_report__
-- organizations) -- same 10 columns, same names, same order, verified column
-- match myself first; its filing_date cast is stg_date-guarded (macros/
-- staging_casts.sql): a value that doesn't parse cleanly reads NULL instead
-- of a silently wrong date.

select
    file_no,
    cik,
    registrant_name,
    address_1,
    address_2,
    city,
    state,
    zip_code,
    filing_date,
    filing_type
from {{ ref('stg_fed_sec_business_development_company_report__organizations') }}
