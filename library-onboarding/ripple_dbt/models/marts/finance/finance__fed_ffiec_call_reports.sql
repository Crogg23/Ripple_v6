{{ config(materialized='table', schema='FINANCE') }}

-- Source: FFIEC Bank Call Reports (CDR) (302 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_ffiec_call_reports__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    doctype_html,
    rssd_id,
    idrssd,
    reporting_period_end_date,
    institution_name,
    total_assets,
    total_deposits,
    total_loans_and_leases,
    net_income,
    tier1_capital,
    past_due_loans_30_89,
    past_due_loans_90_plus,
    mdrm_concept_code,
    value
from {{ ref('stg_fed_ffiec_call_reports__records') }}
