{{ config(materialized='table', schema='FINANCE') }}

-- Source: SEC EDGAR Insider Transactions (Form 3/4/5, 2025Q1) - DERIV_TRANS (1049121 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_sec_insider_deriv_trans__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    accession_number,
    deriv_trans_sk,
    security_title,
    security_title_fn,
    conv_exercise_price,
    conv_exercise_price_fn,
    trans_date,
    trans_date_fn,
    deemed_execution_date,
    deemed_execution_date_fn,
    trans_form_type,
    trans_code,
    equity_swap_involved,
    equity_swap_trans_cd_fn,
    trans_timeliness,
    trans_timeliness_fn,
    trans_shares,
    trans_shares_fn,
    trans_total_value,
    trans_total_value_fn,
    trans_pricepershare,
    trans_pricepershare_fn,
    trans_acquired_disp_cd,
    trans_acquired_disp_cd_fn,
    excercise_date,
    excercise_date_fn,
    expiration_date,
    expiration_date_fn,
    undlyng_sec_title,
    undlyng_sec_title_fn,
    undlyng_sec_shares,
    undlyng_sec_shares_fn,
    undlyng_sec_value,
    undlyng_sec_value_fn,
    shrs_ownd_folwng_trans,
    shrs_ownd_folwng_trans_fn,
    valu_ownd_folwng_trans,
    valu_ownd_folwng_trans_fn,
    direct_indirect_ownership,
    direct_indirect_ownership_fn,
    nature_of_ownership,
    nature_of_ownership_fn
from {{ ref('stg_fed_sec_insider_deriv_trans__records') }}
