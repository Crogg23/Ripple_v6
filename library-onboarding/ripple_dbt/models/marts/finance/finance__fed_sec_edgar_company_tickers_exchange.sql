{{ config(materialized='table', schema='FINANCE') }}

-- Source: FED_SEC_EDGAR_COMPANY_TICKERS_EXCHANGE (1 rows)
-- Generated by scripts/gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_SEC_EDGAR_COMPANY_TICKERS_EXCHANGE') }}
)

select
    FIELDS as fields,
    DATA as data
from source
