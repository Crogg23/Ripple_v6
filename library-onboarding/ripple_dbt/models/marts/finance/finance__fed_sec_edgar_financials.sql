{{ config(materialized='table', schema='FINANCE') }}

-- Source: SEC Financial Statement Data Sets (55,635 rows)  [row count re-verified against the live table 2026-08-11; header had said 6699]
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_sec_edgar_financials__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    adsh,
    cik,
    name,
    sic,
    countryba,
    stprba,
    cityba,
    zipba,
    bas1,
    bas2,
    baph,
    countryma,
    stprma,
    cityma,
    zipma,
    mas1,
    mas2,
    countryinc,
    stprinc,
    ein,
    former,
    changed,
    afs,
    wksi,
    fye,
    form,
    period,
    fy,
    fp,
    filed,
    accepted,
    prevrpt,
    detail,
    instance,
    nciks,
    aciks,
    source_file
from {{ ref('stg_fed_sec_edgar_financials__records') }}
