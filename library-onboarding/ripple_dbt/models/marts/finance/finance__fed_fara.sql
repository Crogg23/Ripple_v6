{{ config(materialized='table', schema='FINANCE') }}

-- Source: FARA Foreign Agents Registration Act Database (30 rows)
-- Generated by gen_mart_models.py
-- Fixed 2026-09-20: was reading source() with bare try_to_number()/try_to_double()/try_to_date() casts, which silently round decimals or mis-parse dates instead of guarding them. Now reads the already-guarded staging model (stg_fed_fara__records) instead -- same columns, same names, same order; every numeric/date cast there is regex-guarded (macros/staging_casts.sql): a value that doesn't parse cleanly reads NULL instead of a silently wrong value.

select
    registrant_name,
    registration_number,
    foreign_principal,
    foreign_principal_country,
    ein,
    registration_date,
    filing_type,
    document_url,
    termination_date,
    activity_description,
    receipts_amount,
    disbursements_amount,
    period_start,
    period_end
from {{ ref('stg_fed_fara__records') }}
