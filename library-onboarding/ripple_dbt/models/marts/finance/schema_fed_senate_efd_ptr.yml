version: 2
models:
- name: finance__fed_senate_efd_ptr
  tests:
  - dbt_utils.unique_combination_of_columns:
      arguments:
        combination_of_columns:
          - filing_id
          - line_no
  description: 'PROBED LIVE 2026-09-22 (scripts/probe_untested_marts.py): row key is (filing_id, line_no), 6,855 rows.'
