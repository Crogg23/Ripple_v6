{{ config(materialized='table', schema='FINANCE') }}

-- Source: Credit Union Call Report Quarterly Data (121713 rows)
-- Generated by gen_mart_models.py

with source as (
    select * from {{ source('ripple_raw', 'FED_NCUA_CALL_REPORTS') }}
)

select
    CU_NUMBER as cu_number,
    try_to_date(CYCLE_DATE) as cycle_date,
    ACCT_CODE as acct_code,
    try_to_double(ACCT_VALUE) as acct_value,
    CU_NAME as cu_name,
    STATE as state,
    CITY as city,
    ZIP_CODE as zip_code,
    EIN as ein,
    CHARTER_TYPE as charter_type,
    TABLE_NAME as table_name,
    ACCT_DESC as acct_desc,
    ACCT_STATUS as acct_status
from source
