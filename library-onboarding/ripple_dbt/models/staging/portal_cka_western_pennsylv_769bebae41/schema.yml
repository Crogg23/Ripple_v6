version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_769BEBAE41
        description: >
          Raw landing table for Healthy Ride Trip Data (archived). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_769bebae41__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TRIP_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: trip_id
        data_tests:
          - unique
          - not_null
