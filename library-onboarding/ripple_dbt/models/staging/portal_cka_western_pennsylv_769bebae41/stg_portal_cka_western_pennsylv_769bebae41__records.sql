{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TRIP_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TRIP_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_769BEBAE41') }}

),

renamed as (

    select
        "TRIP_ID" as trip_id,
        "STARTTIME" as starttime,
        "STOPTIME" as stoptime,
        "BIKEID" as bikeid,
        "TRIPDURATION" as tripduration,
        "FROM_STATION_ID" as from_station_id,
        "FROM_STATION_NAME" as from_station_name,
        "TO_STATION_ID" as to_station_id,
        "TO_STATION_NAME" as to_station_name,
        "USERTYPE" as usertype,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/healthyride-trip-data' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by trip_id order by _loaded_at desc) = 1
