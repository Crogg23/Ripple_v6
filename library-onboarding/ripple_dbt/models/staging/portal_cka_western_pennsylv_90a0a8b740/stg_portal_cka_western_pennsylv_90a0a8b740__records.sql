{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PARCEL_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PARCEL_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (PARCEL_ID) is NOT unique within a load as landed at generation time -- it would collapse 17,478 of 96,713 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_90A0A8B740') }}

),

renamed as (

    select
        "PARCEL_ID" as parcel_id,
        "TAX_YEAR" as tax_year,
        "CLASS" as class,
        "CLASS_GROUP" as class_group,
        "TAX_STATUS" as tax_status,
        "MUNI_CODE" as muni_code,
        "MUNI_NAME" as muni_name,
        "SCHOOL_CODE" as school_code,
        "SCHOOL_DISTRICT" as school_district,
        "HEARING_TYPE" as hearing_type,
        "COMPLAINANT" as complainant,
        "HEARING_STATUS" as hearing_status,
        "STATUS" as status,
        "PRE_APPEAL_LAND" as pre_appeal_land,
        "PRE_APPEAL_BLDG" as pre_appeal_bldg,
        "PRE_APPEAL_TOTAL" as pre_appeal_total,
        "POST_APPEAL_LAND" as post_appeal_land,
        "POST_APPEAL_BLDG" as post_appeal_bldg,
        "POST_APPEAL_TOTAL" as post_appeal_total,
        "HEARING_CHANGE_AMOUNT" as hearing_change_amount,
        "LAST_UPDATE_REASON" as last_update_reason,
        "CURRENT_LAND_VALUE" as current_land_value,
        "CURRENT_BLDG_VALUE" as current_bldg_value,
        "CURRENT_TOTAL_VALUE" as current_total_value,
        "CURRENT_VALUE_VS_PRE_APPEAL" as current_value_vs_pre_appeal,
        "HEARING_DATE" as hearing_date,
        "DISPO_DATE" as dispo_date,
        "ELAPSED_DAYS" as elapsed_days,
        "AS_OF_DATE" as as_of_date,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-property-assessment-appeals' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by parcel_id, tax_year, class, class_group, tax_status, muni_code, muni_name, school_code, school_district, hearing_type, complainant, hearing_status, status, pre_appeal_land, pre_appeal_bldg, pre_appeal_total, post_appeal_land, post_appeal_bldg, post_appeal_total, hearing_change_amount, last_update_reason, current_land_value, current_bldg_value, current_total_value, current_value_vs_pre_appeal, hearing_date, dispo_date, elapsed_days, as_of_date order by _loaded_at desc) = 1
