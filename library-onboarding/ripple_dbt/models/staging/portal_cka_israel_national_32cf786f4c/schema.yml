version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ISRAEL_NATIONAL_32CF786F4C
        description: >
          Raw landing table for מבנים ירוקים. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_israel_national_32cf786f4c__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PROJECT_ID+BUILDING_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: project_id
        data_tests:
          - not_null
      - name: building_id
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - project_id
            - building_id
