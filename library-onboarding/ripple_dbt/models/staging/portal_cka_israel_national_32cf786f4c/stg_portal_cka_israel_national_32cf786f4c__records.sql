{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PROJECT_ID+BUILDING_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PROJECT_ID, BUILDING_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_32CF786F4C') }}

),

renamed as (

    select
        "PROJECT_ID" as project_id,
        "BUILDING_ID" as building_id,
        "DESIGNATION_ID" as designation_id,
        "PROJECT_HALTED" as project_halted,
        "PROJECT_GENERAL_LAST_UPDATE" as project_general_last_update,
        "BUILDING_COUNT" as building_count,
        "DESIGNATION_COUNT" as designation_count,
        "GUSH" as gush,
        "HELKA" as helka,
        "X" as x,
        "Y" as y,
        "MUNICIPALITY_ID" as municipality_id,
        "MUNICIPALITY_NAME" as municipality_name,
        "BUILDING_INFO_LAST_UPDATE" as building_info_last_update,
        "BUILDING_STREET" as building_street,
        "BUILDING_ADDRESS_NUMBER" as building_address_number,
        "BUILDING_ADDRESS_ENTRANCE" as building_address_entrance,
        "BUILDING_MIGRASH" as building_migrash,
        "FLOORS_ABOVE_GROUND" as floors_above_ground,
        "BUILDING_AREA" as building_area,
        "STANDARD_NAME_ID" as standard_name_id,
        "STANDARD_NAME" as standard_name,
        "STANDARD_DESIGNATION_ID" as standard_designation_id,
        "STANDARD_DESIGNATION_NAME" as standard_designation_name,
        "STANDARD_DESIGNATION_MAIN_USE" as standard_designation_main_use,
        "MAIN_USE_NAME" as main_use_name,
        "ROUTE" as route,
        "DESIGNATION_AREA" as designation_area,
        "RESIDENTIAL_UNITS" as residential_units,
        "DESIGNATION_INFO_LAST_UPDATE" as designation_info_last_update,
        "CERTIFICATION_STATUS" as certification_status,
        "CERTIFICATE_DATE_PRE" as certificate_date_pre,
        "CERTIFICATE_INSERT_DATE_PRE" as certificate_insert_date_pre,
        "CERTIFICATE_SCORE_PRE" as certificate_score_pre,
        "CERTIFICATE_STARS_PRE" as certificate_stars_pre,
        "CERTIFICATE_ENERGY_PRE" as certificate_energy_pre,
        "CERTIFICATE_DATE_A" as certificate_date_a,
        "CERTIFICATE_INSERT_DATE_A" as certificate_insert_date_a,
        "CERTIFICATE_SCORE_A" as certificate_score_a,
        "CERTIFICATE_STARS_A" as certificate_stars_a,
        "CERTIFICATE_ENERGY_A" as certificate_energy_a,
        "CERTIFICATE_DATE_B" as certificate_date_b,
        "CERTIFICATE_INSERT_DATE_B" as certificate_insert_date_b,
        "CERTIFICATE_SCORE_B" as certificate_score_b,
        "CERTIFICATE_STARS_B" as certificate_stars_b,
        "CERTIFICATE_ENERGY_B" as certificate_energy_b,
        "DESIGNATION_INFO_STATUS" as designation_info_status,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/greenbuildings' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by project_id, building_id order by _loaded_at desc) = 1
