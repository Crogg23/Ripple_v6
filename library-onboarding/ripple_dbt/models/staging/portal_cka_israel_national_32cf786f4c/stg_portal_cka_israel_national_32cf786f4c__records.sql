{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PROJECT_ID+BUILDING_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PROJECT_ID, BUILDING_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 27 of 47 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   designation_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   project_general_last_update: timestamp
--   building_count: integer
--   designation_count: integer
--   x: float
--   y: float
--   municipality_id: integer
--   building_info_last_update: timestamp
--   floors_above_ground: float
--   building_area: float
--   standard_designation_id: integer
--   standard_designation_main_use: integer
--   designation_area: float
--   residential_units: integer
--   designation_info_last_update: timestamp
--   certificate_date_pre: timestamp
--   certificate_insert_date_pre: timestamp
--   certificate_score_pre: float
--   certificate_stars_pre: integer
--   certificate_date_a: timestamp
--   certificate_insert_date_a: timestamp
--   certificate_score_a: float
--   certificate_stars_a: integer
--   certificate_date_b: timestamp
--   certificate_insert_date_b: timestamp
--   certificate_score_b: float
--   certificate_stars_b: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (PROJECT_ID, BUILDING_ID) is NOT unique within a load as landed at generation time -- it would collapse 33 of 7,534 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_32CF786F4C') }}
    qualify row_number() over (partition by "PROJECT_ID", "BUILDING_ID", "DESIGNATION_ID", "PROJECT_HALTED", "PROJECT_GENERAL_LAST_UPDATE", "BUILDING_COUNT", "DESIGNATION_COUNT", "GUSH", "HELKA", "X", "Y", "MUNICIPALITY_ID", "MUNICIPALITY_NAME", "BUILDING_INFO_LAST_UPDATE", "BUILDING_STREET", "BUILDING_ADDRESS_NUMBER", "BUILDING_ADDRESS_ENTRANCE", "BUILDING_MIGRASH", "FLOORS_ABOVE_GROUND", "BUILDING_AREA", "STANDARD_NAME_ID", "STANDARD_NAME", "STANDARD_DESIGNATION_ID", "STANDARD_DESIGNATION_NAME", "STANDARD_DESIGNATION_MAIN_USE", "MAIN_USE_NAME", "ROUTE", "DESIGNATION_AREA", "RESIDENTIAL_UNITS", "DESIGNATION_INFO_LAST_UPDATE", "CERTIFICATION_STATUS", "CERTIFICATE_DATE_PRE", "CERTIFICATE_INSERT_DATE_PRE", "CERTIFICATE_SCORE_PRE", "CERTIFICATE_STARS_PRE", "CERTIFICATE_ENERGY_PRE", "CERTIFICATE_DATE_A", "CERTIFICATE_INSERT_DATE_A", "CERTIFICATE_SCORE_A", "CERTIFICATE_STARS_A", "CERTIFICATE_ENERGY_A", "CERTIFICATE_DATE_B", "CERTIFICATE_INSERT_DATE_B", "CERTIFICATE_SCORE_B", "CERTIFICATE_STARS_B", "CERTIFICATE_ENERGY_B", "DESIGNATION_INFO_STATUS" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"PROJECT_ID"') }} as project_id,
        {{ null_junk('"BUILDING_ID"') }} as building_id,
        {{ stg_id_text('"DESIGNATION_ID"') }} as designation_id,
        {{ null_junk('"PROJECT_HALTED"') }} as project_halted,
        {{ stg_ts('"PROJECT_GENERAL_LAST_UPDATE"') }} as project_general_last_update,
        {{ stg_int('"BUILDING_COUNT"') }} as building_count,
        {{ stg_int('"DESIGNATION_COUNT"') }} as designation_count,
        {{ null_junk('"GUSH"') }} as gush,
        {{ null_junk('"HELKA"') }} as helka,
        {{ stg_float('"X"') }} as x,
        {{ stg_float('"Y"') }} as y,
        {{ stg_int('"MUNICIPALITY_ID"') }} as municipality_id,
        {{ null_junk('"MUNICIPALITY_NAME"') }} as municipality_name,
        {{ stg_ts('"BUILDING_INFO_LAST_UPDATE"') }} as building_info_last_update,
        {{ null_junk('"BUILDING_STREET"') }} as building_street,
        {{ null_junk('"BUILDING_ADDRESS_NUMBER"') }} as building_address_number,
        {{ null_junk('"BUILDING_ADDRESS_ENTRANCE"') }} as building_address_entrance,
        {{ null_junk('"BUILDING_MIGRASH"') }} as building_migrash,
        {{ stg_float('"FLOORS_ABOVE_GROUND"') }} as floors_above_ground,
        {{ stg_float('"BUILDING_AREA"') }} as building_area,
        {{ null_junk('"STANDARD_NAME_ID"') }} as standard_name_id,
        {{ null_junk('"STANDARD_NAME"') }} as standard_name,
        {{ stg_int('"STANDARD_DESIGNATION_ID"') }} as standard_designation_id,
        {{ null_junk('"STANDARD_DESIGNATION_NAME"') }} as standard_designation_name,
        {{ stg_int('"STANDARD_DESIGNATION_MAIN_USE"') }} as standard_designation_main_use,
        {{ null_junk('"MAIN_USE_NAME"') }} as main_use_name,
        {{ null_junk('"ROUTE"') }} as route,
        {{ stg_float('"DESIGNATION_AREA"') }} as designation_area,
        {{ stg_int('"RESIDENTIAL_UNITS"') }} as residential_units,
        {{ stg_ts('"DESIGNATION_INFO_LAST_UPDATE"') }} as designation_info_last_update,
        {{ null_junk('"CERTIFICATION_STATUS"') }} as certification_status,
        {{ stg_ts('"CERTIFICATE_DATE_PRE"') }} as certificate_date_pre,
        {{ stg_ts('"CERTIFICATE_INSERT_DATE_PRE"') }} as certificate_insert_date_pre,
        {{ stg_float('"CERTIFICATE_SCORE_PRE"') }} as certificate_score_pre,
        {{ stg_int('"CERTIFICATE_STARS_PRE"') }} as certificate_stars_pre,
        {{ null_junk('"CERTIFICATE_ENERGY_PRE"') }} as certificate_energy_pre,
        {{ stg_ts('"CERTIFICATE_DATE_A"') }} as certificate_date_a,
        {{ stg_ts('"CERTIFICATE_INSERT_DATE_A"') }} as certificate_insert_date_a,
        {{ stg_float('"CERTIFICATE_SCORE_A"') }} as certificate_score_a,
        {{ stg_int('"CERTIFICATE_STARS_A"') }} as certificate_stars_a,
        {{ null_junk('"CERTIFICATE_ENERGY_A"') }} as certificate_energy_a,
        {{ stg_ts('"CERTIFICATE_DATE_B"') }} as certificate_date_b,
        {{ stg_ts('"CERTIFICATE_INSERT_DATE_B"') }} as certificate_insert_date_b,
        {{ stg_float('"CERTIFICATE_SCORE_B"') }} as certificate_score_b,
        {{ stg_int('"CERTIFICATE_STARS_B"') }} as certificate_stars_b,
        {{ null_junk('"CERTIFICATE_ENERGY_B"') }} as certificate_energy_b,
        {{ null_junk('"DESIGNATION_INFO_STATUS"') }} as designation_info_status,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/greenbuildings' as _source_url

    from source

)

select * from renamed
