version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_UTAH_OPEN_DATA_P_6F5FCC229F
        description: >
          Raw landing table for 2015 Toxic Release Inventory Utah. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_utah_open_data_p_6f5fcc229f__facilities
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per facility (FRS_ID+YEAR+INDUSTRY_SECTOR_CODE+INDUSTRY_SECTOR+TRI_FACILITY_ID+CAS_COMPOUND_ID+SRS_ID unique). SPINE_ENTITY: facility."
    columns:
      - name: frs_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: year
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: industry_sector_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: industry_sector
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: tri_facility_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: cas_compound_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: srs_id
        data_tests:
          - not_null:
              config:
                severity: warn
