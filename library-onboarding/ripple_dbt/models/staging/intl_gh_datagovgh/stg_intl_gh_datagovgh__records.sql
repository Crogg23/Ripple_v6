{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'INTL_GH_DATAGOVGH') }}

),

renamed as (

    select
        DATASET_TITLE as dataset_title,
        CATALOG as catalog,
        MINISTRY_AGENCY as ministry_agency,
        DESCRIPTION as description,
        RESOURCE_URL as resource_url,
        FORMAT as format,
        DATE_PUBLISHED as date_published,
        LICENSE as license,
        VIEWS_COUNT as views_count,
        DOWNLOADS_COUNT as downloads_count,
        _INGESTED_AT as _loaded_at,
        'https://data.gov.gh/' as _source_url

    from source

)

select * from renamed
