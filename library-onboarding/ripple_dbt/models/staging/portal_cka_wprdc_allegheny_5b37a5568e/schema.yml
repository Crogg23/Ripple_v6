version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_5B37A5568E
        description: >
          Raw landing table for PLI Permits. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_5b37a5568e__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PERMIT_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: permit_id
        data_tests:
          - unique
          - not_null
