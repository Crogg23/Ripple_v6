{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'XC_OWID_AI_INCIDENTS_ANNUAL') }}

),

renamed as (

    select
        ENTITY as entity,
        CODE as code,
        YEAR as year,
        ANNUAL_REPORTED_ARTIFICIAL_INTELLIGENCE_INCIDENTS_AND_CONTROVERSIES as annual_reported_artificial_intelligence_incidents_and_controversies,
        INGESTED_AT as _loaded_at,
        'https://ourworldindata.org/grapher/annual-reported-ai-incidents-controversies.csv?v=1&csvType=full' as _source_url

    from source

)

select * from renamed
