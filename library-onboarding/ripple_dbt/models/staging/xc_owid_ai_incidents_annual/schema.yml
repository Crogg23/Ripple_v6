version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: XC_OWID_AI_INCIDENTS_ANNUAL
        description: >
          Raw landing table for Annual reported AI incidents & controversies (AIID via AI Index). Grain not yet determined.

models:
  - name: stg_xc_owid_ai_incidents_annual__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
