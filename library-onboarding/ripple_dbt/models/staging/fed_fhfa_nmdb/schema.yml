version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_FHFA_NMDB
        description: >
          Raw landing table for National Mortgage Database (NMDB) Public Use File. Grain not yet determined.

models:
  - name: stg_fed_fhfa_nmdb__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
