{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'XC_NAGIX_DPRK_MISSILE_TESTS') }}

),

renamed as (

    select
        DATE as date,
        TIME as time,
        SERIES as series,
        MISSILE as missile,
        FACILITY as facility,
        LANDING as landing,
        APOGEE as apogee,
        DISTANCE as distance,
        BEARING as bearing,
        OUTCOME as outcome,
        DESCRIPTION as description,
        GLIDE as glide,
        INGESTED_AT as _loaded_at,
        'https://raw.githubusercontent.com/nagix/nk-missile-tests/master/data/test.en.json' as _source_url

    from source

)

select * from renamed
