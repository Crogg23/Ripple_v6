version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_VIRGINIA_OPEN_DA_ACE8AC0352
        description: >
          Raw landing table for TMDL Watersheds. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_virginia_open_da_ace8ac0352__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TMDL_EQ_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: tmdl_eq_id
        data_tests:
          - unique
          - not_null
