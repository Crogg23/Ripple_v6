{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TMDL_EQ_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TMDL_EQ_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_ACE8AC0352') }}

),

renamed as (

    select
        "OBJECTID_1" as objectid_1,
        "TMDL_EQ_ID" as tmdl_eq_id,
        "PROJECT" as project,
        "WSHD_ID" as wshd_id,
        "INSERTED_BY" as inserted_by,
        "INSERTED_DATE" as inserted_date,
        "CHANGED_BY" as changed_by,
        "CHANGED_DATE" as changed_date,
        "OBJECTID" as objectid,
        "PJ_ID" as pj_id,
        "PJ_NAME" as pj_name,
        "TMDL_EQ_ID_1" as tmdl_eq_id_1,
        "WSHD_NAME" as wshd_name,
        "PJ_RPT_EPA_APPRVD_DT" as pj_rpt_epa_apprvd_dt,
        "PJ_RPT_FINAL_APPRVD_LINK" as pj_rpt_final_apprvd_link,
        "PJ_RPT_SWCB_APPRVD_DT" as pj_rpt_swcb_apprvd_dt,
        "PROJECT_STATUS" as project_status,
        "POL_NAME" as pol_name,
        "IMP_NAME" as imp_name,
        "REGION" as region,
        "SHAPE_LENGTH" as shape_length,
        "SHAPE_AREA" as shape_area,
        "DATA_DISCLAIMER" as data_disclaimer,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/tmdl-watersheds' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by tmdl_eq_id order by _loaded_at desc) = 1
