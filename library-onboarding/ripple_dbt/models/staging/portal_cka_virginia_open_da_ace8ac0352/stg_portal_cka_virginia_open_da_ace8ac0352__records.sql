{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TMDL_EQ_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TMDL_EQ_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 2 of 23 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid_1: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   project: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (TMDL_EQ_ID) is NOT unique within a load as landed at generation time -- it would collapse 4 of 1,045 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_ACE8AC0352') }}
    qualify row_number() over (partition by "OBJECTID_1", "TMDL_EQ_ID", "PROJECT", "WSHD_ID", "INSERTED_BY", "INSERTED_DATE", "CHANGED_BY", "CHANGED_DATE", "OBJECTID", "PJ_ID", "PJ_NAME", "TMDL_EQ_ID_1", "WSHD_NAME", "PJ_RPT_EPA_APPRVD_DT", "PJ_RPT_FINAL_APPRVD_LINK", "PJ_RPT_SWCB_APPRVD_DT", "PROJECT_STATUS", "POL_NAME", "IMP_NAME", "REGION", "SHAPE_LENGTH", "SHAPE_AREA", "DATA_DISCLAIMER" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID_1"') }} as objectid_1,
        {{ null_junk('"TMDL_EQ_ID"') }} as tmdl_eq_id,
        {{ stg_int('"PROJECT"') }} as project,
        {{ null_junk('"WSHD_ID"') }} as wshd_id,
        {{ null_junk('"INSERTED_BY"') }} as inserted_by,
        {{ null_junk('"INSERTED_DATE"') }} as inserted_date,
        {{ null_junk('"CHANGED_BY"') }} as changed_by,
        {{ null_junk('"CHANGED_DATE"') }} as changed_date,
        {{ null_junk('"OBJECTID"') }} as objectid,
        {{ null_junk('"PJ_ID"') }} as pj_id,
        {{ null_junk('"PJ_NAME"') }} as pj_name,
        {{ null_junk('"TMDL_EQ_ID_1"') }} as tmdl_eq_id_1,
        {{ null_junk('"WSHD_NAME"') }} as wshd_name,
        {{ null_junk('"PJ_RPT_EPA_APPRVD_DT"') }} as pj_rpt_epa_apprvd_dt,
        {{ null_junk('"PJ_RPT_FINAL_APPRVD_LINK"') }} as pj_rpt_final_apprvd_link,
        {{ null_junk('"PJ_RPT_SWCB_APPRVD_DT"') }} as pj_rpt_swcb_apprvd_dt,
        {{ null_junk('"PROJECT_STATUS"') }} as project_status,
        {{ null_junk('"POL_NAME"') }} as pol_name,
        {{ null_junk('"IMP_NAME"') }} as imp_name,
        {{ null_junk('"REGION"') }} as region,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        {{ null_junk('"SHAPE_AREA"') }} as shape_area,
        {{ null_junk('"DATA_DISCLAIMER"') }} as data_disclaimer,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/tmdl-watersheds' as _source_url

    from source

)

select * from renamed
