{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LATITUDE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_8E6BDE379C') }}

),

renamed as (

    select
        "ID" as id,
        "DEVICE_ID" as device_id,
        "RECORD_OID" as record_oid,
        "COUNT_START_DATE" as count_start_date,
        "COUNT_END_DATE" as count_end_date,
        "AVERAGE_DAILY_CAR_TRAFFIC" as average_daily_car_traffic,
        "AVERAGE_DAILY_BIKE_TRAFFIC" as average_daily_bike_traffic,
        "COUNTER_NUMBER" as counter_number,
        "COUNTER_TYPE" as counter_type,
        "SPEED_LIMIT" as speed_limit,
        "MAX_SPEED" as max_speed,
        "MEDIAN_SPEED" as median_speed,
        "PERCENT_OVER_LIMIT" as percent_over_limit,
        "SPEED85_PERCENT" as speed85_percent,
        "SPEED95_PERCENT" as speed95_percent,
        "LONGITUDE" as longitude,
        "LATITUDE" as latitude,
        "NEIGHBORHOOD" as neighborhood,
        "COUNCIL_DISTRICT" as council_district,
        "WARD" as ward,
        "TRACT" as tract,
        "PUBLIC_WORKS_DIVISION" as public_works_division,
        "PLI_DIVISION" as pli_division,
        "POLICE_ZONE" as police_zone,
        "FIRE_ZONE" as fire_zone,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/traffic-count-data-city-of-pittsburgh' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by latitude order by _loaded_at desc) = 1
