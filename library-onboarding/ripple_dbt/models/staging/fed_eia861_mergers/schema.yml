version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_EIA861_MERGERS
        description: >
          Raw landing table, ingestion sweep 2026-08-05. All columns arrive as TEXT.

models:
  - name: stg_fed_eia861_mergers__all
    description: >
      Minimal passthrough staging view for FED_EIA861_MERGERS. Grain not yet
      determined -- snake_case rename only, no dedup.
