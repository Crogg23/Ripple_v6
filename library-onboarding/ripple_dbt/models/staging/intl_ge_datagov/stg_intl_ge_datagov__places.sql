{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (COUNTRY is unique)
-- SPINE_ENTITY: place  (natural_key: COUNTRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 12 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'INTL_GE_DATAGOV') }}
    qualify row_number() over (partition by "COUNTRY" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"DATASET_ID"') }} as dataset_id,
        {{ null_junk('"DATASET_TITLE"') }} as dataset_title,
        {{ null_junk('"PUBLISHER_INSTITUTION"') }} as publisher_institution,
        {{ null_junk('"POLICY_AREA"') }} as policy_area,
        {{ null_junk('"DATE_PUBLISHED"') }} as date_published,
        {{ null_junk('"DATE_UPDATED"') }} as date_updated,
        {{ null_junk('"FORMAT"') }} as format,
        {{ null_junk('"DOWNLOAD_URL"') }} as download_url,
        {{ null_junk('"DESCRIPTION"') }} as description,
        {{ null_junk('"LICENSE"') }} as license,
        {{ null_junk('"COUNTRY"') }} as country,
        {{ null_junk('"OGP_COMMITMENT_ID"') }} as ogp_commitment_id,
        _INGESTED_AT as _loaded_at,
        'https://www.opengovpartnership.org/members/georgia/commitments/GE0019/' as _source_url

    from source

)

select * from renamed
