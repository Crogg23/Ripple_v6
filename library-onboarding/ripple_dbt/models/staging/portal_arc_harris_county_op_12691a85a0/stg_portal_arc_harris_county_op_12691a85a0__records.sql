{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 19 of 42 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   zip4: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   population: integer
--   countyfips: integer
--   sourcedate: integer
--   val_date: integer
--   csllea08id: integer
--   subtype1: integer
--   subtype2: integer
--   tribal: integer
--   numpre: integer
--   numfixsub: integer
--   nummobile: integer
--   ftsworn: integer
--   ftciv: integer
--   ptsworn: integer
--   ptciv: integer
--   creationdate: integer
--   editdate: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_HARRIS_COUNTY_OP_12691A85A0') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP"') }} as zip,
        {{ stg_id_text('"ZIP4"') }} as zip4,
        {{ null_junk('"TELEPHONE"') }} as telephone,
        {{ null_junk('"TYPE"') }} as type,
        {{ null_junk('"STATUS"') }} as status,
        {{ stg_int('"POPULATION"') }} as population,
        {{ null_junk('"COUNTY"') }} as county,
        {{ stg_int('"COUNTYFIPS"') }} as countyfips,
        {{ null_junk('"COUNTRY"') }} as country,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"NAICS_DESC"') }} as naics_desc,
        {{ null_junk('"SOURCE"') }} as source,
        {{ stg_int('"SOURCEDATE"') }} as sourcedate,
        {{ null_junk('"VAL_METHOD"') }} as val_method,
        {{ stg_int('"VAL_DATE"') }} as val_date,
        {{ null_junk('"WEBSITE"') }} as website,
        {{ null_junk('"CI_ID"') }} as ci_id,
        {{ stg_int('"CSLLEA08ID"') }} as csllea08id,
        {{ stg_int('"SUBTYPE1"') }} as subtype1,
        {{ stg_int('"SUBTYPE2"') }} as subtype2,
        {{ stg_int('"TRIBAL"') }} as tribal,
        {{ stg_int('"NUMPRE"') }} as numpre,
        {{ stg_int('"NUMFIXSUB"') }} as numfixsub,
        {{ stg_int('"NUMMOBILE"') }} as nummobile,
        {{ stg_int('"FTSWORN"') }} as ftsworn,
        {{ stg_int('"FTCIV"') }} as ftciv,
        {{ stg_int('"PTSWORN"') }} as ptsworn,
        {{ stg_int('"PTCIV"') }} as ptciv,
        {{ null_junk('"GLOBALID"') }} as globalid,
        {{ stg_int('"CREATIONDATE"') }} as creationdate,
        {{ null_junk('"CREATOR"') }} as creator,
        {{ stg_int('"EDITDATE"') }} as editdate,
        {{ null_junk('"EDITOR"') }} as editor,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://geo-harriscounty.opendata.arcgis.com/datasets/bb9013e5a3d44d99a04f55def1527fba_61' as _source_url

    from source

)

select * from renamed
