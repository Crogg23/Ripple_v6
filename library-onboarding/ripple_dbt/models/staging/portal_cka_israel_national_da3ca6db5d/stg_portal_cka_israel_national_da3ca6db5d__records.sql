{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (WORK_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: WORK_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 11 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   executor_id: integer
--   has_cranes: integer
--   safety_warrents: integer
--   sanctions: integer
--   sanctions_sum: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_DA3CA6DB5D') }}
    qualify row_number() over (partition by "WORK_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"WORK_ID"') }} as work_id,
        {{ null_junk('"SITE_NAME"') }} as site_name,
        {{ null_junk('"EXECUTOR_NAME"') }} as executor_name,
        {{ stg_int('"EXECUTOR_ID"') }} as executor_id,
        {{ null_junk('"FOREMAN_NAME"') }} as foreman_name,
        {{ stg_int('"HAS_CRANES"') }} as has_cranes,
        {{ null_junk('"CITY_NAME"') }} as city_name,
        {{ null_junk('"BUILD_TYPES"') }} as build_types,
        {{ stg_int('"SAFETY_WARRENTS"') }} as safety_warrents,
        {{ stg_int('"SANCTIONS"') }} as sanctions,
        {{ stg_int('"SANCTIONS_SUM"') }} as sanctions_sum,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/b072e36c-a53b-49e1-be08-4a608fcf4638' as _source_url

    from source

)

select * from renamed
