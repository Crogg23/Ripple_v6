{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (WORK_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: WORK_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_DA3CA6DB5D') }}

),

renamed as (

    select
        "WORK_ID" as work_id,
        "SITE_NAME" as site_name,
        "EXECUTOR_NAME" as executor_name,
        "EXECUTOR_ID" as executor_id,
        "FOREMAN_NAME" as foreman_name,
        "HAS_CRANES" as has_cranes,
        "CITY_NAME" as city_name,
        "BUILD_TYPES" as build_types,
        "SAFETY_WARRENTS" as safety_warrents,
        "SANCTIONS" as sanctions,
        "SANCTIONS_SUM" as sanctions_sum,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/b072e36c-a53b-49e1-be08-4a608fcf4638' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by work_id order by _loaded_at desc) = 1
