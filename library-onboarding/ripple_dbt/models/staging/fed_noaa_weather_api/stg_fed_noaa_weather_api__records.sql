{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 32 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   sent: timestamp
--   effective: timestamp
--   onset: timestamp
--   expires: timestamp
--   ends: timestamp
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_NOAA_WEATHER_API') }}
    qualify row_number() over (partition by "ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"TYPE"') }} as type,
        {{ null_junk('"EVENT"') }} as event,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"MESSAGE_TYPE"') }} as message_type,
        {{ null_junk('"SEVERITY"') }} as severity,
        {{ null_junk('"CERTAINTY"') }} as certainty,
        {{ null_junk('"URGENCY"') }} as urgency,
        {{ null_junk('"AREA_DESC"') }} as area_desc,
        {{ stg_ts('"SENT"') }} as sent,
        {{ stg_ts('"EFFECTIVE"') }} as effective,
        {{ stg_ts('"ONSET"') }} as onset,
        {{ stg_ts('"EXPIRES"') }} as expires,
        {{ stg_ts('"ENDS"') }} as ends,
        {{ null_junk('"HEADLINE"') }} as headline,
        {{ null_junk('"DESCRIPTION"') }} as description,
        {{ null_junk('"INSTRUCTION"') }} as instruction,
        {{ null_junk('"SENDER_NAME"') }} as sender_name,
        {{ null_junk('"ZONE_UGC"') }} as zone_ugc,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        {{ null_junk('"STATION_ID"') }} as station_id,
        {{ null_junk('"TEMPERATURE"') }} as temperature,
        {{ null_junk('"WIND_SPEED"') }} as wind_speed,
        {{ null_junk('"WIND_DIRECTION"') }} as wind_direction,
        {{ null_junk('"TIMESTAMP"') }} as timestamp,
        {{ null_junk('"FORECAST_OFFICE"') }} as forecast_office,
        {{ null_junk('"GRID_X"') }} as grid_x,
        {{ null_junk('"GRID_Y"') }} as grid_y,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"FIPS_CODE"') }} as fips_code,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        _INGESTED_AT as _loaded_at,
        'https://www.weather.gov/documentation/services-web-api' as _source_url

    from source

)

select * from renamed
