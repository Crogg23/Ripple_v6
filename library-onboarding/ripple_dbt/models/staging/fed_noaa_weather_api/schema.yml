version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_NOAA_WEATHER_API
        description: >
          Raw landing table for Weather.gov API (National Weather Service). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_fed_noaa_weather_api__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: id
        data_tests:
          - unique
          - not_null
