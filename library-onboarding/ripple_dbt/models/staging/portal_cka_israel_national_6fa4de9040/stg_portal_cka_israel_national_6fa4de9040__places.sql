{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_AREA)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 24 of 29 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   oid: integer
--   pop_2018: integer
--   usetypecod: integer
--   tazarea: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   sumacciden: integer
--   dead: integer
--   sever_inj: integer
--   sligh_inj: integer
--   pedestrinj: integer
--   electric_inj: integer
--   inj0_19: integer
--   inj20_64: integer
--   inj65: integer
--   injtotal: integer
--   totdrivers: integer
--   motorcycle: integer
--   truck: integer
--   bicycle: integer
--   scooters: integer
--   private: integer
--   vehicle: integer
--   acc_index: float
--   yearmonth: integer
--   citycode: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_6FA4DE9040') }}
    qualify row_number() over (partition by "SHAPE_AREA" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_int('"OID"') }} as oid,
        {{ stg_int('"POP_2018"') }} as pop_2018,
        {{ null_junk('"USETYPE"') }} as usetype,
        {{ stg_int('"USETYPECOD"') }} as usetypecod,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"MAINUSE"') }} as mainuse,
        {{ stg_id_text('"TAZAREA"') }} as tazarea,
        {{ stg_int('"SUMACCIDEN"') }} as sumacciden,
        {{ stg_int('"DEAD"') }} as dead,
        {{ stg_int('"SEVER_INJ"') }} as sever_inj,
        {{ stg_int('"SLIGH_INJ"') }} as sligh_inj,
        {{ stg_int('"PEDESTRINJ"') }} as pedestrinj,
        {{ stg_int('"ELECTRIC_INJ"') }} as electric_inj,
        {{ stg_int('"INJ0_19"') }} as inj0_19,
        {{ stg_int('"INJ20_64"') }} as inj20_64,
        {{ stg_int('"INJ65"') }} as inj65,
        {{ stg_int('"INJTOTAL"') }} as injtotal,
        {{ stg_int('"TOTDRIVERS"') }} as totdrivers,
        {{ stg_int('"MOTORCYCLE"') }} as motorcycle,
        {{ stg_int('"TRUCK"') }} as truck,
        {{ stg_int('"BICYCLE"') }} as bicycle,
        {{ stg_int('"SCOOTERS"') }} as scooters,
        {{ stg_int('"PRIVATE"') }} as private,
        {{ stg_int('"VEHICLE"') }} as vehicle,
        {{ stg_float('"ACC_INDEX"') }} as acc_index,
        {{ stg_int('"YEARMONTH"') }} as yearmonth,
        {{ stg_int('"CITYCODE"') }} as citycode,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        {{ null_junk('"SHAPE_AREA"') }} as shape_area,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/accid_taz' as _source_url

    from source

)

select * from renamed
