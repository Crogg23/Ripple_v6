{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 11 of 63 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   date_registration: timestamp
--   org_owned_by_person_s_with: boolean
--   create_dt: timestamp
--   total_authorized_shares: integer
--   dissolution_date: timestamp
--   annual_report_due_date: timestamp
--   began_transacting_in_ct: timestamp
--   computed_region_snd5_k6zv: integer
--   computed_region_dam5_q64j: integer
--   computed_region_nhmp_cq6b: integer
--   computed_region_m4y2_whse: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_CONNECTICUT_OPEN_88B075D7AF') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"ACCOUNTNUMBER"') }} as accountnumber,
        {{ stg_ts('"DATE_REGISTRATION"') }} as date_registration,
        {{ null_junk('"MAILING_ADDRESS"') }} as mailing_address,
        {{ null_junk('"WOMAN_OWNED_ORGANIZATION"') }} as woman_owned_organization,
        {{ null_junk('"VETERAN_OWNED_ORGANIZATION"') }} as veteran_owned_organization,
        {{ null_junk('"MINORITY_OWNED_ORGANIZATION"') }} as minority_owned_organization,
        {{ stg_bool('"ORG_OWNED_BY_PERSON_S_WITH"') }} as org_owned_by_person_s_with,
        {{ null_junk('"ORGANIZATION_IS_LGBTQI_OWNED"') }} as organization_is_lgbtqi_owned,
        {{ stg_ts('"CREATE_DT"') }} as create_dt,
        {{ null_junk('"BUSINESS_TYPE"') }} as business_type,
        {{ null_junk('"CITIZENSHIP"') }} as citizenship,
        {{ null_junk('"COUNTRY_FORMATION"') }} as country_formation,
        {{ null_junk('"STATE_OR_TERRITORY_FORMATION"') }} as state_or_territory_formation,
        {{ null_junk('"BUSINESS_EMAIL_ADDRESS"') }} as business_email_address,
        {{ null_junk('"FORMATION_PLACE"') }} as formation_place,
        {{ null_junk('"REASON_FOR_ADMINISTRATIVE"') }} as reason_for_administrative,
        {{ stg_int('"TOTAL_AUTHORIZED_SHARES"') }} as total_authorized_shares,
        {{ null_junk('"BILLINGSTREET"') }} as billingstreet,
        {{ null_junk('"BILLINGCITY"') }} as billingcity,
        {{ null_junk('"BILLINGCOUNTRY"') }} as billingcountry,
        {{ null_junk('"BILLINGPOSTALCODE"') }} as billingpostalcode,
        {{ null_junk('"BILLINGSTATE"') }} as billingstate,
        {{ stg_ts('"DISSOLUTION_DATE"') }} as dissolution_date,
        {{ null_junk('"CATEGORY_SURVEY_EMAIL_ADDRESS"') }} as category_survey_email_address,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"SUB_STATUS"') }} as sub_status,
        {{ stg_ts('"ANNUAL_REPORT_DUE_DATE"') }} as annual_report_due_date,
        {{ stg_ts('"BEGAN_TRANSACTING_IN_CT"') }} as began_transacting_in_ct,
        {{ null_junk('"BUSINESS_NAME_IN_STATE_COUNTRY"') }} as business_name_in_state_country,
        {{ null_junk('"BILLING_UNIT"') }} as billing_unit,
        {{ null_junk('"MAILING_INTERNATIONAL_ADDRESS"') }} as mailing_international_address,
        {{ null_junk('"DATE_OF_ORGANIZATION_MEETING"') }} as date_of_organization_meeting,
        {{ null_junk('"GEO_LOCATION"') }} as geo_location,
        {{ stg_int('"COMPUTED_REGION_SND5_K6ZV"') }} as computed_region_snd5_k6zv,
        {{ stg_int('"COMPUTED_REGION_DAM5_Q64J"') }} as computed_region_dam5_q64j,
        {{ stg_int('"COMPUTED_REGION_NHMP_CQ6B"') }} as computed_region_nhmp_cq6b,
        {{ stg_int('"COMPUTED_REGION_M4Y2_WHSE"') }} as computed_region_m4y2_whse,
        {{ null_junk('"NAICS_SUB_CODE"') }} as naics_sub_code,
        {{ null_junk('"MAILING_JURISDICTION_ADDRESS"') }} as mailing_jurisdiction_address,
        {{ null_junk('"MAILING_JURISDICTION_2"') }} as mailing_jurisdiction_2,
        {{ null_junk('"MAILING_JURISDICTION"') }} as mailing_jurisdiction,
        {{ null_junk('"MAILING_JURISDICTION_1"') }} as mailing_jurisdiction_1,
        {{ null_junk('"MAILING_JURISDICTION_4"') }} as mailing_jurisdiction_4,
        {{ null_junk('"MAILING_JURISDICTION_COUNTRY"') }} as mailing_jurisdiction_country,
        {{ null_junk('"OFFICE_JURISDICTION_ADDRESS"') }} as office_jurisdiction_address,
        {{ null_junk('"OFFICE_JURISDICTION_2"') }} as office_jurisdiction_2,
        {{ null_junk('"OFFICE_JURISDICTION"') }} as office_jurisdiction,
        {{ null_junk('"OFFICE_JURISDICTION_1"') }} as office_jurisdiction_1,
        {{ null_junk('"OFFICE_JURISDICTION_4"') }} as office_jurisdiction_4,
        {{ null_junk('"OFFICE_IN_JURISDICTION_COUNTRY"') }} as office_in_jurisdiction_country,
        {{ null_junk('"RECORD_ADDRESS"') }} as record_address,
        {{ null_junk('"RECORDS_ADDRESS_STREET"') }} as records_address_street,
        {{ null_junk('"RECORDS_ADDRESS_UNIT"') }} as records_address_unit,
        {{ null_junk('"RECORDS_ADDRESS_CITY"') }} as records_address_city,
        {{ null_junk('"RECORDS_ADDRESS_STATE"') }} as records_address_state,
        {{ null_junk('"RECORDS_ADDRESS_ZIP_CODE"') }} as records_address_zip_code,
        {{ null_junk('"RECORDS_ADDRESS_COUNTRY"') }} as records_address_country,
        {{ null_junk('"MAILING_JURISDICTION_3"') }} as mailing_jurisdiction_3,
        {{ null_junk('"OFFICE_JURISDICTION_3"') }} as office_jurisdiction_3,
        {{ null_junk('"MAIL_JURISDICTION"') }} as mail_jurisdiction,
        INGESTED_AT as _loaded_at,
        'https://data.ct.gov/d/n7gp-d28j' as _source_url

    from source

)

select * from renamed
