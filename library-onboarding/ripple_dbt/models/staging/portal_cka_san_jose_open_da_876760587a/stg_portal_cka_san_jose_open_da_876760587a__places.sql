{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_AREA)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 12 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_876760587A') }}
    qualify row_number() over (partition by "SHAPE_AREA" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"OBJECTID"') }} as objectid,
        {{ null_junk('"FACILITYID"') }} as facilityid,
        {{ null_junk('"GISOBJID"') }} as gisobjid,
        {{ null_junk('"PARKDISTRICT"') }} as parkdistrict,
        {{ null_junk('"DISTRICTCLASS"') }} as districtclass,
        {{ null_junk('"SERVICEYARD"') }} as serviceyard,
        {{ null_junk('"UPDATE_COUNT"') }} as update_count,
        {{ null_junk('"LASTUPDATE"') }} as lastupdate,
        {{ null_junk('"ENTERPRISEID"') }} as enterpriseid,
        {{ null_junk('"INTID"') }} as intid,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        {{ null_junk('"SHAPE_AREA"') }} as shape_area,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/park-districts' as _source_url

    from source

)

select * from renamed
