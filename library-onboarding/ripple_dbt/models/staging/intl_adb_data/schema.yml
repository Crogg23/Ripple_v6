version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: INTL_ADB_DATA
        description: >
          Raw landing table for ADB Data Library — Projects and Economic Indicators. Grain not yet determined.

models:
  - name: stg_intl_adb_data__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
