{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE__LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_F40C1F2ECE') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "EVENT_SOURCE_ID" as event_source_id,
        "CAR_SPEED_LIMIT" as car_speed_limit,
        "TRUCK_SPEED_LIMIT" as truck_speed_limit,
        "SPEEDZONE_TYPE_DSC" as speedzone_type_dsc,
        "AUTHORITY_DSC" as authority_dsc,
        "LENGTH" as length,
        "ROUTE_COMMON_NAME" as route_common_name,
        "LOC_COMP_DIRECTIONALITY_NAME" as loc_comp_directionality_name,
        "ROUTE_FROM_MEASURE" as route_from_measure,
        "FROM_JURISDICTION" as from_jurisdiction,
        "START_LABEL" as start_label,
        "ROUTE_TO_MEASURE" as route_to_measure,
        "TO_JURISDICTION" as to_jurisdiction,
        "END_LABEL" as end_label,
        "COMMENT_TEXT" as comment_text,
        "SUB_DIVISION_NAME" as sub_division_name,
        "RADAR_STUDY_RECEIVED_DATE" as radar_study_received_date,
        "STUDY_DATE" as study_date,
        "RESOLUTION_SIGNED_DATE" as resolution_signed_date,
        "SIGN_INSTALLED_DATE" as sign_installed_date,
        "RESOLUTION_ID" as resolution_id,
        "EVENT_LOCATION_ID" as event_location_id,
        "EVENT_COMPONENT_ID" as event_component_id,
        "RTE_TYPE_CD" as rte_type_cd,
        "RTE_TYPE_NM" as rte_type_nm,
        "FROM_DISTRICT" as from_district,
        "TO_DISTRICT" as to_district,
        "SHAPE__LENGTH" as shape__length,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/vdot-posted-speed-limits' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by shape__length order by _loaded_at desc) = 1
