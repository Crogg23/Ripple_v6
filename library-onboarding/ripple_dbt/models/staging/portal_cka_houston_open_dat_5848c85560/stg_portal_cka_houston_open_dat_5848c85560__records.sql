{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TRANSPORTATION_ID+VEHICLE_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TRANSPORTATION_ID, VEHICLE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_HOUSTON_OPEN_DAT_5848C85560') }}

),

renamed as (

    select
        "APPLICANT_TYPE" as applicant_type,
        "TRANSPORTATION_ID" as transportation_id,
        "DBA_ALIAS" as dba_alias,
        "VEHICLE_ID" as vehicle_id,
        "VEHICLE_STATUS" as vehicle_status,
        "YEAR" as year,
        "MAKE" as make,
        "MODEL" as model,
        "CAPACITY" as capacity,
        "HLL_NO" as hll_no,
        "INSPECTION_STICKER_NO" as inspection_sticker_no,
        "PERMIT_EXPIRATION_DATE" as permit_expiration_date,
        "PERMIT_ISSUE_DATE" as permit_issue_date,
        "MILEAGE" as mileage,
        "VEHICLE_EFFECTIVE_END_DATE" as vehicle_effective_end_date,
        "VEHICLE_ISSUE_DATE" as vehicle_issue_date,
        "COMPANY_OWNER" as company_owner,
        "APPLICANT_COMPANY" as applicant_company,
        "COLOR_SCHEME" as color_scheme,
        "MEDALLION_NUMBER" as medallion_number,
        INGESTED_AT as _loaded_at,
        'https://data.houstontx.gov/dataset/all-active-vehicles-regulated-by-ara-transportation-section-as-of-05-01-2013' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by transportation_id, vehicle_id order by _loaded_at desc) = 1
