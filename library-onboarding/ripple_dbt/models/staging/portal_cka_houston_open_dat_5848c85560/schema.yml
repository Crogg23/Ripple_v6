version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_HOUSTON_OPEN_DAT_5848C85560
        description: >
          Raw landing table for All Active Vehicles Regulated by ARA Transportation Section as of 05/01/2013. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_houston_open_dat_5848c85560__records
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TRANSPORTATION_ID+VEHICLE_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: transportation_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: vehicle_id
        data_tests:
          - not_null:
              config:
                severity: warn
