version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_COLORADO_INFORMA_E80CA7800E
        description: >
          Raw landing table for CDLE - apprenticeship.gov - Apprentice by State Workload. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_colorado_informa_e80ca7800e__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (FIPS+EXIT_DATE+FISCAL_YEAR+START_DATE+STATE_ID+STATE_ID_COPY+APPRENTICE_ID+PROGRAM_ID unique). SPINE_ENTITY: place."
    columns:
      - name: county_fips
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: exit_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: fiscal_year
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: start_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: state_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: state_id_copy
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: apprentice_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: program_id
        data_tests:
          - not_null:
              config:
                severity: warn
