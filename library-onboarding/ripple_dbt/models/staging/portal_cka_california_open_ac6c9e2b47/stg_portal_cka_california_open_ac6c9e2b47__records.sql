{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (STN_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: STN_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 8 of 32 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   gse: float
--   rpe: float
--   well_depth: integer
--   msmt_date: timestamp
--   wlm_rpe: float
--   wlm_gse: float
--   gwe: float
--   gse_gwe: float
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (STN_ID) is NOT unique within a load as landed at generation time -- it would collapse 1,928,625 of 1,976,268 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_CALIFORNIA_OPEN_AC6C9E2B47') }}
    qualify row_number() over (partition by "SITE_CODE", "STN_ID", "SWN", "WELL_NAME", "CONTINUOUS_DATA_STATION_NUMBER", "LATITUDE", "LONGITUDE", "GSE", "RPE", "GSE_METHOD", "GSE_ACC", "BASIN_CODE", "BASIN_NAME", "COUNTY_NAME", "WELL_DEPTH", "WELL_USE", "WELL_TYPE", "WCR_NO", "MONITORING_PROGRAM", "MSMT_DATE", "WLM_RPE", "WLM_GSE", "GWE", "GSE_GWE", "WLM_QA_DESC", "WLM_QA_DETAIL", "WLM_MTHD_DESC", "WLM_ACC_DESC", "WLM_ORG_NAME", "COOP_ORG_NAME", "SOURCE", "MSMT_CMT" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SITE_CODE"') }} as site_code,
        {{ null_junk('"STN_ID"') }} as stn_id,
        {{ null_junk('"SWN"') }} as swn,
        {{ null_junk('"WELL_NAME"') }} as well_name,
        {{ null_junk('"CONTINUOUS_DATA_STATION_NUMBER"') }} as continuous_data_station_number,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ stg_float('"GSE"') }} as gse,
        {{ stg_float('"RPE"') }} as rpe,
        {{ null_junk('"GSE_METHOD"') }} as gse_method,
        {{ null_junk('"GSE_ACC"') }} as gse_acc,
        {{ null_junk('"BASIN_CODE"') }} as basin_code,
        {{ null_junk('"BASIN_NAME"') }} as basin_name,
        {{ null_junk('"COUNTY_NAME"') }} as county_name,
        {{ stg_int('"WELL_DEPTH"') }} as well_depth,
        {{ null_junk('"WELL_USE"') }} as well_use,
        {{ null_junk('"WELL_TYPE"') }} as well_type,
        {{ null_junk('"WCR_NO"') }} as wcr_no,
        {{ null_junk('"MONITORING_PROGRAM"') }} as monitoring_program,
        {{ stg_ts('"MSMT_DATE"') }} as msmt_date,
        {{ stg_float('"WLM_RPE"') }} as wlm_rpe,
        {{ stg_float('"WLM_GSE"') }} as wlm_gse,
        {{ stg_float('"GWE"') }} as gwe,
        {{ stg_float('"GSE_GWE"') }} as gse_gwe,
        {{ null_junk('"WLM_QA_DESC"') }} as wlm_qa_desc,
        {{ null_junk('"WLM_QA_DETAIL"') }} as wlm_qa_detail,
        {{ null_junk('"WLM_MTHD_DESC"') }} as wlm_mthd_desc,
        {{ null_junk('"WLM_ACC_DESC"') }} as wlm_acc_desc,
        {{ null_junk('"WLM_ORG_NAME"') }} as wlm_org_name,
        {{ null_junk('"COOP_ORG_NAME"') }} as coop_org_name,
        {{ null_junk('"SOURCE"') }} as source,
        {{ null_junk('"MSMT_CMT"') }} as msmt_cmt,
        INGESTED_AT as _loaded_at,
        'https://data.ca.gov/dataset/periodic-groundwater-level-measurements' as _source_url

    from source

)

select * from renamed
