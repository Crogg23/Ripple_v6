version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_OPEN_DATA_DC_E1DD5D3551
        description: >
          Raw landing table for Energy Substations Microgrid. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_open_data_dc_e1dd5d3551__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (UNIQUE_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: unique_id
        data_tests:
          - unique
          - not_null
