version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: INTL_FREEDOMHOUSE
        description: >
          Raw landing table for Freedom in the World — All Data (aggregate + category scores). Grain not yet determined.

models:
  - name: stg_intl_freedomhouse__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
