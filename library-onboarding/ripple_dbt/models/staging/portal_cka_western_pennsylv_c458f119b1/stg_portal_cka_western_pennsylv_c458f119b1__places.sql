{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LONGITUDE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_C458F119B1') }}

),

renamed as (

    select
        "PIN" as pin,
        "ADDRESS" as address,
        "TREASURY_SALE_DATE" as treasury_sale_date,
        "TREASURY_SALE_FLAG" as treasury_sale_flag,
        "TOTAL_TAX_DUE" as total_tax_due,
        "CITY_TAX_DUE" as city_tax_due,
        "COUNTY_TAX_DUE" as county_tax_due,
        "SCHOOL_TAX_DUE" as school_tax_due,
        "LIBRARY_TAX_DUE" as library_tax_due,
        "PWSA_TAX_DUE" as pwsa_tax_due,
        "PARKS_TAX_DUE" as parks_tax_due,
        "DEMO_COST_DUE" as demo_cost_due,
        "CLEAN_LIEN_DUE" as clean_lien_due,
        "LATITUDE" as latitude,
        "LONGITUDE" as longitude,
        "NEIGHBORHOOD_NAME" as neighborhood_name,
        "COUNCIL_DISTRICT" as council_district,
        "WARD" as ward,
        "POLICE_ZONE" as police_zone,
        "FIRE_ZONE" as fire_zone,
        "DPW_STREETS" as dpw_streets,
        "DPW_ENVIRO" as dpw_enviro,
        "DPW_PARKS" as dpw_parks,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/city-treasury-sales' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by longitude order by _loaded_at desc) = 1
