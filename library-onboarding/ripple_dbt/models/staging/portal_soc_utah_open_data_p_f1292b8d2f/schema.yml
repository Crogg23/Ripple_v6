version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_UTAH_OPEN_DATA_P_F1292B8D2F
        description: >
          Raw landing table for Office Of Federal Contract Community Investigations Utah 2004-2013. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_utah_open_data_p_f1292b8d2f__organizations
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per organization (DUNS+FISCAL_YEAR+EVALUATION_CLOSED_DATE unique). SPINE_ENTITY: organization."
    columns:
      - name: duns_no
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: fiscal_year
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: evaluation_closed_date
        data_tests:
          - not_null:
              config:
                severity: warn
