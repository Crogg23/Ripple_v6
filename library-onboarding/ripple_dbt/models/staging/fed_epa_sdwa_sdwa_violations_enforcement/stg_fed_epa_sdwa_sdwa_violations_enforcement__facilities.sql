{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per facility (PWSID+COMPL_PER_BEGIN_DATE+COMPL_PER_END_DATE+NON_COMPL_PER_BEGIN_DATE+VIOLATION_ID+FACILITY_ID+SAMPLE_RESULT_ID+CORRECTIVE_ACTION_ID+ENFORCEMENT_ID unique)
-- SPINE_ENTITY: facility  (natural_key: PWSID, COMPL_PER_BEGIN_DATE, COMPL_PER_END_DATE, NON_COMPL_PER_BEGIN_DATE, VIOLATION_ID, FACILITY_ID, SAMPLE_RESULT_ID, CORRECTIVE_ACTION_ID, ENFORCEMENT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 15 of 38 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   pws_deactivation_date: date
--   viol_measure: float
--   state_mcl: float
--   severity_ind_cnt: integer
--   calculated_rtc_date: date
--   public_notification_tier: integer
--   calculated_pub_notif_tier: integer
--   rule_code: integer
--   rule_group_code: integer
--   rule_family_code: integer
--   viol_first_reported_date: date
--   viol_last_reported_date: date
--   enforcement_date: date
--   enf_first_reported_date: date
--   enf_last_reported_date: date
-- spine_entity 'facility' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_SDWA_SDWA_VIOLATIONS_ENFORCEMENT') }}
    qualify row_number() over (partition by "PWSID", "COMPL_PER_BEGIN_DATE", "COMPL_PER_END_DATE", "NON_COMPL_PER_BEGIN_DATE", "VIOLATION_ID", "FACILITY_ID", "SAMPLE_RESULT_ID", "CORRECTIVE_ACTION_ID", "ENFORCEMENT_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SUBMISSIONYEARQUARTER"') }} as submissionyearquarter,
        {{ null_junk('"PWSID"') }} as pwsid,
        {{ null_junk('"VIOLATION_ID"') }} as violation_id,
        {{ null_junk('"FACILITY_ID"') }} as facility_id,
        {{ null_junk('"COMPL_PER_BEGIN_DATE"') }} as compl_per_begin_date,
        {{ null_junk('"COMPL_PER_END_DATE"') }} as compl_per_end_date,
        {{ null_junk('"NON_COMPL_PER_BEGIN_DATE"') }} as non_compl_per_begin_date,
        {{ null_junk('"NON_COMPL_PER_END_DATE"') }} as non_compl_per_end_date,
        {{ stg_date('"PWS_DEACTIVATION_DATE"') }} as pws_deactivation_date,
        {{ null_junk('"VIOLATION_CODE"') }} as violation_code,
        {{ null_junk('"VIOLATION_CATEGORY_CODE"') }} as violation_category_code,
        {{ null_junk('"IS_HEALTH_BASED_IND"') }} as is_health_based_ind,
        {{ null_junk('"CONTAMINANT_CODE"') }} as contaminant_code,
        {{ stg_float('"VIOL_MEASURE"') }} as viol_measure,
        {{ null_junk('"UNIT_OF_MEASURE"') }} as unit_of_measure,
        {{ null_junk('"FEDERAL_MCL"') }} as federal_mcl,
        {{ stg_float('"STATE_MCL"') }} as state_mcl,
        {{ null_junk('"IS_MAJOR_VIOL_IND"') }} as is_major_viol_ind,
        {{ stg_int('"SEVERITY_IND_CNT"') }} as severity_ind_cnt,
        {{ stg_date('"CALCULATED_RTC_DATE"') }} as calculated_rtc_date,
        {{ null_junk('"VIOLATION_STATUS"') }} as violation_status,
        {{ stg_int('"PUBLIC_NOTIFICATION_TIER"') }} as public_notification_tier,
        {{ stg_int('"CALCULATED_PUB_NOTIF_TIER"') }} as calculated_pub_notif_tier,
        {{ null_junk('"VIOL_ORIGINATOR_CODE"') }} as viol_originator_code,
        {{ null_junk('"SAMPLE_RESULT_ID"') }} as sample_result_id,
        {{ null_junk('"CORRECTIVE_ACTION_ID"') }} as corrective_action_id,
        {{ stg_int('"RULE_CODE"') }} as rule_code,
        {{ stg_int('"RULE_GROUP_CODE"') }} as rule_group_code,
        {{ stg_int('"RULE_FAMILY_CODE"') }} as rule_family_code,
        {{ stg_date('"VIOL_FIRST_REPORTED_DATE"') }} as viol_first_reported_date,
        {{ stg_date('"VIOL_LAST_REPORTED_DATE"') }} as viol_last_reported_date,
        {{ null_junk('"ENFORCEMENT_ID"') }} as enforcement_id,
        {{ stg_date('"ENFORCEMENT_DATE"') }} as enforcement_date,
        {{ null_junk('"ENFORCEMENT_ACTION_TYPE_CODE"') }} as enforcement_action_type_code,
        {{ null_junk('"ENF_ACTION_CATEGORY"') }} as enf_action_category,
        {{ null_junk('"ENF_ORIGINATOR_CODE"') }} as enf_originator_code,
        {{ stg_date('"ENF_FIRST_REPORTED_DATE"') }} as enf_first_reported_date,
        {{ stg_date('"ENF_LAST_REPORTED_DATE"') }} as enf_last_reported_date,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
