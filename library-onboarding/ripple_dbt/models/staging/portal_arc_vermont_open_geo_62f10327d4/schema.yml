version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_VERMONT_OPEN_GEO_62F10327D4
        description: >
          Raw landing table for VT Schools (view). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_vermont_open_geo_62f10327d4__places
    description: "GRAIN: one row per place (LATLON is unique). SPINE_ENTITY: place."
    columns:
      - name: latitude
        data_tests:
          - unique
          - not_null
