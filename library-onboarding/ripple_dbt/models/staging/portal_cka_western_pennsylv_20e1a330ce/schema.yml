version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_20E1A330CE
        description: >
          Raw landing table for Allegheny County Supermarkets & Convenience Stores. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_20e1a330ce__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CLIENT_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: client_id
        data_tests:
          - unique
          - not_null
