{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 1 of 15 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   taxyr: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (ID) is NOT unique within a load as landed at generation time -- it would collapse 17 of 1,989 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_COLORADO_INFORMA_6367A44C92') }}
    qualify row_number() over (partition by "ID", "TAXYR", "BUSINESSNAME1", "EIN", "MISSIONDESC", "LOBBYINGACTIVITIESIND", "INDEPENDENTAUDITFINCLSTMTIND", "CONSOLIDATEDAUDITFINCLSTMTIND", "FORM990PROVIDEDTOGVRNBODYIND", "CONFLICTOFINTERESTPOLICYIND", "WHISTLEBLOWERPOLICYIND", "DOCUMENTRETENTIONPOLICYIND", "BUSINESSNAME2", "LICENSEDSTATESCD", "ALLSTATESCD" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ stg_int('"TAXYR"') }} as taxyr,
        {{ null_junk('"BUSINESSNAME1"') }} as businessname1,
        {{ null_junk('"EIN"') }} as ein,
        {{ null_junk('"MISSIONDESC"') }} as missiondesc,
        {{ null_junk('"LOBBYINGACTIVITIESIND"') }} as lobbyingactivitiesind,
        {{ null_junk('"INDEPENDENTAUDITFINCLSTMTIND"') }} as independentauditfinclstmtind,
        {{ null_junk('"CONSOLIDATEDAUDITFINCLSTMTIND"') }} as consolidatedauditfinclstmtind,
        {{ null_junk('"FORM990PROVIDEDTOGVRNBODYIND"') }} as form990providedtogvrnbodyind,
        {{ null_junk('"CONFLICTOFINTERESTPOLICYIND"') }} as conflictofinterestpolicyind,
        {{ null_junk('"WHISTLEBLOWERPOLICYIND"') }} as whistleblowerpolicyind,
        {{ null_junk('"DOCUMENTRETENTIONPOLICYIND"') }} as documentretentionpolicyind,
        {{ null_junk('"BUSINESSNAME2"') }} as businessname2,
        {{ null_junk('"LICENSEDSTATESCD"') }} as licensedstatescd,
        {{ null_junk('"ALLSTATESCD"') }} as allstatescd,
        INGESTED_AT as _loaded_at,
        'https://data.colorado.gov/d/vewn-5ajx' as _source_url

    from source

)

select * from renamed
