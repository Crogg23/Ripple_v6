version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: XC_OWID_FERTILITY
        description: >
          Raw landing table for OWID — Fertility rate (children per woman). Grain not yet determined.

models:
  - name: stg_xc_owid_fertility__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
