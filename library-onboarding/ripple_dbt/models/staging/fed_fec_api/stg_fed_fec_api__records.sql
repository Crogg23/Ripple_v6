{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TRANSACTION_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TRANSACTION_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_FEC_API') }}

),

renamed as (

    select
        "TRANSACTION_ID" as transaction_id,
        "COMMITTEE_ID" as committee_id,
        "CANDIDATE_ID" as candidate_id,
        "CONTRIBUTOR_NAME" as contributor_name,
        "CONTRIBUTOR_EMPLOYER" as contributor_employer,
        "CONTRIBUTOR_OCCUPATION" as contributor_occupation,
        "CONTRIBUTOR_ZIP" as contributor_zip,
        "CONTRIBUTOR_STATE" as contributor_state,
        "CONTRIBUTION_RECEIPT_AMOUNT" as contribution_receipt_amount,
        "CONTRIBUTION_RECEIPT_DATE" as contribution_receipt_date,
        "REPORT_TYPE" as report_type,
        "FILING_FORM" as filing_form,
        "ENTITY_TYPE" as entity_type,
        "PARTY" as party,
        "OFFICE" as office,
        "ELECTION_YEAR" as election_year,
        "STATE" as state,
        "DISTRICT" as district,
        "IS_INDIVIDUAL" as is_individual,
        "LOAD_DATE" as load_date,
        _INGESTED_AT as _loaded_at,
        'https://api.open.fec.gov/developers/' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by transaction_id order by _loaded_at desc) = 1
