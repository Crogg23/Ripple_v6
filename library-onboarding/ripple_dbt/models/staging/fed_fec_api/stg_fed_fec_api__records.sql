{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TRANSACTION_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TRANSACTION_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 4 of 20 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   contribution_receipt_amount: float
--   contribution_receipt_date: date
--   is_individual: boolean
--   load_date: timestamp
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_FEC_API') }}
    qualify row_number() over (partition by "TRANSACTION_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"TRANSACTION_ID"') }} as transaction_id,
        {{ null_junk('"COMMITTEE_ID"') }} as committee_id,
        {{ null_junk('"CANDIDATE_ID"') }} as candidate_id,
        {{ null_junk('"CONTRIBUTOR_NAME"') }} as contributor_name,
        {{ null_junk('"CONTRIBUTOR_EMPLOYER"') }} as contributor_employer,
        {{ null_junk('"CONTRIBUTOR_OCCUPATION"') }} as contributor_occupation,
        {{ null_junk('"CONTRIBUTOR_ZIP"') }} as contributor_zip,
        {{ null_junk('"CONTRIBUTOR_STATE"') }} as contributor_state,
        {{ stg_float('"CONTRIBUTION_RECEIPT_AMOUNT"') }} as contribution_receipt_amount,
        {{ stg_date('"CONTRIBUTION_RECEIPT_DATE"') }} as contribution_receipt_date,
        {{ null_junk('"REPORT_TYPE"') }} as report_type,
        {{ null_junk('"FILING_FORM"') }} as filing_form,
        {{ null_junk('"ENTITY_TYPE"') }} as entity_type,
        {{ null_junk('"PARTY"') }} as party,
        {{ null_junk('"OFFICE"') }} as office,
        {{ null_junk('"ELECTION_YEAR"') }} as election_year,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"DISTRICT"') }} as district,
        {{ stg_bool('"IS_INDIVIDUAL"') }} as is_individual,
        {{ stg_ts('"LOAD_DATE"') }} as load_date,
        _INGESTED_AT as _loaded_at,
        'https://api.open.fec.gov/developers/' as _source_url

    from source

)

select * from renamed
