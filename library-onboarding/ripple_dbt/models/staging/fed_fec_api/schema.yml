version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_FEC_API
        description: >
          Raw landing table for OpenFEC Campaign Finance API. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_fed_fec_api__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TRANSACTION_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: transaction_id
        data_tests:
          - unique
          - not_null
