{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CONTAINER_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: CONTAINER_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_508C030BD9') }}

),

renamed as (

    select
        "CONTAINER_ID" as container_id,
        "RECEPTACLE_MODEL_ID" as receptacle_model_id,
        "ASSIGNMENT_DATE" as assignment_date,
        "LAST_UPDATED_DATE" as last_updated_date,
        "GROUP_NAME" as group_name,
        "ADDRESS" as address,
        "CITY" as city,
        "STATE" as state,
        "ZIP" as zip,
        "NEIGHBORHOOD" as neighborhood,
        "DPW_DIVISION" as dpw_division,
        "COUNCIL_DISTRICT" as council_district,
        "WARD" as ward,
        "FIRE_ZONE" as fire_zone,
        "X" as x,
        "Y" as y,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/smart-trash-containers' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by container_id order by _loaded_at desc) = 1
