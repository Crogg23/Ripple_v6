{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_MEDSL_PRESIDENT_RETURNS') }}

),

renamed as (

    select
        YEAR as year,
        STATE as state,
        STATE_PO as state_po,
        STATE_FIPS as state_fips,
        STATE_CEN as state_cen,
        STATE_IC as state_ic,
        OFFICE as office,
        CANDIDATE as candidate,
        PARTY as party,
        WRITEIN as writein,
        CANDIDATEVOTES as candidatevotes,
        TOTALVOTES as totalvotes,
        VERSION as version,
        NOTES as notes,
        _INGESTED_AT as _loaded_at,
        'https://dataverse.harvard.edu/dataset.xhtml?persistentId=doi:10.7910/DVN/42MVDX' as _source_url

    from source

)

select * from renamed
