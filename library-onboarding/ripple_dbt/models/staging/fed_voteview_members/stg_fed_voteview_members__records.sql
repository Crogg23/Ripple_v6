{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_VOTEVIEW_MEMBERS') }}

),

renamed as (

    select
        CONGRESS as congress,
        CHAMBER as chamber,
        ICPSR as icpsr,
        STATE_ICPSR as state_icpsr,
        DISTRICT_CODE as district_code,
        STATE_ABBREV as state_abbrev,
        PARTY_CODE as party_code,
        OCCUPANCY as occupancy,
        LAST_MEANS as last_means,
        BIONAME as bioname,
        BIOGUIDE_ID as bioguide_id,
        BORN as born,
        DIED as died,
        NOMINATE_DIM1 as nominate_dim1,
        NOMINATE_DIM2 as nominate_dim2,
        NOMINATE_LOG_LIKELIHOOD as nominate_log_likelihood,
        NOMINATE_GEO_MEAN_PROBABILITY as nominate_geo_mean_probability,
        NOMINATE_NUMBER_OF_VOTES as nominate_number_of_votes,
        NOMINATE_NUMBER_OF_ERRORS as nominate_number_of_errors,
        CONDITIONAL as conditional,
        NOKKEN_POOLE_DIM1 as nokken_poole_dim1,
        NOKKEN_POOLE_DIM2 as nokken_poole_dim2,
        _INGESTED_AT as _loaded_at,
        'https://voteview.com/static/data/out/members/HSall_members.csv' as _source_url

    from source

)

select * from renamed
