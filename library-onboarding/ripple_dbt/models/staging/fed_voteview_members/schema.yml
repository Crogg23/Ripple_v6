version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_VOTEVIEW_MEMBERS
        description: >
          Raw landing table for Voteview -- Congressional Member Ideology (DW-NOMINATE). Grain not yet determined.

models:
  - name: stg_fed_voteview_members__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
