version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_COLORADO_INFORMA_DE6C8A6901
        description: >
          Raw landing table for CDLE - apprenticeship.gov - Apprentices by Apprentice Location. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_colorado_informa_de6c8a6901__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (FIPS+FISCAL_YEAR+START_DATE+EXIT_DATE+STATE_ID+STATE_ID_COPY+APPRENTICE_ID+PROGRAM_ID unique). SPINE_ENTITY: place."
    columns:
      - name: county_fips
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: fiscal_year
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: start_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: exit_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: state_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: state_id_copy
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: apprentice_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: program_id
        data_tests:
          - not_null:
              config:
                severity: warn
