{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (FIPS is unique)
-- SPINE_ENTITY: place  (natural_key: GEOID10)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 34 of 54 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   acres: float
--   aland10: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   awater10: integer
--   blkgrp2014: integer
--   blkgrp2018: integer
--   blkgrpce10: integer
--   cdbgty2014: integer
--   cdbgty2018: integer
--   cdbguogi_1: integer
--   cdbguogid2: integer
--   county2014: integer
--   county2018: integer
--   geoid2014: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   hood_no: integer
--   intptlat10: float
--   intptlon10: float
--   lmper2018: float
--   lowmod2014: integer
--   lowmod2018: integer
--   lowmodpc_1: float
--   lowmodpct2: integer
--   lowmodun_1: integer
--   lowmoduniv: integer
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   objectid_1: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   sectors: integer
--   sqmiles: float
--   st_area_sh: float
--   st_length: float
--   state2014: integer
--   state2018: integer
--   statefp10: integer
--   tract2014: integer
--   tract2018: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (GEOID10) is NOT unique within a load as landed at generation time -- it would collapse 1 of 361 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_645F59A90F') }}
    qualify row_number() over (partition by "CDBG2018", "SHAPE__AREA", "SHAPE__LENGTH", "ACRES", "ALAND10", "AWATER10", "BLKGRP2014", "BLKGRP2018", "BLKGRPCE10", "CDBGNAME20", "CDBGNAME_1", "CDBGTY2014", "CDBGTY2018", "CDBGUOGI_1", "CDBGUOGID2", "CENSUSBLOC", "COUNTY2014", "COUNTY2018", "COUNTYFP10", "COUNTYNA_1", "COUNTYNAME", "FUNCSTAT10", "GEOID10", "GEOID2014", "GEOID2018", "HOOD", "HOOD_NO", "HUDTRACTNU", "INTPTLAT10", "INTPTLON10", "LMPER2018", "LOWMOD2014", "LOWMOD2018", "LOWMODPC_1", "LOWMODPCT2", "LOWMODUN_1", "LOWMODUNIV", "MTFCC10", "NAMELSAD10", "OBJECTID", "OBJECTID_1", "SECTORS", "SQMILES", "ST_AREA_SH", "ST_LENGTH", "STATE2014", "STATE2018", "STATEFP10", "STUSAB2014", "STUSAB2018", "TRACT2014", "TRACT2018", "TRACTCE10", "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CDBG2018"') }} as cdbg2018,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ stg_float('"ACRES"') }} as acres,
        {{ stg_id_text('"ALAND10"') }} as aland10,
        {{ stg_int('"AWATER10"') }} as awater10,
        {{ stg_int('"BLKGRP2014"') }} as blkgrp2014,
        {{ stg_int('"BLKGRP2018"') }} as blkgrp2018,
        {{ stg_int('"BLKGRPCE10"') }} as blkgrpce10,
        {{ null_junk('"CDBGNAME20"') }} as cdbgname20,
        {{ null_junk('"CDBGNAME_1"') }} as cdbgname_1,
        {{ stg_int('"CDBGTY2014"') }} as cdbgty2014,
        {{ stg_int('"CDBGTY2018"') }} as cdbgty2018,
        {{ stg_int('"CDBGUOGI_1"') }} as cdbguogi_1,
        {{ stg_int('"CDBGUOGID2"') }} as cdbguogid2,
        {{ null_junk('"CENSUSBLOC"') }} as censusbloc,
        {{ stg_int('"COUNTY2014"') }} as county2014,
        {{ stg_int('"COUNTY2018"') }} as county2018,
        {{ null_junk('"COUNTYFP10"') }} as countyfp10,
        {{ null_junk('"COUNTYNA_1"') }} as countyna_1,
        {{ null_junk('"COUNTYNAME"') }} as countyname,
        {{ null_junk('"FUNCSTAT10"') }} as funcstat10,
        {{ null_junk('"GEOID10"') }} as geoid10,
        {{ stg_id_text('"GEOID2014"') }} as geoid2014,
        {{ null_junk('"GEOID2018"') }} as geoid2018,
        {{ null_junk('"HOOD"') }} as hood,
        {{ stg_int('"HOOD_NO"') }} as hood_no,
        {{ null_junk('"HUDTRACTNU"') }} as hudtractnu,
        {{ stg_float('"INTPTLAT10"') }} as intptlat10,
        {{ stg_float('"INTPTLON10"') }} as intptlon10,
        {{ stg_float('"LMPER2018"') }} as lmper2018,
        {{ stg_int('"LOWMOD2014"') }} as lowmod2014,
        {{ stg_int('"LOWMOD2018"') }} as lowmod2018,
        {{ stg_float('"LOWMODPC_1"') }} as lowmodpc_1,
        {{ stg_int('"LOWMODPCT2"') }} as lowmodpct2,
        {{ stg_int('"LOWMODUN_1"') }} as lowmodun_1,
        {{ stg_int('"LOWMODUNIV"') }} as lowmoduniv,
        {{ null_junk('"MTFCC10"') }} as mtfcc10,
        {{ null_junk('"NAMELSAD10"') }} as namelsad10,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_id_text('"OBJECTID_1"') }} as objectid_1,
        {{ stg_int('"SECTORS"') }} as sectors,
        {{ stg_float('"SQMILES"') }} as sqmiles,
        {{ stg_float('"ST_AREA_SH"') }} as st_area_sh,
        {{ stg_float('"ST_LENGTH"') }} as st_length,
        {{ stg_int('"STATE2014"') }} as state2014,
        {{ stg_int('"STATE2018"') }} as state2018,
        {{ stg_int('"STATEFP10"') }} as statefp10,
        {{ null_junk('"STUSAB2014"') }} as stusab2014,
        {{ null_junk('"STUSAB2018"') }} as stusab2018,
        {{ stg_int('"TRACT2014"') }} as tract2014,
        {{ stg_int('"TRACT2018"') }} as tract2018,
        {{ null_junk('"TRACTCE10"') }} as tractce10,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/pittsburgh-2014-cdbg-block-groups' as _source_url

    from source

)

select * from renamed
