version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: INTL_BD_DATAGOV
        description: >
          Raw landing table for Bangladesh Open Government Data Portal (data.gov.bd). Grain not yet determined.

models:
  - name: stg_intl_bd_datagov__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
