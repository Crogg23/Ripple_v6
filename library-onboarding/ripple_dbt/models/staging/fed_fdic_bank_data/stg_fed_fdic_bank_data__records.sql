{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_FDIC_BANK_DATA') }}

),

renamed as (

    select
        CERT as cert,
        NAME as name,
        CITY as city,
        STNAME as stname,
        STALP as stalp,
        ZIP as zip,
        FIPS as fips,
        BKCLASS as bkclass,
        ACTIVE as active,
        DATEUPDT as dateupdt,
        ESTYMD as estymd,
        ENDEFYMD as endefymd,
        ASSET as asset,
        DEP as dep,
        DEPDOM as depdom,
        NETINC as netinc,
        REPDTE as repdte,
        RSSDID as rssdid,
        CHRTAGNT as chrtagnt,
        INSTCAT as instcat,
        SPECGRP as specgrp,
        HCTMULT as hctmult,
        LATITUDE as latitude,
        LONGITUDE as longitude,
        _INGESTED_AT as _loaded_at,
        'https://banks.data.fdic.gov/docs/' as _source_url

    from source

)

select * from renamed
