{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 26 of 49 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   year: integer
--   active_year: boolean
--   type_of_violence: integer
--   conflict_dset_id: integer
--   conflict_new_id: integer
--   dyad_dset_id: integer
--   dyad_new_id: integer
--   side_a_dset_id: integer
--   side_a_new_id: integer
--   side_b_dset_id: integer
--   side_b_new_id: integer
--   number_of_sources: integer
--   where_prec: integer
--   priogrid_gid: integer
--   event_clarity: integer
--   date_prec: integer
--   date_start: timestamp
--   date_end: timestamp
--   deaths_a: integer
--   deaths_b: integer
--   deaths_civilians: integer
--   deaths_unknown: integer
--   best: integer
--   high: integer
--   low: integer
--   gwnob: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'INTL_UCDP_GED') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"RELID"') }} as relid,
        {{ stg_int('"YEAR"') }} as year,
        {{ stg_bool('"ACTIVE_YEAR"') }} as active_year,
        {{ null_junk('"CODE_STATUS"') }} as code_status,
        {{ stg_int('"TYPE_OF_VIOLENCE"') }} as type_of_violence,
        {{ stg_int('"CONFLICT_DSET_ID"') }} as conflict_dset_id,
        {{ stg_int('"CONFLICT_NEW_ID"') }} as conflict_new_id,
        {{ null_junk('"CONFLICT_NAME"') }} as conflict_name,
        {{ stg_int('"DYAD_DSET_ID"') }} as dyad_dset_id,
        {{ stg_int('"DYAD_NEW_ID"') }} as dyad_new_id,
        {{ null_junk('"DYAD_NAME"') }} as dyad_name,
        {{ stg_int('"SIDE_A_DSET_ID"') }} as side_a_dset_id,
        {{ stg_int('"SIDE_A_NEW_ID"') }} as side_a_new_id,
        {{ null_junk('"SIDE_A"') }} as side_a,
        {{ stg_int('"SIDE_B_DSET_ID"') }} as side_b_dset_id,
        {{ stg_int('"SIDE_B_NEW_ID"') }} as side_b_new_id,
        {{ null_junk('"SIDE_B"') }} as side_b,
        {{ stg_int('"NUMBER_OF_SOURCES"') }} as number_of_sources,
        {{ null_junk('"SOURCE_ARTICLE"') }} as source_article,
        {{ null_junk('"SOURCE_OFFICE"') }} as source_office,
        {{ null_junk('"SOURCE_DATE"') }} as source_date,
        {{ null_junk('"SOURCE_HEADLINE"') }} as source_headline,
        {{ null_junk('"SOURCE_ORIGINAL"') }} as source_original,
        {{ stg_int('"WHERE_PREC"') }} as where_prec,
        {{ null_junk('"WHERE_COORDINATES"') }} as where_coordinates,
        {{ null_junk('"WHERE_DESCRIPTION"') }} as where_description,
        {{ null_junk('"ADM_1"') }} as adm_1,
        {{ null_junk('"ADM_2"') }} as adm_2,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"GEOM_WKT"') }} as geom_wkt,
        {{ stg_int('"PRIOGRID_GID"') }} as priogrid_gid,
        {{ null_junk('"COUNTRY"') }} as country,
        {{ null_junk('"COUNTRY_ID"') }} as country_id,
        {{ null_junk('"REGION"') }} as region,
        {{ stg_int('"EVENT_CLARITY"') }} as event_clarity,
        {{ stg_int('"DATE_PREC"') }} as date_prec,
        {{ stg_ts('"DATE_START"') }} as date_start,
        {{ stg_ts('"DATE_END"') }} as date_end,
        {{ stg_int('"DEATHS_A"') }} as deaths_a,
        {{ stg_int('"DEATHS_B"') }} as deaths_b,
        {{ stg_int('"DEATHS_CIVILIANS"') }} as deaths_civilians,
        {{ stg_int('"DEATHS_UNKNOWN"') }} as deaths_unknown,
        {{ stg_int('"BEST"') }} as best,
        {{ stg_int('"HIGH"') }} as high,
        {{ stg_int('"LOW"') }} as low,
        {{ null_junk('"GWNOA"') }} as gwnoa,
        {{ stg_int('"GWNOB"') }} as gwnob,
        INGESTED_AT as _loaded_at,
        'https://ucdp.uu.se/downloads/' as _source_url

    from source

)

select * from renamed
