version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_CALIFORNIA_OPEN_0AD648012F
        description: >
          Raw landing table for Water Quality Data. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_california_open_0ad648012f__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (STATION_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: station_id
        data_tests:
          - unique
          - not_null
