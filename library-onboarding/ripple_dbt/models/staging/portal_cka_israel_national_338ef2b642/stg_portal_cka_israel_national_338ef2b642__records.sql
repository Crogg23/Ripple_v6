{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FUND_ID+MANAGING_CORPORATION_LEGAL_ID+REPORT_PERIOD unique)
-- SPINE_ENTITY: (not determined)  (natural_key: FUND_ID, MANAGING_CORPORATION_LEGAL_ID, REPORT_PERIOD)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 22 of 39 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   deposits: float
--   withdrawls: float
--   internal_transfers: float
--   net_monthly_deposits: float
--   total_assets: float
--   avg_annual_management_fee: float
--   avg_deposit_fee: float
--   monthly_yield: float
--   year_to_date_yield: float
--   yield_trailing_3_yrs: float
--   yield_trailing_5_yrs: float
--   avg_annual_yield_trailing_3yrs: float
--   avg_annual_yield_trailing_5yrs: float
--   standard_deviation: float
--   alpha: float
--   sharpe_ratio: float
--   liquid_assets_percent: float
--   stock_market_exposure: float
--   foreign_exposure: float
--   foreign_currency_exposure: float
--   reporting_year: integer
--   c_current_date: timestamp
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (FUND_ID, MANAGING_CORPORATION_LEGAL_ID, REPORT_PERIOD) is NOT unique within a load as landed at generation time -- it would collapse 45 of 185,420 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_338EF2B642') }}
    qualify row_number() over (partition by "FUND_ID", "FUND_NAME", "FUND_CLASSIFICATION", "CONTROLLING_CORPORATION", "MANAGING_CORPORATION", "REPORT_PERIOD", "INCEPTION_DATE", "TARGET_POPULATION", "SPECIALIZATION", "SUB_SPECIALIZATION", "DEPOSITS", "WITHDRAWLS", "INTERNAL_TRANSFERS", "NET_MONTHLY_DEPOSITS", "TOTAL_ASSETS", "AVG_ANNUAL_MANAGEMENT_FEE", "AVG_DEPOSIT_FEE", "MONTHLY_YIELD", "YEAR_TO_DATE_YIELD", "YIELD_TRAILING_3_YRS", "YIELD_TRAILING_5_YRS", "AVG_ANNUAL_YIELD_TRAILING_3YRS", "AVG_ANNUAL_YIELD_TRAILING_5YRS", "STANDARD_DEVIATION", "ALPHA", "SHARPE_RATIO", "LIQUID_ASSETS_PERCENT", "STOCK_MARKET_EXPOSURE", "FOREIGN_EXPOSURE", "FOREIGN_CURRENCY_EXPOSURE", "MANAGING_CORPORATION_LEGAL_ID", "REPORTING_YEAR", "C_CURRENT_DATE", "MDS_NAME", "MDS_INFO_NAME", "MDS_DES", "MDS_TYPE", "COL", "COL_2" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"FUND_ID"') }} as fund_id,
        {{ null_junk('"FUND_NAME"') }} as fund_name,
        {{ null_junk('"FUND_CLASSIFICATION"') }} as fund_classification,
        {{ null_junk('"CONTROLLING_CORPORATION"') }} as controlling_corporation,
        {{ null_junk('"MANAGING_CORPORATION"') }} as managing_corporation,
        {{ null_junk('"REPORT_PERIOD"') }} as report_period,
        {{ null_junk('"INCEPTION_DATE"') }} as inception_date,
        {{ null_junk('"TARGET_POPULATION"') }} as target_population,
        {{ null_junk('"SPECIALIZATION"') }} as specialization,
        {{ null_junk('"SUB_SPECIALIZATION"') }} as sub_specialization,
        {{ stg_float('"DEPOSITS"') }} as deposits,
        {{ stg_float('"WITHDRAWLS"') }} as withdrawls,
        {{ stg_float('"INTERNAL_TRANSFERS"') }} as internal_transfers,
        {{ stg_float('"NET_MONTHLY_DEPOSITS"') }} as net_monthly_deposits,
        {{ stg_float('"TOTAL_ASSETS"') }} as total_assets,
        {{ stg_float('"AVG_ANNUAL_MANAGEMENT_FEE"') }} as avg_annual_management_fee,
        {{ stg_float('"AVG_DEPOSIT_FEE"') }} as avg_deposit_fee,
        {{ stg_float('"MONTHLY_YIELD"') }} as monthly_yield,
        {{ stg_float('"YEAR_TO_DATE_YIELD"') }} as year_to_date_yield,
        {{ stg_float('"YIELD_TRAILING_3_YRS"') }} as yield_trailing_3_yrs,
        {{ stg_float('"YIELD_TRAILING_5_YRS"') }} as yield_trailing_5_yrs,
        {{ stg_float('"AVG_ANNUAL_YIELD_TRAILING_3YRS"') }} as avg_annual_yield_trailing_3yrs,
        {{ stg_float('"AVG_ANNUAL_YIELD_TRAILING_5YRS"') }} as avg_annual_yield_trailing_5yrs,
        {{ stg_float('"STANDARD_DEVIATION"') }} as standard_deviation,
        {{ stg_float('"ALPHA"') }} as alpha,
        {{ stg_float('"SHARPE_RATIO"') }} as sharpe_ratio,
        {{ stg_float('"LIQUID_ASSETS_PERCENT"') }} as liquid_assets_percent,
        {{ stg_float('"STOCK_MARKET_EXPOSURE"') }} as stock_market_exposure,
        {{ stg_float('"FOREIGN_EXPOSURE"') }} as foreign_exposure,
        {{ stg_float('"FOREIGN_CURRENCY_EXPOSURE"') }} as foreign_currency_exposure,
        {{ null_junk('"MANAGING_CORPORATION_LEGAL_ID"') }} as managing_corporation_legal_id,
        {{ stg_int('"REPORTING_YEAR"') }} as reporting_year,
        {{ stg_ts('"C_CURRENT_DATE"') }} as c_current_date,
        {{ null_junk('"MDS_NAME"') }} as mds_name,
        {{ null_junk('"MDS_INFO_NAME"') }} as mds_info_name,
        {{ null_junk('"MDS_DES"') }} as mds_des,
        {{ null_junk('"MDS_TYPE"') }} as mds_type,
        {{ null_junk('"COL"') }} as col,
        {{ null_junk('"COL_2"') }} as col_2,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/gemelnet' as _source_url

    from source

)

select * from renamed
