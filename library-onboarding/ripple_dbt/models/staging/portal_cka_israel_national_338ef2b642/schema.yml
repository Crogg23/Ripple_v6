version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ISRAEL_NATIONAL_338EF2B642
        description: >
          Raw landing table for גמל-נט. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_israel_national_338ef2b642__records
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FUND_ID+MANAGING_CORPORATION_LEGAL_ID+REPORT_PERIOD unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: fund_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: managing_corporation_legal_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: report_period
        data_tests:
          - not_null:
              config:
                severity: warn
