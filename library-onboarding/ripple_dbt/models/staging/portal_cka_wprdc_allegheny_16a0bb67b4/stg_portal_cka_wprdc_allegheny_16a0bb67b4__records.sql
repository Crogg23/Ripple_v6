{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CLIENT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: CLIENT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 2 of 12 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   start_date: date
--   accuracy: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_16A0BB67B4') }}
    qualify row_number() over (partition by "CLIENT_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CLIENT_ID"') }} as client_id,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"LEGAL_NAME"') }} as legal_name,
        {{ stg_date('"START_DATE"') }} as start_date,
        {{ null_junk('"STREET"') }} as street,
        {{ null_junk('"STREET_NAME"') }} as street_name,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"LAT"') }} as lat,
        {{ null_junk('"LON"') }} as lon,
        {{ stg_int('"ACCURACY"') }} as accuracy,
        {{ null_junk('"CATEGORY"') }} as category,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-supermarkets-convenience-stores' as _source_url

    from source

)

select * from renamed
