version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_HARRIS_COUNTY_OP_0F9C7FCB29
        description: >
          Raw landing table for Infogroup Business 2019 Homeless Shelters County Int. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_harris_county_op_0f9c7fcb29__places
    description: "GRAIN: one row per place (LATLON is unique). SPINE_ENTITY: place."
    columns:
      - name: longitude
        data_tests:
          - unique
          - not_null
