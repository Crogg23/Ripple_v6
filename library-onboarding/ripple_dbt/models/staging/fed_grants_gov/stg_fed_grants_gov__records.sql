{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (OPPORTUNITY_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: OPPORTUNITY_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 2 of 23 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   posted_date: date
--   close_date: date
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_GRANTS_GOV') }}
    qualify row_number() over (partition by "OPPORTUNITY_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"OPPORTUNITY_ID"') }} as opportunity_id,
        {{ null_junk('"OPPORTUNITY_NUMBER"') }} as opportunity_number,
        {{ null_junk('"OPPORTUNITY_TITLE"') }} as opportunity_title,
        {{ null_junk('"AGENCY_CODE"') }} as agency_code,
        {{ null_junk('"AGENCY_NAME"') }} as agency_name,
        {{ null_junk('"OPPORTUNITY_STATUS"') }} as opportunity_status,
        {{ null_junk('"OPPORTUNITY_CATEGORY"') }} as opportunity_category,
        {{ null_junk('"FUNDING_INSTRUMENT_TYPE"') }} as funding_instrument_type,
        {{ null_junk('"CATEGORY_OF_FUNDING_ACTIVITY"') }} as category_of_funding_activity,
        {{ null_junk('"CFDA_NUMBERS"') }} as cfda_numbers,
        {{ null_junk('"ELIGIBLE_APPLICANTS"') }} as eligible_applicants,
        {{ null_junk('"AWARD_CEILING"') }} as award_ceiling,
        {{ null_junk('"AWARD_FLOOR"') }} as award_floor,
        {{ null_junk('"ESTIMATED_TOTAL_PROGRAM_FUNDING"') }} as estimated_total_program_funding,
        {{ null_junk('"EXPECTED_NUMBER_OF_AWARDS"') }} as expected_number_of_awards,
        {{ stg_date('"POSTED_DATE"') }} as posted_date,
        {{ stg_date('"CLOSE_DATE"') }} as close_date,
        {{ null_junk('"LAST_UPDATED_DATE"') }} as last_updated_date,
        {{ null_junk('"ARCHIVE_DATE"') }} as archive_date,
        {{ null_junk('"SYNOPSIS_DESC"') }} as synopsis_desc,
        {{ null_junk('"GRANTOR_CONTACT_EMAIL"') }} as grantor_contact_email,
        {{ null_junk('"GRANTOR_CONTACT_NAME"') }} as grantor_contact_name,
        {{ null_junk('"VERSION"') }} as version,
        _INGESTED_AT as _loaded_at,
        'https://www.grants.gov/api/api-guide' as _source_url

    from source

)

select * from renamed
