{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (RECORD_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: RECORD_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_OPEN_DATA_SA_E846782F2C') }}

),

renamed as (

    select
        "RECORD_ID" as record_id,
        "APPLICATION_TYPE" as application_type,
        "APPLICATION_NAME" as application_name,
        "PROJECT_NAME" as project_name,
        "RECEIVED_DATE" as received_date,
        "TYPE_OF_REVIEW" as type_of_review,
        "PROJECT_DESCRIPTION" as project_description,
        "OTHER_DESCRIPTION" as other_description,
        "STREET" as street,
        "PRE_DIRECTION" as pre_direction,
        "STREET_NAME" as street_name,
        "STREET_TYPE" as street_type,
        "POST_DIRECTION" as post_direction,
        "UNIT_SUITE" as unit_suite,
        "LEVEL" as level,
        "BUILDING" as building,
        "CITY" as city,
        "STATE" as state,
        "ZIP_CODE" as zip_code,
        "X_COORDINATE" as x_coordinate,
        "Y_COORDINATE" as y_coordinate,
        "LAT" as lat,
        "LONG" as long,
        "PARCEL_S" as parcel_s,
        "COUNCIL_DISTRICT_S" as council_district_s,
        "NEIGHBORHOOD_ASSOCIATION_S" as neighborhood_association_s,
        "SCHOOL_DISTRICT_S" as school_district_s,
        "MASTER_DEVELOPMENT_PLAN_S" as master_development_plan_s,
        "SAN_ANTONIO_CITY_LIMITS" as san_antonio_city_limits,
        "OWNER_NAME" as owner_name,
        "NUMBER_OF_STORIES" as number_of_stories,
        "LAND_SQ_FT" as land_sq_ft,
        "PROJECT_SQUARE_FOOTAGE" as project_square_footage,
        "SCHEDULED_MEETING_DATE" as scheduled_meeting_date,
        "ESTIMATED_START_DATE" as estimated_start_date,
        "ESTIMATED_CONSTRUCTION_COST" as estimated_construction_cost,
        INGESTED_AT as _loaded_at,
        'https://data.sanantonio.gov/dataset/preliminary-plan-reviews' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by record_id order by _loaded_at desc) = 1
