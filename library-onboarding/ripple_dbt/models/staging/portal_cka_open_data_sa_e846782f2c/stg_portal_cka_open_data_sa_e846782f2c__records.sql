{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (RECORD_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: RECORD_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 9 of 36 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   received_date: date
--   x_coordinate: float
--   y_coordinate: float
--   long: float
--   number_of_stories: integer
--   project_square_footage: float
--   scheduled_meeting_date: date
--   estimated_start_date: date
--   estimated_construction_cost: float
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_OPEN_DATA_SA_E846782F2C') }}
    qualify row_number() over (partition by "RECORD_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"RECORD_ID"') }} as record_id,
        {{ null_junk('"APPLICATION_TYPE"') }} as application_type,
        {{ null_junk('"APPLICATION_NAME"') }} as application_name,
        {{ null_junk('"PROJECT_NAME"') }} as project_name,
        {{ stg_date('"RECEIVED_DATE"') }} as received_date,
        {{ null_junk('"TYPE_OF_REVIEW"') }} as type_of_review,
        {{ null_junk('"PROJECT_DESCRIPTION"') }} as project_description,
        {{ null_junk('"OTHER_DESCRIPTION"') }} as other_description,
        {{ null_junk('"STREET"') }} as street,
        {{ null_junk('"PRE_DIRECTION"') }} as pre_direction,
        {{ null_junk('"STREET_NAME"') }} as street_name,
        {{ null_junk('"STREET_TYPE"') }} as street_type,
        {{ null_junk('"POST_DIRECTION"') }} as post_direction,
        {{ null_junk('"UNIT_SUITE"') }} as unit_suite,
        {{ null_junk('"LEVEL"') }} as level,
        {{ null_junk('"BUILDING"') }} as building,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ stg_float('"X_COORDINATE"') }} as x_coordinate,
        {{ stg_float('"Y_COORDINATE"') }} as y_coordinate,
        {{ null_junk('"LAT"') }} as lat,
        {{ stg_float('"LONG"') }} as long,
        {{ null_junk('"PARCEL_S"') }} as parcel_s,
        {{ null_junk('"COUNCIL_DISTRICT_S"') }} as council_district_s,
        {{ null_junk('"NEIGHBORHOOD_ASSOCIATION_S"') }} as neighborhood_association_s,
        {{ null_junk('"SCHOOL_DISTRICT_S"') }} as school_district_s,
        {{ null_junk('"MASTER_DEVELOPMENT_PLAN_S"') }} as master_development_plan_s,
        {{ null_junk('"SAN_ANTONIO_CITY_LIMITS"') }} as san_antonio_city_limits,
        {{ null_junk('"OWNER_NAME"') }} as owner_name,
        {{ stg_int('"NUMBER_OF_STORIES"') }} as number_of_stories,
        {{ null_junk('"LAND_SQ_FT"') }} as land_sq_ft,
        {{ stg_float('"PROJECT_SQUARE_FOOTAGE"') }} as project_square_footage,
        {{ stg_date('"SCHEDULED_MEETING_DATE"') }} as scheduled_meeting_date,
        {{ stg_date('"ESTIMATED_START_DATE"') }} as estimated_start_date,
        {{ stg_float('"ESTIMATED_CONSTRUCTION_COST"') }} as estimated_construction_cost,
        INGESTED_AT as _loaded_at,
        'https://data.sanantonio.gov/dataset/preliminary-plan-reviews' as _source_url

    from source

)

select * from renamed
