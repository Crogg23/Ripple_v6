{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CLOSURE_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: CLOSURE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_A3B7349811') }}

),

renamed as (

    select
        "CLOSURE_ID" as closure_id,
        "PERMIT_ID" as permit_id,
        "PERMIT_TYPE" as permit_type,
        "WORK_DESCRIPTION" as work_description,
        "WORK_TYPE" as work_type,
        "APPLICANT_NAME" as applicant_name,
        "CONTRACTOR_NAME" as contractor_name,
        "SPECIAL_INSTRUCTIONS" as special_instructions,
        "WEEKDAY_HOURS" as weekday_hours,
        "WEEKEND_HOURS" as weekend_hours,
        "FROM_DATE" as from_date,
        "TO_DATE" as to_date,
        "ACTIVE" as active,
        "PRIMARY_STREET" as primary_street,
        "FROM_STREET" as from_street,
        "TO_STREET" as to_street,
        "FULL_CLOSURE" as full_closure,
        "TRAVEL_LANE" as travel_lane,
        "PARKING_LANE" as parking_lane,
        "METERED_PARKING" as metered_parking,
        "SIDEWALK" as sidewalk,
        "SEGMENT_NUM" as segment_num,
        "TOTAL_SEGMENTS" as total_segments,
        "ROADWAY_ID" as roadway_id,
        "GEOMETRY" as geometry,
        "WKT" as wkt,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/street-closures' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by closure_id order by _loaded_at desc) = 1
