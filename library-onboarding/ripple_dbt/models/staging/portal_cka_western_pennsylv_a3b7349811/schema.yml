version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_A3B7349811
        description: >
          Raw landing table for DOMI Street Closures For GIS Mapping. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_a3b7349811__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CLOSURE_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: closure_id
        data_tests:
          - unique
          - not_null
