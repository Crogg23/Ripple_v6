{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LONGITUDE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_305157E72F') }}

),

renamed as (

    select
        "OBJECTID_1" as objectid_1,
        "CREATED_DA" as created_da,
        "CREATED_DATE" as created_date,
        "CREATED_US" as created_us,
        "CREATED_USER" as created_user,
        "DATE1" as date1,
        "DATE2" as date2,
        "LAST_EDI_1" as last_edi_1,
        "LAST_EDITE" as last_edite,
        "LAST_EDITED_DATE" as last_edited_date,
        "LAST_EDITED_USER" as last_edited_user,
        "LATITUDE" as latitude,
        "LOCATION" as location,
        "LONGITUDE" as longitude,
        "LOT" as lot,
        "MAXHOURS" as maxhours,
        "NODE" as node,
        "NODE_1" as node_1,
        "OBJECTID" as objectid,
        "RATE" as rate,
        "RATE2" as rate2,
        "RATETIMEA" as ratetimea,
        "RATETIMEB" as ratetimeb,
        "RESTDATES" as restdates,
        "RESTRICTIO" as restrictio,
        "RESTTIME1" as resttime1,
        "RESTTIME2" as resttime2,
        "SERIAL_NUM" as serial_num,
        "SIM_NUMBER" as sim_number,
        "SPECIALEV" as specialev,
        "TERMINAL_I" as terminal_i,
        "TERMINAL_S" as terminal_s,
        "TIME1" as time1,
        "TIME2" as time2,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/parking-meters-pittsburgh-parking-authority' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by longitude order by _loaded_at desc) = 1
