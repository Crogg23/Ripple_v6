{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_POSITIONS') }}

),

renamed as (

    select
        "ID" as id,
        "DATE_CREATED" as date_created,
        "DATE_MODIFIED" as date_modified,
        "POSITION_TYPE" as position_type,
        "JOB_TITLE" as job_title,
        "SECTOR" as sector,
        "ORGANIZATION_NAME" as organization_name,
        "LOCATION_CITY" as location_city,
        "LOCATION_STATE" as location_state,
        "DATE_NOMINATED" as date_nominated,
        "DATE_ELECTED" as date_elected,
        "DATE_RECESS_APPOINTMENT" as date_recess_appointment,
        "DATE_REFERRED_TO_JUDICIAL_COMMITTEE" as date_referred_to_judicial_committee,
        "DATE_JUDICIAL_COMMITTEE_ACTION" as date_judicial_committee_action,
        "JUDICIAL_COMMITTEE_ACTION" as judicial_committee_action,
        "DATE_HEARING" as date_hearing,
        "DATE_CONFIRMATION" as date_confirmation,
        "DATE_START" as date_start,
        "DATE_GRANULARITY_START" as date_granularity_start,
        "DATE_TERMINATION" as date_termination,
        "TERMINATION_REASON" as termination_reason,
        "DATE_GRANULARITY_TERMINATION" as date_granularity_termination,
        "DATE_RETIREMENT" as date_retirement,
        "NOMINATION_PROCESS" as nomination_process,
        "VOTE_TYPE" as vote_type,
        "VOICE_VOTE" as voice_vote,
        "VOTES_YES" as votes_yes,
        "VOTES_NO" as votes_no,
        "VOTES_YES_PERCENT" as votes_yes_percent,
        "VOTES_NO_PERCENT" as votes_no_percent,
        "HOW_SELECTED" as how_selected,
        "HAS_INFERRED_VALUES" as has_inferred_values,
        "APPOINTER_ID" as appointer_id,
        "COURT_ID" as court_id,
        "PERSON_ID" as person_id,
        "PREDECESSOR_ID" as predecessor_id,
        "SCHOOL_ID" as school_id,
        "SUPERVISOR_ID" as supervisor_id,
        INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by id order by _loaded_at desc) = 1
