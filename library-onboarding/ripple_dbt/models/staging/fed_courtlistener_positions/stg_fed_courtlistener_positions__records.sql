{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 19 of 38 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   date_created: timestamp
--   date_modified: timestamp
--   sector: integer
--   date_nominated: date
--   date_elected: date
--   date_recess_appointment: date
--   date_referred_to_judicial_committee: date
--   date_judicial_committee_action: date
--   date_hearing: date
--   date_confirmation: date
--   date_start: date
--   date_termination: date
--   date_retirement: date
--   votes_yes: integer
--   votes_no: integer
--   appointer_id: integer
--   predecessor_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   school_id: integer
--   supervisor_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_POSITIONS') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ stg_ts('"DATE_CREATED"') }} as date_created,
        {{ stg_ts('"DATE_MODIFIED"') }} as date_modified,
        {{ null_junk('"POSITION_TYPE"') }} as position_type,
        {{ null_junk('"JOB_TITLE"') }} as job_title,
        {{ stg_int('"SECTOR"') }} as sector,
        {{ null_junk('"ORGANIZATION_NAME"') }} as organization_name,
        {{ null_junk('"LOCATION_CITY"') }} as location_city,
        {{ null_junk('"LOCATION_STATE"') }} as location_state,
        {{ stg_date('"DATE_NOMINATED"') }} as date_nominated,
        {{ stg_date('"DATE_ELECTED"') }} as date_elected,
        {{ stg_date('"DATE_RECESS_APPOINTMENT"') }} as date_recess_appointment,
        {{ stg_date('"DATE_REFERRED_TO_JUDICIAL_COMMITTEE"') }} as date_referred_to_judicial_committee,
        {{ stg_date('"DATE_JUDICIAL_COMMITTEE_ACTION"') }} as date_judicial_committee_action,
        {{ null_junk('"JUDICIAL_COMMITTEE_ACTION"') }} as judicial_committee_action,
        {{ stg_date('"DATE_HEARING"') }} as date_hearing,
        {{ stg_date('"DATE_CONFIRMATION"') }} as date_confirmation,
        {{ stg_date('"DATE_START"') }} as date_start,
        {{ null_junk('"DATE_GRANULARITY_START"') }} as date_granularity_start,
        {{ stg_date('"DATE_TERMINATION"') }} as date_termination,
        {{ null_junk('"TERMINATION_REASON"') }} as termination_reason,
        {{ null_junk('"DATE_GRANULARITY_TERMINATION"') }} as date_granularity_termination,
        {{ stg_date('"DATE_RETIREMENT"') }} as date_retirement,
        {{ null_junk('"NOMINATION_PROCESS"') }} as nomination_process,
        {{ null_junk('"VOTE_TYPE"') }} as vote_type,
        {{ null_junk('"VOICE_VOTE"') }} as voice_vote,
        {{ stg_int('"VOTES_YES"') }} as votes_yes,
        {{ stg_int('"VOTES_NO"') }} as votes_no,
        {{ null_junk('"VOTES_YES_PERCENT"') }} as votes_yes_percent,
        {{ null_junk('"VOTES_NO_PERCENT"') }} as votes_no_percent,
        {{ null_junk('"HOW_SELECTED"') }} as how_selected,
        {{ null_junk('"HAS_INFERRED_VALUES"') }} as has_inferred_values,
        {{ stg_int('"APPOINTER_ID"') }} as appointer_id,
        {{ null_junk('"COURT_ID"') }} as court_id,
        {{ null_junk('"PERSON_ID"') }} as person_id,
        {{ stg_id_text('"PREDECESSOR_ID"') }} as predecessor_id,
        {{ stg_int('"SCHOOL_ID"') }} as school_id,
        {{ stg_id_text('"SUPERVISOR_ID"') }} as supervisor_id,
        INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
