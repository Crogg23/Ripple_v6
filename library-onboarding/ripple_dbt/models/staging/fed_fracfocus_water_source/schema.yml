version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_FRACFOCUS_WATER_SOURCE
        description: >
          Raw landing table, ingestion sweep 2026-08-05. All columns arrive as TEXT.

models:
  - name: stg_fed_fracfocus_water_source__all
    description: >
      Minimal passthrough staging view for FED_FRACFOCUS_WATER_SOURCE. Grain not yet
      determined -- snake_case rename only, no dedup.
