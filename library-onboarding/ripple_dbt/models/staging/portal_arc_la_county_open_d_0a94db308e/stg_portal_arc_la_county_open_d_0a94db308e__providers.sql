{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per provider (CCN+NPI+PARTICIPATION_DATE+APPROVAL_DATE+START_DATE unique)
-- SPINE_ENTITY: provider  (natural_key: CCN, NPI, PARTICIPATION_DATE, APPROVAL_DATE, START_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 15 of 61 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   deemed: integer
--   ao_cd: integer
--   dmg_efctv_dt: timestamp
--   capacity: integer
--   zip9: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   county_code: integer
--   district_number: integer
--   license_number: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   initial_license_date: timestamp
--   license_effective_date: timestamp
--   license_expiration_date: timestamp
--   hcai_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   cclho_code: integer
--   data_date: timestamp
-- spine_entity 'provider' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (CCN, NPI, PARTICIPATION_DATE, APPROVAL_DATE, START_DATE) is NOT unique within a load as landed at generation time -- it would collapse 1 of 93 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_LA_COUNTY_OPEN_D_0A94DB308E') }}
    qualify row_number() over (partition by "OBJECTID", "CITY", "ZIP", "LICENSED_CERTIFIED", "FLAG", "T18_19", "FACID", "FAC_STATUS_TYPE_CODE", "ASPEN_FACID", "CCN", "TERMINAT_SW", "PARTICIPATION_DATE", "APPROVAL_DATE", "NPI", "CAN_BE_DEEMED_FAC_TYPE", "CAN_BE_CERTIFIED_FAC_TYPE", "DEEMED", "AO_CD", "DMG_EFCTV_DT", "AO_TRMNTN_DT", "AO_NAME", "FACNAME", "FAC_TYPE_CODE", "FAC_FDR", "LTC", "CAPACITY", "ADDRESS", "ZIP9", "FACADMIN", "CONTACT_EMAIL", "CONTACT_FAX", "CONTACT_PHONE_NUMBER", "COUNTY_CODE", "COUNTY_NAME", "DISTRICT_NUMBER", "DISTRICT_NAME", "ISFACMAIN", "PARENT_FACID", "FAC_FAC_RELATIONSHIP_TYPE_CODE", "START_DATE", "LICENSE_NUMBER", "BUSINESS_NAME", "LICENSE_STATUS_DESCRIPTION", "INITIAL_LICENSE_DATE", "LICENSE_EFFECTIVE_DATE", "LICENSE_EXPIRATION_DATE", "ENTITY_TYPE_DESCRIPTION", "LATITUDE", "LONGITUDE", "LOCATION", "HCAI_ID", "CCLHO_CODE", "CCLHO_NAME", "FIPS_COUNTY_CODE", "BIRTHING_FACILITY_FLAG", "TRAUMA_PED_CTR", "TRAUMA_CTR", "TYPE_OF_CARE", "CRITICAL_ACCESS_HOSPITAL", "DATA_DATE", "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"LICENSED_CERTIFIED"') }} as licensed_certified,
        {{ null_junk('"FLAG"') }} as flag,
        {{ null_junk('"T18_19"') }} as t18_19,
        {{ null_junk('"FACID"') }} as facid,
        {{ null_junk('"FAC_STATUS_TYPE_CODE"') }} as fac_status_type_code,
        {{ null_junk('"ASPEN_FACID"') }} as aspen_facid,
        {{ null_junk('"CCN"') }} as ccn,
        {{ null_junk('"TERMINAT_SW"') }} as terminat_sw,
        {{ null_junk('"PARTICIPATION_DATE"') }} as participation_date,
        {{ null_junk('"APPROVAL_DATE"') }} as approval_date,
        {{ null_junk('"NPI"') }} as npi,
        {{ null_junk('"CAN_BE_DEEMED_FAC_TYPE"') }} as can_be_deemed_fac_type,
        {{ null_junk('"CAN_BE_CERTIFIED_FAC_TYPE"') }} as can_be_certified_fac_type,
        {{ stg_int('"DEEMED"') }} as deemed,
        {{ stg_int('"AO_CD"') }} as ao_cd,
        {{ stg_ts('"DMG_EFCTV_DT"') }} as dmg_efctv_dt,
        {{ null_junk('"AO_TRMNTN_DT"') }} as ao_trmntn_dt,
        {{ null_junk('"AO_NAME"') }} as ao_name,
        {{ null_junk('"FACNAME"') }} as facname,
        {{ null_junk('"FAC_TYPE_CODE"') }} as fac_type_code,
        {{ null_junk('"FAC_FDR"') }} as fac_fdr,
        {{ null_junk('"LTC"') }} as ltc,
        {{ stg_int('"CAPACITY"') }} as capacity,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ stg_id_text('"ZIP9"') }} as zip9,
        {{ null_junk('"FACADMIN"') }} as facadmin,
        {{ null_junk('"CONTACT_EMAIL"') }} as contact_email,
        {{ null_junk('"CONTACT_FAX"') }} as contact_fax,
        {{ null_junk('"CONTACT_PHONE_NUMBER"') }} as contact_phone_number,
        {{ stg_int('"COUNTY_CODE"') }} as county_code,
        {{ null_junk('"COUNTY_NAME"') }} as county_name,
        {{ stg_int('"DISTRICT_NUMBER"') }} as district_number,
        {{ null_junk('"DISTRICT_NAME"') }} as district_name,
        {{ null_junk('"ISFACMAIN"') }} as isfacmain,
        {{ null_junk('"PARENT_FACID"') }} as parent_facid,
        {{ null_junk('"FAC_FAC_RELATIONSHIP_TYPE_CODE"') }} as fac_fac_relationship_type_code,
        {{ null_junk('"START_DATE"') }} as start_date,
        {{ stg_id_text('"LICENSE_NUMBER"') }} as license_number,
        {{ null_junk('"BUSINESS_NAME"') }} as business_name,
        {{ null_junk('"LICENSE_STATUS_DESCRIPTION"') }} as license_status_description,
        {{ stg_ts('"INITIAL_LICENSE_DATE"') }} as initial_license_date,
        {{ stg_ts('"LICENSE_EFFECTIVE_DATE"') }} as license_effective_date,
        {{ stg_ts('"LICENSE_EXPIRATION_DATE"') }} as license_expiration_date,
        {{ null_junk('"ENTITY_TYPE_DESCRIPTION"') }} as entity_type_description,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"LOCATION"') }} as location,
        {{ stg_id_text('"HCAI_ID"') }} as hcai_id,
        {{ stg_int('"CCLHO_CODE"') }} as cclho_code,
        {{ null_junk('"CCLHO_NAME"') }} as cclho_name,
        {{ null_junk('"FIPS_COUNTY_CODE"') }} as fips_county_code,
        {{ null_junk('"BIRTHING_FACILITY_FLAG"') }} as birthing_facility_flag,
        {{ null_junk('"TRAUMA_PED_CTR"') }} as trauma_ped_ctr,
        {{ null_junk('"TRAUMA_CTR"') }} as trauma_ctr,
        {{ null_junk('"TYPE_OF_CARE"') }} as type_of_care,
        {{ null_junk('"CRITICAL_ACCESS_HOSPITAL"') }} as critical_access_hospital,
        {{ stg_ts('"DATA_DATE"') }} as data_date,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.lacounty.gov/datasets/c5a91c789041455485de39a45d0aae0c_2' as _source_url

    from source

)

select * from renamed
