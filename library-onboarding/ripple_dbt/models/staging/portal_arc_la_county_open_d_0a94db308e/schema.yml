version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_LA_COUNTY_OPEN_D_0A94DB308E
        description: >
          Raw landing table for Hospitals. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_la_county_open_d_0a94db308e__providers
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per provider (CCN+NPI+PARTICIPATION_DATE+APPROVAL_DATE+START_DATE unique). SPINE_ENTITY: provider."
    columns:
      - name: ccn
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: npi
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: participation_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: approval_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: start_date
        data_tests:
          - not_null:
              config:
                severity: warn
