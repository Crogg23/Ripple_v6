{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+NAICS+RESOURCE_TYPE+SUB_RESOURCE_TYPE+BUSINESS_ID+METADATA_ID unique)
-- SPINE_ENTITY: place  (natural_key: ZIP, BUSINESS_NAICS_CODE, RESOURCE_TYPE, SUB_RESOURCE_TYPE, BUSINESS_ID, METADATA_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_OPEN_DATA_BR_C110D5CF59') }}

),

renamed as (

    select
        "BUSINESS_NAME" as business_name,
        "FULL_ADDRESS" as full_address,
        "CITY" as city,
        "ZIP" as zip,
        "ABC" as abc,
        "BUSINESS_NAICS_CODE" as business_naics_code,
        "BUSINESS_ID" as business_id,
        "THE_GEOM" as the_geom,
        "ADDRESS_POINT_ID" as address_point_id,
        "POINT_X" as point_x,
        "POINT_Y" as point_y,
        "ADDRESS_NO_COMPLETE" as address_no_complete,
        "ST_NAME_ID" as st_name_id,
        "STREET_NAME_COMPLETE" as street_name_complete,
        "METADATA_ID" as metadata_id,
        "REVENUE_ACCOUNT_NUMBER" as revenue_account_number,
        "HOME_BASED_BUSINESS" as home_based_business,
        "CONSOLIDATED_FILER" as consolidated_filer,
        "RESOURCE_TYPE" as resource_type,
        "LANDMARK_NAME" as landmark_name,
        "SUB_RESOURCE_TYPE" as sub_resource_type,
        "PHONE_NUMBER" as phone_number,
        "NOTATION" as notation,
        "PLACE_NAME" as place_name,
        "WEBSITE" as website,
        INGESTED_AT as _loaded_at,
        'https://data.brla.gov/d/xhjf-mdnv' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by zip, business_naics_code, resource_type, sub_resource_type, business_id, metadata_id order by _loaded_at desc) = 1
