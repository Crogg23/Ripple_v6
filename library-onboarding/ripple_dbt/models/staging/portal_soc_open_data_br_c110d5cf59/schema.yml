version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_OPEN_DATA_BR_C110D5CF59
        description: >
          Raw landing table for ABC_License. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_open_data_br_c110d5cf59__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+NAICS+RESOURCE_TYPE+SUB_RESOURCE_TYPE+BUSINESS_ID+METADATA_ID unique). SPINE_ENTITY: place."
    columns:
      - name: zip
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: business_naics_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: resource_type
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: sub_resource_type
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: business_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: metadata_id
        data_tests:
          - not_null:
              config:
                severity: warn
