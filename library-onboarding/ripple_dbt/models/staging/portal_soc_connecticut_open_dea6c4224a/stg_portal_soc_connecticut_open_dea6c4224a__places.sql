{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (ZIP is unique)
-- SPINE_ENTITY: place  (natural_key: ZIP_CODE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 23 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_CONNECTICUT_OPEN_DEA6C4224A') }}
    qualify row_number() over (partition by "ZIP_CODE" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"FISCAL_YEAR"') }} as fiscal_year,
        {{ null_junk('"COMPANY_NAME"') }} as company_name,
        {{ null_junk('"COMPANY_ADDRESS"') }} as company_address,
        {{ null_junk('"MUNICIPALITY"') }} as municipality,
        {{ null_junk('"COUNTY"') }} as county,
        {{ null_junk('"LEGISLATIVE_DISTRICT"') }} as legislative_district,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"BUSINESS_INDUSTRY"') }} as business_industry,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"MINORITY_WOMAN_VETERAN"') }} as minority_woman_veteran,
        {{ null_junk('"CONTRACT_EXECUTION_DATE"') }} as contract_execution_date,
        {{ null_junk('"GRANT_AMOUNT"') }} as grant_amount,
        {{ null_junk('"LOAN_AMOUNT"') }} as loan_amount,
        {{ null_junk('"TOTAL_ASSISTANCE"') }} as total_assistance,
        {{ null_junk('"TOTAL_PROJECT_COST"') }} as total_project_cost,
        {{ null_junk('"AMOUNT_LEVERAGED"') }} as amount_leveraged,
        {{ null_junk('"FUNDING_SOURCE"') }} as funding_source,
        {{ null_junk('"STATUTORY_REFERENCE"') }} as statutory_reference,
        {{ null_junk('"PER_APPLICATION_EXISTING"') }} as per_application_existing,
        {{ null_junk('"PER_APPLICATION_FULL_TIME"') }} as per_application_full_time,
        {{ null_junk('"PER_APPLICATION_EXISTING_1"') }} as per_application_existing_1,
        {{ null_junk('"PER_APPLICATION_PART_TIME"') }} as per_application_part_time,
        INGESTED_AT as _loaded_at,
        'https://data.ct.gov/d/8j58-xb79' as _source_url

    from source

)

select * from renamed
