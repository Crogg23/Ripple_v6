{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (ZIP is unique)
-- SPINE_ENTITY: place  (natural_key: ZIP_CODE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_CONNECTICUT_OPEN_DEA6C4224A') }}

),

renamed as (

    select
        "FISCAL_YEAR" as fiscal_year,
        "COMPANY_NAME" as company_name,
        "COMPANY_ADDRESS" as company_address,
        "MUNICIPALITY" as municipality,
        "COUNTY" as county,
        "LEGISLATIVE_DISTRICT" as legislative_district,
        "STATE" as state,
        "ZIP_CODE" as zip_code,
        "BUSINESS_INDUSTRY" as business_industry,
        "NAICS_CODE" as naics_code,
        "MINORITY_WOMAN_VETERAN" as minority_woman_veteran,
        "CONTRACT_EXECUTION_DATE" as contract_execution_date,
        "GRANT_AMOUNT" as grant_amount,
        "LOAN_AMOUNT" as loan_amount,
        "TOTAL_ASSISTANCE" as total_assistance,
        "TOTAL_PROJECT_COST" as total_project_cost,
        "AMOUNT_LEVERAGED" as amount_leveraged,
        "FUNDING_SOURCE" as funding_source,
        "STATUTORY_REFERENCE" as statutory_reference,
        "PER_APPLICATION_EXISTING" as per_application_existing,
        "PER_APPLICATION_FULL_TIME" as per_application_full_time,
        "PER_APPLICATION_EXISTING_1" as per_application_existing_1,
        "PER_APPLICATION_PART_TIME" as per_application_part_time,
        INGESTED_AT as _loaded_at,
        'https://data.ct.gov/d/8j58-xb79' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by zip_code order by _loaded_at desc) = 1
