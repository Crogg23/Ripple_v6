{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_039AACD655') }}

),

renamed as (

    select
        "X" as x,
        "Y" as y,
        "LABEL_FLD" as label_fld,
        "PARCEL_IDA" as parcel_ida,
        "ADD_ALIAS" as add_alias,
        "ADDRESS" as address,
        "CITY" as city,
        "FID" as fid,
        "DA_ID" as da_id,
        "REGSOURCE" as regsource,
        "DATEUPDATE" as dateupdate,
        "EFFDATE" as effdate,
        "EXPIREDATE" as expiredate,
        "SITEUNIQID" as siteuniqid,
        "COUNTRY" as country,
        "STATE" as state,
        "COUNTY" as county,
        "ADDCODE" as addcode,
        "ADDDATAURI" as adddatauri,
        "INCORMUN" as incormun,
        "UNINCORCOM" as unincorcom,
        "NEIGHCOM" as neighcom,
        "ADDNUMPRE" as addnumpre,
        "ADDNUMBER" as addnumber,
        "ADDNUMSUF" as addnumsuf,
        "STNPREMOD" as stnpremod,
        "STNPREDIR" as stnpredir,
        "STNPRETYP" as stnpretyp,
        "STNPTYSEP" as stnptysep,
        "STNAME" as stname,
        "STNPOSTYP" as stnpostyp,
        "STNPOSDIR" as stnposdir,
        "STNPOSMOD" as stnposmod,
        "ESN" as esn,
        "MSAGCOM" as msagcom,
        "POSTALCOMN" as postalcomn,
        "POSTALCODE" as postalcode,
        "ZIPPLUS4" as zipplus4,
        "BUILDING" as building,
        "FLOOR" as floor,
        "UNIT" as unit,
        "ROOM" as room,
        "SEAT" as seat,
        "ADDLOCINFO" as addlocinfo,
        "COMPLANDN" as complandn,
        "MILEPOST" as milepost,
        "PLACETYPE" as placetype,
        "PLMETHOD" as plmethod,
        "LONGITUDE" as longitude,
        "LATITUDE" as latitude,
        "ELEVATION" as elevation,
        "LEGSTNPDIR" as legstnpdir,
        "LEGSTNAME" as legstname,
        "LEGSTNPOTY" as legstnpoty,
        "LEGSTPODIR" as legstpodir,
        "EXCEPTION" as exception,
        "ID" as id,
        "STNUMBER" as stnumber,
        "STRUCTURE" as structure,
        "X2" as x2,
        "Y2" as y2,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/addresses2' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by id order by _loaded_at desc) = 1
