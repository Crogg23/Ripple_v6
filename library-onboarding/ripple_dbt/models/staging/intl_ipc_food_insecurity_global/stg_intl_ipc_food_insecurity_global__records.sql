{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'INTL_IPC_FOOD_INSECURITY_GLOBAL') }}

),

renamed as (

    select
        DATE_OF_ANALYSIS as date_of_analysis,
        COUNTRY as country,
        TOTAL_COUNTRY_POPULATION as total_country_population,
        VALIDITY_PERIOD as validity_period,
        C_FROM as c_from,
        C_TO as c_to,
        PHASE as phase,
        NUMBER as number,
        PERCENTAGE as percentage,
        INGESTED_AT as _loaded_at,
        'https://data.humdata.org/dataset/7a7e7428-b8d7-4d2e-91d3-19100500e016/resource/6926dff7-658a-49e1-8d61-0ed8a983fbe1/download/ipc_global_national_long_latest.csv' as _source_url

    from source

)

select * from renamed
