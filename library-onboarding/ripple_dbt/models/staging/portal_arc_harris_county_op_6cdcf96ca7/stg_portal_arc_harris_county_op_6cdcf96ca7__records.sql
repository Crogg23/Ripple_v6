{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+VAL_DATE+IN_100_YR_FLOODPLAIN+IN_500_YR_FLOODPLAIN+ID+STATE_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ZIP, VAL_DATE, IN_100_YR_FLOODPLAIN, IN_500_YR_FLOODPLAIN, ID, STATE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_HARRIS_COUNTY_OP_6CDCF96CA7') }}

),

renamed as (

    select
        "OBJECTID_1" as objectid_1,
        "OBJECTID" as objectid,
        "ID" as id,
        "NAME" as name,
        "ADDRESS" as address,
        "CITY" as city,
        "STATE" as state,
        "ZIP" as zip,
        "ZIP4" as zip4,
        "TELEPHONE" as telephone,
        "TYPE" as type,
        "STATUS" as status,
        "POPULATION" as population,
        "COUNTY" as county,
        "COUNTRY" as country,
        "LATITUDE" as latitude,
        "LONGITUDE" as longitude,
        "NAICS_CODE" as naics_code,
        "NAICS_DESC" as naics_desc,
        "SOURCE" as source,
        "SOURCEDATE" as sourcedate,
        "VAL_METHOD" as val_method,
        "VAL_DATE" as val_date,
        "WEBSITE" as website,
        "STATE_ID" as state_id,
        "OWNER" as owner,
        "TTL_STAFF" as ttl_staff,
        "BEDS" as beds,
        "TRAUMA" as trauma,
        "HELIPAD" as helipad,
        "GENERATOR_ONSITE" as generator_onsite,
        "SELF_SUFFICIENT_ELECTRICITY" as self_sufficient_electricity,
        "IN_100_YR_FLOODPLAIN" as in_100_yr_floodplain,
        "IN_500_YR_FLOODPLAIN" as in_500_yr_floodplain,
        "IN_SURGE_SLOSH_AREA" as in_surge_slosh_area,
        "GLOBALID" as globalid,
        "CREATIONDATE" as creationdate,
        "CREATOR" as creator,
        "EDITDATE" as editdate,
        "EDITOR" as editor,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://geo-harriscounty.opendata.arcgis.com/datasets/faefadda90e248aaae6fcaac7305adef_64' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by zip, val_date, in_100_yr_floodplain, in_500_yr_floodplain, id, state_id order by _loaded_at desc) = 1
