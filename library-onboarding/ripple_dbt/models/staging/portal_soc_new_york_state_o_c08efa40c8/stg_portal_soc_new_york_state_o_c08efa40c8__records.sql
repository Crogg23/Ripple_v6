{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (DEC_ID+YEAR unique)
-- SPINE_ENTITY: (not determined)  (natural_key: DEC_ID, YEAR)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 12 of 19 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   voc: float
--   nox: float
--   co: float
--   co2: float
--   particulates: float
--   pm10: float
--   pm2_5: float
--   haps: float
--   so2: float
--   computed_region_yamh_8v7k: integer
--   computed_region_wbg7_3whc: integer
--   computed_region_kjdx_g34t: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_NEW_YORK_STATE_O_C08EFA40C8') }}
    qualify row_number() over (partition by "DEC_ID", "YEAR" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"YEAR"') }} as year,
        {{ null_junk('"COUNTY"') }} as county,
        {{ null_junk('"MUNICIPALITY"') }} as municipality,
        {{ null_junk('"DEC_ID"') }} as dec_id,
        {{ null_junk('"FACILITY_NAME"') }} as facility_name,
        {{ null_junk('"SIC_CODE"') }} as sic_code,
        {{ stg_float('"VOC"') }} as voc,
        {{ stg_float('"NOX"') }} as nox,
        {{ stg_float('"CO"') }} as co,
        {{ stg_float('"CO2"') }} as co2,
        {{ stg_float('"PARTICULATES"') }} as particulates,
        {{ stg_float('"PM10"') }} as pm10,
        {{ stg_float('"PM2_5"') }} as pm2_5,
        {{ stg_float('"HAPS"') }} as haps,
        {{ stg_float('"SO2"') }} as so2,
        {{ null_junk('"LOCATION"') }} as location,
        {{ stg_int('"COMPUTED_REGION_YAMH_8V7K"') }} as computed_region_yamh_8v7k,
        {{ stg_int('"COMPUTED_REGION_WBG7_3WHC"') }} as computed_region_wbg7_3whc,
        {{ stg_int('"COMPUTED_REGION_KJDX_G34T"') }} as computed_region_kjdx_g34t,
        INGESTED_AT as _loaded_at,
        'https://data.ny.gov/d/4ry5-tfin' as _source_url

    from source

)

select * from renamed
