{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (DEC_ID+YEAR unique)
-- SPINE_ENTITY: (not determined)  (natural_key: DEC_ID, YEAR)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_NEW_YORK_STATE_O_C08EFA40C8') }}

),

renamed as (

    select
        "YEAR" as year,
        "COUNTY" as county,
        "MUNICIPALITY" as municipality,
        "DEC_ID" as dec_id,
        "FACILITY_NAME" as facility_name,
        "SIC_CODE" as sic_code,
        "VOC" as voc,
        "NOX" as nox,
        "CO" as co,
        "CO2" as co2,
        "PARTICULATES" as particulates,
        "PM10" as pm10,
        "PM2_5" as pm2_5,
        "HAPS" as haps,
        "SO2" as so2,
        "LOCATION" as location,
        "COMPUTED_REGION_YAMH_8V7K" as computed_region_yamh_8v7k,
        "COMPUTED_REGION_WBG7_3WHC" as computed_region_wbg7_3whc,
        "COMPUTED_REGION_KJDX_G34T" as computed_region_kjdx_g34t,
        INGESTED_AT as _loaded_at,
        'https://data.ny.gov/d/4ry5-tfin' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by dec_id, year order by _loaded_at desc) = 1
