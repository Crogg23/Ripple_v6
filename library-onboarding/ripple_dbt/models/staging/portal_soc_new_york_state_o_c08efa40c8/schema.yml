version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_NEW_YORK_STATE_O_C08EFA40C8
        description: >
          Raw landing table for Title V Emissions Inventory: Beginning 2010. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_new_york_state_o_c08efa40c8__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (DEC_ID+YEAR unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: dec_id
        data_tests:
          - not_null
      - name: year
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - dec_id
              - year