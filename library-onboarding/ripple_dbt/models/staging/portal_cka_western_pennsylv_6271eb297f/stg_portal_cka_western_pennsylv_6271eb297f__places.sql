{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LATITUDE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 24 of 27 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   c_1a: float
--   c_2a: float
--   c_3a: float
--   c_4a: float
--   c_5a: float
--   c_6a: float
--   c_7a: float
--   c_8a: float
--   c_9a: float
--   c_10a: float
--   c_11a: float
--   c_12p: float
--   c_1p: float
--   c_2p: float
--   c_3p: float
--   c_4p: float
--   c_5p: float
--   c_6p: float
--   c_7p: float
--   c_8p: float
--   c_9p: float
--   c_10p: float
--   c_11p: float
--   c_12a: float
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (LATITUDE) is NOT unique within a load as landed at generation time -- it would collapse 1 of 1,305 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_6271EB297F') }}
    qualify row_number() over (partition by "SENSOR_ID", "LONGITUDE", "LATITUDE", "C_1A", "C_2A", "C_3A", "C_4A", "C_5A", "C_6A", "C_7A", "C_8A", "C_9A", "C_10A", "C_11A", "C_12P", "C_1P", "C_2P", "C_3P", "C_4P", "C_5P", "C_6P", "C_7P", "C_8P", "C_9P", "C_10P", "C_11P", "C_12A" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SENSOR_ID"') }} as sensor_id,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ stg_float('"C_1A"') }} as c_1a,
        {{ stg_float('"C_2A"') }} as c_2a,
        {{ stg_float('"C_3A"') }} as c_3a,
        {{ stg_float('"C_4A"') }} as c_4a,
        {{ stg_float('"C_5A"') }} as c_5a,
        {{ stg_float('"C_6A"') }} as c_6a,
        {{ stg_float('"C_7A"') }} as c_7a,
        {{ stg_float('"C_8A"') }} as c_8a,
        {{ stg_float('"C_9A"') }} as c_9a,
        {{ stg_float('"C_10A"') }} as c_10a,
        {{ stg_float('"C_11A"') }} as c_11a,
        {{ stg_float('"C_12P"') }} as c_12p,
        {{ stg_float('"C_1P"') }} as c_1p,
        {{ stg_float('"C_2P"') }} as c_2p,
        {{ stg_float('"C_3P"') }} as c_3p,
        {{ stg_float('"C_4P"') }} as c_4p,
        {{ stg_float('"C_5P"') }} as c_5p,
        {{ stg_float('"C_6P"') }} as c_6p,
        {{ stg_float('"C_7P"') }} as c_7p,
        {{ stg_float('"C_8P"') }} as c_8p,
        {{ stg_float('"C_9P"') }} as c_9p,
        {{ stg_float('"C_10P"') }} as c_10p,
        {{ stg_float('"C_11P"') }} as c_11p,
        {{ stg_float('"C_12A"') }} as c_12a,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-traffic-counts' as _source_url

    from source

)

select * from renamed
