version: 2

models:
  - name: stg_fed_epa_npdes_npdes_se_violations__records
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per record (spine_entity not determined -- no registry hint available) (NPDES_VIOLATION_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: npdes_violation_id
        data_tests:
          - not_null:
              config:
                severity: warn
