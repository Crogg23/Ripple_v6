version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_415F1FE712
        description: >
          Raw landing table for Allegheny County-Owned Bridges Points. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_415f1fe712__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ET_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: et_id
        data_tests:
          - unique
          - not_null
