{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ET_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ET_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_415F1FE712') }}

),

renamed as (

    select
        "X" as x,
        "Y" as y,
        "FID" as fid,
        "BRIDGE_COD" as bridge_cod,
        "BRBIDGE_NA" as brbidge_na,
        "FACILITYID" as facilityid,
        "SHAPE_LENG" as shape_leng,
        "ET_ID" as et_id,
        "ET_IDP" as et_idp,
        "ET_X" as et_x,
        "ET_Y" as et_y,
        "ET_ANGLE" as et_angle,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-owned-bridges-points' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by et_id order by _loaded_at desc) = 1
