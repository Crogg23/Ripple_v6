{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (FIPS is unique)
-- SPINE_ENTITY: place  (natural_key: FIPS)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_4E9F15E555') }}

),

renamed as (

    select
        "ASSESSORTERRITORY" as assessorterritory,
        "GLOBALID" as globalid,
        "MUNICODE" as municode,
        "VALUATIONAREA" as valuationarea,
        "FIPS" as fips,
        "CONGDIST" as congdist,
        "CNTL_ID" as cntl_id,
        "SCHOOLD" as schoold,
        "TYPE" as type,
        "NAME" as name,
        "EOC" as eoc,
        "ACRES" as acres,
        "YEARCONVERTED" as yearconverted,
        "LABEL" as label,
        "OBJECTID" as objectid,
        "COG" as cog,
        "REGION" as region,
        "SQMI" as sqmi,
        "CNTYCOUNCIL" as cntycouncil,
        "DATASPATIAL_WKB" as dataspatial_wkb,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-municipal-boundaries' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by fips order by _loaded_at desc) = 1
