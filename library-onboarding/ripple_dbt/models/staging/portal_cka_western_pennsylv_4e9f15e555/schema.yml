version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_4E9F15E555
        description: >
          Raw landing table for Allegheny County Municipal Boundaries. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_4e9f15e555__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (FIPS is unique). SPINE_ENTITY: place."
    columns:
      - name: fips
        data_tests:
          - not_null:
              config:
                severity: warn
