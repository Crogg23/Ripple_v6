{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM+CREATED_DATE unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE__LENGTH, CREATED_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (SHAPE__LENGTH, CREATED_DATE) is NOT unique within a load as landed at generation time -- it would collapse 4 of 6,493 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_OPEN_DATA_SA_A8EF161189') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "PLATNUMBER" as platnumber,
        "PLATNAME" as platname,
        "RECORDATIONDATE" as recordationdate,
        "ENGINEER" as engineer,
        "CREATED_DATE" as created_date,
        "LAST_EDITED_DATE" as last_edited_date,
        "SHAPE__AREA" as shape__area,
        "SHAPE__LENGTH" as shape__length,
        INGESTED_AT as _loaded_at,
        'https://data.sanantonio.gov/dataset/recordedplat' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by objectid, platnumber, platname, recordationdate, engineer, created_date, last_edited_date, shape__area, shape__length order by _loaded_at desc) = 1
