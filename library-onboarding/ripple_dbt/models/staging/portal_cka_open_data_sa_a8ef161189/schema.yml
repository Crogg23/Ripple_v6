version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_OPEN_DATA_SA_A8EF161189
        description: >
          Raw landing table for RecordedPlat. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_open_data_sa_a8ef161189__places
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GEOM+CREATED_DATE unique). SPINE_ENTITY: place."
    columns:
      - name: shape__length
        data_tests:
          - not_null
      - name: created_date
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - shape__length
            - created_date
