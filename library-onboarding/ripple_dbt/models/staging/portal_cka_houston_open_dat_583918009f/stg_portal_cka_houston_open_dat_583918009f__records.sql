{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GEO_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: GEO_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 7 of 9 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   b23025_001e: integer
--   b23025_002e: integer
--   b23025_003e: integer
--   b23025_004e: integer
--   b23025_005e: integer
--   b23025_006e: integer
--   b23025_007e: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_HOUSTON_OPEN_DAT_583918009F') }}
    qualify row_number() over (partition by "GEO_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"GEO_ID"') }} as geo_id,
        {{ null_junk('"NAME"') }} as name,
        {{ stg_int('"B23025_001E"') }} as b23025_001e,
        {{ stg_int('"B23025_002E"') }} as b23025_002e,
        {{ stg_int('"B23025_003E"') }} as b23025_003e,
        {{ stg_int('"B23025_004E"') }} as b23025_004e,
        {{ stg_int('"B23025_005E"') }} as b23025_005e,
        {{ stg_int('"B23025_006E"') }} as b23025_006e,
        {{ stg_int('"B23025_007E"') }} as b23025_007e,
        INGESTED_AT as _loaded_at,
        'https://data.houstontx.gov/dataset/acs-5-year-population-estimates-by-employment-status-at-block-group-level' as _source_url

    from source

)

select * from renamed
