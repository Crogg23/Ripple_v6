{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (NPDES_VIOLATION_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: NPDES_VIOLATION_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 7 of 18 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   comp_schedule_event_id: integer
--   comp_schedule_nmbr: integer
--   schedule_date: date
--   actual_date: date
--   rnc_detection_date: date
--   rnc_resolution_date: date
--   report_received_date: date
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_NPDES_NPDES_CS_VIOLATIONS') }}
    qualify row_number() over (partition by "NPDES_VIOLATION_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"NPDES_ID"') }} as npdes_id,
        {{ null_junk('"NPDES_VIOLATION_ID"') }} as npdes_violation_id,
        {{ null_junk('"VIOLATION_TYPE_CODE"') }} as violation_type_code,
        {{ stg_int('"COMP_SCHEDULE_EVENT_ID"') }} as comp_schedule_event_id,
        {{ stg_int('"COMP_SCHEDULE_NMBR"') }} as comp_schedule_nmbr,
        {{ null_junk('"VIOLATION_CODE"') }} as violation_code,
        {{ null_junk('"VIOLATION_DESC"') }} as violation_desc,
        {{ null_junk('"SCHEDULE_EVENT_CODE"') }} as schedule_event_code,
        {{ null_junk('"SCHEDULE_EVENT_DESC"') }} as schedule_event_desc,
        {{ stg_date('"SCHEDULE_DATE"') }} as schedule_date,
        {{ stg_date('"ACTUAL_DATE"') }} as actual_date,
        {{ null_junk('"RNC_DETECTION_CODE"') }} as rnc_detection_code,
        {{ null_junk('"RNC_DETECTION_DESC"') }} as rnc_detection_desc,
        {{ stg_date('"RNC_DETECTION_DATE"') }} as rnc_detection_date,
        {{ null_junk('"RNC_RESOLUTION_CODE"') }} as rnc_resolution_code,
        {{ null_junk('"RNC_RESOLUTION_DESC"') }} as rnc_resolution_desc,
        {{ stg_date('"RNC_RESOLUTION_DATE"') }} as rnc_resolution_date,
        {{ stg_date('"REPORT_RECEIVED_DATE"') }} as report_received_date,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
