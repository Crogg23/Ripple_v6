{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per case (CASE_ID unique)
-- SPINE_ENTITY: case  (natural_key: CASE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'case' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_E4C004D662') }}

),

renamed as (

    select
        "CASE_ID" as case_id,
        "OPEN_DATE" as open_date,
        "CASE_TOPIC" as case_topic,
        "SERVICE_NAME" as service_name,
        "ASSIGNED_DEPARTMENT" as assigned_department,
        "ASSIGNED_TEAM" as assigned_team,
        "CASE_STATUS" as case_status,
        "CLOSURE_REASON" as closure_reason,
        "CLOSURE_COMMENTS" as closure_comments,
        "CLOSE_DATE" as close_date,
        "TARGET_CLOSE_DATE" as target_close_date,
        "ON_TIME" as on_time,
        "REPORT_SOURCE" as report_source,
        "FULL_ADDRESS" as full_address,
        "STREET_NUMBER" as street_number,
        "STREET_NAME" as street_name,
        "ZIP_CODE" as zip_code,
        "NEIGHBORHOOD" as neighborhood,
        "PUBLIC_WORKS_DISTRICT" as public_works_district,
        "CITY_COUNCIL_DISTRICT" as city_council_district,
        "FIRE_DISTRICT" as fire_district,
        "POLICE_DISTRICT" as police_district,
        "WARD" as ward,
        "PRECINCT" as precinct,
        "SUBMITTED_PHOTO" as submitted_photo,
        "CLOSED_PHOTO" as closed_photo,
        "LONGITUDE" as longitude,
        "LATITUDE" as latitude,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/311-service-requests' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by case_id order by _loaded_at desc) = 1
