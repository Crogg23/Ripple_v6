{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per case (CASE_ID unique)
-- SPINE_ENTITY: case  (natural_key: CASE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 10 of 45 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   open_date: timestamp
--   close_date: timestamp
--   target_close_date: timestamp
--   city_council_district: integer
--   fire_district: integer
--   case_enquiry_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   open_dt: timestamp
--   sla_target_dt: timestamp
--   closed_dt: timestamp
--   neighborhood_services_district: integer
-- spine_entity 'case' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (CASE_ID) is NOT unique within a load as landed at generation time -- it would collapse 1,949,137 of 2,000,000 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_E4C004D662') }}
    qualify row_number() over (partition by "CASE_ID", "OPEN_DATE", "CASE_TOPIC", "SERVICE_NAME", "ASSIGNED_DEPARTMENT", "ASSIGNED_TEAM", "CASE_STATUS", "CLOSURE_REASON", "CLOSURE_COMMENTS", "CLOSE_DATE", "TARGET_CLOSE_DATE", "ON_TIME", "REPORT_SOURCE", "FULL_ADDRESS", "STREET_NUMBER", "STREET_NAME", "ZIP_CODE", "NEIGHBORHOOD", "PUBLIC_WORKS_DISTRICT", "CITY_COUNCIL_DISTRICT", "FIRE_DISTRICT", "POLICE_DISTRICT", "WARD", "PRECINCT", "SUBMITTED_PHOTO", "CLOSED_PHOTO", "LONGITUDE", "LATITUDE", "CASE_ENQUIRY_ID", "OPEN_DT", "SLA_TARGET_DT", "CLOSED_DT", "CASE_TITLE", "SUBJECT", "REASON", "TYPE", "QUEUE", "DEPARTMENT", "LOCATION", "PWD_DISTRICT", "NEIGHBORHOOD_SERVICES_DISTRICT", "LOCATION_STREET_NAME", "LOCATION_ZIPCODE", "GEOM_4326", "SOURCE" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CASE_ID"') }} as case_id,
        {{ stg_ts('"OPEN_DATE"') }} as open_date,
        {{ null_junk('"CASE_TOPIC"') }} as case_topic,
        {{ null_junk('"SERVICE_NAME"') }} as service_name,
        {{ null_junk('"ASSIGNED_DEPARTMENT"') }} as assigned_department,
        {{ null_junk('"ASSIGNED_TEAM"') }} as assigned_team,
        {{ null_junk('"CASE_STATUS"') }} as case_status,
        {{ null_junk('"CLOSURE_REASON"') }} as closure_reason,
        {{ null_junk('"CLOSURE_COMMENTS"') }} as closure_comments,
        {{ stg_ts('"CLOSE_DATE"') }} as close_date,
        {{ stg_ts('"TARGET_CLOSE_DATE"') }} as target_close_date,
        {{ null_junk('"ON_TIME"') }} as on_time,
        {{ null_junk('"REPORT_SOURCE"') }} as report_source,
        {{ null_junk('"FULL_ADDRESS"') }} as full_address,
        {{ null_junk('"STREET_NUMBER"') }} as street_number,
        {{ null_junk('"STREET_NAME"') }} as street_name,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"NEIGHBORHOOD"') }} as neighborhood,
        {{ null_junk('"PUBLIC_WORKS_DISTRICT"') }} as public_works_district,
        {{ stg_int('"CITY_COUNCIL_DISTRICT"') }} as city_council_district,
        {{ stg_int('"FIRE_DISTRICT"') }} as fire_district,
        {{ null_junk('"POLICE_DISTRICT"') }} as police_district,
        {{ null_junk('"WARD"') }} as ward,
        {{ null_junk('"PRECINCT"') }} as precinct,
        {{ null_junk('"SUBMITTED_PHOTO"') }} as submitted_photo,
        {{ null_junk('"CLOSED_PHOTO"') }} as closed_photo,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ stg_id_text('"CASE_ENQUIRY_ID"') }} as case_enquiry_id,
        {{ stg_ts('"OPEN_DT"') }} as open_dt,
        {{ stg_ts('"SLA_TARGET_DT"') }} as sla_target_dt,
        {{ stg_ts('"CLOSED_DT"') }} as closed_dt,
        {{ null_junk('"CASE_TITLE"') }} as case_title,
        {{ null_junk('"SUBJECT"') }} as subject,
        {{ null_junk('"REASON"') }} as reason,
        {{ null_junk('"TYPE"') }} as type,
        {{ null_junk('"QUEUE"') }} as queue,
        {{ null_junk('"DEPARTMENT"') }} as department,
        {{ null_junk('"LOCATION"') }} as location,
        {{ null_junk('"PWD_DISTRICT"') }} as pwd_district,
        {{ stg_int('"NEIGHBORHOOD_SERVICES_DISTRICT"') }} as neighborhood_services_district,
        {{ null_junk('"LOCATION_STREET_NAME"') }} as location_street_name,
        {{ null_junk('"LOCATION_ZIPCODE"') }} as location_zipcode,
        {{ null_junk('"GEOM_4326"') }} as geom_4326,
        {{ null_junk('"SOURCE"') }} as source,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/311-service-requests' as _source_url

    from source

)

select * from renamed
