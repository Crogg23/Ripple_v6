version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_SANTA_CLARA_COUN_487FCD1FAB
        description: >
          Raw landing table for Santa Clara County Cases of Media Interest. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_santa_clara_coun_487fcd1fab__cases
    description: "GRAIN: one row per case (DOCKET is unique). SPINE_ENTITY: case."
    columns:
      - name: docket
        data_tests:
          - unique
          - not_null
