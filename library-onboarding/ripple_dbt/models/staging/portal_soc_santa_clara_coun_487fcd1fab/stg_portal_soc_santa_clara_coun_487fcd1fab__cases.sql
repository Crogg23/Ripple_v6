{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per case (DOCKET is unique)
-- SPINE_ENTITY: case  (natural_key: DOCKET)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 7 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'case' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_SANTA_CLARA_COUN_487FCD1FAB') }}
    qualify row_number() over (partition by "DOCKET" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"DAO_MEDIA_ID"') }} as dao_media_id,
        {{ null_junk('"DEFENDANT"') }} as defendant,
        {{ null_junk('"DOCKET"') }} as docket,
        {{ null_junk('"PROSECUTOR"') }} as prosecutor,
        {{ null_junk('"CHARGES"') }} as charges,
        {{ null_junk('"NEXT_COURT_DATE"') }} as next_court_date,
        {{ null_junk('"UPDATE_DATE"') }} as update_date,
        INGESTED_AT as _loaded_at,
        'https://data.sccgov.org/d/3gws-n3a5' as _source_url

    from source

)

select * from renamed
