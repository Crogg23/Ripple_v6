version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_SAN_JOSE_OPEN_DA_00B8041D47
        description: >
          Raw landing table for 311 Service Request Data. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_san_jose_open_da_00b8041d47__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INCIDENT_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: incident_id
        data_tests:
          - unique
          - not_null
