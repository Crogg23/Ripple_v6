{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INCIDENT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: INCIDENT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 10 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_00B8041D47') }}
    qualify row_number() over (partition by "INCIDENT_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"INCIDENT_ID"') }} as incident_id,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"SOURCE"') }} as source,
        {{ null_junk('"CATEGORY"') }} as category,
        {{ null_junk('"SERVICE_TYPE"') }} as service_type,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"DATE_CREATED"') }} as date_created,
        {{ null_junk('"DATE_LAST_UPDATED"') }} as date_last_updated,
        {{ null_junk('"DEPARTMENT"') }} as department,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/311-service-request-data' as _source_url

    from source

)

select * from renamed
