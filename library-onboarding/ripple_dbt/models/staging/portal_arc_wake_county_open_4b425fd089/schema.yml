version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_WAKE_COUNTY_OPEN_4B425FD089
        description: >
          Raw landing table for Wake County Nonprofits. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_wake_county_open_4b425fd089__organizations
    description: "GRAIN: one row per organization (EIN is unique). SPINE_ENTITY: organization."
    columns:
      - name: user_ein__not_used
        data_tests:
          - unique
          - not_null
