{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per organization (EIN is unique)
-- SPINE_ENTITY: organization  (natural_key: USER_EIN__NOT_USED)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 26 of 100 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   score: float
--   rank: integer
--   addnum: integer
--   addnumfrom: integer
--   addnumto: integer
--   postal: integer
--   distance: integer
--   x: float
--   y: float
--   displayx: float
--   displayy: float
--   xmin: float
--   xmax: float
--   ymin: float
--   ymax: float
--   in_postal: integer
--   user_total_expenses: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   user_column1: integer
--   user_table3_group: integer
--   user_table3_subsection: integer
--   user_table3_affiliation: integer
--   user_ruling: integer
--   ruling_text: integer
--   total_expenses_int: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   total_expenses_fixed: integer
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_WAKE_COUNTY_OPEN_4B425FD089') }}
    qualify row_number() over (partition by "USER_EIN__NOT_USED" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"LOC_NAME"') }} as loc_name,
        {{ null_junk('"STATUS"') }} as status,
        {{ stg_float('"SCORE"') }} as score,
        {{ null_junk('"MATCH_TYPE"') }} as match_type,
        {{ null_junk('"MATCH_ADDR"') }} as match_addr,
        {{ null_junk('"LONGLABEL"') }} as longlabel,
        {{ null_junk('"SHORTLABEL"') }} as shortlabel,
        {{ null_junk('"ADDR_TYPE"') }} as addr_type,
        {{ null_junk('"TYPE"') }} as type,
        {{ null_junk('"PLACENAME"') }} as placename,
        {{ null_junk('"PLACE_ADDR"') }} as place_addr,
        {{ null_junk('"PHONE"') }} as phone,
        {{ null_junk('"URL"') }} as url,
        {{ stg_int('"RANK"') }} as rank,
        {{ null_junk('"ADDBLDG"') }} as addbldg,
        {{ stg_int('"ADDNUM"') }} as addnum,
        {{ stg_int('"ADDNUMFROM"') }} as addnumfrom,
        {{ stg_int('"ADDNUMTO"') }} as addnumto,
        {{ null_junk('"ADDRANGE"') }} as addrange,
        {{ null_junk('"SIDE"') }} as side,
        {{ null_junk('"STPREDIR"') }} as stpredir,
        {{ null_junk('"STPRETYPE"') }} as stpretype,
        {{ null_junk('"STNAME"') }} as stname,
        {{ null_junk('"STTYPE"') }} as sttype,
        {{ null_junk('"STDIR"') }} as stdir,
        {{ null_junk('"BLDGTYPE"') }} as bldgtype,
        {{ null_junk('"BLDGNAME"') }} as bldgname,
        {{ null_junk('"LEVELTYPE"') }} as leveltype,
        {{ null_junk('"LEVELNAME"') }} as levelname,
        {{ null_junk('"UNITTYPE"') }} as unittype,
        {{ null_junk('"UNITNAME"') }} as unitname,
        {{ null_junk('"SUBADDR"') }} as subaddr,
        {{ null_junk('"STADDR"') }} as staddr,
        {{ null_junk('"BLOCK"') }} as block,
        {{ null_junk('"SECTOR"') }} as sector,
        {{ null_junk('"NBRHD"') }} as nbrhd,
        {{ null_junk('"DISTRICT"') }} as district,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"METROAREA"') }} as metroarea,
        {{ null_junk('"SUBREGION"') }} as subregion,
        {{ null_junk('"REGION"') }} as region,
        {{ null_junk('"REGIONABBR"') }} as regionabbr,
        {{ null_junk('"TERRITORY"') }} as territory,
        {{ null_junk('"ZONE"') }} as zone,
        {{ stg_int('"POSTAL"') }} as postal,
        {{ null_junk('"POSTALEXT"') }} as postalext,
        {{ null_junk('"COUNTRY"') }} as country,
        {{ null_junk('"CNTRYNAME"') }} as cntryname,
        {{ null_junk('"LANGCODE"') }} as langcode,
        {{ stg_int('"DISTANCE"') }} as distance,
        {{ stg_float('"X"') }} as x,
        {{ stg_float('"Y"') }} as y,
        {{ stg_float('"DISPLAYX"') }} as displayx,
        {{ stg_float('"DISPLAYY"') }} as displayy,
        {{ stg_float('"XMIN"') }} as xmin,
        {{ stg_float('"XMAX"') }} as xmax,
        {{ stg_float('"YMIN"') }} as ymin,
        {{ stg_float('"YMAX"') }} as ymax,
        {{ null_junk('"EXINFO"') }} as exinfo,
        {{ null_junk('"IN_ADDRESS"') }} as in_address,
        {{ null_junk('"IN_ADDRESS2"') }} as in_address2,
        {{ null_junk('"IN_ADDRESS3"') }} as in_address3,
        {{ null_junk('"IN_NEIGHBORHOOD"') }} as in_neighborhood,
        {{ null_junk('"IN_CITY"') }} as in_city,
        {{ null_junk('"IN_SUBREGION"') }} as in_subregion,
        {{ null_junk('"IN_REGION"') }} as in_region,
        {{ stg_int('"IN_POSTAL"') }} as in_postal,
        {{ null_junk('"IN_POSTALEXT"') }} as in_postalext,
        {{ null_junk('"IN_COUNTRYCODE"') }} as in_countrycode,
        {{ null_junk('"USER_ORGANIZATION"') }} as user_organization,
        {{ null_junk('"USER_EIN__NOT_USED"') }} as user_ein__not_used,
        {{ stg_id_text('"USER_TOTAL_EXPENSES"') }} as user_total_expenses,
        {{ null_junk('"USER_BUDGET_CATEGORY"') }} as user_budget_category,
        {{ null_junk('"USER_SERVICE_AREA"') }} as user_service_area,
        {{ null_junk('"USER_CITY__NOT_USED"') }} as user_city__not_used,
        {{ null_junk('"USER_STATE__DO_NOT_USE"') }} as user_state__do_not_use,
        {{ null_junk('"USER_ZIP_CODE__NOT_USED"') }} as user_zip_code__not_used,
        {{ null_junk('"USER_FIRST_NAME"') }} as user_first_name,
        {{ null_junk('"USER_LAST_NAME"') }} as user_last_name,
        {{ null_junk('"USER_EIN"') }} as user_ein,
        {{ null_junk('"USER_ORGANIZATION_NAME"') }} as user_organization_name,
        {{ null_junk('"USER_TABLE3_ICO"') }} as user_table3_ico,
        {{ null_junk('"USER_STREET_ADDRESS"') }} as user_street_address,
        {{ null_junk('"USER_CITY"') }} as user_city,
        {{ null_junk('"USER_STATE"') }} as user_state,
        {{ null_junk('"USER_ZIP_CODE"') }} as user_zip_code,
        {{ stg_int('"USER_COLUMN1"') }} as user_column1,
        {{ stg_int('"USER_TABLE3_GROUP"') }} as user_table3_group,
        {{ stg_int('"USER_TABLE3_SUBSECTION"') }} as user_table3_subsection,
        {{ stg_int('"USER_TABLE3_AFFILIATION"') }} as user_table3_affiliation,
        {{ null_junk('"USER_CLASSIFICATIONS"') }} as user_classifications,
        {{ stg_int('"USER_RULING"') }} as user_ruling,
        {{ stg_int('"RULING_TEXT"') }} as ruling_text,
        {{ stg_id_text('"TOTAL_EXPENSES_INT"') }} as total_expenses_int,
        {{ null_junk('"EIN_INT"') }} as ein_int,
        {{ null_junk('"EIN_FIXED"') }} as ein_fixed,
        {{ stg_int('"TOTAL_EXPENSES_FIXED"') }} as total_expenses_fixed,
        {{ null_junk('"CLASSIFICATIONS_FIXED"') }} as classifications_fixed,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wake.gov/datasets/bbe32208544649f698bdf8ae41c1becc_0' as _source_url,
    'ENT_' || LEFT(MD5('EIN' || '|' || (CASE WHEN LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', ''))) = 0 OR LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', ''))) > 9 OR NOT REGEXP_LIKE(UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', '')), '^[0-9]+$') OR LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', '')), 9, '0') = REPEAT('0', 9) OR (LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', ''))) >= 4 AND UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', '')) = REPEAT(LEFT(UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', '')), 1), LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', ''))))) OR LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', '')), 9, '0') = REPEAT(LEFT(LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', '')), 9, '0'), 1), 9) OR UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', '')) IN ('123456789', '987654321', '1234567890', '0987654321', '12345678', '87654321', '123456', '654321') OR LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', '')), 9, '0') IN ('123456789', '987654321', '1234567890', '0987654321', '12345678', '87654321', '123456', '654321') THEN NULL ELSE LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("USER_EIN__NOT_USED"), '[^0-9A-Za-z]', '')), 9, '0') END)), 16) as spine_entity_id  -- joins LIBRARY_META."CONNECT".ENTITY_GOLDEN.ENTITY_ID

    from source

)

select * from renamed
