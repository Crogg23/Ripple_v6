version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_ORANGE_COUNTY_OP_43A1FE3FBE
        description: >
          Raw landing table for OCWatershed.DBO.SAR Existing Dev Inventory. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_orange_county_op_43a1fe3fbe__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INVENTORY_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: inventory_id
        data_tests:
          - unique
          - not_null
