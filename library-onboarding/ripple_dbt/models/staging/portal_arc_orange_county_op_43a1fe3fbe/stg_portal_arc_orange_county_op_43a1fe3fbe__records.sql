{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INVENTORY_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: INVENTORY_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_ORANGE_COUNTY_OP_43A1FE3FBE') }}

),

renamed as (

    select
        "OBJECTID_1" as objectid_1,
        "OBJECTID" as objectid,
        "FACILITY_T" as facility_t,
        "REGION" as region,
        "WDID" as wdid,
        "BUSINESS_N" as business_n,
        "STREET_NUM" as street_num,
        "STREET_NAM" as street_nam,
        "STREET_TYP" as street_typ,
        "CITY" as city,
        "ZIP" as zip,
        "WATERSHED" as watershed,
        "LATITIUDE" as latitiude,
        "LONGITUDE" as longitude,
        "PRIORITY" as priority,
        "INSPECTION" as inspection,
        "CONTACT" as contact,
        "PHONE_NUMB" as phone_numb,
        "WQMP" as wqmp,
        "WQMP_NUMBE" as wqmp_numbe,
        "INVENTORY_ID" as inventory_id,
        "GLOBALID" as globalid,
        "CONTACT_EMAIL" as contact_email,
        "SIC_DESCRIPTION" as sic_description,
        "BUSINESS_LIC_NUMBER" as business_lic_number,
        "SIC_CODE" as sic_code,
        "FOOD_OWNER" as food_owner,
        "BUSINESS_NAME_ORIGINAL" as business_name_original,
        "CREATIONDATE" as creationdate,
        "CREATOR" as creator,
        "EDITDATE" as editdate,
        "EDITOR" as editor,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://data-ocpw.opendata.arcgis.com/datasets/4b168e3f9a1144a4979f8c6dd5280691_0' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by inventory_id order by _loaded_at desc) = 1
