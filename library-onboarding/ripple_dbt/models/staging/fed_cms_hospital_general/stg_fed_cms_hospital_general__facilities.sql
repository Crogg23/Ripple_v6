{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per facility (CCN is unique)
-- SPINE_ENTITY: facility  (natural_key: CCN)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.

with source as (

    select * from {{ source('ripple_raw', 'FED_CMS_HOSPITAL_GENERAL') }}

),

renamed as (

    select
        "CCN" as ccn,
        "FACILITY_NAME" as facility_name,
        "ADDRESS" as address,
        "CITY_TOWN" as city_town,
        "STATE" as state,
        "ZIP_CODE" as zip_code,
        "COUNTY_PARISH" as county_parish,
        "TELEPHONE_NUMBER" as telephone_number,
        "HOSPITAL_TYPE" as hospital_type,
        "HOSPITAL_OWNERSHIP" as hospital_ownership,
        "EMERGENCY_SERVICES" as emergency_services,
        "MEETS_CRITERIA_FOR_BIRTHING_FRIENDLY_DESIGNATION" as meets_criteria_for_birthing_friendly_designation,
        "HOSPITAL_OVERALL_RATING" as hospital_overall_rating,
        "HOSPITAL_OVERALL_RATING_FOOTNOTE" as hospital_overall_rating_footnote,
        "MORT_GROUP_MEASURE_COUNT" as mort_group_measure_count,
        "COUNT_OF_FACILITY_MORT_MEASURES" as count_of_facility_mort_measures,
        "COUNT_OF_MORT_MEASURES_BETTER" as count_of_mort_measures_better,
        "COUNT_OF_MORT_MEASURES_NO_DIFFERENT" as count_of_mort_measures_no_different,
        "COUNT_OF_MORT_MEASURES_WORSE" as count_of_mort_measures_worse,
        "MORT_GROUP_FOOTNOTE" as mort_group_footnote,
        "SAFETY_GROUP_MEASURE_COUNT" as safety_group_measure_count,
        "COUNT_OF_FACILITY_SAFETY_MEASURES" as count_of_facility_safety_measures,
        "COUNT_OF_SAFETY_MEASURES_BETTER" as count_of_safety_measures_better,
        "COUNT_OF_SAFETY_MEASURES_NO_DIFFERENT" as count_of_safety_measures_no_different,
        "COUNT_OF_SAFETY_MEASURES_WORSE" as count_of_safety_measures_worse,
        "SAFETY_GROUP_FOOTNOTE" as safety_group_footnote,
        "READM_GROUP_MEASURE_COUNT" as readm_group_measure_count,
        "COUNT_OF_FACILITY_READM_MEASURES" as count_of_facility_readm_measures,
        "COUNT_OF_READM_MEASURES_BETTER" as count_of_readm_measures_better,
        "COUNT_OF_READM_MEASURES_NO_DIFFERENT" as count_of_readm_measures_no_different,
        "COUNT_OF_READM_MEASURES_WORSE" as count_of_readm_measures_worse,
        "READM_GROUP_FOOTNOTE" as readm_group_footnote,
        "PT_EXP_GROUP_MEASURE_COUNT" as pt_exp_group_measure_count,
        "COUNT_OF_FACILITY_PT_EXP_MEASURES" as count_of_facility_pt_exp_measures,
        "PT_EXP_GROUP_FOOTNOTE" as pt_exp_group_footnote,
        "TE_GROUP_MEASURE_COUNT" as te_group_measure_count,
        "COUNT_OF_FACILITY_TE_MEASURES" as count_of_facility_te_measures,
        "TE_GROUP_FOOTNOTE" as te_group_footnote,
        _INGESTED_AT as _loaded_at,
        'https://data.cms.gov/provider-data/dataset/xubh-q36u' as _source_url,
    'ENT_' || LEFT(MD5('CCN' || '|' || (CASE WHEN LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("CCN"), '[^0-9A-Za-z]', ''))) = 0 OR LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("CCN"), '[^0-9A-Za-z]', ''))) > 6 OR LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("CCN"), '[^0-9A-Za-z]', '')), 6, '0') = REPEAT('0', 6) THEN NULL ELSE LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("CCN"), '[^0-9A-Za-z]', '')), 6, '0') END)), 16) as spine_entity_id  -- joins LIBRARY_META."CONNECT".ENTITY_GOLDEN.ENTITY_ID

    from source

)

select * from renamed
qualify row_number() over (partition by ccn order by _loaded_at desc) = 1
