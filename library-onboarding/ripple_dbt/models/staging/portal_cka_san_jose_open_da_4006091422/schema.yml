version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_SAN_JOSE_OPEN_DA_4006091422
        description: >
          Raw landing table for School District Secondary. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_san_jose_open_da_4006091422__places
    description: "GRAIN: one row per place (GEOM is unique). SPINE_ENTITY: place."
    columns:
      - name: shape_area
        data_tests:
          - unique
          - not_null
