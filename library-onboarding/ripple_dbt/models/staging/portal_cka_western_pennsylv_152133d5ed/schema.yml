version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_152133D5ED
        description: >
          Raw landing table for Public Water Supplier Service Areas. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_152133d5ed__facilities
    description: "GRAIN: one row per facility (PWSID is unique). SPINE_ENTITY: facility."
    columns:
      - name: pws_id
        data_tests:
          - unique
          - not_null
