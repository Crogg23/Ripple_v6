{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (USER_FORMATTED_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: USER_FORMATTED_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_ATLANTA_DATAATLA_DE1E9D4350') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "IN_SINGLELINE" as in_singleline,
        "USER_CUSTOMER_TYPE" as user_customer_type,
        "USER_ACCOUNT_TYPE" as user_account_type,
        "USER_FORMATTED_ID" as user_formatted_id,
        "USER_LIST_NAME" as user_list_name,
        "USER_LIST_ADDRESS" as user_list_address,
        "USER_COMMENCE" as user_commence,
        "USER_LICENSE_TYPE" as user_license_type,
        "USER_LICENSE_SUBTYPE" as user_license_subtype,
        "USER_NAICS_CODE" as user_naics_code,
        "FID_ATLMAINSTREETDISTRICTSROW_MLK" as fid_atlmainstreetdistrictsrow_mlk,
        "DISTRICT_1" as district_1,
        "COMMENCE_INT" as commence_int,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://dpcd-coaplangis.opendata.arcgis.com/datasets/c014edfd602948519c0dd5d2a492917a_46' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by user_formatted_id order by _loaded_at desc) = 1
