{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (USER_FORMATTED_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: USER_FORMATTED_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 3 of 15 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   fid_atlmainstreetdistrictsrow_mlk: integer
--   commence_int: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_ATLANTA_DATAATLA_DE1E9D4350') }}
    qualify row_number() over (partition by "USER_FORMATTED_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"IN_SINGLELINE"') }} as in_singleline,
        {{ null_junk('"USER_CUSTOMER_TYPE"') }} as user_customer_type,
        {{ null_junk('"USER_ACCOUNT_TYPE"') }} as user_account_type,
        {{ null_junk('"USER_FORMATTED_ID"') }} as user_formatted_id,
        {{ null_junk('"USER_LIST_NAME"') }} as user_list_name,
        {{ null_junk('"USER_LIST_ADDRESS"') }} as user_list_address,
        {{ null_junk('"USER_COMMENCE"') }} as user_commence,
        {{ null_junk('"USER_LICENSE_TYPE"') }} as user_license_type,
        {{ null_junk('"USER_LICENSE_SUBTYPE"') }} as user_license_subtype,
        {{ null_junk('"USER_NAICS_CODE"') }} as user_naics_code,
        {{ stg_int('"FID_ATLMAINSTREETDISTRICTSROW_MLK"') }} as fid_atlmainstreetdistrictsrow_mlk,
        {{ null_junk('"DISTRICT_1"') }} as district_1,
        {{ stg_int('"COMMENCE_INT"') }} as commence_int,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://dpcd-coaplangis.opendata.arcgis.com/datasets/c014edfd602948519c0dd5d2a492917a_46' as _source_url

    from source

)

select * from renamed
