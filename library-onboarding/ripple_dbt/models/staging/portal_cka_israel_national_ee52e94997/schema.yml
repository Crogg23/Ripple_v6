version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ISRAEL_NATIONAL_EE52E94997
        description: >
          Raw landing table for אירועי מחלת הכלבת בישראל. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_israel_national_ee52e94997__records
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ANIMAL_LAB_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: animal_lab_id
        data_tests:
          - not_null:
              config:
                severity: warn
