{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ANIMAL_LAB_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ANIMAL_LAB_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_EE52E94997') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "ANIMAL_LAB_ID" as animal_lab_id,
        "EVENT" as event,
        "DATE" as date,
        "LINKTOTEST" as linktotest,
        "OPENLINK" as openlink,
        "LINKTOMRE" as linktomre,
        "LINKMOREOPEN" as linkmoreopen,
        "YEAR" as year,
        "SPECIESNAMEENG" as speciesnameeng,
        "REGIONENG" as regioneng,
        "REGIONHEB" as regionheb,
        "SPECIESNAMEHEB" as speciesnameheb,
        "SPECIES" as species,
        "SETTLEMENTHEB" as settlementheb,
        "SETTLEMENTENG" as settlementeng,
        "LOCATIONNOTSETTLEMENTENG" as locationnotsettlementeng,
        "LOCATIONNOTSETTLEMENTHEB" as locationnotsettlementheb,
        "GLOBALID" as globalid,
        "X" as x,
        "Y" as y,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/rabies-occurrence' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by animal_lab_id order by _loaded_at desc) = 1
