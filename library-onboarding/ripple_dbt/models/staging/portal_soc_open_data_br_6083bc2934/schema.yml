version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_OPEN_DATA_BR_6083BC2934
        description: >
          Raw landing table for Property Information. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_open_data_br_6083bc2934__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (LOT_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: lot_id
        data_tests:
          - unique
          - not_null
