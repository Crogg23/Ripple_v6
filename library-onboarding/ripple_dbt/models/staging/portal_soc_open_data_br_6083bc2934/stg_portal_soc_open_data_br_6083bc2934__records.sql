{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (LOT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: LOT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 15 of 53 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   subd_id: integer
--   pla_dist: integer
--   sub_area: integer
--   lb_map: integer
--   ward_number: integer
--   tax_section: integer
--   census_tract: integer
--   block_group: integer
--   dotd_map: integer
--   council_dist_no: integer
--   area_meas_acres: float
--   computed_region_idcr_7zcb: integer
--   computed_region_8tu6_j4iw: integer
--   computed_region_hfgy_t898: integer
--   business_id: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (LOT_ID) is NOT unique within a load as landed at generation time -- it would collapse 20,942 of 199,821 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_OPEN_DATA_BR_6083BC2934') }}
    qualify row_number() over (partition by "ADDRESS_POINT_ID", "LOT_ID", "ADDRESS_ID", "ADDRESS_NO_COMPLETE", "ST_NAME", "ST_SUFFIX_TYPE", "FULL_ADDRESS", "CITY", "ZIP", "SUBD_ID", "SUBDIVISION", "PLA_DIST", "SUB_AREA", "LB_MAP", "LOT_NUM", "LOT_LOCATION", "WARD_NUMBER", "TAX_SECTION", "PLSS", "CENSUS_TRACT", "BLOCK_GROUP", "DOTD_MAP", "POLICE_DISTRICT", "FIRE_DISTRICT", "SCHOOL_DISTRICT", "VOTING_DISTRICT", "COUNCIL_DIST_NO", "ENTERPRISE_ZONE", "ECONOMIC_DEV_ZONE", "INDUSTRIAL_AREA", "EXISTING_LAND_USE", "AREA_MEAS_ACRES", "GEO_LOCATION", "COMPUTED_REGION_IDCR_7ZCB", "COMPUTED_REGION_8TU6_J4IW", "COMPUTED_REGION_HFGY_T898", "FUTURE_LAND_USE", "ZONING_TYPE", "UDOD_NAME", "DESIGN_LEVEL", "PROPERTY_INFO", "BLOCK_NUM", "ST_PREFIX_DIR", "BUSINESS_ID", "BUSINESS_NAME", "BUSINESS_NAICS_CODE", "REDEVELOPMENT_DISTRICT", "ST_PREFIX_TYPE", "HISTORIC_DISTRICT", "ST_SUFFIX_DIR", "HISTORIC_LANDMARK", "UDD_NAME", "ST_EXT" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ADDRESS_POINT_ID"') }} as address_point_id,
        {{ null_junk('"LOT_ID"') }} as lot_id,
        {{ null_junk('"ADDRESS_ID"') }} as address_id,
        {{ null_junk('"ADDRESS_NO_COMPLETE"') }} as address_no_complete,
        {{ null_junk('"ST_NAME"') }} as st_name,
        {{ null_junk('"ST_SUFFIX_TYPE"') }} as st_suffix_type,
        {{ null_junk('"FULL_ADDRESS"') }} as full_address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"ZIP"') }} as zip,
        {{ stg_int('"SUBD_ID"') }} as subd_id,
        {{ null_junk('"SUBDIVISION"') }} as subdivision,
        {{ stg_int('"PLA_DIST"') }} as pla_dist,
        {{ stg_int('"SUB_AREA"') }} as sub_area,
        {{ stg_int('"LB_MAP"') }} as lb_map,
        {{ null_junk('"LOT_NUM"') }} as lot_num,
        {{ null_junk('"LOT_LOCATION"') }} as lot_location,
        {{ stg_int('"WARD_NUMBER"') }} as ward_number,
        {{ stg_int('"TAX_SECTION"') }} as tax_section,
        {{ null_junk('"PLSS"') }} as plss,
        {{ stg_int('"CENSUS_TRACT"') }} as census_tract,
        {{ stg_int('"BLOCK_GROUP"') }} as block_group,
        {{ stg_int('"DOTD_MAP"') }} as dotd_map,
        {{ null_junk('"POLICE_DISTRICT"') }} as police_district,
        {{ null_junk('"FIRE_DISTRICT"') }} as fire_district,
        {{ null_junk('"SCHOOL_DISTRICT"') }} as school_district,
        {{ null_junk('"VOTING_DISTRICT"') }} as voting_district,
        {{ stg_int('"COUNCIL_DIST_NO"') }} as council_dist_no,
        {{ null_junk('"ENTERPRISE_ZONE"') }} as enterprise_zone,
        {{ null_junk('"ECONOMIC_DEV_ZONE"') }} as economic_dev_zone,
        {{ null_junk('"INDUSTRIAL_AREA"') }} as industrial_area,
        {{ null_junk('"EXISTING_LAND_USE"') }} as existing_land_use,
        {{ stg_float('"AREA_MEAS_ACRES"') }} as area_meas_acres,
        {{ null_junk('"GEO_LOCATION"') }} as geo_location,
        {{ stg_int('"COMPUTED_REGION_IDCR_7ZCB"') }} as computed_region_idcr_7zcb,
        {{ stg_int('"COMPUTED_REGION_8TU6_J4IW"') }} as computed_region_8tu6_j4iw,
        {{ stg_int('"COMPUTED_REGION_HFGY_T898"') }} as computed_region_hfgy_t898,
        {{ null_junk('"FUTURE_LAND_USE"') }} as future_land_use,
        {{ null_junk('"ZONING_TYPE"') }} as zoning_type,
        {{ null_junk('"UDOD_NAME"') }} as udod_name,
        {{ null_junk('"DESIGN_LEVEL"') }} as design_level,
        {{ null_junk('"PROPERTY_INFO"') }} as property_info,
        {{ null_junk('"BLOCK_NUM"') }} as block_num,
        {{ null_junk('"ST_PREFIX_DIR"') }} as st_prefix_dir,
        {{ stg_int('"BUSINESS_ID"') }} as business_id,
        {{ null_junk('"BUSINESS_NAME"') }} as business_name,
        {{ null_junk('"BUSINESS_NAICS_CODE"') }} as business_naics_code,
        {{ null_junk('"REDEVELOPMENT_DISTRICT"') }} as redevelopment_district,
        {{ null_junk('"ST_PREFIX_TYPE"') }} as st_prefix_type,
        {{ null_junk('"HISTORIC_DISTRICT"') }} as historic_district,
        {{ null_junk('"ST_SUFFIX_DIR"') }} as st_suffix_dir,
        {{ null_junk('"HISTORIC_LANDMARK"') }} as historic_landmark,
        {{ null_junk('"UDD_NAME"') }} as udd_name,
        {{ null_junk('"ST_EXT"') }} as st_ext,
        INGESTED_AT as _loaded_at,
        'https://data.brla.gov/d/re5c-hrw9' as _source_url

    from source

)

select * from renamed
