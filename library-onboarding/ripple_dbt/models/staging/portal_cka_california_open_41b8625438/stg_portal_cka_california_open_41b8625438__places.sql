{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LONGITUDE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 20 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_CALIFORNIA_OPEN_41B8625438') }}
    qualify row_number() over (partition by "LONGITUDE" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"STATION"') }} as station,
        {{ null_junk('"SITE_CODE"') }} as site_code,
        {{ null_junk('"STNAME"') }} as stname,
        {{ null_junk('"WELL_NAME"') }} as well_name,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"LLDATUM"') }} as lldatum,
        {{ null_junk('"POSACC"') }} as posacc,
        {{ null_junk('"ELEV"') }} as elev,
        {{ null_junk('"ELEVDATUM"') }} as elevdatum,
        {{ null_junk('"ELEVACC"') }} as elevacc,
        {{ null_junk('"COUNTY_NAME"') }} as county_name,
        {{ null_junk('"BASIN_CODE"') }} as basin_code,
        {{ null_junk('"BASIN_NAME"') }} as basin_name,
        {{ null_junk('"WELL_DEPTH"') }} as well_depth,
        {{ null_junk('"WELL_USE"') }} as well_use,
        {{ null_junk('"WELL_TYPE"') }} as well_type,
        {{ null_junk('"WCR_NO"') }} as wcr_no,
        {{ null_junk('"WDL"') }} as wdl,
        {{ null_junk('"COMMENT"') }} as comment,
        INGESTED_AT as _loaded_at,
        'https://data.ca.gov/dataset/wdl-ground-surface-displacement-land-subsidence-monitoring' as _source_url

    from source

)

select * from renamed
