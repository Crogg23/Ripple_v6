{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per asset (ASSET_ID unique)
-- SPINE_ENTITY: asset  (natural_key: ASSET_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'asset' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_B9904B63C2') }}

),

renamed as (

    select
        "NAME" as name,
        "ASSET_TYPE" as asset_type,
        "ASSET_ID" as asset_id,
        "TAGS" as tags,
        "LOCATION_ID" as location_id,
        "STREET_ADDRESS" as street_address,
        "UNIT" as unit,
        "UNIT_TYPE" as unit_type,
        "MUNICIPALITY" as municipality,
        "CITY" as city,
        "STATE" as state,
        "ZIP_CODE" as zip_code,
        "LATITUDE" as latitude,
        "LONGITUDE" as longitude,
        "PARCEL_ID" as parcel_id,
        "RESIDENCE" as residence,
        "IFFY_GEOCODING" as iffy_geocoding,
        "AVAILABLE_TRANSPORTATION" as available_transportation,
        "PARENT_LOCATION_ID" as parent_location_id,
        "PARENT_LOCATION" as parent_location,
        "URL" as url,
        "EMAIL" as email,
        "PHONE" as phone,
        "HOURS_OF_OPERATION" as hours_of_operation,
        "HOLIDAY_HOURS_OF_OPERATION" as holiday_hours_of_operation,
        "PERIODICITY" as periodicity,
        "CAPACITY" as capacity,
        "WIFI_NETWORK" as wifi_network,
        "INTERNET_ACCESS" as internet_access,
        "COMPUTERS_AVAILABLE" as computers_available,
        "ACCESSIBILITY" as accessibility,
        "OPEN_TO_PUBLIC" as open_to_public,
        "CHILD_FRIENDLY" as child_friendly,
        "SENSITIVE" as sensitive,
        "DO_NOT_DISPLAY" as do_not_display,
        "LOCALIZABILITY" as localizability,
        "SERVICES" as services,
        "HARD_TO_COUNT_POPULATION" as hard_to_count_population,
        "DATA_SOURCE_NAMES" as data_source_names,
        "DATA_SOURCE_URLS" as data_source_urls,
        "ORGANIZATION_NAME" as organization_name,
        "ORGANIZATION_PHONE" as organization_phone,
        "ORGANIZATION_EMAIL" as organization_email,
        "ETL_NOTES" as etl_notes,
        "GEOCODING_PROPERTIES" as geocoding_properties,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-assets' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by asset_id order by _loaded_at desc) = 1
