{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per asset (ASSET_ID unique)
-- SPINE_ENTITY: asset  (natural_key: ASSET_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 9 of 51 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   location_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   residence: boolean
--   phone: integer
--   capacity: integer
--   computers_available: boolean
--   accessibility: boolean
--   child_friendly: boolean
--   sensitive: boolean
--   do_not_display: boolean
-- spine_entity 'asset' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (ASSET_ID) is NOT unique within a load as landed at generation time -- it would collapse 175 of 32,177 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_B9904B63C2') }}
    qualify row_number() over (partition by "NAME", "ASSET_TYPE", "ASSET_ID", "TAGS", "LOCATION_ID", "STREET_ADDRESS", "UNIT", "UNIT_TYPE", "MUNICIPALITY", "CITY", "STATE", "ZIP_CODE", "LATITUDE", "LONGITUDE", "PARCEL_ID", "RESIDENCE", "IFFY_GEOCODING", "AVAILABLE_TRANSPORTATION", "PARENT_LOCATION_ID", "PARENT_LOCATION", "URL", "EMAIL", "PHONE", "HOURS_OF_OPERATION", "HOLIDAY_HOURS_OF_OPERATION", "PERIODICITY", "CAPACITY", "WIFI_NETWORK", "INTERNET_ACCESS", "COMPUTERS_AVAILABLE", "ACCESSIBILITY", "OPEN_TO_PUBLIC", "CHILD_FRIENDLY", "SENSITIVE", "DO_NOT_DISPLAY", "LOCALIZABILITY", "SERVICES", "HARD_TO_COUNT_POPULATION", "DATA_SOURCE_NAMES", "DATA_SOURCE_URLS", "ORGANIZATION_NAME", "ORGANIZATION_PHONE", "ORGANIZATION_EMAIL", "ETL_NOTES", "GEOCODING_PROPERTIES", "DATA_SOURCE_NAME", "DATA_SOURCE_URL", "PRIMARY_KEY_FROM_ROCKET", "COUNT", "CONCAT_DATA_SOURCE_NAME", "CONCAT_DATA_SOURCE_URL" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"ASSET_TYPE"') }} as asset_type,
        {{ null_junk('"ASSET_ID"') }} as asset_id,
        {{ null_junk('"TAGS"') }} as tags,
        {{ stg_id_text('"LOCATION_ID"') }} as location_id,
        {{ null_junk('"STREET_ADDRESS"') }} as street_address,
        {{ null_junk('"UNIT"') }} as unit,
        {{ null_junk('"UNIT_TYPE"') }} as unit_type,
        {{ null_junk('"MUNICIPALITY"') }} as municipality,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"PARCEL_ID"') }} as parcel_id,
        {{ stg_bool('"RESIDENCE"') }} as residence,
        {{ null_junk('"IFFY_GEOCODING"') }} as iffy_geocoding,
        {{ null_junk('"AVAILABLE_TRANSPORTATION"') }} as available_transportation,
        {{ null_junk('"PARENT_LOCATION_ID"') }} as parent_location_id,
        {{ null_junk('"PARENT_LOCATION"') }} as parent_location,
        {{ null_junk('"URL"') }} as url,
        {{ null_junk('"EMAIL"') }} as email,
        {{ stg_int('"PHONE"') }} as phone,
        {{ null_junk('"HOURS_OF_OPERATION"') }} as hours_of_operation,
        {{ null_junk('"HOLIDAY_HOURS_OF_OPERATION"') }} as holiday_hours_of_operation,
        {{ null_junk('"PERIODICITY"') }} as periodicity,
        {{ stg_int('"CAPACITY"') }} as capacity,
        {{ null_junk('"WIFI_NETWORK"') }} as wifi_network,
        {{ null_junk('"INTERNET_ACCESS"') }} as internet_access,
        {{ stg_bool('"COMPUTERS_AVAILABLE"') }} as computers_available,
        {{ stg_bool('"ACCESSIBILITY"') }} as accessibility,
        {{ null_junk('"OPEN_TO_PUBLIC"') }} as open_to_public,
        {{ stg_bool('"CHILD_FRIENDLY"') }} as child_friendly,
        {{ stg_bool('"SENSITIVE"') }} as sensitive,
        {{ stg_bool('"DO_NOT_DISPLAY"') }} as do_not_display,
        {{ null_junk('"LOCALIZABILITY"') }} as localizability,
        {{ null_junk('"SERVICES"') }} as services,
        {{ null_junk('"HARD_TO_COUNT_POPULATION"') }} as hard_to_count_population,
        {{ null_junk('"DATA_SOURCE_NAMES"') }} as data_source_names,
        {{ null_junk('"DATA_SOURCE_URLS"') }} as data_source_urls,
        {{ null_junk('"ORGANIZATION_NAME"') }} as organization_name,
        {{ null_junk('"ORGANIZATION_PHONE"') }} as organization_phone,
        {{ null_junk('"ORGANIZATION_EMAIL"') }} as organization_email,
        {{ null_junk('"ETL_NOTES"') }} as etl_notes,
        {{ null_junk('"GEOCODING_PROPERTIES"') }} as geocoding_properties,
        {{ null_junk('"DATA_SOURCE_NAME"') }} as data_source_name,
        {{ null_junk('"DATA_SOURCE_URL"') }} as data_source_url,
        {{ null_junk('"PRIMARY_KEY_FROM_ROCKET"') }} as primary_key_from_rocket,
        {{ null_junk('"COUNT"') }} as count,
        {{ null_junk('"CONCAT_DATA_SOURCE_NAME"') }} as concat_data_source_name,
        {{ null_junk('"CONCAT_DATA_SOURCE_URL"') }} as concat_data_source_url,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-assets' as _source_url

    from source

)

select * from renamed
