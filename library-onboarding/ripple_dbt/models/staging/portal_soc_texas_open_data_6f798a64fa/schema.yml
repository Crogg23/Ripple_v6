version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_TEXAS_OPEN_DATA_6F798A64FA
        description: >
          Raw landing table for Active insurance company appointments for agencies and businesses. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_texas_open_data_6f798a64fa__organizations
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per organization (EIN+ACTIVE_DATE+APPOINTMENT_TYPE+NAIC_ID unique). SPINE_ENTITY: organization."
    columns:
      - name: ein
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: active_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: appointment_type
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: naic_id
        data_tests:
          - not_null:
              config:
                severity: warn
