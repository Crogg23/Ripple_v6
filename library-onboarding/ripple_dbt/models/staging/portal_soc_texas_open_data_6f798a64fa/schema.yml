version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_TEXAS_OPEN_DATA_6F798A64FA
        description: >
          Raw landing table for Active insurance company appointments for agencies and businesses. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_texas_open_data_6f798a64fa__organizations
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (EIN+ACTIVE_DATE+APPOINTMENT_TYPE+NAIC_ID unique). SPINE_ENTITY: organization."
    columns:
      - name: ein
        data_tests:
          - not_null
      - name: active_date
        data_tests:
          - not_null
      - name: appointment_type
        data_tests:
          - not_null
      - name: naic_id
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - ein
              - active_date
              - appointment_type
              - naic_id