version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_TUCSON_OPEN_DATA_E0EDEA39BE
        description: >
          Raw landing table for Stores Proposal2. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_tucson_open_data_e0edea39be__places
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+INDUSTRY_DESC unique). SPINE_ENTITY: place."
    columns:
      - name: zip
        data_tests:
          - not_null
      - name: industry_desc
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - zip
            - industry_desc
