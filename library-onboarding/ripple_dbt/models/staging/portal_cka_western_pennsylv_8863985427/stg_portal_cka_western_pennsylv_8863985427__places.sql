{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LONGITUDE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 7 of 22 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   validated: boolean
--   homemade_pierogies: boolean
--   take_out: boolean
--   alcohol: boolean
--   lunch: boolean
--   handicap: boolean
--   publish: boolean
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_8863985427') }}
    qualify row_number() over (partition by "LONGITUDE" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_bool('"VALIDATED"') }} as validated,
        {{ null_junk('"VENUE_NAME"') }} as venue_name,
        {{ null_junk('"VENUE_TYPE"') }} as venue_type,
        {{ null_junk('"VENUE_ADDRESS"') }} as venue_address,
        {{ null_junk('"WEBSITE"') }} as website,
        {{ null_junk('"EVENTS"') }} as events,
        {{ null_junk('"ETC"') }} as etc,
        {{ null_junk('"MENU_URL"') }} as menu_url,
        {{ null_junk('"MENU_TEXT"') }} as menu_text,
        {{ null_junk('"VENUE_NOTES"') }} as venue_notes,
        {{ null_junk('"PHONE"') }} as phone,
        {{ null_junk('"EMAIL"') }} as email,
        {{ stg_bool('"HOMEMADE_PIEROGIES"') }} as homemade_pierogies,
        {{ stg_bool('"TAKE_OUT"') }} as take_out,
        {{ stg_bool('"ALCOHOL"') }} as alcohol,
        {{ stg_bool('"LUNCH"') }} as lunch,
        {{ stg_bool('"HANDICAP"') }} as handicap,
        {{ stg_bool('"PUBLISH"') }} as publish,
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"EVENTS_JSON"') }} as events_json,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/pittsburgh-fish-fry-map' as _source_url

    from source

)

select * from renamed
