version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_UTAH_OPEN_DATA_P_5EF68422FF
        description: >
          Raw landing table for All State of Utah Federal Prime Contracts 2008 - 2018. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_utah_open_data_p_5ef68422ff__organizations
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (DUNS+PERIOD_OF_PERFORMANCE_START+PERIOD_OF_PERFORMANCE_CURRENT+PERIOD_OF_PERFORMANCE+AWARD_ID_PIID+PARENT_AWARD_AGENCY_ID+PARENT_AWARD_ID unique). SPINE_ENTITY: organization."
    columns:
      - name: recipient_duns
        data_tests:
          - not_null
      - name: period_of_performance_start
        data_tests:
          - not_null
      - name: period_of_performance_current
        data_tests:
          - not_null
      - name: period_of_performance
        data_tests:
          - not_null
      - name: award_id_piid
        data_tests:
          - not_null
      - name: parent_award_agency_id
        data_tests:
          - not_null
      - name: parent_award_id
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - recipient_duns
              - period_of_performance_start
              - period_of_performance_current
              - period_of_performance
              - award_id_piid
              - parent_award_agency_id
              - parent_award_id