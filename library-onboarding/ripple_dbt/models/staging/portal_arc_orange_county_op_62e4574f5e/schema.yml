version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_ORANGE_COUNTY_OP_62E4574F5E
        description: >
          Raw landing table for SDR Industrial Facilities. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_orange_county_op_62e4574f5e__facilities
    description: "GRAIN: one row per facility (FACILITY_ID unique). SPINE_ENTITY: facility."
    columns:
      - name: facility_id
        data_tests:
          - unique
          - not_null
