{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CCN+DATE unique)
-- SPINE_ENTITY: facility  (natural_key: CCN_REV, DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'facility' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_OPEN_DATA_DC_D8E55D5B7F') }}

),

renamed as (

    select
        "FID" as fid,
        "CCN_REV" as ccn_rev,
        "DATE" as date,
        "FATALITIES" as fatalities,
        "Y" as y,
        "X" as x,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://opendata.dc.gov/datasets/6dbd23cd2c174bb8a6bc94d48e51e04b_0' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by ccn_rev, date order by _loaded_at desc) = 1
