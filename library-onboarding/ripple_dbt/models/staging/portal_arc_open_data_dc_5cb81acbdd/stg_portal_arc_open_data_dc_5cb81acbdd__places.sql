{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (SAFEGRAPH_PLACE_ID unique)
-- SPINE_ENTITY: place  (natural_key: SAFEGRAPH_PLACE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 40 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_OPEN_DATA_DC_5CB81ACBDD') }}
    qualify row_number() over (partition by "SAFEGRAPH_PLACE_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"OBJECTID"') }} as objectid,
        {{ null_junk('"JOIN_COUNT"') }} as join_count,
        {{ null_junk('"TARGET_FID"') }} as target_fid,
        {{ null_junk('"WARD"') }} as ward,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"SAFEGRAPH_PLACE_ID"') }} as safegraph_place_id,
        {{ null_junk('"PARENT_SAFEGRAPH_PLACE_ID"') }} as parent_safegraph_place_id,
        {{ null_junk('"LOCATION_NAME"') }} as location_name,
        {{ null_junk('"BRANDS"') }} as brands,
        {{ null_junk('"TOP_CATEGORY"') }} as top_category,
        {{ null_junk('"SUB_CATEGORY"') }} as sub_category,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"STREET_ADDRESS"') }} as street_address,
        {{ null_junk('"PRIMARY_NUMBER"') }} as primary_number,
        {{ null_junk('"STREET_PREDIRECTION"') }} as street_predirection,
        {{ null_junk('"STREET_NAME"') }} as street_name,
        {{ null_junk('"STREET_SUFFIX"') }} as street_suffix,
        {{ null_junk('"STREET_POSTDIRECTION"') }} as street_postdirection,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"REGION"') }} as region,
        {{ null_junk('"POSTAL_CODE"') }} as postal_code,
        {{ null_junk('"ISO_COUNTRY_CODE"') }} as iso_country_code,
        {{ null_junk('"PHONE_NUMBER"') }} as phone_number,
        {{ null_junk('"CATEGORY_TAGS"') }} as category_tags,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"DATA_VERSION"') }} as data_version,
        {{ null_junk('"RAW_VISIT_COUNTS"') }} as raw_visit_counts,
        {{ null_junk('"RAW_VISITOR_COUNTS"') }} as raw_visitor_counts,
        {{ null_junk('"VISITS_BY_DAY"') }} as visits_by_day,
        {{ null_junk('"VISITS4_12_2020"') }} as visits4_12_2020,
        {{ null_junk('"VISITS4_13_2020"') }} as visits4_13_2020,
        {{ null_junk('"VISITS4_14_2020"') }} as visits4_14_2020,
        {{ null_junk('"VISITS4_15_2020"') }} as visits4_15_2020,
        {{ null_junk('"VISITS4_16_2020"') }} as visits4_16_2020,
        {{ null_junk('"VISITS4_17_2020"') }} as visits4_17_2020,
        {{ null_junk('"VISITS4_18_2020"') }} as visits4_18_2020,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://opendata.dc.gov/datasets/e7de8e9c16a446ad800a6cdffe9855d5_0' as _source_url

    from source

)

select * from renamed
