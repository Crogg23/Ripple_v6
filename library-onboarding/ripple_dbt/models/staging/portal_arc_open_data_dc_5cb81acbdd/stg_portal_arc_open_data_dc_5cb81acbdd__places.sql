{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (SAFEGRAPH_PLACE_ID unique)
-- SPINE_ENTITY: place  (natural_key: SAFEGRAPH_PLACE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_OPEN_DATA_DC_5CB81ACBDD') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "JOIN_COUNT" as join_count,
        "TARGET_FID" as target_fid,
        "WARD" as ward,
        "NAME" as name,
        "SAFEGRAPH_PLACE_ID" as safegraph_place_id,
        "PARENT_SAFEGRAPH_PLACE_ID" as parent_safegraph_place_id,
        "LOCATION_NAME" as location_name,
        "BRANDS" as brands,
        "TOP_CATEGORY" as top_category,
        "SUB_CATEGORY" as sub_category,
        "NAICS_CODE" as naics_code,
        "STREET_ADDRESS" as street_address,
        "PRIMARY_NUMBER" as primary_number,
        "STREET_PREDIRECTION" as street_predirection,
        "STREET_NAME" as street_name,
        "STREET_SUFFIX" as street_suffix,
        "STREET_POSTDIRECTION" as street_postdirection,
        "CITY" as city,
        "REGION" as region,
        "POSTAL_CODE" as postal_code,
        "ISO_COUNTRY_CODE" as iso_country_code,
        "PHONE_NUMBER" as phone_number,
        "CATEGORY_TAGS" as category_tags,
        "LONGITUDE" as longitude,
        "LATITUDE" as latitude,
        "DATA_VERSION" as data_version,
        "RAW_VISIT_COUNTS" as raw_visit_counts,
        "RAW_VISITOR_COUNTS" as raw_visitor_counts,
        "VISITS_BY_DAY" as visits_by_day,
        "VISITS4_12_2020" as visits4_12_2020,
        "VISITS4_13_2020" as visits4_13_2020,
        "VISITS4_14_2020" as visits4_14_2020,
        "VISITS4_15_2020" as visits4_15_2020,
        "VISITS4_16_2020" as visits4_16_2020,
        "VISITS4_17_2020" as visits4_17_2020,
        "VISITS4_18_2020" as visits4_18_2020,
        "SHAPE__AREA" as shape__area,
        "SHAPE__LENGTH" as shape__length,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://opendata.dc.gov/datasets/e7de8e9c16a446ad800a6cdffe9855d5_0' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by safegraph_place_id order by _loaded_at desc) = 1
