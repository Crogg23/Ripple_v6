{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GEO_ID+YEAR unique)
-- SPINE_ENTITY: (not determined)  (natural_key: GEO_ID, YEAR)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 4 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_HOUSTON_OPEN_DAT_CFDCDF13FD') }}
    qualify row_number() over (partition by "GEO_ID", "YEAR" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"YEAR"') }} as year,
        {{ null_junk('"GEO_ID"') }} as geo_id,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"B25064_001E"') }} as b25064_001e,
        INGESTED_AT as _loaded_at,
        'https://data.houstontx.gov/dataset/city-of-houston-median-gross-rent' as _source_url

    from source

)

select * from renamed
