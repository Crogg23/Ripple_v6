{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (MISGERET_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: MISGERET_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 12 of 29 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   organizations_busines_num: integer
--   registered_business_id: integer
--   authority_id: integer
--   actual_capacity: integer
--   from_age: integer
--   to_age: integer
--   startd: timestamp
--   telephone: integer
--   fax: integer
--   mailing_box_id: integer
--   gisx: integer
--   gisy: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_B37BA647D0') }}
    qualify row_number() over (partition by "MISGERET_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"MISGERET_ID"') }} as misgeret_id,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"TYPE_DESCR"') }} as type_descr,
        {{ null_junk('"HEAD_DEPARTMENT"') }} as head_department,
        {{ null_junk('"SECOND_CLASSIFIC"') }} as second_classific,
        {{ null_junk('"OWNER_CODE_DESCR"') }} as owner_code_descr,
        {{ null_junk('"C_ORGANIZATION"') }} as c_organization,
        {{ stg_int('"ORGANIZATIONS_BUSINES_NUM"') }} as organizations_busines_num,
        {{ stg_int('"REGISTERED_BUSINESS_ID"') }} as registered_business_id,
        {{ null_junk('"AUTHORITYS"') }} as authoritys,
        {{ stg_int('"AUTHORITY_ID"') }} as authority_id,
        {{ null_junk('"REGION_DESCR"') }} as region_descr,
        {{ stg_int('"ACTUAL_CAPACITY"') }} as actual_capacity,
        {{ null_junk('"GENDER_DESCR"') }} as gender_descr,
        {{ stg_int('"FROM_AGE"') }} as from_age,
        {{ stg_int('"TO_AGE"') }} as to_age,
        {{ null_junk('"STATUS_DES"') }} as status_des,
        {{ stg_ts('"STARTD"') }} as startd,
        {{ null_junk('"MANEGER_NAME"') }} as maneger_name,
        {{ null_junk('"RELIGION"') }} as religion,
        {{ null_junk('"EDUCATE"') }} as educate,
        {{ null_junk('"CITY_NAME"') }} as city_name,
        {{ null_junk('"ADREES"') }} as adrees,
        {{ stg_int('"TELEPHONE"') }} as telephone,
        {{ stg_int('"FAX"') }} as fax,
        {{ stg_int('"MAILING_BOX_ID"') }} as mailing_box_id,
        {{ stg_int('"GISX"') }} as gisx,
        {{ stg_int('"GISY"') }} as gisy,
        {{ null_junk('"TARGET_POPULATION_DESCR"') }} as target_population_descr,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/welfare-frames' as _source_url

    from source

)

select * from renamed
