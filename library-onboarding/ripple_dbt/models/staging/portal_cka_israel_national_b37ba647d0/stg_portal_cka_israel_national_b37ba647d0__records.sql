{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (MISGERET_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: MISGERET_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_B37BA647D0') }}

),

renamed as (

    select
        "MISGERET_ID" as misgeret_id,
        "NAME" as name,
        "TYPE_DESCR" as type_descr,
        "HEAD_DEPARTMENT" as head_department,
        "SECOND_CLASSIFIC" as second_classific,
        "OWNER_CODE_DESCR" as owner_code_descr,
        "C_ORGANIZATION" as c_organization,
        "ORGANIZATIONS_BUSINES_NUM" as organizations_busines_num,
        "REGISTERED_BUSINESS_ID" as registered_business_id,
        "AUTHORITYS" as authoritys,
        "AUTHORITY_ID" as authority_id,
        "REGION_DESCR" as region_descr,
        "ACTUAL_CAPACITY" as actual_capacity,
        "GENDER_DESCR" as gender_descr,
        "FROM_AGE" as from_age,
        "TO_AGE" as to_age,
        "STATUS_DES" as status_des,
        "STARTD" as startd,
        "MANEGER_NAME" as maneger_name,
        "RELIGION" as religion,
        "EDUCATE" as educate,
        "CITY_NAME" as city_name,
        "ADREES" as adrees,
        "TELEPHONE" as telephone,
        "FAX" as fax,
        "MAILING_BOX_ID" as mailing_box_id,
        "GISX" as gisx,
        "GISY" as gisy,
        "TARGET_POPULATION_DESCR" as target_population_descr,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/welfare-frames' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by misgeret_id order by _loaded_at desc) = 1
