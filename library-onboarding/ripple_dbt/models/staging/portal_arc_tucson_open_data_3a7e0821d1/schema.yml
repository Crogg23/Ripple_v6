version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_TUCSON_OPEN_DATA_3A7E0821D1
        description: >
          Raw landing table for Residential, by size. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_tucson_open_data_3a7e0821d1__cases
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per case (DOCKET+LAST_EDITED_DATE unique). SPINE_ENTITY: case."
    columns:
      - name: docket
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: last_edited_date
        data_tests:
          - not_null:
              config:
                severity: warn
