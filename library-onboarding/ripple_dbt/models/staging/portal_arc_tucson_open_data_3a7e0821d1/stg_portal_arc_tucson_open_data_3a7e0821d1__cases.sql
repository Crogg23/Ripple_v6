{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per case (DOCKET+LAST_EDITED_DATE unique)
-- SPINE_ENTITY: case  (natural_key: DOCKET, LAST_EDITED_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 17 of 77 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   gisarea: float
--   gisacres: float
--   x_hpgn: float
--   y_hpgn: float
--   seq_num_d: integer
--   parcel_use: integer
--   landmeas: float
--   lastchange: integer
--   page: integer
--   recorddate: date, YYYYMMDD
--   taxyr: integer
--   limnet: integer
--   fcv: integer
--   created_date_cot: integer
--   last_edited_date_cot: integer
--   parcelsize: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
-- spine_entity 'case' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (DOCKET, LAST_EDITED_DATE) is NOT unique within a load as landed at generation time -- it would collapse 1 of 61 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_TUCSON_OPEN_DATA_3A7E0821D1') }}
    qualify row_number() over (partition by "OBJECTID", "PARCEL", "GISAREA", "GISACRES", "X_HPGN", "Y_HPGN", "LON", "LAT", "LOT_R", "LINK", "TRS_OL", "MP_OL", "SEQ_NUM_S", "JURIS_OL", "CURZONE_OL", "ADDRESS_OL", "SEQ_NUM_D", "PARCEL_USE", "LANDMEAS", "LANDUNIT", "LASTCHANGE", "LEGAL1", "LEGAL2", "LEGAL3", "LEGAL4", "LEGAL5", "LOT", "MAIL1", "MAIL2", "MAIL3", "MAIL4", "MAIL5", "MP", "PAGE", "RECORDDATE", "DOCKET", "RECTRACT", "SECTMODIF", "TAXAREA", "ZIP", "ZIP4", "TAXYR", "LIMNET", "FCV", "SHAPE_LENG", "ADDRESSEE", "ADDRESS", "CITY", "STATE_PROVINCE", "COUNTRY", "POSTAL_CODE", "SITE_ADDRESS", "SITE_ZIP", "SITE_ZIPCITY", "USE_DESC", "SPT_DESC", "PPT_DESC", "DATASOURCE", "URL", "URL2", "ADR_STATUS", "LEGAL_DESC", "OWN", "VAN", "YEARBUILT", "LAST_EDITED_USER", "LAST_EDITED_DATE", "GLOBALID", "CREATED_USER_COT", "CREATED_DATE_COT", "LAST_EDITED_USER_COT", "LAST_EDITED_DATE_COT", "PARCELSIZE", "PARCELSIZECLASS", "SHAPE__AREA", "SHAPE__LENGTH", "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"PARCEL"') }} as parcel,
        {{ stg_float('"GISAREA"') }} as gisarea,
        {{ stg_float('"GISACRES"') }} as gisacres,
        {{ stg_float('"X_HPGN"') }} as x_hpgn,
        {{ stg_float('"Y_HPGN"') }} as y_hpgn,
        {{ null_junk('"LON"') }} as lon,
        {{ null_junk('"LAT"') }} as lat,
        {{ null_junk('"LOT_R"') }} as lot_r,
        {{ null_junk('"LINK"') }} as link,
        {{ null_junk('"TRS_OL"') }} as trs_ol,
        {{ null_junk('"MP_OL"') }} as mp_ol,
        {{ null_junk('"SEQ_NUM_S"') }} as seq_num_s,
        {{ null_junk('"JURIS_OL"') }} as juris_ol,
        {{ null_junk('"CURZONE_OL"') }} as curzone_ol,
        {{ null_junk('"ADDRESS_OL"') }} as address_ol,
        {{ stg_int('"SEQ_NUM_D"') }} as seq_num_d,
        {{ stg_int('"PARCEL_USE"') }} as parcel_use,
        {{ stg_float('"LANDMEAS"') }} as landmeas,
        {{ null_junk('"LANDUNIT"') }} as landunit,
        {{ stg_int('"LASTCHANGE"') }} as lastchange,
        {{ null_junk('"LEGAL1"') }} as legal1,
        {{ null_junk('"LEGAL2"') }} as legal2,
        {{ null_junk('"LEGAL3"') }} as legal3,
        {{ null_junk('"LEGAL4"') }} as legal4,
        {{ null_junk('"LEGAL5"') }} as legal5,
        {{ null_junk('"LOT"') }} as lot,
        {{ null_junk('"MAIL1"') }} as mail1,
        {{ null_junk('"MAIL2"') }} as mail2,
        {{ null_junk('"MAIL3"') }} as mail3,
        {{ null_junk('"MAIL4"') }} as mail4,
        {{ null_junk('"MAIL5"') }} as mail5,
        {{ null_junk('"MP"') }} as mp,
        {{ stg_int('"PAGE"') }} as page,
        {{ stg_date('"RECORDDATE"', 'YYYYMMDD') }} as recorddate,
        {{ null_junk('"DOCKET"') }} as docket,
        {{ null_junk('"RECTRACT"') }} as rectract,
        {{ null_junk('"SECTMODIF"') }} as sectmodif,
        {{ null_junk('"TAXAREA"') }} as taxarea,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"ZIP4"') }} as zip4,
        {{ stg_int('"TAXYR"') }} as taxyr,
        {{ stg_int('"LIMNET"') }} as limnet,
        {{ stg_int('"FCV"') }} as fcv,
        {{ null_junk('"SHAPE_LENG"') }} as shape_leng,
        {{ null_junk('"ADDRESSEE"') }} as addressee,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE_PROVINCE"') }} as state_province,
        {{ null_junk('"COUNTRY"') }} as country,
        {{ null_junk('"POSTAL_CODE"') }} as postal_code,
        {{ null_junk('"SITE_ADDRESS"') }} as site_address,
        {{ null_junk('"SITE_ZIP"') }} as site_zip,
        {{ null_junk('"SITE_ZIPCITY"') }} as site_zipcity,
        {{ null_junk('"USE_DESC"') }} as use_desc,
        {{ null_junk('"SPT_DESC"') }} as spt_desc,
        {{ null_junk('"PPT_DESC"') }} as ppt_desc,
        {{ null_junk('"DATASOURCE"') }} as datasource,
        {{ null_junk('"URL"') }} as url,
        {{ null_junk('"URL2"') }} as url2,
        {{ null_junk('"ADR_STATUS"') }} as adr_status,
        {{ null_junk('"LEGAL_DESC"') }} as legal_desc,
        {{ null_junk('"OWN"') }} as own,
        {{ null_junk('"VAN"') }} as van,
        {{ null_junk('"YEARBUILT"') }} as yearbuilt,
        {{ null_junk('"LAST_EDITED_USER"') }} as last_edited_user,
        {{ null_junk('"LAST_EDITED_DATE"') }} as last_edited_date,
        {{ null_junk('"GLOBALID"') }} as globalid,
        {{ null_junk('"CREATED_USER_COT"') }} as created_user_cot,
        {{ stg_int('"CREATED_DATE_COT"') }} as created_date_cot,
        {{ null_junk('"LAST_EDITED_USER_COT"') }} as last_edited_user_cot,
        {{ stg_int('"LAST_EDITED_DATE_COT"') }} as last_edited_date_cot,
        {{ stg_id_text('"PARCELSIZE"') }} as parcelsize,
        {{ null_junk('"PARCELSIZECLASS"') }} as parcelsizeclass,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://gisdata.tucsonaz.gov/datasets/9c08efa71c4f430d8fc17f78c5387855_3' as _source_url

    from source

)

select * from renamed
