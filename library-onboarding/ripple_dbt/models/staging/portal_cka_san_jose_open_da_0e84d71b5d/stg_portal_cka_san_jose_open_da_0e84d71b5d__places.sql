{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_AREA)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 7 of 24 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   facilityid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   intid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   projectobjectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   rsnnumber: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   mergeno: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   buildingheight: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_0E84D71B5D') }}
    qualify row_number() over (partition by "SHAPE_AREA" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_id_text('"FACILITYID"') }} as facilityid,
        {{ stg_id_text('"INTID"') }} as intid,
        {{ null_junk('"CREATIONDATE"') }} as creationdate,
        {{ null_junk('"CREATOR"') }} as creator,
        {{ null_junk('"PROJECTNAME"') }} as projectname,
        {{ stg_id_text('"PROJECTOBJECTID"') }} as projectobjectid,
        {{ null_junk('"PROJECTTYPE"') }} as projecttype,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"PROJECTSTATUS"') }} as projectstatus,
        {{ null_junk('"LASTUPDATE"') }} as lastupdate,
        {{ null_junk('"NOTES"') }} as notes,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        {{ null_junk('"SHAPE_AREA"') }} as shape_area,
        {{ null_junk('"PARCELSNUM"') }} as parcelsnum,
        {{ null_junk('"FILENUM"') }} as filenum,
        {{ null_junk('"PROJECTNOTES"') }} as projectnotes,
        {{ null_junk('"CONTACTNAME"') }} as contactname,
        {{ null_junk('"CONTACTEMAIL"') }} as contactemail,
        {{ null_junk('"DEVELOPERNAME"') }} as developername,
        {{ stg_id_text('"RSNNUMBER"') }} as rsnnumber,
        {{ stg_id_text('"MERGENO"') }} as mergeno,
        {{ null_junk('"CONTACT"') }} as contact,
        {{ stg_int('"BUILDINGHEIGHT"') }} as buildingheight,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/major-private-development-footprints' as _source_url

    from source

)

select * from renamed
