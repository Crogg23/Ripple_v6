version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_FEC_CAND_CMTE_LINKAGE
        description: >
          Raw landing table for FEC Candidate-Committee Linkage (bulk, multi-cycle). Grain not yet determined.

models:
  - name: stg_fed_fec_cand_cmte_linkage__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
