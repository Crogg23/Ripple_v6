{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_CHICAGO_DATA_POR_26DE83BAF4') }}

),

renamed as (

    select
        "ID" as id,
        "DOCKET_NUMBER" as docket_number,
        "NOV_NUMBER" as nov_number,
        "ADDRESS" as address,
        "STREET_NUMBER" as street_number,
        "STREET_DIRECTION" as street_direction,
        "STREET_NAME" as street_name,
        "STREET_TYPE" as street_type,
        "WARD" as ward,
        "ISSUING_DEPARTMENT" as issuing_department,
        "HEARING_DATE" as hearing_date,
        "CASE_DISPOSITION" as case_disposition,
        "IMPOSED_FINE" as imposed_fine,
        "ADMIN_COSTS" as admin_costs,
        "LAST_MODIFIED_DATE" as last_modified_date,
        "VIOLATION_DATE" as violation_date,
        "VIOLATION_CODE" as violation_code,
        "VIOLATION_DESCRIPTION" as violation_description,
        "RESPONDENTS" as respondents,
        "LATITUDE" as latitude,
        "LONGITUDE" as longitude,
        "LOCATION" as location,
        "COMPUTED_REGION_AWAF_S7UX" as computed_region_awaf_s7ux,
        "COMPUTED_REGION_6MKV_F3DW" as computed_region_6mkv_f3dw,
        "COMPUTED_REGION_VRXF_VC4K" as computed_region_vrxf_vc4k,
        "COMPUTED_REGION_BDYS_3D7I" as computed_region_bdys_3d7i,
        "COMPUTED_REGION_43WA_7QMU" as computed_region_43wa_7qmu,
        INGESTED_AT as _loaded_at,
        'https://data.cityofchicago.org/d/6br9-quuz' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by id order by _loaded_at desc) = 1
