{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (FIPS is unique)
-- SPINE_ENTITY: place  (natural_key: COUNTY_FIPS)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 10 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_FRA_SAFETY') }}
    qualify row_number() over (partition by "COUNTY_FIPS" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"REPORT_ID"') }} as report_id,
        {{ null_junk('"RAILROAD_NAME"') }} as railroad_name,
        {{ null_junk('"EVENT_DATE"') }} as event_date,
        {{ null_junk('"STATE_FIPS"') }} as state_fips,
        {{ null_junk('"COUNTY_FIPS"') }} as county_fips,
        {{ null_junk('"EVENT_TYPE"') }} as event_type,
        {{ null_junk('"CASUALTIES"') }} as casualties,
        {{ null_junk('"EQUIPMENT_TYPE"') }} as equipment_type,
        {{ null_junk('"CAUSE_CODE"') }} as cause_code,
        {{ null_junk('"NARRATIVE"') }} as narrative,
        _INGESTED_AT as _loaded_at,
        'https://safetydata.fra.dot.gov/MasterWebService/FRASafetyDataAPIs.aspx' as _source_url

    from source

)

select * from renamed
