version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_B6C7223760
        description: >
          Raw landing table for Open Space. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_b6c7223760__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (OS_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: os_id
        data_tests:
          - unique
          - not_null
