{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (OS_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: OS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 3 of 27 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   typecode: integer
--   acres: float
--   yearacquired: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_B6C7223760') }}
    qualify row_number() over (partition by "OS_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SITE_NAME"') }} as site_name,
        {{ null_junk('"OWNERSHIP"') }} as ownership,
        {{ null_junk('"PROTECTION"') }} as protection,
        {{ stg_int('"TYPECODE"') }} as typecode,
        {{ null_junk('"DISTRICT"') }} as district,
        {{ stg_float('"ACRES"') }} as acres,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"ZONAGG"') }} as zonagg,
        {{ null_junk('"TYPELONG"') }} as typelong,
        {{ null_junk('"OS_OWN_JUR"') }} as os_own_jur,
        {{ null_junk('"OS_MNGMNT"') }} as os_mngmnt,
        {{ null_junk('"POS"') }} as pos,
        {{ null_junk('"PA"') }} as pa,
        {{ null_junk('"ALT_NAME"') }} as alt_name,
        {{ null_junk('"AGNCYJURIS"') }} as agncyjuris,
        {{ null_junk('"PARK_ID"') }} as park_id,
        {{ null_junk('"REGION"') }} as region,
        {{ null_junk('"OS_ID"') }} as os_id,
        {{ null_junk('"F_100FTRULE"') }} as f_100ftrule,
        {{ null_junk('"ZIPCODE"') }} as zipcode,
        {{ null_junk('"PARCELNUMBER"') }} as parcelnumber,
        {{ stg_int('"YEARACQUIRED"') }} as yearacquired,
        {{ null_junk('"STAREA"') }} as starea,
        {{ null_junk('"STLENGTH"') }} as stlength,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        {{ null_junk('"SHAPE_AREA"') }} as shape_area,
        {{ null_junk('"SHAPE_WKT"') }} as shape_wkt,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/open-space' as _source_url

    from source

)

select * from renamed
