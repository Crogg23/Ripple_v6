{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (OS_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: OS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_B6C7223760') }}

),

renamed as (

    select
        "SITE_NAME" as site_name,
        "OWNERSHIP" as ownership,
        "PROTECTION" as protection,
        "TYPECODE" as typecode,
        "DISTRICT" as district,
        "ACRES" as acres,
        "ADDRESS" as address,
        "ZONAGG" as zonagg,
        "TYPELONG" as typelong,
        "OS_OWN_JUR" as os_own_jur,
        "OS_MNGMNT" as os_mngmnt,
        "POS" as pos,
        "PA" as pa,
        "ALT_NAME" as alt_name,
        "AGNCYJURIS" as agncyjuris,
        "PARK_ID" as park_id,
        "REGION" as region,
        "OS_ID" as os_id,
        "F_100FTRULE" as f_100ftrule,
        "ZIPCODE" as zipcode,
        "PARCELNUMBER" as parcelnumber,
        "YEARACQUIRED" as yearacquired,
        "STAREA" as starea,
        "STLENGTH" as stlength,
        "SHAPE_LENGTH" as shape_length,
        "SHAPE_AREA" as shape_area,
        "SHAPE_WKT" as shape_wkt,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/open-space' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by os_id order by _loaded_at desc) = 1
