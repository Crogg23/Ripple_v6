version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: INTL_GDELT
        description: >
          Raw landing table for GDELT 2.0 Global Events Database. Grain not yet determined.

models:
  - name: stg_intl_gdelt__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
