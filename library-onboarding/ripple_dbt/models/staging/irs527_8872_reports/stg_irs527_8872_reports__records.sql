{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FORM_ID_NUMBER unique)
-- SPINE_ENTITY: (not determined)  (natural_key: FORM_ID_NUMBER)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 14 of 48 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   form_type: integer
--   period_begin_date: date, YYYYMMDD
--   period_end_date: date, YYYYMMDD
--   initial_report_ind: integer
--   amended_report_ind: integer
--   final_report_ind: integer
--   org_formation_date: integer
--   qtr_indicator: integer
--   pre_or_post_elect_date: date, YYYYMMDD
--   sched_a_ind: integer
--   total_sched_a: integer
--   sched_b_ind: integer
--   total_sched_b: integer
--   insert_datetime: timestamp
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'IRS527_8872_REPORTS') }}
    qualify row_number() over (partition by "FORM_ID_NUMBER" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_int('"FORM_TYPE"') }} as form_type,
        {{ null_junk('"FORM_ID_NUMBER"') }} as form_id_number,
        {{ stg_date('"PERIOD_BEGIN_DATE"', 'YYYYMMDD') }} as period_begin_date,
        {{ stg_date('"PERIOD_END_DATE"', 'YYYYMMDD') }} as period_end_date,
        {{ stg_int('"INITIAL_REPORT_IND"') }} as initial_report_ind,
        {{ stg_int('"AMENDED_REPORT_IND"') }} as amended_report_ind,
        {{ stg_int('"FINAL_REPORT_IND"') }} as final_report_ind,
        {{ null_junk('"CHANGE_OF_ADDRESS_IND"') }} as change_of_address_ind,
        {{ null_junk('"ORGANIZATION_NAME"') }} as organization_name,
        {{ null_junk('"EIN"') }} as ein,
        {{ null_junk('"MAILING_ADDR1"') }} as mailing_addr1,
        {{ null_junk('"MAILING_ADDR2"') }} as mailing_addr2,
        {{ null_junk('"MAILING_CITY"') }} as mailing_city,
        {{ null_junk('"MAILING_STATE"') }} as mailing_state,
        {{ null_junk('"MAILING_ZIP"') }} as mailing_zip,
        {{ null_junk('"MAILING_ZIP_EXT"') }} as mailing_zip_ext,
        {{ null_junk('"EMAIL_ADDRESS"') }} as email_address,
        {{ stg_int('"ORG_FORMATION_DATE"') }} as org_formation_date,
        {{ null_junk('"CUSTODIAN_NAME"') }} as custodian_name,
        {{ null_junk('"CUSTODIAN_ADDR1"') }} as custodian_addr1,
        {{ null_junk('"CUSTODIAN_ADDR2"') }} as custodian_addr2,
        {{ null_junk('"CUSTODIAN_CITY"') }} as custodian_city,
        {{ null_junk('"CUSTODIAN_STATE"') }} as custodian_state,
        {{ null_junk('"CUSTODIAN_ZIP"') }} as custodian_zip,
        {{ null_junk('"CUSTODIAN_ZIP_EXT"') }} as custodian_zip_ext,
        {{ null_junk('"CONTACT_NAME"') }} as contact_name,
        {{ null_junk('"CONTACT_ADDR1"') }} as contact_addr1,
        {{ null_junk('"CONTACT_ADDR2"') }} as contact_addr2,
        {{ null_junk('"CONTACT_CITY"') }} as contact_city,
        {{ null_junk('"CONTACT_STATE"') }} as contact_state,
        {{ null_junk('"CONTACT_ZIP"') }} as contact_zip,
        {{ null_junk('"CONTACT_ZIP_EXT"') }} as contact_zip_ext,
        {{ null_junk('"BUSINESS_ADDR1"') }} as business_addr1,
        {{ null_junk('"BUSINESS_ADDR2"') }} as business_addr2,
        {{ null_junk('"BUSINESS_CITY"') }} as business_city,
        {{ null_junk('"BUSINESS_STATE"') }} as business_state,
        {{ null_junk('"BUSINESS_ZIP"') }} as business_zip,
        {{ null_junk('"BUSINESS_ZIP_EXT"') }} as business_zip_ext,
        {{ stg_int('"QTR_INDICATOR"') }} as qtr_indicator,
        {{ null_junk('"MONTHLY_RPT_MONTH"') }} as monthly_rpt_month,
        {{ null_junk('"PRE_ELECT_TYPE"') }} as pre_elect_type,
        {{ stg_date('"PRE_OR_POST_ELECT_DATE"', 'YYYYMMDD') }} as pre_or_post_elect_date,
        {{ null_junk('"PRE_OR_POST_ELECT_STATE"') }} as pre_or_post_elect_state,
        {{ stg_int('"SCHED_A_IND"') }} as sched_a_ind,
        {{ stg_int('"TOTAL_SCHED_A"') }} as total_sched_a,
        {{ stg_int('"SCHED_B_IND"') }} as sched_b_ind,
        {{ stg_int('"TOTAL_SCHED_B"') }} as total_sched_b,
        {{ stg_ts('"INSERT_DATETIME"') }} as insert_datetime,
        INGESTED_AT as _loaded_at,
        'https://www.irs.gov/charities-non-profits/political-organizations/political-organization-filing-and-disclosure' as _source_url

    from source

)

select * from renamed
