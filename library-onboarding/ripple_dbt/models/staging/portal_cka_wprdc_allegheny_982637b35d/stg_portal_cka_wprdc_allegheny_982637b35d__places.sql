{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (FIPS is unique)
-- SPINE_ENTITY: place  (natural_key: GEOID10)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 22 of 40 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   statefp10: integer
--   created_date: timestamp
--   pghdb_sde_neighborhood_2010_are: float
--   last_edited_date: timestamp
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   hood_no: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   sqmiles: float
--   blkgrpce10: integer
--   fid_neighb: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   awater10: integer
--   acres: float
--   page_number: integer
--   intptlon10: float
--   neighbor: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   intptlat10: float
--   fid_blockg: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   unique_id: integer
--   perimeter: float
--   neighbor_i: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   sectors: integer
--   dpwdiv: integer
--   aland10: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_982637B35D') }}
    qualify row_number() over (partition by "GEOID10" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_int('"STATEFP10"') }} as statefp10,
        {{ stg_ts('"CREATED_DATE"') }} as created_date,
        {{ null_junk('"PLANNERASSIGN"') }} as plannerassign,
        {{ stg_float('"PGHDB_SDE_NEIGHBORHOOD_2010_ARE"') }} as pghdb_sde_neighborhood_2010_are,
        {{ null_junk('"FUNCSTAT10"') }} as funcstat10,
        {{ null_junk('"MTFCC10"') }} as mtfcc10,
        {{ stg_ts('"LAST_EDITED_DATE"') }} as last_edited_date,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_id_text('"HOOD_NO"') }} as hood_no,
        {{ stg_float('"SQMILES"') }} as sqmiles,
        {{ null_junk('"LAST_EDITED_USER"') }} as last_edited_user,
        {{ stg_int('"BLKGRPCE10"') }} as blkgrpce10,
        {{ null_junk('"GLOBALID"') }} as globalid,
        {{ null_junk('"NAMELSAD10"') }} as namelsad10,
        {{ stg_id_text('"FID_NEIGHB"') }} as fid_neighb,
        {{ null_junk('"COUNTYFP10"') }} as countyfp10,
        {{ stg_int('"AWATER10"') }} as awater10,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ stg_float('"ACRES"') }} as acres,
        {{ stg_int('"PAGE_NUMBER"') }} as page_number,
        {{ null_junk('"SHAPE_AR_1"') }} as shape_ar_1,
        {{ null_junk('"CREATED_USER"') }} as created_user,
        {{ stg_float('"INTPTLON10"') }} as intptlon10,
        {{ stg_id_text('"NEIGHBOR"') }} as neighbor,
        {{ stg_float('"INTPTLAT10"') }} as intptlat10,
        {{ stg_id_text('"FID_BLOCKG"') }} as fid_blockg,
        {{ null_junk('"TEMP"') }} as temp,
        {{ null_junk('"GEOID10"') }} as geoid10,
        {{ stg_int('"UNIQUE_ID"') }} as unique_id,
        {{ stg_float('"PERIMETER"') }} as perimeter,
        {{ null_junk('"TRACTCE10"') }} as tractce10,
        {{ null_junk('"SHAPE_LE_1"') }} as shape_le_1,
        {{ stg_id_text('"NEIGHBOR_I"') }} as neighbor_i,
        {{ stg_int('"SECTORS"') }} as sectors,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ stg_int('"DPWDIV"') }} as dpwdiv,
        {{ null_junk('"SHAPE_LENG"') }} as shape_leng,
        {{ null_junk('"HOOD"') }} as hood,
        {{ stg_id_text('"ALAND10"') }} as aland10,
        {{ null_junk('"DATASPATIAL_WKB"') }} as dataspatial_wkb,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/neighborhoods2' as _source_url

    from source

)

select * from renamed
