version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_DHS_HIFLD

models:
  - name: stg_fed_dhs_hifld__facilities
    description: "SAMPLE ONLY -- NOT the full dataset. DHS HIFLD infrastructure facilities: a 500-row slice of one layer. objectid+layer unique in the slice. Use for shape/testing only."
    columns:
      - name: objectid
        data_tests: [not_null]
      - name: name
        description: Blank across the sampled layer (power transmission lines carry no names) as published.
