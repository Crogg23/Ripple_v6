version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_DHS_HIFLD
        description: >
          Raw landing table for Homeland Infrastructure Foundation-Level Data (HIFLD). Grain not yet determined.

models:
  - name: stg_fed_dhs_hifld__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
