version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_OPEN_DATA_DC_D359158BC8
        description: >
          Raw landing table for Barber and Beauty Shops in DC. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_open_data_dc_d359158bc8__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INFOUSA_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: infousa_id
        data_tests:
          - unique
          - not_null
