version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_RETRACTION_WATCH

models:
  - name: stg_fed_retraction_watch__retractions
    description: "Retraction Watch database: one row per retraction/correction record. RECORD_ID blank on 214 rows as published; unique among non-blanks."
    columns:
      - name: record_id
        description: Blank on 214 rows as published; dbt's unique test ignores nulls.
        data_tests: [unique]
      - name: title
        description: Blank on 214 rows (the same blank-RECORD_ID stub rows) as published.
