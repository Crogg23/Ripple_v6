{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (SAFEGRAPH_PLACE_ID unique)
-- SPINE_ENTITY: place  (natural_key: SAFEGRAPH_PLACE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 9 of 29 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid_1: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   phone_number: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   creationdate: integer
--   editdate: integer
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   join_count: integer
--   target_fid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   tractce: integer
--   overall_index: float
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_LA_COUNTY_OPEN_D_2E79EB67C8') }}
    qualify row_number() over (partition by "SAFEGRAPH_PLACE_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID_1"') }} as objectid_1,
        {{ null_junk('"LOCATION_NAME"') }} as location_name,
        {{ null_junk('"STREET_ADDRESS"') }} as street_address,
        {{ null_junk('"STREET_NAME"') }} as street_name,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"REGION"') }} as region,
        {{ null_junk('"POSTAL_CODE"') }} as postal_code,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"BRANDS"') }} as brands,
        {{ stg_id_text('"PHONE_NUMBER"') }} as phone_number,
        {{ null_junk('"SOURCE"') }} as source,
        {{ null_junk('"SAFEGRAPH_PLACE_ID"') }} as safegraph_place_id,
        {{ null_junk('"DESC"') }} as desc,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ stg_int('"CREATIONDATE"') }} as creationdate,
        {{ null_junk('"CREATOR"') }} as creator,
        {{ stg_int('"EDITDATE"') }} as editdate,
        {{ null_junk('"EDITOR"') }} as editor,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_int('"JOIN_COUNT"') }} as join_count,
        {{ stg_id_text('"TARGET_FID"') }} as target_fid,
        {{ stg_int('"TRACTCE"') }} as tractce,
        {{ null_junk('"GEOID"') }} as geoid,
        {{ null_junk('"GEO_ID_FULL"') }} as geo_id_full,
        {{ null_junk('"GEO_ID"') }} as geo_id,
        {{ stg_float('"OVERALL_INDEX"') }} as overall_index,
        {{ null_junk('"INDEX_CATEGORY"') }} as index_category,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.lacounty.gov/datasets/682f52458e3f40439caecae2c86810ee_1' as _source_url

    from source

)

select * from renamed
