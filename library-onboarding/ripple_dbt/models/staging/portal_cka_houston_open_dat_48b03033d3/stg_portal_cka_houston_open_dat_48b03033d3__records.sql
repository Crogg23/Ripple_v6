{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (BUSINESS_AREA_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: BUSINESS_AREA_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 4 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_HOUSTON_OPEN_DAT_48B03033D3') }}
    qualify row_number() over (partition by "BUSINESS_AREA_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"BUSINESS_AREA_ID"') }} as business_area_id,
        {{ null_junk('"BUSINESS_AREA_TYPE"') }} as business_area_type,
        {{ null_junk('"BUSINESS_AREA_NAME"') }} as business_area_name,
        {{ null_junk('"BUSINESS_AREA_SHORT_NAME"') }} as business_area_short_name,
        INGESTED_AT as _loaded_at,
        'https://data.houstontx.gov/dataset/city-of-houston-business-areas' as _source_url

    from source

)

select * from renamed
