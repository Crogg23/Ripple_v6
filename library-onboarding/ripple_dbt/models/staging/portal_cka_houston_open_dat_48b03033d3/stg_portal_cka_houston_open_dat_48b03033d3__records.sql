{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (BUSINESS_AREA_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: BUSINESS_AREA_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_HOUSTON_OPEN_DAT_48B03033D3') }}

),

renamed as (

    select
        "BUSINESS_AREA_ID" as business_area_id,
        "BUSINESS_AREA_TYPE" as business_area_type,
        "BUSINESS_AREA_NAME" as business_area_name,
        "BUSINESS_AREA_SHORT_NAME" as business_area_short_name,
        INGESTED_AT as _loaded_at,
        'https://data.houstontx.gov/dataset/city-of-houston-business-areas' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by business_area_id order by _loaded_at desc) = 1
