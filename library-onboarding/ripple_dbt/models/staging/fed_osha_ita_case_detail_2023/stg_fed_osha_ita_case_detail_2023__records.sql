{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 19 of 49 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   establishment_id: integer
--   establishment_type: integer
--   size: integer
--   annual_average_employees: integer
--   total_hours_worked: integer
--   soc_reviewed: integer
--   soc_probability: float
--   date_of_incident: date
--   incident_outcome: integer
--   dafw_num_away: integer
--   djtr_num_tr: integer
--   type_of_incident: integer
--   time_unknown: integer
--   year_filing_for: integer
--   nature_code_pred: integer
--   part_code_pred: integer
--   event_code_pred: integer
--   source_code_pred: integer
--   sec_source_code_pred: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'FED_OSHA_ITA_CASE_DETAIL_2023') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ stg_int('"ESTABLISHMENT_ID"') }} as establishment_id,
        {{ null_junk('"ESTABLISHMENT_NAME"') }} as establishment_name,
        {{ null_junk('"EIN"') }} as ein,
        {{ null_junk('"COMPANY_NAME"') }} as company_name,
        {{ null_junk('"STREET_ADDRESS"') }} as street_address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"NAICS_YEAR"') }} as naics_year,
        {{ null_junk('"INDUSTRY_DESCRIPTION"') }} as industry_description,
        {{ stg_int('"ESTABLISHMENT_TYPE"') }} as establishment_type,
        {{ stg_int('"SIZE"') }} as size,
        {{ stg_int('"ANNUAL_AVERAGE_EMPLOYEES"') }} as annual_average_employees,
        {{ stg_int('"TOTAL_HOURS_WORKED"') }} as total_hours_worked,
        {{ null_junk('"CASE_NUMBER"') }} as case_number,
        {{ null_junk('"JOB_DESCRIPTION"') }} as job_description,
        {{ null_junk('"SOC_CODE"') }} as soc_code,
        {{ null_junk('"SOC_DESCRIPTION"') }} as soc_description,
        {{ stg_int('"SOC_REVIEWED"') }} as soc_reviewed,
        {{ stg_float('"SOC_PROBABILITY"') }} as soc_probability,
        {{ stg_date('"DATE_OF_INCIDENT"') }} as date_of_incident,
        {{ stg_int('"INCIDENT_OUTCOME"') }} as incident_outcome,
        {{ stg_int('"DAFW_NUM_AWAY"') }} as dafw_num_away,
        {{ stg_int('"DJTR_NUM_TR"') }} as djtr_num_tr,
        {{ stg_int('"TYPE_OF_INCIDENT"') }} as type_of_incident,
        {{ null_junk('"TIME_STARTED_WORK"') }} as time_started_work,
        {{ null_junk('"TIME_OF_INCIDENT"') }} as time_of_incident,
        {{ stg_int('"TIME_UNKNOWN"') }} as time_unknown,
        {{ null_junk('"DATE_OF_DEATH"') }} as date_of_death,
        {{ null_junk('"CREATED_TIMESTAMP"') }} as created_timestamp,
        {{ stg_int('"YEAR_FILING_FOR"') }} as year_filing_for,
        {{ null_junk('"NEW_NAR_WHAT_HAPPENED"') }} as new_nar_what_happened,
        {{ null_junk('"NEW_NAR_BEFORE_INCIDENT"') }} as new_nar_before_incident,
        {{ null_junk('"NEW_INCIDENT_LOCATION"') }} as new_incident_location,
        {{ null_junk('"NEW_NAR_INJURY_ILLNESS"') }} as new_nar_injury_illness,
        {{ null_junk('"NEW_NAR_OBJECT_SUBSTANCE"') }} as new_nar_object_substance,
        {{ null_junk('"NEW_INCIDENT_DESCRIPTION"') }} as new_incident_description,
        {{ stg_int('"NATURE_CODE_PRED"') }} as nature_code_pred,
        {{ null_junk('"NATURE_TITLE_PRED"') }} as nature_title_pred,
        {{ stg_int('"PART_CODE_PRED"') }} as part_code_pred,
        {{ null_junk('"PART_TITLE_PRED"') }} as part_title_pred,
        {{ stg_int('"EVENT_CODE_PRED"') }} as event_code_pred,
        {{ null_junk('"EVENT_TITLE_PRED"') }} as event_title_pred,
        {{ stg_int('"SOURCE_CODE_PRED"') }} as source_code_pred,
        {{ null_junk('"SOURCE_TITLE_PRED"') }} as source_title_pred,
        {{ stg_int('"SEC_SOURCE_CODE_PRED"') }} as sec_source_code_pred,
        {{ null_junk('"SEC_SOURCE_TITLE_PRED"') }} as sec_source_title_pred,
        INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
