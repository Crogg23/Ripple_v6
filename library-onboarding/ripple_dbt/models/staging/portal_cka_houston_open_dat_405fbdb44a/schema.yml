version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_HOUSTON_OPEN_DAT_405FBDB44A
        description: >
          Raw landing table for ACS 5-Year Population Estimates by Race and Ethnicity at Block Group Level (Harris, Fort Bend, Montgomery). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_houston_open_dat_405fbdb44a__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GEO_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: geo_id
        data_tests:
          - unique
          - not_null
