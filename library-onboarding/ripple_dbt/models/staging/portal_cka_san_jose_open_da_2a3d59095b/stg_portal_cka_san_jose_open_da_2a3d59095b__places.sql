{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LATITUDE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 10 of 39 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   x: float
--   y: float
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   parcelid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   facilityid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   intid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   intnum: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   shopid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   losno: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   rsn: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (LATITUDE) is NOT unique within a load as landed at generation time -- it would collapse 14 of 22,668 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_2A3D59095B') }}
    qualify row_number() over (partition by "X", "Y", "OBJECTID", "PARCELID", "FACILITYID", "INTID", "INTTYPE", "INTNAME", "PLANCRT", "PLANMOD", "UNITYNAME", "ASTREETDIRECTION", "ASTREETNAME", "ASTREETTYPE", "BSTREETDIRECTION", "BSTREETNAME", "BSTREETTYPE", "INTERSECTIONTYPE", "TRAFFICCONTROLTYPE", "INTNUM", "SHOPID", "MAINTID", "LOSNO", "SHOPSTATUS", "DEVICETYPE", "OWNEDBY", "ACTIVATIONDATE", "SHOPOPERATOR", "FLAG", "RSN", "CONNECTSTREET", "LONGITUDE", "LATITUDE", "LASTUPDATE", "NOTES", "MODELFLAG", "PRIVATE", "INCORPORATED", "CREATIONDATE" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_float('"X"') }} as x,
        {{ stg_float('"Y"') }} as y,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_id_text('"PARCELID"') }} as parcelid,
        {{ stg_id_text('"FACILITYID"') }} as facilityid,
        {{ stg_id_text('"INTID"') }} as intid,
        {{ null_junk('"INTTYPE"') }} as inttype,
        {{ null_junk('"INTNAME"') }} as intname,
        {{ null_junk('"PLANCRT"') }} as plancrt,
        {{ null_junk('"PLANMOD"') }} as planmod,
        {{ null_junk('"UNITYNAME"') }} as unityname,
        {{ null_junk('"ASTREETDIRECTION"') }} as astreetdirection,
        {{ null_junk('"ASTREETNAME"') }} as astreetname,
        {{ null_junk('"ASTREETTYPE"') }} as astreettype,
        {{ null_junk('"BSTREETDIRECTION"') }} as bstreetdirection,
        {{ null_junk('"BSTREETNAME"') }} as bstreetname,
        {{ null_junk('"BSTREETTYPE"') }} as bstreettype,
        {{ null_junk('"INTERSECTIONTYPE"') }} as intersectiontype,
        {{ null_junk('"TRAFFICCONTROLTYPE"') }} as trafficcontroltype,
        {{ stg_id_text('"INTNUM"') }} as intnum,
        {{ stg_id_text('"SHOPID"') }} as shopid,
        {{ null_junk('"MAINTID"') }} as maintid,
        {{ stg_id_text('"LOSNO"') }} as losno,
        {{ null_junk('"SHOPSTATUS"') }} as shopstatus,
        {{ null_junk('"DEVICETYPE"') }} as devicetype,
        {{ null_junk('"OWNEDBY"') }} as ownedby,
        {{ null_junk('"ACTIVATIONDATE"') }} as activationdate,
        {{ null_junk('"SHOPOPERATOR"') }} as shopoperator,
        {{ null_junk('"FLAG"') }} as flag,
        {{ stg_id_text('"RSN"') }} as rsn,
        {{ null_junk('"CONNECTSTREET"') }} as connectstreet,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LASTUPDATE"') }} as lastupdate,
        {{ null_junk('"NOTES"') }} as notes,
        {{ null_junk('"MODELFLAG"') }} as modelflag,
        {{ null_junk('"PRIVATE"') }} as private,
        {{ null_junk('"INCORPORATED"') }} as incorporated,
        {{ null_junk('"CREATIONDATE"') }} as creationdate,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/street-intersections' as _source_url

    from source

)

select * from renamed
