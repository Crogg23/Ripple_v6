{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LATITUDE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_2A3D59095B') }}

),

renamed as (

    select
        "X" as x,
        "Y" as y,
        "OBJECTID" as objectid,
        "PARCELID" as parcelid,
        "FACILITYID" as facilityid,
        "INTID" as intid,
        "INTTYPE" as inttype,
        "INTNAME" as intname,
        "PLANCRT" as plancrt,
        "PLANMOD" as planmod,
        "UNITYNAME" as unityname,
        "ASTREETDIRECTION" as astreetdirection,
        "ASTREETNAME" as astreetname,
        "ASTREETTYPE" as astreettype,
        "BSTREETDIRECTION" as bstreetdirection,
        "BSTREETNAME" as bstreetname,
        "BSTREETTYPE" as bstreettype,
        "INTERSECTIONTYPE" as intersectiontype,
        "TRAFFICCONTROLTYPE" as trafficcontroltype,
        "INTNUM" as intnum,
        "SHOPID" as shopid,
        "MAINTID" as maintid,
        "LOSNO" as losno,
        "SHOPSTATUS" as shopstatus,
        "DEVICETYPE" as devicetype,
        "OWNEDBY" as ownedby,
        "ACTIVATIONDATE" as activationdate,
        "SHOPOPERATOR" as shopoperator,
        "FLAG" as flag,
        "RSN" as rsn,
        "CONNECTSTREET" as connectstreet,
        "LONGITUDE" as longitude,
        "LATITUDE" as latitude,
        "LASTUPDATE" as lastupdate,
        "NOTES" as notes,
        "MODELFLAG" as modelflag,
        "PRIVATE" as private,
        "INCORPORATED" as incorporated,
        "CREATIONDATE" as creationdate,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/street-intersections' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by latitude order by _loaded_at desc) = 1
