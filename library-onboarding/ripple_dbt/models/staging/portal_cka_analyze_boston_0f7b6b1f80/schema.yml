version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_0F7B6B1F80
        description: >
          Raw landing table for Boston Neighborhood Boundaries. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_0f7b6b1f80__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (NEIGHBORHOOD_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: neighborhood_id
        data_tests:
          - unique
          - not_null
