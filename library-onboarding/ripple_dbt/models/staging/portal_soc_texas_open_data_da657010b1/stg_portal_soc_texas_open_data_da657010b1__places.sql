{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+RESP_BEGIN_DATE unique)
-- SPINE_ENTITY: place  (natural_key: LOC_ZIP, RESP_BEGIN_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_TEXAS_OPEN_DATA_DA657010B1') }}

),

renamed as (

    select
        "TP_NUMBER" as tp_number,
        "LOC_NUMBER" as loc_number,
        "LOC_NAME" as loc_name,
        "ADDRESS_NUMBER" as address_number,
        "ADDRESS_TEXT" as address_text,
        "LOC_STATE" as loc_state,
        "LOC_CITY" as loc_city,
        "LOC_ZIP" as loc_zip,
        "LOC_ZIP4" as loc_zip4,
        "TABC_PERMIT_NUMBER" as tabc_permit_number,
        "LOC_COUNTY" as loc_county,
        "NAICS" as naics,
        "LOC_JURIS" as loc_juris,
        "SALES_TAX_LOCATION" as sales_tax_location,
        "LOC_CITY_ANNEX" as loc_city_annex,
        "RESP_BEGIN_DATE" as resp_begin_date,
        "OUT_OF_BUSINESS_DATE" as out_of_business_date,
        INGESTED_AT as _loaded_at,
        'https://data.texas.gov/d/8f4g-cpk9' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by loc_zip, resp_begin_date order by _loaded_at desc) = 1
