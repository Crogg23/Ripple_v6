version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_C7339FBA3B
        description: >
          Raw landing table for Internet Accessibility in Pennsylvania. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_c7339fba3b__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GEO_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: geo_id
        data_tests:
          - unique
          - not_null
