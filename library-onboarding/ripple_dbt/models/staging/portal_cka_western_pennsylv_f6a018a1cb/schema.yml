version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_F6A018A1CB
        description: >
          Raw landing table for Allegheny County Restaurant/Food Facility Inspections and Locations. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_f6a018a1cb__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INSPECTION_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: inspection_id
        data_tests:
          - unique
          - not_null
