{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INSPECTION_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: INSPECTION_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 2 of 17 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   bus_st_date: date
--   inspect_dt: date
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_F6A018A1CB') }}
    qualify row_number() over (partition by "INSPECTION_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"INSPECTION_ID"') }} as inspection_id,
        {{ null_junk('"PLACARD_DESC"') }} as placard_desc,
        {{ null_junk('"FACILITY_NAME"') }} as facility_name,
        {{ stg_date('"BUS_ST_DATE"') }} as bus_st_date,
        {{ null_junk('"FACILITY_TYPE"') }} as facility_type,
        {{ null_junk('"CATEGORY"') }} as category,
        {{ null_junk('"NONPROFIT"') }} as nonprofit,
        {{ null_junk('"NUM"') }} as num,
        {{ null_junk('"STREET"') }} as street,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"MUNICIPAL"') }} as municipal,
        {{ stg_date('"INSPECT_DT"') }} as inspect_dt,
        {{ null_junk('"INSPECTION_PURPOSE"') }} as inspection_purpose,
        {{ null_junk('"REINSPECTION_NEED"') }} as reinspection_need,
        {{ null_junk('"PERMIT_STATUS"') }} as permit_status,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-restaurant-food-facility-inspection-violations' as _source_url

    from source

)

select * from renamed
