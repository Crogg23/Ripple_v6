{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LONGITUDE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 33 of 45 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   managed_by_city: boolean
--   accepts_alkaline_batteries: boolean
--   accepts_automotive_batteries: boolean
--   accepts_cfl_lightbulbs: boolean
--   accepts_clothing: boolean
--   accepts_collectibles: boolean
--   accepts_computers_and_peripherals: boolean
--   accepts_construction_and_demolition_waste: boolean
--   accepts_fluorescent_tube_lightbulbs: boolean
--   accepts_freon_appliances: boolean
--   accepts_household_chemicals_and_waste: boolean
--   accepts_household_recyclables: boolean
--   accepts_plastic_bags_and_films: boolean
--   accepts_prescription_medication: boolean
--   accepts_propane_tanks: boolean
--   accepts_rechargeable_batteries: boolean
--   accepts_scrap_metal: boolean
--   accepts_small_business_recyclables: boolean
--   accepts_tires: boolean
--   accepts_tvs_and_monitors: boolean
--   accepts_yard_debris: boolean
--   accepts_cell_phones: boolean
--   accepts_general_electronics: boolean
--   accepts_ink_and_toner: boolean
--   accepts_motor_oil: boolean
--   inactive: boolean
--   council_district: integer
--   ward: integer
--   tract: integer
--   public_works_division: integer
--   pli_division: integer
--   police_zone: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_CC9BA8BED2') }}
    qualify row_number() over (partition by "LONGITUDE" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"ID"') }} as id,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"ADDRESS_NUMBER"') }} as address_number,
        {{ null_junk('"STREET"') }} as street,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"HOURS_OF_OPERATION"') }} as hours_of_operation,
        {{ null_junk('"NOTES"') }} as notes,
        {{ null_junk('"PHONE_NUMBER"') }} as phone_number,
        {{ null_junk('"WEBSITE"') }} as website,
        {{ stg_bool('"MANAGED_BY_CITY"') }} as managed_by_city,
        {{ stg_bool('"ACCEPTS_ALKALINE_BATTERIES"') }} as accepts_alkaline_batteries,
        {{ stg_bool('"ACCEPTS_AUTOMOTIVE_BATTERIES"') }} as accepts_automotive_batteries,
        {{ stg_bool('"ACCEPTS_CFL_LIGHTBULBS"') }} as accepts_cfl_lightbulbs,
        {{ stg_bool('"ACCEPTS_CLOTHING"') }} as accepts_clothing,
        {{ stg_bool('"ACCEPTS_COLLECTIBLES"') }} as accepts_collectibles,
        {{ stg_bool('"ACCEPTS_COMPUTERS_AND_PERIPHERALS"') }} as accepts_computers_and_peripherals,
        {{ stg_bool('"ACCEPTS_CONSTRUCTION_AND_DEMOLITION_WASTE"') }} as accepts_construction_and_demolition_waste,
        {{ stg_bool('"ACCEPTS_FLUORESCENT_TUBE_LIGHTBULBS"') }} as accepts_fluorescent_tube_lightbulbs,
        {{ stg_bool('"ACCEPTS_FREON_APPLIANCES"') }} as accepts_freon_appliances,
        {{ stg_bool('"ACCEPTS_HOUSEHOLD_CHEMICALS_AND_WASTE"') }} as accepts_household_chemicals_and_waste,
        {{ stg_bool('"ACCEPTS_HOUSEHOLD_RECYCLABLES"') }} as accepts_household_recyclables,
        {{ stg_bool('"ACCEPTS_PLASTIC_BAGS_AND_FILMS"') }} as accepts_plastic_bags_and_films,
        {{ stg_bool('"ACCEPTS_PRESCRIPTION_MEDICATION"') }} as accepts_prescription_medication,
        {{ stg_bool('"ACCEPTS_PROPANE_TANKS"') }} as accepts_propane_tanks,
        {{ stg_bool('"ACCEPTS_RECHARGEABLE_BATTERIES"') }} as accepts_rechargeable_batteries,
        {{ stg_bool('"ACCEPTS_SCRAP_METAL"') }} as accepts_scrap_metal,
        {{ stg_bool('"ACCEPTS_SMALL_BUSINESS_RECYCLABLES"') }} as accepts_small_business_recyclables,
        {{ stg_bool('"ACCEPTS_TIRES"') }} as accepts_tires,
        {{ stg_bool('"ACCEPTS_TVS_AND_MONITORS"') }} as accepts_tvs_and_monitors,
        {{ stg_bool('"ACCEPTS_YARD_DEBRIS"') }} as accepts_yard_debris,
        {{ stg_bool('"ACCEPTS_CELL_PHONES"') }} as accepts_cell_phones,
        {{ stg_bool('"ACCEPTS_GENERAL_ELECTRONICS"') }} as accepts_general_electronics,
        {{ stg_bool('"ACCEPTS_INK_AND_TONER"') }} as accepts_ink_and_toner,
        {{ stg_bool('"ACCEPTS_MOTOR_OIL"') }} as accepts_motor_oil,
        {{ stg_bool('"INACTIVE"') }} as inactive,
        {{ null_junk('"NEIGHBORHOOD"') }} as neighborhood,
        {{ stg_int('"COUNCIL_DISTRICT"') }} as council_district,
        {{ stg_int('"WARD"') }} as ward,
        {{ stg_int('"TRACT"') }} as tract,
        {{ stg_int('"PUBLIC_WORKS_DIVISION"') }} as public_works_division,
        {{ stg_int('"PLI_DIVISION"') }} as pli_division,
        {{ stg_int('"POLICE_ZONE"') }} as police_zone,
        {{ null_junk('"FIRE_ZONE"') }} as fire_zone,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/waste-recovery-locations' as _source_url

    from source

)

select * from renamed
