version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_FD81B4FB82
        description: >
          Raw landing table for Smart Trash Containers. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_fd81b4fb82__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CONTAINER_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: container_id
        data_tests:
          - unique
          - not_null
