{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CONTAINER_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: CONTAINER_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 8 of 16 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   receptacle_model_id: integer
--   assignment_date: timestamp
--   last_updated_date: timestamp
--   dpw_division: integer
--   council_district: integer
--   ward: integer
--   x: float
--   y: float
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_FD81B4FB82') }}
    qualify row_number() over (partition by "CONTAINER_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CONTAINER_ID"') }} as container_id,
        {{ stg_int('"RECEPTACLE_MODEL_ID"') }} as receptacle_model_id,
        {{ stg_ts('"ASSIGNMENT_DATE"') }} as assignment_date,
        {{ stg_ts('"LAST_UPDATED_DATE"') }} as last_updated_date,
        {{ null_junk('"GROUP_NAME"') }} as group_name,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"NEIGHBORHOOD"') }} as neighborhood,
        {{ stg_int('"DPW_DIVISION"') }} as dpw_division,
        {{ stg_int('"COUNCIL_DISTRICT"') }} as council_district,
        {{ stg_int('"WARD"') }} as ward,
        {{ null_junk('"FIRE_ZONE"') }} as fire_zone,
        {{ stg_float('"X"') }} as x,
        {{ stg_float('"Y"') }} as y,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/smart-trash-containers' as _source_url

    from source

)

select * from renamed
