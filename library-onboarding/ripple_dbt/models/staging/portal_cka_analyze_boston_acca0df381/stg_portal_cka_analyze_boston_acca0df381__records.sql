{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (MAIN_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: MAIN_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_ACCA0DF381') }}

),

renamed as (

    select
        "MAIN_ID" as main_id,
        "ST_NAME" as st_name,
        "DIST" as dist,
        "DIST_NAME" as dist_name,
        "START_TIME" as start_time,
        "END_TIME" as end_time,
        "SIDE" as side,
        "C_FROM" as c_from,
        "C_TO" as c_to,
        "MILES" as miles,
        "SECTION" as section,
        "ONE_WAY" as one_way,
        "WEEK_1" as week_1,
        "WEEK_2" as week_2,
        "WEEK_3" as week_3,
        "WEEK_4" as week_4,
        "WEEK_5" as week_5,
        "SUNDAY" as sunday,
        "MONDAY" as monday,
        "TUESDAY" as tuesday,
        "WEDNESDAY" as wednesday,
        "THURSDAY" as thursday,
        "FRIDAY" as friday,
        "SATURDAY" as saturday,
        "EVERY_DAY" as every_day,
        "YEAR_ROUND" as year_round,
        "NORTH_END_PILOT" as north_end_pilot,
        "TIMESTAMP" as timestamp,
        "PARENT" as parent,
        "LOSTA" as losta,
        "HISTA" as hista,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/street-sweeping-schedules' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by main_id order by _loaded_at desc) = 1
