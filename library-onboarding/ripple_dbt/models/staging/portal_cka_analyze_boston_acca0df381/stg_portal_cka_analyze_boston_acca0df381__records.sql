{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (MAIN_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: MAIN_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 4 of 31 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   miles: float
--   timestamp: timestamp
--   losta: integer
--   hista: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_ACCA0DF381') }}
    qualify row_number() over (partition by "MAIN_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"MAIN_ID"') }} as main_id,
        {{ null_junk('"ST_NAME"') }} as st_name,
        {{ null_junk('"DIST"') }} as dist,
        {{ null_junk('"DIST_NAME"') }} as dist_name,
        {{ null_junk('"START_TIME"') }} as start_time,
        {{ null_junk('"END_TIME"') }} as end_time,
        {{ null_junk('"SIDE"') }} as side,
        {{ null_junk('"C_FROM"') }} as c_from,
        {{ null_junk('"C_TO"') }} as c_to,
        {{ stg_float('"MILES"') }} as miles,
        {{ null_junk('"SECTION"') }} as section,
        {{ null_junk('"ONE_WAY"') }} as one_way,
        {{ null_junk('"WEEK_1"') }} as week_1,
        {{ null_junk('"WEEK_2"') }} as week_2,
        {{ null_junk('"WEEK_3"') }} as week_3,
        {{ null_junk('"WEEK_4"') }} as week_4,
        {{ null_junk('"WEEK_5"') }} as week_5,
        {{ null_junk('"SUNDAY"') }} as sunday,
        {{ null_junk('"MONDAY"') }} as monday,
        {{ null_junk('"TUESDAY"') }} as tuesday,
        {{ null_junk('"WEDNESDAY"') }} as wednesday,
        {{ null_junk('"THURSDAY"') }} as thursday,
        {{ null_junk('"FRIDAY"') }} as friday,
        {{ null_junk('"SATURDAY"') }} as saturday,
        {{ null_junk('"EVERY_DAY"') }} as every_day,
        {{ null_junk('"YEAR_ROUND"') }} as year_round,
        {{ null_junk('"NORTH_END_PILOT"') }} as north_end_pilot,
        {{ stg_ts('"TIMESTAMP"') }} as timestamp,
        {{ null_junk('"PARENT"') }} as parent,
        {{ stg_int('"LOSTA"') }} as losta,
        {{ stg_int('"HISTA"') }} as hista,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/street-sweeping-schedules' as _source_url

    from source

)

select * from renamed
