version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_ACCA0DF381
        description: >
          Raw landing table for Street Sweeping Schedules. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_acca0df381__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (MAIN_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: main_id
        data_tests:
          - unique
          - not_null
