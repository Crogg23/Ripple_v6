{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 6 of 17 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   taxyr: integer
--   votingmembersgoverningbodycnt: integer
--   votingmembersindependentcnt: integer
--   totalemployeecnt: integer
--   totalvolunteerscnt: integer
--   formationyr: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_COLORADO_INFORMA_A50DA9B699') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ stg_int('"TAXYR"') }} as taxyr,
        {{ null_junk('"BUSINESSNAME1"') }} as businessname1,
        {{ null_junk('"EIN"') }} as ein,
        {{ stg_int('"VOTINGMEMBERSGOVERNINGBODYCNT"') }} as votingmembersgoverningbodycnt,
        {{ stg_int('"VOTINGMEMBERSINDEPENDENTCNT"') }} as votingmembersindependentcnt,
        {{ stg_int('"TOTALEMPLOYEECNT"') }} as totalemployeecnt,
        {{ stg_int('"TOTALVOLUNTEERSCNT"') }} as totalvolunteerscnt,
        {{ null_junk('"ACTIVITYORMISSIONDESC"') }} as activityormissiondesc,
        {{ null_junk('"TYPEOFORGANIZATIONCORPIND"') }} as typeoforganizationcorpind,
        {{ null_junk('"TYPEOFORGANIZATIONTRUSTIND"') }} as typeoforganizationtrustind,
        {{ null_junk('"TYPEOFORGANIZATIONASSOCIND"') }} as typeoforganizationassocind,
        {{ null_junk('"TYPEOFORGANIZATIONOTHERIND"') }} as typeoforganizationotherind,
        {{ stg_int('"FORMATIONYR"') }} as formationyr,
        {{ null_junk('"LEGALDOMICILESTATECD"') }} as legaldomicilestatecd,
        {{ null_junk('"BUSINESSNAME2"') }} as businessname2,
        {{ null_junk('"OTHERORGANIZATIONDSC"') }} as otherorganizationdsc,
        INGESTED_AT as _loaded_at,
        'https://data.colorado.gov/d/68n7-r6rp' as _source_url

    from source

)

select * from renamed
