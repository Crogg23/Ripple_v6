version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_ED65B530A3
        description: >
          Raw landing table for Pennsylvania Vacant Addresses. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_ed65b530a3__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (FIPS+QUARTER unique). SPINE_ENTITY: place."
    columns:
      - name: geoid
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: quarter
        data_tests:
          - not_null:
              config:
                severity: warn
