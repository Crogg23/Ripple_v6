{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (OBJECT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: OBJECT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 12 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_DENSHO_DDR') }}
    qualify row_number() over (partition by "OBJECT_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"OBJECT_ID"') }} as object_id,
        {{ null_junk('"COLLECTION_ID"') }} as collection_id,
        {{ null_junk('"TITLE"') }} as title,
        {{ null_junk('"OBJECT_TYPE"') }} as object_type,
        {{ null_junk('"NARRATOR"') }} as narrator,
        {{ null_junk('"FACILITY"') }} as facility,
        {{ null_junk('"TOPIC"') }} as topic,
        {{ null_junk('"DATE"') }} as date,
        {{ null_junk('"DESCRIPTION"') }} as description,
        {{ null_junk('"RIGHTS"') }} as rights,
        {{ null_junk('"URL"') }} as url,
        {{ null_junk('"FIPS"') }} as fips,
        _INGESTED_AT as _loaded_at,
        'https://ddr.densho.org/' as _source_url

    from source

)

select * from renamed
