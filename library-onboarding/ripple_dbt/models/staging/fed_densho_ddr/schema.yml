version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_DENSHO_DDR
        description: >
          Raw landing table for Densho Digital Repository (DDR). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_fed_densho_ddr__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (OBJECT_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: object_id
        data_tests:
          - unique
          - not_null
