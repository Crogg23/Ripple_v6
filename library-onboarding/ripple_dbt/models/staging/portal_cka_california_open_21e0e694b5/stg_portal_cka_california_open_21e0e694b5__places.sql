{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE__LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 12 of 47 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   mapcode: integer
--   pct_bare: float
--   pct_tree: float
--   pct_shrub: float
--   pct_herb: float
--   bare_cat: integer
--   tree_cat: integer
--   shrub_cat: integer
--   herb_cat: integer
--   acres: float
--   hectares: float
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_CALIFORNIA_OPEN_21E0E694B5') }}
    qualify row_number() over (partition by "SHAPE__LENGTH" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"MCVNAME"') }} as mcvname,
        {{ null_junk('"MCVLEVEL"') }} as mcvlevel,
        {{ stg_int('"MAPCODE"') }} as mapcode,
        {{ null_junk('"MAPCLASS"') }} as mapclass,
        {{ null_junk('"FB_HISTORIC"') }} as fb_historic,
        {{ null_junk('"ALLIANCE"') }} as alliance,
        {{ null_junk('"ASSOCIATIO_1"') }} as associatio_1,
        {{ null_junk('"HETEROGENEITY"') }} as heterogeneity,
        {{ null_junk('"TREE_REL_COV"') }} as tree_rel_cov,
        {{ null_junk('"SHRUB_REL_COV"') }} as shrub_rel_cov,
        {{ null_junk('"HERB_REL_COV"') }} as herb_rel_cov,
        {{ null_junk('"FIELD_VER_1"') }} as field_ver_1,
        {{ null_junk('"NOTES1"') }} as notes1,
        {{ stg_float('"PCT_BARE"') }} as pct_bare,
        {{ stg_float('"PCT_TREE"') }} as pct_tree,
        {{ stg_float('"PCT_SHRUB"') }} as pct_shrub,
        {{ stg_float('"PCT_HERB"') }} as pct_herb,
        {{ stg_int('"BARE_CAT"') }} as bare_cat,
        {{ stg_int('"TREE_CAT"') }} as tree_cat,
        {{ stg_int('"SHRUB_CAT"') }} as shrub_cat,
        {{ stg_int('"HERB_CAT"') }} as herb_cat,
        {{ null_junk('"F1_FORMATIONCLASS_NAME"') }} as f1_formationclass_name,
        {{ null_junk('"F2_FORMATIONSUBCLASS_NAME"') }} as f2_formationsubclass_name,
        {{ null_junk('"F3_FORMATION_NAME"') }} as f3_formation_name,
        {{ null_junk('"F4_DIVISION_NAME"') }} as f4_division_name,
        {{ null_junk('"F5_MACROGROUP_NAME"') }} as f5_macrogroup_name,
        {{ null_junk('"F6_GROUP_NAME"') }} as f6_group_name,
        {{ null_junk('"NATUESERVE"') }} as natueserve,
        {{ null_junk('"USNVC_CLAS"') }} as usnvc_clas,
        {{ null_junk('"UID"') }} as uid,
        {{ null_junk('"CALVEGTYPE"') }} as calvegtype,
        {{ null_junk('"CALVEGCODE"') }} as calvegcode,
        {{ null_junk('"CWHRTYPE"') }} as cwhrtype,
        {{ null_junk('"CWHRCODE"') }} as cwhrcode,
        {{ null_junk('"GLOBALRANK"') }} as globalrank,
        {{ null_junk('"STATERANK"') }} as staterank,
        {{ null_junk('"SENSITIVE"') }} as sensitive,
        {{ null_junk('"CACODE"') }} as cacode,
        {{ null_junk('"MCVALLIANCE"') }} as mcvalliance,
        {{ null_junk('"MCVGROUP"') }} as mcvgroup,
        {{ null_junk('"MCVMACROGROUP"') }} as mcvmacrogroup,
        {{ null_junk('"COMMUNITYLINK"') }} as communitylink,
        {{ stg_float('"ACRES"') }} as acres,
        {{ stg_float('"HECTARES"') }} as hectares,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        INGESTED_AT as _loaded_at,
        'https://data.ca.gov/dataset/vegetation-fallbrook-naval-weapons-station-ds2958' as _source_url

    from source

)

select * from renamed
