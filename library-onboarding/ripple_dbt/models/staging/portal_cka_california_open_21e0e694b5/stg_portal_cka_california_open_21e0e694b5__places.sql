{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE__LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_CALIFORNIA_OPEN_21E0E694B5') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "MCVNAME" as mcvname,
        "MCVLEVEL" as mcvlevel,
        "MAPCODE" as mapcode,
        "MAPCLASS" as mapclass,
        "FB_HISTORIC" as fb_historic,
        "ALLIANCE" as alliance,
        "ASSOCIATIO_1" as associatio_1,
        "HETEROGENEITY" as heterogeneity,
        "TREE_REL_COV" as tree_rel_cov,
        "SHRUB_REL_COV" as shrub_rel_cov,
        "HERB_REL_COV" as herb_rel_cov,
        "FIELD_VER_1" as field_ver_1,
        "NOTES1" as notes1,
        "PCT_BARE" as pct_bare,
        "PCT_TREE" as pct_tree,
        "PCT_SHRUB" as pct_shrub,
        "PCT_HERB" as pct_herb,
        "BARE_CAT" as bare_cat,
        "TREE_CAT" as tree_cat,
        "SHRUB_CAT" as shrub_cat,
        "HERB_CAT" as herb_cat,
        "F1_FORMATIONCLASS_NAME" as f1_formationclass_name,
        "F2_FORMATIONSUBCLASS_NAME" as f2_formationsubclass_name,
        "F3_FORMATION_NAME" as f3_formation_name,
        "F4_DIVISION_NAME" as f4_division_name,
        "F5_MACROGROUP_NAME" as f5_macrogroup_name,
        "F6_GROUP_NAME" as f6_group_name,
        "NATUESERVE" as natueserve,
        "USNVC_CLAS" as usnvc_clas,
        "UID" as uid,
        "CALVEGTYPE" as calvegtype,
        "CALVEGCODE" as calvegcode,
        "CWHRTYPE" as cwhrtype,
        "CWHRCODE" as cwhrcode,
        "GLOBALRANK" as globalrank,
        "STATERANK" as staterank,
        "SENSITIVE" as sensitive,
        "CACODE" as cacode,
        "MCVALLIANCE" as mcvalliance,
        "MCVGROUP" as mcvgroup,
        "MCVMACROGROUP" as mcvmacrogroup,
        "COMMUNITYLINK" as communitylink,
        "ACRES" as acres,
        "HECTARES" as hectares,
        "SHAPE__AREA" as shape__area,
        "SHAPE__LENGTH" as shape__length,
        INGESTED_AT as _loaded_at,
        'https://data.ca.gov/dataset/vegetation-fallbrook-naval-weapons-station-ds2958' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by shape__length order by _loaded_at desc) = 1
