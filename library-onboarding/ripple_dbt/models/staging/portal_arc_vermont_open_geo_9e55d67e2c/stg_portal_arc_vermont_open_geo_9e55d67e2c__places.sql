{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 32 of 51 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   enrollment: integer
--   yearbuilt: integer
--   f2014enroll: integer
--   f2015enroll: integer
--   f2016enroll: integer
--   f2017enroll: integer
--   f2018enroll: integer
--   f2019enroll: integer
--   f2020enroll: integer
--   f2021enroll: integer
--   f2022enroll: integer
--   f2023enroll: integer
--   leadershipfte: float
--   leadershipaveragesalary: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   paraprofessionalfte: float
--   paraprofessionalaveragesalary: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   studentservicesfte: float
--   studentservicesaveragesalary: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   supportservicesfte: float
--   supportservicesaveragesalary: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   teacherfte: float
--   teacheraveragesalary: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   g1classsize: float
--   g2classsize: float
--   g3classsize: float
--   g4classsize: float
--   g5classsize: float
--   ms_coreclassaverage: float
--   hs_coreclassaverage: float
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   f2024enroll: integer
--   pk_adult_2024enroll: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (GEOMETRY) is NOT unique within a load as landed at generation time -- it would collapse 2 of 286 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_VERMONT_OPEN_GEO_9E55D67E2C') }}
    qualify row_number() over (partition by "SCHOOL", "SCHOOLIDENTIFIER", "ADDRESS", "GRADES", "GRADESLIST", "ENROLLMENT", "TYPE", "ENROLLYEAR", "ENROLLCATEGORY", "ACCESSTYPE", "TOWN", "TOWNMC", "DISTRICT", "YEARBUILT", "FCICATEGORY", "F2014ENROLL", "F2015ENROLL", "F2016ENROLL", "F2017ENROLL", "F2018ENROLL", "F2019ENROLL", "F2020ENROLL", "F2021ENROLL", "F2022ENROLL", "F2023ENROLL", "COUNTY", "PARENT_ORGANIZATION", "OPERATING_ORGANIZATION", "NCES_ID", "LEADERSHIPFTE", "LEADERSHIPAVERAGESALARY", "PARAPROFESSIONALFTE", "PARAPROFESSIONALAVERAGESALARY", "STUDENTSERVICESFTE", "STUDENTSERVICESAVERAGESALARY", "SUPPORTSERVICESFTE", "SUPPORTSERVICESAVERAGESALARY", "TEACHERFTE", "TEACHERAVERAGESALARY", "G1CLASSSIZE", "G2CLASSSIZE", "G3CLASSSIZE", "G4CLASSSIZE", "G5CLASSSIZE", "MS_CORECLASSAVERAGE", "HS_CORECLASSAVERAGE", "GRADECOMBINED_CLASSSIZE", "OBJECTID", "F2024ENROLL", "PK_ADULT_2024ENROLL", "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SCHOOL"') }} as school,
        {{ null_junk('"SCHOOLIDENTIFIER"') }} as schoolidentifier,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"GRADES"') }} as grades,
        {{ null_junk('"GRADESLIST"') }} as gradeslist,
        {{ stg_int('"ENROLLMENT"') }} as enrollment,
        {{ null_junk('"TYPE"') }} as type,
        {{ null_junk('"ENROLLYEAR"') }} as enrollyear,
        {{ null_junk('"ENROLLCATEGORY"') }} as enrollcategory,
        {{ null_junk('"ACCESSTYPE"') }} as accesstype,
        {{ null_junk('"TOWN"') }} as town,
        {{ null_junk('"TOWNMC"') }} as townmc,
        {{ null_junk('"DISTRICT"') }} as district,
        {{ stg_int('"YEARBUILT"') }} as yearbuilt,
        {{ null_junk('"FCICATEGORY"') }} as fcicategory,
        {{ stg_int('"F2014ENROLL"') }} as f2014enroll,
        {{ stg_int('"F2015ENROLL"') }} as f2015enroll,
        {{ stg_int('"F2016ENROLL"') }} as f2016enroll,
        {{ stg_int('"F2017ENROLL"') }} as f2017enroll,
        {{ stg_int('"F2018ENROLL"') }} as f2018enroll,
        {{ stg_int('"F2019ENROLL"') }} as f2019enroll,
        {{ stg_int('"F2020ENROLL"') }} as f2020enroll,
        {{ stg_int('"F2021ENROLL"') }} as f2021enroll,
        {{ stg_int('"F2022ENROLL"') }} as f2022enroll,
        {{ stg_int('"F2023ENROLL"') }} as f2023enroll,
        {{ null_junk('"COUNTY"') }} as county,
        {{ null_junk('"PARENT_ORGANIZATION"') }} as parent_organization,
        {{ null_junk('"OPERATING_ORGANIZATION"') }} as operating_organization,
        {{ null_junk('"NCES_ID"') }} as nces_id,
        {{ stg_float('"LEADERSHIPFTE"') }} as leadershipfte,
        {{ stg_id_text('"LEADERSHIPAVERAGESALARY"') }} as leadershipaveragesalary,
        {{ stg_float('"PARAPROFESSIONALFTE"') }} as paraprofessionalfte,
        {{ stg_id_text('"PARAPROFESSIONALAVERAGESALARY"') }} as paraprofessionalaveragesalary,
        {{ stg_float('"STUDENTSERVICESFTE"') }} as studentservicesfte,
        {{ stg_id_text('"STUDENTSERVICESAVERAGESALARY"') }} as studentservicesaveragesalary,
        {{ stg_float('"SUPPORTSERVICESFTE"') }} as supportservicesfte,
        {{ stg_id_text('"SUPPORTSERVICESAVERAGESALARY"') }} as supportservicesaveragesalary,
        {{ stg_float('"TEACHERFTE"') }} as teacherfte,
        {{ stg_id_text('"TEACHERAVERAGESALARY"') }} as teacheraveragesalary,
        {{ stg_float('"G1CLASSSIZE"') }} as g1classsize,
        {{ stg_float('"G2CLASSSIZE"') }} as g2classsize,
        {{ stg_float('"G3CLASSSIZE"') }} as g3classsize,
        {{ stg_float('"G4CLASSSIZE"') }} as g4classsize,
        {{ stg_float('"G5CLASSSIZE"') }} as g5classsize,
        {{ stg_float('"MS_CORECLASSAVERAGE"') }} as ms_coreclassaverage,
        {{ stg_float('"HS_CORECLASSAVERAGE"') }} as hs_coreclassaverage,
        {{ null_junk('"GRADECOMBINED_CLASSSIZE"') }} as gradecombined_classsize,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_int('"F2024ENROLL"') }} as f2024enroll,
        {{ stg_int('"PK_ADULT_2024ENROLL"') }} as pk_adult_2024enroll,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://geodata.vermont.gov/datasets/946049a138e5444185b8a3cfddd76d98_0' as _source_url

    from source

)

select * from renamed
