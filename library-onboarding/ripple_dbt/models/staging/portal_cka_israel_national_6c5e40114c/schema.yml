version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ISRAEL_NATIONAL_6C5E40114C
        description: >
          Raw landing table for ביטוח-נט. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_israel_national_6c5e40114c__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FUND_ID+REPORT_PERIOD unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: fund_id
        data_tests:
          - not_null
      - name: report_period
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - fund_id
              - report_period