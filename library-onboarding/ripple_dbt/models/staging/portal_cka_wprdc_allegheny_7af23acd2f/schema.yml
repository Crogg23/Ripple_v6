version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_7AF23ACD2F
        description: >
          Raw landing table for POGOH Trip Data. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_7af23acd2f__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (START_STATION_ID+END_STATION_ID+START_DATE unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: start_station_id
        data_tests:
          - not_null
      - name: end_station_id
        data_tests:
          - not_null
      - name: start_date
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - start_station_id
              - end_station_id
              - start_date