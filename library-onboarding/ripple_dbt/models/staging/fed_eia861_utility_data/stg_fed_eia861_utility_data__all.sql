{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED -- passthrough staging view (snake_case rename only, no dedup).
-- Generated by scripts/gen_minimal_staging_for_new_sources.py, 2026-08-05 ingestion sweep
-- (housing/health/energy block, part 2). Acquisition only -- no analysis/publish.

with source as (

    select * from {{ source('ripple_raw', 'FED_EIA861_UTILITY_DATA') }}

),

renamed as (

    select
        DATA_YEAR as data_year,
        UTILITY_NUMBER as utility_number,
        UTILITY_NAME as utility_name,
        STATE as state,
        OWNERSHIP_TYPE as ownership_type,
        NERC_REGION as nerc_region,
        TRE as tre,
        FRCC as frcc,
        MRO as mro,
        NPCC as npcc,
        RFC as rfc,
        SERC as serc,
        SPP as spp,
        WECC as wecc,
        CAISO as caiso,
        ERCOT as ercot,
        PJM as pjm,
        NYISO as nyiso,
        SPP_1 as spp_1,
        MISO as miso,
        ISONE as isone,
        OTHER as other,
        GENERATION as generation,
        TRANSMISSION as transmission,
        BUYING_TRANSMISSION as buying_transmission,
        DISTRIBUTION as distribution,
        BUYING_DISTRIBUTION as buying_distribution,
        WHOLESALE_MARKETING as wholesale_marketing,
        RETAIL_MARKETING as retail_marketing,
        BUNDLED as bundled,
        ALT_FUEL_VEHICLE as alt_fuel_vehicle,
        ALT_FUEL_VEHICLE_2 as alt_fuel_vehicle_2,
        _INGESTED_AT as _loaded_at,
        _SOURCE_RUN_ID as _source_run_id,
        _SRC_FILE as _src_file

    from source

)

select * from renamed
