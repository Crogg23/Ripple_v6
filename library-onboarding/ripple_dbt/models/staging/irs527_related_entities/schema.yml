version: 2

models:
  - name: stg_irs527_related_entities__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ENTITY_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: entity_id
        data_tests:
          - unique
          - not_null
