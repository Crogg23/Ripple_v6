version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_CALIFORNIA_OPEN_49D5A45DE1
        description: >
          Raw landing table for Foothill Yellow-Legged Frog (Feather River DPS) Range - CWHR A043b [ds3345]. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_california_open_49d5a45de1__places
    description: "GRAIN: one row per place (GEOM is unique). SPINE_ENTITY: place."
    columns:
      - name: shape__length
        data_tests:
          - unique
          - not_null
