version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_TN_DATA_TENNESSE_655F6BCC6D
        description: >
          Raw landing table for Business Data. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_tn_data_tennesse_655f6bcc6d__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INFOUSA_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: infousa_id
        data_tests:
          - unique
          - not_null
