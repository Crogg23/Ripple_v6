{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 23 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_B542229640') }}
    qualify row_number() over (partition by "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"GLOBALID"') }} as globalid,
        {{ null_junk('"OBJECTID_1"') }} as objectid_1,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ null_junk('"ACRES"') }} as acres,
        {{ null_junk('"CARTID"') }} as cartid,
        {{ null_junk('"CREATED_DATE"') }} as created_date,
        {{ null_junk('"CREATED_USER"') }} as created_user,
        {{ null_junk('"DATE_RESOL"') }} as date_resol,
        {{ null_junk('"DPWDIV"') }} as dpwdiv,
        {{ null_junk('"GREENWAY"') }} as greenway,
        {{ null_junk('"GREENWAY_I"') }} as greenway_i,
        {{ null_junk('"LABEL"') }} as label,
        {{ null_junk('"LAST_EDITED_DATE"') }} as last_edited_date,
        {{ null_junk('"LAST_EDITED_USER"') }} as last_edited_user,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"NHOOD"') }} as nhood,
        {{ null_junk('"OBJECTID"') }} as objectid,
        {{ null_junk('"PERIMETER"') }} as perimeter,
        {{ null_junk('"PGHDB_SDE_GREENWAYSCARTEGRAPH_A"') }} as pghdb_sde_greenwayscartegraph_a,
        {{ null_junk('"SHAPE_LENG"') }} as shape_leng,
        {{ null_junk('"SQMILES"') }} as sqmiles,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/greenways' as _source_url

    from source

)

select * from renamed
