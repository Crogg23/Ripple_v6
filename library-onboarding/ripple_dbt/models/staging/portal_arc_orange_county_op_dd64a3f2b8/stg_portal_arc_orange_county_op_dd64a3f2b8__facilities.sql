{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per facility (FACILITY_ID unique)
-- SPINE_ENTITY: facility  (natural_key: FACILITY_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'facility' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_ORANGE_COUNTY_OP_DD64A3F2B8') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "FACILITY_ID" as facility_id,
        "OPERATIONAL_STATUS" as operational_status,
        "NAME" as name,
        "ADDRESS" as address,
        "CITY" as city,
        "ZIP" as zip,
        "INSPECTION_DATE" as inspection_date,
        "PHONE" as phone,
        "PE" as pe,
        "PROGRAM_ELEMENT" as program_element,
        "GIS_LATITUDE" as gis_latitude,
        "GIS_LONGITUDE" as gis_longitude,
        "EXISTING_DEVELOPMENT" as existing_development,
        "MOBILE_BUSINESS" as mobile_business,
        "NAICS_ICS" as naics_ics,
        "NOI_WDID" as noi_wdid,
        "POLLUTANTS_IDENTIFICATION" as pollutants_identification,
        "ADJACENCY_TO_ESA__Y_N" as adjacency_to_esa__y_n,
        "WATER_BODY_SEGMENT_IMPAIRED__Y" as water_body_segment_impaired__y,
        "GLOBALID" as globalid,
        "CREATIONDATE" as creationdate,
        "CREATOR" as creator,
        "EDITDATE" as editdate,
        "EDITOR" as editor,
        INGESTED_AT as _loaded_at,
        'https://data-ocpw.opendata.arcgis.com/datasets/84183ad800e64572a7d253383701b3ed_7' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by facility_id order by _loaded_at desc) = 1
