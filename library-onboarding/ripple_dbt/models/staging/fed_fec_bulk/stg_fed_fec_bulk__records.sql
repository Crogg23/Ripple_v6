{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FEC_CMTE_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: FEC_CMTE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_FEC_BULK') }}

),

renamed as (

    select
        "FEC_CMTE_ID" as fec_cmte_id,
        "CMTE_NM" as cmte_nm,
        "TRES_NM" as tres_nm,
        "CMTE_ST1" as cmte_st1,
        "CMTE_ST2" as cmte_st2,
        "CMTE_CITY" as cmte_city,
        "CMTE_ST" as cmte_st,
        "CMTE_ZIP" as cmte_zip,
        "CMTE_DSGN" as cmte_dsgn,
        "CMTE_TP" as cmte_tp,
        "CMTE_PTY_AFFILIATION" as cmte_pty_affiliation,
        "CMTE_FILING_FREQ" as cmte_filing_freq,
        "ORG_TP" as org_tp,
        "CONNECTED_ORG_NM" as connected_org_nm,
        "FEC_CAND_ID" as fec_cand_id,
        _INGESTED_AT as _loaded_at,
        'https://www.fec.gov/data/browse-data/?tab=bulk-data' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by fec_cmte_id order by _loaded_at desc) = 1
