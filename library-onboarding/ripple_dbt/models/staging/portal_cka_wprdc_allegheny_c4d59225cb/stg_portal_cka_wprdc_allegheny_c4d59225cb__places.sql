{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_C4D59225CB') }}

),

renamed as (

    select
        "GLOBALID" as globalid,
        "OBJECTID_1" as objectid_1,
        "SHAPE__LENGTH" as shape__length,
        "CARTEID" as carteid,
        "CLASS" as class,
        "COUNCIL_LT" as council_lt,
        "COUNCIL_RT" as council_rt,
        "CREATED_DATE" as created_date,
        "CREATED_USER" as created_user,
        "DIR" as dir,
        "DOMI_CLASS" as domi_class,
        "DPW_ZON_LT" as dpw_zon_lt,
        "DPW_ZON_RT" as dpw_zon_rt,
        "FIRE_ZN_LT" as fire_zn_lt,
        "FIRE_ZN_RT" as fire_zn_rt,
        "FROMSTREET" as fromstreet,
        "HOOD_LEFT" as hood_left,
        "HOOD_RIGHT" as hood_right,
        "LAST_EDITED_DATE" as last_edited_date,
        "LAST_EDITED_USER" as last_edited_user,
        "MEASURLGTH" as measurlgth,
        "NAME" as name,
        "NUM_LANES" as num_lanes,
        "OBJECTID" as objectid,
        "ONEWAY" as oneway,
        "OWNER" as owner,
        "PAVECLASS" as paveclass,
        "PREFIX" as prefix,
        "REPLACED" as replaced,
        "RETIRED" as retired,
        "ROADWIDTH" as roadwidth,
        "SPEEDLIMIT" as speedlimit,
        "STREETNAME" as streetname,
        "SUFFIX" as suffix,
        "TOSTREET" as tostreet,
        "TYPE" as type,
        "VOTE_DT_LT" as vote_dt_lt,
        "VOTE_DT_RT" as vote_dt_rt,
        "WARD_LT" as ward_lt,
        "WARD_RG" as ward_rg,
        "ZIPL" as zipl,
        "ZIPR" as zipr,
        "ZONE_LEFT" as zone_left,
        "ZONE_RIGHT" as zone_right,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/pittsburgh-street-centerlines' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by geometry order by _loaded_at desc) = 1
