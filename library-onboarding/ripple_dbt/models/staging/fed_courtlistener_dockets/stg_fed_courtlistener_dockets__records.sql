{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 15 of 54 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   date_created: timestamp
--   date_modified: timestamp
--   source: integer
--   date_last_index: timestamp
--   date_filed: date
--   date_terminated: date
--   date_last_filing: date
--   ia_date_first_change: timestamp
--   view_count: integer
--   date_blocked: date
--   idb_data_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   originating_court_information_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   referred_to_id: integer
--   federal_dn_office_code: integer
--   federal_defendant_number: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_DOCKETS') }}
    qualify row_number() over (partition by "ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ stg_ts('"DATE_CREATED"') }} as date_created,
        {{ stg_ts('"DATE_MODIFIED"') }} as date_modified,
        {{ stg_int('"SOURCE"') }} as source,
        {{ null_junk('"APPEAL_FROM_STR"') }} as appeal_from_str,
        {{ null_junk('"ASSIGNED_TO_STR"') }} as assigned_to_str,
        {{ null_junk('"REFERRED_TO_STR"') }} as referred_to_str,
        {{ null_junk('"PANEL_STR"') }} as panel_str,
        {{ stg_ts('"DATE_LAST_INDEX"') }} as date_last_index,
        {{ null_junk('"DATE_CERT_GRANTED"') }} as date_cert_granted,
        {{ null_junk('"DATE_CERT_DENIED"') }} as date_cert_denied,
        {{ null_junk('"DATE_ARGUED"') }} as date_argued,
        {{ null_junk('"DATE_REARGUED"') }} as date_reargued,
        {{ null_junk('"DATE_REARGUMENT_DENIED"') }} as date_reargument_denied,
        {{ stg_date('"DATE_FILED"') }} as date_filed,
        {{ stg_date('"DATE_TERMINATED"') }} as date_terminated,
        {{ stg_date('"DATE_LAST_FILING"') }} as date_last_filing,
        {{ null_junk('"CASE_NAME_SHORT"') }} as case_name_short,
        {{ null_junk('"CASE_NAME"') }} as case_name,
        {{ null_junk('"CASE_NAME_FULL"') }} as case_name_full,
        {{ null_junk('"SLUG"') }} as slug,
        {{ null_junk('"DOCKET_NUMBER"') }} as docket_number,
        {{ null_junk('"DOCKET_NUMBER_CORE"') }} as docket_number_core,
        {{ null_junk('"PACER_CASE_ID"') }} as pacer_case_id,
        {{ null_junk('"CAUSE"') }} as cause,
        {{ null_junk('"NATURE_OF_SUIT"') }} as nature_of_suit,
        {{ null_junk('"JURY_DEMAND"') }} as jury_demand,
        {{ null_junk('"JURISDICTION_TYPE"') }} as jurisdiction_type,
        {{ null_junk('"APPELLATE_FEE_STATUS"') }} as appellate_fee_status,
        {{ null_junk('"APPELLATE_CASE_TYPE_INFORMATION"') }} as appellate_case_type_information,
        {{ null_junk('"MDL_STATUS"') }} as mdl_status,
        {{ null_junk('"FILEPATH_LOCAL"') }} as filepath_local,
        {{ null_junk('"FILEPATH_IA"') }} as filepath_ia,
        {{ null_junk('"FILEPATH_IA_JSON"') }} as filepath_ia_json,
        {{ null_junk('"IA_UPLOAD_FAILURE_COUNT"') }} as ia_upload_failure_count,
        {{ null_junk('"IA_NEEDS_UPLOAD"') }} as ia_needs_upload,
        {{ stg_ts('"IA_DATE_FIRST_CHANGE"') }} as ia_date_first_change,
        {{ stg_int('"VIEW_COUNT"') }} as view_count,
        {{ stg_date('"DATE_BLOCKED"') }} as date_blocked,
        {{ null_junk('"BLOCKED"') }} as blocked,
        {{ null_junk('"APPEAL_FROM_ID"') }} as appeal_from_id,
        {{ null_junk('"ASSIGNED_TO_ID"') }} as assigned_to_id,
        {{ null_junk('"COURT_ID"') }} as court_id,
        {{ stg_id_text('"IDB_DATA_ID"') }} as idb_data_id,
        {{ stg_id_text('"ORIGINATING_COURT_INFORMATION_ID"') }} as originating_court_information_id,
        {{ stg_int('"REFERRED_TO_ID"') }} as referred_to_id,
        {{ null_junk('"FEDERAL_DN_CASE_TYPE"') }} as federal_dn_case_type,
        {{ stg_int('"FEDERAL_DN_OFFICE_CODE"') }} as federal_dn_office_code,
        {{ null_junk('"FEDERAL_DN_JUDGE_INITIALS_ASSIGNED"') }} as federal_dn_judge_initials_assigned,
        {{ null_junk('"FEDERAL_DN_JUDGE_INITIALS_REFERRED"') }} as federal_dn_judge_initials_referred,
        {{ stg_int('"FEDERAL_DEFENDANT_NUMBER"') }} as federal_defendant_number,
        {{ null_junk('"PARENT_DOCKET_ID"') }} as parent_docket_id,
        {{ null_junk('"DOCKET_NUMBER_RAW"') }} as docket_number_raw,
        {{ null_junk('"DOCKET_NUMBER_SOURCE"') }} as docket_number_source,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
