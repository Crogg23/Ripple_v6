version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: XC_OWID_LIFE_EXPECTANCY
        description: >
          Raw landing table for OWID — Life expectancy. Grain not yet determined.

models:
  - name: stg_xc_owid_life_expectancy__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
