version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_CDC_OVERDOSE
        description: >
          Raw landing table for CDC VSRR Provisional Drug Overdose Death Counts. Grain not yet determined.

models:
  - name: stg_fed_cdc_overdose__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
