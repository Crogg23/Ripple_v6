{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_CDC_OVERDOSE') }}

),

renamed as (

    select
        STATE as state,
        YEAR as year,
        MONTH as month,
        PERIOD as period,
        INDICATOR as indicator,
        DATA_VALUE as data_value,
        PERCENT_COMPLETE as percent_complete,
        PERCENT_PENDING_INVESTIGATION as percent_pending_investigation,
        STATE_NAME as state_name,
        FOOTNOTE as footnote,
        FOOTNOTE_SYMBOL as footnote_symbol,
        PREDICTED_VALUE as predicted_value,
        INGESTED_AT as _loaded_at,
        'https://data.cdc.gov/resource/xkb8-kh2a.csv?$limit=2000000' as _source_url

    from source

)

select * from renamed
