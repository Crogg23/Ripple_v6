version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: XC_OWID_TEMP_ANOMALY
        description: >
          Raw landing table for OWID — Global temperature anomaly. Grain not yet determined.

models:
  - name: stg_xc_owid_temp_anomaly__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
