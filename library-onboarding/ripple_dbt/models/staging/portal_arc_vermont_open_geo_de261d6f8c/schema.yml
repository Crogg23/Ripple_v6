version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_VERMONT_OPEN_GEO_DE261D6F8C
        description: >
          Raw landing table for VT Schools: Public. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_vermont_open_geo_de261d6f8c__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (GEOM is unique). SPINE_ENTITY: place."
    columns:
      - name: geometry
        data_tests:
          - not_null:
              config:
                severity: warn
