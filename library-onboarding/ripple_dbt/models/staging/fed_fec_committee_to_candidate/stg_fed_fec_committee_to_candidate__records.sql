{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (SUB_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: SUB_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 23 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   image_num: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   transaction_dt: date, MMDDYYYY
--   transaction_amt: integer
--   file_num: integer
--   cycle: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_FEC_COMMITTEE_TO_CANDIDATE') }}
    qualify row_number() over (partition by "SUB_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CMTE_ID"') }} as cmte_id,
        {{ null_junk('"AMNDT_IND"') }} as amndt_ind,
        {{ null_junk('"RPT_TP"') }} as rpt_tp,
        {{ null_junk('"TRANSACTION_PGI"') }} as transaction_pgi,
        {{ stg_id_text('"IMAGE_NUM"') }} as image_num,
        {{ null_junk('"TRANSACTION_TP"') }} as transaction_tp,
        {{ null_junk('"ENTITY_TP"') }} as entity_tp,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"EMPLOYER"') }} as employer,
        {{ null_junk('"OCCUPATION"') }} as occupation,
        {{ stg_date('"TRANSACTION_DT"', 'MMDDYYYY') }} as transaction_dt,
        {{ stg_int('"TRANSACTION_AMT"') }} as transaction_amt,
        {{ null_junk('"OTHER_ID"') }} as other_id,
        {{ null_junk('"CAND_ID"') }} as cand_id,
        {{ null_junk('"TRAN_ID"') }} as tran_id,
        {{ stg_int('"FILE_NUM"') }} as file_num,
        {{ null_junk('"MEMO_CD"') }} as memo_cd,
        {{ null_junk('"MEMO_TEXT"') }} as memo_text,
        {{ null_junk('"SUB_ID"') }} as sub_id,
        {{ stg_int('"CYCLE"') }} as cycle,
        _INGESTED_AT as _loaded_at,
        'https://www.fec.gov/files/bulk-downloads/2024/pas224.zip' as _source_url

    from source

)

select * from renamed
