version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_CMS_OPEN_PAYMENTS_GNRL
        description: >
          Raw landing table for CMS Open Payments - General Payments (full program year). Grain not yet determined.

models:
  - name: stg_fed_cms_open_payments_gnrl__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
