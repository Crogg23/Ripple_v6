{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (DEP_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: DEP_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 3 of 57 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   yr_fst_prd: integer
--   yr_lst_prd: integer
--   disc_yr: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_USGS_MINERALS') }}
    qualify row_number() over (partition by "DEP_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"COUNTRY"') }} as country,
        {{ null_junk('"FIPS"') }} as fips,
        {{ null_junk('"COMMODITY"') }} as commodity,
        {{ null_junk('"YEAR"') }} as year,
        {{ null_junk('"PRODUCTION_QUANTITY"') }} as production_quantity,
        {{ null_junk('"PRODUCTION_UNIT"') }} as production_unit,
        {{ null_junk('"RESERVES"') }} as reserves,
        {{ null_junk('"RANK"') }} as rank,
        {{ null_junk('"IMPORT_RELIANCE_PCT"') }} as import_reliance_pct,
        {{ null_junk('"US_STATE"') }} as us_state,
        {{ null_junk('"VALUE_USD"') }} as value_usd,
        {{ null_junk('"DATA_SOURCE_NOTES"') }} as data_source_notes,
        {{ null_junk('"DEP_ID"') }} as dep_id,
        {{ null_junk('"URL"') }} as url,
        {{ null_junk('"MRDS_ID"') }} as mrds_id,
        {{ null_junk('"MAS_ID"') }} as mas_id,
        {{ null_junk('"SITE_NAME"') }} as site_name,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"REGION"') }} as region,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"COUNTY"') }} as county,
        {{ null_junk('"COM_TYPE"') }} as com_type,
        {{ null_junk('"COMMOD1"') }} as commod1,
        {{ null_junk('"COMMOD2"') }} as commod2,
        {{ null_junk('"COMMOD3"') }} as commod3,
        {{ null_junk('"OPER_TYPE"') }} as oper_type,
        {{ null_junk('"DEP_TYPE"') }} as dep_type,
        {{ null_junk('"PROD_SIZE"') }} as prod_size,
        {{ null_junk('"DEV_STAT"') }} as dev_stat,
        {{ null_junk('"ORE"') }} as ore,
        {{ null_junk('"GANGUE"') }} as gangue,
        {{ null_junk('"OTHER_MATL"') }} as other_matl,
        {{ null_junk('"OREBODY_FM"') }} as orebody_fm,
        {{ null_junk('"WORK_TYPE"') }} as work_type,
        {{ null_junk('"MODEL"') }} as model,
        {{ null_junk('"ALTERATION"') }} as alteration,
        {{ null_junk('"CONC_PROC"') }} as conc_proc,
        {{ null_junk('"NAMES"') }} as names,
        {{ null_junk('"ORE_CTRL"') }} as ore_ctrl,
        {{ null_junk('"REPORTER"') }} as reporter,
        {{ null_junk('"HROCK_UNIT"') }} as hrock_unit,
        {{ null_junk('"HROCK_TYPE"') }} as hrock_type,
        {{ null_junk('"AROCK_UNIT"') }} as arock_unit,
        {{ null_junk('"AROCK_TYPE"') }} as arock_type,
        {{ null_junk('"STRUCTURE"') }} as structure,
        {{ null_junk('"TECTONIC"') }} as tectonic,
        {{ null_junk('"REF"') }} as ref,
        {{ null_junk('"YFP_BA"') }} as yfp_ba,
        {{ stg_int('"YR_FST_PRD"') }} as yr_fst_prd,
        {{ null_junk('"YLP_BA"') }} as ylp_ba,
        {{ stg_int('"YR_LST_PRD"') }} as yr_lst_prd,
        {{ null_junk('"DY_BA"') }} as dy_ba,
        {{ stg_int('"DISC_YR"') }} as disc_yr,
        {{ null_junk('"PROD_YRS"') }} as prod_yrs,
        {{ null_junk('"DISCR"') }} as discr,
        {{ null_junk('"SCORE"') }} as score,
        _INGESTED_AT as _loaded_at,
        'https://www.usgs.gov/centers/national-minerals-information-center' as _source_url

    from source

)

select * from renamed
