{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (SWK_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: SWK_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_5CCB249B71') }}

),

renamed as (

    select
        "SWK_ID" as swk_id,
        "NEW_INSP_D" as new_insp_d,
        "INSP" as insp,
        "MATERIAL" as material,
        "SWK_WIDTH" as swk_width,
        "SWK_SLOPE" as swk_slope,
        "DAM_LENGTH" as dam_length,
        "DAM_WIDTH" as dam_width,
        "SCI" as sci,
        "CURB_TYPE" as curb_type,
        "CURB_REV" as curb_rev,
        "AREAWAY" as areaway,
        "NOTES" as notes,
        "ADD_NOTE" as add_note,
        "INSP_DATE" as insp_date,
        "DAM_AREA" as dam_area,
        "RCR" as rcr,
        "UTIL" as util,
        "SURVEY" as survey,
        "DISTRICT" as district,
        "SWK_AREA" as swk_area,
        "PARENT" as parent,
        "SNOW_ROUTE" as snow_route,
        "SEG_ID" as seg_id,
        "SIDE" as side,
        "ROUTE" as route,
        "INSPECTED" as inspected,
        "SHAPE_LENGTH" as shape_length,
        "SHAPE_AREA" as shape_area,
        "SHAPE_WKT" as shape_wkt,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/sidewalk-inventory' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by swk_id order by _loaded_at desc) = 1
