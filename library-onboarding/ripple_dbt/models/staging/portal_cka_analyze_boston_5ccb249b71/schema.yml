version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_5CCB249B71
        description: >
          Raw landing table for Sidewalk Inventory. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_5ccb249b71__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (SWK_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: swk_id
        data_tests:
          - unique
          - not_null
