{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (SCHOOLS_UNIVERSAL_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: SCHOOLS_UNIVERSAL_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_TUCSON_OPEN_DATA_55D5164315') }}

),

renamed as (

    select
        "OID" as oid,
        "SCHOOLS_UNIVERSAL_ID" as schools_universal_id,
        "SCHOOLS_NCES_ID" as schools_nces_id,
        "SCHOOLS_STATE_ID" as schools_state_id,
        "SCHOOLS_NAME" as schools_name,
        "SCHOOLS_SCHOOL_SUMMARY" as schools_school_summary,
        "SCHOOLS_TYPE" as schools_type,
        "SCHOOLS_LEVEL_CODES" as schools_level_codes,
        "SCHOOLS_LEVEL" as schools_level,
        "SCHOOLS_STREET" as schools_street,
        "SCHOOLS_CITY" as schools_city,
        "SCHOOLS_STATE" as schools_state,
        "SCHOOLS_FIPSCOUNTY" as schools_fipscounty,
        "SCHOOLS_ZIP" as schools_zip,
        "SCHOOLS_PHONE" as schools_phone,
        "SCHOOLS_FAX" as schools_fax,
        "SCHOOLS_COUNTY" as schools_county,
        "SCHOOLS_LAT" as schools_lat,
        "SCHOOLS_LON" as schools_lon,
        "SCHOOLS_DISTRICT_NAME" as schools_district_name,
        "SCHOOLS_DISTRICT_ID" as schools_district_id,
        "SCHOOLS_WEB_SITE" as schools_web_site,
        "SCHOOLS_OVERVIEW_URL" as schools_overview_url,
        "SCHOOLS_RATING" as schools_rating,
        "SCHOOLS_YEAR" as schools_year,
        "SCHOOLS_RATING_DESCRIPTION" as schools_rating_description,
        "SCHOOLS_DISTANCE" as schools_distance,
        "DDLAT" as ddlat,
        "DDLON" as ddlon,
        "ORIG_OID" as orig_oid,
        "SCHOOLS_RATING_NUM" as schools_rating_num,
        "SCHOOL_RATING_TEXT" as school_rating_text,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://gisdata.tucsonaz.gov/datasets/7e84e5d3c27649739b643e4274f0b9c3_1' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by schools_universal_id order by _loaded_at desc) = 1
