{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (SCHOOLS_UNIVERSAL_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: SCHOOLS_UNIVERSAL_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 10 of 33 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   oid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   schools_fipscounty: integer
--   schools_district_id: integer
--   schools_rating: integer
--   schools_year: integer
--   schools_distance: float
--   ddlat: float
--   ddlon: float
--   orig_oid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   schools_rating_num: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_TUCSON_OPEN_DATA_55D5164315') }}
    qualify row_number() over (partition by "SCHOOLS_UNIVERSAL_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OID"') }} as oid,
        {{ null_junk('"SCHOOLS_UNIVERSAL_ID"') }} as schools_universal_id,
        {{ null_junk('"SCHOOLS_NCES_ID"') }} as schools_nces_id,
        {{ null_junk('"SCHOOLS_STATE_ID"') }} as schools_state_id,
        {{ null_junk('"SCHOOLS_NAME"') }} as schools_name,
        {{ null_junk('"SCHOOLS_SCHOOL_SUMMARY"') }} as schools_school_summary,
        {{ null_junk('"SCHOOLS_TYPE"') }} as schools_type,
        {{ null_junk('"SCHOOLS_LEVEL_CODES"') }} as schools_level_codes,
        {{ null_junk('"SCHOOLS_LEVEL"') }} as schools_level,
        {{ null_junk('"SCHOOLS_STREET"') }} as schools_street,
        {{ null_junk('"SCHOOLS_CITY"') }} as schools_city,
        {{ null_junk('"SCHOOLS_STATE"') }} as schools_state,
        {{ stg_int('"SCHOOLS_FIPSCOUNTY"') }} as schools_fipscounty,
        {{ null_junk('"SCHOOLS_ZIP"') }} as schools_zip,
        {{ null_junk('"SCHOOLS_PHONE"') }} as schools_phone,
        {{ null_junk('"SCHOOLS_FAX"') }} as schools_fax,
        {{ null_junk('"SCHOOLS_COUNTY"') }} as schools_county,
        {{ null_junk('"SCHOOLS_LAT"') }} as schools_lat,
        {{ null_junk('"SCHOOLS_LON"') }} as schools_lon,
        {{ null_junk('"SCHOOLS_DISTRICT_NAME"') }} as schools_district_name,
        {{ stg_int('"SCHOOLS_DISTRICT_ID"') }} as schools_district_id,
        {{ null_junk('"SCHOOLS_WEB_SITE"') }} as schools_web_site,
        {{ null_junk('"SCHOOLS_OVERVIEW_URL"') }} as schools_overview_url,
        {{ stg_int('"SCHOOLS_RATING"') }} as schools_rating,
        {{ stg_int('"SCHOOLS_YEAR"') }} as schools_year,
        {{ null_junk('"SCHOOLS_RATING_DESCRIPTION"') }} as schools_rating_description,
        {{ stg_float('"SCHOOLS_DISTANCE"') }} as schools_distance,
        {{ stg_float('"DDLAT"') }} as ddlat,
        {{ stg_float('"DDLON"') }} as ddlon,
        {{ stg_id_text('"ORIG_OID"') }} as orig_oid,
        {{ stg_int('"SCHOOLS_RATING_NUM"') }} as schools_rating_num,
        {{ null_junk('"SCHOOL_RATING_TEXT"') }} as school_rating_text,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://gisdata.tucsonaz.gov/datasets/7e84e5d3c27649739b643e4274f0b9c3_1' as _source_url

    from source

)

select * from renamed
