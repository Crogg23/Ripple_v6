version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_FTC_DATASETS
        description: >
          Raw landing table for FTC Enforcement Actions and Data Sets. Grain not yet determined.

models:
  - name: stg_fed_ftc_datasets__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
