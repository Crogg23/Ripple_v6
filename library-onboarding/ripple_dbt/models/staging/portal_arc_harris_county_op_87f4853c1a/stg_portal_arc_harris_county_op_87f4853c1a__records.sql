{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 8 of 39 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid_1: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   population: integer
--   countyfips: integer
--   sourcedate: integer
--   val_date: integer
--   ttl_staff: integer
--   beds: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_HARRIS_COUNTY_OP_87F4853C1A') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID_1"') }} as objectid_1,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"ZIP4"') }} as zip4,
        {{ null_junk('"TELEPHONE"') }} as telephone,
        {{ null_junk('"TYPE"') }} as type,
        {{ null_junk('"STATUS"') }} as status,
        {{ stg_int('"POPULATION"') }} as population,
        {{ null_junk('"COUNTY"') }} as county,
        {{ stg_int('"COUNTYFIPS"') }} as countyfips,
        {{ null_junk('"COUNTRY"') }} as country,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"NAICS_DESC"') }} as naics_desc,
        {{ null_junk('"SOURCE"') }} as source,
        {{ stg_int('"SOURCEDATE"') }} as sourcedate,
        {{ null_junk('"VAL_METHOD"') }} as val_method,
        {{ stg_int('"VAL_DATE"') }} as val_date,
        {{ null_junk('"WEBSITE"') }} as website,
        {{ null_junk('"STATE_ID"') }} as state_id,
        {{ null_junk('"ALT_NAME"') }} as alt_name,
        {{ null_junk('"ST_FIPS"') }} as st_fips,
        {{ null_junk('"OWNER"') }} as owner,
        {{ stg_int('"TTL_STAFF"') }} as ttl_staff,
        {{ stg_int('"BEDS"') }} as beds,
        {{ null_junk('"TRAUMA"') }} as trauma,
        {{ null_junk('"HELIPAD"') }} as helipad,
        {{ null_junk('"GENERATOR_ONSITE"') }} as generator_onsite,
        {{ null_junk('"SELF_SUFFICIENT_ELECTRICITY"') }} as self_sufficient_electricity,
        {{ null_junk('"IN_100_YR_FLOODPLAIN"') }} as in_100_yr_floodplain,
        {{ null_junk('"IN_500_YR_FLOODPLAIN"') }} as in_500_yr_floodplain,
        {{ null_junk('"IN_SURGE_SLOSH_AREA"') }} as in_surge_slosh_area,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://geo-harriscounty.opendata.arcgis.com/datasets/1b25d08de73b4c62b0b057f1f79a0c76_5' as _source_url

    from source

)

select * from renamed
