{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ENROLLMENT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ENROLLMENT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 2 of 39 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   incorporation_date: date
--   reh_conversion_date: date
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_CMS_HOSPITAL_ENROLLMENTS') }}
    qualify row_number() over (partition by "ENROLLMENT_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ENROLLMENT_ID"') }} as enrollment_id,
        {{ null_junk('"ENROLLMENT_STATE"') }} as enrollment_state,
        {{ null_junk('"PROVIDER_TYPE_CODE"') }} as provider_type_code,
        {{ null_junk('"PROVIDER_TYPE_TEXT"') }} as provider_type_text,
        {{ null_junk('"NPI"') }} as npi,
        {{ null_junk('"MULTIPLE_NPI_FLAG"') }} as multiple_npi_flag,
        {{ null_junk('"CCN"') }} as ccn,
        {{ null_junk('"ASSOCIATE_ID"') }} as associate_id,
        {{ null_junk('"ORGANIZATION_NAME"') }} as organization_name,
        {{ null_junk('"DOING_BUSINESS_AS_NAME"') }} as doing_business_as_name,
        {{ stg_date('"INCORPORATION_DATE"') }} as incorporation_date,
        {{ null_junk('"INCORPORATION_STATE"') }} as incorporation_state,
        {{ null_junk('"ORGANIZATION_TYPE_STRUCTURE"') }} as organization_type_structure,
        {{ null_junk('"ORGANIZATION_OTHER_TYPE_TEXT"') }} as organization_other_type_text,
        {{ null_junk('"PROPRIETARY_NONPROFIT"') }} as proprietary_nonprofit,
        {{ null_junk('"ADDRESS_LINE_1"') }} as address_line_1,
        {{ null_junk('"ADDRESS_LINE_2"') }} as address_line_2,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"PRACTICE_LOCATION_TYPE"') }} as practice_location_type,
        {{ null_junk('"LOCATION_OTHER_TYPE_TEXT"') }} as location_other_type_text,
        {{ null_junk('"SUBGROUP_GENERAL"') }} as subgroup_general,
        {{ null_junk('"SUBGROUP_ACUTE_CARE"') }} as subgroup_acute_care,
        {{ null_junk('"SUBGROUP_ALCOHOL_DRUG"') }} as subgroup_alcohol_drug,
        {{ null_junk('"SUBGROUP_CHILDRENS"') }} as subgroup_childrens,
        {{ null_junk('"SUBGROUP_LONG_TERM"') }} as subgroup_long_term,
        {{ null_junk('"SUBGROUP_PSYCHIATRIC"') }} as subgroup_psychiatric,
        {{ null_junk('"SUBGROUP_REHABILITATION"') }} as subgroup_rehabilitation,
        {{ null_junk('"SUBGROUP_SHORT_TERM"') }} as subgroup_short_term,
        {{ null_junk('"SUBGROUP_SWING_BED_APPROVED"') }} as subgroup_swing_bed_approved,
        {{ null_junk('"SUBGROUP_PSYCHIATRIC_UNIT"') }} as subgroup_psychiatric_unit,
        {{ null_junk('"SUBGROUP_REHABILITATION_UNIT"') }} as subgroup_rehabilitation_unit,
        {{ null_junk('"SUBGROUP_SPECIALTY_HOSPITAL"') }} as subgroup_specialty_hospital,
        {{ null_junk('"SUBGROUP_OTHER"') }} as subgroup_other,
        {{ null_junk('"SUBGROUP_OTHER_TEXT"') }} as subgroup_other_text,
        {{ null_junk('"REH_CONVERSION_FLAG"') }} as reh_conversion_flag,
        {{ stg_date('"REH_CONVERSION_DATE"') }} as reh_conversion_date,
        {{ null_junk('"CAH_OR_HOSPITAL_CCN"') }} as cah_or_hospital_ccn,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
