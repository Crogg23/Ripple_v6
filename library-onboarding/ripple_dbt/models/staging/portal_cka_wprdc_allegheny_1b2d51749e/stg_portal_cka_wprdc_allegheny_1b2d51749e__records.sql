{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CLOSURE_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: CLOSURE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 11 of 26 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   from_date: date
--   to_date: date
--   active: boolean
--   full_closure: boolean
--   travel_lane: boolean
--   parking_lane: boolean
--   metered_parking: boolean
--   sidewalk: boolean
--   segment_num: integer
--   total_segments: integer
--   roadway_id: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_1B2D51749E') }}
    qualify row_number() over (partition by "CLOSURE_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CLOSURE_ID"') }} as closure_id,
        {{ null_junk('"PERMIT_ID"') }} as permit_id,
        {{ null_junk('"PERMIT_TYPE"') }} as permit_type,
        {{ null_junk('"WORK_DESCRIPTION"') }} as work_description,
        {{ null_junk('"WORK_TYPE"') }} as work_type,
        {{ null_junk('"APPLICANT_NAME"') }} as applicant_name,
        {{ null_junk('"CONTRACTOR_NAME"') }} as contractor_name,
        {{ null_junk('"SPECIAL_INSTRUCTIONS"') }} as special_instructions,
        {{ null_junk('"WEEKDAY_HOURS"') }} as weekday_hours,
        {{ null_junk('"WEEKEND_HOURS"') }} as weekend_hours,
        {{ stg_date('"FROM_DATE"') }} as from_date,
        {{ stg_date('"TO_DATE"') }} as to_date,
        {{ stg_bool('"ACTIVE"') }} as active,
        {{ null_junk('"PRIMARY_STREET"') }} as primary_street,
        {{ null_junk('"FROM_STREET"') }} as from_street,
        {{ null_junk('"TO_STREET"') }} as to_street,
        {{ stg_bool('"FULL_CLOSURE"') }} as full_closure,
        {{ stg_bool('"TRAVEL_LANE"') }} as travel_lane,
        {{ stg_bool('"PARKING_LANE"') }} as parking_lane,
        {{ stg_bool('"METERED_PARKING"') }} as metered_parking,
        {{ stg_bool('"SIDEWALK"') }} as sidewalk,
        {{ stg_int('"SEGMENT_NUM"') }} as segment_num,
        {{ stg_int('"TOTAL_SEGMENTS"') }} as total_segments,
        {{ stg_int('"ROADWAY_ID"') }} as roadway_id,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        {{ null_junk('"WKT"') }} as wkt,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/street-closures' as _source_url

    from source

)

select * from renamed
