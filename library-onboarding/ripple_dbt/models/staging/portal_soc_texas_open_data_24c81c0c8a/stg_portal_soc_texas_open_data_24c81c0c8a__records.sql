{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TWC_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TWC_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 14 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   wst_status_cd_dt: timestamp
--   origin_cd: integer
--   wst_radioact_flg: boolean
--   new_chem_subst_flg: boolean
--   wst_mgmt_loc_cd: boolean
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_TEXAS_OPEN_DATA_24C81C0C8A') }}
    qualify row_number() over (partition by "TWC_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SWR_NUM_TXT"') }} as swr_num_txt,
        {{ null_junk('"TX_WST_CD"') }} as tx_wst_cd,
        {{ null_junk('"EPA_FORM_CD"') }} as epa_form_cd,
        {{ null_junk('"WST_STATUS_CD"') }} as wst_status_cd,
        {{ stg_ts('"WST_STATUS_CD_DT"') }} as wst_status_cd_dt,
        {{ null_junk('"WST_DESC_TXT"') }} as wst_desc_txt,
        {{ stg_int('"ORIGIN_CD"') }} as origin_cd,
        {{ null_junk('"WST_SOURCE_CD"') }} as wst_source_cd,
        {{ null_junk('"NAICS_CD"') }} as naics_cd,
        {{ stg_bool('"WST_RADIOACT_FLG"') }} as wst_radioact_flg,
        {{ stg_bool('"NEW_CHEM_SUBST_FLG"') }} as new_chem_subst_flg,
        {{ null_junk('"TWC_ID"') }} as twc_id,
        {{ stg_bool('"WST_MGMT_LOC_CD"') }} as wst_mgmt_loc_cd,
        {{ null_junk('"SYS_TYP_CD"') }} as sys_typ_cd,
        INGESTED_AT as _loaded_at,
        'https://data.texas.gov/d/bwem-j8ee' as _source_url

    from source

)

select * from renamed
