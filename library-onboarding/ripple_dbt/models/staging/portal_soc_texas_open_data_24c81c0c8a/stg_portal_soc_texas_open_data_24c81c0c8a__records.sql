{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TWC_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TWC_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_TEXAS_OPEN_DATA_24C81C0C8A') }}

),

renamed as (

    select
        "SWR_NUM_TXT" as swr_num_txt,
        "TX_WST_CD" as tx_wst_cd,
        "EPA_FORM_CD" as epa_form_cd,
        "WST_STATUS_CD" as wst_status_cd,
        "WST_STATUS_CD_DT" as wst_status_cd_dt,
        "WST_DESC_TXT" as wst_desc_txt,
        "ORIGIN_CD" as origin_cd,
        "WST_SOURCE_CD" as wst_source_cd,
        "NAICS_CD" as naics_cd,
        "WST_RADIOACT_FLG" as wst_radioact_flg,
        "NEW_CHEM_SUBST_FLG" as new_chem_subst_flg,
        "TWC_ID" as twc_id,
        "WST_MGMT_LOC_CD" as wst_mgmt_loc_cd,
        "SYS_TYP_CD" as sys_typ_cd,
        INGESTED_AT as _loaded_at,
        'https://data.texas.gov/d/bwem-j8ee' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by twc_id order by _loaded_at desc) = 1
