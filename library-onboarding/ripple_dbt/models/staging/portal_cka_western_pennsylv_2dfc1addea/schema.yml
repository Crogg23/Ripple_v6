version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_2DFC1ADDEA
        description: >
          Raw landing table for Allegheny County Fatal Accidental Overdoses. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_2dfc1addea__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+DEATH_DATE_AND_TIME unique). SPINE_ENTITY: place."
    columns:
      - name: decedent_zip
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: death_date_and_time
        data_tests:
          - not_null:
              config:
                severity: warn
