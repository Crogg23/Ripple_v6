{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INDICENT_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: INDICENT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_CALIFORNIA_OPEN_7F65D27DB7') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "UTILITYCOMPANY" as utilitycompany,
        "START_DATE_TIME" as start_date_time,
        "ESTIMATED_RESTORATION_DATE_TIME" as estimated_restoration_date_time,
        "CAUSE" as cause,
        "IMPACTED_CUSTOMERS" as impacted_customers,
        "COUNTY" as county,
        "OUTAGE_STATUS" as outage_status,
        "OUTAGE_TYPE" as outage_type,
        "GLOBALID" as globalid,
        "OUTAGETYPECOLOR" as outagetypecolor,
        "OUTAGESTATUSCOLOR" as outagestatuscolor,
        "INDICENT_ID" as indicent_id,
        "X" as x,
        "Y" as y,
        INGESTED_AT as _loaded_at,
        'https://data.ca.gov/dataset/power-outage-incidents' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by indicent_id order by _loaded_at desc) = 1
