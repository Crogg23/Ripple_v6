version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_51B8DCF278
        description: >
          Raw landing table for City of Pittsburgh Property Tax Abatements. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_51b8dcf278__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (LATLON+START_YEAR+APPROVED_DATE unique). SPINE_ENTITY: place."
    columns:
      - name: latitude
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: start_year
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: approved_date
        data_tests:
          - not_null:
              config:
                severity: warn
