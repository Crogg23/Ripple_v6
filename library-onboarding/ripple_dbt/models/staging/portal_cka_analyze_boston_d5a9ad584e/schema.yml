version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_D5A9AD584E
        description: >
          Raw landing table for Zoning Reform Impact Tracker. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_d5a9ad584e__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+PERMIT_APPLICATION_FILING_DATE+PERMIT_ISSUED_DATE+REZONING_EFFECTIVE_DATE+PARCEL_ID+SAM_ID unique). SPINE_ENTITY: place."
    columns:
      - name: zip_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: permit_application_filing_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: permit_issued_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: rezoning_effective_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: parcel_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: sam_id
        data_tests:
          - not_null:
              config:
                severity: warn
