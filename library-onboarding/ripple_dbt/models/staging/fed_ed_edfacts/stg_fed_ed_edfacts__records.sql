{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_ED_EDFACTS') }}

),

renamed as (

    select
        SCHOOL_YEAR as school_year,
        STATE_FIPS as state_fips,
        LEA_ID as lea_id,
        SCHOOL_ID as school_id,
        AGENCY_NAME as agency_name,
        SCHOOL_NAME as school_name,
        GRADE_SPAN as grade_span,
        TOTAL_ENROLLMENT as total_enrollment,
        SUBGROUP as subgroup,
        PROGRAM_PARTICIPATION as program_participation,
        PROFICIENCY_LEVEL as proficiency_level,
        ASSESSMENT_SUBJECT as assessment_subject,
        GRADUATION_RATE as graduation_rate,
        CHRONIC_ABSENTEEISM_RATE as chronic_absenteeism_rate,
        SEA_ID as sea_id,
        _INGESTED_AT as _loaded_at,
        'https://www.ed.gov/data/edfacts-initiative' as _source_url

    from source

)

select * from renamed
