{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE__LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (SHAPE__LENGTH) is NOT unique within a load as landed at generation time -- it would collapse 99,760 of 197,984 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_OPEN_DATA_SA_58B0401C9A') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "CARTID" as cartid,
        "MSAG_NAME" as msag_name,
        "FROMSTREET" as fromstreet,
        "TOSTREET" as tostreet,
        "JURISDICTION" as jurisdiction,
        "DISTRICT" as district,
        "LENGTHFEET" as lengthfeet,
        "INSTALLDATE" as installdate,
        "PCI" as pci,
        "ROW_TYPE" as row_type,
        "SURFACE_TYPE" as surface_type,
        "PAVEMENTWIDTH" as pavementwidth,
        "SPEEDLIMIT" as speedlimit,
        "ONEWAY" as oneway,
        "OWNER" as owner,
        "ROADFUNCTION" as roadfunction,
        "MAINTENANCERESPONSIBILITY" as maintenanceresponsibility,
        "STATUS" as status,
        "STAGE" as stage,
        "SHAPE__LENGTH" as shape__length,
        INGESTED_AT as _loaded_at,
        'https://data.sanantonio.gov/dataset/pavements' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by objectid, cartid, msag_name, fromstreet, tostreet, jurisdiction, district, lengthfeet, installdate, pci, row_type, surface_type, pavementwidth, speedlimit, oneway, owner, roadfunction, maintenanceresponsibility, status, stage, shape__length order by _loaded_at desc) = 1
