{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_FARA') }}

),

renamed as (

    select
        REGISTRANT_NAME as registrant_name,
        REGISTRATION_NUMBER as registration_number,
        FOREIGN_PRINCIPAL as foreign_principal,
        FOREIGN_PRINCIPAL_COUNTRY as foreign_principal_country,
        EIN as ein,
        REGISTRATION_DATE as registration_date,
        FILING_TYPE as filing_type,
        DOCUMENT_URL as document_url,
        TERMINATION_DATE as termination_date,
        ACTIVITY_DESCRIPTION as activity_description,
        RECEIPTS_AMOUNT as receipts_amount,
        DISBURSEMENTS_AMOUNT as disbursements_amount,
        PERIOD_START as period_start,
        PERIOD_END as period_end,
        _INGESTED_AT as _loaded_at,
        'https://www.justice.gov/nsd-fara' as _source_url

    from source

)

select * from renamed
