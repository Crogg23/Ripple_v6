version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_GOVINFO_BILL_COSPONSORS
        description: >
          Raw landing table for GovInfo BILLSTATUS -- Cosponsor Extract. Grain not yet determined.

models:
  - name: stg_fed_govinfo_bill_cosponsors__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
