version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_UTAH_OPEN_DATA_P_1614522F52
        description: >
          Raw landing table for DWQUPDESDischargers_data. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_utah_open_data_p_1614522f52__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (NPDES_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: npdes_id
        data_tests:
          - unique
          - not_null
