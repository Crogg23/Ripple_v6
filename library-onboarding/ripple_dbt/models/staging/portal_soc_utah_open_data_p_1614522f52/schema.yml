version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_UTAH_OPEN_DATA_P_1614522F52
        description: >
          Raw landing table for DWQUPDESDischargers_data. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_utah_open_data_p_1614522f52__facilities
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per facility (NPDES_ID is unique). SPINE_ENTITY: facility."
    columns:
      - name: npdes_id
        data_tests:
          - not_null:
              config:
                severity: warn
