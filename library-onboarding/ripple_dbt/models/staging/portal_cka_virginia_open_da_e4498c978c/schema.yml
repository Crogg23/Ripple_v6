version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_VIRGINIA_OPEN_DA_E4498C978C
        description: >
          Raw landing table for PREP Reports. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_virginia_open_da_e4498c978c__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PRP_REPORT_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: prp_report_id
        data_tests:
          - unique
          - not_null
