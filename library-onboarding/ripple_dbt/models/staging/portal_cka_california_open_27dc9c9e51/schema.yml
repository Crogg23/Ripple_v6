version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_CALIFORNIA_OPEN_27DC9C9E51
        description: >
          Raw landing table for California Fire Perimeters (all). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_california_open_27dc9c9e51__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (GEOM is unique). SPINE_ENTITY: place."
    columns:
      - name: shape__length
        data_tests:
          - not_null:
              config:
                severity: warn
