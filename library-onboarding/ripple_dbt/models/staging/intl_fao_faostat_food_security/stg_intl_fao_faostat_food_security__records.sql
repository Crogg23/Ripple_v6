{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'INTL_FAO_FAOSTAT_FOOD_SECURITY') }}

),

renamed as (

    select
        AREA_CODE as area_code,
        AREA_CODE_M49 as area_code_m49,
        AREA as area,
        ITEM_CODE as item_code,
        ITEM as item,
        ELEMENT_CODE as element_code,
        ELEMENT as element,
        YEAR_CODE as year_code,
        YEAR as year,
        UNIT as unit,
        VALUE as value,
        FLAG as flag,
        NOTE as note,
        INGESTED_AT as _loaded_at,
        'https://bulks-faostat.fao.org/production/Food_Security_Data_E_All_Data_(Normalized).zip' as _source_url

    from source

)

select * from renamed
