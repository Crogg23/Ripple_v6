{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INSPECTION_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: INSPECTION_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 31 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   inspection_date: date
--   plan_id: integer
--   inspect_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   dt_insp: date
--   inspector_id: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (INSPECTION_ID) is NOT unique within a load as landed at generation time -- it would collapse 176,719 of 195,963 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_DDB61776D0') }}
    qualify row_number() over (partition by "INSPECTION_ID", "PERMIT_ID", "INSPECTION_DATE", "ADDRESS", "CITY", "STATE", "ZIP_CODE", "MUNICIPAL", "BUILDING_TYPE", "PLUMBER_NAME", "FINAL_INSP_YN", "STORM_DRAIN", "BUILDING_DRAIN", "WATER_SERVICE", "STORM_SEWER", "ROUGH_ABOVE_GROUND", "WATER_HEATER", "BUILDING_SEWER", "WATER_DISTRIBUTION", "INDIRECT_WASTE", "PLAN_ID", "PLAN_NUMB", "INSPECT_ID", "DT_INSP", "INSPECTOR_ID", "INSP_ST_TM", "INSP_END_TM", "VIOLATIONS_FLAG", "SECTION_ID", "VIOLATION", "DESCRIPTION" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"INSPECTION_ID"') }} as inspection_id,
        {{ null_junk('"PERMIT_ID"') }} as permit_id,
        {{ stg_date('"INSPECTION_DATE"') }} as inspection_date,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"MUNICIPAL"') }} as municipal,
        {{ null_junk('"BUILDING_TYPE"') }} as building_type,
        {{ null_junk('"PLUMBER_NAME"') }} as plumber_name,
        {{ null_junk('"FINAL_INSP_YN"') }} as final_insp_yn,
        {{ null_junk('"STORM_DRAIN"') }} as storm_drain,
        {{ null_junk('"BUILDING_DRAIN"') }} as building_drain,
        {{ null_junk('"WATER_SERVICE"') }} as water_service,
        {{ null_junk('"STORM_SEWER"') }} as storm_sewer,
        {{ null_junk('"ROUGH_ABOVE_GROUND"') }} as rough_above_ground,
        {{ null_junk('"WATER_HEATER"') }} as water_heater,
        {{ null_junk('"BUILDING_SEWER"') }} as building_sewer,
        {{ null_junk('"WATER_DISTRIBUTION"') }} as water_distribution,
        {{ null_junk('"INDIRECT_WASTE"') }} as indirect_waste,
        {{ stg_int('"PLAN_ID"') }} as plan_id,
        {{ null_junk('"PLAN_NUMB"') }} as plan_numb,
        {{ stg_id_text('"INSPECT_ID"') }} as inspect_id,
        {{ stg_date('"DT_INSP"') }} as dt_insp,
        {{ stg_int('"INSPECTOR_ID"') }} as inspector_id,
        {{ null_junk('"INSP_ST_TM"') }} as insp_st_tm,
        {{ null_junk('"INSP_END_TM"') }} as insp_end_tm,
        {{ null_junk('"VIOLATIONS_FLAG"') }} as violations_flag,
        {{ null_junk('"SECTION_ID"') }} as section_id,
        {{ null_junk('"VIOLATION"') }} as violation,
        {{ null_junk('"DESCRIPTION"') }} as description,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-plumbing-inspections' as _source_url

    from source

)

select * from renamed
