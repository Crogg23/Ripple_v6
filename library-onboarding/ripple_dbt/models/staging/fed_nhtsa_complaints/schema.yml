version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_NHTSA_COMPLAINTS
        description: >
          Raw landing table for NHTSA ODI Consumer Complaints (flat file). Grain not yet determined.

models:
  - name: stg_fed_nhtsa_complaints__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
