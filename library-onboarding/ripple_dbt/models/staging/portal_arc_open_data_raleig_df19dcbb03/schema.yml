version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_OPEN_DATA_RALEIG_DF19DCBB03
        description: >
          Raw landing table for InfoGroup Business Listings. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_open_data_raleig_df19dcbb03__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INFOUSA_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: infousa_id
        data_tests:
          - unique
          - not_null
