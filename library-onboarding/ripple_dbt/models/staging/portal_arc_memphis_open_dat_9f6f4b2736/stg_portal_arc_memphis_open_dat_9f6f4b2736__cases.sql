{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per case (DOCKET is unique)
-- SPINE_ENTITY: case  (natural_key: DOCKET)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'case' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_MEMPHIS_OPEN_DAT_9F6F4B2736') }}

),

renamed as (

    select
        "FID" as fid,
        "CASETYPE" as casetype,
        "YEAR" as year,
        "CASENUM" as casenum,
        "DOCKET" as docket,
        "ATLAS_GRID" as atlas_grid,
        "CASE_RULIN" as case_rulin,
        "RULINGDATE" as rulingdate,
        "COMMENTS" as comments,
        "ADDRESS" as address,
        "CREATED_US" as created_us,
        "CREATED_DA" as created_da,
        "LAST_EDITE" as last_edite,
        "LAST_EDI_1" as last_edi_1,
        "GLOBALID" as globalid,
        "SHAPE_STAR" as shape_star,
        "SHAPE_STLE" as shape_stle,
        "SHAPE__AREA" as shape__area,
        "SHAPE__LENGTH" as shape__length,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.memphistn.gov/datasets/da86bc8da68640c1922e36b9ed1aa1ff_0' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by docket order by _loaded_at desc) = 1
