version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_9CCBEFBACC
        description: >
          Raw landing table for Pennsylvania Vacant Addresses. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_9ccbefbacc__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FIPS+QUARTER unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: geoid
        data_tests:
          - not_null
      - name: quarter
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - geoid
            - quarter
