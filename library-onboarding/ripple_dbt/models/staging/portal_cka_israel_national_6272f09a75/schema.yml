version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ISRAEL_NATIONAL_6272F09A75
        description: >
          Raw landing table for רשימת תאגידים בנקאיים בישראל - כתובות וקישורים. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_israel_national_6272f09a75__places
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+CATEGORY+BANK_CODE unique). SPINE_ENTITY: place."
    columns:
      - name: zip_code
        data_tests:
          - not_null
      - name: category
        data_tests:
          - not_null
      - name: bank_code
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - zip_code
            - category
            - bank_code
