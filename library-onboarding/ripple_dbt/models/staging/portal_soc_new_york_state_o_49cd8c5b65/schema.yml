version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_NEW_YORK_STATE_O_49CD8C5B65
        description: >
          Raw landing table for US Patents Issued to Health Research Inc on Behalf of Roswell Park Cancer Institute. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_new_york_state_o_49cd8c5b65__assets
    description: "GRAIN: one row per asset (PATENT is unique). SPINE_ENTITY: asset."
    columns:
      - name: patent_number
        data_tests:
          - unique
          - not_null
