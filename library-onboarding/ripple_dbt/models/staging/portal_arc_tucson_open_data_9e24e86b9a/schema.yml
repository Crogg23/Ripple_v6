version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_TUCSON_OPEN_DATA_9E24E86B9A
        description: >
          Raw landing table for O3 unlimitedres parcels. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_tucson_open_data_9e24e86b9a__cases
    description: "GRAIN: one row per case (DOCKET is unique). SPINE_ENTITY: case."
    columns:
      - name: docket
        data_tests:
          - unique
          - not_null
