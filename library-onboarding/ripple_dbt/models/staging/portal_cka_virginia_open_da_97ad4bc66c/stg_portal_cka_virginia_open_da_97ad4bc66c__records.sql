{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CORE_PARENT_SYS_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: CORE_PARENT_SYS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 9 of 32 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   upc: integer
--   year_of_completion: integer
--   county_mp_from: float
--   county_mp_to: float
--   state_mp_from: float
--   state_mp_to: float
--   lane_miles: float
--   aadt: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (CORE_PARENT_SYS_ID) is NOT unique within a load as landed at generation time -- it would collapse 23 of 4,457 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_97AD4BC66C') }}
    qualify row_number() over (partition by "OBJECTID", "CORE_PARENT_SYS_ID", "UPC", "DISTRICT_NAME", "SCHEDULE", "SYSTEM", "YEAR_OF_COMPLETION", "COUNTY_NAME", "ROUTE_NAME", "ROUTE_COMMON_NAME", "LANE_DIRECTION", "COUNTY_MP_FROM", "COUNTY_MP_TO", "FROM_DESCRIPTION", "TO_DESCRIPTION", "STATE_MP_FROM", "STATE_MP_TO", "STREET_NAMES", "LANE_MILES", "PMS_TREATMENT_TYPE", "AADT", "STATE_PROJECT_NUMBER", "MISC", "PROJECT_MANAGER", "NT_LOGIN", "TELEPHONE", "EMAIL", "PROJECT_STATUS", "CREATIONDATE", "CREATOR", "EDITDATE", "EDITOR" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"CORE_PARENT_SYS_ID"') }} as core_parent_sys_id,
        {{ stg_int('"UPC"') }} as upc,
        {{ null_junk('"DISTRICT_NAME"') }} as district_name,
        {{ null_junk('"SCHEDULE"') }} as schedule,
        {{ null_junk('"SYSTEM"') }} as system,
        {{ stg_int('"YEAR_OF_COMPLETION"') }} as year_of_completion,
        {{ null_junk('"COUNTY_NAME"') }} as county_name,
        {{ null_junk('"ROUTE_NAME"') }} as route_name,
        {{ null_junk('"ROUTE_COMMON_NAME"') }} as route_common_name,
        {{ null_junk('"LANE_DIRECTION"') }} as lane_direction,
        {{ stg_float('"COUNTY_MP_FROM"') }} as county_mp_from,
        {{ stg_float('"COUNTY_MP_TO"') }} as county_mp_to,
        {{ null_junk('"FROM_DESCRIPTION"') }} as from_description,
        {{ null_junk('"TO_DESCRIPTION"') }} as to_description,
        {{ stg_float('"STATE_MP_FROM"') }} as state_mp_from,
        {{ stg_float('"STATE_MP_TO"') }} as state_mp_to,
        {{ null_junk('"STREET_NAMES"') }} as street_names,
        {{ stg_float('"LANE_MILES"') }} as lane_miles,
        {{ null_junk('"PMS_TREATMENT_TYPE"') }} as pms_treatment_type,
        {{ stg_int('"AADT"') }} as aadt,
        {{ null_junk('"STATE_PROJECT_NUMBER"') }} as state_project_number,
        {{ null_junk('"MISC"') }} as misc,
        {{ null_junk('"PROJECT_MANAGER"') }} as project_manager,
        {{ null_junk('"NT_LOGIN"') }} as nt_login,
        {{ null_junk('"TELEPHONE"') }} as telephone,
        {{ null_junk('"EMAIL"') }} as email,
        {{ null_junk('"PROJECT_STATUS"') }} as project_status,
        {{ null_junk('"CREATIONDATE"') }} as creationdate,
        {{ null_junk('"CREATOR"') }} as creator,
        {{ null_junk('"EDITDATE"') }} as editdate,
        {{ null_junk('"EDITOR"') }} as editor,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/statewide-paving-status-map-public-view-2026' as _source_url

    from source

)

select * from renamed
