{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CORE_PARENT_SYS_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: CORE_PARENT_SYS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_97AD4BC66C') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "CORE_PARENT_SYS_ID" as core_parent_sys_id,
        "UPC" as upc,
        "DISTRICT_NAME" as district_name,
        "SCHEDULE" as schedule,
        "SYSTEM" as system,
        "YEAR_OF_COMPLETION" as year_of_completion,
        "COUNTY_NAME" as county_name,
        "ROUTE_NAME" as route_name,
        "ROUTE_COMMON_NAME" as route_common_name,
        "LANE_DIRECTION" as lane_direction,
        "COUNTY_MP_FROM" as county_mp_from,
        "COUNTY_MP_TO" as county_mp_to,
        "FROM_DESCRIPTION" as from_description,
        "TO_DESCRIPTION" as to_description,
        "STATE_MP_FROM" as state_mp_from,
        "STATE_MP_TO" as state_mp_to,
        "STREET_NAMES" as street_names,
        "LANE_MILES" as lane_miles,
        "PMS_TREATMENT_TYPE" as pms_treatment_type,
        "AADT" as aadt,
        "STATE_PROJECT_NUMBER" as state_project_number,
        "MISC" as misc,
        "PROJECT_MANAGER" as project_manager,
        "NT_LOGIN" as nt_login,
        "TELEPHONE" as telephone,
        "EMAIL" as email,
        "PROJECT_STATUS" as project_status,
        "CREATIONDATE" as creationdate,
        "CREATOR" as creator,
        "EDITDATE" as editdate,
        "EDITOR" as editor,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/statewide-paving-status-map-public-view-2026' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by core_parent_sys_id order by _loaded_at desc) = 1
