{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LONGITUDE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 8 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_58CF45D167') }}
    qualify row_number() over (partition by "LONGITUDE" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"HOSPITAL"') }} as hospital,
        {{ null_junk('"FULL_ADDRESS"') }} as full_address,
        {{ null_junk('"BED_SIZE_CATEGORY"') }} as bed_size_category,
        {{ null_junk('"HOSPITAL_SYSTEM"') }} as hospital_system,
        {{ null_junk('"MEDICAL_SCHOOL_AFFILIATION"') }} as medical_school_affiliation,
        {{ null_junk('"VDH_HEALTH_PLANNING_REGION"') }} as vdh_health_planning_region,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/vdh-pud-hai-acute-map-of-acute-care-hospitals-in-virginia' as _source_url

    from source

)

select * from renamed
