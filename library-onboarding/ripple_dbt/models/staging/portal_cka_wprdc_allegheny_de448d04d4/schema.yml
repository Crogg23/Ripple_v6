version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_DE448D04D4
        description: >
          Raw landing table for toxic-release-inventory. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_de448d04d4__facilities
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per facility (FRS_ID+REPORTING_YEAR+FIPS+ZIP+TRI_FACILITY_ID+TRI_CHEM_ID+SRS_ID unique). SPINE_ENTITY: facility."
    columns:
      - name: epa_registry_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: reporting_year
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: state_county_fips_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: zip_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: tri_facility_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: tri_chem_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: srs_id
        data_tests:
          - not_null:
              config:
                severity: warn
