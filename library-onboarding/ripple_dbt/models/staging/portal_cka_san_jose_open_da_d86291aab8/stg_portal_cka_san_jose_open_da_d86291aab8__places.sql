{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 36 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   encumid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   intid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   levelmin: integer
--   levelmax: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_D86291AAB8') }}
    qualify row_number() over (partition by "SHAPE_LENGTH" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CREATIONDATE"') }} as creationdate,
        {{ null_junk('"CREATOR"') }} as creator,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_id_text('"ENCUMID"') }} as encumid,
        {{ stg_id_text('"INTID"') }} as intid,
        {{ null_junk('"ENCUMTYPE"') }} as encumtype,
        {{ null_junk('"ENCUMSUBTYPE"') }} as encumsubtype,
        {{ null_junk('"EASEMENT"') }} as easement,
        {{ null_junk('"TAXLINE"') }} as taxline,
        {{ null_junk('"LOTLINE"') }} as lotline,
        {{ null_junk('"TRACTLINE"') }} as tractline,
        {{ null_junk('"PLANLINE"') }} as planline,
        {{ null_junk('"ROADLINE"') }} as roadline,
        {{ null_junk('"RAILLINE"') }} as railline,
        {{ null_junk('"BUILDINGLINE"') }} as buildingline,
        {{ null_junk('"WATERLINE"') }} as waterline,
        {{ null_junk('"MUNICIPALLINE"') }} as municipalline,
        {{ null_junk('"CENTERLINE"') }} as centerline,
        {{ null_junk('"GCELINE"') }} as gceline,
        {{ null_junk('"LCELINE"') }} as lceline,
        {{ null_junk('"OTHERLINE"') }} as otherline,
        {{ null_junk('"CLOSINGLINE"') }} as closingline,
        {{ null_junk('"CARTOGRAPHICLINE"') }} as cartographicline,
        {{ null_junk('"CONSTRUCTIONLINE"') }} as constructionline,
        {{ null_junk('"CONFLICTLINE"') }} as conflictline,
        {{ stg_int('"LEVELMIN"') }} as levelmin,
        {{ stg_int('"LEVELMAX"') }} as levelmax,
        {{ null_junk('"ROWWIDTH"') }} as rowwidth,
        {{ null_junk('"PLANCRT"') }} as plancrt,
        {{ null_junk('"PLANMOD"') }} as planmod,
        {{ null_junk('"LASTUPDATE"') }} as lastupdate,
        {{ null_junk('"LASTEDITOR"') }} as lasteditor,
        {{ null_junk('"NOTES"') }} as notes,
        {{ null_junk('"GLOBALID"') }} as globalid,
        {{ null_junk('"ENTERPRISEID"') }} as enterpriseid,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/freeway' as _source_url

    from source

)

select * from renamed
