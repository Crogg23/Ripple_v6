{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_D86291AAB8') }}

),

renamed as (

    select
        "CREATIONDATE" as creationdate,
        "CREATOR" as creator,
        "OBJECTID" as objectid,
        "ENCUMID" as encumid,
        "INTID" as intid,
        "ENCUMTYPE" as encumtype,
        "ENCUMSUBTYPE" as encumsubtype,
        "EASEMENT" as easement,
        "TAXLINE" as taxline,
        "LOTLINE" as lotline,
        "TRACTLINE" as tractline,
        "PLANLINE" as planline,
        "ROADLINE" as roadline,
        "RAILLINE" as railline,
        "BUILDINGLINE" as buildingline,
        "WATERLINE" as waterline,
        "MUNICIPALLINE" as municipalline,
        "CENTERLINE" as centerline,
        "GCELINE" as gceline,
        "LCELINE" as lceline,
        "OTHERLINE" as otherline,
        "CLOSINGLINE" as closingline,
        "CARTOGRAPHICLINE" as cartographicline,
        "CONSTRUCTIONLINE" as constructionline,
        "CONFLICTLINE" as conflictline,
        "LEVELMIN" as levelmin,
        "LEVELMAX" as levelmax,
        "ROWWIDTH" as rowwidth,
        "PLANCRT" as plancrt,
        "PLANMOD" as planmod,
        "LASTUPDATE" as lastupdate,
        "LASTEDITOR" as lasteditor,
        "NOTES" as notes,
        "GLOBALID" as globalid,
        "ENTERPRISEID" as enterpriseid,
        "SHAPE_LENGTH" as shape_length,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/freeway' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by shape_length order by _loaded_at desc) = 1
