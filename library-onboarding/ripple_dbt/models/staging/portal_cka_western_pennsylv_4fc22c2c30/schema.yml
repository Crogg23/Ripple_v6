version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_4FC22C2C30
        description: >
          Raw landing table for Allegheny County Asbestos Permits. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_4fc22c2c30__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+PERMIT_ISSUE_DATE+PERMIT_EXPIRE_DATE+PROJECT_TYPE+PARCEL_ID unique). SPINE_ENTITY: place."
    columns:
      - name: zip_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: permit_issue_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: permit_expire_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: project_type
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: parcel_id
        data_tests:
          - not_null:
              config:
                severity: warn
