version: 2

models:
  - name: stg_fed_google_polads_creative_stats__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (AD_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: ad_id
        data_tests:
          - unique
          - not_null
