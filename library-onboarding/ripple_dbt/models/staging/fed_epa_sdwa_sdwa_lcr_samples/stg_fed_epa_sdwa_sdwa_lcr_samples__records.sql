{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (SAR_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: SAR_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_SDWA_SDWA_LCR_SAMPLES') }}

),

renamed as (

    select
        "SUBMISSIONYEARQUARTER" as submissionyearquarter,
        "PWSID" as pwsid,
        "SAMPLE_ID" as sample_id,
        "SAMPLING_END_DATE" as sampling_end_date,
        "SAMPLING_START_DATE" as sampling_start_date,
        "RECONCILIATION_ID" as reconciliation_id,
        "SAMPLE_FIRST_REPORTED_DATE" as sample_first_reported_date,
        "SAMPLE_LAST_REPORTED_DATE" as sample_last_reported_date,
        "SAR_ID" as sar_id,
        "CONTAMINANT_CODE" as contaminant_code,
        "RESULT_SIGN_CODE" as result_sign_code,
        "SAMPLE_MEASURE" as sample_measure,
        "UNIT_OF_MEASURE" as unit_of_measure,
        "SAR_FIRST_REPORTED_DATE" as sar_first_reported_date,
        "SAR_LAST_REPORTED_DATE" as sar_last_reported_date,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by sar_id order by _loaded_at desc) = 1
