{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (NPI+HCPCS_CODE unique)
-- SPINE_ENTITY: (not determined)  (natural_key: NPI, HCPCS_CODE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_UTAH_OPEN_DATA_P_55EF6EF0C6') }}

),

renamed as (

    select
        "NPI" as npi,
        "IND_PAC_ID" as ind_pac_id,
        "LST_NM" as lst_nm,
        "FRST_NM" as frst_nm,
        "SPEC" as spec,
        "PRAC_ST" as prac_st,
        "HCPCS_CODE" as hcpcs_code,
        "HCPCS_DESCRIPTION" as hcpcs_description,
        "LINE_SRVC_CNT" as line_srvc_cnt,
        "BENE_CNT" as bene_cnt,
        INGESTED_AT as _loaded_at,
        'https://opendata.utah.gov/d/har7-cn9s' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by npi, hcpcs_code order by _loaded_at desc) = 1
