version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_UTAH_OPEN_DATA_P_55EF6EF0C6
        description: >
          Raw landing table for Utah Clinician Utilization Data 2019. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_utah_open_data_p_55ef6ef0c6__providers
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (NPI+HCPCS_CODE unique). SPINE_ENTITY: provider."
    columns:
      - name: npi
        data_tests:
          - not_null
      - name: hcpcs_code
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - npi
              - hcpcs_code