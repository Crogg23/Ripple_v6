version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_TEXAS_OPEN_DATA_D83872D208
        description: >
          Raw landing table for Texas Commission on Environmental Quality - Civil Judgments Issued. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_texas_open_data_d83872d208__cases
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per case (DOCKET+ORDER_DATE unique). SPINE_ENTITY: case."
    columns:
      - name: district_court_docket_no
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: order_date
        data_tests:
          - not_null:
              config:
                severity: warn
