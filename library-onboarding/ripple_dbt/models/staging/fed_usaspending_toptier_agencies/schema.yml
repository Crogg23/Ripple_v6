version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_USASPENDING_TOPTIER_AGENCIES
        description: >
          Raw landing table for USAspending Top-Tier Federal Agencies. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_fed_usaspending_toptier_agencies__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (AGENCY_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: agency_id
        data_tests:
          - unique
          - not_null
