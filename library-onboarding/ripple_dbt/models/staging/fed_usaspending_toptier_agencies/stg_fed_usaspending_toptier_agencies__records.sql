{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (AGENCY_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: AGENCY_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_USASPENDING_TOPTIER_AGENCIES') }}

),

renamed as (

    select
        "AGENCY_ID" as agency_id,
        "TOPTIER_CODE" as toptier_code,
        "ABBREVIATION" as abbreviation,
        "AGENCY_NAME" as agency_name,
        "CONGRESSIONAL_JUSTIFICATION_URL" as congressional_justification_url,
        "ACTIVE_FY" as active_fy,
        "ACTIVE_FQ" as active_fq,
        "OUTLAY_AMOUNT" as outlay_amount,
        "OBLIGATED_AMOUNT" as obligated_amount,
        "BUDGET_AUTHORITY_AMOUNT" as budget_authority_amount,
        "CURRENT_TOTAL_BUDGET_AUTHORITY_AMOUNT" as current_total_budget_authority_amount,
        "PERCENTAGE_OF_TOTAL_BUDGET_AUTHORITY" as percentage_of_total_budget_authority,
        "AGENCY_SLUG" as agency_slug,
        _INGESTED_AT as _loaded_at,
        'https://api.usaspending.gov/api/v2/references/toptier_agencies/' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by agency_id order by _loaded_at desc) = 1
