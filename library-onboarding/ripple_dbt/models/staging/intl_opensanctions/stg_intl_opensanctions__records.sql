{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 3 of 16 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   first_seen: timestamp
--   last_seen: timestamp
--   last_change: timestamp
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'INTL_OPENSANCTIONS') }}
    qualify row_number() over (partition by "ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"SCHEMA"') }} as schema,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"ALIASES"') }} as aliases,
        {{ null_junk('"BIRTH_DATE"') }} as birth_date,
        {{ null_junk('"COUNTRIES"') }} as countries,
        {{ null_junk('"ADDRESSES"') }} as addresses,
        {{ null_junk('"IDENTIFIERS"') }} as identifiers,
        {{ null_junk('"SANCTIONS"') }} as sanctions,
        {{ null_junk('"PHONES"') }} as phones,
        {{ null_junk('"EMAILS"') }} as emails,
        {{ null_junk('"PROGRAM_IDS"') }} as program_ids,
        {{ null_junk('"DATASET"') }} as dataset,
        {{ stg_ts('"FIRST_SEEN"') }} as first_seen,
        {{ stg_ts('"LAST_SEEN"') }} as last_seen,
        {{ stg_ts('"LAST_CHANGE"') }} as last_change,
        _INGESTED_AT as _loaded_at,
        'https://www.opensanctions.org/datasets/sanctions/' as _source_url

    from source

)

select * from renamed
