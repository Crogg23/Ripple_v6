{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GEOM+DATE_DESIGNATED_1+CREATED_DATE+LAST_EDITED_DATE+UNIQUE_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: SHAPE_WKT, DATE_DESIGNATED_1, CREATED_DATE, LAST_EDITED_DATE, UNIQUE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_5288DB6955') }}

),

renamed as (

    select
        "UNIQUE_ID" as unique_id,
        "NEIGHBORHOOD" as neighborhood,
        "COMMON_NAME" as common_name,
        "STATUS" as status,
        "AREAS_UNDER_JURISDICTION" as areas_under_jurisdiction,
        "PETITIONER" as petitioner,
        "STUDY_REPORT" as study_report,
        "DISPLAY" as display,
        "DEMOLISHED" as demolished,
        "DATE_DESIGNATED_1" as date_designated_1,
        "CREATED_USER" as created_user,
        "CREATED_DATE" as created_date,
        "LAST_EDITED_USER" as last_edited_user,
        "LAST_EDITED_DATE" as last_edited_date,
        "SHAPE_LENGTH" as shape_length,
        "SHAPE_AREA" as shape_area,
        "SHAPE_WKT" as shape_wkt,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/boston-landmarks-commission-blc-landmarks' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by shape_wkt, date_designated_1, created_date, last_edited_date, unique_id order by _loaded_at desc) = 1
