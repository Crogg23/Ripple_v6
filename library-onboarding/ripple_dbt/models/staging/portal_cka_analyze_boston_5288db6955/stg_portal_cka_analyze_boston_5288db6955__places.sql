{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM+DATE_DESIGNATED_1+CREATED_DATE+LAST_EDITED_DATE+UNIQUE_ID unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_WKT, DATE_DESIGNATED_1, CREATED_DATE, LAST_EDITED_DATE, UNIQUE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 1 of 17 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   last_edited_user: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_5288DB6955') }}
    qualify row_number() over (partition by "SHAPE_WKT", "DATE_DESIGNATED_1", "CREATED_DATE", "LAST_EDITED_DATE", "UNIQUE_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"UNIQUE_ID"') }} as unique_id,
        {{ null_junk('"NEIGHBORHOOD"') }} as neighborhood,
        {{ null_junk('"COMMON_NAME"') }} as common_name,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"AREAS_UNDER_JURISDICTION"') }} as areas_under_jurisdiction,
        {{ null_junk('"PETITIONER"') }} as petitioner,
        {{ null_junk('"STUDY_REPORT"') }} as study_report,
        {{ null_junk('"DISPLAY"') }} as display,
        {{ null_junk('"DEMOLISHED"') }} as demolished,
        {{ null_junk('"DATE_DESIGNATED_1"') }} as date_designated_1,
        {{ null_junk('"CREATED_USER"') }} as created_user,
        {{ null_junk('"CREATED_DATE"') }} as created_date,
        {{ stg_int('"LAST_EDITED_USER"') }} as last_edited_user,
        {{ null_junk('"LAST_EDITED_DATE"') }} as last_edited_date,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        {{ null_junk('"SHAPE_AREA"') }} as shape_area,
        {{ null_junk('"SHAPE_WKT"') }} as shape_wkt,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/boston-landmarks-commission-blc-landmarks' as _source_url

    from source

)

select * from renamed
