{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per facility (PWSID+VISIT_DATE+FIRST_REPORTED_DATE+LAST_REPORTED_DATE unique)
-- SPINE_ENTITY: facility  (natural_key: PWSID, VISIT_DATE, FIRST_REPORTED_DATE, LAST_REPORTED_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 20 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'facility' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- DEDUPE: registry natural_key (PWSID, VISIT_DATE, FIRST_REPORTED_DATE, LAST_REPORTED_DATE) is NOT unique within a load as landed at generation time -- it would collapse 47,541 of 2,495,249 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_SDWA_SDWA_SITE_VISITS') }}
    qualify row_number() over (partition by "SUBMISSIONYEARQUARTER", "PWSID", "VISIT_ID", "VISIT_DATE", "AGENCY_TYPE_CODE", "VISIT_REASON_CODE", "MANAGEMENT_OPS_EVAL_CODE", "SOURCE_WATER_EVAL_CODE", "SECURITY_EVAL_CODE", "PUMPS_EVAL_CODE", "OTHER_EVAL_CODE", "COMPLIANCE_EVAL_CODE", "DATA_VERIFICATION_EVAL_CODE", "TREATMENT_EVAL_CODE", "FINISHED_WATER_STOR_EVAL_CODE", "DISTRIBUTION_EVAL_CODE", "FINANCIAL_EVAL_CODE", "VISIT_COMMENTS", "FIRST_REPORTED_DATE", "LAST_REPORTED_DATE" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SUBMISSIONYEARQUARTER"') }} as submissionyearquarter,
        {{ null_junk('"PWSID"') }} as pwsid,
        {{ null_junk('"VISIT_ID"') }} as visit_id,
        {{ null_junk('"VISIT_DATE"') }} as visit_date,
        {{ null_junk('"AGENCY_TYPE_CODE"') }} as agency_type_code,
        {{ null_junk('"VISIT_REASON_CODE"') }} as visit_reason_code,
        {{ null_junk('"MANAGEMENT_OPS_EVAL_CODE"') }} as management_ops_eval_code,
        {{ null_junk('"SOURCE_WATER_EVAL_CODE"') }} as source_water_eval_code,
        {{ null_junk('"SECURITY_EVAL_CODE"') }} as security_eval_code,
        {{ null_junk('"PUMPS_EVAL_CODE"') }} as pumps_eval_code,
        {{ null_junk('"OTHER_EVAL_CODE"') }} as other_eval_code,
        {{ null_junk('"COMPLIANCE_EVAL_CODE"') }} as compliance_eval_code,
        {{ null_junk('"DATA_VERIFICATION_EVAL_CODE"') }} as data_verification_eval_code,
        {{ null_junk('"TREATMENT_EVAL_CODE"') }} as treatment_eval_code,
        {{ null_junk('"FINISHED_WATER_STOR_EVAL_CODE"') }} as finished_water_stor_eval_code,
        {{ null_junk('"DISTRIBUTION_EVAL_CODE"') }} as distribution_eval_code,
        {{ null_junk('"FINANCIAL_EVAL_CODE"') }} as financial_eval_code,
        {{ null_junk('"VISIT_COMMENTS"') }} as visit_comments,
        {{ null_junk('"FIRST_REPORTED_DATE"') }} as first_reported_date,
        {{ null_junk('"LAST_REPORTED_DATE"') }} as last_reported_date,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
