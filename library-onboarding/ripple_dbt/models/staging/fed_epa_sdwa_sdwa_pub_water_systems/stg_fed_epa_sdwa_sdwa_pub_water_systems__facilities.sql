{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per facility (PWSID is unique)
-- SPINE_ENTITY: facility  (natural_key: PWSID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 15 of 51 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   pws_deactivation_date: date
--   dbpr_schedule_cat_code: integer
--   lt2_schedule_cat_code: integer
--   population_served_count: integer
--   pop_cat_2_code: integer
--   pop_cat_3_code: integer
--   pop_cat_4_code: integer
--   pop_cat_5_code: integer
--   pop_cat_11_code: integer
--   service_connections_count: integer
--   first_reported_date: date
--   last_reported_date: date
--   source_protection_begin_date: date
--   outstanding_perform_begin_date: date
--   reduced_monitoring_begin_date: date

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_SDWA_SDWA_PUB_WATER_SYSTEMS') }}
    qualify row_number() over (partition by "PWSID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SUBMISSIONYEARQUARTER"') }} as submissionyearquarter,
        {{ null_junk('"PWSID"') }} as pwsid,
        {{ null_junk('"PWS_NAME"') }} as pws_name,
        {{ null_junk('"PRIMACY_AGENCY_CODE"') }} as primacy_agency_code,
        {{ null_junk('"EPA_REGION"') }} as epa_region,
        {{ null_junk('"SEASON_BEGIN_DATE"') }} as season_begin_date,
        {{ null_junk('"SEASON_END_DATE"') }} as season_end_date,
        {{ null_junk('"PWS_ACTIVITY_CODE"') }} as pws_activity_code,
        {{ stg_date('"PWS_DEACTIVATION_DATE"') }} as pws_deactivation_date,
        {{ null_junk('"PWS_TYPE_CODE"') }} as pws_type_code,
        {{ stg_int('"DBPR_SCHEDULE_CAT_CODE"') }} as dbpr_schedule_cat_code,
        {{ null_junk('"CDS_ID"') }} as cds_id,
        {{ null_junk('"GW_SW_CODE"') }} as gw_sw_code,
        {{ stg_int('"LT2_SCHEDULE_CAT_CODE"') }} as lt2_schedule_cat_code,
        {{ null_junk('"OWNER_TYPE_CODE"') }} as owner_type_code,
        {{ stg_int('"POPULATION_SERVED_COUNT"') }} as population_served_count,
        {{ stg_int('"POP_CAT_2_CODE"') }} as pop_cat_2_code,
        {{ stg_int('"POP_CAT_3_CODE"') }} as pop_cat_3_code,
        {{ stg_int('"POP_CAT_4_CODE"') }} as pop_cat_4_code,
        {{ stg_int('"POP_CAT_5_CODE"') }} as pop_cat_5_code,
        {{ stg_int('"POP_CAT_11_CODE"') }} as pop_cat_11_code,
        {{ null_junk('"PRIMACY_TYPE"') }} as primacy_type,
        {{ null_junk('"PRIMARY_SOURCE_CODE"') }} as primary_source_code,
        {{ null_junk('"IS_GRANT_ELIGIBLE_IND"') }} as is_grant_eligible_ind,
        {{ null_junk('"IS_WHOLESALER_IND"') }} as is_wholesaler_ind,
        {{ null_junk('"IS_SCHOOL_OR_DAYCARE_IND"') }} as is_school_or_daycare_ind,
        {{ stg_int('"SERVICE_CONNECTIONS_COUNT"') }} as service_connections_count,
        {{ null_junk('"SUBMISSION_STATUS_CODE"') }} as submission_status_code,
        {{ null_junk('"ORG_NAME"') }} as org_name,
        {{ null_junk('"ADMIN_NAME"') }} as admin_name,
        {{ null_junk('"EMAIL_ADDR"') }} as email_addr,
        {{ null_junk('"PHONE_NUMBER"') }} as phone_number,
        {{ null_junk('"PHONE_EXT_NUMBER"') }} as phone_ext_number,
        {{ null_junk('"FAX_NUMBER"') }} as fax_number,
        {{ null_junk('"ALT_PHONE_NUMBER"') }} as alt_phone_number,
        {{ null_junk('"ADDRESS_LINE1"') }} as address_line1,
        {{ null_junk('"ADDRESS_LINE2"') }} as address_line2,
        {{ null_junk('"CITY_NAME"') }} as city_name,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"COUNTRY_CODE"') }} as country_code,
        {{ stg_date('"FIRST_REPORTED_DATE"') }} as first_reported_date,
        {{ stg_date('"LAST_REPORTED_DATE"') }} as last_reported_date,
        {{ null_junk('"STATE_CODE"') }} as state_code,
        {{ null_junk('"SOURCE_WATER_PROTECTION_CODE"') }} as source_water_protection_code,
        {{ stg_date('"SOURCE_PROTECTION_BEGIN_DATE"') }} as source_protection_begin_date,
        {{ null_junk('"OUTSTANDING_PERFORMER"') }} as outstanding_performer,
        {{ stg_date('"OUTSTANDING_PERFORM_BEGIN_DATE"') }} as outstanding_perform_begin_date,
        {{ null_junk('"REDUCED_RTCR_MONITORING"') }} as reduced_rtcr_monitoring,
        {{ stg_date('"REDUCED_MONITORING_BEGIN_DATE"') }} as reduced_monitoring_begin_date,
        {{ null_junk('"REDUCED_MONITORING_END_DATE"') }} as reduced_monitoring_end_date,
        {{ null_junk('"SEASONAL_STARTUP_SYSTEM"') }} as seasonal_startup_system,
        _INGESTED_AT as _loaded_at,
        '' as _source_url,
    'ENT_' || LEFT(MD5('PWSID' || '|' || (CASE WHEN LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("PWSID"), '[^0-9A-Za-z]', ''))) = 9 THEN UPPER(REGEXP_REPLACE(TO_VARCHAR("PWSID"), '[^0-9A-Za-z]', '')) ELSE NULL END)), 16) as spine_entity_id  -- joins LIBRARY_META."CONNECT".ENTITY_GOLDEN.ENTITY_ID

    from source

)

select * from renamed
