version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_CDC_SUICIDE_RATES
        description: >
          Raw landing table for CDC NCHS Death rates for suicide by demographics. Grain not yet determined.

models:
  - name: stg_fed_cdc_suicide_rates__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
