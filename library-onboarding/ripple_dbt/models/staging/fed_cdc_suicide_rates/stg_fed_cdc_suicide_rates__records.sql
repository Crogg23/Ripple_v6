{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_CDC_SUICIDE_RATES') }}

),

renamed as (

    select
        INDICATOR as indicator,
        UNIT as unit,
        UNIT_NUM as unit_num,
        STUB_NAME as stub_name,
        STUB_NAME_NUM as stub_name_num,
        STUB_LABEL as stub_label,
        STUB_LABEL_NUM as stub_label_num,
        YEAR as year,
        YEAR_NUM as year_num,
        AGE as age,
        AGE_NUM as age_num,
        ESTIMATE as estimate,
        FLAG as flag,
        INGESTED_AT as _loaded_at,
        'https://data.cdc.gov/resource/9j2v-jamp.csv?$limit=2000000' as _source_url

    from source

)

select * from renamed
