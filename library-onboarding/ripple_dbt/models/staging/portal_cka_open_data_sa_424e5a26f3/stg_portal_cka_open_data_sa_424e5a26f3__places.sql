{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE__LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 3 of 13 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   district: integer
--   length: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (SHAPE__LENGTH) is NOT unique within a load as landed at generation time -- it would collapse 24 of 3,597 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_OPEN_DATA_SA_424E5A26F3') }}
    qualify row_number() over (partition by "OBJECTID", "CARTID", "BIKEFACILITYTYPE", "MSAG_NAME", "PROJECTNAME", "JURISDICTION", "DISTRICT", "LENGTH", "MAINTENANCERESPONSIBILITY", "STATUS", "FACILITYNAME", "COMMENTS", "SHAPE__LENGTH" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"CARTID"') }} as cartid,
        {{ null_junk('"BIKEFACILITYTYPE"') }} as bikefacilitytype,
        {{ null_junk('"MSAG_NAME"') }} as msag_name,
        {{ null_junk('"PROJECTNAME"') }} as projectname,
        {{ null_junk('"JURISDICTION"') }} as jurisdiction,
        {{ stg_int('"DISTRICT"') }} as district,
        {{ stg_int('"LENGTH"') }} as length,
        {{ null_junk('"MAINTENANCERESPONSIBILITY"') }} as maintenanceresponsibility,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"FACILITYNAME"') }} as facilityname,
        {{ null_junk('"COMMENTS"') }} as comments,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        INGESTED_AT as _loaded_at,
        'https://data.sanantonio.gov/dataset/bike-facilities' as _source_url

    from source

)

select * from renamed
