{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_DB1AEFD5B6') }}

),

renamed as (

    select
        "ID" as id,
        "BUSINESS_NAME" as business_name,
        "ADDRESS" as address,
        "ADDRESS_ENTERED_MANUALLY" as address_entered_manually,
        "X_COORD" as x_coord,
        "Y_COORD" as y_coord,
        "BUSINESS_TYPE" as business_type,
        "BUSINESS_SUBTYPE" as business_subtype,
        "VACANT" as vacant,
        "WINDOW_SIZE" as window_size,
        "BUSINESS_SIZE" as business_size,
        "FLOOR_NUMBER" as floor_number,
        "MULTIPLE_FLOORS" as multiple_floors,
        "BUILDING_TYPE" as building_type,
        "FACING_DIRECTION" as facing_direction,
        "OWN_PARKING_LOT" as own_parking_lot,
        "LOADING_DOCK" as loading_dock,
        "ART" as art,
        "ART_STATE" as art_state,
        "ACCESS_INFO" as access_info,
        "DOOR_OPEN_BUTTON" as door_open_button,
        "ADA_RAMP" as ada_ramp,
        "FLAT_ENTRANCE" as flat_entrance,
        "CURRENT_STATE" as current_state,
        "BOARDED_UP" as boarded_up,
        "UNDER_CONSTRUCTION" as under_construction,
        "ACTIVE_LEASE" as active_lease,
        "CREATION_DATE" as creation_date,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/business-inventory' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by id order by _loaded_at desc) = 1
