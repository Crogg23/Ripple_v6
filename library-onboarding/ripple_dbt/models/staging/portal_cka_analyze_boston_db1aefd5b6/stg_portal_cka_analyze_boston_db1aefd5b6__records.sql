{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 3 of 28 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   x_coord: float
--   y_coord: float
--   creation_date: timestamp
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_DB1AEFD5B6') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"BUSINESS_NAME"') }} as business_name,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"ADDRESS_ENTERED_MANUALLY"') }} as address_entered_manually,
        {{ stg_float('"X_COORD"') }} as x_coord,
        {{ stg_float('"Y_COORD"') }} as y_coord,
        {{ null_junk('"BUSINESS_TYPE"') }} as business_type,
        {{ null_junk('"BUSINESS_SUBTYPE"') }} as business_subtype,
        {{ null_junk('"VACANT"') }} as vacant,
        {{ null_junk('"WINDOW_SIZE"') }} as window_size,
        {{ null_junk('"BUSINESS_SIZE"') }} as business_size,
        {{ null_junk('"FLOOR_NUMBER"') }} as floor_number,
        {{ null_junk('"MULTIPLE_FLOORS"') }} as multiple_floors,
        {{ null_junk('"BUILDING_TYPE"') }} as building_type,
        {{ null_junk('"FACING_DIRECTION"') }} as facing_direction,
        {{ null_junk('"OWN_PARKING_LOT"') }} as own_parking_lot,
        {{ null_junk('"LOADING_DOCK"') }} as loading_dock,
        {{ null_junk('"ART"') }} as art,
        {{ null_junk('"ART_STATE"') }} as art_state,
        {{ null_junk('"ACCESS_INFO"') }} as access_info,
        {{ null_junk('"DOOR_OPEN_BUTTON"') }} as door_open_button,
        {{ null_junk('"ADA_RAMP"') }} as ada_ramp,
        {{ null_junk('"FLAT_ENTRANCE"') }} as flat_entrance,
        {{ null_junk('"CURRENT_STATE"') }} as current_state,
        {{ null_junk('"BOARDED_UP"') }} as boarded_up,
        {{ null_junk('"UNDER_CONSTRUCTION"') }} as under_construction,
        {{ null_junk('"ACTIVE_LEASE"') }} as active_lease,
        {{ stg_ts('"CREATION_DATE"') }} as creation_date,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/business-inventory' as _source_url

    from source

)

select * from renamed
