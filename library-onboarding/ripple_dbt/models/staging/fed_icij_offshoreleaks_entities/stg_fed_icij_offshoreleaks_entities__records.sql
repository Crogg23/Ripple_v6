{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (NODE_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: NODE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 4 of 21 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   internal_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   inactivation_date: date
--   struck_off_date: date
--   dorm_date: date
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'FED_ICIJ_OFFSHORELEAKS_ENTITIES') }}
    qualify row_number() over (partition by "NODE_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"NODE_ID"') }} as node_id,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"ORIGINAL_NAME"') }} as original_name,
        {{ null_junk('"FORMER_NAME"') }} as former_name,
        {{ null_junk('"JURISDICTION"') }} as jurisdiction,
        {{ null_junk('"JURISDICTION_DESCRIPTION"') }} as jurisdiction_description,
        {{ null_junk('"COMPANY_TYPE"') }} as company_type,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ stg_id_text('"INTERNAL_ID"') }} as internal_id,
        {{ null_junk('"INCORPORATION_DATE"') }} as incorporation_date,
        {{ stg_date('"INACTIVATION_DATE"') }} as inactivation_date,
        {{ stg_date('"STRUCK_OFF_DATE"') }} as struck_off_date,
        {{ stg_date('"DORM_DATE"') }} as dorm_date,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"SERVICE_PROVIDER"') }} as service_provider,
        {{ null_junk('"IBCRUC"') }} as ibcruc,
        {{ null_junk('"COUNTRY_CODES"') }} as country_codes,
        {{ null_junk('"COUNTRIES"') }} as countries,
        {{ null_junk('"SOURCEID"') }} as sourceid,
        {{ null_junk('"VALID_UNTIL"') }} as valid_until,
        {{ null_junk('"NOTE"') }} as note,
        INGESTED_AT as _loaded_at,
        'https://offshoreleaks.icij.org/pages/database' as _source_url

    from source

)

select * from renamed
