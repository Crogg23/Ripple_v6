version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_CALIFORNIA_OPEN_A5D78A8B63
        description: >
          Raw landing table for DWR Continuous Data Download Links. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_california_open_a5d78a8b63__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (LATLON+LAST_MODIFIED_DATE+DATE_DATA_REFERS_TO+DWR_GW_SITE_CODE unique). SPINE_ENTITY: place."
    columns:
      - name: latitude
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: last_modified_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: date_data_refers_to
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: dwr_gw_site_code
        data_tests:
          - not_null:
              config:
                severity: warn
