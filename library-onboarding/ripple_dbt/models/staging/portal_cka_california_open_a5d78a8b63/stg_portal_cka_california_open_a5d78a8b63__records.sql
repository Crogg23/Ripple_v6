{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (LATLON+LAST_MODIFIED_DATE+DATE_DATA_REFERS_TO+DWR_GW_SITE_CODE unique)
-- SPINE_ENTITY: (not determined)  (natural_key: LATITUDE, LAST_MODIFIED_DATE, DATE_DATA_REFERS_TO, DWR_GW_SITE_CODE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_CALIFORNIA_OPEN_A5D78A8B63') }}

),

renamed as (

    select
        "COUNTY" as county,
        "STATION_SHORT_NAME" as station_short_name,
        "STATION_NAME" as station_name,
        "CDEC_ID" as cdec_id,
        "POSITIONAL_ACCURACY" as positional_accuracy,
        "DWR_GW_SITE_CODE" as dwr_gw_site_code,
        "STATION_TYPE" as station_type,
        "LONGITUDE" as longitude,
        "COMMENTS" as comments,
        "LLDATUM" as lldatum,
        "STATION_NUMBER" as station_number,
        "LAST_MODIFIED_DATE" as last_modified_date,
        "LATITUDE" as latitude,
        "LAST_MODIFIED_BY" as last_modified_by,
        "SOURCE" as source,
        "DATE_DATA_REFERS_TO" as date_data_refers_to,
        INGESTED_AT as _loaded_at,
        'https://data.ca.gov/dataset/dwr-continuous-data-download-links' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by latitude, last_modified_date, date_data_refers_to, dwr_gw_site_code order by _loaded_at desc) = 1
