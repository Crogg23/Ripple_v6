version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_NIH_REPORTER
        description: >
          Raw landing table for NIH RePORTER API. Grain not yet determined.

models:
  - name: stg_fed_nih_reporter__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
