{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_NIH_REPORTER') }}

),

renamed as (

    select
        APPL_ID as appl_id,
        SUBPROJECT_ID as subproject_id,
        FISCAL_YEAR as fiscal_year,
        PROJECT_NUM as project_num,
        CORE_PROJECT_NUM as core_project_num,
        ORG_NAME as org_name,
        ORG_CITY as org_city,
        ORG_STATE as org_state,
        ORG_STATE_NAME as org_state_name,
        ORG_COUNTRY as org_country,
        ORG_DUNS as org_duns,
        ORG_UEI as org_uei,
        ORG_IPF_CODE as org_ipf_code,
        ORG_ZIP as org_zip,
        ORG_FIPS as org_fips,
        DEPT_TYPE as dept_type,
        ORG_DEPT as org_dept,
        PI_NAMES as pi_names,
        PI_PROFILE_IDS as pi_profile_ids,
        PO_NAMES as po_names,
        PROJECT_TITLE as project_title,
        ABSTRACT_TEXT as abstract_text,
        PROJECT_START_DATE as project_start_date,
        PROJECT_END_DATE as project_end_date,
        BUDGET_START_DATE as budget_start_date,
        BUDGET_END_DATE as budget_end_date,
        AWARD_NOTICE_DATE as award_notice_date,
        DATE_ADDED as date_added,
        AWARD_AMOUNT as award_amount,
        DIRECT_COST_AMT as direct_cost_amt,
        INDIRECT_COST_AMT as indirect_cost_amt,
        AGENCY_CODE as agency_code,
        ACTIVITY_CODE as activity_code,
        AWARD_TYPE as award_type,
        FUNDING_MECHANISM as funding_mechanism,
        OPPORTUNITY_NUMBER as opportunity_number,
        CFDA_CODE as cfda_code,
        ARRA_FUNDED as arra_funded,
        COVID_RESPONSE as covid_response,
        SPENDING_CATEGORIES as spending_categories,
        STUDY_SECTION as study_section,
        STUDY_SECTION_NAME as study_section_name,
        SRG_CODE as srg_code,
        CONG_DIST as cong_dist,
        REPORTER_PROJECT_URL as reporter_project_url,
        TERMS as terms,
        _INGESTED_AT as _loaded_at,
        'https://api.reporter.nih.gov/' as _source_url

    from source

)

select * from renamed
