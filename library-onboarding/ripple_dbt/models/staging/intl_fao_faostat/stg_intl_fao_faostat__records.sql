{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'INTL_FAO_FAOSTAT') }}

),

renamed as (

    select
        DATASETCODE as datasetcode,
        DATASETNAME as datasetname,
        TOPIC as topic,
        DATASETDESCRIPTION as datasetdescription,
        DATEUPDATE as dateupdate,
        FILELOCATION as filelocation,
        FILESIZE as filesize,
        FILEROWS as filerows,
        _INGESTED_AT as _loaded_at,
        'https://bulks-faostat.fao.org/production/datasets_E.json' as _source_url

    from source

)

select * from renamed
