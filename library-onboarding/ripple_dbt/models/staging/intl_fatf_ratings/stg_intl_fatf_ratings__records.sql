{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'INTL_FATF_RATINGS') }}

),

renamed as (

    select
        COUNTRY as country,
        FATF_RECOMMENDATION as fatf_recommendation,
        RATING as rating,
        EVALUATION_ROUND as evaluation_round,
        EVALUATION_YEAR as evaluation_year,
        DIMENSION as dimension,
        ASSESSMENT_BODY as assessment_body,
        _INGESTED_AT as _loaded_at,
        'https://www.fatf-gafi.org/en/publications/Mutualevaluations/Assessment-ratings.html' as _source_url

    from source

)

select * from renamed
