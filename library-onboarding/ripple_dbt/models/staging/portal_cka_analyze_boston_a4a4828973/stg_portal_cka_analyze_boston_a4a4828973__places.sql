{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_WKT)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_A4A4828973') }}

),

renamed as (

    select
        "RAMP_ID" as ramp_id,
        "COND" as cond,
        "INSP" as insp,
        "ACCESS" as access,
        "C_CROSS" as c_cross,
        "APRON_SL" as apron_sl,
        "LANDING_SL" as landing_sl,
        "LIP" as lip,
        "PWD" as pwd,
        "CITYCODE" as citycode,
        "RCI" as rci,
        "REPAIR_ALT" as repair_alt,
        "HIGHWAY" as highway,
        "ADDRESS" as address,
        "NPR" as npr,
        "REVEAL" as reveal,
        "SWK_WIDTH" as swk_width,
        "CONST_DATE" as const_date,
        "SWK_MATL" as swk_matl,
        "MATL" as matl,
        "CONST_PROG" as const_prog,
        "RAMP_CAT" as ramp_cat,
        "INSP_NOTES" as insp_notes,
        "INSP_DATE" as insp_date,
        "CONST_NOTE" as const_note,
        "EST_COST" as est_cost,
        "GIS_NOTES" as gis_notes,
        "AAB_COMP" as aab_comp,
        "PARENT" as parent,
        "POINT_X" as point_x,
        "POINT_Y" as point_y,
        "ID2" as id2,
        "AAB_LIKE_C" as aab_like_c,
        "SHAPE_WKT" as shape_wkt,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/pedestrian-ramp-inventory' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by shape_wkt order by _loaded_at desc) = 1
