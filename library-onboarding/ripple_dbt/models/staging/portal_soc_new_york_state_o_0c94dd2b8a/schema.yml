version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_NEW_YORK_STATE_O_0C94DD2B8A
        description: >
          Raw landing table for SPDES Multi-Sector General Permit (MSGP) Facilities. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_new_york_state_o_0c94dd2b8a__facilities
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per facility (NPDES_ID is unique). SPINE_ENTITY: facility."
    columns:
      - name: npdes_id
        data_tests:
          - not_null:
              config:
                severity: warn
