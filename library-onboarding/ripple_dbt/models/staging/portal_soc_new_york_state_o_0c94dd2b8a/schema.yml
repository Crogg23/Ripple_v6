version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_NEW_YORK_STATE_O_0C94DD2B8A
        description: >
          Raw landing table for SPDES Multi-Sector General Permit (MSGP) Facilities. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_new_york_state_o_0c94dd2b8a__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (NPDES_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: npdes_id
        data_tests:
          - unique
          - not_null
