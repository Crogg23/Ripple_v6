version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_VIRGINIA_OPEN_DA_AC3869EE0E
        description: >
          Raw landing table for Tax Parcels. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_virginia_open_da_ac3869ee0e__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PARCEL_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: parcel_id
        data_tests:
          - unique
          - not_null
