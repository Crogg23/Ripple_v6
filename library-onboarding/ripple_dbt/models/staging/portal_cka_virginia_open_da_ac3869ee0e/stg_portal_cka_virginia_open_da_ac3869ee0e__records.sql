{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PARCEL_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PARCEL_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 37 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   building_n: integer
--   o_acct: integer
--   date_tran: date
--   ac: float
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (PARCEL_ID) is NOT unique within a load as landed at generation time -- it would collapse 1 of 11,856 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_AC3869EE0E') }}
    qualify row_number() over (partition by "OBJECTID", "PARCEL_ID", "BUILDING_N", "APT_NO", "STREET_NO", "USE_CODE", "NEIGHBORHO", "ZONING", "MAP_INFO", "LABEL_FLD", "PARCEL_IDA", "O_ACCT", "OWNER_N", "OWNER_A", "STREET", "CITY", "STATE", "ZIP", "TRANSFER_D", "LOC_DESC", "LEGAL_DESC", "P_ADDRESS", "IN_FLOODZONE", "FLOODZONE_URL", "DATE_TRAN", "GLOBALID", "SQFT", "AC", "LOTS_SPLITS", "P_CITY", "P_STATE", "P_ZIP", "VACANT", "TAXDUE", "EPL", "SHAPE__AREA", "SHAPE__LENGTH" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"PARCEL_ID"') }} as parcel_id,
        {{ stg_int('"BUILDING_N"') }} as building_n,
        {{ null_junk('"APT_NO"') }} as apt_no,
        {{ null_junk('"STREET_NO"') }} as street_no,
        {{ null_junk('"USE_CODE"') }} as use_code,
        {{ null_junk('"NEIGHBORHO"') }} as neighborho,
        {{ null_junk('"ZONING"') }} as zoning,
        {{ null_junk('"MAP_INFO"') }} as map_info,
        {{ null_junk('"LABEL_FLD"') }} as label_fld,
        {{ null_junk('"PARCEL_IDA"') }} as parcel_ida,
        {{ stg_int('"O_ACCT"') }} as o_acct,
        {{ null_junk('"OWNER_N"') }} as owner_n,
        {{ null_junk('"OWNER_A"') }} as owner_a,
        {{ null_junk('"STREET"') }} as street,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"TRANSFER_D"') }} as transfer_d,
        {{ null_junk('"LOC_DESC"') }} as loc_desc,
        {{ null_junk('"LEGAL_DESC"') }} as legal_desc,
        {{ null_junk('"P_ADDRESS"') }} as p_address,
        {{ null_junk('"IN_FLOODZONE"') }} as in_floodzone,
        {{ null_junk('"FLOODZONE_URL"') }} as floodzone_url,
        {{ stg_date('"DATE_TRAN"') }} as date_tran,
        {{ null_junk('"GLOBALID"') }} as globalid,
        {{ null_junk('"SQFT"') }} as sqft,
        {{ stg_float('"AC"') }} as ac,
        {{ null_junk('"LOTS_SPLITS"') }} as lots_splits,
        {{ null_junk('"P_CITY"') }} as p_city,
        {{ null_junk('"P_STATE"') }} as p_state,
        {{ null_junk('"P_ZIP"') }} as p_zip,
        {{ null_junk('"VACANT"') }} as vacant,
        {{ null_junk('"TAXDUE"') }} as taxdue,
        {{ null_junk('"EPL"') }} as epl,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/tax-parcels' as _source_url

    from source

)

select * from renamed
