{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PARCEL_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PARCEL_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_AC3869EE0E') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "PARCEL_ID" as parcel_id,
        "BUILDING_N" as building_n,
        "APT_NO" as apt_no,
        "STREET_NO" as street_no,
        "USE_CODE" as use_code,
        "NEIGHBORHO" as neighborho,
        "ZONING" as zoning,
        "MAP_INFO" as map_info,
        "LABEL_FLD" as label_fld,
        "PARCEL_IDA" as parcel_ida,
        "O_ACCT" as o_acct,
        "OWNER_N" as owner_n,
        "OWNER_A" as owner_a,
        "STREET" as street,
        "CITY" as city,
        "STATE" as state,
        "ZIP" as zip,
        "TRANSFER_D" as transfer_d,
        "LOC_DESC" as loc_desc,
        "LEGAL_DESC" as legal_desc,
        "P_ADDRESS" as p_address,
        "IN_FLOODZONE" as in_floodzone,
        "FLOODZONE_URL" as floodzone_url,
        "DATE_TRAN" as date_tran,
        "GLOBALID" as globalid,
        "SQFT" as sqft,
        "AC" as ac,
        "LOTS_SPLITS" as lots_splits,
        "P_CITY" as p_city,
        "P_STATE" as p_state,
        "P_ZIP" as p_zip,
        "VACANT" as vacant,
        "TAXDUE" as taxdue,
        "EPL" as epl,
        "SHAPE__AREA" as shape__area,
        "SHAPE__LENGTH" as shape__length,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/tax-parcels' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by parcel_id order by _loaded_at desc) = 1
