version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_CALIFORNIA_OPEN_F8F7B5716B
        description: >
          Raw landing table for Urban Water Use Objectives – Compiled Report Data. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_california_open_f8f7b5716b__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ORG_ID+FY_START_DATE unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: org_id
        data_tests:
          - not_null
      - name: fy_start_date
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - org_id
              - fy_start_date