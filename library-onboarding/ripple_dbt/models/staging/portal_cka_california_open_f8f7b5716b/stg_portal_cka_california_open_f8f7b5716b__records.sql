{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ORG_ID+FY_START_DATE unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ORG_ID, FY_START_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_CALIFORNIA_OPEN_F8F7B5716B') }}

),

renamed as (

    select
        "ORG_ID" as org_id,
        "SUPPLIER_NAME" as supplier_name,
        "FY_START_DATE" as fy_start_date,
        "FY_END_DATE" as fy_end_date,
        "DATE_SUBMITTED_INITIAL" as date_submitted_initial,
        "DATE_RESUBMITTED" as date_resubmitted,
        "LATE_SUBMISSION" as late_submission,
        "SUBMITTED_INVALID_FILE" as submitted_invalid_file,
        "NO_SUBMISSION" as no_submission,
        INGESTED_AT as _loaded_at,
        'https://data.ca.gov/dataset/urban-water-use-objectives-compiled-report-data' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by org_id, fy_start_date order by _loaded_at desc) = 1
