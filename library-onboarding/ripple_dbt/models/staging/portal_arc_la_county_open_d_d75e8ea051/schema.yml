version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_LA_COUNTY_OPEN_D_D75E8EA051
        description: >
          Raw landing table for Risk-Screening Environmental Indicators (RSEI) Score. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_la_county_open_d_d75e8ea051__facilities
    description: "GRAIN: one row per facility (TRI_FACILITY_ID unique). SPINE_ENTITY: facility."
    columns:
      - name: tri_facility_id
        data_tests:
          - unique
          - not_null
