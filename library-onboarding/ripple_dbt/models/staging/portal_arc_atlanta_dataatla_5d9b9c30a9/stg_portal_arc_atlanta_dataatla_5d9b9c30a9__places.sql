{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (ZIP+INDUSTRY_DESC unique)
-- SPINE_ENTITY: place  (natural_key: ZIP, INDUSTRY_DESC)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 33 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_ATLANTA_DATAATLA_5D9B9C30A9') }}
    qualify row_number() over (partition by "ZIP", "INDUSTRY_DESC" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"OBJECTID"') }} as objectid,
        {{ null_junk('"CONAME"') }} as coname,
        {{ null_junk('"ADDR"') }} as addr,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE_NAME"') }} as state_name,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"ZIP4"') }} as zip4,
        {{ null_junk('"NAICS"') }} as naics,
        {{ null_junk('"NAICS_ALL"') }} as naics_all,
        {{ null_junk('"SIC"') }} as sic,
        {{ null_junk('"SIC_ALL"') }} as sic_all,
        {{ null_junk('"INDUSTRY_DESC"') }} as industry_desc,
        {{ null_junk('"AFFILIATE"') }} as affiliate,
        {{ null_junk('"BRAND"') }} as brand,
        {{ null_junk('"HQNAME"') }} as hqname,
        {{ null_junk('"LOC_CONF"') }} as loc_conf,
        {{ null_junk('"PLACETYPE"') }} as placetype,
        {{ null_junk('"SQFOOTAGE"') }} as sqfootage,
        {{ null_junk('"MIN_SQFT"') }} as min_sqft,
        {{ null_junk('"MAX_SQFT"') }} as max_sqft,
        {{ null_junk('"EMPNUM"') }} as empnum,
        {{ null_junk('"SALESVOL"') }} as salesvol,
        {{ null_junk('"SOURCE"') }} as source,
        {{ null_junk('"ESRI_PID"') }} as esri_pid,
        {{ null_junk('"DESC"') }} as desc,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"CREATIONDATE"') }} as creationdate,
        {{ null_junk('"CREATOR"') }} as creator,
        {{ null_junk('"EDITDATE"') }} as editdate,
        {{ null_junk('"EDITOR"') }} as editor,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://dpcd-coaplangis.opendata.arcgis.com/datasets/9f36698941294d47a978555609d41709_9' as _source_url

    from source

)

select * from renamed
