{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+INDUSTRY_DESC unique)
-- SPINE_ENTITY: place  (natural_key: ZIP, INDUSTRY_DESC)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_ATLANTA_DATAATLA_5D9B9C30A9') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "CONAME" as coname,
        "ADDR" as addr,
        "CITY" as city,
        "STATE_NAME" as state_name,
        "STATE" as state,
        "ZIP" as zip,
        "ZIP4" as zip4,
        "NAICS" as naics,
        "NAICS_ALL" as naics_all,
        "SIC" as sic,
        "SIC_ALL" as sic_all,
        "INDUSTRY_DESC" as industry_desc,
        "AFFILIATE" as affiliate,
        "BRAND" as brand,
        "HQNAME" as hqname,
        "LOC_CONF" as loc_conf,
        "PLACETYPE" as placetype,
        "SQFOOTAGE" as sqfootage,
        "MIN_SQFT" as min_sqft,
        "MAX_SQFT" as max_sqft,
        "EMPNUM" as empnum,
        "SALESVOL" as salesvol,
        "SOURCE" as source,
        "ESRI_PID" as esri_pid,
        "DESC" as desc,
        "LATITUDE" as latitude,
        "LONGITUDE" as longitude,
        "CREATIONDATE" as creationdate,
        "CREATOR" as creator,
        "EDITDATE" as editdate,
        "EDITOR" as editor,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://dpcd-coaplangis.opendata.arcgis.com/datasets/9f36698941294d47a978555609d41709_9' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by zip, industry_desc order by _loaded_at desc) = 1
