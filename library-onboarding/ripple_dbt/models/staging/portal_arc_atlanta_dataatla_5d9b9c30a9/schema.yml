version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_ATLANTA_DATAATLA_5D9B9C30A9
        description: >
          Raw landing table for Atlanta public health facilities. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_atlanta_dataatla_5d9b9c30a9__places
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+INDUSTRY_DESC unique). SPINE_ENTITY: place."
    columns:
      - name: zip
        data_tests:
          - not_null
      - name: industry_desc
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - zip
              - industry_desc