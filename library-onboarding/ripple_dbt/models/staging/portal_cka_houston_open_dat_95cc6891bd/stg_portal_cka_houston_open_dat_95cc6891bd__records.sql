{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GEO_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: GEO_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_HOUSTON_OPEN_DAT_95CC6891BD') }}

),

renamed as (

    select
        "GEO_ID" as geo_id,
        "NAME" as name,
        "B25002_001E" as b25002_001e,
        "B25002_002E" as b25002_002e,
        "B25002_003E" as b25002_003e,
        "B25003_002E" as b25003_002e,
        "B25003_003E" as b25003_003e,
        INGESTED_AT as _loaded_at,
        'https://data.houstontx.gov/dataset/acs-5-year-housing-unit-estimates-by-occupancy-and-tenure-at-block-group-level' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by geo_id order by _loaded_at desc) = 1
