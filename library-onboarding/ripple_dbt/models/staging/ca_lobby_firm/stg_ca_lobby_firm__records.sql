{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FIRM_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: FIRM_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 16 of 18 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   session_id: integer
--   current_qtr_amt: float
--   session_total_amt: float
--   contributor_id: integer
--   session_yr_1: integer
--   session_yr_2: integer
--   yr_1_ytd_amt: float
--   yr_2_ytd_amt: float
--   qtr_1: float
--   qtr_2: float
--   qtr_3: float
--   qtr_4: float
--   qtr_5: float
--   qtr_6: float
--   qtr_7: float
--   qtr_8: float
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'CA_LOBBY_FIRM') }}
    qualify row_number() over (partition by "FIRM_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"FIRM_ID"') }} as firm_id,
        {{ stg_int('"SESSION_ID"') }} as session_id,
        {{ null_junk('"FIRM_NAME"') }} as firm_name,
        {{ stg_float('"CURRENT_QTR_AMT"') }} as current_qtr_amt,
        {{ stg_float('"SESSION_TOTAL_AMT"') }} as session_total_amt,
        {{ stg_int('"CONTRIBUTOR_ID"') }} as contributor_id,
        {{ stg_int('"SESSION_YR_1"') }} as session_yr_1,
        {{ stg_int('"SESSION_YR_2"') }} as session_yr_2,
        {{ stg_float('"YR_1_YTD_AMT"') }} as yr_1_ytd_amt,
        {{ stg_float('"YR_2_YTD_AMT"') }} as yr_2_ytd_amt,
        {{ stg_float('"QTR_1"') }} as qtr_1,
        {{ stg_float('"QTR_2"') }} as qtr_2,
        {{ stg_float('"QTR_3"') }} as qtr_3,
        {{ stg_float('"QTR_4"') }} as qtr_4,
        {{ stg_float('"QTR_5"') }} as qtr_5,
        {{ stg_float('"QTR_6"') }} as qtr_6,
        {{ stg_float('"QTR_7"') }} as qtr_7,
        {{ stg_float('"QTR_8"') }} as qtr_8,
        INGESTED_AT as _loaded_at,
        'https://www.sos.ca.gov/campaign-lobbying/cal-access-resources/raw-data-campaign-finance-and-lobbying-activity' as _source_url

    from source

)

select * from renamed
