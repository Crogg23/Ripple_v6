version: 2

models:
  - name: stg_ca_lobby_firm__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FIRM_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: firm_id
        data_tests:
          - unique
          - not_null
