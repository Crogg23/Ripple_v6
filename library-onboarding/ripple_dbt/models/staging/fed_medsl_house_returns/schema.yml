version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_MEDSL_HOUSE_RETURNS
        description: >
          Raw landing table for MEDSL -- U.S. House constituency returns (district level). Grain not yet determined.

models:
  - name: stg_fed_medsl_house_returns__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
