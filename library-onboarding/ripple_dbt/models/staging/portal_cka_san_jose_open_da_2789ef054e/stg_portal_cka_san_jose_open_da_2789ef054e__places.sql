{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (SHAPE_LENGTH) is NOT unique within a load as landed at generation time -- it would collapse 6 of 6,486 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_2789EF054E') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "PARCELID" as parcelid,
        "FACILITYID" as facilityid,
        "INTID" as intid,
        "STREETMASTERID" as streetmasterid,
        "FROMINTERID" as frominterid,
        "TOINTERID" as tointerid,
        "FROMLEFT" as fromleft,
        "TOLEFT" as toleft,
        "FROMRIGHT" as fromright,
        "TORIGHT" as toright,
        "ADDRNUMTYPE" as addrnumtype,
        "FULLNAME" as fullname,
        "ONEWAYDIR" as onewaydir,
        "MODELFLAG" as modelflag,
        "STREETCLASS" as streetclass,
        "FUNCTCLASS" as functclass,
        "SPEEDLIMIT" as speedlimit,
        "PRIVATE" as private,
        "OFFICIAL" as official,
        "MUNILEFT" as munileft,
        "MUNIRIGHT" as muniright,
        "ZIPLEFT" as zipleft,
        "ZIPRIGHT" as zipright,
        "FOCWIDTH" as focwidth,
        "ROWWIDTH" as rowwidth,
        "FEATURECLASS" as featureclass,
        "PLANCRT" as plancrt,
        "PLANMOD" as planmod,
        "LASTUPDATE" as lastupdate,
        "NOTES" as notes,
        "RSN" as rsn,
        "INCORPORATED" as incorporated,
        "SHAPE_LENGTH" as shape_length,
        "ESNLEFT" as esnleft,
        "ESNRIGHT" as esnright,
        "FHWAFUNCTCLASS" as fhwafunctclass,
        "CREATIONDATE" as creationdate,
        "RESPONSIBILITY" as responsibility,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/private-street' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by objectid, parcelid, facilityid, intid, streetmasterid, frominterid, tointerid, fromleft, toleft, fromright, toright, addrnumtype, fullname, onewaydir, modelflag, streetclass, functclass, speedlimit, private, official, munileft, muniright, zipleft, zipright, focwidth, rowwidth, featureclass, plancrt, planmod, lastupdate, notes, rsn, incorporated, shape_length, esnleft, esnright, fhwafunctclass, creationdate, responsibility order by _loaded_at desc) = 1
