version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: XC_OWID_GINI
        description: >
          Raw landing table for OWID — Gini index (income inequality). Grain not yet determined.

models:
  - name: stg_xc_owid_gini__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
