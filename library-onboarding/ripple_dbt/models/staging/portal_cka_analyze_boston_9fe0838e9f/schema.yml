version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_9FE0838E9F
        description: >
          Raw landing table for Certified Business Directory. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_9fe0838e9f__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+DATE_BUSINESS_ESTABLISHED+BUSINESS_TYPE+COB_CATEGORY_CODES1 unique). SPINE_ENTITY: place."
    columns:
      - name: zipcode
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: date_business_established
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: business_type
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: cob_category_codes1
        data_tests:
          - not_null:
              config:
                severity: warn
