{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (ZIP+DATE_BUSINESS_ESTABLISHED+BUSINESS_TYPE+COB_CATEGORY_CODES1 unique)
-- SPINE_ENTITY: place  (natural_key: ZIPCODE, DATE_BUSINESS_ESTABLISHED, BUSINESS_TYPE, COB_CATEGORY_CODES1)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 30 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (ZIPCODE, DATE_BUSINESS_ESTABLISHED, BUSINESS_TYPE, COB_CATEGORY_CODES1) is NOT unique within a load as landed at generation time -- it would collapse 3 of 968 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_9FE0838E9F') }}
    qualify row_number() over (partition by "COMPANY_NAME", "SERVICES_PROVIDED", "MBE_WBE_CERT", "SMALL_LOCAL_CERT", "VETERAN_CERT", "SDO_CERTIFIED", "CITY_REGISTERED", "ADDRESS", "CITY", "STATE", "ZIPCODE", "CONTACT_NAME", "CONTACT_TITLE", "PHONE", "FAX", "EMAIL", "WEBSITE", "BUSINESS_TYPE", "CONSTRUCTION", "DATE_BUSINESS_ESTABLISHED", "NUMBER_EMPLOYEES", "COB_CATEGORY_CODES1", "COB_CATEGORY_CODES2", "COB_CATEGORY_CODES3", "NAICS_CODES1", "NAICS_CODES2", "NAICS_CODES3", "UNSPSC_CODE1", "UNSPSC_CODE2", "UNSPSC_CODE3" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"COMPANY_NAME"') }} as company_name,
        {{ null_junk('"SERVICES_PROVIDED"') }} as services_provided,
        {{ null_junk('"MBE_WBE_CERT"') }} as mbe_wbe_cert,
        {{ null_junk('"SMALL_LOCAL_CERT"') }} as small_local_cert,
        {{ null_junk('"VETERAN_CERT"') }} as veteran_cert,
        {{ null_junk('"SDO_CERTIFIED"') }} as sdo_certified,
        {{ null_junk('"CITY_REGISTERED"') }} as city_registered,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIPCODE"') }} as zipcode,
        {{ null_junk('"CONTACT_NAME"') }} as contact_name,
        {{ null_junk('"CONTACT_TITLE"') }} as contact_title,
        {{ null_junk('"PHONE"') }} as phone,
        {{ null_junk('"FAX"') }} as fax,
        {{ null_junk('"EMAIL"') }} as email,
        {{ null_junk('"WEBSITE"') }} as website,
        {{ null_junk('"BUSINESS_TYPE"') }} as business_type,
        {{ null_junk('"CONSTRUCTION"') }} as construction,
        {{ null_junk('"DATE_BUSINESS_ESTABLISHED"') }} as date_business_established,
        {{ null_junk('"NUMBER_EMPLOYEES"') }} as number_employees,
        {{ null_junk('"COB_CATEGORY_CODES1"') }} as cob_category_codes1,
        {{ null_junk('"COB_CATEGORY_CODES2"') }} as cob_category_codes2,
        {{ null_junk('"COB_CATEGORY_CODES3"') }} as cob_category_codes3,
        {{ null_junk('"NAICS_CODES1"') }} as naics_codes1,
        {{ null_junk('"NAICS_CODES2"') }} as naics_codes2,
        {{ null_junk('"NAICS_CODES3"') }} as naics_codes3,
        {{ null_junk('"UNSPSC_CODE1"') }} as unspsc_code1,
        {{ null_junk('"UNSPSC_CODE2"') }} as unspsc_code2,
        {{ null_junk('"UNSPSC_CODE3"') }} as unspsc_code3,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/certified-business-directory' as _source_url

    from source

)

select * from renamed
