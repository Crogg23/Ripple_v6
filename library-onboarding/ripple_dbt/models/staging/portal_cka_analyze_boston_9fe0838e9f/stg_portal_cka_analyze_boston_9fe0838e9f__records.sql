{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+DATE_BUSINESS_ESTABLISHED+BUSINESS_TYPE+COB_CATEGORY_CODES1 unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ZIPCODE, DATE_BUSINESS_ESTABLISHED, BUSINESS_TYPE, COB_CATEGORY_CODES1)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_9FE0838E9F') }}

),

renamed as (

    select
        "COMPANY_NAME" as company_name,
        "SERVICES_PROVIDED" as services_provided,
        "MBE_WBE_CERT" as mbe_wbe_cert,
        "SMALL_LOCAL_CERT" as small_local_cert,
        "VETERAN_CERT" as veteran_cert,
        "SDO_CERTIFIED" as sdo_certified,
        "CITY_REGISTERED" as city_registered,
        "ADDRESS" as address,
        "CITY" as city,
        "STATE" as state,
        "ZIPCODE" as zipcode,
        "CONTACT_NAME" as contact_name,
        "CONTACT_TITLE" as contact_title,
        "PHONE" as phone,
        "FAX" as fax,
        "EMAIL" as email,
        "WEBSITE" as website,
        "BUSINESS_TYPE" as business_type,
        "CONSTRUCTION" as construction,
        "DATE_BUSINESS_ESTABLISHED" as date_business_established,
        "NUMBER_EMPLOYEES" as number_employees,
        "COB_CATEGORY_CODES1" as cob_category_codes1,
        "COB_CATEGORY_CODES2" as cob_category_codes2,
        "COB_CATEGORY_CODES3" as cob_category_codes3,
        "NAICS_CODES1" as naics_codes1,
        "NAICS_CODES2" as naics_codes2,
        "NAICS_CODES3" as naics_codes3,
        "UNSPSC_CODE1" as unspsc_code1,
        "UNSPSC_CODE2" as unspsc_code2,
        "UNSPSC_CODE3" as unspsc_code3,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/certified-business-directory' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by zipcode, date_business_established, business_type, cob_category_codes1 order by _loaded_at desc) = 1
