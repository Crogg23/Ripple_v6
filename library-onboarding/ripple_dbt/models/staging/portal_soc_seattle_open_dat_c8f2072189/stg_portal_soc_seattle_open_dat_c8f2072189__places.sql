{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (ZIP+LICENSE_START_DATE unique)
-- SPINE_ENTITY: place  (natural_key: ZIP, LICENSE_START_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 13 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (ZIP, LICENSE_START_DATE) is NOT unique within a load as landed at generation time -- it would collapse 4,673 of 84,399 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_SEATTLE_OPEN_DAT_C8F2072189') }}
    qualify row_number() over (partition by "BUSINESS_LEGAL_NAME", "TRADE_NAME", "OWNERSHIP_TYPE", "NAICS_CODE", "NAICS_DESCRIPTION", "LICENSE_START_DATE", "STREET_ADDRESS", "CITY", "STATE", "ZIP", "BUSINESS_PHONE", "CITY_ACCOUNT_NUMBER", "UBI" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"BUSINESS_LEGAL_NAME"') }} as business_legal_name,
        {{ null_junk('"TRADE_NAME"') }} as trade_name,
        {{ null_junk('"OWNERSHIP_TYPE"') }} as ownership_type,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"NAICS_DESCRIPTION"') }} as naics_description,
        {{ null_junk('"LICENSE_START_DATE"') }} as license_start_date,
        {{ null_junk('"STREET_ADDRESS"') }} as street_address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"BUSINESS_PHONE"') }} as business_phone,
        {{ null_junk('"CITY_ACCOUNT_NUMBER"') }} as city_account_number,
        {{ null_junk('"UBI"') }} as ubi,
        INGESTED_AT as _loaded_at,
        'https://data.seattle.gov/d/wnbq-64tb' as _source_url

    from source

)

select * from renamed
