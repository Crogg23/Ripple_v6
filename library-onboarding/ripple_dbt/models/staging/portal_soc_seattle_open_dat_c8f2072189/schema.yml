version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_SEATTLE_OPEN_DAT_C8F2072189
        description: >
          Raw landing table for Active Business License Tax Certificate. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_seattle_open_dat_c8f2072189__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+LICENSE_START_DATE unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: zip
        data_tests:
          - not_null
      - name: license_start_date
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - zip
            - license_start_date
