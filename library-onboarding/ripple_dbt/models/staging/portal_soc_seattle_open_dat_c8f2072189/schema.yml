version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_SEATTLE_OPEN_DAT_C8F2072189
        description: >
          Raw landing table for Active Business License Tax Certificate. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_seattle_open_dat_c8f2072189__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+LICENSE_START_DATE unique). SPINE_ENTITY: place."
    columns:
      - name: zip
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: license_start_date
        data_tests:
          - not_null:
              config:
                severity: warn
