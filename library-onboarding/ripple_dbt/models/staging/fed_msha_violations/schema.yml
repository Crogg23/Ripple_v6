version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_MSHA_VIOLATIONS
        description: >
          Raw landing table for MSHA Mine Safety Violations. Grain not yet determined.

models:
  - name: stg_fed_msha_violations__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
