version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_ORANGE_COUNTY_OP_0DEA033879
        description: >
          Raw landing table for SDR Existing Development Inventory 242509. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_orange_county_op_0dea033879__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INVENTORY_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: inventory_id
        data_tests:
          - unique
          - not_null
