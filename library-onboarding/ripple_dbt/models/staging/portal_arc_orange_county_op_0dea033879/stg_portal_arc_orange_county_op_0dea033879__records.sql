{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INVENTORY_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: INVENTORY_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_ORANGE_COUNTY_OP_0DEA033879') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "REGION" as region,
        "WDID" as wdid,
        "SIC_CODE" as sic_code,
        "SIC_DESCRIPTION" as sic_description,
        "BUSINESS_NAME" as business_name,
        "STREET_NUMBER" as street_number,
        "CITY" as city,
        "ZIP" as zip,
        "WATERSHED" as watershed,
        "LATITIUDE" as latitiude,
        "LONGITUDE" as longitude,
        "PRIORITY" as priority,
        "INSPECTION_FREQ" as inspection_freq,
        "INVENTORY_ID" as inventory_id,
        "FACILITY_TYPE" as facility_type,
        "STREET_NAM" as street_nam,
        "STREET_TYP" as street_typ,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://data-ocpw.opendata.arcgis.com/datasets/0ea6ad2325664640a1f14a5be0260484_1' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by inventory_id order by _loaded_at desc) = 1
