version: 2

models:
  - name: stg_fed_cms_fiscal_intermediary_shared_system_attending_and_rendering__providers
    description: "GRAIN: one row per provider (NPI is unique). SPINE_ENTITY: provider."
    columns:
      - name: npi
        data_tests:
          - unique
          - not_null
