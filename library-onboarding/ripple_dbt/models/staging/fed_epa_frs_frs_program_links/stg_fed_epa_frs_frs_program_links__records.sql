{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PGM_SYS_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PGM_SYS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 13 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- DEDUPE: registry natural_key (PGM_SYS_ID) is NOT unique within a load as landed at generation time -- it would collapse 9,644 of 4,406,498 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_FRS_FRS_PROGRAM_LINKS') }}
    qualify row_number() over (partition by "PGM_SYS_ACRNM", "PGM_SYS_ID", "REGISTRY_ID", "PRIMARY_NAME", "LOCATION_ADDRESS", "SUPPLEMENTAL_LOCATION", "CITY_NAME", "COUNTY_NAME", "FIPS_CODE", "STATE_CODE", "STATE_NAME", "COUNTRY_NAME", "POSTAL_CODE" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"PGM_SYS_ACRNM"') }} as pgm_sys_acrnm,
        {{ null_junk('"PGM_SYS_ID"') }} as pgm_sys_id,
        {{ null_junk('"REGISTRY_ID"') }} as registry_id,
        {{ null_junk('"PRIMARY_NAME"') }} as primary_name,
        {{ null_junk('"LOCATION_ADDRESS"') }} as location_address,
        {{ null_junk('"SUPPLEMENTAL_LOCATION"') }} as supplemental_location,
        {{ null_junk('"CITY_NAME"') }} as city_name,
        {{ null_junk('"COUNTY_NAME"') }} as county_name,
        {{ null_junk('"FIPS_CODE"') }} as fips_code,
        {{ null_junk('"STATE_CODE"') }} as state_code,
        {{ null_junk('"STATE_NAME"') }} as state_name,
        {{ null_junk('"COUNTRY_NAME"') }} as country_name,
        {{ null_junk('"POSTAL_CODE"') }} as postal_code,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
