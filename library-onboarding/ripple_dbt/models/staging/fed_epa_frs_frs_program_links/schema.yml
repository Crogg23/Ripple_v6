version: 2

models:
  - name: stg_fed_epa_frs_frs_program_links__records
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PGM_SYS_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: pgm_sys_id
        data_tests:
          - not_null:
              config:
                severity: warn
