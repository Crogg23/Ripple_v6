{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (DATASET_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: DATASET_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 17 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   metadata_created: timestamp
--   metadata_modified: timestamp
--   date_published: timestamp
--   time_period_coverage_start: timestamp
--   time_period_coverage_end: timestamp
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'INTL_CA_OPEN_CANADA') }}
    qualify row_number() over (partition by "DATASET_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"DATASET_ID"') }} as dataset_id,
        {{ null_junk('"TITLE_EN"') }} as title_en,
        {{ null_junk('"TITLE_FR"') }} as title_fr,
        {{ null_junk('"C_ORGANIZATION"') }} as c_organization,
        {{ stg_ts('"METADATA_CREATED"') }} as metadata_created,
        {{ stg_ts('"METADATA_MODIFIED"') }} as metadata_modified,
        {{ null_junk('"NOTES_EN"') }} as notes_en,
        {{ null_junk('"NOTES_FR"') }} as notes_fr,
        {{ null_junk('"LICENSE_ID"') }} as license_id,
        {{ null_junk('"RESOURCES"') }} as resources,
        {{ null_junk('"TAGS"') }} as tags,
        {{ null_junk('"SUBJECT"') }} as subject,
        {{ null_junk('"FREQUENCY"') }} as frequency,
        {{ null_junk('"SPATIAL"') }} as spatial,
        {{ stg_ts('"DATE_PUBLISHED"') }} as date_published,
        {{ stg_ts('"TIME_PERIOD_COVERAGE_START"') }} as time_period_coverage_start,
        {{ stg_ts('"TIME_PERIOD_COVERAGE_END"') }} as time_period_coverage_end,
        _INGESTED_AT as _loaded_at,
        'https://open.canada.ca/data/' as _source_url

    from source

)

select * from renamed
