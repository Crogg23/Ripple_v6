{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_SEC_INSIDER_REPORTINGOWNER') }}

),

renamed as (

    select
        ACCESSION_NUMBER as accession_number,
        RPTOWNERCIK as rptownercik,
        RPTOWNERNAME as rptownername,
        RPTOWNER_RELATIONSHIP as rptowner_relationship,
        RPTOWNER_TITLE as rptowner_title,
        RPTOWNER_TXT as rptowner_txt,
        RPTOWNER_STREET1 as rptowner_street1,
        RPTOWNER_STREET2 as rptowner_street2,
        RPTOWNER_CITY as rptowner_city,
        RPTOWNER_STATE as rptowner_state,
        RPTOWNER_ZIPCODE as rptowner_zipcode,
        RPTOWNER_STATE_DESC as rptowner_state_desc,
        FILE_NUMBER as file_number,
        _INGESTED_AT as _loaded_at,
        'https://www.sec.gov/files/structureddata/data/insider-transactions-data-sets/2025q1_form345.zip' as _source_url

    from source

)

select * from renamed
