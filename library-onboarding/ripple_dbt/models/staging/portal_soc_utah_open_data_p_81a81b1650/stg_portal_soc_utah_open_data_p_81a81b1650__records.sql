{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 6 of 25 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   lines: integer
--   max_volt: float
--   min_volt: float
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   sourcedate: timestamp
--   val_date: timestamp
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_UTAH_OPEN_DATA_P_81A81B1650') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"COUNTRY"') }} as country,
        {{ null_junk('"COUNTY"') }} as county,
        {{ null_junk('"COUNTYFIPS"') }} as countyfips,
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ stg_int('"LINES"') }} as lines,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"MAX_INFER"') }} as max_infer,
        {{ stg_float('"MAX_VOLT"') }} as max_volt,
        {{ null_junk('"MIN_INFER"') }} as min_infer,
        {{ stg_float('"MIN_VOLT"') }} as min_volt,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"NAICS_DESC"') }} as naics_desc,
        {{ null_junk('"NAME"') }} as name,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"SOURCE"') }} as source,
        {{ stg_ts('"SOURCEDATE"') }} as sourcedate,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"TYPE"') }} as type,
        {{ stg_ts('"VAL_DATE"') }} as val_date,
        {{ null_junk('"VAL_METHOD"') }} as val_method,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"POINT"') }} as point,
        INGESTED_AT as _loaded_at,
        'https://opendata.utah.gov/d/ue3j-mf2w' as _source_url

    from source

)

select * from renamed
