version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_1C103EE2CD
        description: >
          Raw landing table for Condemned and Dead-End Properties. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_1c103ee2cd__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+CREATE_DATE+PROPERTY_TYPE+INSPECTION_STATUS+PARCEL_ID unique). SPINE_ENTITY: place."
    columns:
      - name: zip_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: create_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: property_type
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: inspection_status
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: parcel_id
        data_tests:
          - not_null:
              config:
                severity: warn
