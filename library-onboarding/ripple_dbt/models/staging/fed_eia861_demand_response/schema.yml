version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_EIA861_DEMAND_RESPONSE
        description: >
          Raw landing table, ingestion sweep 2026-08-05. All columns arrive as TEXT.

models:
  - name: stg_fed_eia861_demand_response__all
    description: >
      Minimal passthrough staging view for FED_EIA861_DEMAND_RESPONSE. Grain not yet
      determined -- snake_case rename only, no dedup.
