version: 2

models:
  - name: stg_fed_cms_federally_qualified_health_center_enrollments__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ENROLLMENT_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: enrollment_id
        data_tests:
          - unique
          - not_null
