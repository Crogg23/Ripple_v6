{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 22 of 48 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid_1: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   carteid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   council_lt: integer
--   council_rt: integer
--   created_date: timestamp
--   dpw_zon_lt: integer
--   dpw_zon_rt: integer
--   last_edited_date: timestamp
--   measurlgth: float
--   num_lanes: integer
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   replaced: timestamp
--   roadwidth: float
--   speedlimit: integer
--   vote_dt_lt: integer
--   vote_dt_rt: integer
--   ward_lt: integer
--   ward_rg: integer
--   zipl: integer
--   zipr: integer
--   zone_left: integer
--   zone_right: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (GEOMETRY) is NOT unique within a load as landed at generation time -- it would collapse 19,004 of 38,625 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_4E5C66124A') }}
    qualify row_number() over (partition by "GLOBALID", "OBJECTID_1", "SHAPE__LENGTH", "CARTEID", "CLASS", "COUNCIL_LT", "COUNCIL_RT", "CREATED_DATE", "CREATED_USER", "DIR", "DOMI_CLASS", "DPW_ZON_LT", "DPW_ZON_RT", "FIRE_ZN_LT", "FIRE_ZN_RT", "FROMSTREET", "HOOD_LEFT", "HOOD_RIGHT", "LAST_EDITED_DATE", "LAST_EDITED_USER", "MEASURLGTH", "NAME", "NUM_LANES", "OBJECTID", "ONEWAY", "OWNER", "PAVECLASS", "PREFIX", "REPLACED", "RETIRED", "ROADWIDTH", "SPEEDLIMIT", "STREETNAME", "SUFFIX", "TOSTREET", "TYPE", "VOTE_DT_LT", "VOTE_DT_RT", "WARD_LT", "WARD_RG", "ZIPL", "ZIPR", "ZONE_LEFT", "ZONE_RIGHT", "GEOMETRY", "OBJECTID_1_2", "GLOBALID_2", "SHAPE_LENGTH" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"GLOBALID"') }} as globalid,
        {{ stg_id_text('"OBJECTID_1"') }} as objectid_1,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ stg_id_text('"CARTEID"') }} as carteid,
        {{ null_junk('"CLASS"') }} as class,
        {{ stg_int('"COUNCIL_LT"') }} as council_lt,
        {{ stg_int('"COUNCIL_RT"') }} as council_rt,
        {{ stg_ts('"CREATED_DATE"') }} as created_date,
        {{ null_junk('"CREATED_USER"') }} as created_user,
        {{ null_junk('"DIR"') }} as dir,
        {{ null_junk('"DOMI_CLASS"') }} as domi_class,
        {{ stg_int('"DPW_ZON_LT"') }} as dpw_zon_lt,
        {{ stg_int('"DPW_ZON_RT"') }} as dpw_zon_rt,
        {{ null_junk('"FIRE_ZN_LT"') }} as fire_zn_lt,
        {{ null_junk('"FIRE_ZN_RT"') }} as fire_zn_rt,
        {{ null_junk('"FROMSTREET"') }} as fromstreet,
        {{ null_junk('"HOOD_LEFT"') }} as hood_left,
        {{ null_junk('"HOOD_RIGHT"') }} as hood_right,
        {{ stg_ts('"LAST_EDITED_DATE"') }} as last_edited_date,
        {{ null_junk('"LAST_EDITED_USER"') }} as last_edited_user,
        {{ stg_float('"MEASURLGTH"') }} as measurlgth,
        {{ null_junk('"NAME"') }} as name,
        {{ stg_int('"NUM_LANES"') }} as num_lanes,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"ONEWAY"') }} as oneway,
        {{ null_junk('"OWNER"') }} as owner,
        {{ null_junk('"PAVECLASS"') }} as paveclass,
        {{ null_junk('"PREFIX"') }} as prefix,
        {{ stg_ts('"REPLACED"') }} as replaced,
        {{ null_junk('"RETIRED"') }} as retired,
        {{ stg_float('"ROADWIDTH"') }} as roadwidth,
        {{ stg_int('"SPEEDLIMIT"') }} as speedlimit,
        {{ null_junk('"STREETNAME"') }} as streetname,
        {{ null_junk('"SUFFIX"') }} as suffix,
        {{ null_junk('"TOSTREET"') }} as tostreet,
        {{ null_junk('"TYPE"') }} as type,
        {{ stg_int('"VOTE_DT_LT"') }} as vote_dt_lt,
        {{ stg_int('"VOTE_DT_RT"') }} as vote_dt_rt,
        {{ stg_int('"WARD_LT"') }} as ward_lt,
        {{ stg_int('"WARD_RG"') }} as ward_rg,
        {{ stg_int('"ZIPL"') }} as zipl,
        {{ stg_int('"ZIPR"') }} as zipr,
        {{ stg_int('"ZONE_LEFT"') }} as zone_left,
        {{ stg_int('"ZONE_RIGHT"') }} as zone_right,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        {{ null_junk('"OBJECTID_1_2"') }} as objectid_1_2,
        {{ null_junk('"GLOBALID_2"') }} as globalid_2,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/pittsburgh-street-centerlines' as _source_url

    from source

)

select * from renamed
