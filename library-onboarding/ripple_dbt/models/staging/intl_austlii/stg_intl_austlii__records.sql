{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'INTL_AUSTLII') }}

),

renamed as (

    select
        DOCUMENT_URL as document_url,
        TITLE as title,
        JURISDICTION as jurisdiction,
        C_DATABASE as c_database,
        DOCUMENT_TYPE as document_type,
        DECISION_DATE as decision_date,
        PARTIES as parties,
        CITATION as citation,
        FULL_TEXT as full_text,
        CATCHWORDS as catchwords,
        SCRAPED_AT as scraped_at,
        _INGESTED_AT as _loaded_at,
        'https://www.austlii.edu.au/' as _source_url

    from source

)

select * from renamed
