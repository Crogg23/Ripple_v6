{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'INTL_ES_DATOSGOB') }}

),

renamed as (

    select
        URI as uri,
        TITLE as title,
        DESCRIPTION as description,
        PUBLISHER as publisher,
        SECTOR as sector,
        GEOGRAPHIC_COVERAGE as geographic_coverage,
        FORMAT as format,
        LICENSE as license,
        ISSUED as issued,
        MODIFIED as modified,
        DISTRIBUTION_URL as distribution_url,
        _INGESTED_AT as _loaded_at,
        'https://datos.gob.es/en/apidata' as _source_url

    from source

)

select * from renamed
