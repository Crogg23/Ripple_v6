{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (WUDS_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: WUDS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_EEF5B53BC5') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "WUDS_ID" as wuds_id,
        "NAME" as name,
        "CNTY_CODE" as cnty_code,
        "CNTY_NAME" as cnty_name,
        "OWNERSHIP" as ownership,
        "GW_SOURCE" as gw_source,
        "SW_SOURCE" as sw_source,
        "INTCONNECT" as intconnect,
        "LAST_DATE" as last_date,
        "PWS_ID" as pws_id,
        "SHAPEAREA" as shapearea,
        "SHAPELEN" as shapelen,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/pa-public-water-systems' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by wuds_id order by _loaded_at desc) = 1
