version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_EEF5B53BC5
        description: >
          Raw landing table for Public Water Supplier Service Areas. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_eef5b53bc5__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (WUDS_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: wuds_id
        data_tests:
          - unique
          - not_null
