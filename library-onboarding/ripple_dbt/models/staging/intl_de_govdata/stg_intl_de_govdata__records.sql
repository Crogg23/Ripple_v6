{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (DATASET_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: DATASET_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 4 of 16 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   metadata_created: timestamp
--   metadata_modified: timestamp
--   temporal_start: timestamp
--   temporal_end: timestamp
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'INTL_DE_GOVDATA') }}
    qualify row_number() over (partition by "DATASET_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"DATASET_ID"') }} as dataset_id,
        {{ null_junk('"TITLE"') }} as title,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"PUBLISHER"') }} as publisher,
        {{ null_junk('"DESCRIPTION"') }} as description,
        {{ null_junk('"TAGS"') }} as tags,
        {{ null_junk('"GROUPS"') }} as groups,
        {{ null_junk('"LICENSE_ID"') }} as license_id,
        {{ stg_ts('"METADATA_CREATED"') }} as metadata_created,
        {{ stg_ts('"METADATA_MODIFIED"') }} as metadata_modified,
        {{ stg_ts('"TEMPORAL_START"') }} as temporal_start,
        {{ stg_ts('"TEMPORAL_END"') }} as temporal_end,
        {{ null_junk('"SPATIAL"') }} as spatial,
        {{ null_junk('"IS_HVD"') }} as is_hvd,
        {{ null_junk('"RESOURCES"') }} as resources,
        {{ null_junk('"EXTRAS"') }} as extras,
        _INGESTED_AT as _loaded_at,
        'https://www.govdata.de/' as _source_url

    from source

)

select * from renamed
