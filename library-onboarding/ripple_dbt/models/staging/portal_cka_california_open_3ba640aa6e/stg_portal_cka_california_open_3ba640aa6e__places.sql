{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE__LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 19 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_CALIFORNIA_OPEN_3BA640AA6E') }}
    qualify row_number() over (partition by "SHAPE__LENGTH" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"OBJECTID"') }} as objectid,
        {{ null_junk('"COUNTY_JURISDICTION"') }} as county_jurisdiction,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"ZONE_NAME"') }} as zone_name,
        {{ null_junk('"ZONE_ID"') }} as zone_id,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"EVENT_TYPE"') }} as event_type,
        {{ null_junk('"CRITICAL_INFO"') }} as critical_info,
        {{ null_junk('"PUBLIC_INFO"') }} as public_info,
        {{ null_junk('"EDIT_DATE"') }} as edit_date,
        {{ null_junk('"STATEWIDE_LAST_UPDATED"') }} as statewide_last_updated,
        {{ null_junk('"NOTES"') }} as notes,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ null_junk('"GLOBALID"') }} as globalid,
        {{ null_junk('"CREATIONDATE"') }} as creationdate,
        {{ null_junk('"CREATOR"') }} as creator,
        {{ null_junk('"EDITDATE"') }} as editdate,
        {{ null_junk('"EDITOR"') }} as editor,
        INGESTED_AT as _loaded_at,
        'https://data.ca.gov/dataset/california-evacuation-aggregation-layer' as _source_url

    from source

)

select * from renamed
