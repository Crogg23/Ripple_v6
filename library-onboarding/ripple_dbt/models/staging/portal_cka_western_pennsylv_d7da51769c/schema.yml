version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_D7DA51769C
        description: >
          Raw landing table for Allegheny County Filed Property Assessment Appeals. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_d7da51769c__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PARCEL_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: parcel_id
        data_tests:
          - unique
          - not_null
