version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ISRAEL_NATIONAL_50096DDB0E
        description: >
          Raw landing table for רשימת צווי בטיחות. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_israel_national_50096ddb0e__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (WARRENT_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: warrent_id
        data_tests:
          - unique
          - not_null
