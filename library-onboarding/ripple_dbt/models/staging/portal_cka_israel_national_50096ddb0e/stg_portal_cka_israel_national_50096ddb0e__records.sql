{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (WARRENT_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: WARRENT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_50096DDB0E') }}

),

renamed as (

    select
        "SEND_DATE" as send_date,
        "WARRENT_ID" as warrent_id,
        "WORK_ID" as work_id,
        "WORK_NAME" as work_name,
        "CITY_NAME" as city_name,
        "EXECUTOR_NAME" as executor_name,
        "CATEGORY_NAME" as category_name,
        "FELONY_NAME" as felony_name,
        "LAW_NAME" as law_name,
        "CLAUSE_NAME" as clause_name,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/safetyorderslist' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by warrent_id order by _loaded_at desc) = 1
