{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (WARRENT_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: WARRENT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 1 of 10 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   work_id: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_50096DDB0E') }}
    qualify row_number() over (partition by "WARRENT_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SEND_DATE"') }} as send_date,
        {{ null_junk('"WARRENT_ID"') }} as warrent_id,
        {{ stg_int('"WORK_ID"') }} as work_id,
        {{ null_junk('"WORK_NAME"') }} as work_name,
        {{ null_junk('"CITY_NAME"') }} as city_name,
        {{ null_junk('"EXECUTOR_NAME"') }} as executor_name,
        {{ null_junk('"CATEGORY_NAME"') }} as category_name,
        {{ null_junk('"FELONY_NAME"') }} as felony_name,
        {{ null_junk('"LAW_NAME"') }} as law_name,
        {{ null_junk('"CLAUSE_NAME"') }} as clause_name,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/safetyorderslist' as _source_url

    from source

)

select * from renamed
