{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+PERMIT_DATE unique)
-- SPINE_ENTITY: place  (natural_key: LOC_ZIP, PERMIT_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_TEXAS_OPEN_DATA_A415622C5D') }}

),

renamed as (

    select
        "TP_NUMBER" as tp_number,
        "TP_NAME" as tp_name,
        "TP_ADDRESS" as tp_address,
        "TP_CITY" as tp_city,
        "TP_STATE" as tp_state,
        "TP_ZIP" as tp_zip,
        "TP_ZIP4" as tp_zip4,
        "TP_COUNTY" as tp_county,
        "ORG_TYPE" as org_type,
        "LOC_NUMBER" as loc_number,
        "LOC_NAME" as loc_name,
        "ADDRESS_NUMBER" as address_number,
        "ADDRESS_TEXT" as address_text,
        "PERMIT_DATE" as permit_date,
        "JURIS_CITY" as juris_city,
        "LOC_CITY" as loc_city,
        "LOC_STATE" as loc_state,
        "LOC_ZIP" as loc_zip,
        "LOC_ZIP4" as loc_zip4,
        "LOC_COUNTY" as loc_county,
        "NAICS" as naics,
        "LOC_JURIS" as loc_juris,
        "CITY_TAID" as city_taid,
        "MASS_TRANSIT_AUTH1_TAID" as mass_transit_auth1_taid,
        "MASS_TRANSIT_AUTH2_TAID" as mass_transit_auth2_taid,
        "COUNTY_TAID" as county_taid,
        "SPECIAL_PURP_DIST1_TAID" as special_purp_dist1_taid,
        "SPECIAL_PURP_DIST2_TAID" as special_purp_dist2_taid,
        "SPECIAL_PURP_DIST3_TAID" as special_purp_dist3_taid,
        "SPECIAL_PURP_DIST4_TAID" as special_purp_dist4_taid,
        "UNIQUE_TAID" as unique_taid,
        "FIRST_SALE_DATE" as first_sale_date,
        "OUT_OF_BUSINESS_DATE" as out_of_business_date,
        INGESTED_AT as _loaded_at,
        'https://data.texas.gov/d/3kx8-uryv' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by loc_zip, permit_date order by _loaded_at desc) = 1
