version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_TEXAS_OPEN_DATA_A415622C5D
        description: >
          Raw landing table for All Permitted Sales Tax Locations and Local Sales Tax Responsibility. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_texas_open_data_a415622c5d__places
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+PERMIT_DATE unique). SPINE_ENTITY: place."
    columns:
      - name: loc_zip
        data_tests:
          - not_null
      - name: permit_date
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - loc_zip
            - permit_date
