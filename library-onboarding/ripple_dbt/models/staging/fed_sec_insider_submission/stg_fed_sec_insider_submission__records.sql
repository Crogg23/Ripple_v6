{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_SEC_INSIDER_SUBMISSION') }}

),

renamed as (

    select
        ACCESSION_NUMBER as accession_number,
        FILING_DATE as filing_date,
        PERIOD_OF_REPORT as period_of_report,
        DATE_OF_ORIG_SUB as date_of_orig_sub,
        NO_SECURITIES_OWNED as no_securities_owned,
        NOT_SUBJECT_SEC16 as not_subject_sec16,
        FORM3_HOLDINGS_REPORTED as form3_holdings_reported,
        FORM4_TRANS_REPORTED as form4_trans_reported,
        DOCUMENT_TYPE as document_type,
        ISSUERCIK as issuercik,
        ISSUERNAME as issuername,
        ISSUERTRADINGSYMBOL as issuertradingsymbol,
        REMARKS as remarks,
        _INGESTED_AT as _loaded_at,
        'https://www.sec.gov/files/structureddata/data/insider-transactions-data-sets/2025q1_form345.zip' as _source_url

    from source

)

select * from renamed
