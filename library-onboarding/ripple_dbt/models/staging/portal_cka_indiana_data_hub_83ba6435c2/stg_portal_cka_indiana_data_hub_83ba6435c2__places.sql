{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (LOCATION_ID+DATE unique)
-- SPINE_ENTITY: place  (natural_key: LOCATION_ID, DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_INDIANA_DATA_HUB_83BA6435C2') }}

),

renamed as (

    select
        "LOCATION_ID" as location_id,
        "DATE" as date,
        "COVID_COUNT" as covid_count,
        "COVID_COUNT_MOVING_AVG" as covid_count_moving_avg,
        "COVID_DEATHS" as covid_deaths,
        "COVID_DEATHS_MOVING_AVG" as covid_deaths_moving_avg,
        "COVID_TESTS" as covid_tests,
        "COVID_TESTS_MOVING_AVG" as covid_tests_moving_avg,
        "POSITIVE_TEST_RATE" as positive_test_rate,
        "POSITIVE_TEST_RATE_MOVING_AVG" as positive_test_rate_moving_avg,
        "COVID_TESTS_ADMINISTRATED" as covid_tests_administrated,
        "COVID_TESTS_ADMINISTRATED_MOVING_AVG" as covid_tests_administrated_moving_avg,
        "ALL_TESTS_POSITIVE_TEST_RATE" as all_tests_positive_test_rate,
        "COVID_POSITIVE_TESTS_ADMIN" as covid_positive_tests_admin,
        "ALL_TESTS_POSITIVE_TEST_RATE_MOVING_AVG" as all_tests_positive_test_rate_moving_avg,
        "COVID_COUNT_BY_SPECIMEN_COLLECTION_DATE" as covid_count_by_specimen_collection_date,
        "COUNTY_NAME" as county_name,
        INGESTED_AT as _loaded_at,
        'https://hub.mph.in.gov/dataset/covid' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by location_id, date order by _loaded_at desc) = 1
