{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (LOCATION_ID+DATE unique)
-- SPINE_ENTITY: (not determined)  (natural_key: LOCATION_ID, DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (LOCATION_ID, DATE) is NOT unique within a load as landed at generation time -- it would collapse 853,924 of 980,219 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_INDIANA_DATA_HUB_83BA6435C2') }}

),

renamed as (

    select
        "LOCATION_ID" as location_id,
        "DATE" as date,
        "COVID_COUNT" as covid_count,
        "COVID_COUNT_MOVING_AVG" as covid_count_moving_avg,
        "COVID_DEATHS" as covid_deaths,
        "COVID_DEATHS_MOVING_AVG" as covid_deaths_moving_avg,
        "COVID_TESTS" as covid_tests,
        "COVID_TESTS_MOVING_AVG" as covid_tests_moving_avg,
        "POSITIVE_TEST_RATE" as positive_test_rate,
        "POSITIVE_TEST_RATE_MOVING_AVG" as positive_test_rate_moving_avg,
        "COVID_TESTS_ADMINISTRATED" as covid_tests_administrated,
        "COVID_TESTS_ADMINISTRATED_MOVING_AVG" as covid_tests_administrated_moving_avg,
        "ALL_TESTS_POSITIVE_TEST_RATE" as all_tests_positive_test_rate,
        "COVID_POSITIVE_TESTS_ADMIN" as covid_positive_tests_admin,
        "ALL_TESTS_POSITIVE_TEST_RATE_MOVING_AVG" as all_tests_positive_test_rate_moving_avg,
        "COVID_COUNT_BY_SPECIMEN_COLLECTION_DATE" as covid_count_by_specimen_collection_date,
        "COUNTY_NAME" as county_name,
        "FIELD_NAME" as field_name,
        "FIELD_DESCRIPTION" as field_description,
        "DISTRICT" as district,
        "COVID_TEST" as covid_test,
        "POSITIVITY_RATE_ALL_TESTS" as positivity_rate_all_tests,
        "DAILY_DELTA_DEATHS" as daily_delta_deaths,
        "DAILY_BASE_DEATHS" as daily_base_deaths,
        "DAILY_DELTA_CASES" as daily_delta_cases,
        "DAILY_BASE_CASES" as daily_base_cases,
        "COVID_COUNT_CUMSUM" as covid_count_cumsum,
        "COVID_DEATHS_CUMSUM" as covid_deaths_cumsum,
        "DAILY_DELTA_TESTS" as daily_delta_tests,
        "DAILY_BASE_TESTS" as daily_base_tests,
        "COVID_TEST_CUMSUM" as covid_test_cumsum,
        "COUNTY" as county,
        "REGION" as region,
        "FIPS" as fips,
        "AGE_GROUP" as age_group,
        "SINGLE_DOSE_ADMINISTERED" as single_dose_administered,
        "ALL_DOSES_ADMINISTERED" as all_doses_administered,
        "FULLY_VACCINATED" as fully_vaccinated,
        "BOOSTER_DOSE_ADMINISTERED" as booster_dose_administered,
        "NEW_SINGLE_DOSE_ADMINISTERED" as new_single_dose_administered,
        "NEW_ALL_DOSES_ADMINISTERED" as new_all_doses_administered,
        "NEW_FULLY_VACCINATED" as new_fully_vaccinated,
        "NEW_BOOSTER_DOSE_ADMINISTERED" as new_booster_dose_administered,
        "CURRENT_AS_OF" as current_as_of,
        "FIRST_DOSE_ADMINISTERED" as first_dose_administered,
        "SECOND_DOSE_ADMINISTERED" as second_dose_administered,
        "NEW_FIRST_DOSE_ADMINISTERED" as new_first_dose_administered,
        "NEW_SECOND_DOSE_ADMINISTERED" as new_second_dose_administered,
        "ETHNICITY" as ethnicity,
        "ZIP_CD" as zip_cd,
        "ELIGIBLE_POPULATION" as eligible_population,
        "FIELD" as field,
        "DESCRIPTION" as description,
        "REPORT_DATE" as report_date,
        "CASE_TYPE" as case_type,
        "NEW_CONFIRMED_CASES" as new_confirmed_cases,
        "TOTAL_CONFIRMED_CASES" as total_confirmed_cases,
        "NEW_CONFIRMED_DEATHS" as new_confirmed_deaths,
        "TOTAL_CONFIRMED_DEATHS" as total_confirmed_deaths,
        "COUNTY_2" as county_2,
        "MONTH" as month,
        "RACE" as race,
        "POPULATION" as population,
        "CASES" as cases,
        "DEATHS" as deaths,
        "STATUS_TYPE" as status_type,
        "TOTAL" as total,
        "GENDER" as gender,
        "AGEGRP" as agegrp,
        INGESTED_AT as _loaded_at,
        'https://hub.mph.in.gov/dataset/covid' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by location_id, date, covid_count, covid_count_moving_avg, covid_deaths, covid_deaths_moving_avg, covid_tests, covid_tests_moving_avg, positive_test_rate, positive_test_rate_moving_avg, covid_tests_administrated, covid_tests_administrated_moving_avg, all_tests_positive_test_rate, covid_positive_tests_admin, all_tests_positive_test_rate_moving_avg, covid_count_by_specimen_collection_date, county_name, field_name, field_description, district, covid_test, positivity_rate_all_tests, daily_delta_deaths, daily_base_deaths, daily_delta_cases, daily_base_cases, covid_count_cumsum, covid_deaths_cumsum, daily_delta_tests, daily_base_tests, covid_test_cumsum, county, region, fips, age_group, single_dose_administered, all_doses_administered, fully_vaccinated, booster_dose_administered, new_single_dose_administered, new_all_doses_administered, new_fully_vaccinated, new_booster_dose_administered, current_as_of, first_dose_administered, second_dose_administered, new_first_dose_administered, new_second_dose_administered, ethnicity, zip_cd, eligible_population, field, description, report_date, case_type, new_confirmed_cases, total_confirmed_cases, new_confirmed_deaths, total_confirmed_deaths, county_2, month, race, population, cases, deaths, status_type, total, gender, agegrp order by _loaded_at desc) = 1
