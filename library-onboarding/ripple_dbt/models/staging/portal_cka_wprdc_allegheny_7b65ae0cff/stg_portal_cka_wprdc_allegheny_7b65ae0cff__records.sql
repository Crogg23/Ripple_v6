{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INSPECTION_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: INSPECTION_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_7B65AE0CFF') }}

),

renamed as (

    select
        "INSPECTION_ID" as inspection_id,
        "PLACARD_DESC" as placard_desc,
        "FACILITY_NAME" as facility_name,
        "BUS_ST_DATE" as bus_st_date,
        "FACILITY_TYPE" as facility_type,
        "CATEGORY" as category,
        "NONPROFIT" as nonprofit,
        "NUM" as num,
        "STREET" as street,
        "CITY" as city,
        "STATE" as state,
        "ZIP_CODE" as zip_code,
        "MUNICIPAL" as municipal,
        "INSPECT_DT" as inspect_dt,
        "INSPECTION_PURPOSE" as inspection_purpose,
        "REINSPECTION_NEED" as reinspection_need,
        "PERMIT_STATUS" as permit_status,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-restaurant-food-facility-inspection-violations' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by inspection_id order by _loaded_at desc) = 1
