{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per facility (FRS_ID+REPORTING_YEAR+FIPS+ZIP+TRI_FACILITY_ID+TRI_CHEM_ID+SRS_ID unique)
-- SPINE_ENTITY: facility  (natural_key: EPA_REGISTRY_ID, REPORTING_YEAR, STATE_COUNTY_FIPS_CODE, ZIP_CODE, TRI_FACILITY_ID, TRI_CHEM_ID, SRS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'facility' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (EPA_REGISTRY_ID, REPORTING_YEAR, STATE_COUNTY_FIPS_CODE, ZIP_CODE, TRI_FACILITY_ID, TRI_CHEM_ID, SRS_ID) is NOT unique within a load as landed at generation time -- it would collapse 163,837 of 177,741 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_0AF7431C6C') }}

),

renamed as (

    select
        "TRI_FACILITY_ID" as tri_facility_id,
        "DOC_CTRL_NUM" as doc_ctrl_num,
        "FACILITY_NAME" as facility_name,
        "STREET_ADDRESS" as street_address,
        "CITY_NAME" as city_name,
        "COUNTY_NAME" as county_name,
        "STATE_COUNTY_FIPS_CODE" as state_county_fips_code,
        "STATE_ABBR" as state_abbr,
        "ZIP_CODE" as zip_code,
        "REGION" as region,
        "FAC_CLOSED_IND" as fac_closed_ind,
        "ASGN_FEDERAL_IND" as asgn_federal_ind,
        "ASGN_AGENCY" as asgn_agency,
        "PARENT_CO_DB_NUM" as parent_co_db_num,
        "PARENT_CO_NAME" as parent_co_name,
        "STANDARDIZED_PARENT_COMPANY" as standardized_parent_company,
        "EPA_REGISTRY_ID" as epa_registry_id,
        "TRADE_SECRET_IND" as trade_secret_ind,
        "REPORTING_YEAR" as reporting_year,
        "CAS_NUM" as cas_num,
        "ELEMENTAL_METAL_INCLUDED" as elemental_metal_included,
        "TRI_CHEM_ID" as tri_chem_id,
        "CHEM_NAME" as chem_name,
        "METAL_IND" as metal_ind,
        "PBT_IND" as pbt_ind,
        "LIST_3350" as list_3350,
        "CARCINOGEN" as carcinogen,
        "CLEAN_AIR" as clean_air,
        "PRIMARY_SIC_CODE" as primary_sic_code,
        "SIC_CODES" as sic_codes,
        "PRIMARY_NAICS_CODE" as primary_naics_code,
        "NAICS_CODES" as naics_codes,
        "INDUSTRY_CODE" as industry_code,
        "SRS_ID" as srs_id,
        "PFAS_IND" as pfas_ind,
        "ENVIRONMENTAL_MEDIUM" as environmental_medium,
        "RELEASE_RANGE_CODE" as release_range_code,
        "REL_EST_AMT" as rel_est_amt,
        "RELEASE_NA" as release_na,
        "RELEASE_BASIS_EST_CODE" as release_basis_est_code,
        "TOTAL_RELEASE" as total_release,
        "WATER_SEQUENCE_NUM" as water_sequence_num,
        "STREAM_NAME" as stream_name,
        "STORM_WATER_NA" as storm_water_na,
        "STORM_WATER_PERCENT" as storm_water_percent,
        "REACH_CODE" as reach_code,
        "TRANSFER_LOC_NUM" as transfer_loc_num,
        "OFF_SITE_AMOUNT_SEQUENCE" as off_site_amount_sequence,
        "TYPE_OF_WASTE_MANAGEMENT" as type_of_waste_management,
        "TRANSFER_RANGE_CODE" as transfer_range_code,
        "TRANSFER_AMT" as transfer_amt,
        "TRANSFER_EST_NA" as transfer_est_na,
        "TRANSFER_BASIS_EST_CODE" as transfer_basis_est_code,
        "OFF_SITE_NAME" as off_site_name,
        "OFF_SITE_STREET_ADDRESS" as off_site_street_address,
        "TRANSFER_CITY_NAME" as transfer_city_name,
        "TRANSFER_COUNTY_NAME" as transfer_county_name,
        "TRANSFER_STATE_ABBR" as transfer_state_abbr,
        "TRANSFER_ZIP_CODE" as transfer_zip_code,
        "TRANSFER_COUNTRY_CODE" as transfer_country_code,
        "RCRA_NUM" as rcra_num,
        "CONTROLLED_LOC" as controlled_loc,
        "TRANSFER_LOC_EPA_REGISTRY_ID" as transfer_loc_epa_registry_id,
        "ID" as id,
        "MAIL_NAME" as mail_name,
        "MAIL_STREET_ADDRESS" as mail_street_address,
        "MAIL_CITY" as mail_city,
        "MAIL_STATE_ABBR" as mail_state_abbr,
        "MAIL_PROVINCE" as mail_province,
        "MAIL_COUNTRY" as mail_country,
        "MAIL_ZIP_CODE" as mail_zip_code,
        "FRS_ID" as frs_id,
        "FAC_LATITUDE" as fac_latitude,
        "FAC_LONGITUDE" as fac_longitude,
        "PREF_LATITUDE" as pref_latitude,
        "PREF_LONGITUDE" as pref_longitude,
        "PREF_ACCURACY" as pref_accuracy,
        "PREF_COLLECT_METH" as pref_collect_meth,
        "PREF_DESC_CATEGORY" as pref_desc_category,
        "PREF_HORIZONTAL_DATUM" as pref_horizontal_datum,
        "PREF_SOURCE_SCALE" as pref_source_scale,
        "PREF_QA_CODE" as pref_qa_code,
        "ASGN_PARTIAL_IND" as asgn_partial_ind,
        "ASGN_PUBLIC_CONTACT" as asgn_public_contact,
        "ASGN_PUBLIC_PHONE" as asgn_public_phone,
        "ASGN_PUBLIC_PHONE_EXT" as asgn_public_phone_ext,
        "ASGN_PUBLIC_CONTACT_EMAIL" as asgn_public_contact_email,
        "BIA_CODE" as bia_code,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/toxic-release-inventory' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by tri_facility_id, doc_ctrl_num, facility_name, street_address, city_name, county_name, state_county_fips_code, state_abbr, zip_code, region, fac_closed_ind, asgn_federal_ind, asgn_agency, parent_co_db_num, parent_co_name, standardized_parent_company, epa_registry_id, trade_secret_ind, reporting_year, cas_num, elemental_metal_included, tri_chem_id, chem_name, metal_ind, pbt_ind, list_3350, carcinogen, clean_air, primary_sic_code, sic_codes, primary_naics_code, naics_codes, industry_code, srs_id, pfas_ind, environmental_medium, release_range_code, rel_est_amt, release_na, release_basis_est_code, total_release, water_sequence_num, stream_name, storm_water_na, storm_water_percent, reach_code, transfer_loc_num, off_site_amount_sequence, type_of_waste_management, transfer_range_code, transfer_amt, transfer_est_na, transfer_basis_est_code, off_site_name, off_site_street_address, transfer_city_name, transfer_county_name, transfer_state_abbr, transfer_zip_code, transfer_country_code, rcra_num, controlled_loc, transfer_loc_epa_registry_id, id, mail_name, mail_street_address, mail_city, mail_state_abbr, mail_province, mail_country, mail_zip_code, frs_id, fac_latitude, fac_longitude, pref_latitude, pref_longitude, pref_accuracy, pref_collect_meth, pref_desc_category, pref_horizontal_datum, pref_source_scale, pref_qa_code, asgn_partial_ind, asgn_public_contact, asgn_public_phone, asgn_public_phone_ext, asgn_public_contact_email, bia_code order by _loaded_at desc) = 1
