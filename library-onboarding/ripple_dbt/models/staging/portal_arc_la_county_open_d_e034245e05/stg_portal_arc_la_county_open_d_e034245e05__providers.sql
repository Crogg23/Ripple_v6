{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per provider (NPI+SCC_TYPE unique)
-- SPINE_ENTITY: provider  (natural_key: NPI, SCC_TYPE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 14 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'provider' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_LA_COUNTY_OPEN_D_E034245E05') }}
    qualify row_number() over (partition by "NPI", "SCC_TYPE" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SCC"') }} as scc,
        {{ null_junk('"CENTER_NAME"') }} as center_name,
        {{ null_junk('"SCC_TYPE"') }} as scc_type,
        {{ null_junk('"COUNTY"') }} as county,
        {{ null_junk('"NPI"') }} as npi,
        {{ null_junk('"PROVIDER_NAME"') }} as provider_name,
        {{ null_junk('"BUSINESS_NAME"') }} as business_name,
        {{ null_junk('"ADDRESS_LINE_1"') }} as address_line_1,
        {{ null_junk('"ADDRESS_LINE_2"') }} as address_line_2,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"PHONE"') }} as phone,
        {{ null_junk('"FID"') }} as fid,
        INGESTED_AT as _loaded_at,
        'https://data.lacounty.gov/datasets/7f1d80ae47934f568c2d2bf83925408f_1' as _source_url

    from source

)

select * from renamed
