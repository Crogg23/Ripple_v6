{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GIS_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: GIS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 42 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_WISCONSIN_OPEN_D_717F8037EB') }}
    qualify row_number() over (partition by "GIS_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"OBJECTID"') }} as objectid,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"FACILITY_NAME"') }} as facility_name,
        {{ null_junk('"LABEL"') }} as label,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"ADDRESS_2"') }} as address_2,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"ZIP_4"') }} as zip_4,
        {{ null_junk('"TYPE_DESC"') }} as type_desc,
        {{ null_junk('"SUB_TYPE_DESC"') }} as sub_type_desc,
        {{ null_junk('"FED_TYPE_DESC"') }} as fed_type_desc,
        {{ null_junk('"WI_LICENSE_NUM"') }} as wi_license_num,
        {{ null_junk('"STATE_FIPS"') }} as state_fips,
        {{ null_junk('"COUNTY"') }} as county,
        {{ null_junk('"COUNTY_FIPS"') }} as county_fips,
        {{ null_junk('"LAT"') }} as lat,
        {{ null_junk('"LON"') }} as lon,
        {{ null_junk('"USNG"') }} as usng,
        {{ null_junk('"GIS_ID"') }} as gis_id,
        {{ null_junk('"ASPEN_FACILITY_ID"') }} as aspen_facility_id,
        {{ null_junk('"FACILITY_INTERNAL_ID"') }} as facility_internal_id,
        {{ null_junk('"MEDICARE_ID"') }} as medicare_id,
        {{ null_junk('"MEDICAID_ID"') }} as medicaid_id,
        {{ null_junk('"HSIP_ID"') }} as hsip_id,
        {{ null_junk('"NPI_ID"') }} as npi_id,
        {{ null_junk('"AHA_ID"') }} as aha_id,
        {{ null_junk('"TYPE_CODE"') }} as type_code,
        {{ null_junk('"FACILITY_TYPE_CODE"') }} as facility_type_code,
        {{ null_junk('"WCRS_ID"') }} as wcrs_id,
        {{ null_junk('"PHONE_NUMBER"') }} as phone_number,
        {{ null_junk('"BED_COUNT"') }} as bed_count,
        {{ null_junk('"TRAUMA"') }} as trauma,
        {{ null_junk('"TRAUMA_CLASS"') }} as trauma_class,
        {{ null_junk('"HCC_REGION"') }} as hcc_region,
        {{ null_junk('"WITRAC_NAME"') }} as witrac_name,
        {{ null_junk('"EMRESOURCE_ID"') }} as emresource_id,
        {{ null_junk('"URL"') }} as url,
        {{ null_junk('"LAST_EDITED_DATE"') }} as last_edited_date,
        {{ null_junk('"TRAUMA_CLASS_TEXT"') }} as trauma_class_text,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.dhsgis.wi.gov/datasets/3601c216fbc1461d96b859e9e3b6249a_2' as _source_url

    from source

)

select * from renamed
