{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PGM_SYS_ID+ACTIVITY_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PGM_SYS_ID, ACTIVITY_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 10 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- DEDUPE: registry natural_key (PGM_SYS_ID, ACTIVITY_ID) is NOT unique within a load as landed at generation time -- it would collapse 634 of 1,779,096 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_ICIS_AIR_ICIS_AIR_FCES_PCES') }}
    qualify row_number() over (partition by "PGM_SYS_ID", "ACTIVITY_ID", "STATE_EPA_FLAG", "ACTIVITY_TYPE_CODE", "ACTIVITY_TYPE_DESC", "COMP_MONITOR_TYPE_CODE", "COMP_MONITOR_TYPE_DESC", "ACTUAL_END_DATE", "PROGRAM_CODES", "ACTIVITY_PURPOSE_DESC" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"PGM_SYS_ID"') }} as pgm_sys_id,
        {{ null_junk('"ACTIVITY_ID"') }} as activity_id,
        {{ null_junk('"STATE_EPA_FLAG"') }} as state_epa_flag,
        {{ null_junk('"ACTIVITY_TYPE_CODE"') }} as activity_type_code,
        {{ null_junk('"ACTIVITY_TYPE_DESC"') }} as activity_type_desc,
        {{ null_junk('"COMP_MONITOR_TYPE_CODE"') }} as comp_monitor_type_code,
        {{ null_junk('"COMP_MONITOR_TYPE_DESC"') }} as comp_monitor_type_desc,
        {{ null_junk('"ACTUAL_END_DATE"') }} as actual_end_date,
        {{ null_junk('"PROGRAM_CODES"') }} as program_codes,
        {{ null_junk('"ACTIVITY_PURPOSE_DESC"') }} as activity_purpose_desc,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
