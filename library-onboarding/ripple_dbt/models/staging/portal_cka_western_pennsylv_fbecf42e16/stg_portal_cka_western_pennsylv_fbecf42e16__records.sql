{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (REF_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: REF_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 6 of 22 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   score: float
--   user_fld: integer
--   x: float
--   y: float
--   zipcodes: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_FBECF42E16') }}
    qualify row_number() over (partition by "REF_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ARC_CITY"') }} as arc_city,
        {{ null_junk('"ARC_STATE"') }} as arc_state,
        {{ null_junk('"ARC_STREET"') }} as arc_street,
        {{ null_junk('"ARC_ZIP"') }} as arc_zip,
        {{ null_junk('"ADDR_TYPE"') }} as addr_type,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"LOC_NAME"') }} as loc_name,
        {{ null_junk('"MATCH_ADDR"') }} as match_addr,
        {{ null_junk('"MATCH_TYPE"') }} as match_type,
        {{ null_junk('"MUNICIPA_1"') }} as municipa_1,
        {{ null_junk('"MUNICIPALI"') }} as municipali,
        {{ null_junk('"NAME"') }} as name,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"REF_ID"') }} as ref_id,
        {{ stg_float('"SCORE"') }} as score,
        {{ null_junk('"SIDE"') }} as side,
        {{ null_junk('"STATUS"') }} as status,
        {{ stg_int('"USER_FLD"') }} as user_fld,
        {{ stg_float('"X"') }} as x,
        {{ stg_float('"Y"') }} as y,
        {{ stg_int('"ZIPCODES"') }} as zipcodes,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-wic-vendor-locations' as _source_url

    from source

)

select * from renamed
