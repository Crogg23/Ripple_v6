version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_F82E02E6B2
        description: >
          Raw landing table for Landslides. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_f82e02e6b2__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (EV_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: ev_id
        data_tests:
          - unique
          - not_null
