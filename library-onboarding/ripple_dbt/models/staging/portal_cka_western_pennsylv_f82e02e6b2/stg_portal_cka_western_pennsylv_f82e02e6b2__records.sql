{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (EV_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: EV_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_F82E02E6B2') }}

),

renamed as (

    select
        "SRC_NAME" as src_name,
        "SRC_LINK" as src_link,
        "EV_ID" as ev_id,
        "EV_DATE" as ev_date,
        "EV_TIME" as ev_time,
        "EV_TITLE" as ev_title,
        "EV_DESC" as ev_desc,
        "LOC_DESC" as loc_desc,
        "LOC_ACCU" as loc_accu,
        "LS_CAT" as ls_cat,
        "LS_TRIG" as ls_trig,
        "LS_SIZE" as ls_size,
        "LS_SETTING" as ls_setting,
        "FATALITIES" as fatalities,
        "INJURIES" as injuries,
        "STORM_NAME" as storm_name,
        "PHOTO_LINK" as photo_link,
        "COMMENTS" as comments,
        "EV_IMP_SRC" as ev_imp_src,
        "EV_IMP_ID" as ev_imp_id,
        "LATITUDE" as latitude,
        "LONGITUDE" as longitude,
        "CTRY_NAME" as ctry_name,
        "CTRY_CODE" as ctry_code,
        "DIV_NAME" as div_name,
        "GAZ_POINT" as gaz_point,
        "GAZ_DIST" as gaz_dist,
        "SUB_DATE" as sub_date,
        "EDIT_DATE" as edit_date,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/landslides' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by ev_id order by _loaded_at desc) = 1
