{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_DEA_ARCOS') }}

),

renamed as (

    select
        STATE as state,
        FIPS as fips,
        COUNTY as county,
        DRUG_NAME as drug_name,
        YEAR as year,
        GRAMS_PER_100K as grams_per_100k,
        TOTAL_GRAMS as total_grams,
        TOTAL_DOSAGE_UNITS as total_dosage_units,
        BUYERS as buyers,
        POPULATION as population,
        _INGESTED_AT as _loaded_at,
        'https://www.deadiversion.usdoj.gov/arcos/retail_drug_summary/arcos-drug-summary-reports.html' as _source_url

    from source

)

select * from renamed
