version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_TAMPA_OPEN_DATA_31262C6D5A
        description: >
          Raw landing table for revenue-and-finance-dataset. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_tampa_open_data_31262c6d5a__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: id
        data_tests:
          - unique
          - not_null
