version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_ATLANTA_DATAATLA_79E3C7BD36
        description: >
          Raw landing table for ChildCare OutServiceArea In LILA. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_atlanta_dataatla_79e3c7bd36__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: id
        data_tests:
          - unique
          - not_null
