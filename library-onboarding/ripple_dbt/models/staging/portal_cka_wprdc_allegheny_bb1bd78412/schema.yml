version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_BB1BD78412
        description: >
          Raw landing table for City of Pittsburgh Property Tax Delinquency. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_bb1bd78412__places
    description: "GRAIN: one row per place (LATLON is unique). SPINE_ENTITY: place."
    columns:
      - name: latitude
        data_tests:
          - unique
          - not_null
