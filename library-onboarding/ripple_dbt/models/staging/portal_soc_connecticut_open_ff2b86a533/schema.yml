version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_CONNECTICUT_OPEN_FF2B86A533
        description: >
          Raw landing table for Department of Economic and Community Development (DECD) – Business Assistance Portfolio. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_connecticut_open_ff2b86a533__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+FISCAL_YEAR+CONTRACT_EXECUTION_DATE unique). SPINE_ENTITY: place."
    columns:
      - name: zip_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: fiscal_year
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: contract_execution_date
        data_tests:
          - not_null:
              config:
                severity: warn
