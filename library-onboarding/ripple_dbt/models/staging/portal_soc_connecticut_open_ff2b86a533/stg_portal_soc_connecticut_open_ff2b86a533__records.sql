{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+FISCAL_YEAR+CONTRACT_EXECUTION_DATE unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ZIP_CODE, FISCAL_YEAR, CONTRACT_EXECUTION_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_CONNECTICUT_OPEN_FF2B86A533') }}

),

renamed as (

    select
        "FISCAL_YEAR" as fiscal_year,
        "COMPANY_NAME" as company_name,
        "COMPANY_ADDRESS" as company_address,
        "MUNICIPALITY" as municipality,
        "COUNTY_1" as county_1,
        "STATE" as state,
        "ZIP_CODE" as zip_code,
        "BUSINESS_INDUSTRY" as business_industry,
        "NAICS_CODE" as naics_code,
        "WOMAN_MINORITY_VETERAN_INTERNATIONAL_EXPORT" as woman_minority_veteran_international_export,
        "CONTRACT_EXECUTION_DATE" as contract_execution_date,
        "GRANT_AMOUNT" as grant_amount,
        "LOAN_AMOUNT" as loan_amount,
        "TOTAL_ASSISTANCE" as total_assistance,
        "TOTAL_PROJECT_COST" as total_project_cost,
        "AMOUNT_LEVERAGED" as amount_leveraged,
        "FUNDING_SOURCE" as funding_source,
        "STATUTORY_REFERENCE" as statutory_reference,
        "PER_CONTRACT_FULL_TIME_CT_JOBS_TO_BE_RETAINED" as per_contract_full_time_ct_jobs_to_be_retained,
        "PER_CONTRACT_FULL_TIME_CT_JOBS_TO_BE_CREATED" as per_contract_full_time_ct_jobs_to_be_created,
        "PER_CONTRACT_PART_TIME_CT_JOBS_TO_BE_RETAINED" as per_contract_part_time_ct_jobs_to_be_retained,
        "PER_CONTRACT_PART_TIME_CT_JOBS_TO_BE_CREATED" as per_contract_part_time_ct_jobs_to_be_created,
        "ACTUAL_JOBS_AT_TIME_OF_REVIEW" as actual_jobs_at_time_of_review,
        "JOB_OBLIGATION_STATUS" as job_obligation_status,
        "PENALTY_IF_APPLICABLE" as penalty_if_applicable,
        "FORGIVENESS_EARNED_IF_APPLICABLE" as forgiveness_earned_if_applicable,
        "PER_APPLICATION_EXISTING_FULL_TIME_JOBS_IN_CT" as per_application_existing_full_time_jobs_in_ct,
        "PER_APPLICATION_FULL_TIME_JOBS_TO_BE_CREATED_IN_CT" as per_application_full_time_jobs_to_be_created_in_ct,
        "PER_APPLICATION_EXISTING_PART_TIME_JOBS_IN_CT" as per_application_existing_part_time_jobs_in_ct,
        "PER_APPLICATION_PART_TIME_JOBS_TO_BE_CREATED_IN_CT" as per_application_part_time_jobs_to_be_created_in_ct,
        "LOCATION_1" as location_1,
        "GEOCODED_LOCATION" as geocoded_location,
        "COMPUTED_REGION_SND5_K6ZV" as computed_region_snd5_k6zv,
        "COMPUTED_REGION_DAM5_Q64J" as computed_region_dam5_q64j,
        "COMPUTED_REGION_NHMP_CQ6B" as computed_region_nhmp_cq6b,
        "COMPUTED_REGION_M4Y2_WHSE" as computed_region_m4y2_whse,
        "FULL_TIME_CT_EMPLOYMENT_AT_6_30_18" as full_time_ct_employment_at_6_30_18,
        INGESTED_AT as _loaded_at,
        'https://data.ct.gov/d/xnw3-nytd' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by zip_code, fiscal_year, contract_execution_date order by _loaded_at desc) = 1
