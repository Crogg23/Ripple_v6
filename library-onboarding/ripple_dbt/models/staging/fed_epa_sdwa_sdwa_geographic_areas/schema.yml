version: 2

models:
  - name: stg_fed_epa_sdwa_sdwa_geographic_areas__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GEO_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: geo_id
        data_tests:
          - unique
          - not_null
