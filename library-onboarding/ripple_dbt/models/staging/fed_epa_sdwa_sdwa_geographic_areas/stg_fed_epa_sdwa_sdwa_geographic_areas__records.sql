{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GEO_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: GEO_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 1 of 11 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   last_reported_date: date
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_SDWA_SDWA_GEOGRAPHIC_AREAS') }}
    qualify row_number() over (partition by "GEO_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SUBMISSIONYEARQUARTER"') }} as submissionyearquarter,
        {{ null_junk('"PWSID"') }} as pwsid,
        {{ null_junk('"GEO_ID"') }} as geo_id,
        {{ null_junk('"AREA_TYPE_CODE"') }} as area_type_code,
        {{ null_junk('"TRIBAL_CODE"') }} as tribal_code,
        {{ null_junk('"STATE_SERVED"') }} as state_served,
        {{ null_junk('"ANSI_ENTITY_CODE"') }} as ansi_entity_code,
        {{ null_junk('"ZIP_CODE_SERVED"') }} as zip_code_served,
        {{ null_junk('"CITY_SERVED"') }} as city_served,
        {{ null_junk('"COUNTY_SERVED"') }} as county_served,
        {{ stg_date('"LAST_REPORTED_DATE"') }} as last_reported_date,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
