{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (MARKET_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: MARKET_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 1 of 12 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   phone: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_B78655DE4F') }}
    qualify row_number() over (partition by "MARKET_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"MARKET_ID"') }} as market_id,
        {{ null_junk('"MARKET_TYPE"') }} as market_type,
        {{ null_junk('"MARKET_NAME"') }} as market_name,
        {{ null_junk('"ADDRESS1"') }} as address1,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ stg_int('"PHONE"') }} as phone,
        {{ null_junk('"PHONE_EXT"') }} as phone_ext,
        {{ null_junk('"COUNTY"') }} as county,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-farmers-markets-locations' as _source_url

    from source

)

select * from renamed
