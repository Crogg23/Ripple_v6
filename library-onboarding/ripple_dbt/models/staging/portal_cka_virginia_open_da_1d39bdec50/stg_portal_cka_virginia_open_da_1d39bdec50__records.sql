{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FAC_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: FAC_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_1D39BDEC50') }}

),

renamed as (

    select
        "X" as x,
        "Y" as y,
        "OBJECTID" as objectid,
        "FAC_ID" as fac_id,
        "CHANGED_BY" as changed_by,
        "CHANGED_DATE" as changed_date,
        "INSERTED_BY" as inserted_by,
        "INSERTED_DATE" as inserted_date,
        "VERIFIEDBY" as verifiedby,
        "VERIFYDATE" as verifydate,
        "REFERENCE_POINT" as reference_point,
        "FAC_ID_1" as fac_id_1,
        "CEDS_FAC_ID" as ceds_fac_id,
        "FAC_NAME" as fac_name,
        "FAC_TYPE" as fac_type,
        "FAC_ADDR1" as fac_addr1,
        "FAC_ADDR2" as fac_addr2,
        "FAC_CITY" as fac_city,
        "FAC_STATE" as fac_state,
        "FAC_ZIP5" as fac_zip5,
        "FAC_LOCATION" as fac_location,
        "NAME" as name,
        "PARENT_REG_ALT_DESCRIPTION" as parent_reg_alt_description,
        "FAC_ACTIVE_YN" as fac_active_yn,
        "FAC_FED_REG_YN" as fac_fed_reg_yn,
        "FAC_ACTIVE_UST" as fac_active_ust,
        "FAC_INACTIVE_UST" as fac_inactive_ust,
        "FAC_ACTIVE_AST" as fac_active_ast,
        "FAC_INACTIVE_AST" as fac_inactive_ast,
        "LAT" as lat,
        "LON" as lon,
        "FAC_INSP_YEAR" as fac_insp_year,
        "FAC_RISK_RANK" as fac_risk_rank,
        "DATA_DISCLAIMER" as data_disclaimer,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/registered-petroleum-tank-facilities' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by fac_id order by _loaded_at desc) = 1
