{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FAC_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: FAC_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 12 of 27 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   x: float
--   y: float
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   tank_fac_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   ceds_fac_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   facility_phone: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   rbis_inspection_year: integer
--   ast_capacity: integer
--   number_active_ust: integer
--   number_inactive_ust: integer
--   number_active_ast: integer
--   number_inactive_ast: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_1D39BDEC50') }}
    qualify row_number() over (partition by "FAC_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_float('"X"') }} as x,
        {{ stg_float('"Y"') }} as y,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"FAC_ID"') }} as fac_id,
        {{ stg_id_text('"TANK_FAC_ID"') }} as tank_fac_id,
        {{ stg_id_text('"CEDS_FAC_ID"') }} as ceds_fac_id,
        {{ null_junk('"TANK_FAC_NAME"') }} as tank_fac_name,
        {{ null_junk('"FACILITY_TYPE"') }} as facility_type,
        {{ null_junk('"REGISTERED_ADDRESS1"') }} as registered_address1,
        {{ null_junk('"REGISTERED_ADDRESS2"') }} as registered_address2,
        {{ null_junk('"REGISTERED_CITY"') }} as registered_city,
        {{ null_junk('"REGISTERED_STATE"') }} as registered_state,
        {{ null_junk('"REGISTERED_ZIP_CODE"') }} as registered_zip_code,
        {{ stg_id_text('"FACILITY_PHONE"') }} as facility_phone,
        {{ stg_int('"RBIS_INSPECTION_YEAR"') }} as rbis_inspection_year,
        {{ null_junk('"RBIS_RISK_RANK"') }} as rbis_risk_rank,
        {{ null_junk('"COUNTY"') }} as county,
        {{ null_junk('"DEQ_REGION"') }} as deq_region,
        {{ null_junk('"FACILITY_ACTIVE"') }} as facility_active,
        {{ null_junk('"FEDERALLY_REGULATED"') }} as federally_regulated,
        {{ stg_int('"AST_CAPACITY"') }} as ast_capacity,
        {{ stg_int('"NUMBER_ACTIVE_UST"') }} as number_active_ust,
        {{ stg_int('"NUMBER_INACTIVE_UST"') }} as number_inactive_ust,
        {{ stg_int('"NUMBER_ACTIVE_AST"') }} as number_active_ast,
        {{ stg_int('"NUMBER_INACTIVE_AST"') }} as number_inactive_ast,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/registered-petroleum-tank-facilities' as _source_url

    from source

)

select * from renamed
