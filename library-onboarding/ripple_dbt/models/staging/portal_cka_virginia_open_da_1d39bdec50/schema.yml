version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_VIRGINIA_OPEN_DA_1D39BDEC50
        description: >
          Raw landing table for Registered Petroleum Tank Facilities. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_virginia_open_da_1d39bdec50__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FAC_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: fac_id
        data_tests:
          - unique
          - not_null
