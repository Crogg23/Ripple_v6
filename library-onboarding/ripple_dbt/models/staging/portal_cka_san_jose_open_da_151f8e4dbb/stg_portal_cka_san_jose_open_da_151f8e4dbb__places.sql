{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 20 of 39 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   parcelid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   facilityid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   intid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   streetmasterid: integer
--   frominterid: integer
--   tointerid: integer
--   fromleft: integer
--   toleft: integer
--   fromright: integer
--   toright: integer
--   speedlimit: integer
--   zipleft: integer
--   zipright: integer
--   focwidth: float
--   rowwidth: float
--   rsn: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   esnleft: integer
--   esnright: integer
--   fhwafunctclass: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (SHAPE_LENGTH) is NOT unique within a load as landed at generation time -- it would collapse 3 of 30,163 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_151F8E4DBB') }}
    qualify row_number() over (partition by "OBJECTID", "PARCELID", "FACILITYID", "INTID", "STREETMASTERID", "FROMINTERID", "TOINTERID", "FROMLEFT", "TOLEFT", "FROMRIGHT", "TORIGHT", "ADDRNUMTYPE", "FULLNAME", "ONEWAYDIR", "MODELFLAG", "STREETCLASS", "FUNCTCLASS", "SPEEDLIMIT", "PRIVATE", "OFFICIAL", "MUNILEFT", "MUNIRIGHT", "ZIPLEFT", "ZIPRIGHT", "FOCWIDTH", "ROWWIDTH", "FEATURECLASS", "PLANCRT", "PLANMOD", "LASTUPDATE", "NOTES", "RSN", "INCORPORATED", "SHAPE_LENGTH", "ESNLEFT", "ESNRIGHT", "FHWAFUNCTCLASS", "CREATIONDATE", "RESPONSIBILITY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_id_text('"PARCELID"') }} as parcelid,
        {{ stg_id_text('"FACILITYID"') }} as facilityid,
        {{ stg_id_text('"INTID"') }} as intid,
        {{ stg_int('"STREETMASTERID"') }} as streetmasterid,
        {{ stg_int('"FROMINTERID"') }} as frominterid,
        {{ stg_int('"TOINTERID"') }} as tointerid,
        {{ stg_int('"FROMLEFT"') }} as fromleft,
        {{ stg_int('"TOLEFT"') }} as toleft,
        {{ stg_int('"FROMRIGHT"') }} as fromright,
        {{ stg_int('"TORIGHT"') }} as toright,
        {{ null_junk('"ADDRNUMTYPE"') }} as addrnumtype,
        {{ null_junk('"FULLNAME"') }} as fullname,
        {{ null_junk('"ONEWAYDIR"') }} as onewaydir,
        {{ null_junk('"MODELFLAG"') }} as modelflag,
        {{ null_junk('"STREETCLASS"') }} as streetclass,
        {{ null_junk('"FUNCTCLASS"') }} as functclass,
        {{ stg_int('"SPEEDLIMIT"') }} as speedlimit,
        {{ null_junk('"PRIVATE"') }} as private,
        {{ null_junk('"OFFICIAL"') }} as official,
        {{ null_junk('"MUNILEFT"') }} as munileft,
        {{ null_junk('"MUNIRIGHT"') }} as muniright,
        {{ stg_int('"ZIPLEFT"') }} as zipleft,
        {{ stg_int('"ZIPRIGHT"') }} as zipright,
        {{ stg_float('"FOCWIDTH"') }} as focwidth,
        {{ stg_float('"ROWWIDTH"') }} as rowwidth,
        {{ null_junk('"FEATURECLASS"') }} as featureclass,
        {{ null_junk('"PLANCRT"') }} as plancrt,
        {{ null_junk('"PLANMOD"') }} as planmod,
        {{ null_junk('"LASTUPDATE"') }} as lastupdate,
        {{ null_junk('"NOTES"') }} as notes,
        {{ stg_id_text('"RSN"') }} as rsn,
        {{ null_junk('"INCORPORATED"') }} as incorporated,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        {{ stg_int('"ESNLEFT"') }} as esnleft,
        {{ stg_int('"ESNRIGHT"') }} as esnright,
        {{ stg_int('"FHWAFUNCTCLASS"') }} as fhwafunctclass,
        {{ null_junk('"CREATIONDATE"') }} as creationdate,
        {{ null_junk('"RESPONSIBILITY"') }} as responsibility,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/public-street' as _source_url

    from source

)

select * from renamed
