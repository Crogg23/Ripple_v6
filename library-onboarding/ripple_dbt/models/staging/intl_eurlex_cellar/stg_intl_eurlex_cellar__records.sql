{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'INTL_EURLEX_CELLAR') }}

),

renamed as (

    select
        CELEX_ID as celex_id,
        ELI_URI as eli_uri,
        ECLI_ID as ecli_id,
        TITLE as title,
        DOCUMENT_TYPE as document_type,
        AUTHOR as author,
        DATE_PUBLISHED as date_published,
        DATE_OF_DOCUMENT as date_of_document,
        IN_FORCE as in_force,
        SUBJECT_MATTER as subject_matter,
        COUNTRY as country,
        LANGUAGE as language,
        OJ_SERIES as oj_series,
        OJ_NUMBER as oj_number,
        CONTENT_URL as content_url,
        FORMAT_AVAILABLE as format_available,
        CELLAR_URI as cellar_uri,
        _INGESTED_AT as _loaded_at,
        'https://eur-lex.europa.eu/content/help/data-reuse/reuse-contents-eurlex-details.html' as _source_url

    from source

)

select * from renamed
