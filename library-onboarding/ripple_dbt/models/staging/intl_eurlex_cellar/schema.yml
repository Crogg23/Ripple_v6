version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: INTL_EURLEX_CELLAR
        description: >
          Raw landing table for EUR-Lex CELLAR — SPARQL/REST API. Grain not yet determined.

models:
  - name: stg_intl_eurlex_cellar__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
