{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PROJECT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PROJECT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 9 of 25 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   display_on_web: integer
--   pops: integer
--   last_filed_date: date
--   last_board_approved_date: date
--   total_residential_units: integer
--   gross_square_footage: integer
--   total_development_cost: integer
--   point_x: float
--   point_y: float
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_824B1B659E') }}
    qualify row_number() over (partition by "PROJECT_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"PROJECT_ID"') }} as project_id,
        {{ null_junk('"PROJECT__PROJECT_NAME"') }} as project__project_name,
        {{ null_junk('"PROJECT_STREET_NUMBER"') }} as project_street_number,
        {{ null_junk('"PROJECT_STREET_NAME"') }} as project_street_name,
        {{ null_junk('"PROJECT_STREET_SUFFIX"') }} as project_street_suffix,
        {{ null_junk('"NEIGHBORHOOD"') }} as neighborhood,
        {{ null_junk('"PROJECT_ZIP_CODE"') }} as project_zip_code,
        {{ null_junk('"PROJECT_STATUS"') }} as project_status,
        {{ null_junk('"PROJECT__RECORD_TYPE"') }} as project__record_type,
        {{ stg_int('"DISPLAY_ON_WEB"') }} as display_on_web,
        {{ stg_int('"POPS"') }} as pops,
        {{ stg_date('"LAST_FILED_DATE"') }} as last_filed_date,
        {{ stg_date('"LAST_BOARD_APPROVED_DATE"') }} as last_board_approved_date,
        {{ null_junk('"PROJECT_USES"') }} as project_uses,
        {{ stg_int('"TOTAL_RESIDENTIAL_UNITS"') }} as total_residential_units,
        {{ stg_int('"GROSS_SQUARE_FOOTAGE"') }} as gross_square_footage,
        {{ stg_int('"TOTAL_DEVELOPMENT_COST"') }} as total_development_cost,
        {{ null_junk('"SAM_IDS"') }} as sam_ids,
        {{ null_junk('"DESCRIPTION"') }} as description,
        {{ null_junk('"WEBSITE_URL"') }} as website_url,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"SHAPE_WKT"') }} as shape_wkt,
        {{ stg_float('"POINT_X"') }} as point_x,
        {{ stg_float('"POINT_Y"') }} as point_y,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/article80-development-projects' as _source_url

    from source

)

select * from renamed
