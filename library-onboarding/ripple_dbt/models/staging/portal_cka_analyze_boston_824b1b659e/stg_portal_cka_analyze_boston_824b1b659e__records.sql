{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PROJECT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PROJECT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_824B1B659E') }}

),

renamed as (

    select
        "PROJECT_ID" as project_id,
        "PROJECT__PROJECT_NAME" as project__project_name,
        "PROJECT_STREET_NUMBER" as project_street_number,
        "PROJECT_STREET_NAME" as project_street_name,
        "PROJECT_STREET_SUFFIX" as project_street_suffix,
        "NEIGHBORHOOD" as neighborhood,
        "PROJECT_ZIP_CODE" as project_zip_code,
        "PROJECT_STATUS" as project_status,
        "PROJECT__RECORD_TYPE" as project__record_type,
        "DISPLAY_ON_WEB" as display_on_web,
        "POPS" as pops,
        "LAST_FILED_DATE" as last_filed_date,
        "LAST_BOARD_APPROVED_DATE" as last_board_approved_date,
        "PROJECT_USES" as project_uses,
        "TOTAL_RESIDENTIAL_UNITS" as total_residential_units,
        "GROSS_SQUARE_FOOTAGE" as gross_square_footage,
        "TOTAL_DEVELOPMENT_COST" as total_development_cost,
        "SAM_IDS" as sam_ids,
        "DESCRIPTION" as description,
        "WEBSITE_URL" as website_url,
        "LONGITUDE" as longitude,
        "LATITUDE" as latitude,
        "SHAPE_WKT" as shape_wkt,
        "POINT_X" as point_x,
        "POINT_Y" as point_y,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/article80-development-projects' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by project_id order by _loaded_at desc) = 1
