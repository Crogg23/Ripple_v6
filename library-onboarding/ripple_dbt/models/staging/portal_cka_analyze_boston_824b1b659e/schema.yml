version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_824B1B659E
        description: >
          Raw landing table for Article 80 Development Projects (website). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_824b1b659e__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PROJECT_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: project_id
        data_tests:
          - unique
          - not_null
