{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_USITC_DATAWEB') }}

),

renamed as (

    select
        TRADE_TYPE as trade_type,
        HTS_CODE as hts_code,
        COMMODITY_DESCRIPTION as commodity_description,
        COUNTRY_CODE as country_code,
        COUNTRY_NAME as country_name,
        YEAR as year,
        MONTH as month,
        CUSTOMS_VALUE as customs_value,
        CIF_VALUE as cif_value,
        QUANTITY as quantity,
        QUANTITY_UNIT as quantity_unit,
        TARIFF_RATE as tariff_rate,
        DUTY_COLLECTED as duty_collected,
        TRADE_PROGRAM as trade_program,
        NAICS_CODE as naics_code,
        SITC_CODE as sitc_code,
        PERIOD_DATE as period_date,
        _INGESTED_AT as _loaded_at,
        'https://dataweb.usitc.gov/' as _source_url

    from source

)

select * from renamed
