version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_BE3C51E436
        description: >
          Raw landing table for Allegheny County Traffic Counts (archived). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_be3c51e436__places
    description: "GRAIN: one row per place (LATLON is unique). SPINE_ENTITY: place."
    columns:
      - name: latitude
        data_tests:
          - unique
          - not_null
