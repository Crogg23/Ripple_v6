version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_OPEN_DATA_RALEIG_BBF9ABCA0D
        description: >
          Raw landing table for BAO . All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_open_data_raleig_bbf9abca0d__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: id
        data_tests:
          - unique
          - not_null
