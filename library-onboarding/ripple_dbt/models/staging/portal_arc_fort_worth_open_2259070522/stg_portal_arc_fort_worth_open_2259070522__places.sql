{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 17 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_FORT_WORTH_OPEN_2259070522') }}
    qualify row_number() over (partition by "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"FID"') }} as fid,
        {{ null_junk('"EMPNAME"') }} as empname,
        {{ null_junk('"EMPADDR"') }} as empaddr,
        {{ null_junk('"EMPLOYEES"') }} as employees,
        {{ null_junk('"NAICS"') }} as naics,
        {{ null_junk('"STARTDATE"') }} as startdate,
        {{ null_junk('"LASTEDITED"') }} as lastedited,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"EMPID"') }} as empid,
        {{ null_junk('"MAILADDR"') }} as mailaddr,
        {{ null_junk('"MAILCITY"') }} as mailcity,
        {{ null_junk('"MAILZIP"') }} as mailzip,
        {{ null_junk('"SOURCEDATE"') }} as sourcedate,
        {{ null_junk('"FEATURENAM"') }} as featurenam,
        {{ null_junk('"ACTIVITYSE"') }} as activityse,
        {{ null_junk('"TOP20RF"') }} as top20rf,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.fortworthtexas.gov/datasets/ab17d8de3b4d4a1595fc11a405a14455_0' as _source_url

    from source

)

select * from renamed
