version: 2

models:
  - name: stg_xc_mapping_police_violence__records
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per record (spine_entity not determined -- no registry hint available) (MPV_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: mpv_id
        data_tests:
          - not_null:
              config:
                severity: warn
