{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_916D2D00A4') }}

),

renamed as (

    select
        "OBJECTID_1" as objectid_1,
        "SHAPE__AREA" as shape__area,
        "SHAPE__LENGTH" as shape__length,
        "ALTERNATE_CONTACT_EMAIL" as alternate_contact_email,
        "ALTERNATE_CONTACT_MAILING_ADDRE" as alternate_contact_mailing_addre,
        "ALTERNATE_CONTACT_NAME" as alternate_contact_name,
        "ALTERNATE_CONTACT_PHONE" as alternate_contact_phone,
        "ALTERNATE_CONTACT_PHONE_EXT" as alternate_contact_phone_ext,
        "MEETING_DATES__AND_TIMES" as meeting_dates__and_times,
        "MEETING_LOCATION_ADDRESS" as meeting_location_address,
        "OBJECTID" as objectid,
        "ORGANIZATION_ADDRESS" as organization_address,
        "ORGANIZATION_NAME" as organization_name,
        "PRIMARY_CONTACT_EMAIL" as primary_contact_email,
        "PRIMARY_CONTACT_MAILING_ADDRESS" as primary_contact_mailing_address,
        "PRIMARY_CONTACT_NAME" as primary_contact_name,
        "PRIMARY_CONTACT_PHONE" as primary_contact_phone,
        "PRIMARY_CONTACT_PHONE_EXT" as primary_contact_phone_ext,
        "RCO_APPROVAL_DATE" as rco_approval_date,
        "RCO_EXPIRATION_DATE" as rco_expiration_date,
        "THIRD_CONTACT_EMAIL" as third_contact_email,
        "THIRD_CONTACT_NAME" as third_contact_name,
        "WEBSITE" as website,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/registered-community-groups' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by geometry order by _loaded_at desc) = 1
