{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 4 of 24 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid_1: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   rco_approval_date: timestamp
--   rco_expiration_date: timestamp
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_916D2D00A4') }}
    qualify row_number() over (partition by "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID_1"') }} as objectid_1,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ null_junk('"ALTERNATE_CONTACT_EMAIL"') }} as alternate_contact_email,
        {{ null_junk('"ALTERNATE_CONTACT_MAILING_ADDRE"') }} as alternate_contact_mailing_addre,
        {{ null_junk('"ALTERNATE_CONTACT_NAME"') }} as alternate_contact_name,
        {{ null_junk('"ALTERNATE_CONTACT_PHONE"') }} as alternate_contact_phone,
        {{ null_junk('"ALTERNATE_CONTACT_PHONE_EXT"') }} as alternate_contact_phone_ext,
        {{ null_junk('"MEETING_DATES__AND_TIMES"') }} as meeting_dates__and_times,
        {{ null_junk('"MEETING_LOCATION_ADDRESS"') }} as meeting_location_address,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"ORGANIZATION_ADDRESS"') }} as organization_address,
        {{ null_junk('"ORGANIZATION_NAME"') }} as organization_name,
        {{ null_junk('"PRIMARY_CONTACT_EMAIL"') }} as primary_contact_email,
        {{ null_junk('"PRIMARY_CONTACT_MAILING_ADDRESS"') }} as primary_contact_mailing_address,
        {{ null_junk('"PRIMARY_CONTACT_NAME"') }} as primary_contact_name,
        {{ null_junk('"PRIMARY_CONTACT_PHONE"') }} as primary_contact_phone,
        {{ null_junk('"PRIMARY_CONTACT_PHONE_EXT"') }} as primary_contact_phone_ext,
        {{ stg_ts('"RCO_APPROVAL_DATE"') }} as rco_approval_date,
        {{ stg_ts('"RCO_EXPIRATION_DATE"') }} as rco_expiration_date,
        {{ null_junk('"THIRD_CONTACT_EMAIL"') }} as third_contact_email,
        {{ null_junk('"THIRD_CONTACT_NAME"') }} as third_contact_name,
        {{ null_junk('"WEBSITE"') }} as website,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/registered-community-groups' as _source_url

    from source

)

select * from renamed
