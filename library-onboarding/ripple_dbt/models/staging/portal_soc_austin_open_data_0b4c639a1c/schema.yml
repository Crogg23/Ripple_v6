version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_AUSTIN_OPEN_DATA_0B4C639A1C
        description: >
          Raw landing table for APD Sworn Retirements and Separations. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_austin_open_data_0b4c639a1c__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (EMPLOYEE_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: employee_id
        data_tests:
          - unique
          - not_null
