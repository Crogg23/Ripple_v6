version: 2

models:
  - name: stg_ca_lobby_firm_lobbyist__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (LOBBYIST_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: lobbyist_id
        data_tests:
          - unique
          - not_null
