version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_OPEN_DATA_DC_74D9616143
        description: >
          Raw landing table for Metro Bus Stops 2023 - 2025. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_open_data_dc_74d9616143__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (EGIS_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: egis_id
        data_tests:
          - unique
          - not_null
