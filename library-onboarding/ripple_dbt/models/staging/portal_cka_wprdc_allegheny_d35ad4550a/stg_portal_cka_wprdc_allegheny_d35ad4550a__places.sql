{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 10 of 35 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   acres: float
--   created_date: timestamp
--   last_edited_date: timestamp
--   number: integer
--   perimeter: float
--   permitpk: integer
--   permitpk_i: integer
--   sqmiles: float
--   uniqueid: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_D35AD4550A') }}
    qualify row_number() over (partition by "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ stg_float('"ACRES"') }} as acres,
        {{ null_junk('"CODE"') }} as code,
        {{ null_junk('"CODE_DISTRICT"') }} as code_district,
        {{ null_junk('"CODE_RPP"') }} as code_rpp,
        {{ stg_ts('"CREATED_DATE"') }} as created_date,
        {{ null_junk('"CREATED_USER"') }} as created_user,
        {{ null_junk('"DISTRICT"') }} as district,
        {{ null_junk('"ENFORCEMENT"') }} as enforcement,
        {{ null_junk('"EXPIRATION_DATE"') }} as expiration_date,
        {{ null_junk('"FRIDAY"') }} as friday,
        {{ null_junk('"GRACE_PERIOD"') }} as grace_period,
        {{ null_junk('"HCMAP"') }} as hcmap,
        {{ stg_ts('"LAST_EDITED_DATE"') }} as last_edited_date,
        {{ null_junk('"LAST_EDITED_USER"') }} as last_edited_user,
        {{ null_junk('"MONDAY"') }} as monday,
        {{ null_junk('"NEIGHBORHO"') }} as neighborho,
        {{ stg_int('"NUMBER"') }} as number,
        {{ null_junk('"OTHER_TIME"') }} as other_time,
        {{ stg_float('"PERIMETER"') }} as perimeter,
        {{ stg_int('"PERMITPK"') }} as permitpk,
        {{ stg_int('"PERMITPK_I"') }} as permitpk_i,
        {{ null_junk('"PGHDB_SDE_PERMITPARKING_AREA"') }} as pghdb_sde_permitparking_area,
        {{ null_junk('"SATURDAY"') }} as saturday,
        {{ stg_float('"SQMILES"') }} as sqmiles,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"SUNDAY"') }} as sunday,
        {{ null_junk('"THURSDAY"') }} as thursday,
        {{ null_junk('"TUESDAY"') }} as tuesday,
        {{ stg_int('"UNIQUEID"') }} as uniqueid,
        {{ null_junk('"VISITOR_PASS"') }} as visitor_pass,
        {{ null_junk('"WEDNESDAY"') }} as wednesday,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/residential-permit-parking-program-areas' as _source_url

    from source

)

select * from renamed
