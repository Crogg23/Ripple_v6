version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_OPEN_DATA_SA_19461CBD4A
        description: >
          Raw landing table for Mandatory Detention Area. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_open_data_sa_19461cbd4a__places
    description: "GRAIN: one row per place (GEOM is unique). SPINE_ENTITY: place."
    columns:
      - name: shape__length
        data_tests:
          - unique
          - not_null
