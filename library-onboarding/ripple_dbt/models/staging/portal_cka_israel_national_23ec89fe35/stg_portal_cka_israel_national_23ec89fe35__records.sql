{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GIS_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: GIS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_23EC89FE35') }}

),

renamed as (

    select
        "GIS_ID" as gis_id,
        "NAME" as name,
        "ADDRESS" as address,
        "MUNICIPAL" as municipal,
        "X" as x,
        "Y" as y,
        "FIRST_VISI" as first_visi,
        "FIRST_RESU" as first_resu,
        "SECOND_VIS" as second_vis,
        "SECOND_RES" as second_res,
        "THIRD_VISI" as third_visi,
        "THIRD_RESU" as third_resu,
        "RESULTS_MA" as results_ma,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/fireant' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by gis_id order by _loaded_at desc) = 1
