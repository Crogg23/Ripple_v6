version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_VIRGINIA_OPEN_DA_AE7DCCB05B
        description: >
          Raw landing table for Virginia Pollutant Discharge Elimination System (VPDES) Permit Outfalls. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_virginia_open_da_ae7dccb05b__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (OUTFALL_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: outfall_id
        data_tests:
          - unique
          - not_null
