{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (OUTFALL_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: OUTFALL_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 3 of 18 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   x: float
--   y: float
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_AE7DCCB05B') }}
    qualify row_number() over (partition by "OUTFALL_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_float('"X"') }} as x,
        {{ stg_float('"Y"') }} as y,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"OUTFALL_ID"') }} as outfall_id,
        {{ null_junk('"INSERTED_BY"') }} as inserted_by,
        {{ null_junk('"INSERTED_DATE"') }} as inserted_date,
        {{ null_junk('"CHANGED_BY"') }} as changed_by,
        {{ null_junk('"CHANGED_DATE"') }} as changed_date,
        {{ null_junk('"REFERENCE_POINT"') }} as reference_point,
        {{ null_junk('"VERIFYDATE"') }} as verifydate,
        {{ null_junk('"VERIFIEDBY"') }} as verifiedby,
        {{ null_junk('"OUTFALLNO"') }} as outfallno,
        {{ null_junk('"VAP_PMT_NO"') }} as vap_pmt_no,
        {{ null_junk('"VAP_TYPE"') }} as vap_type,
        {{ null_junk('"VAP_TYPE_ALT_VERSION"') }} as vap_type_alt_version,
        {{ null_junk('"FAC_NAME"') }} as fac_name,
        {{ null_junk('"VAP_MAJOR_MINOR"') }} as vap_major_minor,
        {{ null_junk('"DATA_DISCLAIMER"') }} as data_disclaimer,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/virginia-pollutant-discharge-elimination-system-vpdes-permit-outfalls' as _source_url

    from source

)

select * from renamed
