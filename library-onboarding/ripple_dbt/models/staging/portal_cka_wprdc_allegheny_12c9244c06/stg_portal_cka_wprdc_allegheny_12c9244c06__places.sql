{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (ZIP+LAST_EDIT_DATE unique)
-- SPINE_ENTITY: place  (natural_key: ZIPCODE, LAST_EDIT_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (ZIPCODE, LAST_EDIT_DATE) is NOT unique within a load as landed at generation time -- it would collapse 2 of 410 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_12C9244C06') }}

),

renamed as (

    select
        "X" as x,
        "Y" as y,
        "OBJECTID" as objectid,
        "LEA_AUN" as lea_aun,
        "LEA_AIU_DESCRIPTION" as lea_aiu_description,
        "LEA_INST_NAME" as lea_inst_name,
        "SCHOOL_NAME" as school_name,
        "CITY" as city,
        "STATE" as state,
        "ZIPCODE" as zipcode,
        "FULL_ADDRESS" as full_address,
        "GRADE_LIST" as grade_list,
        "ELEMENTARY" as elementary,
        "SECONDARY" as secondary,
        "GRADE_K" as grade_k,
        "GRADE_1" as grade_1,
        "GRADE_2" as grade_2,
        "GRADE_3" as grade_3,
        "GRADE_4" as grade_4,
        "GRADE_5" as grade_5,
        "GRADE_6" as grade_6,
        "GRADE_7" as grade_7,
        "GRADE_8" as grade_8,
        "GRADE_9" as grade_9,
        "GRADE_10" as grade_10,
        "GRADE_11" as grade_11,
        "GRADE_12" as grade_12,
        "CYBER_SCHOOL" as cyber_school,
        "LAST_EDIT_DATE" as last_edit_date,
        "SCHOOL_CATEGORY" as school_category,
        "SCHOOL_CATEGORY_DESCRIPTION" as school_category_description,
        "ADDRESS" as address,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-schools' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by x, y, objectid, lea_aun, lea_aiu_description, lea_inst_name, school_name, city, state, zipcode, full_address, grade_list, elementary, secondary, grade_k, grade_1, grade_2, grade_3, grade_4, grade_5, grade_6, grade_7, grade_8, grade_9, grade_10, grade_11, grade_12, cyber_school, last_edit_date, school_category, school_category_description, address order by _loaded_at desc) = 1
