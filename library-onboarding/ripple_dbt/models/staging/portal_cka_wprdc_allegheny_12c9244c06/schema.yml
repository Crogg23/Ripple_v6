version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_12C9244C06
        description: >
          Raw landing table for Allegheny County Schools (Public, Private, Charter, and Career and Technical Centers). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_12c9244c06__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+LAST_EDIT_DATE unique). SPINE_ENTITY: place."
    columns:
      - name: zipcode
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: last_edit_date
        data_tests:
          - not_null:
              config:
                severity: warn
