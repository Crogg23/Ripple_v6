{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ACTIVITY_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ACTIVITY_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 2 of 16 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   actual_begin_date: date
--   actual_end_date: date
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- DEDUPE: registry natural_key (ACTIVITY_ID) is NOT unique within a load as landed at generation time -- it would collapse 695 of 260,556 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_ICIS_FEC_ICIS_FEC_EPA_INSPECTIONS') }}
    qualify row_number() over (partition by "REGISTRY_ID", "PGM_SYS_ACRNM", "PGM_SYS_ID", "FACILITY_NAME", "EPA_REGION_CODE", "ACTIVITY_ID", "ACTIVITY_TYPE_CODE", "ACTIVITY_TYPE_DESC", "COMP_MONITOR_UID", "COMP_MONITOR_TYPES", "STATUTE_CODE", "LAW_SECTION_CODE", "ACTUAL_BEGIN_DATE", "ACTUAL_END_DATE", "AGENCY", "COMP_MONITOR_AGENCY_TYPES" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"REGISTRY_ID"') }} as registry_id,
        {{ null_junk('"PGM_SYS_ACRNM"') }} as pgm_sys_acrnm,
        {{ null_junk('"PGM_SYS_ID"') }} as pgm_sys_id,
        {{ null_junk('"FACILITY_NAME"') }} as facility_name,
        {{ null_junk('"EPA_REGION_CODE"') }} as epa_region_code,
        {{ null_junk('"ACTIVITY_ID"') }} as activity_id,
        {{ null_junk('"ACTIVITY_TYPE_CODE"') }} as activity_type_code,
        {{ null_junk('"ACTIVITY_TYPE_DESC"') }} as activity_type_desc,
        {{ null_junk('"COMP_MONITOR_UID"') }} as comp_monitor_uid,
        {{ null_junk('"COMP_MONITOR_TYPES"') }} as comp_monitor_types,
        {{ null_junk('"STATUTE_CODE"') }} as statute_code,
        {{ null_junk('"LAW_SECTION_CODE"') }} as law_section_code,
        {{ stg_date('"ACTUAL_BEGIN_DATE"') }} as actual_begin_date,
        {{ stg_date('"ACTUAL_END_DATE"') }} as actual_end_date,
        {{ null_junk('"AGENCY"') }} as agency,
        {{ null_junk('"COMP_MONITOR_AGENCY_TYPES"') }} as comp_monitor_agency_types,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
