version: 2

models:
  - name: stg_fed_epa_icis_fec_icis_fec_epa_inspections__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ACTIVITY_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: activity_id
        data_tests:
          - unique
          - not_null
