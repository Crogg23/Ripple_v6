{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per organization (CIK+PERIOD+FY unique)
-- SPINE_ENTITY: organization  (natural_key: CIK, PERIOD, FY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 7 of 36 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   changed: date, YYYYMMDD
--   wksi: integer
--   filed: date, YYYYMMDD
--   accepted: timestamp
--   prevrpt: integer
--   detail: integer
--   nciks: integer
-- spine_entity 'organization' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- DEDUPE: registry natural_key (CIK, PERIOD, FY) is NOT unique within a load as landed at generation time -- it would collapse 102 of 6,304 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'FED_SEC_DERA_SUB_2025Q4') }}
    qualify row_number() over (partition by "ADSH", "CIK", "NAME", "SIC", "COUNTRYBA", "STPRBA", "CITYBA", "ZIPBA", "BAS1", "BAS2", "BAPH", "COUNTRYMA", "STPRMA", "CITYMA", "ZIPMA", "MAS1", "MAS2", "COUNTRYINC", "STPRINC", "EIN", "FORMER", "CHANGED", "AFS", "WKSI", "FYE", "FORM", "PERIOD", "FY", "FP", "FILED", "ACCEPTED", "PREVRPT", "DETAIL", "INSTANCE", "NCIKS", "ACIKS" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ADSH"') }} as adsh,
        {{ null_junk('"CIK"') }} as cik,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"SIC"') }} as sic,
        {{ null_junk('"COUNTRYBA"') }} as countryba,
        {{ null_junk('"STPRBA"') }} as stprba,
        {{ null_junk('"CITYBA"') }} as cityba,
        {{ null_junk('"ZIPBA"') }} as zipba,
        {{ null_junk('"BAS1"') }} as bas1,
        {{ null_junk('"BAS2"') }} as bas2,
        {{ null_junk('"BAPH"') }} as baph,
        {{ null_junk('"COUNTRYMA"') }} as countryma,
        {{ null_junk('"STPRMA"') }} as stprma,
        {{ null_junk('"CITYMA"') }} as cityma,
        {{ null_junk('"ZIPMA"') }} as zipma,
        {{ null_junk('"MAS1"') }} as mas1,
        {{ null_junk('"MAS2"') }} as mas2,
        {{ null_junk('"COUNTRYINC"') }} as countryinc,
        {{ null_junk('"STPRINC"') }} as stprinc,
        {{ null_junk('"EIN"') }} as ein,
        {{ null_junk('"FORMER"') }} as former,
        {{ stg_date('"CHANGED"', 'YYYYMMDD') }} as changed,
        {{ null_junk('"AFS"') }} as afs,
        {{ stg_int('"WKSI"') }} as wksi,
        {{ null_junk('"FYE"') }} as fye,
        {{ null_junk('"FORM"') }} as form,
        {{ null_junk('"PERIOD"') }} as period,
        {{ null_junk('"FY"') }} as fy,
        {{ null_junk('"FP"') }} as fp,
        {{ stg_date('"FILED"', 'YYYYMMDD') }} as filed,
        {{ stg_ts('"ACCEPTED"') }} as accepted,
        {{ stg_int('"PREVRPT"') }} as prevrpt,
        {{ stg_int('"DETAIL"') }} as detail,
        {{ null_junk('"INSTANCE"') }} as instance,
        {{ stg_int('"NCIKS"') }} as nciks,
        {{ null_junk('"ACIKS"') }} as aciks,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
