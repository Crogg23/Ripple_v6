{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per organization (CIK+PERIOD+FY unique)
-- SPINE_ENTITY: organization  (natural_key: CIK, PERIOD, FY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'organization' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- DEDUPE: registry natural_key (CIK, PERIOD, FY) is NOT unique within a load as landed at generation time -- it would collapse 102 of 6,304 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'FED_SEC_DERA_SUB_2025Q4') }}

),

renamed as (

    select
        "ADSH" as adsh,
        "CIK" as cik,
        "NAME" as name,
        "SIC" as sic,
        "COUNTRYBA" as countryba,
        "STPRBA" as stprba,
        "CITYBA" as cityba,
        "ZIPBA" as zipba,
        "BAS1" as bas1,
        "BAS2" as bas2,
        "BAPH" as baph,
        "COUNTRYMA" as countryma,
        "STPRMA" as stprma,
        "CITYMA" as cityma,
        "ZIPMA" as zipma,
        "MAS1" as mas1,
        "MAS2" as mas2,
        "COUNTRYINC" as countryinc,
        "STPRINC" as stprinc,
        "EIN" as ein,
        "FORMER" as former,
        "CHANGED" as changed,
        "AFS" as afs,
        "WKSI" as wksi,
        "FYE" as fye,
        "FORM" as form,
        "PERIOD" as period,
        "FY" as fy,
        "FP" as fp,
        "FILED" as filed,
        "ACCEPTED" as accepted,
        "PREVRPT" as prevrpt,
        "DETAIL" as detail,
        "INSTANCE" as instance,
        "NCIKS" as nciks,
        "ACIKS" as aciks,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by adsh, cik, name, sic, countryba, stprba, cityba, zipba, bas1, bas2, baph, countryma, stprma, cityma, zipma, mas1, mas2, countryinc, stprinc, ein, former, changed, afs, wksi, fye, form, period, fy, fp, filed, accepted, prevrpt, detail, instance, nciks, aciks order by _loaded_at desc) = 1
