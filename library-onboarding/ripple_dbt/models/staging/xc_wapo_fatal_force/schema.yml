version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: XC_WAPO_FATAL_FORCE
        description: >
          Raw landing table for Washington Post Fatal Force database (v2). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_xc_wapo_fatal_force__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: id
        data_tests:
          - unique
          - not_null
