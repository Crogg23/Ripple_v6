{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (FIPS is unique)
-- SPINE_ENTITY: place  (natural_key: GEOID20)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 10 of 30 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   aland20: integer
--   awater20: integer
--   blockce20: integer
--   fid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   housing20: integer
--   intptlat20: float
--   intptlon20: float
--   pop20: integer
--   statefp20: integer
--   uace20: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (GEOID20) is NOT unique within a load as landed at generation time -- it would collapse 24,786 of 49,574 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_7880B791DF') }}
    qualify row_number() over (partition by "ALAND20", "AWATER20", "BLOCKCE20", "COUNTYFP20", "FID", "FUNCSTAT20", "GEOID20", "GEOIDFQ20", "HOUSING20", "INTPTLAT20", "INTPTLON20", "MTFCC20", "NAME20", "POP20", "STATEFP20", "SHAPE__AREA", "SHAPE__LENGTH", "TRACTCE20", "UACE20", "UR20", "GEOMETRY", "OBJECTID", "GEOID", "NAME", "ALAND", "AWATER", "INTPTLAT", "INTPTLON", "SHAPE_AREA", "SHAPE_LENGTH" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_int('"ALAND20"') }} as aland20,
        {{ stg_int('"AWATER20"') }} as awater20,
        {{ stg_int('"BLOCKCE20"') }} as blockce20,
        {{ null_junk('"COUNTYFP20"') }} as countyfp20,
        {{ stg_id_text('"FID"') }} as fid,
        {{ null_junk('"FUNCSTAT20"') }} as funcstat20,
        {{ null_junk('"GEOID20"') }} as geoid20,
        {{ null_junk('"GEOIDFQ20"') }} as geoidfq20,
        {{ stg_int('"HOUSING20"') }} as housing20,
        {{ stg_float('"INTPTLAT20"') }} as intptlat20,
        {{ stg_float('"INTPTLON20"') }} as intptlon20,
        {{ null_junk('"MTFCC20"') }} as mtfcc20,
        {{ null_junk('"NAME20"') }} as name20,
        {{ stg_int('"POP20"') }} as pop20,
        {{ stg_int('"STATEFP20"') }} as statefp20,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ null_junk('"TRACTCE20"') }} as tractce20,
        {{ stg_int('"UACE20"') }} as uace20,
        {{ null_junk('"UR20"') }} as ur20,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        {{ null_junk('"OBJECTID"') }} as objectid,
        {{ null_junk('"GEOID"') }} as geoid,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"ALAND"') }} as aland,
        {{ null_junk('"AWATER"') }} as awater,
        {{ null_junk('"INTPTLAT"') }} as intptlat,
        {{ null_junk('"INTPTLON"') }} as intptlon,
        {{ null_junk('"SHAPE_AREA"') }} as shape_area,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-census-blocks-2021' as _source_url

    from source

)

select * from renamed
