version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_OPEN_BALTIMORE_751D91C991
        description: >
          Raw landing table for Hospitals in Maryland 2020. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_open_baltimore_751d91c991__facilities
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CCN+TYPE unique). SPINE_ENTITY: facility."
    columns:
      - name: ccn
        data_tests:
          - not_null
      - name: type
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - ccn
              - type