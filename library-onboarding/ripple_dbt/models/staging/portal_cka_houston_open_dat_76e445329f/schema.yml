version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_HOUSTON_OPEN_DAT_76E445329F
        description: >
          Raw landing table for All Paid Vehicle Permits 05/01/2011 to 04/30/2012. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_houston_open_dat_76e445329f__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TRANSPORTATION_ID+VEHICLE_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: transportation_id
        data_tests:
          - not_null
      - name: vehicle_id
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - transportation_id
            - vehicle_id
