version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ISRAEL_NATIONAL_07EC1AF377
        description: >
          Raw landing table for תקנות הירושה – בתי הדין השרעיים – בשפה הערבית. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_israel_national_07ec1af377__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (DEACEASED_ID+DEACEASED_DATE unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: deaceased_id
        data_tests:
          - not_null
      - name: deaceased_date
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - deaceased_id
            - deaceased_date
