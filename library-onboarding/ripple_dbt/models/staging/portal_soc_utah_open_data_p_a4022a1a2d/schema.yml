version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_UTAH_OPEN_DATA_P_A4022A1A2D
        description: >
          Raw landing table for Weber County TRI 1990-2000. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_utah_open_data_p_a4022a1a2d__facilities
    description: "GRAIN: one row per facility (TRI_FACILITY_ID+CAS_COMPOUND_ID+YEAR unique). SPINE_ENTITY: facility."
    columns:
      - name: tri_facility_id
        data_tests:
          - not_null
      - name: cas_compound_id
        data_tests:
          - not_null
      - name: year
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - tri_facility_id
            - cas_compound_id
            - year
