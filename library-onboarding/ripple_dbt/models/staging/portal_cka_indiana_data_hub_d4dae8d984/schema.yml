version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_INDIANA_DATA_HUB_D4DAE8D984
        description: >
          Raw landing table for Medicaid Claims: Emergency Department. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_indiana_data_hub_d4dae8d984__places
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+YEAR+AGE_GROUP unique). SPINE_ENTITY: place."
    columns:
      - name: zipcode
        data_tests:
          - not_null
      - name: year
        data_tests:
          - not_null
      - name: age_group
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - zipcode
            - year
            - age_group
