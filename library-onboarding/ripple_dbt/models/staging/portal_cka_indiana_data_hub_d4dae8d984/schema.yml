version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_INDIANA_DATA_HUB_D4DAE8D984
        description: >
          Raw landing table for medicaid-claims-emergency-department. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_indiana_data_hub_d4dae8d984__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+YEAR+AGE_GROUP unique). SPINE_ENTITY: place."
    columns:
      - name: zipcode
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: year
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: age_group
        data_tests:
          - not_null:
              config:
                severity: warn
