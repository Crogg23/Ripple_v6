version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_FEC_PAC_SUMMARY
        description: >
          Raw landing table for FEC PAC/Party Committee Summary (bulk, multi-cycle). Grain not yet determined.

models:
  - name: stg_fed_fec_pac_summary__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
