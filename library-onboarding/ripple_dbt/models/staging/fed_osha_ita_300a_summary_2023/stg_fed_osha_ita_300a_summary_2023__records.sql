{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 20 of 32 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   establishment_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   establishment_type: integer
--   size: integer
--   annual_average_employees: integer
--   total_hours_worked: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   no_injuries_illnesses: integer
--   total_deaths: integer
--   total_dafw_cases: integer
--   total_djtr_cases: integer
--   total_other_cases: integer
--   total_dafw_days: integer
--   total_djtr_days: integer
--   total_injuries: integer
--   total_skin_disorders: integer
--   total_respiratory_conditions: integer
--   total_poisonings: integer
--   total_hearing_loss: integer
--   total_other_illnesses: integer
--   created_timestamp: date
--   year_filing_for: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'FED_OSHA_ITA_300A_SUMMARY_2023') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"ESTABLISHMENT_NAME"') }} as establishment_name,
        {{ stg_id_text('"ESTABLISHMENT_ID"') }} as establishment_id,
        {{ null_junk('"EIN"') }} as ein,
        {{ null_junk('"COMPANY_NAME"') }} as company_name,
        {{ null_junk('"STREET_ADDRESS"') }} as street_address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"NAICS_YEAR"') }} as naics_year,
        {{ null_junk('"INDUSTRY_DESCRIPTION"') }} as industry_description,
        {{ stg_int('"ESTABLISHMENT_TYPE"') }} as establishment_type,
        {{ stg_int('"SIZE"') }} as size,
        {{ stg_int('"ANNUAL_AVERAGE_EMPLOYEES"') }} as annual_average_employees,
        {{ stg_id_text('"TOTAL_HOURS_WORKED"') }} as total_hours_worked,
        {{ stg_int('"NO_INJURIES_ILLNESSES"') }} as no_injuries_illnesses,
        {{ stg_int('"TOTAL_DEATHS"') }} as total_deaths,
        {{ stg_int('"TOTAL_DAFW_CASES"') }} as total_dafw_cases,
        {{ stg_int('"TOTAL_DJTR_CASES"') }} as total_djtr_cases,
        {{ stg_int('"TOTAL_OTHER_CASES"') }} as total_other_cases,
        {{ stg_int('"TOTAL_DAFW_DAYS"') }} as total_dafw_days,
        {{ stg_int('"TOTAL_DJTR_DAYS"') }} as total_djtr_days,
        {{ stg_int('"TOTAL_INJURIES"') }} as total_injuries,
        {{ stg_int('"TOTAL_SKIN_DISORDERS"') }} as total_skin_disorders,
        {{ stg_int('"TOTAL_RESPIRATORY_CONDITIONS"') }} as total_respiratory_conditions,
        {{ stg_int('"TOTAL_POISONINGS"') }} as total_poisonings,
        {{ stg_int('"TOTAL_HEARING_LOSS"') }} as total_hearing_loss,
        {{ stg_int('"TOTAL_OTHER_ILLNESSES"') }} as total_other_illnesses,
        {{ stg_date('"CREATED_TIMESTAMP"') }} as created_timestamp,
        {{ null_junk('"CHANGE_REASON"') }} as change_reason,
        {{ stg_int('"YEAR_FILING_FOR"') }} as year_filing_for,
        INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
