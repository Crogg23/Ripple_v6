version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_TAMPA_OPEN_DATA_18B980D54D
        description: >
          Raw landing table for ebo-dataset. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_tampa_open_data_18b980d54d__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID+DATE unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: id
        data_tests:
          - not_null
      - name: date
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - id
              - date