{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 8 of 28 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   created_date: timestamp
--   inline_fid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   last_edited_date: timestamp
--   segments: integer
--   smolnflag: integer
--   st_length: float
--   total_leng: float
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_36A8032A4C') }}
    qualify row_number() over (partition by "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"GLOBALID_1"') }} as globalid_1,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"SHAPE_LENG"') }} as shape_leng,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ null_junk('"ALTNAME"') }} as altname,
        {{ null_junk('"CLEANDONE"') }} as cleandone,
        {{ null_junk('"CONDITION"') }} as condition,
        {{ stg_ts('"CREATED_DATE"') }} as created_date,
        {{ null_junk('"CREATED_USER"') }} as created_user,
        {{ null_junk('"GLOBALID"') }} as globalid,
        {{ null_junk('"GREENWAY"') }} as greenway,
        {{ stg_id_text('"INLINE_FID"') }} as inline_fid,
        {{ stg_ts('"LAST_EDITED_DATE"') }} as last_edited_date,
        {{ null_junk('"LAST_EDITED_USER"') }} as last_edited_user,
        {{ null_junk('"MATERIAL"') }} as material,
        {{ null_junk('"NOTES"') }} as notes,
        {{ null_junk('"PARK"') }} as park,
        {{ null_junk('"PUBLICVIEW"') }} as publicview,
        {{ null_junk('"RAD"') }} as rad,
        {{ stg_int('"SEGMENTS"') }} as segments,
        {{ stg_int('"SMOLNFLAG"') }} as smolnflag,
        {{ stg_float('"ST_LENGTH"') }} as st_length,
        {{ stg_float('"TOTAL_LENG"') }} as total_leng,
        {{ null_junk('"TRAIL_NAME"') }} as trail_name,
        {{ null_junk('"TRAIL_RATI"') }} as trail_rati,
        {{ null_junk('"USE_TYPE"') }} as use_type,
        {{ null_junk('"WARNINGMSG"') }} as warningmsg,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/pittsburgh-trails' as _source_url

    from source

)

select * from renamed
