{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (LINKAGE_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: LINKAGE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 3 of 8 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   cand_election_yr: integer
--   fec_election_yr: integer
--   cycle: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_FEC_BULK_LINKAGES') }}
    qualify row_number() over (partition by "LINKAGE_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CAND_ID"') }} as cand_id,
        {{ stg_int('"CAND_ELECTION_YR"') }} as cand_election_yr,
        {{ stg_int('"FEC_ELECTION_YR"') }} as fec_election_yr,
        {{ null_junk('"CMTE_ID"') }} as cmte_id,
        {{ null_junk('"CMTE_TP"') }} as cmte_tp,
        {{ null_junk('"CMTE_DSGN"') }} as cmte_dsgn,
        {{ null_junk('"LINKAGE_ID"') }} as linkage_id,
        {{ stg_int('"CYCLE"') }} as cycle,
        _INGESTED_AT as _loaded_at,
        'https://www.fec.gov/files/bulk-downloads/2024/ccl24.zip' as _source_url

    from source

)

select * from renamed
