version: 2

models:
  - name: stg_fed_fec_bulk_linkages__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (LINKAGE_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: linkage_id
        data_tests:
          - unique
          - not_null
