version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_FEC_COMMITTEES
        description: >
          Raw landing table for FEC Committee Master (bulk, multi-cycle). Grain not yet determined.

models:
  - name: stg_fed_fec_committees__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
