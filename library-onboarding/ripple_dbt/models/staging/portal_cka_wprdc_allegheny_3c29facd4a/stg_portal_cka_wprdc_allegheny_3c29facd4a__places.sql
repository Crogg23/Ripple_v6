{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_AREA)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 18 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_3C29FACD4A') }}
    qualify row_number() over (partition by "SHAPE_AREA" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"FID"') }} as fid,
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"AREA"') }} as area,
        {{ null_junk('"DISTRICT"') }} as district,
        {{ null_junk('"MEMBERS"') }} as members,
        {{ null_junk('"LOCKED"') }} as locked,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"POPULATION"') }} as population,
        {{ null_junk('"F18_POP"') }} as f18_pop,
        {{ null_junk('"F18_AP_BLK"') }} as f18_ap_blk,
        {{ null_junk('"NH18_WHT"') }} as nh18_wht,
        {{ null_junk('"IDEAL_VALU"') }} as ideal_valu,
        {{ null_junk('"DEVIATION"') }} as deviation,
        {{ null_junk('"F_DEVIATIO"') }} as f_deviatio,
        {{ null_junk('"F_18_AP_BL"') }} as f_18_ap_bl,
        {{ null_junk('"F_NH18_WHT"') }} as f_nh18_wht,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        {{ null_junk('"SHAPE_AREA"') }} as shape_area,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-pennsylvania-u-s-legislative-congressional-district-boundaries' as _source_url

    from source

)

select * from renamed
