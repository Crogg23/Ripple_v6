version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_3C29FACD4A
        description: >
          Raw landing table for Allegheny County Pennsylvania U.S. Legislative Congressional District Boundaries. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_3c29facd4a__places
    description: "GRAIN: one row per place (GEOM is unique). SPINE_ENTITY: place."
    columns:
      - name: shape_area
        data_tests:
          - unique
          - not_null
