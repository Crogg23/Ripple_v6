{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE__LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 15 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   width: float
--   district: integer
--   length: float
--   sidewalkscore: float
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (SHAPE__LENGTH) is NOT unique within a load as landed at generation time -- it would collapse 23 of 99,147 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_OPEN_DATA_SA_2C667A6FC6') }}
    qualify row_number() over (partition by "OBJECTID", "CARTID", "SIDEWALKTYPE", "WIDTH", "MSAG_NAME", "PROJECTNAME", "JURIDICTION", "DISTRICT", "FROMSTREET", "TOSTREET", "LENGTH", "MAINTENANCERESPONSIBILITY", "STATUS", "SIDEWALKSCORE", "SHAPE__LENGTH" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"CARTID"') }} as cartid,
        {{ null_junk('"SIDEWALKTYPE"') }} as sidewalktype,
        {{ stg_float('"WIDTH"') }} as width,
        {{ null_junk('"MSAG_NAME"') }} as msag_name,
        {{ null_junk('"PROJECTNAME"') }} as projectname,
        {{ null_junk('"JURIDICTION"') }} as juridiction,
        {{ stg_int('"DISTRICT"') }} as district,
        {{ null_junk('"FROMSTREET"') }} as fromstreet,
        {{ null_junk('"TOSTREET"') }} as tostreet,
        {{ stg_float('"LENGTH"') }} as length,
        {{ null_junk('"MAINTENANCERESPONSIBILITY"') }} as maintenanceresponsibility,
        {{ null_junk('"STATUS"') }} as status,
        {{ stg_float('"SIDEWALKSCORE"') }} as sidewalkscore,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        INGESTED_AT as _loaded_at,
        'https://data.sanantonio.gov/dataset/sidewalks' as _source_url

    from source

)

select * from renamed
