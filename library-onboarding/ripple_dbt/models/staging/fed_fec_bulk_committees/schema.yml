version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_FEC_BULK_COMMITTEES
        description: >
          Raw landing table for FEC Bulk Data -- Committee Master (cm.txt), 2026 refresh. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_fed_fec_bulk_committees__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FEC_CMTE_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: fec_cmte_id
        data_tests:
          - unique
          - not_null
