{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PARENT_ID+CLASSIFICATION_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PARENT_ID, CLASSIFICATION_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 11 of 27 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   record_date: date
--   table_nbr: integer
--   src_line_nbr: integer
--   print_order_nbr: integer
--   line_code_nbr: integer
--   sequence_level_nbr: integer
--   record_fiscal_year: integer
--   record_fiscal_quarter: integer
--   record_calendar_year: integer
--   record_calendar_quarter: integer
--   record_calendar_day: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'FED_TREASURY_MTS_RECEIPTS') }}
    qualify row_number() over (partition by "PARENT_ID", "CLASSIFICATION_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_date('"RECORD_DATE"') }} as record_date,
        {{ null_junk('"PARENT_ID"') }} as parent_id,
        {{ null_junk('"CLASSIFICATION_ID"') }} as classification_id,
        {{ null_junk('"CLASSIFICATION_DESC"') }} as classification_desc,
        {{ null_junk('"CURRENT_MONTH_GROSS_RCPT_AMT"') }} as current_month_gross_rcpt_amt,
        {{ null_junk('"CURRENT_MONTH_REFUND_AMT"') }} as current_month_refund_amt,
        {{ null_junk('"CURRENT_MONTH_NET_RCPT_AMT"') }} as current_month_net_rcpt_amt,
        {{ null_junk('"CURRENT_FYTD_GROSS_RCPT_AMT"') }} as current_fytd_gross_rcpt_amt,
        {{ null_junk('"CURRENT_FYTD_REFUND_AMT"') }} as current_fytd_refund_amt,
        {{ null_junk('"CURRENT_FYTD_NET_RCPT_AMT"') }} as current_fytd_net_rcpt_amt,
        {{ null_junk('"PRIOR_FYTD_GROSS_RCPT_AMT"') }} as prior_fytd_gross_rcpt_amt,
        {{ null_junk('"PRIOR_FYTD_REFUND_AMT"') }} as prior_fytd_refund_amt,
        {{ null_junk('"PRIOR_FYTD_NET_RCPT_AMT"') }} as prior_fytd_net_rcpt_amt,
        {{ stg_int('"TABLE_NBR"') }} as table_nbr,
        {{ stg_int('"SRC_LINE_NBR"') }} as src_line_nbr,
        {{ stg_int('"PRINT_ORDER_NBR"') }} as print_order_nbr,
        {{ stg_int('"LINE_CODE_NBR"') }} as line_code_nbr,
        {{ null_junk('"DATA_TYPE_CD"') }} as data_type_cd,
        {{ null_junk('"RECORD_TYPE_CD"') }} as record_type_cd,
        {{ stg_int('"SEQUENCE_LEVEL_NBR"') }} as sequence_level_nbr,
        {{ null_junk('"SEQUENCE_NUMBER_CD"') }} as sequence_number_cd,
        {{ stg_int('"RECORD_FISCAL_YEAR"') }} as record_fiscal_year,
        {{ stg_int('"RECORD_FISCAL_QUARTER"') }} as record_fiscal_quarter,
        {{ stg_int('"RECORD_CALENDAR_YEAR"') }} as record_calendar_year,
        {{ stg_int('"RECORD_CALENDAR_QUARTER"') }} as record_calendar_quarter,
        {{ null_junk('"RECORD_CALENDAR_MONTH"') }} as record_calendar_month,
        {{ stg_int('"RECORD_CALENDAR_DAY"') }} as record_calendar_day,
        INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
