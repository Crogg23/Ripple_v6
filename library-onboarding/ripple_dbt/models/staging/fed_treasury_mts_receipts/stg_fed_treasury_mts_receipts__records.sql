{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PARENT_ID+CLASSIFICATION_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PARENT_ID, CLASSIFICATION_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'FED_TREASURY_MTS_RECEIPTS') }}

),

renamed as (

    select
        "RECORD_DATE" as record_date,
        "PARENT_ID" as parent_id,
        "CLASSIFICATION_ID" as classification_id,
        "CLASSIFICATION_DESC" as classification_desc,
        "CURRENT_MONTH_GROSS_RCPT_AMT" as current_month_gross_rcpt_amt,
        "CURRENT_MONTH_REFUND_AMT" as current_month_refund_amt,
        "CURRENT_MONTH_NET_RCPT_AMT" as current_month_net_rcpt_amt,
        "CURRENT_FYTD_GROSS_RCPT_AMT" as current_fytd_gross_rcpt_amt,
        "CURRENT_FYTD_REFUND_AMT" as current_fytd_refund_amt,
        "CURRENT_FYTD_NET_RCPT_AMT" as current_fytd_net_rcpt_amt,
        "PRIOR_FYTD_GROSS_RCPT_AMT" as prior_fytd_gross_rcpt_amt,
        "PRIOR_FYTD_REFUND_AMT" as prior_fytd_refund_amt,
        "PRIOR_FYTD_NET_RCPT_AMT" as prior_fytd_net_rcpt_amt,
        "TABLE_NBR" as table_nbr,
        "SRC_LINE_NBR" as src_line_nbr,
        "PRINT_ORDER_NBR" as print_order_nbr,
        "LINE_CODE_NBR" as line_code_nbr,
        "DATA_TYPE_CD" as data_type_cd,
        "RECORD_TYPE_CD" as record_type_cd,
        "SEQUENCE_LEVEL_NBR" as sequence_level_nbr,
        "SEQUENCE_NUMBER_CD" as sequence_number_cd,
        "RECORD_FISCAL_YEAR" as record_fiscal_year,
        "RECORD_FISCAL_QUARTER" as record_fiscal_quarter,
        "RECORD_CALENDAR_YEAR" as record_calendar_year,
        "RECORD_CALENDAR_QUARTER" as record_calendar_quarter,
        "RECORD_CALENDAR_MONTH" as record_calendar_month,
        "RECORD_CALENDAR_DAY" as record_calendar_day,
        INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by parent_id, classification_id order by _loaded_at desc) = 1
