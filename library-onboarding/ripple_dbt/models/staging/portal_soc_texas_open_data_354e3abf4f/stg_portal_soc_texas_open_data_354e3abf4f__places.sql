{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (ZIP+NOR_REGISTRATION_DATE unique)
-- SPINE_ENTITY: place  (natural_key: SITE_ZIP_CD, NOR_REGISTRATION_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 18 of 45 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   tceq_region_num: integer
--   long_dec_coord_num: float
--   nor_amend_date: timestamp
--   nor_reg_status_chg_date: timestamp
--   steers_flg: boolean
--   site_class_generator: boolean
--   site_class_rev_dist: boolean
--   aws_reqd_flg: boolean
--   site_class_receiver: boolean
--   mwrs_reqd_flg: boolean
--   site_class_transporter: boolean
--   transport_for_hire: boolean
--   transport_owned_waste: boolean
--   site_class_transfer: boolean
--   reg_healthcare_prov: boolean
--   site_class_recycler: boolean
--   healthcare_fac_optin_date: date
--   rcra_permit_num: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (SITE_ZIP_CD, NOR_REGISTRATION_DATE) is NOT unique within a load as landed at generation time -- it would collapse 6,206 of 42,272 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_TEXAS_OPEN_DATA_354E3ABF4F') }}
    qualify row_number() over (partition by "SWR_NUM_TXT", "RN_NUM_TXT", "EPA_ID_NUM", "FACILITY_SITE_NAME", "FACILITY_STATUS", "PHYS_LOC_DESC_TXT", "SITE_DELIV_TXT", "SITE_CITY_NAME", "SITE_COUNTY_NAME", "SITE_USPS_STATE_CD", "SITE_ZIP_CD", "SITE_COUNTRY_NAME", "TCEQ_REGION_NUM", "LAT_DEC_COORD_NUM", "LONG_DEC_COORD_NUM", "PRIMARY_NAICS_CODE", "LAND_TYP_CD", "NOR_REGISTRATION_DATE", "NOR_AMEND_DATE", "NOR_REG_STATUS_CHG_DATE", "STEERS_FLG", "SITE_CLASS_GENERATOR", "HW_GENERATOR_CLASS", "GENR_TYP_CD", "SITE_CLASS_REV_DIST", "AWS_REQD_FLG", "SITE_CLASS_RECEIVER", "MWRS_REQD_FLG", "SITE_CLASS_TRANSPORTER", "TRANSPORT_FOR_HIRE", "TRANSPORT_OWNED_WASTE", "SITE_CLASS_TRANSFER", "REG_HEALTHCARE_PROV", "SITE_CLASS_RECYCLER", "TRANSPORT_IND_CLASS1", "TRANSPORT_HW", "RCVR_FAC_CAT_CD", "IW_PMT_NUM_TXT", "HEALTHCARE_FAC_OPTIN_DATE", "MW_PMT_NUM_TXT", "RCRA_PERMIT_NUM", "HW_PERMIT_STATUS", "REV_DIST_BEGIN_DATE", "REV_DIST_END_DATE", "HEALTHCARE_FAC_WITHDRAW_DATE" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SWR_NUM_TXT"') }} as swr_num_txt,
        {{ null_junk('"RN_NUM_TXT"') }} as rn_num_txt,
        {{ null_junk('"EPA_ID_NUM"') }} as epa_id_num,
        {{ null_junk('"FACILITY_SITE_NAME"') }} as facility_site_name,
        {{ null_junk('"FACILITY_STATUS"') }} as facility_status,
        {{ null_junk('"PHYS_LOC_DESC_TXT"') }} as phys_loc_desc_txt,
        {{ null_junk('"SITE_DELIV_TXT"') }} as site_deliv_txt,
        {{ null_junk('"SITE_CITY_NAME"') }} as site_city_name,
        {{ null_junk('"SITE_COUNTY_NAME"') }} as site_county_name,
        {{ null_junk('"SITE_USPS_STATE_CD"') }} as site_usps_state_cd,
        {{ null_junk('"SITE_ZIP_CD"') }} as site_zip_cd,
        {{ null_junk('"SITE_COUNTRY_NAME"') }} as site_country_name,
        {{ stg_int('"TCEQ_REGION_NUM"') }} as tceq_region_num,
        {{ null_junk('"LAT_DEC_COORD_NUM"') }} as lat_dec_coord_num,
        {{ stg_float('"LONG_DEC_COORD_NUM"') }} as long_dec_coord_num,
        {{ null_junk('"PRIMARY_NAICS_CODE"') }} as primary_naics_code,
        {{ null_junk('"LAND_TYP_CD"') }} as land_typ_cd,
        {{ null_junk('"NOR_REGISTRATION_DATE"') }} as nor_registration_date,
        {{ stg_ts('"NOR_AMEND_DATE"') }} as nor_amend_date,
        {{ stg_ts('"NOR_REG_STATUS_CHG_DATE"') }} as nor_reg_status_chg_date,
        {{ stg_bool('"STEERS_FLG"') }} as steers_flg,
        {{ stg_bool('"SITE_CLASS_GENERATOR"') }} as site_class_generator,
        {{ null_junk('"HW_GENERATOR_CLASS"') }} as hw_generator_class,
        {{ null_junk('"GENR_TYP_CD"') }} as genr_typ_cd,
        {{ stg_bool('"SITE_CLASS_REV_DIST"') }} as site_class_rev_dist,
        {{ stg_bool('"AWS_REQD_FLG"') }} as aws_reqd_flg,
        {{ stg_bool('"SITE_CLASS_RECEIVER"') }} as site_class_receiver,
        {{ stg_bool('"MWRS_REQD_FLG"') }} as mwrs_reqd_flg,
        {{ stg_bool('"SITE_CLASS_TRANSPORTER"') }} as site_class_transporter,
        {{ stg_bool('"TRANSPORT_FOR_HIRE"') }} as transport_for_hire,
        {{ stg_bool('"TRANSPORT_OWNED_WASTE"') }} as transport_owned_waste,
        {{ stg_bool('"SITE_CLASS_TRANSFER"') }} as site_class_transfer,
        {{ stg_bool('"REG_HEALTHCARE_PROV"') }} as reg_healthcare_prov,
        {{ stg_bool('"SITE_CLASS_RECYCLER"') }} as site_class_recycler,
        {{ null_junk('"TRANSPORT_IND_CLASS1"') }} as transport_ind_class1,
        {{ null_junk('"TRANSPORT_HW"') }} as transport_hw,
        {{ null_junk('"RCVR_FAC_CAT_CD"') }} as rcvr_fac_cat_cd,
        {{ null_junk('"IW_PMT_NUM_TXT"') }} as iw_pmt_num_txt,
        {{ stg_date('"HEALTHCARE_FAC_OPTIN_DATE"') }} as healthcare_fac_optin_date,
        {{ null_junk('"MW_PMT_NUM_TXT"') }} as mw_pmt_num_txt,
        {{ stg_id_text('"RCRA_PERMIT_NUM"') }} as rcra_permit_num,
        {{ null_junk('"HW_PERMIT_STATUS"') }} as hw_permit_status,
        {{ null_junk('"REV_DIST_BEGIN_DATE"') }} as rev_dist_begin_date,
        {{ null_junk('"REV_DIST_END_DATE"') }} as rev_dist_end_date,
        {{ null_junk('"HEALTHCARE_FAC_WITHDRAW_DATE"') }} as healthcare_fac_withdraw_date,
        INGESTED_AT as _loaded_at,
        'https://data.texas.gov/d/v287-9kbw' as _source_url

    from source

)

select * from renamed
