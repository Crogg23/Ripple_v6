version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_OPEN_DATA_DC_454981B380
        description: >
          Raw landing table for MPD Crime Blotter Survey results. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_open_data_dc_454981b380__facilities
    description: "GRAIN: one row per facility (CCN is unique). SPINE_ENTITY: facility."
    columns:
      - name: ccn
        data_tests:
          - unique
          - not_null
