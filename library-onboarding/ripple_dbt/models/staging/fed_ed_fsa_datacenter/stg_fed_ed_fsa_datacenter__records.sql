{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_ED_FSA_DATACENTER') }}

),

renamed as (

    select
        LOAN_TYPE as loan_type,
        BORROWER_COUNT as borrower_count,
        OUTSTANDING_BALANCE as outstanding_balance,
        REPAYMENT_STATUS as repayment_status,
        DEFAULT_RATE as default_rate,
        SCHOOL_NAME as school_name,
        SCHOOL_OPEID as school_opeid,
        EIN as ein,
        STATE as state,
        QUARTER_DATE as quarter_date,
        AID_YEAR as aid_year,
        DISBURSEMENT_AMOUNT as disbursement_amount,
        LOAN_SERVICER as loan_servicer,
        INCOME_DRIVEN_REPAYMENT_PLAN as income_driven_repayment_plan,
        _INGESTED_AT as _loaded_at,
        'https://studentaid.gov/data-center' as _source_url

    from source

)

select * from renamed
