{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (BUS_LICENSE_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: BUS_LICENSE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_511B70EB2B') }}

),

renamed as (

    select
        "OPERATOR_NM" as operator_nm,
        "CLUSTER_NM" as cluster_nm,
        "BUS_LICENSE_ID" as bus_license_id,
        "STONE_PROOF_NM" as stone_proof_nm,
        "BULLET_PROOF_NM" as bullet_proof_nm,
        "BUSSIZE_NM" as bussize_nm,
        "BUSTYPE_NM" as bustype_nm,
        "SEATSNUM" as seatsnum,
        "PRODUCTION_YEAR" as production_year,
        "PRODUCTION_COUNTRY" as production_country,
        "PROPULSIONTYPE_NM" as propulsiontype_nm,
        "TOTAL_KILOMETER" as total_kilometer,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/bus_fleet' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by bus_license_id order by _loaded_at desc) = 1
