version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ISRAEL_NATIONAL_511B70EB2B
        description: >
          Raw landing table for ציי רכב אוטובוסים. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_israel_national_511b70eb2b__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (BUS_LICENSE_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: bus_license_id
        data_tests:
          - unique
          - not_null
