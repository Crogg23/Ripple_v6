version: 2

models:
  - name: stg_fed_sec_dera_sub_2026q1__organizations
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per organization (CIK+PERIOD+FY unique). SPINE_ENTITY: organization."
    columns:
      - name: cik
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: period
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: fy
        data_tests:
          - not_null:
              config:
                severity: warn
