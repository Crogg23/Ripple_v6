version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_HUD_DATA
        description: >
          Raw landing table for HUD Open Data (huduser.gov). Grain not yet determined.

models:
  - name: stg_fed_hud_data__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
