{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 10 of 21 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   budgeted_amount: float
--   fiscal_year: integer
--   start_date: date
--   inactive: boolean
--   council_district: integer
--   ward: integer
--   tract: integer
--   public_works_division: integer
--   pli_division: integer
--   police_zone: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_20C6FC6029') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"TASK_DESCRIPTION"') }} as task_description,
        {{ null_junk('"AREA"') }} as area,
        {{ stg_float('"BUDGETED_AMOUNT"') }} as budgeted_amount,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"ASSET_ID"') }} as asset_id,
        {{ null_junk('"ASSET_TYPE"') }} as asset_type,
        {{ stg_int('"FISCAL_YEAR"') }} as fiscal_year,
        {{ stg_date('"START_DATE"') }} as start_date,
        {{ stg_bool('"INACTIVE"') }} as inactive,
        {{ null_junk('"NEIGHBORHOOD"') }} as neighborhood,
        {{ stg_int('"COUNCIL_DISTRICT"') }} as council_district,
        {{ stg_int('"WARD"') }} as ward,
        {{ stg_int('"TRACT"') }} as tract,
        {{ stg_int('"PUBLIC_WORKS_DIVISION"') }} as public_works_division,
        {{ stg_int('"PLI_DIVISION"') }} as pli_division,
        {{ stg_int('"POLICE_ZONE"') }} as police_zone,
        {{ null_junk('"FIRE_ZONE"') }} as fire_zone,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/capital-projects' as _source_url

    from source

)

select * from renamed
