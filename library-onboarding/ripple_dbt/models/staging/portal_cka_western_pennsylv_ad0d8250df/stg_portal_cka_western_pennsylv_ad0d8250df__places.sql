{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LATITUDE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_AD0D8250DF') }}

),

renamed as (

    select
        "PIN" as pin,
        "ADDRESS" as address,
        "BILLING_CITY" as billing_city,
        "CURRENT_DELQ_TAX" as current_delq_tax,
        "CURRENT_DELQ_PI" as current_delq_pi,
        "PRIOR_YEARS" as prior_years,
        "PRIOR_DELQ_TAX" as prior_delq_tax,
        "PRIOR_DELQ_PI" as prior_delq_pi,
        "STATE_DESCRIPTION" as state_description,
        "NEIGHBORHOOD" as neighborhood,
        "COUNCIL_DISTRICT" as council_district,
        "WARD" as ward,
        "PUBLIC_WORKS_DIVISION" as public_works_division,
        "PLI_DIVISION" as pli_division,
        "POLICE_ZONE" as police_zone,
        "FIRE_ZONE" as fire_zone,
        "LONGITUDE" as longitude,
        "LATITUDE" as latitude,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/city-of-pittsburgh-property-tax-delinquency' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by latitude order by _loaded_at desc) = 1
