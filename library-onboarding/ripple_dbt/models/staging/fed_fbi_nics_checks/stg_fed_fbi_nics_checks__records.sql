{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_FBI_NICS_CHECKS') }}

),

renamed as (

    select
        MONTH as month,
        STATE as state,
        PERMIT as permit,
        PERMIT_RECHECK as permit_recheck,
        HANDGUN as handgun,
        LONG_GUN as long_gun,
        OTHER as other,
        MULTIPLE as multiple,
        ADMIN as admin,
        PREPAWN_HANDGUN as prepawn_handgun,
        PREPAWN_LONG_GUN as prepawn_long_gun,
        PREPAWN_OTHER as prepawn_other,
        REDEMPTION_HANDGUN as redemption_handgun,
        REDEMPTION_LONG_GUN as redemption_long_gun,
        REDEMPTION_OTHER as redemption_other,
        RETURNED_HANDGUN as returned_handgun,
        RETURNED_LONG_GUN as returned_long_gun,
        RETURNED_OTHER as returned_other,
        RENTALS_HANDGUN as rentals_handgun,
        RENTALS_LONG_GUN as rentals_long_gun,
        PRIVATE_SALE_HANDGUN as private_sale_handgun,
        PRIVATE_SALE_LONG_GUN as private_sale_long_gun,
        PRIVATE_SALE_OTHER as private_sale_other,
        RETURN_TO_SELLER_HANDGUN as return_to_seller_handgun,
        RETURN_TO_SELLER_LONG_GUN as return_to_seller_long_gun,
        RETURN_TO_SELLER_OTHER as return_to_seller_other,
        TOTALS as totals,
        INGESTED_AT as _loaded_at,
        'https://raw.githubusercontent.com/BuzzFeedNews/nics-firearm-background-checks/master/data/nics-firearm-background-checks.csv' as _source_url

    from source

)

select * from renamed
