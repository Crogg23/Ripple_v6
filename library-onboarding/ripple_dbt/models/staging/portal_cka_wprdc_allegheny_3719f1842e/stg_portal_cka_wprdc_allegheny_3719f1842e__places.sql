{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 49 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   capacity: integer
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   typecode: integer
--   x: float
--   y: float
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_3719F1842E') }}
    qualify row_number() over (partition by "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ADA_ACCESSIBLE_FACILITY"') }} as ada_accessible_facility,
        {{ null_junk('"ADA_ACCESSIBLE_PLAYGROUND__NEAR"') }} as ada_accessible_playground__near,
        {{ null_junk('"ADA_ACCESSIBLE_PORT_A_JOHN__NEA"') }} as ada_accessible_port_a_john__nea,
        {{ null_junk('"ADA_ACCESSIBLE_RESTROOM"') }} as ada_accessible_restroom,
        {{ null_junk('"ACCESS_TO_FIRE_EXTINGUISHER"') }} as access_to_fire_extinguisher,
        {{ null_junk('"ACCESS_TO_KITCHENETTE"') }} as access_to_kitchenette,
        {{ null_junk('"ACCESS_TO_PARKING_LOT"') }} as access_to_parking_lot,
        {{ null_junk('"ACCESS_TO_PLAYGROUND"') }} as access_to_playground,
        {{ null_junk('"ACCESS_TO_RESTROOMS"') }} as access_to_restrooms,
        {{ null_junk('"ACCESS_TO_SPRAY_PARK"') }} as access_to_spray_park,
        {{ null_junk('"ACCESS_TO_VENDING_MACHINES"') }} as access_to_vending_machines,
        {{ null_junk('"BOCCI_COURT"') }} as bocci_court,
        {{ stg_int('"CAPACITY"') }} as capacity,
        {{ null_junk('"CENTER"') }} as center,
        {{ null_junk('"CENTERCODE"') }} as centercode,
        {{ null_junk('"FACILITY_TYPE"') }} as facility_type,
        {{ null_junk('"FEATURENAME"') }} as featurename,
        {{ null_junk('"FURNITURE__CHAIRS"') }} as furniture__chairs,
        {{ null_junk('"FURNITURE__TABLES"') }} as furniture__tables,
        {{ null_junk('"GLOBALID"') }} as globalid,
        {{ null_junk('"INDOOR__AIR_CONDITIONING"') }} as indoor__air_conditioning,
        {{ null_junk('"INDOOR__AUDIO_SYSTEM"') }} as indoor__audio_system,
        {{ null_junk('"INDOOR__HEATING"') }} as indoor__heating,
        {{ null_junk('"INDOOR__TELEVISION"') }} as indoor__television,
        {{ null_junk('"NAME"') }} as name,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"OUTDOOR__BASKETBALL_COURTS__NEA"') }} as outdoor__basketball_courts__nea,
        {{ null_junk('"OUTDOOR__FISHING"') }} as outdoor__fishing,
        {{ null_junk('"OUTDOOR__GRILL"') }} as outdoor__grill,
        {{ null_junk('"OUTDOOR__HORSESHOE_PITS__NEAR"') }} as outdoor__horseshoe_pits__near,
        {{ null_junk('"OUTDOOR__LAKE_VIEW"') }} as outdoor__lake_view,
        {{ null_junk('"OUTDOOR__LIGHTING"') }} as outdoor__lighting,
        {{ null_junk('"OUTDOOR__SWIMMING_POOL__NEAR"') }} as outdoor__swimming_pool__near,
        {{ null_junk('"OUTDOOR__VOLLEYBALL"') }} as outdoor__volleyball,
        {{ null_junk('"OUTDOOR__WATER_SPIGOT"') }} as outdoor__water_spigot,
        {{ null_junk('"OUTLET__DATA"') }} as outlet__data,
        {{ null_junk('"OUTLET__POWER"') }} as outlet__power,
        {{ null_junk('"OUTLET__TELEPHONE"') }} as outlet__telephone,
        {{ null_junk('"PET_FRIENDLY"') }} as pet_friendly,
        {{ null_junk('"PORT_A_JOHN__NEAR"') }} as port_a_john__near,
        {{ null_junk('"RESOURCE_TYPE"') }} as resource_type,
        {{ null_junk('"SMOKING_NOT_PERMITTED"') }} as smoking_not_permitted,
        {{ null_junk('"SMOKING_PERMITTED"') }} as smoking_permitted,
        {{ stg_int('"TYPECODE"') }} as typecode,
        {{ null_junk('"UNICODE"') }} as unicode,
        {{ null_junk('"WEBSITE"') }} as website,
        {{ stg_float('"X"') }} as x,
        {{ stg_float('"Y"') }} as y,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-park-features' as _source_url

    from source

)

select * from renamed
