version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: XC_OWID_CO2
        description: >
          Raw landing table for OWID — Annual CO2 emissions by country. Grain not yet determined.

models:
  - name: stg_xc_owid_co2__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
