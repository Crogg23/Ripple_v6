version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_VIRGINIA_OPEN_DA_77706D86DA
        description: >
          Raw landing table for Stormwater Permits Daily. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_virginia_open_da_77706d86da__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (SWC_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: swc_id
        data_tests:
          - unique
          - not_null
