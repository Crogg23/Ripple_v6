{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per filing (FILING_ID+AMEND_ID unique)
-- SPINE_ENTITY: filing  (natural_key: FILING_ID, AMEND_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 42 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'filing' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'CA_LOBBY_AMENDMENTS') }}
    qualify row_number() over (partition by "FILING_ID", "AMEND_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"FILING_ID"') }} as filing_id,
        {{ null_junk('"AMEND_ID"') }} as amend_id,
        {{ null_junk('"REC_TYPE"') }} as rec_type,
        {{ null_junk('"FORM_TYPE"') }} as form_type,
        {{ null_junk('"EXEC_DATE"') }} as exec_date,
        {{ null_junk('"FROM_DATE"') }} as from_date,
        {{ null_junk('"THRU_DATE"') }} as thru_date,
        {{ null_junk('"ADD_L_CB"') }} as add_l_cb,
        {{ null_junk('"ADD_L_EFF"') }} as add_l_eff,
        {{ null_junk('"A_L_NAML"') }} as a_l_naml,
        {{ null_junk('"A_L_NAMF"') }} as a_l_namf,
        {{ null_junk('"A_L_NAMT"') }} as a_l_namt,
        {{ null_junk('"A_L_NAMS"') }} as a_l_nams,
        {{ null_junk('"DEL_L_CB"') }} as del_l_cb,
        {{ null_junk('"DEL_L_EFF"') }} as del_l_eff,
        {{ null_junk('"D_L_NAML"') }} as d_l_naml,
        {{ null_junk('"D_L_NAMF"') }} as d_l_namf,
        {{ null_junk('"D_L_NAMT"') }} as d_l_namt,
        {{ null_junk('"D_L_NAMS"') }} as d_l_nams,
        {{ null_junk('"ADD_LE_CB"') }} as add_le_cb,
        {{ null_junk('"ADD_LE_EFF"') }} as add_le_eff,
        {{ null_junk('"A_LE_NAML"') }} as a_le_naml,
        {{ null_junk('"A_LE_NAMF"') }} as a_le_namf,
        {{ null_junk('"A_LE_NAMT"') }} as a_le_namt,
        {{ null_junk('"A_LE_NAMS"') }} as a_le_nams,
        {{ null_junk('"DEL_LE_CB"') }} as del_le_cb,
        {{ null_junk('"DEL_LE_EFF"') }} as del_le_eff,
        {{ null_junk('"D_LE_NAML"') }} as d_le_naml,
        {{ null_junk('"D_LE_NAMF"') }} as d_le_namf,
        {{ null_junk('"D_LE_NAMT"') }} as d_le_namt,
        {{ null_junk('"D_LE_NAMS"') }} as d_le_nams,
        {{ null_junk('"ADD_LF_CB"') }} as add_lf_cb,
        {{ null_junk('"ADD_LF_EFF"') }} as add_lf_eff,
        {{ null_junk('"A_LF_NAME"') }} as a_lf_name,
        {{ null_junk('"DEL_LF_CB"') }} as del_lf_cb,
        {{ null_junk('"DEL_LF_EFF"') }} as del_lf_eff,
        {{ null_junk('"D_LF_NAME"') }} as d_lf_name,
        {{ null_junk('"OTHER_CB"') }} as other_cb,
        {{ null_junk('"OTHER_EFF"') }} as other_eff,
        {{ null_junk('"OTHER_DESC"') }} as other_desc,
        {{ null_junk('"F606_YES"') }} as f606_yes,
        {{ null_junk('"F606_NO"') }} as f606_no,
        INGESTED_AT as _loaded_at,
        'https://www.sos.ca.gov/campaign-lobbying/cal-access-resources/raw-data-campaign-finance-and-lobbying-activity' as _source_url

    from source

)

select * from renamed
