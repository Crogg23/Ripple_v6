{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_CDC_HEALTH_INSURANCE') }}

),

renamed as (

    select
        INDICATOR as indicator,
        C_GROUP as c_group,
        STATE as state,
        SUBGROUP as subgroup,
        PHASE as phase,
        TIME_PERIOD as time_period,
        TIME_PERIOD_LABEL as time_period_label,
        TIME_PERIOD_START_DATE as time_period_start_date,
        TIME_PERIOD_END_DATE as time_period_end_date,
        VALUE as value,
        LOWCI as lowci,
        HIGHCI as highci,
        CONFIDENCE_INTERVAL as confidence_interval,
        QUARTILE_RANGE as quartile_range,
        QUARTILE_NUMBER as quartile_number,
        SUPPRESSION_FLAG as suppression_flag,
        INGESTED_AT as _loaded_at,
        'https://data.cdc.gov/resource/jb9g-gnvr.csv?$limit=2000000' as _source_url

    from source

)

select * from renamed
