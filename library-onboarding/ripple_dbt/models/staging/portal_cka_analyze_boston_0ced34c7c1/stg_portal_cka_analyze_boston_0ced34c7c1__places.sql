{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (ZIP is unique)
-- SPINE_ENTITY: place  (natural_key: ZIP)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 14 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_0CED34C7C1') }}
    qualify row_number() over (partition by "ZIP" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"BLDG_ID"') }} as bldg_id,
        {{ null_junk('"BID"') }} as bid,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"POINT_X"') }} as point_x,
        {{ null_junk('"POINT_Y"') }} as point_y,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"NEIGHBORHOOD"') }} as neighborhood,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"FT_SQFT"') }} as ft_sqft,
        {{ null_junk('"STORY_HT"') }} as story_ht,
        {{ null_junk('"PARCEL_ID"') }} as parcel_id,
        {{ null_junk('"DISTRICT"') }} as district,
        {{ null_junk('"SHAPE_WKT"') }} as shape_wkt,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/boston-police-stations-bpd-only' as _source_url

    from source

)

select * from renamed
