version: 2

models:
  - name: stg_fed_epa_npdes_icis_facilities__facilities
    description: "GRAIN: one row per facility (NPDES_ID is unique). SPINE_ENTITY: facility."
    columns:
      - name: npdes_id
        data_tests:
          - unique
          - not_null
