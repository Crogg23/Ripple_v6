{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_AREA)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_846214A7C0') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "FACILITYID" as facilityid,
        "GISOBJID" as gisobjid,
        "LOCATIONID" as locationid,
        "BUILDINGELEV" as buildingelev,
        "BUILDINGHEIGHT" as buildingheight,
        "ADDRESS" as address,
        "PARKNAME" as parkname,
        "COUNCILDISTRICT" as councildistrict,
        "PARKDISTRICT" as parkdistrict,
        "SOLAR" as solar,
        "ELECTRICAL" as electrical,
        "SANITARY" as sanitary,
        "WATER" as water,
        "HVAC" as hvac,
        "KITCHEN" as kitchen,
        "HISTORIC" as historic,
        "RESTROOM" as restroom,
        "NUMBEROFFLOORS" as numberoffloors,
        "PARKING" as parking,
        "HANDICAPPARKING" as handicapparking,
        "BLDGTYPE" as bldgtype,
        "BLDGMATERIALTYPE" as bldgmaterialtype,
        "BUILDINGNAME" as buildingname,
        "ESTIMATEDAREA" as estimatedarea,
        "ADAACCESS" as adaaccess,
        "REVENUEGEN" as revenuegen,
        "EMERGENCYUSE" as emergencyuse,
        "EQUIPMENTSTORAGE" as equipmentstorage,
        "MAINTBY" as maintby,
        "LASTUPDATE" as lastupdate,
        "ENTERPRISEID" as enterpriseid,
        "BUILDINGSTATUS" as buildingstatus,
        "INTID" as intid,
        "PARKTYPE" as parktype,
        "PARKCLASS" as parkclass,
        "PARKSUBCLASS" as parksubclass,
        "SHAPE_LENGTH" as shape_length,
        "SHAPE_AREA" as shape_area,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/park-buildings' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by shape_area order by _loaded_at desc) = 1
