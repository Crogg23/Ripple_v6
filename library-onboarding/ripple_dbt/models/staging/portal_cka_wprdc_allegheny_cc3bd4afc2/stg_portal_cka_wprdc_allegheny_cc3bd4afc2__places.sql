{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (FIPS is unique)
-- SPINE_ENTITY: place  (natural_key: GEOID20)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_CC3BD4AFC2') }}

),

renamed as (

    select
        "ALAND20" as aland20,
        "AWATER20" as awater20,
        "BLOCKCE20" as blockce20,
        "COUNTYFP20" as countyfp20,
        "FID" as fid,
        "FUNCSTAT20" as funcstat20,
        "GEOID20" as geoid20,
        "GEOIDFQ20" as geoidfq20,
        "HOUSING20" as housing20,
        "INTPTLAT20" as intptlat20,
        "INTPTLON20" as intptlon20,
        "MTFCC20" as mtfcc20,
        "NAME20" as name20,
        "POP20" as pop20,
        "STATEFP20" as statefp20,
        "SHAPE__AREA" as shape__area,
        "SHAPE__LENGTH" as shape__length,
        "TRACTCE20" as tractce20,
        "UACE20" as uace20,
        "UR20" as ur20,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-census-blocks-2021' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by geoid20 order by _loaded_at desc) = 1
