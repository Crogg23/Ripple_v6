{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (POLYGON_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: POLYGON_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_5FC2A4D010') }}

),

renamed as (

    select
        "STATUS" as status,
        "POLYGON_ID" as polygon_id,
        "PARK_NAME" as park_name,
        "ALT_NAME" as alt_name,
        "PARK_ADDRESS" as park_address,
        "NEIGHBORHOOD" as neighborhood,
        "INCLUSION" as inclusion,
        "STAIR_FREE" as stair_free,
        "BENCHES_WHEELCHAIR" as benches_wheelchair,
        "TABLE_WHEELCHAIR" as table_wheelchair,
        "DRINKING_FOUNTAIN" as drinking_fountain,
        "SPRAY_PLAY" as spray_play,
        "BENCHES_ALONG_PATH" as benches_along_path,
        "SHADED_SEATING" as shaded_seating,
        "SHADE_TYPE" as shade_type,
        "BATHROOM" as bathroom,
        "ACCESSIBLE_PLAY" as accessible_play,
        "SENSORY_PLAY" as sensory_play,
        "OTHER_INCLUSIVE" as other_inclusive,
        "COMMUNICATION" as communication,
        "PARKING_TYPE" as parking_type,
        "NEAR_BUS" as near_bus,
        "NEAR_TRAIN" as near_train,
        "DISSOLVED_OS_ADDRESS" as dissolved_os_address,
        "DISSOLVED_OS" as dissolved_os,
        "DISSOLVED_OS_DISTRICT" as dissolved_os_district,
        "DISSOLVED_OS_ZIP_CODE" as dissolved_os_zip_code,
        "SHAPE_LENGTH" as shape_length,
        "SHAPE_AREA" as shape_area,
        "SHAPE_WKT" as shape_wkt,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/bprd-accessible-park-details-augmented' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by polygon_id order by _loaded_at desc) = 1
