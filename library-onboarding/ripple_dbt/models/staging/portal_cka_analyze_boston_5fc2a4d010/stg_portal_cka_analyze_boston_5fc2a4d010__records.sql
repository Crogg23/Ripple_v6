{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (POLYGON_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: POLYGON_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 30 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_5FC2A4D010') }}
    qualify row_number() over (partition by "POLYGON_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"POLYGON_ID"') }} as polygon_id,
        {{ null_junk('"PARK_NAME"') }} as park_name,
        {{ null_junk('"ALT_NAME"') }} as alt_name,
        {{ null_junk('"PARK_ADDRESS"') }} as park_address,
        {{ null_junk('"NEIGHBORHOOD"') }} as neighborhood,
        {{ null_junk('"INCLUSION"') }} as inclusion,
        {{ null_junk('"STAIR_FREE"') }} as stair_free,
        {{ null_junk('"BENCHES_WHEELCHAIR"') }} as benches_wheelchair,
        {{ null_junk('"TABLE_WHEELCHAIR"') }} as table_wheelchair,
        {{ null_junk('"DRINKING_FOUNTAIN"') }} as drinking_fountain,
        {{ null_junk('"SPRAY_PLAY"') }} as spray_play,
        {{ null_junk('"BENCHES_ALONG_PATH"') }} as benches_along_path,
        {{ null_junk('"SHADED_SEATING"') }} as shaded_seating,
        {{ null_junk('"SHADE_TYPE"') }} as shade_type,
        {{ null_junk('"BATHROOM"') }} as bathroom,
        {{ null_junk('"ACCESSIBLE_PLAY"') }} as accessible_play,
        {{ null_junk('"SENSORY_PLAY"') }} as sensory_play,
        {{ null_junk('"OTHER_INCLUSIVE"') }} as other_inclusive,
        {{ null_junk('"COMMUNICATION"') }} as communication,
        {{ null_junk('"PARKING_TYPE"') }} as parking_type,
        {{ null_junk('"NEAR_BUS"') }} as near_bus,
        {{ null_junk('"NEAR_TRAIN"') }} as near_train,
        {{ null_junk('"DISSOLVED_OS_ADDRESS"') }} as dissolved_os_address,
        {{ null_junk('"DISSOLVED_OS"') }} as dissolved_os,
        {{ null_junk('"DISSOLVED_OS_DISTRICT"') }} as dissolved_os_district,
        {{ null_junk('"DISSOLVED_OS_ZIP_CODE"') }} as dissolved_os_zip_code,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        {{ null_junk('"SHAPE_AREA"') }} as shape_area,
        {{ null_junk('"SHAPE_WKT"') }} as shape_wkt,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/bprd-accessible-park-details-augmented' as _source_url

    from source

)

select * from renamed
