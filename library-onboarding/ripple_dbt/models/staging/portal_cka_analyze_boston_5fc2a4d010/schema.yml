version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_5FC2A4D010
        description: >
          Raw landing table for BPRD Accessible Park Details Augmented. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_5fc2a4d010__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (POLYGON_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: polygon_id
        data_tests:
          - unique
          - not_null
