{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (EQUIPMENT_GROUP_ID+MANUFACTER_ID+MODEL_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: EQUIPMENT_GROUP_ID, MANUFACTER_ID, MODEL_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 11 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_44788840FC') }}
    qualify row_number() over (partition by "EQUIPMENT_GROUP_ID", "MANUFACTER_ID", "MODEL_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"EQUIPMENT_GROUP_ID"') }} as equipment_group_id,
        {{ null_junk('"EQUIPMENT_GROUP"') }} as equipment_group,
        {{ null_junk('"DEVICE_TYPE"') }} as device_type,
        {{ null_junk('"MANUFACTER_ID"') }} as manufacter_id,
        {{ null_junk('"MANUFACTER_NAME"') }} as manufacter_name,
        {{ null_junk('"MODEL_ID"') }} as model_id,
        {{ null_junk('"MANUFACTOR_MODEL"') }} as manufactor_model,
        {{ null_junk('"EQITF_IS_COMMERCIAL"') }} as eqitf_is_commercial,
        {{ null_junk('"EQITF_CONFIRMATION_NUMBER"') }} as eqitf_confirmation_number,
        {{ null_junk('"EQITF_CONFIRMATION_VALIDITY"') }} as eqitf_confirmation_validity,
        {{ null_junk('"IS_VALID"') }} as is_valid,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/metrology' as _source_url

    from source

)

select * from renamed
