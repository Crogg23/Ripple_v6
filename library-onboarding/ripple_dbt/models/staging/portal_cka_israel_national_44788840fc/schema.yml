version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ISRAEL_NATIONAL_44788840FC
        description: >
          Raw landing table for משקלות ומידות. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_israel_national_44788840fc__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (EQUIPMENT_GROUP_ID+MANUFACTER_ID+MODEL_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: equipment_group_id
        data_tests:
          - not_null
      - name: manufacter_id
        data_tests:
          - not_null
      - name: model_id
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - equipment_group_id
              - manufacter_id
              - model_id