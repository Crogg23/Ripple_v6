{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (FIPS is unique)
-- SPINE_ENTITY: place  (natural_key: GEOID10)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_D179E603D1') }}

),

renamed as (

    select
        "STATEFP10" as statefp10,
        "CREATED_DATE" as created_date,
        "PLANNERASSIGN" as plannerassign,
        "PGHDB_SDE_NEIGHBORHOOD_2010_ARE" as pghdb_sde_neighborhood_2010_are,
        "FUNCSTAT10" as funcstat10,
        "MTFCC10" as mtfcc10,
        "LAST_EDITED_DATE" as last_edited_date,
        "OBJECTID" as objectid,
        "HOOD_NO" as hood_no,
        "SQMILES" as sqmiles,
        "LAST_EDITED_USER" as last_edited_user,
        "BLKGRPCE10" as blkgrpce10,
        "GLOBALID" as globalid,
        "NAMELSAD10" as namelsad10,
        "FID_NEIGHB" as fid_neighb,
        "COUNTYFP10" as countyfp10,
        "AWATER10" as awater10,
        "SHAPE__AREA" as shape__area,
        "ACRES" as acres,
        "PAGE_NUMBER" as page_number,
        "SHAPE_AR_1" as shape_ar_1,
        "CREATED_USER" as created_user,
        "INTPTLON10" as intptlon10,
        "NEIGHBOR" as neighbor,
        "INTPTLAT10" as intptlat10,
        "FID_BLOCKG" as fid_blockg,
        "TEMP" as temp,
        "GEOID10" as geoid10,
        "UNIQUE_ID" as unique_id,
        "PERIMETER" as perimeter,
        "TRACTCE10" as tractce10,
        "SHAPE_LE_1" as shape_le_1,
        "NEIGHBOR_I" as neighbor_i,
        "SECTORS" as sectors,
        "SHAPE__LENGTH" as shape__length,
        "DPWDIV" as dpwdiv,
        "SHAPE_LENG" as shape_leng,
        "HOOD" as hood,
        "ALAND10" as aland10,
        "DATASPATIAL_WKB" as dataspatial_wkb,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/neighborhoods2' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by geoid10 order by _loaded_at desc) = 1
