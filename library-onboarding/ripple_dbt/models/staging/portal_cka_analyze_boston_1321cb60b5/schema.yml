version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_1321CB60B5
        description: >
          Raw landing table for Vision Zero Fatality Records. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_1321cb60b5__places
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (LATLON+DATE_TIME unique). SPINE_ENTITY: place."
    columns:
      - name: lat
        data_tests:
          - not_null
      - name: date_time
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - lat
            - date_time
