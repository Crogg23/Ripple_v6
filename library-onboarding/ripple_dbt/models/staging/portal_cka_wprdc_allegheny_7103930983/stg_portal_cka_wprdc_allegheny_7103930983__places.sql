{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 11 of 16 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   angle: integer
--   area: integer
--   ballfiel_1: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   ballfields: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   perimeter: integer
--   point_x: float
--   point_y: float
--   polygonid: integer
--   scale: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_7103930983') }}
    qualify row_number() over (partition by "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ACTIVE"') }} as active,
        {{ stg_int('"ANGLE"') }} as angle,
        {{ stg_int('"AREA"') }} as area,
        {{ stg_id_text('"BALLFIEL_1"') }} as ballfiel_1,
        {{ stg_id_text('"BALLFIELDS"') }} as ballfields,
        {{ null_junk('"CITYID"') }} as cityid,
        {{ null_junk('"DPW_NAME"') }} as dpw_name,
        {{ null_junk('"FIELD_NAME"') }} as field_name,
        {{ stg_id_text('"ID"') }} as id,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_int('"PERIMETER"') }} as perimeter,
        {{ stg_float('"POINT_X"') }} as point_x,
        {{ stg_float('"POINT_Y"') }} as point_y,
        {{ stg_int('"POLYGONID"') }} as polygonid,
        {{ stg_int('"SCALE"') }} as scale,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/ballfields-pgh' as _source_url

    from source

)

select * from renamed
