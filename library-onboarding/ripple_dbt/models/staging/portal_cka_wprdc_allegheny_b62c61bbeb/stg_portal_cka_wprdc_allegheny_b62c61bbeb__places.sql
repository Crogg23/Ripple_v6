{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: DATASPATIAL_WKB)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 4 of 13 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   bikeinfdis: float
--   slmdist: float
--   id: integer
--   bldist: float
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_B62C61BBEB') }}
    qualify row_number() over (partition by "DATASPATIAL_WKB" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"BL_SYEARTO"') }} as bl_syearto,
        {{ null_junk('"RS"') }} as rs,
        {{ null_junk('"BLNAME"') }} as blname,
        {{ null_junk('"YEARTOT"') }} as yeartot,
        {{ null_junk('"NOTE"') }} as note,
        {{ stg_float('"BIKEINFDIS"') }} as bikeinfdis,
        {{ null_junk('"CROSSST"') }} as crossst,
        {{ stg_float('"SLMDIST"') }} as slmdist,
        {{ null_junk('"YEAR_ADDED"') }} as year_added,
        {{ stg_int('"ID"') }} as id,
        {{ stg_float('"BLDIST"') }} as bldist,
        {{ null_junk('"RBL"') }} as rbl,
        {{ null_junk('"DATASPATIAL_WKB"') }} as dataspatial_wkb,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/shape-files-for-bikepgh-s-pittsburgh-bike-map' as _source_url

    from source

)

select * from renamed
