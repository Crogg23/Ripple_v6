version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_WASHINGTON_STATE_11CD1995B7
        description: >
          Raw landing table for Complaint By Practice. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_washington_state_11cd1995b7__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: id
        data_tests:
          - unique
          - not_null
