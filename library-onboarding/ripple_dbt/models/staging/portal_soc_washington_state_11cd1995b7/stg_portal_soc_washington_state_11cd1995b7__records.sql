{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 7 of 24 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   openeddate: timestamp
--   openedyear: integer
--   estimatedsavings: float
--   practiceid: integer
--   actualsavings: float
--   complaintid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   businessid: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_WASHINGTON_STATE_11CD1995B7') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_ts('"OPENEDDATE"') }} as openeddate,
        {{ stg_int('"OPENEDYEAR"') }} as openedyear,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"PRACTICECODE"') }} as practicecode,
        {{ null_junk('"PRACTICENAME"') }} as practicename,
        {{ null_junk('"BUSINESSCATEGORY"') }} as businesscategory,
        {{ null_junk('"NAICS"') }} as naics,
        {{ null_junk('"BUSINESSNAME"') }} as businessname,
        {{ null_junk('"BUSINESSSTREETLINE1"') }} as businessstreetline1,
        {{ null_junk('"BUSINESSSTREETLINE2"') }} as businessstreetline2,
        {{ null_junk('"BUSINESSCITY"') }} as businesscity,
        {{ null_junk('"BUSINESSSTATE"') }} as businessstate,
        {{ null_junk('"BUSINESSZIP"') }} as businesszip,
        {{ stg_float('"ESTIMATEDSAVINGS"') }} as estimatedsavings,
        {{ stg_int('"PRACTICEID"') }} as practiceid,
        {{ stg_float('"ACTUALSAVINGS"') }} as actualsavings,
        {{ stg_id_text('"COMPLAINTID"') }} as complaintid,
        {{ stg_int('"BUSINESSID"') }} as businessid,
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"NAICSNAME"') }} as naicsname,
        {{ null_junk('"LOCATION"') }} as location,
        {{ null_junk('"COMPUTED_REGION_8DDD_YN5V"') }} as computed_region_8ddd_yn5v,
        {{ null_junk('"COMPUTED_REGION_X4YS_RTND"') }} as computed_region_x4ys_rtnd,
        {{ null_junk('"COMPUTED_REGION_FNY7_VC3J"') }} as computed_region_fny7_vc3j,
        INGESTED_AT as _loaded_at,
        'https://data.wa.gov/d/fvrz-yz45' as _source_url

    from source

)

select * from renamed
