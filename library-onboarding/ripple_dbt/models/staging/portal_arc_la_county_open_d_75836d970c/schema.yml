version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_LA_COUNTY_OPEN_D_75836D970C
        description: >
          Raw landing table for SCAQMD Facilities Unincorporated April 2024. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_la_county_open_d_75836d970c__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FAC_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: fac_id
        data_tests:
          - unique
          - not_null
