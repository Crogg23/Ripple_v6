version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_VIRGINIA_OPEN_DA_651C0C423A
        description: >
          Raw landing table for Biosolids Site Address Info Table. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_virginia_open_da_651c0c423a__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+STARTING_DATE+EXPIRATION_DATE+INSERTED_DATE+BUFFER_ID unique). SPINE_ENTITY: place."
    columns:
      - name: zip_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: starting_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: expiration_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: inserted_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: buffer_id
        data_tests:
          - not_null:
              config:
                severity: warn
