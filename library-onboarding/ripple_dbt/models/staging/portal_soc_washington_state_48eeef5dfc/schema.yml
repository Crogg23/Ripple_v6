version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_WASHINGTON_STATE_48EEEF5DFC
        description: >
          Raw landing table for Attorney General Consumer Complaints. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_washington_state_48eeef5dfc__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (BUSINESS_ID+ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: business_id
        data_tests:
          - not_null
      - name: id
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - business_id
              - id