{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (BUSINESS_ID+ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: BUSINESS_ID, ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_WASHINGTON_STATE_48EEEF5DFC') }}

),

renamed as (

    select
        "OPENEDDATE" as openeddate,
        "OPENEDYEAR" as openedyear,
        "STATUS" as status,
        "ESTIMATEDSAVINGS" as estimatedsavings,
        "ACTUALSAVINGS" as actualsavings,
        "BUSINESSCATEGORY" as businesscategory,
        "NAICS" as naics,
        "BUSINESS" as business,
        "BUSINESSSTREETLINE1" as businessstreetline1,
        "BUSINESSSTREETLINE2" as businessstreetline2,
        "BUSINESSCITY" as businesscity,
        "BUSINESSSTATE" as businessstate,
        "BUSINESSZIP" as businesszip,
        "BUSINESS_ID" as business_id,
        "ID" as id,
        INGESTED_AT as _loaded_at,
        'https://data.wa.gov/d/gpri-47xz' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by business_id, id order by _loaded_at desc) = 1
