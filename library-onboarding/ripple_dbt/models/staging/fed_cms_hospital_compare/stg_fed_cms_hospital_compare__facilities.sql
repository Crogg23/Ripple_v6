{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per facility (FACILITY_ID unique)
-- SPINE_ENTITY: facility  (natural_key: FACILITY_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 25 of 38 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   hospital_overall_rating: integer
--   mort_group_measure_count: integer
--   count_of_facility_mort_measures: integer
--   count_of_mort_measures_better: integer
--   count_of_mort_measures_no_different: integer
--   count_of_mort_measures_worse: integer
--   mort_group_footnote: integer
--   safety_group_measure_count: integer
--   count_of_facility_safety_measures: integer
--   count_of_safety_measures_better: integer
--   count_of_safety_measures_no_different: integer
--   count_of_safety_measures_worse: integer
--   safety_group_footnote: integer
--   readm_group_measure_count: integer
--   count_of_facility_readm_measures: integer
--   count_of_readm_measures_better: integer
--   count_of_readm_measures_no_different: integer
--   count_of_readm_measures_worse: integer
--   readm_group_footnote: integer
--   pt_exp_group_measure_count: integer
--   count_of_facility_pt_exp_measures: integer
--   pt_exp_group_footnote: integer
--   te_group_measure_count: integer
--   count_of_facility_te_measures: integer
--   te_group_footnote: integer
-- spine_entity 'facility' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_CMS_HOSPITAL_COMPARE') }}
    qualify row_number() over (partition by "FACILITY_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"FACILITY_ID"') }} as facility_id,
        {{ null_junk('"FACILITY_NAME"') }} as facility_name,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"CITY_TOWN"') }} as city_town,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"COUNTY_PARISH"') }} as county_parish,
        {{ null_junk('"TELEPHONE_NUMBER"') }} as telephone_number,
        {{ null_junk('"HOSPITAL_TYPE"') }} as hospital_type,
        {{ null_junk('"HOSPITAL_OWNERSHIP"') }} as hospital_ownership,
        {{ null_junk('"EMERGENCY_SERVICES"') }} as emergency_services,
        {{ null_junk('"MEETS_CRITERIA_FOR_BIRTHING_FRIENDLY_DESIGNATION"') }} as meets_criteria_for_birthing_friendly_designation,
        {{ stg_int('"HOSPITAL_OVERALL_RATING"') }} as hospital_overall_rating,
        {{ null_junk('"HOSPITAL_OVERALL_RATING_FOOTNOTE"') }} as hospital_overall_rating_footnote,
        {{ stg_int('"MORT_GROUP_MEASURE_COUNT"') }} as mort_group_measure_count,
        {{ stg_int('"COUNT_OF_FACILITY_MORT_MEASURES"') }} as count_of_facility_mort_measures,
        {{ stg_int('"COUNT_OF_MORT_MEASURES_BETTER"') }} as count_of_mort_measures_better,
        {{ stg_int('"COUNT_OF_MORT_MEASURES_NO_DIFFERENT"') }} as count_of_mort_measures_no_different,
        {{ stg_int('"COUNT_OF_MORT_MEASURES_WORSE"') }} as count_of_mort_measures_worse,
        {{ stg_int('"MORT_GROUP_FOOTNOTE"') }} as mort_group_footnote,
        {{ stg_int('"SAFETY_GROUP_MEASURE_COUNT"') }} as safety_group_measure_count,
        {{ stg_int('"COUNT_OF_FACILITY_SAFETY_MEASURES"') }} as count_of_facility_safety_measures,
        {{ stg_int('"COUNT_OF_SAFETY_MEASURES_BETTER"') }} as count_of_safety_measures_better,
        {{ stg_int('"COUNT_OF_SAFETY_MEASURES_NO_DIFFERENT"') }} as count_of_safety_measures_no_different,
        {{ stg_int('"COUNT_OF_SAFETY_MEASURES_WORSE"') }} as count_of_safety_measures_worse,
        {{ stg_int('"SAFETY_GROUP_FOOTNOTE"') }} as safety_group_footnote,
        {{ stg_int('"READM_GROUP_MEASURE_COUNT"') }} as readm_group_measure_count,
        {{ stg_int('"COUNT_OF_FACILITY_READM_MEASURES"') }} as count_of_facility_readm_measures,
        {{ stg_int('"COUNT_OF_READM_MEASURES_BETTER"') }} as count_of_readm_measures_better,
        {{ stg_int('"COUNT_OF_READM_MEASURES_NO_DIFFERENT"') }} as count_of_readm_measures_no_different,
        {{ stg_int('"COUNT_OF_READM_MEASURES_WORSE"') }} as count_of_readm_measures_worse,
        {{ stg_int('"READM_GROUP_FOOTNOTE"') }} as readm_group_footnote,
        {{ stg_int('"PT_EXP_GROUP_MEASURE_COUNT"') }} as pt_exp_group_measure_count,
        {{ stg_int('"COUNT_OF_FACILITY_PT_EXP_MEASURES"') }} as count_of_facility_pt_exp_measures,
        {{ stg_int('"PT_EXP_GROUP_FOOTNOTE"') }} as pt_exp_group_footnote,
        {{ stg_int('"TE_GROUP_MEASURE_COUNT"') }} as te_group_measure_count,
        {{ stg_int('"COUNT_OF_FACILITY_TE_MEASURES"') }} as count_of_facility_te_measures,
        {{ stg_int('"TE_GROUP_FOOTNOTE"') }} as te_group_footnote,
        _INGESTED_AT as _loaded_at,
        'https://data.cms.gov/provider-data/docs' as _source_url

    from source

)

select * from renamed
