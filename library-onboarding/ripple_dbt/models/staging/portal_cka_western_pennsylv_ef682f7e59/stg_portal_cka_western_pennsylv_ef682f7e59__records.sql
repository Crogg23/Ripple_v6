{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PERMIT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PERMIT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_EF682F7E59') }}

),

renamed as (

    select
        "PERMIT_ID" as permit_id,
        "PERMIT_TYPE" as permit_type,
        "OWNER_NAME" as owner_name,
        "CONTRACTOR_NAME" as contractor_name,
        "WORK_DESCRIPTION" as work_description,
        "WORK_TYPE" as work_type,
        "COMMERCIAL_OR_RESIDENTIAL" as commercial_or_residential,
        "TOTAL_PROJECT_VALUE" as total_project_value,
        "ISSUE_DATE" as issue_date,
        "PARCEL_NUM" as parcel_num,
        "ADDRESS" as address,
        "LATITUDE" as latitude,
        "LONGITUDE" as longitude,
        "COUNCIL_DISTRICT" as council_district,
        "NEIGHBORHOOD" as neighborhood,
        "WARD" as ward,
        "ZIP_CODE" as zip_code,
        "STATUS" as status,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/pli-permits' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by permit_id order by _loaded_at desc) = 1
