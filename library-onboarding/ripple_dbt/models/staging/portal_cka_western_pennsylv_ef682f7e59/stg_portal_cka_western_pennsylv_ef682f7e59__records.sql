{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PERMIT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PERMIT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 4 of 18 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   total_project_value: integer
--   issue_date: date
--   council_district: integer
--   ward: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_EF682F7E59') }}
    qualify row_number() over (partition by "PERMIT_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"PERMIT_ID"') }} as permit_id,
        {{ null_junk('"PERMIT_TYPE"') }} as permit_type,
        {{ null_junk('"OWNER_NAME"') }} as owner_name,
        {{ null_junk('"CONTRACTOR_NAME"') }} as contractor_name,
        {{ null_junk('"WORK_DESCRIPTION"') }} as work_description,
        {{ null_junk('"WORK_TYPE"') }} as work_type,
        {{ null_junk('"COMMERCIAL_OR_RESIDENTIAL"') }} as commercial_or_residential,
        {{ stg_int('"TOTAL_PROJECT_VALUE"') }} as total_project_value,
        {{ stg_date('"ISSUE_DATE"') }} as issue_date,
        {{ null_junk('"PARCEL_NUM"') }} as parcel_num,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ stg_int('"COUNCIL_DISTRICT"') }} as council_district,
        {{ null_junk('"NEIGHBORHOOD"') }} as neighborhood,
        {{ stg_int('"WARD"') }} as ward,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"STATUS"') }} as status,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/pli-permits' as _source_url

    from source

)

select * from renamed
