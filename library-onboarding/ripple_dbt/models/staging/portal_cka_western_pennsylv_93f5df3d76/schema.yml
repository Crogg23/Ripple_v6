version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_93F5DF3D76
        description: >
          Raw landing table for City-Owned Property (Real Estate Tax Database). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_93f5df3d76__places
    description: "GRAIN: one row per place (LATLON is unique). SPINE_ENTITY: place."
    columns:
      - name: longitude
        data_tests:
          - unique
          - not_null
