{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PGM_SYS_ID+PROGRAM_CODE+PROGRAM_DESC+AIR_PROGRAM_SUBPART_CODE unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PGM_SYS_ID, PROGRAM_CODE, PROGRAM_DESC, AIR_PROGRAM_SUBPART_CODE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_ICIS_AIR_ICIS_AIR_PROGRAM_SUBPARTS') }}

),

renamed as (

    select
        "PGM_SYS_ID" as pgm_sys_id,
        "PROGRAM_CODE" as program_code,
        "PROGRAM_DESC" as program_desc,
        "AIR_PROGRAM_SUBPART_CODE" as air_program_subpart_code,
        "AIR_PROGRAM_SUBPART_DESC" as air_program_subpart_desc,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by pgm_sys_id, program_code, program_desc, air_program_subpart_code order by _loaded_at desc) = 1
