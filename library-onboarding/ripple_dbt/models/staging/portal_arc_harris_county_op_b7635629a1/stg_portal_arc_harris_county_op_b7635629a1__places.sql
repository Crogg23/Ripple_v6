{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 14 of 48 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   fid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   blkgrp: integer
--   block: integer
--   county: integer
--   state: integer
--   dist_nbr: integer
--   dist_nbr_1: integer
--   dist_nbr_2: integer
--   region_num: integer
--   district_n: integer
--   objecti_11: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   join_cou_7: integer
--   target_f_7: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   objecti_12: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_HARRIS_COUNTY_OP_B7635629A1') }}
    qualify row_number() over (partition by "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"FID"') }} as fid,
        {{ stg_int('"BLKGRP"') }} as blkgrp,
        {{ stg_int('"BLOCK"') }} as block,
        {{ stg_int('"COUNTY"') }} as county,
        {{ null_junk('"NAME"') }} as name,
        {{ stg_int('"STATE"') }} as state,
        {{ null_junk('"REP_NM"') }} as rep_nm,
        {{ stg_int('"DIST_NBR"') }} as dist_nbr,
        {{ null_junk('"REP_NM_1"') }} as rep_nm_1,
        {{ stg_int('"DIST_NBR_1"') }} as dist_nbr_1,
        {{ stg_int('"DIST_NBR_2"') }} as dist_nbr_2,
        {{ null_junk('"REP_NM_12"') }} as rep_nm_12,
        {{ null_junk('"SNBNAME"') }} as snbname,
        {{ null_junk('"DISTRICT"') }} as district,
        {{ null_junk('"MEMBER"') }} as member,
        {{ null_junk('"TURF_NAME"') }} as turf_name,
        {{ stg_int('"REGION_NUM"') }} as region_num,
        {{ null_junk('"NAME20"') }} as name20,
        {{ null_junk('"SDLEA"') }} as sdlea,
        {{ null_junk('"NAME_1"') }} as name_1,
        {{ null_junk('"NAME2"') }} as name2,
        {{ stg_int('"DISTRICT_N"') }} as district_n,
        {{ null_junk('"DISTRICT_1"') }} as district_1,
        {{ null_junk('"DISTRICT_C"') }} as district_c,
        {{ null_junk('"NCES_DISTR"') }} as nces_distr,
        {{ null_junk('"SHAPE__A_5"') }} as shape__a_5,
        {{ null_junk('"SHAPE__L_5"') }} as shape__l_5,
        {{ null_junk('"SHAPE_L_11"') }} as shape_l_11,
        {{ null_junk('"SHAPE__A_6"') }} as shape__a_6,
        {{ null_junk('"SHAPE__L_6"') }} as shape__l_6,
        {{ null_junk('"SHAPE_L_12"') }} as shape_l_12,
        {{ null_junk('"SHAPE_AR_7"') }} as shape_ar_7,
        {{ stg_id_text('"OBJECTI_11"') }} as objecti_11,
        {{ stg_int('"JOIN_COU_7"') }} as join_cou_7,
        {{ stg_id_text('"TARGET_F_7"') }} as target_f_7,
        {{ stg_int('"OBJECTI_12"') }} as objecti_12,
        {{ null_junk('"NAME_12"') }} as name_12,
        {{ null_junk('"CREATED_US"') }} as created_us,
        {{ null_junk('"CREATED_DA"') }} as created_da,
        {{ null_junk('"LAST_EDI_1"') }} as last_edi_1,
        {{ null_junk('"LAST_EDI_2"') }} as last_edi_2,
        {{ null_junk('"SHAPE__A_7"') }} as shape__a_7,
        {{ null_junk('"SHAPE__L_7"') }} as shape__l_7,
        {{ null_junk('"SHAPE_L_13"') }} as shape_l_13,
        {{ null_junk('"SHAPE_AR_8"') }} as shape_ar_8,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://geo-harriscounty.opendata.arcgis.com/datasets/3ba9ceb0d125487f940abf8b84189cb8_0' as _source_url

    from source

)

select * from renamed
