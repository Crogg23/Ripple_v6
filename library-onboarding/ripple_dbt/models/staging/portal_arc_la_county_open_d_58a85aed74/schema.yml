version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_LA_COUNTY_OPEN_D_58A85AED74
        description: >
          Raw landing table for Review. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_la_county_open_d_58a85aed74__providers
    description: "GRAIN: one row per provider (NPI is unique). SPINE_ENTITY: provider."
    columns:
      - name: npi
        data_tests:
          - unique
          - not_null
