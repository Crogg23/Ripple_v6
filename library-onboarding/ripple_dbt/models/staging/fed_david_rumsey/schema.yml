version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_DAVID_RUMSEY
        description: >
          Raw landing table for David Rumsey Historical Map Collection (IIIF + LUNA API). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_fed_david_rumsey__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (MANIFEST_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: manifest_id
        data_tests:
          - unique
          - not_null
