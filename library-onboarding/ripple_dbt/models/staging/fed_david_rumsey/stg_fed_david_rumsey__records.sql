{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (MANIFEST_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: MANIFEST_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 13 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_DAVID_RUMSEY') }}
    qualify row_number() over (partition by "MANIFEST_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"MANIFEST_ID"') }} as manifest_id,
        {{ null_junk('"LABEL"') }} as label,
        {{ null_junk('"DESCRIPTION"') }} as description,
        {{ null_junk('"DATE_PUBLISHED"') }} as date_published,
        {{ null_junk('"AUTHOR"') }} as author,
        {{ null_junk('"SUBJECT"') }} as subject,
        {{ null_junk('"THUMBNAIL_URL"') }} as thumbnail_url,
        {{ null_junk('"IIIF_IMAGE_URL"') }} as iiif_image_url,
        {{ null_junk('"LUNA_RECORD_ID"') }} as luna_record_id,
        {{ null_junk('"RIGHTS"') }} as rights,
        {{ null_junk('"GEO_COVERAGE"') }} as geo_coverage,
        {{ null_junk('"SCALE"') }} as scale,
        {{ null_junk('"PHYSICAL_DIMENSIONS"') }} as physical_dimensions,
        _INGESTED_AT as _loaded_at,
        'https://iiif.io/guides/guides/davidrumsey.com/' as _source_url

    from source

)

select * from renamed
