{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (MANIFEST_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: MANIFEST_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_DAVID_RUMSEY') }}

),

renamed as (

    select
        "MANIFEST_ID" as manifest_id,
        "LABEL" as label,
        "DESCRIPTION" as description,
        "DATE_PUBLISHED" as date_published,
        "AUTHOR" as author,
        "SUBJECT" as subject,
        "THUMBNAIL_URL" as thumbnail_url,
        "IIIF_IMAGE_URL" as iiif_image_url,
        "LUNA_RECORD_ID" as luna_record_id,
        "RIGHTS" as rights,
        "GEO_COVERAGE" as geo_coverage,
        "SCALE" as scale,
        "PHYSICAL_DIMENSIONS" as physical_dimensions,
        _INGESTED_AT as _loaded_at,
        'https://iiif.io/guides/guides/davidrumsey.com/' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by manifest_id order by _loaded_at desc) = 1
