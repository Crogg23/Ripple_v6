{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per person (FEC_CAND_ID+CYCLE unique)
-- SPINE_ENTITY: person  (natural_key: CAND_ID, CYCLE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 19 of 31 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   pty_cd: integer
--   ttl_receipts: float
--   trans_from_auth: float
--   ttl_disb: float
--   trans_to_auth: float
--   coh_bop: float
--   coh_cop: float
--   cand_contrib: float
--   cand_loans: float
--   other_loans: float
--   cand_loan_repay: float
--   other_loan_repay: float
--   debts_owed_by: float
--   ttl_indiv_contrib: float
--   other_pol_cmte_contrib: float
--   pol_pty_contrib: float
--   cvg_end_dt: date
--   indiv_refunds: float
--   cmte_refunds: float
-- spine_entity 'person' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_FEC_BULK_SUMMARY') }}
    qualify row_number() over (partition by "CAND_ID", "CYCLE" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CAND_ID"') }} as cand_id,
        {{ null_junk('"CAND_NAME"') }} as cand_name,
        {{ null_junk('"CAND_ICI"') }} as cand_ici,
        {{ stg_int('"PTY_CD"') }} as pty_cd,
        {{ null_junk('"CAND_PTY_AFFILIATION"') }} as cand_pty_affiliation,
        {{ stg_float('"TTL_RECEIPTS"') }} as ttl_receipts,
        {{ stg_float('"TRANS_FROM_AUTH"') }} as trans_from_auth,
        {{ stg_float('"TTL_DISB"') }} as ttl_disb,
        {{ stg_float('"TRANS_TO_AUTH"') }} as trans_to_auth,
        {{ stg_float('"COH_BOP"') }} as coh_bop,
        {{ stg_float('"COH_COP"') }} as coh_cop,
        {{ stg_float('"CAND_CONTRIB"') }} as cand_contrib,
        {{ stg_float('"CAND_LOANS"') }} as cand_loans,
        {{ stg_float('"OTHER_LOANS"') }} as other_loans,
        {{ stg_float('"CAND_LOAN_REPAY"') }} as cand_loan_repay,
        {{ stg_float('"OTHER_LOAN_REPAY"') }} as other_loan_repay,
        {{ stg_float('"DEBTS_OWED_BY"') }} as debts_owed_by,
        {{ stg_float('"TTL_INDIV_CONTRIB"') }} as ttl_indiv_contrib,
        {{ null_junk('"CAND_OFFICE_ST"') }} as cand_office_st,
        {{ null_junk('"CAND_OFFICE_DISTRICT"') }} as cand_office_district,
        {{ null_junk('"SPEC_ELECTION"') }} as spec_election,
        {{ null_junk('"PRIM_ELECTION"') }} as prim_election,
        {{ null_junk('"RUN_ELECTION"') }} as run_election,
        {{ null_junk('"GEN_ELECTION"') }} as gen_election,
        {{ null_junk('"GEN_ELECTION_PRECENT"') }} as gen_election_precent,
        {{ stg_float('"OTHER_POL_CMTE_CONTRIB"') }} as other_pol_cmte_contrib,
        {{ stg_float('"POL_PTY_CONTRIB"') }} as pol_pty_contrib,
        {{ stg_date('"CVG_END_DT"') }} as cvg_end_dt,
        {{ stg_float('"INDIV_REFUNDS"') }} as indiv_refunds,
        {{ stg_float('"CMTE_REFUNDS"') }} as cmte_refunds,
        {{ null_junk('"CYCLE"') }} as cycle,
        _INGESTED_AT as _loaded_at,
        'https://www.fec.gov/files/bulk-downloads/2024/weball24.zip' as _source_url

    from source

)

select * from renamed
