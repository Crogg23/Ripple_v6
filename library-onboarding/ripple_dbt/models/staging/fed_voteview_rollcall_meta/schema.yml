version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_VOTEVIEW_ROLLCALL_META
        description: >
          Raw landing table for Voteview -- Roll-Call Metadata. Grain not yet determined.

models:
  - name: stg_fed_voteview_rollcall_meta__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
