{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TRANSPORTATION_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TRANSPORTATION_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_HOUSTON_OPEN_DAT_702315B033') }}

),

renamed as (

    select
        "TRANSPORTATION_ID" as transportation_id,
        "APPLICATION_DESCRIPTION" as application_description,
        "DBA_ALIAS" as dba_alias,
        "COMPANY" as company,
        "STATUS" as status,
        "HLL_CSS_NUMBER" as hll_css_number,
        "FULL_STREET_NAME" as full_street_name,
        "CITY_NAME" as city_name,
        "ZIPCODE" as zipcode,
        "ACTIVE_VEHICLE_COUNT" as active_vehicle_count,
        "ALL_VEHICLE_COUNT" as all_vehicle_count,
        "SHORT_DESC" as short_desc,
        INGESTED_AT as _loaded_at,
        'https://data.houstontx.gov/dataset/all-active-transportation-companies-regulated-by-ara-transportation-section-a' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by transportation_id order by _loaded_at desc) = 1
