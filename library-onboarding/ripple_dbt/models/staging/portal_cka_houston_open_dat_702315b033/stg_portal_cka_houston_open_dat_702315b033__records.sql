{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TRANSPORTATION_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TRANSPORTATION_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 2 of 12 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   active_vehicle_count: integer
--   all_vehicle_count: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_HOUSTON_OPEN_DAT_702315B033') }}
    qualify row_number() over (partition by "TRANSPORTATION_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"TRANSPORTATION_ID"') }} as transportation_id,
        {{ null_junk('"APPLICATION_DESCRIPTION"') }} as application_description,
        {{ null_junk('"DBA_ALIAS"') }} as dba_alias,
        {{ null_junk('"COMPANY"') }} as company,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"HLL_CSS_NUMBER"') }} as hll_css_number,
        {{ null_junk('"FULL_STREET_NAME"') }} as full_street_name,
        {{ null_junk('"CITY_NAME"') }} as city_name,
        {{ null_junk('"ZIPCODE"') }} as zipcode,
        {{ stg_int('"ACTIVE_VEHICLE_COUNT"') }} as active_vehicle_count,
        {{ stg_int('"ALL_VEHICLE_COUNT"') }} as all_vehicle_count,
        {{ null_junk('"SHORT_DESC"') }} as short_desc,
        INGESTED_AT as _loaded_at,
        'https://data.houstontx.gov/dataset/all-active-transportation-companies-regulated-by-ara-transportation-section-a' as _source_url

    from source

)

select * from renamed
