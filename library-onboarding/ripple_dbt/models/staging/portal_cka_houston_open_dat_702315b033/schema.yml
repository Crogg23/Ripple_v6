version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_HOUSTON_OPEN_DAT_702315B033
        description: >
          Raw landing table for All Active Transportation Companies Regulated by ARA Transportation Section a.... All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_houston_open_dat_702315b033__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TRANSPORTATION_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: transportation_id
        data_tests:
          - unique
          - not_null
