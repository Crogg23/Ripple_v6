{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 6 of 21 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   taxyr: integer
--   totalacreagecnt: float
--   staffhoursspentenforcementcnt: integer
--   expensesincurredenforcementamt: integer
--   totaleasementscnt: integer
--   stateseasementsheldcnt: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_COLORADO_INFORMA_F78543C045') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ stg_int('"TAXYR"') }} as taxyr,
        {{ null_junk('"BUSINESSNAME1"') }} as businessname1,
        {{ null_junk('"EIN"') }} as ein,
        {{ null_junk('"CONSERVATIONEASEMENTSIND"') }} as conservationeasementsind,
        {{ null_junk('"PRESERVATIONPUBLICUSEIND"') }} as preservationpublicuseind,
        {{ null_junk('"PROTECTIONNATURALHABITATIND"') }} as protectionnaturalhabitatind,
        {{ null_junk('"PRESERVATIONOPENSPACEIND"') }} as preservationopenspaceind,
        {{ null_junk('"HISTORICLANDAREAIND"') }} as historiclandareaind,
        {{ null_junk('"HISTORICSTRUCTUREIND"') }} as historicstructureind,
        {{ stg_float('"TOTALACREAGECNT"') }} as totalacreagecnt,
        {{ null_junk('"WRITTENPOLICYMONITORINGIND"') }} as writtenpolicymonitoringind,
        {{ stg_int('"STAFFHOURSSPENTENFORCEMENTCNT"') }} as staffhoursspentenforcementcnt,
        {{ stg_int('"EXPENSESINCURREDENFORCEMENTAMT"') }} as expensesincurredenforcementamt,
        {{ null_junk('"SECTION170HRQRSTSFDIND"') }} as section170hrqrstsfdind,
        {{ null_junk('"BUSINESSNAME2"') }} as businessname2,
        {{ stg_int('"TOTALEASEMENTSCNT"') }} as totaleasementscnt,
        {{ null_junk('"HISTORICSTRUCTUREEASEMENTSCNT"') }} as historicstructureeasementscnt,
        {{ null_junk('"HISTORICSTRCTREASEMENTSAFTRCNT"') }} as historicstrctreasementsaftrcnt,
        {{ null_junk('"MODIFIEDEASEMENTSCNT"') }} as modifiedeasementscnt,
        {{ stg_int('"STATESEASEMENTSHELDCNT"') }} as stateseasementsheldcnt,
        INGESTED_AT as _loaded_at,
        'https://data.colorado.gov/d/u4xw-xiec' as _source_url

    from source

)

select * from renamed
