{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PGM_SYS_ID+BEGIN_DATE+UPDATED_DATE+PROGRAM_CODE unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PGM_SYS_ID, BEGIN_DATE, UPDATED_DATE, PROGRAM_CODE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 7 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_ICIS_AIR_ICIS_AIR_PROGRAMS') }}
    qualify row_number() over (partition by "PGM_SYS_ID", "BEGIN_DATE", "UPDATED_DATE", "PROGRAM_CODE" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"PGM_SYS_ID"') }} as pgm_sys_id,
        {{ null_junk('"PROGRAM_CODE"') }} as program_code,
        {{ null_junk('"PROGRAM_DESC"') }} as program_desc,
        {{ null_junk('"AIR_OPERATING_STATUS_CODE"') }} as air_operating_status_code,
        {{ null_junk('"AIR_OPERATING_STATUS_DESC"') }} as air_operating_status_desc,
        {{ null_junk('"BEGIN_DATE"') }} as begin_date,
        {{ null_junk('"UPDATED_DATE"') }} as updated_date,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
