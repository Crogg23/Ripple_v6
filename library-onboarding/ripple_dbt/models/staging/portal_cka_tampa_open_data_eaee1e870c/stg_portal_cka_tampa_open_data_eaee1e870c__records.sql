{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_TAMPA_OPEN_DATA_EAEE1E870C') }}

),

renamed as (

    select
        "ID" as id,
        "ORGANZIATION" as organziation,
        "CHARTNAME" as chartname,
        "DESCRIPTION" as description,
        "CATEGORY" as category,
        "SUMMARY" as summary,
        "PERIOD" as period,
        "DATE" as date,
        "DATA_TYPE" as data_type,
        "VALUE" as value,
        "AUTOMATION" as automation,
        "CUSTOM_TEXT_1" as custom_text_1,
        "CUSTOM_TEXT_2" as custom_text_2,
        "CUSTOM_TEXT_3" as custom_text_3,
        "CUSTOM_NUMBER_1" as custom_number_1,
        "CUSTOM_NUMBER_2" as custom_number_2,
        "CUSTOM_NUMBER_3" as custom_number_3,
        "CUSTOM_DATE_1" as custom_date_1,
        "CUSTOM_DATE_2" as custom_date_2,
        "CUSTOM_DATE_3" as custom_date_3,
        "LAST_UPDATE" as last_update,
        "LAST_UPDATE_COT" as last_update_cot,
        "LAST_UPDATE_OGI" as last_update_ogi,
        INGESTED_AT as _loaded_at,
        'https://opendata.tampa.gov/dataset/neighborhood-engagement-dataset' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by id order by _loaded_at desc) = 1
