version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_TEXAS_OPEN_DATA_5410A1009F
        description: >
          Raw landing table for Active Sales Tax Permit Holders. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_texas_open_data_5410a1009f__places
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+OUTLET_PERMIT_ISSUE_DATE unique). SPINE_ENTITY: place."
    columns:
      - name: outlet_zip_code
        data_tests:
          - not_null
      - name: outlet_permit_issue_date
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - outlet_zip_code
            - outlet_permit_issue_date
