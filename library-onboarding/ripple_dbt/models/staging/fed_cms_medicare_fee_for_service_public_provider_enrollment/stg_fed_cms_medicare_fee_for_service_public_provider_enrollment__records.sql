{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ENRLMT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ENRLMT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_CMS_MEDICARE_FEE_FOR_SERVICE_PUBLIC_PROVIDER_ENROLLMENT') }}

),

renamed as (

    select
        "NPI" as npi,
        "MULTIPLE_NPI_FLAG" as multiple_npi_flag,
        "PECOS_ASCT_CNTL_ID" as pecos_asct_cntl_id,
        "ENRLMT_ID" as enrlmt_id,
        "PROVIDER_TYPE_CD" as provider_type_cd,
        "PROVIDER_TYPE_DESC" as provider_type_desc,
        "STATE_CD" as state_cd,
        "FIRST_NAME" as first_name,
        "MDL_NAME" as mdl_name,
        "LAST_NAME" as last_name,
        "ORG_NAME" as org_name,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by enrlmt_id order by _loaded_at desc) = 1
