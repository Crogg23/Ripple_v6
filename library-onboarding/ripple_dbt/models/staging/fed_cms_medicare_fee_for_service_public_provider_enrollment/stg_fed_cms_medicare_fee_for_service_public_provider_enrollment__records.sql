{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ENRLMT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ENRLMT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 11 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_CMS_MEDICARE_FEE_FOR_SERVICE_PUBLIC_PROVIDER_ENROLLMENT') }}
    qualify row_number() over (partition by "ENRLMT_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"NPI"') }} as npi,
        {{ null_junk('"MULTIPLE_NPI_FLAG"') }} as multiple_npi_flag,
        {{ null_junk('"PECOS_ASCT_CNTL_ID"') }} as pecos_asct_cntl_id,
        {{ null_junk('"ENRLMT_ID"') }} as enrlmt_id,
        {{ null_junk('"PROVIDER_TYPE_CD"') }} as provider_type_cd,
        {{ null_junk('"PROVIDER_TYPE_DESC"') }} as provider_type_desc,
        {{ null_junk('"STATE_CD"') }} as state_cd,
        {{ null_junk('"FIRST_NAME"') }} as first_name,
        {{ null_junk('"MDL_NAME"') }} as mdl_name,
        {{ null_junk('"LAST_NAME"') }} as last_name,
        {{ null_junk('"ORG_NAME"') }} as org_name,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
