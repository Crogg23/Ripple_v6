version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_IRELAND_NATIONAL_4049D41C91
        description: >
          Raw landing table for Greystones-Delgany & Kilcoole Local Planning Framework Boundary (Adopted 2026). All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_ireland_national_4049d41c91__places
    description: "GRAIN: one row per place (GEOM is unique). SPINE_ENTITY: place."
    columns:
      - name: shape__length
        data_tests:
          - unique
          - not_null
