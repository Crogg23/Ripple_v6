{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE__LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 12 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_IRELAND_NATIONAL_4049D41C91') }}
    qualify row_number() over (partition by "SHAPE__LENGTH" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"OBJECTID"') }} as objectid,
        {{ null_junk('"SHAPE_LENG"') }} as shape_leng,
        {{ null_junk('"BUFF_DIST"') }} as buff_dist,
        {{ null_junk('"ORIG_FID"') }} as orig_fid,
        {{ null_junk('"PMA_NO"') }} as pma_no,
        {{ null_junk('"SETTLEMENT_LEVEL"') }} as settlement_level,
        {{ null_junk('"NAME_OF_SETTLEMENT"') }} as name_of_settlement,
        {{ null_junk('"AREA_HA"') }} as area_ha,
        {{ null_junk('"PLAN_DESCRIPTION"') }} as plan_description,
        {{ null_junk('"LEVEL_DESCRIPTION"') }} as level_description,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        INGESTED_AT as _loaded_at,
        'https://data.gov.ie/dataset/greystones-delgany-kilcoole-local-planning-framework-boundary-adopted-2026' as _source_url

    from source

)

select * from renamed
