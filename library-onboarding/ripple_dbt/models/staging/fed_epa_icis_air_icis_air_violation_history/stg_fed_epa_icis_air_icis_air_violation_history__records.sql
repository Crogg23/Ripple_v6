{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PGM_SYS_ID+ACTIVITY_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PGM_SYS_ID, ACTIVITY_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 16 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_ICIS_AIR_ICIS_AIR_VIOLATION_HISTORY') }}
    qualify row_number() over (partition by "PGM_SYS_ID", "ACTIVITY_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"PGM_SYS_ID"') }} as pgm_sys_id,
        {{ null_junk('"ACTIVITY_ID"') }} as activity_id,
        {{ null_junk('"AGENCY_TYPE_DESC"') }} as agency_type_desc,
        {{ null_junk('"STATE_CODE"') }} as state_code,
        {{ null_junk('"AIR_LCON_CODE"') }} as air_lcon_code,
        {{ null_junk('"COMP_DETERMINATION_UID"') }} as comp_determination_uid,
        {{ null_junk('"ENF_RESPONSE_POLICY_CODE"') }} as enf_response_policy_code,
        {{ null_junk('"PROGRAM_CODES"') }} as program_codes,
        {{ null_junk('"PROGRAM_DESCS"') }} as program_descs,
        {{ null_junk('"POLLUTANT_CODES"') }} as pollutant_codes,
        {{ null_junk('"POLLUTANT_DESCS"') }} as pollutant_descs,
        {{ null_junk('"EARLIEST_FRV_DETERM_DATE"') }} as earliest_frv_determ_date,
        {{ null_junk('"HPV_DAYZERO_DATE"') }} as hpv_dayzero_date,
        {{ null_junk('"HPV_RESOLVED_DATE"') }} as hpv_resolved_date,
        {{ null_junk('"DSCV_PATHWAY_DATE"') }} as dscv_pathway_date,
        {{ null_junk('"NFTC_PATHWAY_DATE"') }} as nftc_pathway_date,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
