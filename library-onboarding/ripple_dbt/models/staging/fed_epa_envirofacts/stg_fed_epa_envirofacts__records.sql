{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_ENVIROFACTS') }}

),

renamed as (

    select
        TABLE_NAME as table_name,
        PROGRAM_SCHEMA as program_schema,
        STATE_CODE as state_code,
        CITY_NAME as city_name,
        POSTAL_CODE as postal_code,
        COUNTY_NAME as county_name,
        FRS_ID as frs_id,
        HANDLER_ID as handler_id,
        SITE_ID as site_id,
        FACILITY_NAME as facility_name,
        LATITUDE as latitude,
        LONGITUDE as longitude,
        CREATED_DATE as created_date,
        MEDIA_NAME as media_name,
        _INGESTED_AT as _loaded_at,
        'https://www.epa.gov/enviro/envirofacts-data-service-api' as _source_url

    from source

)

select * from renamed
