{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per provider (NPI is unique)
-- SPINE_ENTITY: provider  (natural_key: NPI)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 32 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.

with source as (

    select * from {{ source('ripple_raw', 'FED_CMS_HPT_MRF') }}
    qualify row_number() over (partition by "NPI" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"HOSPITAL_NAME"') }} as hospital_name,
        {{ null_junk('"HOSPITAL_LOCATION"') }} as hospital_location,
        {{ null_junk('"HOSPITAL_ADDRESS"') }} as hospital_address,
        {{ null_junk('"HOSPITAL_LICENSURE_INFORMATION"') }} as hospital_licensure_information,
        {{ null_junk('"NPI"') }} as npi,
        {{ null_junk('"MRF_DATE"') }} as mrf_date,
        {{ null_junk('"CMS_TEMPLATE_VERSION"') }} as cms_template_version,
        {{ null_junk('"ATTESTATION_STATEMENT"') }} as attestation_statement,
        {{ null_junk('"ATTESTER_NAME"') }} as attester_name,
        {{ null_junk('"GENERAL_DESCRIPTION"') }} as general_description,
        {{ null_junk('"SETTING"') }} as setting,
        {{ null_junk('"BILLING_CODE"') }} as billing_code,
        {{ null_junk('"CODE_TYPE"') }} as code_type,
        {{ null_junk('"MODIFIERS"') }} as modifiers,
        {{ null_junk('"PAYER_NAME"') }} as payer_name,
        {{ null_junk('"PLAN_NAME"') }} as plan_name,
        {{ null_junk('"STANDARD_CHARGE_METHOD"') }} as standard_charge_method,
        {{ null_junk('"GROSS_CHARGE"') }} as gross_charge,
        {{ null_junk('"DISCOUNTED_CASH_PRICE"') }} as discounted_cash_price,
        {{ null_junk('"PAYER_SPECIFIC_NEGOTIATED_CHARGE_DOLLAR"') }} as payer_specific_negotiated_charge_dollar,
        {{ null_junk('"PAYER_SPECIFIC_NEGOTIATED_CHARGE_PERCENTAGE"') }} as payer_specific_negotiated_charge_percentage,
        {{ null_junk('"PAYER_SPECIFIC_NEGOTIATED_CHARGE_ALGORITHM"') }} as payer_specific_negotiated_charge_algorithm,
        {{ null_junk('"MEDIAN_ALLOWED_AMOUNT"') }} as median_allowed_amount,
        {{ null_junk('"TENTH_PERCENTILE_ALLOWED_AMOUNT"') }} as tenth_percentile_allowed_amount,
        {{ null_junk('"NINETIETH_PERCENTILE_ALLOWED_AMOUNT"') }} as ninetieth_percentile_allowed_amount,
        {{ null_junk('"COUNT_OF_ALLOWED_AMOUNTS"') }} as count_of_allowed_amounts,
        {{ null_junk('"DE_IDENTIFIED_MIN_NEGOTIATED_CHARGE"') }} as de_identified_min_negotiated_charge,
        {{ null_junk('"DE_IDENTIFIED_MAX_NEGOTIATED_CHARGE"') }} as de_identified_max_negotiated_charge,
        {{ null_junk('"DRUG_UNIT_OF_MEASUREMENT"') }} as drug_unit_of_measurement,
        {{ null_junk('"DRUG_TYPE_OF_MEASUREMENT"') }} as drug_type_of_measurement,
        {{ null_junk('"ADDITIONAL_GENERIC_NOTES"') }} as additional_generic_notes,
        {{ null_junk('"ADDITIONAL_PAYER_SPECIFIC_NOTES"') }} as additional_payer_specific_notes,
        _INGESTED_AT as _loaded_at,
        'https://github.com/CMSgov/hospital-price-transparency' as _source_url,
    'ENT_' || LEFT(MD5('NPI' || '|' || (CASE WHEN LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', ''))) = 0 OR LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', ''))) > 10 OR NOT REGEXP_LIKE(UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', '')), '^[0-9]+$') OR LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', '')), 10, '0') = REPEAT('0', 10) OR (LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', ''))) >= 4 AND UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', '')) = REPEAT(LEFT(UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', '')), 1), LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', ''))))) OR LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', '')), 10, '0') = REPEAT(LEFT(LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', '')), 10, '0'), 1), 10) OR UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', '')) IN ('123456789', '987654321', '1234567890', '0987654321', '12345678', '87654321', '123456', '654321') OR LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', '')), 10, '0') IN ('123456789', '987654321', '1234567890', '0987654321', '12345678', '87654321', '123456', '654321') THEN NULL ELSE LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("NPI"), '[^0-9A-Za-z]', '')), 10, '0') END)), 16) as spine_entity_id  -- joins LIBRARY_META."CONNECT".ENTITY_GOLDEN.ENTITY_ID

    from source

)

select * from renamed
