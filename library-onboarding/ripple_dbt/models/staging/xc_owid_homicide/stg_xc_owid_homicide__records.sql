{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'XC_OWID_HOMICIDE') }}

),

renamed as (

    select
        ENTITY as entity,
        CODE as code,
        YEAR as year,
        HOMICIDE_RATE_PER_100_000_POPULATION as homicide_rate_per_100_000_population,
        WORLD_REGION_ACCORDING_TO_OWID as world_region_according_to_owid,
        INGESTED_AT as _loaded_at,
        'https://ourworldindata.org/grapher/homicide-rate-unodc.csv?csvType=full' as _source_url

    from source

)

select * from renamed
