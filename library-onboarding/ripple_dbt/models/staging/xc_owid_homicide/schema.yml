version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: XC_OWID_HOMICIDE
        description: >
          Raw landing table for OWID — Homicide rate (UNODC). Grain not yet determined.

models:
  - name: stg_xc_owid_homicide__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
