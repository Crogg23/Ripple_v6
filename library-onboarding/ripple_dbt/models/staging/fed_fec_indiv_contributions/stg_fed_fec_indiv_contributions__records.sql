{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (SUB_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: SUB_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'FED_FEC_INDIV_CONTRIBUTIONS') }}

),

renamed as (

    select
        "CMTE_ID" as cmte_id,
        "AMNDT_IND" as amndt_ind,
        "RPT_TP" as rpt_tp,
        "TRANSACTION_PGI" as transaction_pgi,
        "IMAGE_NUM" as image_num,
        "TRANSACTION_TP" as transaction_tp,
        "ENTITY_TP" as entity_tp,
        "NAME" as name,
        "CITY" as city,
        "STATE" as state,
        "ZIP_CODE" as zip_code,
        "EMPLOYER" as employer,
        "OCCUPATION" as occupation,
        "TRANSACTION_DT" as transaction_dt,
        "TRANSACTION_AMT" as transaction_amt,
        "OTHER_ID" as other_id,
        "TRAN_ID" as tran_id,
        "FILE_NUM" as file_num,
        "MEMO_CD" as memo_cd,
        "MEMO_TEXT" as memo_text,
        "SUB_ID" as sub_id,
        INGESTED_AT as _loaded_at,
        'https://www.fec.gov/files/bulk-downloads/2024/indiv24.zip' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by sub_id order by _loaded_at desc) = 1
