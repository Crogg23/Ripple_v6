{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per organization (EIN is unique)
-- SPINE_ENTITY: organization  (natural_key: PLAN_EIN)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_UTAH_OPEN_DATA_P_C357F1F9E1') }}

),

renamed as (

    select
        "PLAN_NAME" as plan_name,
        "PLAN_ADMINISTRATOR" as plan_administrator,
        "ADMIN_STATE" as admin_state,
        "ADMIN_ZIP_CODE" as admin_zip_code,
        "PLAN_EIN" as plan_ein,
        "PLAN_NO" as plan_no,
        "PLAN_YEAR" as plan_year,
        "CASE_TYPE" as case_type,
        "CLOSING_REASON" as closing_reason,
        "CLOSING_DATE" as closing_date,
        "PENALTY_AMOUNT" as penalty_amount,
        INGESTED_AT as _loaded_at,
        'https://opendata.utah.gov/d/dmkh-jgap' as _source_url,
    'ENT_' || LEFT(MD5('EIN' || '|' || (CASE WHEN LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("PLAN_EIN"), '[^0-9A-Za-z]', ''))) = 0 OR LENGTH(UPPER(REGEXP_REPLACE(TO_VARCHAR("PLAN_EIN"), '[^0-9A-Za-z]', ''))) > 9 OR LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("PLAN_EIN"), '[^0-9A-Za-z]', '')), 9, '0') = REPEAT('0', 9) THEN NULL ELSE LPAD(UPPER(REGEXP_REPLACE(TO_VARCHAR("PLAN_EIN"), '[^0-9A-Za-z]', '')), 9, '0') END)), 16) as spine_entity_id  -- joins LIBRARY_META."CONNECT".ENTITY_GOLDEN.ENTITY_ID

    from source

)

select * from renamed
qualify row_number() over (partition by plan_ein order by _loaded_at desc) = 1
