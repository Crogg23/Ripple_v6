version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WESTERN_PENNSYLV_6A4C3E0E78
        description: >
          Raw landing table for Allegheny County Public Swimming Pool, Hot Tub, and Spa Inspections. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_western_pennsylv_6a4c3e0e78__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INSPECTION_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: inspection_id
        data_tests:
          - unique
          - not_null
