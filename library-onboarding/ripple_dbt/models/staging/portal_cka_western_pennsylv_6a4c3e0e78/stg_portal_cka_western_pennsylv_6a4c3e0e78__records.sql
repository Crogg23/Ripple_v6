{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INSPECTION_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: INSPECTION_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_6A4C3E0E78') }}

),

renamed as (

    select
        "INSPECTION_ID" as inspection_id,
        "FACILITY_ID" as facility_id,
        "FACILITY_NAME" as facility_name,
        "FACILITY_ADDRESS" as facility_address,
        "FACILITY_ADDRESS_2" as facility_address_2,
        "FACILITY_MUNICIPALITY_NAME" as facility_municipality_name,
        "FACILITY_CITY" as facility_city,
        "FACILITY_COUNTY" as facility_county,
        "FACILITY_STATE" as facility_state,
        "FACILITY_POSTAL_CODE" as facility_postal_code,
        "FACILITY_LATITUDE" as facility_latitude,
        "FACILITY_LONGITUDE" as facility_longitude,
        "VENUE_TYPE" as venue_type,
        "INSPECTION_DATE" as inspection_date,
        "INSPECTION_END" as inspection_end,
        "INSPECTION_PURPOSE" as inspection_purpose,
        "INSPECTION_PASSED" as inspection_passed,
        "INSPECTION_NUMBER" as inspection_number,
        "INSPECTOR_NAME" as inspector_name,
        "FREE_CHLORINE_SHALLOW" as free_chlorine_shallow,
        "FREE_CHLORINE_DEEP" as free_chlorine_deep,
        "COMBINED_CHLORINE_SHALLOW" as combined_chlorine_shallow,
        "COMBINED_CHLORINE_DEEP" as combined_chlorine_deep,
        "FREE_BROMINE_SHALLOW" as free_bromine_shallow,
        "FREE_BROMINE_DEEP" as free_bromine_deep,
        "PH_VALUE_SHALLOW" as ph_value_shallow,
        "PH_VALUE_DEEP" as ph_value_deep,
        "CYANURIC_ACID" as cyanuric_acid,
        "TURNOVER" as turnover,
        "ENCLOSURE" as enclosure,
        "MAIN_DRAIN_VISIBLE" as main_drain_visible,
        "SAFETY_EQUIPMENT" as safety_equipment,
        "DISINFECTANT_LEVEL" as disinfectant_level,
        "PH_BALANCE" as ph_balance,
        "NO_IMMINENT_HEALTH_HAZARDS" as no_imminent_health_hazards,
        "ABATED_DATE" as abated_date,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-public-swimming-pool-hot-tub-and-spa-inspections' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by inspection_id order by _loaded_at desc) = 1
