{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (DATASET_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: DATASET_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 13 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'INTL_BR_DADOS_GOV') }}
    qualify row_number() over (partition by "DATASET_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"DATASET_ID"') }} as dataset_id,
        {{ null_junk('"TITLE"') }} as title,
        {{ null_junk('"C_ORGANIZATION"') }} as c_organization,
        {{ null_junk('"THEME"') }} as theme,
        {{ null_junk('"NUM_RESOURCES"') }} as num_resources,
        {{ null_junk('"DOWNLOAD_COUNT"') }} as download_count,
        {{ null_junk('"LAST_UPDATED"') }} as last_updated,
        {{ null_junk('"LICENSE"') }} as license,
        {{ null_junk('"DESCRIPTION"') }} as description,
        {{ null_junk('"RESOURCE_URLS"') }} as resource_urls,
        {{ null_junk('"TAGS"') }} as tags,
        {{ null_junk('"GEOGRAPHIC_COVERAGE"') }} as geographic_coverage,
        {{ null_junk('"TEMPORAL_COVERAGE"') }} as temporal_coverage,
        _INGESTED_AT as _loaded_at,
        'https://dados.gov.br/' as _source_url

    from source

)

select * from renamed
