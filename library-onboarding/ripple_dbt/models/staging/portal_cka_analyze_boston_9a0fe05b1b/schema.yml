version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_9A0FE05B1B
        description: >
          Raw landing table for City of Boston Contract Award. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_9a0fe05b1b__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CNTRCT_HDR_CNTRCT_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: cntrct_hdr_cntrct_id
        data_tests:
          - unique
          - not_null
