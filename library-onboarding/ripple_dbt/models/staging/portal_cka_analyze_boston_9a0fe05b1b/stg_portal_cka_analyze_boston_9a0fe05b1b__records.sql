{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CNTRCT_HDR_CNTRCT_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: CNTRCT_HDR_CNTRCT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_9A0FE05B1B') }}

),

renamed as (

    select
        "CNTRCT_HDR_CNTRCT_ID" as cntrct_hdr_cntrct_id,
        "FY_CNTRCT_BEGIN_DT" as fy_cntrct_begin_dt,
        "FYQ_CNTRCT_BEGIN_DT" as fyq_cntrct_begin_dt,
        "CS_DOC_HDR_DEPTID" as cs_doc_hdr_deptid,
        "DEPT_TBL_DESCR_3_DIGIT" as dept_tbl_descr_3_digit,
        "CNTRCT_HDR_VERSION_NBR" as cntrct_hdr_version_nbr,
        "VENDOR_NAME1" as vendor_name1,
        "CNTRCT_HDR_DESCR" as cntrct_hdr_descr,
        "CNTRCT_HDR_VNDR_CNTRCT_REF" as cntrct_hdr_vndr_cntrct_ref,
        "CNTRCT_HDR_CNTRCT_BEGIN_DT" as cntrct_hdr_cntrct_begin_dt,
        "CNTRCT_HDR_CNTRCT_EXPIRE_DT" as cntrct_hdr_cntrct_expire_dt,
        "AMT_CNTRCT_MAX" as amt_cntrct_max,
        "CS_DOC_HDR_DESCR60" as cs_doc_hdr_descr60,
        "CITY_OR_STATE_MBE_WBE_CERTIFIED" as city_or_state_mbe_wbe_certified,
        "CITY_SLBE_SBE_CERTIFIED" as city_slbe_sbe_certified,
        "PROCUREMENT_OR_OTHER_CATEGORY" as procurement_or_other_category,
        "CONTRACT_METHOD_SUBCATEGORY" as contract_method_subcategory,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/city-of-boston-contract-award' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by cntrct_hdr_cntrct_id order by _loaded_at desc) = 1
