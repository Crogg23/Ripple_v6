{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CNTRCT_HDR_CNTRCT_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: CNTRCT_HDR_CNTRCT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 17 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   fy_cntrct_begin_dt: integer
--   cntrct_hdr_version_nbr: integer
--   cntrct_hdr_cntrct_begin_dt: date
--   cntrct_hdr_cntrct_expire_dt: date
--   amt_cntrct_max: float
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_9A0FE05B1B') }}
    qualify row_number() over (partition by "CNTRCT_HDR_CNTRCT_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CNTRCT_HDR_CNTRCT_ID"') }} as cntrct_hdr_cntrct_id,
        {{ stg_int('"FY_CNTRCT_BEGIN_DT"') }} as fy_cntrct_begin_dt,
        {{ null_junk('"FYQ_CNTRCT_BEGIN_DT"') }} as fyq_cntrct_begin_dt,
        {{ null_junk('"CS_DOC_HDR_DEPTID"') }} as cs_doc_hdr_deptid,
        {{ null_junk('"DEPT_TBL_DESCR_3_DIGIT"') }} as dept_tbl_descr_3_digit,
        {{ stg_int('"CNTRCT_HDR_VERSION_NBR"') }} as cntrct_hdr_version_nbr,
        {{ null_junk('"VENDOR_NAME1"') }} as vendor_name1,
        {{ null_junk('"CNTRCT_HDR_DESCR"') }} as cntrct_hdr_descr,
        {{ null_junk('"CNTRCT_HDR_VNDR_CNTRCT_REF"') }} as cntrct_hdr_vndr_cntrct_ref,
        {{ stg_date('"CNTRCT_HDR_CNTRCT_BEGIN_DT"') }} as cntrct_hdr_cntrct_begin_dt,
        {{ stg_date('"CNTRCT_HDR_CNTRCT_EXPIRE_DT"') }} as cntrct_hdr_cntrct_expire_dt,
        {{ stg_float('"AMT_CNTRCT_MAX"') }} as amt_cntrct_max,
        {{ null_junk('"CS_DOC_HDR_DESCR60"') }} as cs_doc_hdr_descr60,
        {{ null_junk('"CITY_OR_STATE_MBE_WBE_CERTIFIED"') }} as city_or_state_mbe_wbe_certified,
        {{ null_junk('"CITY_SLBE_SBE_CERTIFIED"') }} as city_slbe_sbe_certified,
        {{ null_junk('"PROCUREMENT_OR_OTHER_CATEGORY"') }} as procurement_or_other_category,
        {{ null_junk('"CONTRACT_METHOD_SUBCATEGORY"') }} as contract_method_subcategory,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/city-of-boston-contract-award' as _source_url

    from source

)

select * from renamed
