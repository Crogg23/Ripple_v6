{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_UTAH_OPEN_DATA_P_8DE28DA9D9') }}

),

renamed as (

    select
        "CITY" as city,
        "COUNTRY" as country,
        "COUNTY" as county,
        "COUNTYFIPS" as countyfips,
        "ID" as id,
        "LATITUDE" as latitude,
        "LINES" as lines,
        "LONGITUDE" as longitude,
        "MAX_INFER" as max_infer,
        "MAX_VOLT" as max_volt,
        "MIN_INFER" as min_infer,
        "MIN_VOLT" as min_volt,
        "NAICS_CODE" as naics_code,
        "NAICS_DESC" as naics_desc,
        "NAME" as name,
        "OBJECTID" as objectid,
        "SOURCE" as source,
        "SOURCEDATE" as sourcedate,
        "STATE" as state,
        "STATUS" as status,
        "TYPE" as type,
        "VAL_DATE" as val_date,
        "VAL_METHOD" as val_method,
        "ZIP" as zip,
        "POINT" as point,
        "COMPUTED_REGION_5D9V_6BUI" as computed_region_5d9v_6bui,
        "COMPUTED_REGION_QMWN_IMPY" as computed_region_qmwn_impy,
        "COMPUTED_REGION_JDNU_JMST" as computed_region_jdnu_jmst,
        "COMPUTED_REGION_2FPW_SWV9" as computed_region_2fpw_swv9,
        "COMPUTED_REGION_MI24_NG5Q" as computed_region_mi24_ng5q,
        INGESTED_AT as _loaded_at,
        'https://opendata.utah.gov/d/bht4-rfst' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by id order by _loaded_at desc) = 1
