version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_F1B3F76830
        description: >
          Raw landing table for Building and Property Violations. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_f1b3f76830__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+STATUS_DTTM unique). SPINE_ENTITY: place."
    columns:
      - name: contact_zip
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: status_dttm
        data_tests:
          - not_null:
              config:
                severity: warn
