version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_F1B3F76830
        description: >
          Raw landing table for Building and Property Violations. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_f1b3f76830__places
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+STATUS_DTTM unique). SPINE_ENTITY: place."
    columns:
      - name: contact_zip
        data_tests:
          - not_null
      - name: status_dttm
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - contact_zip
              - status_dttm