{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+CREATE_DATE+PROPERTY_TYPE+INSPECTION_STATUS+PARCEL_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ZIP_CODE, CREATE_DATE, PROPERTY_TYPE, INSPECTION_STATUS, PARCEL_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_23B8B5B7D2') }}

),

renamed as (

    select
        "PARCEL_ID" as parcel_id,
        "ADDRESS" as address,
        "ZIP_CODE" as zip_code,
        "OWNER" as owner,
        "PROPERTY_TYPE" as property_type,
        "CREATE_DATE" as create_date,
        "LATEST_INSPECTION_RESULT" as latest_inspection_result,
        "LATEST_INSPECTION_SCORE" as latest_inspection_score,
        "RECORD_NUMBER" as record_number,
        "INSPECTION_STATUS" as inspection_status,
        "LATITUDE" as latitude,
        "LONGITUDE" as longitude,
        "NEIGHBORHOOD" as neighborhood,
        "COUNCIL_DISTRICT" as council_district,
        "WARD" as ward,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/condemned-properties' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by zip_code, create_date, property_type, inspection_status, parcel_id order by _loaded_at desc) = 1
