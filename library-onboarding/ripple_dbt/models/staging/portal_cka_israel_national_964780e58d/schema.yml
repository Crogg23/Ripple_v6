version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ISRAEL_NATIONAL_964780E58D
        description: >
          Raw landing table for תחזית חצי-יומית למזהמי אוויר. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_israel_national_964780e58d__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TYPE_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: type_id
        data_tests:
          - unique
          - not_null
