{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TYPE_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TYPE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_964780E58D') }}

),

renamed as (

    select
        "TYPE_ID" as type_id,
        "TYPE" as type,
        "NAME" as name,
        "TITLE" as title,
        "AIR_QUALITY" as air_quality,
        "FILE_LINK" as file_link,
        "FILL_COLOR" as fill_color,
        "SHOW" as show,
        "DAY_OF_WEEK" as day_of_week,
        "DATE" as date,
        "CONTENT" as content,
        "MORNING" as morning,
        "MORNING_CONTENT" as morning_content,
        "NOON" as noon,
        "NOON_CONTENT" as noon_content,
        "EVENING" as evening,
        "EVENING_CONTENT" as evening_content,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/cf-output' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by type_id order by _loaded_at desc) = 1
