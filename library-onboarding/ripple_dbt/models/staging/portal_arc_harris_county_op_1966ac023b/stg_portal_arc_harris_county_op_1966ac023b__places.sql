{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+VAL_DATE unique)
-- SPINE_ENTITY: place  (natural_key: ZIP, VAL_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_HARRIS_COUNTY_OP_1966AC023B') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "NCESID" as ncesid,
        "NAME" as name,
        "ADDRESS" as address,
        "CITY" as city,
        "STATE" as state,
        "ZIP" as zip,
        "ZIP4" as zip4,
        "TELEPHONE" as telephone,
        "TYPE" as type,
        "STATUS" as status,
        "POPULATION" as population,
        "COUNTY" as county,
        "COUNTYFIPS" as countyfips,
        "COUNTRY" as country,
        "LATITUDE" as latitude,
        "LONGITUDE" as longitude,
        "NAICS_CODE" as naics_code,
        "NAICS_DESC" as naics_desc,
        "SOURCE" as source,
        "SOURCE_DAT" as source_dat,
        "VAL_METHOD" as val_method,
        "VAL_DATE" as val_date,
        "WEBSITE" as website,
        "LEVEL" as level,
        "ENROLLMENT" as enrollment,
        "ST_GRADE" as st_grade,
        "END_GRADE" as end_grade,
        "DISTRICTID" as districtid,
        "FT_TEACHER" as ft_teacher,
        "SHELTER_ID" as shelter_id,
        "VAILD" as vaild,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://geo-harriscounty.opendata.arcgis.com/datasets/6bb905559d7e41db86cacee9aabefa8d_0' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by zip, val_date order by _loaded_at desc) = 1
