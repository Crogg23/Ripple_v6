version: 2

models:
  - name: stg_fed_sec_business_development_company_report__organizations
    description: "GRAIN: one row per organization (CIK is unique). SPINE_ENTITY: organization."
    columns:
      - name: cik
        data_tests:
          - unique
          - not_null
