{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (SSWD_NP_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: SSWD_NP_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_11B79485F6') }}

),

renamed as (

    select
        "X" as x,
        "Y" as y,
        "OBJECTID" as objectid,
        "SSWD_NP_ID" as sswd_np_id,
        "SSWD_NP_PERMIT_NO" as sswd_np_permit_no,
        "SSWD_CLASSIFICATION" as sswd_classification,
        "REGION" as region,
        "ENTERED_BY" as entered_by,
        "ENTERED_DATE" as entered_date,
        "REFERENCE_POINT" as reference_point,
        "DELINEATION_AGENT" as delineation_agent,
        "APPLICANT" as applicant,
        "COUNTY" as county,
        "APPROVAL_DATE" as approval_date,
        "SITE_ACREAGE" as site_acreage,
        "FAC_NAME" as fac_name,
        "PROJECT_MANAGER" as project_manager,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/vwp-sswd' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by sswd_np_id order by _loaded_at desc) = 1
