{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (SSWD_NP_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: SSWD_NP_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 4 of 17 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   x: float
--   y: float
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   site_acreage: float
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_VIRGINIA_OPEN_DA_11B79485F6') }}
    qualify row_number() over (partition by "SSWD_NP_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_float('"X"') }} as x,
        {{ stg_float('"Y"') }} as y,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"SSWD_NP_ID"') }} as sswd_np_id,
        {{ null_junk('"SSWD_NP_PERMIT_NO"') }} as sswd_np_permit_no,
        {{ null_junk('"SSWD_CLASSIFICATION"') }} as sswd_classification,
        {{ null_junk('"REGION"') }} as region,
        {{ null_junk('"ENTERED_BY"') }} as entered_by,
        {{ null_junk('"ENTERED_DATE"') }} as entered_date,
        {{ null_junk('"REFERENCE_POINT"') }} as reference_point,
        {{ null_junk('"DELINEATION_AGENT"') }} as delineation_agent,
        {{ null_junk('"APPLICANT"') }} as applicant,
        {{ null_junk('"COUNTY"') }} as county,
        {{ null_junk('"APPROVAL_DATE"') }} as approval_date,
        {{ stg_float('"SITE_ACREAGE"') }} as site_acreage,
        {{ null_junk('"FAC_NAME"') }} as fac_name,
        {{ null_junk('"PROJECT_MANAGER"') }} as project_manager,
        INGESTED_AT as _loaded_at,
        'https://data.virginia.gov/dataset/vwp-sswd' as _source_url

    from source

)

select * from renamed
