{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 13 of 30 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   ref_id: integer
--   schoolid: integer
--   yearbuilt: integer
--   numstories: float
--   cost: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   numstudent: integer
--   backuppowe: integer
--   x: float
--   y: float
--   numstudent12: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   numstudents13: integer
--   point_x: float
--   point_y: float
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_7F82F529E8') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"MATCH_TYPE"') }} as match_type,
        {{ stg_int('"REF_ID"') }} as ref_id,
        {{ null_junk('"ID1"') }} as id1,
        {{ null_junk('"ID"') }} as id,
        {{ stg_int('"SCHOOLID"') }} as schoolid,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"ZIPCODE"') }} as zipcode,
        {{ null_junk('"CONTACT"') }} as contact,
        {{ null_junk('"PHONENUMBE"') }} as phonenumbe,
        {{ stg_int('"YEARBUILT"') }} as yearbuilt,
        {{ stg_float('"NUMSTORIES"') }} as numstories,
        {{ stg_id_text('"COST"') }} as cost,
        {{ stg_int('"NUMSTUDENT"') }} as numstudent,
        {{ stg_int('"BACKUPPOWE"') }} as backuppowe,
        {{ null_junk('"SHELTERCAP"') }} as sheltercap,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"COMMENT"') }} as comment,
        {{ stg_float('"X"') }} as x,
        {{ stg_float('"Y"') }} as y,
        {{ stg_id_text('"NUMSTUDENT12"') }} as numstudent12,
        {{ null_junk('"CAMPUSHOUS"') }} as campushous,
        {{ stg_int('"NUMSTUDENTS13"') }} as numstudents13,
        {{ null_junk('"URL"') }} as url,
        {{ null_junk('"ADDRESS2013"') }} as address2013,
        {{ null_junk('"SHAPE_WKT"') }} as shape_wkt,
        {{ stg_float('"POINT_X"') }} as point_x,
        {{ stg_float('"POINT_Y"') }} as point_y,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/colleges-and-universities' as _source_url

    from source

)

select * from renamed
