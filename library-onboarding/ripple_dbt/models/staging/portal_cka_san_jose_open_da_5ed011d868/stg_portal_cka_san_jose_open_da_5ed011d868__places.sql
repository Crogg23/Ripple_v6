{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_LENGTH)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 4 of 27 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   facilityid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   intid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   prjid: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_5ED011D868') }}
    qualify row_number() over (partition by "SHAPE_LENGTH" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_id_text('"FACILITYID"') }} as facilityid,
        {{ stg_id_text('"INTID"') }} as intid,
        {{ stg_int('"PRJID"') }} as prjid,
        {{ null_junk('"PRJNAME"') }} as prjname,
        {{ null_junk('"COUNCILDISTRICT"') }} as councildistrict,
        {{ null_junk('"LOCATION"') }} as location,
        {{ null_junk('"DESCRIPTION"') }} as description,
        {{ null_junk('"PHASE"') }} as phase,
        {{ null_junk('"CURRENTSTATUS"') }} as currentstatus,
        {{ null_junk('"DIVISION"') }} as division,
        {{ null_junk('"LASTREVIEWED"') }} as lastreviewed,
        {{ null_junk('"LASTREVIEWEDUTC"') }} as lastreviewedutc,
        {{ null_junk('"PROJECTMANAGER"') }} as projectmanager,
        {{ null_junk('"TELEPHONE"') }} as telephone,
        {{ null_junk('"EMAIL"') }} as email,
        {{ null_junk('"AWARDDATE"') }} as awarddate,
        {{ null_junk('"AWARDDATEUTC"') }} as awarddateutc,
        {{ null_junk('"CONSTRUCTIONSTARTDATE"') }} as constructionstartdate,
        {{ null_junk('"CONSTRUCTIONSTARTDATEUTC"') }} as constructionstartdateutc,
        {{ null_junk('"BENEFICIALUSEDATE"') }} as beneficialusedate,
        {{ null_junk('"BENEFICIALUSEDATEUTC"') }} as beneficialusedateutc,
        {{ null_junk('"LASTUPDATE"') }} as lastupdate,
        {{ null_junk('"NOTES"') }} as notes,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        {{ null_junk('"GLOBALID"') }} as globalid,
        {{ null_junk('"CREATIONDATE"') }} as creationdate,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/capital-improvement-project-lines' as _source_url

    from source

)

select * from renamed
