{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_FFIEC_CALL_REPORTS') }}

),

renamed as (

    select
        DOCTYPE_HTML as doctype_html,
        RSSD_ID as rssd_id,
        IDRSSD as idrssd,
        REPORTING_PERIOD_END_DATE as reporting_period_end_date,
        INSTITUTION_NAME as institution_name,
        TOTAL_ASSETS as total_assets,
        TOTAL_DEPOSITS as total_deposits,
        TOTAL_LOANS_AND_LEASES as total_loans_and_leases,
        NET_INCOME as net_income,
        TIER1_CAPITAL as tier1_capital,
        PAST_DUE_LOANS_30_89 as past_due_loans_30_89,
        PAST_DUE_LOANS_90_PLUS as past_due_loans_90_plus,
        MDRM_CONCEPT_CODE as mdrm_concept_code,
        VALUE as value,
        _INGESTED_AT as _loaded_at,
        'https://cdr.ffiec.gov/public/PWS/DownloadBulkData.aspx' as _source_url

    from source

)

select * from renamed
