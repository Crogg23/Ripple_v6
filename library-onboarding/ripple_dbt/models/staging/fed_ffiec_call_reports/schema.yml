version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_FFIEC_CALL_REPORTS
        description: >
          Raw landing table for FFIEC Bank Call Reports (CDR). Grain not yet determined.

models:
  - name: stg_fed_ffiec_call_reports__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
