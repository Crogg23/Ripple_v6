{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GEO_ID+YEAR unique)
-- SPINE_ENTITY: (not determined)  (natural_key: GEO_ID, YEAR)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_HOUSTON_OPEN_DAT_1629EF6392') }}

),

renamed as (

    select
        "YEAR" as year,
        "GEO_ID" as geo_id,
        "NAME" as name,
        "TOTAL" as total,
        "NO_SCHOOLING" as no_schooling,
        "NURSERY_TO_4TH" as nursery_to_4th,
        "C_5TH_AND_6TH" as c_5th_and_6th,
        "C_7TH_AND_8TH" as c_7th_and_8th,
        "C_9TH" as c_9th,
        "C_10TH" as c_10th,
        "C_11TH" as c_11th,
        "C_12TH_NO_DIPLOMA" as c_12th_no_diploma,
        "HS_GRAD_GED_OR_ALT" as hs_grad_ged_or_alt,
        "SOME_COL_LESS_THAN_1_YR" as some_col_less_than_1_yr,
        "SOME_COL_1_YRS_NO_DEGREE" as some_col_1_yrs_no_degree,
        "ASSOC" as assoc,
        "BACHELOR" as bachelor,
        "MASTER" as master,
        "PROF_SCHOOL_DGR" as prof_school_dgr,
        "DOCT_DGR" as doct_dgr,
        INGESTED_AT as _loaded_at,
        'https://data.houstontx.gov/dataset/city-of-houston-population-estimates-by-education-attainment' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by geo_id, year order by _loaded_at desc) = 1
