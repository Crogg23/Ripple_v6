{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GEO_ID+YEAR unique)
-- SPINE_ENTITY: (not determined)  (natural_key: GEO_ID, YEAR)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 20 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_HOUSTON_OPEN_DAT_1629EF6392') }}
    qualify row_number() over (partition by "GEO_ID", "YEAR" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"YEAR"') }} as year,
        {{ null_junk('"GEO_ID"') }} as geo_id,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"TOTAL"') }} as total,
        {{ null_junk('"NO_SCHOOLING"') }} as no_schooling,
        {{ null_junk('"NURSERY_TO_4TH"') }} as nursery_to_4th,
        {{ null_junk('"C_5TH_AND_6TH"') }} as c_5th_and_6th,
        {{ null_junk('"C_7TH_AND_8TH"') }} as c_7th_and_8th,
        {{ null_junk('"C_9TH"') }} as c_9th,
        {{ null_junk('"C_10TH"') }} as c_10th,
        {{ null_junk('"C_11TH"') }} as c_11th,
        {{ null_junk('"C_12TH_NO_DIPLOMA"') }} as c_12th_no_diploma,
        {{ null_junk('"HS_GRAD_GED_OR_ALT"') }} as hs_grad_ged_or_alt,
        {{ null_junk('"SOME_COL_LESS_THAN_1_YR"') }} as some_col_less_than_1_yr,
        {{ null_junk('"SOME_COL_1_YRS_NO_DEGREE"') }} as some_col_1_yrs_no_degree,
        {{ null_junk('"ASSOC"') }} as assoc,
        {{ null_junk('"BACHELOR"') }} as bachelor,
        {{ null_junk('"MASTER"') }} as master,
        {{ null_junk('"PROF_SCHOOL_DGR"') }} as prof_school_dgr,
        {{ null_junk('"DOCT_DGR"') }} as doct_dgr,
        INGESTED_AT as _loaded_at,
        'https://data.houstontx.gov/dataset/city-of-houston-population-estimates-by-education-attainment' as _source_url

    from source

)

select * from renamed
