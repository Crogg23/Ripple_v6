{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (UNIQUE_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: UNIQUE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_CMS_MEDICARE_DIABETES_PREVENTION_PROGRAM') }}

),

renamed as (

    select
        "NAME_OF_INITIATIVE" as name_of_initiative,
        "ORGANIZATION_NAME" as organization_name,
        "LOCATION_NAME" as location_name,
        "LOCATION_1" as location_1,
        "STREET_ADDRESS_LINE_1" as street_address_line_1,
        "STREET_ADDRESS_LINE_2" as street_address_line_2,
        "CITY" as city,
        "STATE" as state,
        "ZIP_CODE" as zip_code,
        "TELEPHONE_NUMBER" as telephone_number,
        "NPI" as npi,
        "CATEGORY" as category,
        "UNIQUE_ID" as unique_id,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by unique_id order by _loaded_at desc) = 1
