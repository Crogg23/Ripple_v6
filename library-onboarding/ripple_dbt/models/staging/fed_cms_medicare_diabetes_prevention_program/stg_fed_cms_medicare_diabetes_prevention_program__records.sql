{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (UNIQUE_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: UNIQUE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 13 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_CMS_MEDICARE_DIABETES_PREVENTION_PROGRAM') }}
    qualify row_number() over (partition by "UNIQUE_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"NAME_OF_INITIATIVE"') }} as name_of_initiative,
        {{ null_junk('"ORGANIZATION_NAME"') }} as organization_name,
        {{ null_junk('"LOCATION_NAME"') }} as location_name,
        {{ null_junk('"LOCATION_1"') }} as location_1,
        {{ null_junk('"STREET_ADDRESS_LINE_1"') }} as street_address_line_1,
        {{ null_junk('"STREET_ADDRESS_LINE_2"') }} as street_address_line_2,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"TELEPHONE_NUMBER"') }} as telephone_number,
        {{ null_junk('"NPI"') }} as npi,
        {{ null_junk('"CATEGORY"') }} as category,
        {{ null_junk('"UNIQUE_ID"') }} as unique_id,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
