version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_NEW_YORK_STATE_O_EFF72C4402
        description: >
          Raw landing table for Database of Economic Incentives. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_new_york_state_o_eff72c4402__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PROJECT_ID_NUMBER unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: project_id_number
        data_tests:
          - unique
          - not_null
