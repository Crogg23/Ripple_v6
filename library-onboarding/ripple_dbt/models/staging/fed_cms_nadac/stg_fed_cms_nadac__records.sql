{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_CMS_NADAC') }}

),

renamed as (

    select
        NDC_DESCRIPTION as ndc_description,
        NDC as ndc,
        NADAC_PER_UNIT as nadac_per_unit,
        EFFECTIVE_DATE as effective_date,
        PRICING_UNIT as pricing_unit,
        PHARMACY_TYPE_INDICATOR as pharmacy_type_indicator,
        OTC as otc,
        EXPLANATION_CODE as explanation_code,
        CLASSIFICATION_FOR_RATE_SETTING as classification_for_rate_setting,
        CORRESPONDING_GENERIC_DRUG_NADAC_PER_UNIT as corresponding_generic_drug_nadac_per_unit,
        CORRESPONDING_GENERIC_DRUG_EFFECTIVE_DATE as corresponding_generic_drug_effective_date,
        AS_OF_DATE as as_of_date,
        INGESTED_AT as _loaded_at,
        'https://download.medicaid.gov/data/nadac-national-average-drug-acquisition-cost-12-25-2024.csv' as _source_url

    from source

)

select * from renamed
