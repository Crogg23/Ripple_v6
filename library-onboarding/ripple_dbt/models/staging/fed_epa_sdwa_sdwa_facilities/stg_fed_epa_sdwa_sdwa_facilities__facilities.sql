{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per facility (PWSID+FACILITY_DEACTIVATION_DATE+FIRST_REPORTED_DATE+LAST_REPORTED_DATE+FACILITY_ID+STATE_FACILITY_ID unique)
-- SPINE_ENTITY: facility  (natural_key: PWSID, FACILITY_DEACTIVATION_DATE, FIRST_REPORTED_DATE, LAST_REPORTED_DATE, FACILITY_ID, STATE_FACILITY_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 19 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'facility' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_SDWA_SDWA_FACILITIES') }}
    qualify row_number() over (partition by "PWSID", "FACILITY_DEACTIVATION_DATE", "FIRST_REPORTED_DATE", "LAST_REPORTED_DATE", "FACILITY_ID", "STATE_FACILITY_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"SUBMISSIONYEARQUARTER"') }} as submissionyearquarter,
        {{ null_junk('"PWSID"') }} as pwsid,
        {{ null_junk('"FACILITY_ID"') }} as facility_id,
        {{ null_junk('"FACILITY_NAME"') }} as facility_name,
        {{ null_junk('"STATE_FACILITY_ID"') }} as state_facility_id,
        {{ null_junk('"FACILITY_ACTIVITY_CODE"') }} as facility_activity_code,
        {{ null_junk('"FACILITY_DEACTIVATION_DATE"') }} as facility_deactivation_date,
        {{ null_junk('"FACILITY_TYPE_CODE"') }} as facility_type_code,
        {{ null_junk('"SUBMISSION_STATUS_CODE"') }} as submission_status_code,
        {{ null_junk('"IS_SOURCE_IND"') }} as is_source_ind,
        {{ null_junk('"WATER_TYPE_CODE"') }} as water_type_code,
        {{ null_junk('"AVAILABILITY_CODE"') }} as availability_code,
        {{ null_junk('"SELLER_TREATMENT_CODE"') }} as seller_treatment_code,
        {{ null_junk('"SELLER_PWSID"') }} as seller_pwsid,
        {{ null_junk('"SELLER_PWS_NAME"') }} as seller_pws_name,
        {{ null_junk('"FILTRATION_STATUS_CODE"') }} as filtration_status_code,
        {{ null_junk('"IS_SOURCE_TREATED_IND"') }} as is_source_treated_ind,
        {{ null_junk('"FIRST_REPORTED_DATE"') }} as first_reported_date,
        {{ null_junk('"LAST_REPORTED_DATE"') }} as last_reported_date,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
