version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_VIRGINIA_OPEN_DA_6973FD2BA7
        description: >
          Raw landing table for VDH PUD HAI LTACH VA Long-term Acute Care Hospitals. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_virginia_open_da_6973fd2ba7__places
    description: "GRAIN: one row per place (ZIP is unique). SPINE_ENTITY: place."
    columns:
      - name: zip_code
        data_tests:
          - unique
          - not_null
