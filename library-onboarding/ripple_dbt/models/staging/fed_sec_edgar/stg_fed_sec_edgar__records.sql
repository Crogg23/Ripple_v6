{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_SEC_EDGAR') }}

),

renamed as (

    select
        CIK as cik,
        ENTITYNAME as entityname,
        EIN as ein,
        ACCESSIONNUMBER as accessionnumber,
        FILEDAT as filedat,
        REPORTDATE as reportdate,
        FORM as form,
        PRIMARYDOCUMENT as primarydocument,
        PRIMARYDOCDESCRIPTION as primarydocdescription,
        ITEMS as items,
        ISINLINEXBRL as isinlinexbrl,
        ISXBRL as isxbrl,
        SIC as sic,
        SICDESCRIPTION as sicdescription,
        STATEOFINCORPORATION as stateofincorporation,
        FISCALYEAREND as fiscalyearend,
        TICKERS as tickers,
        EXCHANGES as exchanges,
        _INGESTED_AT as _loaded_at,
        'https://www.sec.gov/search-filings/edgar-application-programming-interfaces' as _source_url

    from source

)

select * from renamed
