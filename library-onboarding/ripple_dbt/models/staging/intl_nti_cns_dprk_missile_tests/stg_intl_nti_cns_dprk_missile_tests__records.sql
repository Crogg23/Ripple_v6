{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'INTL_NTI_CNS_DPRK_MISSILE_TESTS') }}

),

renamed as (

    select
        F1 as f1,
        DATE as date,
        DATE_ENTERED_UPDATED as date_entered_updated,
        LAUNCH_TIME_UTC as launch_time_utc,
        MISSILE_NAME as missile_name,
        MISSILE_TYPE as missile_type,
        LAUNCH_AGENCY_AUTHORITY as launch_agency_authority,
        FACILITY_NAME as facility_name,
        FACILITY_LOCATION as facility_location,
        OTHER_NAME as other_name,
        FACILITY_LATITUDE as facility_latitude,
        FACILITY_LONGITUDE as facility_longitude,
        LANDING_LOCATION as landing_location,
        APOGEE as apogee,
        DISTANCE_TRAVELLED as distance_travelled,
        CONFIRMATION_STATUS as confirmation_status,
        TEST_OUTCOME as test_outcome,
        ADDITIONAL_INFORMATION as additional_information,
        SOURCE_S as source_s,
        INGESTED_AT as _loaded_at,
        'https://www.nti.org/wp-content/uploads/2021/10/north_korea_missile_test_database.xlsx' as _source_url

    from source

)

select * from renamed
