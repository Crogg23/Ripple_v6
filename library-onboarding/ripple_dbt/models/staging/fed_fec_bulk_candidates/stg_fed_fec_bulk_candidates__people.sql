{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per person (FEC_CAND_ID+CAND_ELECTION_YR+CYCLE unique)
-- SPINE_ENTITY: person  (natural_key: CAND_ID, CAND_ELECTION_YR, CYCLE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 16 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'person' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_FEC_BULK_CANDIDATES') }}
    qualify row_number() over (partition by "CAND_ID", "CAND_ELECTION_YR", "CYCLE" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CAND_ID"') }} as cand_id,
        {{ null_junk('"CAND_NAME"') }} as cand_name,
        {{ null_junk('"CAND_PTY_AFFILIATION"') }} as cand_pty_affiliation,
        {{ null_junk('"CAND_ELECTION_YR"') }} as cand_election_yr,
        {{ null_junk('"CAND_OFFICE_ST"') }} as cand_office_st,
        {{ null_junk('"CAND_OFFICE"') }} as cand_office,
        {{ null_junk('"CAND_OFFICE_DISTRICT"') }} as cand_office_district,
        {{ null_junk('"CAND_ICI"') }} as cand_ici,
        {{ null_junk('"CAND_STATUS"') }} as cand_status,
        {{ null_junk('"CAND_PCC"') }} as cand_pcc,
        {{ null_junk('"CAND_ST1"') }} as cand_st1,
        {{ null_junk('"CAND_ST2"') }} as cand_st2,
        {{ null_junk('"CAND_CITY"') }} as cand_city,
        {{ null_junk('"CAND_ST"') }} as cand_st,
        {{ null_junk('"CAND_ZIP"') }} as cand_zip,
        {{ null_junk('"CYCLE"') }} as cycle,
        _INGESTED_AT as _loaded_at,
        'https://www.fec.gov/files/bulk-downloads/2024/cn24.zip' as _source_url

    from source

)

select * from renamed
