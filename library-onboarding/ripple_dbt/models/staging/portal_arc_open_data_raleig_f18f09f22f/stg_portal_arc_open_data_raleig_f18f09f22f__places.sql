{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (ZIP+APPLICATION_DATE unique)
-- SPINE_ENTITY: place  (natural_key: SHIPPING_ZIP_POSTAL_CODE, APPLICATION_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 19 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_OPEN_DATA_RALEIG_F18F09F22F') }}
    qualify row_number() over (partition by "SHIPPING_ZIP_POSTAL_CODE", "APPLICATION_DATE" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"APPLICATION_DATE"') }} as application_date,
        {{ null_junk('"ACCOUNT_NAME"') }} as account_name,
        {{ null_junk('"COMPANY_STRUCTURE"') }} as company_structure,
        {{ null_junk('"PRIMARY_OWNER"') }} as primary_owner,
        {{ null_junk('"STAGE"') }} as stage,
        {{ null_junk('"AMOUNT"') }} as amount,
        {{ null_junk('"SHIPPING_ADDRESS_LINE_1"') }} as shipping_address_line_1,
        {{ null_junk('"SHIPPING_CITY"') }} as shipping_city,
        {{ null_junk('"SHIPPING_ZIP_POSTAL_CODE"') }} as shipping_zip_postal_code,
        {{ null_junk('"FULL_ADDRESS"') }} as full_address,
        {{ null_junk('"PRIMARY_OWNER_EMAIL"') }} as primary_owner_email,
        {{ null_junk('"PHONE"') }} as phone,
        {{ null_junk('"PRIMARY_OWNER_RACE"') }} as primary_owner_race,
        {{ null_junk('"MINORITY_OWNED_FIRM"') }} as minority_owned_firm,
        {{ null_junk('"WOMEN_OWNED_FIRM"') }} as women_owned_firm,
        {{ null_junk('"VETERAN_OWNED_FIRM"') }} as veteran_owned_firm,
        {{ null_junk('"NAICS_6_DIGIT_CODE"') }} as naics_6_digit_code,
        {{ null_junk('"NAICS_6_DIGIT_DESCRIPTION"') }} as naics_6_digit_description,
        {{ null_junk('"FID"') }} as fid,
        INGESTED_AT as _loaded_at,
        'https://data.raleighnc.gov/datasets/1042dd661c9d42e49594c9a500928f29_1' as _source_url

    from source

)

select * from renamed
