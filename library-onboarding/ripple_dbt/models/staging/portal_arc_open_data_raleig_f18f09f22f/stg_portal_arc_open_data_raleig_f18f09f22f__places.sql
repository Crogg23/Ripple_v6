{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+APPLICATION_DATE unique)
-- SPINE_ENTITY: place  (natural_key: SHIPPING_ZIP_POSTAL_CODE, APPLICATION_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_OPEN_DATA_RALEIG_F18F09F22F') }}

),

renamed as (

    select
        "APPLICATION_DATE" as application_date,
        "ACCOUNT_NAME" as account_name,
        "COMPANY_STRUCTURE" as company_structure,
        "PRIMARY_OWNER" as primary_owner,
        "STAGE" as stage,
        "AMOUNT" as amount,
        "SHIPPING_ADDRESS_LINE_1" as shipping_address_line_1,
        "SHIPPING_CITY" as shipping_city,
        "SHIPPING_ZIP_POSTAL_CODE" as shipping_zip_postal_code,
        "FULL_ADDRESS" as full_address,
        "PRIMARY_OWNER_EMAIL" as primary_owner_email,
        "PHONE" as phone,
        "PRIMARY_OWNER_RACE" as primary_owner_race,
        "MINORITY_OWNED_FIRM" as minority_owned_firm,
        "WOMEN_OWNED_FIRM" as women_owned_firm,
        "VETERAN_OWNED_FIRM" as veteran_owned_firm,
        "NAICS_6_DIGIT_CODE" as naics_6_digit_code,
        "NAICS_6_DIGIT_DESCRIPTION" as naics_6_digit_description,
        "FID" as fid,
        INGESTED_AT as _loaded_at,
        'https://data.raleighnc.gov/datasets/1042dd661c9d42e49594c9a500928f29_1' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by shipping_zip_postal_code, application_date order by _loaded_at desc) = 1
