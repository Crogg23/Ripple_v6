version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_OPEN_DATA_RALEIG_F18F09F22F
        description: >
          Raw landing table for Review. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_open_data_raleig_f18f09f22f__places
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+APPLICATION_DATE unique). SPINE_ENTITY: place."
    columns:
      - name: shipping_zip_postal_code
        data_tests:
          - not_null
      - name: application_date
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - shipping_zip_postal_code
              - application_date