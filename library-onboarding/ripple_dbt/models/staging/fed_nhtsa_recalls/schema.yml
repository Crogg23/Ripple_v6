version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_NHTSA_RECALLS
        description: >
          Raw landing table for NHTSA ODI Recalls (flat file). Grain not yet determined.

models:
  - name: stg_fed_nhtsa_recalls__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
