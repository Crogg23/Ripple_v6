{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per event (EVENT_ID unique)
-- SPINE_ENTITY: event  (natural_key: EVENT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 16 of 51 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   begin_yearmonth: integer
--   begin_day: integer
--   begin_time: integer
--   end_yearmonth: integer
--   end_day: integer
--   end_time: integer
--   episode_id: integer
--   year: integer
--   injuries_direct: integer
--   injuries_indirect: integer
--   deaths_direct: integer
--   deaths_indirect: integer
--   magnitude: float
--   tor_length: float
--   begin_range: float
--   end_range: float
-- spine_entity 'event' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'FED_NOAA_STORM_EVENTS') }}
    qualify row_number() over (partition by "EVENT_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_int('"BEGIN_YEARMONTH"') }} as begin_yearmonth,
        {{ stg_int('"BEGIN_DAY"') }} as begin_day,
        {{ stg_int('"BEGIN_TIME"') }} as begin_time,
        {{ stg_int('"END_YEARMONTH"') }} as end_yearmonth,
        {{ stg_int('"END_DAY"') }} as end_day,
        {{ stg_int('"END_TIME"') }} as end_time,
        {{ stg_int('"EPISODE_ID"') }} as episode_id,
        {{ null_junk('"EVENT_ID"') }} as event_id,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"STATE_FIPS"') }} as state_fips,
        {{ stg_int('"YEAR"') }} as year,
        {{ null_junk('"MONTH_NAME"') }} as month_name,
        {{ null_junk('"EVENT_TYPE"') }} as event_type,
        {{ null_junk('"CZ_TYPE"') }} as cz_type,
        {{ null_junk('"CZ_FIPS"') }} as cz_fips,
        {{ null_junk('"CZ_NAME"') }} as cz_name,
        {{ null_junk('"WFO"') }} as wfo,
        {{ null_junk('"BEGIN_DATE_TIME"') }} as begin_date_time,
        {{ null_junk('"CZ_TIMEZONE"') }} as cz_timezone,
        {{ null_junk('"END_DATE_TIME"') }} as end_date_time,
        {{ stg_int('"INJURIES_DIRECT"') }} as injuries_direct,
        {{ stg_int('"INJURIES_INDIRECT"') }} as injuries_indirect,
        {{ stg_int('"DEATHS_DIRECT"') }} as deaths_direct,
        {{ stg_int('"DEATHS_INDIRECT"') }} as deaths_indirect,
        {{ null_junk('"DAMAGE_PROPERTY"') }} as damage_property,
        {{ null_junk('"DAMAGE_CROPS"') }} as damage_crops,
        {{ null_junk('"SOURCE"') }} as source,
        {{ stg_float('"MAGNITUDE"') }} as magnitude,
        {{ null_junk('"MAGNITUDE_TYPE"') }} as magnitude_type,
        {{ null_junk('"FLOOD_CAUSE"') }} as flood_cause,
        {{ null_junk('"CATEGORY"') }} as category,
        {{ null_junk('"TOR_F_SCALE"') }} as tor_f_scale,
        {{ stg_float('"TOR_LENGTH"') }} as tor_length,
        {{ null_junk('"TOR_WIDTH"') }} as tor_width,
        {{ null_junk('"TOR_OTHER_WFO"') }} as tor_other_wfo,
        {{ null_junk('"TOR_OTHER_CZ_STATE"') }} as tor_other_cz_state,
        {{ null_junk('"TOR_OTHER_CZ_FIPS"') }} as tor_other_cz_fips,
        {{ null_junk('"TOR_OTHER_CZ_NAME"') }} as tor_other_cz_name,
        {{ stg_float('"BEGIN_RANGE"') }} as begin_range,
        {{ null_junk('"BEGIN_AZIMUTH"') }} as begin_azimuth,
        {{ null_junk('"BEGIN_LOCATION"') }} as begin_location,
        {{ stg_float('"END_RANGE"') }} as end_range,
        {{ null_junk('"END_AZIMUTH"') }} as end_azimuth,
        {{ null_junk('"END_LOCATION"') }} as end_location,
        {{ null_junk('"BEGIN_LAT"') }} as begin_lat,
        {{ null_junk('"BEGIN_LON"') }} as begin_lon,
        {{ null_junk('"END_LAT"') }} as end_lat,
        {{ null_junk('"END_LON"') }} as end_lon,
        {{ null_junk('"EPISODE_NARRATIVE"') }} as episode_narrative,
        {{ null_junk('"EVENT_NARRATIVE"') }} as event_narrative,
        {{ null_junk('"DATA_SOURCE"') }} as data_source,
        INGESTED_AT as _loaded_at,
        'https://www.ncei.noaa.gov/pub/data/swdi/stormevents/csvfiles/StormEvents_details-ftp_v1.0_d2025_c20260526.csv.gz' as _source_url

    from source

)

select * from renamed
