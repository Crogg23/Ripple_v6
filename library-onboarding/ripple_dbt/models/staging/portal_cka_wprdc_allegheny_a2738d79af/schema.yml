version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_A2738D79AF
        description: >
          Raw landing table for Housing Market Value Analysis 2021. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_a2738d79af__places
    description: "GRAIN: one row per place (FIPS is unique). SPINE_ENTITY: place."
    columns:
      - name: geoid
        data_tests:
          - unique
          - not_null
