{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (OBJECT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: OBJECT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 3 of 87 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   x: float
--   y: float
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_HARRIS_COUNTY_OP_D354D2D6E2') }}
    qualify row_number() over (partition by "OBJECT_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"OBJECT_ID"') }} as object_id,
        {{ null_junk('"COMPANY_BUSINESS_NAME"') }} as company_business_name,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"ADDRESS_1"') }} as address_1,
        {{ null_junk('"PHONE"') }} as phone,
        {{ null_junk('"M_F"') }} as m_f,
        {{ null_junk('"LANGUAGE"') }} as language,
        {{ null_junk('"INSURANCE"') }} as insurance,
        {{ null_junk('"SPECIALTY"') }} as specialty,
        {{ null_junk('"OFFER_VIRTUAL"') }} as offer_virtual,
        {{ null_junk('"PHONE_1"') }} as phone_1,
        {{ null_junk('"ADDRESS_12"') }} as address_12,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE_NAME"') }} as state_name,
        {{ null_junk('"STATE_ABBREVIATION"') }} as state_abbreviation,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"ZIP_4_EXTENSION"') }} as zip_4_extension,
        {{ null_junk('"PRIMARY_NAICS"') }} as primary_naics,
        {{ null_junk('"ALL_NAICS_CODES"') }} as all_naics_codes,
        {{ null_junk('"PRIMARY_SIC"') }} as primary_sic,
        {{ null_junk('"ALL_SIC_CODES"') }} as all_sic_codes,
        {{ null_junk('"INDUSTRY_DESCRIPTION"') }} as industry_description,
        {{ null_junk('"AFFILIATED_ORGS"') }} as affiliated_orgs,
        {{ null_junk('"BRANDS"') }} as brands,
        {{ null_junk('"HEADQUARTERS_NAME"') }} as headquarters_name,
        {{ null_junk('"LOCATION_CONFIDENCE"') }} as location_confidence,
        {{ null_junk('"NAICS_INDUSTRY_SECTOR"') }} as naics_industry_sector,
        {{ null_junk('"BUSINESS_CATEGORY"') }} as business_category,
        {{ null_junk('"SQUARE_FOOTAGE"') }} as square_footage,
        {{ null_junk('"SQUARE_FOOT_MINIMUM"') }} as square_foot_minimum,
        {{ null_junk('"SQUARE_FOOT_MAXIMUM"') }} as square_foot_maximum,
        {{ null_junk('"EMPLOYEE_COUNT"') }} as employee_count,
        {{ null_junk('"SALES_VOLUME"') }} as sales_volume,
        {{ null_junk('"CORPORATE_PARENT_NAME"') }} as corporate_parent_name,
        {{ null_junk('"SOURCE"') }} as source,
        {{ null_junk('"ESRI_PID"') }} as esri_pid,
        {{ null_junk('"DESCRIPTION"') }} as description,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"LOC_NAME"') }} as loc_name,
        {{ null_junk('"SCORE"') }} as score,
        {{ null_junk('"MATCH_TYPE"') }} as match_type,
        {{ null_junk('"MATCH_ADDR"') }} as match_addr,
        {{ null_junk('"ADDR_TYPE"') }} as addr_type,
        {{ null_junk('"ADDNUM"') }} as addnum,
        {{ null_junk('"SIDE"') }} as side,
        {{ null_junk('"STPREDIR"') }} as stpredir,
        {{ null_junk('"STPRETYPE"') }} as stpretype,
        {{ null_junk('"STNAME"') }} as stname,
        {{ null_junk('"STTYPE"') }} as sttype,
        {{ null_junk('"STDIR"') }} as stdir,
        {{ null_junk('"STADDR"') }} as staddr,
        {{ null_junk('"COUNTY"') }} as county,
        {{ null_junk('"STATEABBR"') }} as stateabbr,
        {{ stg_float('"X"') }} as x,
        {{ stg_float('"Y"') }} as y,
        {{ null_junk('"DISPLAYX"') }} as displayx,
        {{ null_junk('"DISPLAYY"') }} as displayy,
        {{ null_junk('"XMIN"') }} as xmin,
        {{ null_junk('"XMAX"') }} as xmax,
        {{ null_junk('"YMIN"') }} as ymin,
        {{ null_junk('"YMAX"') }} as ymax,
        {{ null_junk('"ADDNUMFROM"') }} as addnumfrom,
        {{ null_junk('"ADDNUMTO"') }} as addnumto,
        {{ null_junk('"COUNTRY"') }} as country,
        {{ null_junk('"LANGCODE"') }} as langcode,
        {{ null_junk('"DISTANCE"') }} as distance,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"IN_SINGLEL"') }} as in_singlel,
        {{ null_junk('"USER_ZIP_C"') }} as user_zip_c,
        {{ null_junk('"USER_NAME"') }} as user_name,
        {{ null_junk('"USER_ADDRE"') }} as user_addre,
        {{ null_junk('"USER_MD"') }} as user_md,
        {{ null_junk('"USER_LPC"') }} as user_lpc,
        {{ null_junk('"USER_PHD_E"') }} as user_phd_e,
        {{ null_junk('"USER_MSW_L"') }} as user_msw_l,
        {{ null_junk('"USER_LMFT"') }} as user_lmft,
        {{ null_junk('"USER_LCDC"') }} as user_lcdc,
        {{ null_junk('"USER_OTHER"') }} as user_other,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"NAME_1"') }} as name_1,
        {{ null_junk('"ZIP_CODE_1"') }} as zip_code_1,
        {{ null_junk('"NAME_12"') }} as name_12,
        {{ null_junk('"ADDRESS_12_13"') }} as address_12_13,
        {{ null_junk('"PHONE_NUMBER"') }} as phone_number,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://geo-harriscounty.opendata.arcgis.com/datasets/de50825b651b4232b9e647795333484a_0' as _source_url

    from source

)

select * from renamed
