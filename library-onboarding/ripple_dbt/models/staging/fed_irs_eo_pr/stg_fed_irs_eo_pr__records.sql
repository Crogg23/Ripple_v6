{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_IRS_EO_PR') }}

),

renamed as (

    select
        EIN as ein,
        NAME as name,
        ICO as ico,
        STREET as street,
        CITY as city,
        STATE as state,
        ZIP as zip,
        GROUP as group,
        SUBSECTION as subsection,
        AFFILIATION as affiliation,
        CLASSIFICATION as classification,
        RULING as ruling,
        DEDUCTIBILITY as deductibility,
        FOUNDATION as foundation,
        ACTIVITY as activity,
        ORGANIZATION as organization,
        STATUS as status,
        TAX_PERIOD as tax_period,
        ASSET_CD as asset_cd,
        INCOME_CD as income_cd,
        FILING_REQ_CD as filing_req_cd,
        PF_FILING_REQ_CD as pf_filing_req_cd,
        ACCT_PD as acct_pd,
        ASSET_AMT as asset_amt,
        INCOME_AMT as income_amt,
        REVENUE_AMT as revenue_amt,
        NTEE_CD as ntee_cd,
        SORT_NAME as sort_name,
        _INGESTED_AT as _loaded_at,
        'https://www.irs.gov/pub/irs-soi/eo_pr.csv' as _source_url

    from source

)

select * from renamed
