version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_USASPENDING_SUBAWARDS

models:
  - name: stg_fed_usaspending_subawards__subawards
    description: "SAMPLE ONLY -- NOT the full dataset. USAspending subawards: a 5,000-row API slice of the multi-million-row subaward corpus, landed as flat JSON records. Use for shape/testing only."
    columns:
      - name: subaward_id
        data_tests: [not_null, unique]
