version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_SF_OPENDATA_DATA_C19EE9EB44
        description: >
          Raw landing table for Mental Health (MH) and Substance Use Disorder (SUD) Provider Directory - Site List. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_sf_opendata_data_c19ee9eb44__providers
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (NPI+NAME unique). SPINE_ENTITY: provider."
    columns:
      - name: npi
        data_tests:
          - not_null
      - name: program_name
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - npi
            - program_name
