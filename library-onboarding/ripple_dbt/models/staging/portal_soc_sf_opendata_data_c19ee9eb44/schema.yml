version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_SF_OPENDATA_DATA_C19EE9EB44
        description: >
          Raw landing table for Mental Health (MH) and Substance Use Disorder (SUD) Provider Directory - Site List. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_sf_opendata_data_c19ee9eb44__providers
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per provider (NPI+NAME unique). SPINE_ENTITY: provider."
    columns:
      - name: npi
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: program_name
        data_tests:
          - not_null:
              config:
                severity: warn
