{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (NPI+NAME unique)
-- SPINE_ENTITY: (not determined)  (natural_key: NPI, PROGRAM_NAME)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_SF_OPENDATA_DATA_C19EE9EB44') }}

),

renamed as (

    select
        "PROVIDER_DIRECTORY" as provider_directory,
        "SITE_ID" as site_id,
        "NPI" as npi,
        "PROGRAM_NAME" as program_name,
        "LEGAL_ENTITY" as legal_entity,
        "SERVICES" as services,
        "LANGUAGE_LINE" as language_line,
        "ADA_COMPLIANT" as ada_compliant,
        "ACCEPTING_NEW" as accepting_new,
        "ACCEPTING_NEW_COMMENT" as accepting_new_comment,
        "ACCEPTING_NEW_CHIP_MEMBERS" as accepting_new_chip_members,
        "OFFERS_TELEHEALTH_SERVICES" as offers_telehealth_services,
        "ADDRESS_LINE_1" as address_line_1,
        "CITY" as city,
        "STATE" as state,
        "PHONE_NUMBER" as phone_number,
        "ZIP" as zip,
        "WEBSITE" as website,
        "AGE_GROUP" as age_group,
        "CULTURAL_COMPETENCIES" as cultural_competencies,
        "SPECIALTY" as specialty,
        "HOURS_OF_OPERATION" as hours_of_operation,
        "TYPE_OF_CARE" as type_of_care,
        "TYPE_OF_SERVICE" as type_of_service,
        "DATA_AS_OF" as data_as_of,
        "DATA_UPDATED_AT" as data_updated_at,
        "DATA_LOADED_AT" as data_loaded_at,
        "EMAIL_ADDRESS" as email_address,
        "GENDER_SERVED" as gender_served,
        "LANGUAGES" as languages,
        "ADDRESS_LINE_2" as address_line_2,
        "TREATMENT_TYPE" as treatment_type,
        "LEVEL_OF_CARE" as level_of_care,
        INGESTED_AT as _loaded_at,
        'https://data.sfgov.org/d/p6m6-pkpk' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by npi, program_name order by _loaded_at desc) = 1
