{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LAT)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_9EB7D97238') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "FACILITYID" as facilityid,
        "INTID" as intid,
        "GISOBJID" as gisobjid,
        "LOCATIONID" as locationid,
        "ASSETID" as assetid,
        "NAME" as name,
        "CENTERTYPE" as centertype,
        "ADDRESS" as address,
        "COUNCILDISTRICT" as councildistrict,
        "PARKDISTRICT" as parkdistrict,
        "CCSTATUS" as ccstatus,
        "ESTIMATEDAREA" as estimatedarea,
        "RESTROOMCONTRACT" as restroomcontract,
        "LANDSCAPECONTRACT" as landscapecontract,
        "DATEOPENED" as dateopened,
        "HANDICAPPARKING" as handicapparking,
        "RENOVATEDYEAR" as renovatedyear,
        "BLDGMATERIALTYPE" as bldgmaterialtype,
        "NUMBEROFFLOORS" as numberoffloors,
        "DESCRIPTION" as description,
        "LAT" as lat,
        "LONG" as long,
        "HYPERLINK" as hyperlink,
        "KITCHEN" as kitchen,
        "GYM" as gym,
        "PARKING" as parking,
        "ELEVATOR" as elevator,
        "LASTUPDATE" as lastupdate,
        "SHAPE_LENGTH" as shape_length,
        "SHAPE_AREA" as shape_area,
        "SENIORNUTRITION" as seniornutrition,
        "CENTERSUBTYPE" as centersubtype,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/community-center' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by lat order by _loaded_at desc) = 1
