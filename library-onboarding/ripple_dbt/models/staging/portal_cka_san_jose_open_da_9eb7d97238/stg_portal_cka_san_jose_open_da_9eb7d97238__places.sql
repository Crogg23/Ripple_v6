{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON is unique)
-- SPINE_ENTITY: place  (natural_key: LAT)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 11 of 33 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   facilityid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   intid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   councildistrict: integer
--   parkdistrict: integer
--   estimatedarea: integer
--   handicapparking: integer
--   renovatedyear: integer
--   numberoffloors: integer
--   long: float
--   parking: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_9EB7D97238') }}
    qualify row_number() over (partition by "LAT" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_id_text('"FACILITYID"') }} as facilityid,
        {{ stg_id_text('"INTID"') }} as intid,
        {{ null_junk('"GISOBJID"') }} as gisobjid,
        {{ null_junk('"LOCATIONID"') }} as locationid,
        {{ null_junk('"ASSETID"') }} as assetid,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"CENTERTYPE"') }} as centertype,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ stg_int('"COUNCILDISTRICT"') }} as councildistrict,
        {{ stg_int('"PARKDISTRICT"') }} as parkdistrict,
        {{ null_junk('"CCSTATUS"') }} as ccstatus,
        {{ stg_int('"ESTIMATEDAREA"') }} as estimatedarea,
        {{ null_junk('"RESTROOMCONTRACT"') }} as restroomcontract,
        {{ null_junk('"LANDSCAPECONTRACT"') }} as landscapecontract,
        {{ null_junk('"DATEOPENED"') }} as dateopened,
        {{ stg_int('"HANDICAPPARKING"') }} as handicapparking,
        {{ stg_int('"RENOVATEDYEAR"') }} as renovatedyear,
        {{ null_junk('"BLDGMATERIALTYPE"') }} as bldgmaterialtype,
        {{ stg_int('"NUMBEROFFLOORS"') }} as numberoffloors,
        {{ null_junk('"DESCRIPTION"') }} as description,
        {{ null_junk('"LAT"') }} as lat,
        {{ stg_float('"LONG"') }} as long,
        {{ null_junk('"HYPERLINK"') }} as hyperlink,
        {{ null_junk('"KITCHEN"') }} as kitchen,
        {{ null_junk('"GYM"') }} as gym,
        {{ stg_int('"PARKING"') }} as parking,
        {{ null_junk('"ELEVATOR"') }} as elevator,
        {{ null_junk('"LASTUPDATE"') }} as lastupdate,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        {{ null_junk('"SHAPE_AREA"') }} as shape_area,
        {{ null_junk('"SENIORNUTRITION"') }} as seniornutrition,
        {{ null_junk('"CENTERSUBTYPE"') }} as centersubtype,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/community-center' as _source_url

    from source

)

select * from renamed
