version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_FHFA_HPI
        description: >
          Raw landing table for FHFA House Price Index (HPI) — Master File. Grain not yet determined.

models:
  - name: stg_fed_fhfa_hpi__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
