{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_FHFA_HPI') }}

),

renamed as (

    select
        HPI_TYPE as hpi_type,
        HPI_FLAVOR as hpi_flavor,
        FREQUENCY as frequency,
        LEVEL as level,
        PLACE_NAME as place_name,
        PLACE_ID as place_id,
        YR as yr,
        PERIOD as period,
        INDEX_NSA as index_nsa,
        INDEX_SA as index_sa,
        RSTDERR as rstderr,
        NOTE as note,
        INGESTED_AT as _loaded_at,
        'https://www.fhfa.gov/hpi/download/monthly/hpi_master.csv' as _source_url

    from source

)

select * from renamed
