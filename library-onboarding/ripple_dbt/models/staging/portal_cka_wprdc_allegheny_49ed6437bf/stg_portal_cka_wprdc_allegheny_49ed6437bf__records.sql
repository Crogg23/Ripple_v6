{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (INSPECTION_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: INSPECTION_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_49ED6437BF') }}

),

renamed as (

    select
        "INSPECTION_ID" as inspection_id,
        "PERMIT_ID" as permit_id,
        "INSPECTION_DATE" as inspection_date,
        "ADDRESS" as address,
        "CITY" as city,
        "STATE" as state,
        "ZIP_CODE" as zip_code,
        "MUNICIPAL" as municipal,
        "BUILDING_TYPE" as building_type,
        "PLUMBER_NAME" as plumber_name,
        "FINAL_INSP_YN" as final_insp_yn,
        "STORM_DRAIN" as storm_drain,
        "BUILDING_DRAIN" as building_drain,
        "WATER_SERVICE" as water_service,
        "STORM_SEWER" as storm_sewer,
        "ROUGH_ABOVE_GROUND" as rough_above_ground,
        "WATER_HEATER" as water_heater,
        "BUILDING_SEWER" as building_sewer,
        "WATER_DISTRIBUTION" as water_distribution,
        "INDIRECT_WASTE" as indirect_waste,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-plumbing-inspections' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by inspection_id order by _loaded_at desc) = 1
