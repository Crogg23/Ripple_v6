version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: XC_WAYBACK_DOJ_EPSTEIN
        description: >
          Raw landing table for Wayback CDX Census — DOJ Epstein Library Listing Pages. Grain not yet determined.

models:
  - name: stg_xc_wayback_doj_epstein__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
