version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_CALIFORNIA_OPEN_6611464444
        description: >
          Raw landing table for Continuous Groundwater Level Measurements. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_california_open_6611464444__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (LATLON+SITE_CODE unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: longitude
        data_tests:
          - not_null
      - name: site_code
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - longitude
            - site_code
