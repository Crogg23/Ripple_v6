{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CSP_SCH_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: CSP_SCH_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_7D75FD803F') }}

),

renamed as (

    select
        "BLDG_ID" as bldg_id,
        "BLDG_NAME" as bldg_name,
        "ADDRESS" as address,
        "CITY" as city,
        "ZIPCODE" as zipcode,
        "CSP_SCH_ID" as csp_sch_id,
        "SCH_ID" as sch_id,
        "SCH_NAME" as sch_name,
        "SCH_LABEL" as sch_label,
        "SCH_TYPE" as sch_type,
        "SHARED" as shared,
        "COMPLEX" as complex,
        "SHAPE_WKT" as shape_wkt,
        "POINT_X" as point_x,
        "POINT_Y" as point_y,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/public-schools' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by csp_sch_id order by _loaded_at desc) = 1
