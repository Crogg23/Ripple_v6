version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ANALYZE_BOSTON_7D75FD803F
        description: >
          Raw landing table for Public Schools. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_analyze_boston_7d75fd803f__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (CSP_SCH_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: csp_sch_id
        data_tests:
          - unique
          - not_null
