version: 2

models:
  - name: stg_fed_irs_pub78_eligible_donees__organizations
    description: "GRAIN: one row per organization (EIN is unique). SPINE_ENTITY: organization."
    columns:
      - name: ein
        data_tests:
          - unique
          - not_null
