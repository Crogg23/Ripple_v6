version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_CALIFORNIA_OPEN_9C50AEF41D
        description: >
          Raw landing table for CalHeatScore Live Data for API Use. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_california_open_9c50aef41d__places
    description: "GRAIN: one row per place (ZIP is unique). SPINE_ENTITY: place."
    columns:
      - name: zip_code
        data_tests:
          - unique
          - not_null
