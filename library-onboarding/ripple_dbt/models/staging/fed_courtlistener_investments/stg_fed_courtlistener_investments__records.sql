{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 4 of 18 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   date_created: timestamp
--   date_modified: timestamp
--   page_number: integer
--   financial_disclosure_id: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'FED_COURTLISTENER_INVESTMENTS') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ stg_ts('"DATE_CREATED"') }} as date_created,
        {{ stg_ts('"DATE_MODIFIED"') }} as date_modified,
        {{ stg_int('"PAGE_NUMBER"') }} as page_number,
        {{ null_junk('"DESCRIPTION"') }} as description,
        {{ null_junk('"REDACTED"') }} as redacted,
        {{ null_junk('"INCOME_DURING_REPORTING_PERIOD_CODE"') }} as income_during_reporting_period_code,
        {{ null_junk('"INCOME_DURING_REPORTING_PERIOD_TYPE"') }} as income_during_reporting_period_type,
        {{ null_junk('"GROSS_VALUE_CODE"') }} as gross_value_code,
        {{ null_junk('"GROSS_VALUE_METHOD"') }} as gross_value_method,
        {{ null_junk('"TRANSACTION_DURING_REPORTING_PERIOD"') }} as transaction_during_reporting_period,
        {{ null_junk('"TRANSACTION_DATE_RAW"') }} as transaction_date_raw,
        {{ null_junk('"TRANSACTION_DATE"') }} as transaction_date,
        {{ null_junk('"TRANSACTION_VALUE_CODE"') }} as transaction_value_code,
        {{ null_junk('"TRANSACTION_GAIN_CODE"') }} as transaction_gain_code,
        {{ null_junk('"TRANSACTION_PARTNER"') }} as transaction_partner,
        {{ null_junk('"HAS_INFERRED_VALUES"') }} as has_inferred_values,
        {{ stg_int('"FINANCIAL_DISCLOSURE_ID"') }} as financial_disclosure_id,
        INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
