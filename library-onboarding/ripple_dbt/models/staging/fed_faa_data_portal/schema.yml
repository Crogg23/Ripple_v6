version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_FAA_DATA_PORTAL
        description: >
          Raw landing table for FAA Data Portal (data.faa.gov). Grain not yet determined.

models:
  - name: stg_fed_faa_data_portal__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
