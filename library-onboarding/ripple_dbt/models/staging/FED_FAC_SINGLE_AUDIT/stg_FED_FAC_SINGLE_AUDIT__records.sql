{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (REPORT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: REPORT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 28 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   audit_year: integer
--   cognizant_agency: integer
--   fy_start_date: date
--   fy_end_date: date
--   fac_accepted_date: date
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_FAC_SINGLE_AUDIT') }}
    qualify row_number() over (partition by "REPORT_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"REPORT_ID"') }} as report_id,
        {{ stg_int('"AUDIT_YEAR"') }} as audit_year,
        {{ null_junk('"AUDITEE_UEI"') }} as auditee_uei,
        {{ null_junk('"AUDITEE_EIN"') }} as auditee_ein,
        {{ null_junk('"AUDITEE_NAME"') }} as auditee_name,
        {{ null_junk('"AUDITEE_CITY"') }} as auditee_city,
        {{ null_junk('"AUDITEE_STATE"') }} as auditee_state,
        {{ null_junk('"AUDITEE_ZIP"') }} as auditee_zip,
        {{ null_junk('"AUDITEE_ADDRESS_LINE_1"') }} as auditee_address_line_1,
        {{ null_junk('"AUDITOR_EIN"') }} as auditor_ein,
        {{ null_junk('"AUDITOR_FIRM_NAME"') }} as auditor_firm_name,
        {{ null_junk('"AUDITOR_CITY"') }} as auditor_city,
        {{ null_junk('"AUDITOR_STATE"') }} as auditor_state,
        {{ null_junk('"AUDITOR_ZIP"') }} as auditor_zip,
        {{ null_junk('"ENTITY_TYPE"') }} as entity_type,
        {{ null_junk('"AUDIT_TYPE"') }} as audit_type,
        {{ null_junk('"GAAP_RESULTS"') }} as gaap_results,
        {{ null_junk('"IS_GOING_CONCERN_INCLUDED"') }} as is_going_concern_included,
        {{ null_junk('"IS_INTERNAL_CONTROL_MATERIAL_WEAKNESS_DISCLOSED"') }} as is_internal_control_material_weakness_disclosed,
        {{ null_junk('"IS_MATERIAL_NONCOMPLIANCE_DISCLOSED"') }} as is_material_noncompliance_disclosed,
        "TOTAL_AMOUNT_EXPENDED" as total_amount_expended,
        {{ null_junk('"IS_LOW_RISK_AUDITEE"') }} as is_low_risk_auditee,
        {{ null_junk('"OVERSIGHT_AGENCY"') }} as oversight_agency,
        {{ stg_int('"COGNIZANT_AGENCY"') }} as cognizant_agency,
        {{ stg_date('"FY_START_DATE"') }} as fy_start_date,
        {{ stg_date('"FY_END_DATE"') }} as fy_end_date,
        {{ stg_date('"FAC_ACCEPTED_DATE"') }} as fac_accepted_date,
        {{ null_junk('"DATA_SOURCE"') }} as data_source,
        _INGESTED_AT as _loaded_at,
        'https://api.fac.gov/general' as _source_url

    from source

)

select * from renamed
