version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_DATALA_LOS_ANGEL_361B8161B7
        description: >
          Raw landing table for Listing of All Businesses. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_datala_los_angel_361b8161b7__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+LOCATION_START_DATE+LOCATION_END_DATE+ZIP unique). SPINE_ENTITY: place."
    columns:
      - name: mailing_zip_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: location_start_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: location_end_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: zip_code
        data_tests:
          - not_null:
              config:
                severity: warn
