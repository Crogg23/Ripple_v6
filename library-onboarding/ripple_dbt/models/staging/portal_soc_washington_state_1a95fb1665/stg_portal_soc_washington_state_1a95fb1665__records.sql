{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (EIN+AUDIT_CLOSED_DATE+NAME+AUDIT_TYPE unique)
-- SPINE_ENTITY: (not determined)  (natural_key: UBI_EIN, AUDIT_CLOSED_DATE, RECIPIENT_TYPE, AUDIT_TYPE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_WASHINGTON_STATE_1A95FB1665') }}

),

renamed as (

    select
        "AUDIT_CONTROL_NUMBER" as audit_control_number,
        "RECIPIENT_NAME" as recipient_name,
        "RECIPIENT_TYPE" as recipient_type,
        "AUDIT_TYPE" as audit_type,
        "AUDIT_RECEIVED_OR_CONDUCTED" as audit_received_or_conducted,
        "AUDIT_CLOSED_DATE" as audit_closed_date,
        "UBI_EIN" as ubi_ein,
        INGESTED_AT as _loaded_at,
        'https://data.wa.gov/d/ncqf-syud' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by ubi_ein, audit_closed_date, recipient_type, audit_type order by _loaded_at desc) = 1
