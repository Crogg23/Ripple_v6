version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_WASHINGTON_STATE_1A95FB1665
        description: >
          Raw landing table for Department of Licensing Data Sharing Contract Audits History. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_washington_state_1a95fb1665__organizations
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per organization (EIN+AUDIT_CLOSED_DATE+NAME+AUDIT_TYPE unique). SPINE_ENTITY: organization."
    columns:
      - name: ubi_ein
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: audit_closed_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: recipient_type
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: audit_type
        data_tests:
          - not_null:
              config:
                severity: warn
