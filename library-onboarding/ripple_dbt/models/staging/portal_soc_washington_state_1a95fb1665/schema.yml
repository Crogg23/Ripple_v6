version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_WASHINGTON_STATE_1A95FB1665
        description: >
          Raw landing table for Department of Licensing Data Sharing Contract Audits History. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_washington_state_1a95fb1665__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (EIN+AUDIT_CLOSED_DATE+NAME+AUDIT_TYPE unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: ubi_ein
        data_tests:
          - not_null
      - name: audit_closed_date
        data_tests:
          - not_null
      - name: recipient_type
        data_tests:
          - not_null
      - name: audit_type
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - ubi_ein
            - audit_closed_date
            - recipient_type
            - audit_type
