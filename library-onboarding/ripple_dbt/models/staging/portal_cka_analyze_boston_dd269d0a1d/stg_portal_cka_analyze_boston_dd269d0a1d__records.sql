{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+SEGMENT_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ZIP_R, SEGMENT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_DD269D0A1D') }}

),

renamed as (

    select
        "SEGMENT_ID" as segment_id,
        "L_F_ADD" as l_f_add,
        "L_T_ADD" as l_t_add,
        "R_F_ADD" as r_f_add,
        "R_T_ADD" as r_t_add,
        "STREET_ID" as street_id,
        "PRE_DIR" as pre_dir,
        "ST_NAME" as st_name,
        "ST_TYPE" as st_type,
        "SUF_DIR" as suf_dir,
        "ALTERNATE_NAME" as alternate_name,
        "CFCC" as cfcc,
        "SPEEDLIMIT" as speedlimit,
        "ONEWAY" as oneway,
        "F_ZLEV" as f_zlev,
        "T_ZLEV" as t_zlev,
        "FT_COST" as ft_cost,
        "TF_COST" as tf_cost,
        "FT_DIR" as ft_dir,
        "TF_DIR" as tf_dir,
        "SHIELD" as shield,
        "HWY_NUM" as hwy_num,
        "MUN_L" as mun_l,
        "MUN_R" as mun_r,
        "NBHD_L" as nbhd_l,
        "NBHD_R" as nbhd_r,
        "STATE00_L" as state00_l,
        "STATE00_R" as state00_r,
        "COUNTY00_L" as county00_l,
        "COUNTY00_R" as county00_r,
        "MCD00_L" as mcd00_l,
        "MCD00_R" as mcd00_r,
        "ZIP_L" as zip_l,
        "ZIP_R" as zip_r,
        "SHAPE_LENGTH" as shape_length,
        "SHAPE_WKT" as shape_wkt,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/boston-street-segments-sam-system' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by zip_r, segment_id order by _loaded_at desc) = 1
