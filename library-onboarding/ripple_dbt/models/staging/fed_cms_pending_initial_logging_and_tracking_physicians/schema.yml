version: 2

models:
  - name: stg_fed_cms_pending_initial_logging_and_tracking_physicians__providers
    description: "GRAIN: one row per provider (NPI is unique). SPINE_ENTITY: provider."
    columns:
      - name: npi
        data_tests:
          - unique
          - not_null
