{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_AAD84A1F6E') }}

),

renamed as (

    select
        "ID" as id,
        "PARCEL_ID" as parcel_id,
        "INACTIVE" as inactive,
        "NAME" as name,
        "RENTABLE" as rentable,
        "TYPE" as type,
        "PRIMARY_USER" as primary_user,
        "ADDRESS_NUMBER" as address_number,
        "STREET" as street,
        "ZIP" as zip,
        "IMAGE" as image,
        "NEIGHBORHOOD" as neighborhood,
        "COUNCIL_DISTRICT" as council_district,
        "WARD" as ward,
        "TRACT" as tract,
        "PUBLIC_WORKS_DIVISION" as public_works_division,
        "PLI_DIVISION" as pli_division,
        "POLICE_ZONE" as police_zone,
        "FIRE_ZONE" as fire_zone,
        "LATITUDE" as latitude,
        "LONGITUDE" as longitude,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/city-of-pittsburgh-facilities' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by id order by _loaded_at desc) = 1
