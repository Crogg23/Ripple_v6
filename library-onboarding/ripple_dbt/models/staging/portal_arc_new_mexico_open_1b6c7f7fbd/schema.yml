version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_NEW_MEXICO_OPEN_1B6C7F7FBD
        description: >
          Raw landing table for Electric Retail NM Clip. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_new_mexico_open_1b6c7f7fbd__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: id
        data_tests:
          - unique
          - not_null
