{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 17 of 42 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   fid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   sourcedate: integer
--   val_date: integer
--   summr_peak: float
--   wintr_peak: float
--   summer_cap: float
--   winter_cap: float
--   net_gen: integer
--   purchased: integer
--   net_ex: integer
--   retail_mwh: integer
--   wsale_mwh: integer
--   total_mwh: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   trans_mwh: integer
--   customers: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   year: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_NEW_MEXICO_OPEN_1B6C7F7FBD') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"FID"') }} as fid,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"TELEPHONE"') }} as telephone,
        {{ null_junk('"TYPE"') }} as type,
        {{ null_junk('"COUNTRY"') }} as country,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"NAICS_DESC"') }} as naics_desc,
        {{ null_junk('"SOURCE"') }} as source,
        {{ stg_int('"SOURCEDATE"') }} as sourcedate,
        {{ null_junk('"VAL_METHOD"') }} as val_method,
        {{ stg_int('"VAL_DATE"') }} as val_date,
        {{ null_junk('"WEBSITE"') }} as website,
        {{ null_junk('"REGULATED"') }} as regulated,
        {{ null_junk('"CNTRL_AREA"') }} as cntrl_area,
        {{ null_junk('"PLAN_AREA"') }} as plan_area,
        {{ null_junk('"HOLDING_CO"') }} as holding_co,
        {{ stg_float('"SUMMR_PEAK"') }} as summr_peak,
        {{ stg_float('"WINTR_PEAK"') }} as wintr_peak,
        {{ stg_float('"SUMMER_CAP"') }} as summer_cap,
        {{ stg_float('"WINTER_CAP"') }} as winter_cap,
        {{ stg_int('"NET_GEN"') }} as net_gen,
        {{ stg_int('"PURCHASED"') }} as purchased,
        {{ stg_int('"NET_EX"') }} as net_ex,
        {{ stg_int('"RETAIL_MWH"') }} as retail_mwh,
        {{ stg_int('"WSALE_MWH"') }} as wsale_mwh,
        {{ stg_id_text('"TOTAL_MWH"') }} as total_mwh,
        {{ stg_int('"TRANS_MWH"') }} as trans_mwh,
        {{ stg_id_text('"CUSTOMERS"') }} as customers,
        {{ stg_int('"YEAR"') }} as year,
        {{ null_junk('"SHAPE__ARE"') }} as shape__are,
        {{ null_junk('"SHAPE__LEN"') }} as shape__len,
        {{ null_junk('"SHAPE_LENG"') }} as shape_leng,
        {{ null_junk('"SHAPE_AREA"') }} as shape_area,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data-nmenv.opendata.arcgis.com/datasets/f812e7be7b524edcaffb86158eca1738_0' as _source_url

    from source

)

select * from renamed
