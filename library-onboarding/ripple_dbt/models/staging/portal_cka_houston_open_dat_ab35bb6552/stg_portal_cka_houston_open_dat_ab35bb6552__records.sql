{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+IN_DATE+DUE_DATE+OUT_DATE+ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ZIP, IN_DATE, DUE_DATE, OUT_DATE, ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_HOUSTON_OPEN_DAT_AB35BB6552') }}

),

renamed as (

    select
        "ID" as id,
        "TOT" as tot,
        "TYPE" as type,
        "BREED" as breed,
        "SUB_BREED" as sub_breed,
        "IN_TYPE" as in_type,
        "IN_SUB" as in_sub,
        "CONDITION" as condition,
        "IN_DATE" as in_date,
        "DUE_DATE" as due_date,
        "OUT_TYPE" as out_type,
        "OUT_SUB" as out_sub,
        "OUT_DATE" as out_date,
        "ZIP" as zip,
        "STREET_NUM" as street_num,
        "STREET_NAME" as street_name,
        "AC" as ac,
        "DAYS" as days,
        "ADDRESS" as address,
        "LAT" as lat,
        "LONG" as long,
        INGESTED_AT as _loaded_at,
        'https://data.houstontx.gov/dataset/barc-dog-intakes-for-calendar-years-2011-and-2012' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by zip, in_date, due_date, out_date, id order by _loaded_at desc) = 1
