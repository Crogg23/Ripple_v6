version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: XC_OWID_CPI
        description: >
          Raw landing table for OWID — Corruption Perceptions Index (TI). Grain not yet determined.

models:
  - name: stg_xc_owid_cpi__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
