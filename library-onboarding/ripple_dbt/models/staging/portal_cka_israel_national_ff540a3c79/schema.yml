version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ISRAEL_NATIONAL_FF540A3C79
        description: >
          Raw landing table for רשימת איזורי תעשייה. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_israel_national_ff540a3c79__places
    description: "GRAIN: one row per place (GEOM is unique). SPINE_ENTITY: place."
    columns:
      - name: geom
        data_tests:
          - unique
          - not_null
