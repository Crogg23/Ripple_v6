{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOM)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_FF540A3C79') }}

),

renamed as (

    select
        "ALINA_ID" as alina_id,
        "WEBSITE" as website,
        "ALINA_DESCRIPTION" as alina_description,
        "ALINA_DESCRIPTION_ENG" as alina_description_eng,
        "ALINA_NAME" as alina_name,
        "ALINA_ENG_NAME" as alina_eng_name,
        "DISTRICT" as district,
        "DISTRICT_EN" as district_en,
        "AVAILABLE_SPACE" as available_space,
        "OCCUPIED_SPACE" as occupied_space,
        "FUTURE_EMPLOYEES" as future_employees,
        "CURRENT_EMPLOYEES" as current_employees,
        "OPEN_TO_MARKET" as open_to_market,
        "GEOM" as geom,
        "CONTACT_NAME" as contact_name,
        "CONTACT_ROLE" as contact_role,
        "CONTACT_EMAIL" as contact_email,
        "CONTACT_PHONE" as contact_phone,
        "CONTACT_MOBILE" as contact_mobile,
        "SMALL_FACTORIES" as small_factories,
        "MEDIUM_FACTORIES" as medium_factories,
        "BIG_FACTORIES" as big_factories,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/industrial-zones-info' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by geom order by _loaded_at desc) = 1
