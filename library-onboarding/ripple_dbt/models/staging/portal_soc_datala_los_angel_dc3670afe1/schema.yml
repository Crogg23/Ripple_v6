version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_DATALA_LOS_ANGEL_DC3670AFE1
        description: >
          Raw landing table for Listing of Active Businesses. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_datala_los_angel_dc3670afe1__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+LOCATION_START_DATE+ZIP unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: mailing_zip_code
        data_tests:
          - not_null
      - name: location_start_date
        data_tests:
          - not_null
      - name: zip_code
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - mailing_zip_code
            - location_start_date
            - zip_code
