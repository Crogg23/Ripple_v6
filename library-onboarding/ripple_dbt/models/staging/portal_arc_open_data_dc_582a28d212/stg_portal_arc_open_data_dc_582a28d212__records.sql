{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GIS_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: GIS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 14 of 34 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   join_count: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   target_fid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   phone_number: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   raw_visit_counts: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   raw_visitor_counts: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits_by_day: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_5_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_6_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_7_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_8_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_9_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_10_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_11_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_OPEN_DATA_DC_582A28D212') }}
    qualify row_number() over (partition by "GIS_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_id_text('"JOIN_COUNT"') }} as join_count,
        {{ stg_id_text('"TARGET_FID"') }} as target_fid,
        {{ null_junk('"GIS_ID"') }} as gis_id,
        {{ null_junk('"WEB_URL"') }} as web_url,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"NBH_NAMES"') }} as nbh_names,
        {{ null_junk('"TYPE"') }} as type,
        {{ null_junk('"SAFEGRAPH_PLACE_ID"') }} as safegraph_place_id,
        {{ null_junk('"LOCATION_NAME"') }} as location_name,
        {{ null_junk('"BRANDS"') }} as brands,
        {{ null_junk('"TOP_CATEGORY"') }} as top_category,
        {{ null_junk('"SUB_CATEGORY"') }} as sub_category,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"STREET_ADDRESS"') }} as street_address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"REGION"') }} as region,
        {{ null_junk('"POSTAL_CODE"') }} as postal_code,
        {{ null_junk('"ISO_COUNTRY_CODE"') }} as iso_country_code,
        {{ stg_id_text('"PHONE_NUMBER"') }} as phone_number,
        {{ null_junk('"CATEGORY_TAGS"') }} as category_tags,
        {{ stg_id_text('"RAW_VISIT_COUNTS"') }} as raw_visit_counts,
        {{ stg_id_text('"RAW_VISITOR_COUNTS"') }} as raw_visitor_counts,
        {{ stg_id_text('"VISITS_BY_DAY"') }} as visits_by_day,
        {{ stg_id_text('"VISITS4_5_2020"') }} as visits4_5_2020,
        {{ stg_id_text('"VISITS4_6_2020"') }} as visits4_6_2020,
        {{ stg_id_text('"VISITS4_7_2020"') }} as visits4_7_2020,
        {{ stg_id_text('"VISITS4_8_2020"') }} as visits4_8_2020,
        {{ stg_id_text('"VISITS4_9_2020"') }} as visits4_9_2020,
        {{ stg_id_text('"VISITS4_10_2020"') }} as visits4_10_2020,
        {{ stg_id_text('"VISITS4_11_2020"') }} as visits4_11_2020,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://opendata.dc.gov/datasets/1435385b7bff49ffb67908a08f34540d_0' as _source_url

    from source

)

select * from renamed
