version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_OPEN_DATA_DC_582A28D212
        description: >
          Raw landing table for DC Footfall Business Data NBHD. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_open_data_dc_582a28d212__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (GIS_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: gis_id
        data_tests:
          - unique
          - not_null
