version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_1BA8209338
        description: >
          Raw landing table for Allegheny County Finished Property Assessment Appeals. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_1ba8209338__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PARCEL_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: parcel_id
        data_tests:
          - unique
          - not_null
