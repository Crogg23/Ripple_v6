{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per facility (TRI_FACILITY_ID unique)
-- SPINE_ENTITY: facility  (natural_key: TRI_FACILITY_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 28 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   frsid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   year: integer
--   census_tract: integer
--   census_block_group: integer
-- spine_entity 'facility' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_LA_COUNTY_OPEN_D_E549C7C921') }}
    qualify row_number() over (partition by "TRI_FACILITY_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ null_junk('"TRI_FACILITY_NAME"') }} as tri_facility_name,
        {{ null_junk('"TRI_FACILITY_ID"') }} as tri_facility_id,
        {{ stg_id_text('"FRSID"') }} as frsid,
        {{ stg_int('"YEAR"') }} as year,
        {{ stg_int('"CENSUS_TRACT"') }} as census_tract,
        {{ stg_int('"CENSUS_BLOCK_GROUP"') }} as census_block_group,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"STREET_ADDRESS"') }} as street_address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"COUNTY"') }} as county,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"CHEMICAL"') }} as chemical,
        {{ null_junk('"INDUSTRY_SECTOR"') }} as industry_sector,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"CAS_CODE"') }} as cas_code,
        {{ null_junk('"PARENT_COMPANY"') }} as parent_company,
        {{ null_junk('"YEAR_LAST_REPORTED_TO_TRI_PROGR"') }} as year_last_reported_to_tri_progr,
        {{ null_junk('"DCN"') }} as dcn,
        {{ null_junk('"RSEI_SCORE_FOR_CHROMIUM"') }} as rsei_score_for_chromium,
        {{ null_junk('"CANCER_RSEI_SCORE_FOR_CHROMIUM"') }} as cancer_rsei_score_for_chromium,
        {{ null_junk('"NON_CANCER_RSEI_SCORE_FOR_CHROM"') }} as non_cancer_rsei_score_for_chrom,
        {{ null_junk('"TOTAL_RSEI_SCORE_FOR_ALL_MEDIA"') }} as total_rsei_score_for_all_media,
        {{ null_junk('"TOTAL_CANCER_RSEI_SCORE_FOR_ALL"') }} as total_cancer_rsei_score_for_all,
        {{ null_junk('"TOTAL_NON_CANCER_RSEI_SCORE_FOR"') }} as total_non_cancer_rsei_score_for,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.lacounty.gov/datasets/736e6c56d58942898faf3e9fab9f90ff_0' as _source_url

    from source

)

select * from renamed
