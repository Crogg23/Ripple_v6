{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per facility (TRI_FACILITY_ID unique)
-- SPINE_ENTITY: facility  (natural_key: TRI_FACILITY_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'facility' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_LA_COUNTY_OPEN_D_E549C7C921') }}

),

renamed as (

    select
        "OBJECTID" as objectid,
        "TRI_FACILITY_NAME" as tri_facility_name,
        "TRI_FACILITY_ID" as tri_facility_id,
        "FRSID" as frsid,
        "YEAR" as year,
        "CENSUS_TRACT" as census_tract,
        "CENSUS_BLOCK_GROUP" as census_block_group,
        "ZIP_CODE" as zip_code,
        "STREET_ADDRESS" as street_address,
        "CITY" as city,
        "COUNTY" as county,
        "STATE" as state,
        "LATITUDE" as latitude,
        "LONGITUDE" as longitude,
        "CHEMICAL" as chemical,
        "INDUSTRY_SECTOR" as industry_sector,
        "NAICS_CODE" as naics_code,
        "CAS_CODE" as cas_code,
        "PARENT_COMPANY" as parent_company,
        "YEAR_LAST_REPORTED_TO_TRI_PROGR" as year_last_reported_to_tri_progr,
        "DCN" as dcn,
        "RSEI_SCORE_FOR_CHROMIUM" as rsei_score_for_chromium,
        "CANCER_RSEI_SCORE_FOR_CHROMIUM" as cancer_rsei_score_for_chromium,
        "NON_CANCER_RSEI_SCORE_FOR_CHROM" as non_cancer_rsei_score_for_chrom,
        "TOTAL_RSEI_SCORE_FOR_ALL_MEDIA" as total_rsei_score_for_all_media,
        "TOTAL_CANCER_RSEI_SCORE_FOR_ALL" as total_cancer_rsei_score_for_all,
        "TOTAL_NON_CANCER_RSEI_SCORE_FOR" as total_non_cancer_rsei_score_for,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.lacounty.gov/datasets/736e6c56d58942898faf3e9fab9f90ff_0' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by tri_facility_id order by _loaded_at desc) = 1
