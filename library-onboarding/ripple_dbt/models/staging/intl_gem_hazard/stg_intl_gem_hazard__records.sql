{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (HAZARD_MODEL_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: HAZARD_MODEL_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 9 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'INTL_GEM_HAZARD') }}
    qualify row_number() over (partition by "HAZARD_MODEL_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"MODEL_VERSION"') }} as model_version,
        {{ null_junk('"COUNTRY"') }} as country,
        {{ null_junk('"FIPS"') }} as fips,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"PGA_10PCT_50YR"') }} as pga_10pct_50yr,
        {{ null_junk('"VS30"') }} as vs30,
        {{ null_junk('"HAZARD_MODEL_ID"') }} as hazard_model_id,
        {{ null_junk('"DOI"') }} as doi,
        _INGESTED_AT as _loaded_at,
        'https://hazard.openquake.org/gem/' as _source_url

    from source

)

select * from renamed
