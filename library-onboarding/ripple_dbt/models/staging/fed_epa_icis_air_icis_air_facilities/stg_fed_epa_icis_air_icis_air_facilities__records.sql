{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PGM_SYS_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PGM_SYS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 19 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_ICIS_AIR_ICIS_AIR_FACILITIES') }}
    qualify row_number() over (partition by "PGM_SYS_ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"PGM_SYS_ID"') }} as pgm_sys_id,
        {{ null_junk('"REGISTRY_ID"') }} as registry_id,
        {{ null_junk('"FACILITY_NAME"') }} as facility_name,
        {{ null_junk('"STREET_ADDRESS"') }} as street_address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"COUNTY_NAME"') }} as county_name,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP_CODE"') }} as zip_code,
        {{ null_junk('"EPA_REGION"') }} as epa_region,
        {{ null_junk('"SIC_CODES"') }} as sic_codes,
        {{ null_junk('"NAICS_CODES"') }} as naics_codes,
        {{ null_junk('"FACILITY_TYPE_CODE"') }} as facility_type_code,
        {{ null_junk('"AIR_POLLUTANT_CLASS_CODE"') }} as air_pollutant_class_code,
        {{ null_junk('"AIR_POLLUTANT_CLASS_DESC"') }} as air_pollutant_class_desc,
        {{ null_junk('"AIR_OPERATING_STATUS_CODE"') }} as air_operating_status_code,
        {{ null_junk('"AIR_OPERATING_STATUS_DESC"') }} as air_operating_status_desc,
        {{ null_junk('"CURRENT_HPV"') }} as current_hpv,
        {{ null_junk('"LOCAL_CONTROL_REGION_CODE"') }} as local_control_region_code,
        {{ null_junk('"LOCAL_CONTROL_REGION_NAME"') }} as local_control_region_name,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
