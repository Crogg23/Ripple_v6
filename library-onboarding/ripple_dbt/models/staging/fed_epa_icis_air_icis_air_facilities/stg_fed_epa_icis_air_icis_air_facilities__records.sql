{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PGM_SYS_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PGM_SYS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_EPA_ICIS_AIR_ICIS_AIR_FACILITIES') }}

),

renamed as (

    select
        "PGM_SYS_ID" as pgm_sys_id,
        "REGISTRY_ID" as registry_id,
        "FACILITY_NAME" as facility_name,
        "STREET_ADDRESS" as street_address,
        "CITY" as city,
        "COUNTY_NAME" as county_name,
        "STATE" as state,
        "ZIP_CODE" as zip_code,
        "EPA_REGION" as epa_region,
        "SIC_CODES" as sic_codes,
        "NAICS_CODES" as naics_codes,
        "FACILITY_TYPE_CODE" as facility_type_code,
        "AIR_POLLUTANT_CLASS_CODE" as air_pollutant_class_code,
        "AIR_POLLUTANT_CLASS_DESC" as air_pollutant_class_desc,
        "AIR_OPERATING_STATUS_CODE" as air_operating_status_code,
        "AIR_OPERATING_STATUS_DESC" as air_operating_status_desc,
        "CURRENT_HPV" as current_hpv,
        "LOCAL_CONTROL_REGION_CODE" as local_control_region_code,
        "LOCAL_CONTROL_REGION_NAME" as local_control_region_name,
        _INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by pgm_sys_id order by _loaded_at desc) = 1
