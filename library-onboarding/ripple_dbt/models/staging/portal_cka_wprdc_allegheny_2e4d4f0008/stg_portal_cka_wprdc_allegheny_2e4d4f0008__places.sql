{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: GEOMETRY)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 15 of 24 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   crest: integer
--   dams_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   dam_height: float
--   drain_area: float
--   east: float
--   easting: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   fid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   north: float
--   northing: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   perimeter: integer
--   storage_no: float
--   storage_to: float
--   surface_no: float
--   surface_to: float
--   zone: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_2E4D4F0008') }}
    qualify row_number() over (partition by "GEOMETRY" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"CLASS"') }} as class,
        {{ stg_int('"CREST"') }} as crest,
        {{ stg_id_text('"DAMS_ID"') }} as dams_id,
        {{ stg_float('"DAM_HEIGHT"') }} as dam_height,
        {{ null_junk('"DAM_NAME"') }} as dam_name,
        {{ null_junk('"DAM_NO"') }} as dam_no,
        {{ stg_float('"DRAIN_AREA"') }} as drain_area,
        {{ stg_float('"EAST"') }} as east,
        {{ stg_id_text('"EASTING"') }} as easting,
        {{ stg_id_text('"FID"') }} as fid,
        {{ stg_float('"NORTH"') }} as north,
        {{ stg_id_text('"NORTHING"') }} as northing,
        {{ stg_int('"PERIMETER"') }} as perimeter,
        {{ null_junk('"PERMITTEE"') }} as permittee,
        {{ null_junk('"PURPOSE"') }} as purpose,
        {{ null_junk('"QUAD_NAME"') }} as quad_name,
        {{ stg_float('"STORAGE_NO"') }} as storage_no,
        {{ stg_float('"STORAGE_TO"') }} as storage_to,
        {{ null_junk('"STREAM"') }} as stream,
        {{ stg_float('"SURFACE_NO"') }} as surface_no,
        {{ stg_float('"SURFACE_TO"') }} as surface_to,
        {{ null_junk('"TYPE"') }} as type,
        {{ stg_int('"ZONE"') }} as zone,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-dam-locations2' as _source_url

    from source

)

select * from renamed
