{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+INSTITUTE_CODE unique)
-- SPINE_ENTITY: place  (natural_key: ZIP_CODE, INSTITUTE_CODE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ISRAEL_NATIONAL_C05E5881A0') }}

),

renamed as (

    select
        "CITY" as city,
        "ADDRESS" as address,
        "INSTITUTE_CODE" as institute_code,
        "INSTITUTE_NAME" as institute_name,
        "CITYCODE" as citycode,
        "STREETCODE" as streetcode,
        "STREET" as street,
        "BUILDING_NUM" as building_num,
        "ZIP_CODE" as zip_code,
        "PHONE" as phone,
        "PHONE2" as phone2,
        "FAX" as fax,
        "EMAIL" as email,
        "MANAGER" as manager,
        "NOTES" as notes,
        "OWNERSHIPCODE" as ownershipcode,
        "OWNERSHIPDESC" as ownershipdesc,
        INGESTED_AT as _loaded_at,
        'https://data.gov.il/dataset/drugs-rehab' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by zip_code, institute_code order by _loaded_at desc) = 1
