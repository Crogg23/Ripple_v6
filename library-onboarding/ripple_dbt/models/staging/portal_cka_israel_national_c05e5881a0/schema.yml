version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_ISRAEL_NATIONAL_C05E5881A0
        description: >
          Raw landing table for אשפוזיות לגמילה מסמים ומאלכוהול. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_israel_national_c05e5881a0__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+INSTITUTE_CODE unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: zip_code
        data_tests:
          - not_null
      - name: institute_code
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - zip_code
            - institute_code
