version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_OPEN_DATA_MINNEA_5AC94C44DA
        description: >
          Raw landing table for Police Incidents 2011. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_open_data_minnea_5ac94c44da__facilities
    description: "GRAIN: one row per facility (CCN is unique). SPINE_ENTITY: facility."
    columns:
      - name: ccn
        data_tests:
          - unique
          - not_null
