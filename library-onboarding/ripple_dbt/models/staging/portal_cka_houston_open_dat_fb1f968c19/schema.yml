version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_HOUSTON_OPEN_DAT_FB1F968C19
        description: >
          Raw landing table for City of Houston Fund Centers. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_houston_open_dat_fb1f968c19__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FUND_CENTER_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: fund_center_id
        data_tests:
          - unique
          - not_null
