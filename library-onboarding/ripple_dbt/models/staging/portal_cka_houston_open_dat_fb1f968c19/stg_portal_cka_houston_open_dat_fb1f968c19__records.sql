{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (FUND_CENTER_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: FUND_CENTER_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 2 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_HOUSTON_OPEN_DAT_FB1F968C19') }}
    qualify row_number() over (partition by "FUND_CENTER_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"FUND_CENTER_ID"') }} as fund_center_id,
        {{ null_junk('"FUND_CENTER_NAME"') }} as fund_center_name,
        INGESTED_AT as _loaded_at,
        'https://data.houstontx.gov/dataset/city-of-houston-fund-centers' as _source_url

    from source

)

select * from renamed
