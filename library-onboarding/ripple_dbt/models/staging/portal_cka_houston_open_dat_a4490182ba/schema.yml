version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_HOUSTON_OPEN_DAT_A4490182BA
        description: >
          Raw landing table for BARC Animal Service Requests. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_houston_open_dat_a4490182ba__places
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per place (ZIP+SR_CREATE_DATE+DUE_DATE+DATE_CLOSED+TAX_ID unique). SPINE_ENTITY: place."
    columns:
      - name: zipcode
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: sr_create_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: due_date
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: date_closed
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: tax_id
        data_tests:
          - not_null:
              config:
                severity: warn
