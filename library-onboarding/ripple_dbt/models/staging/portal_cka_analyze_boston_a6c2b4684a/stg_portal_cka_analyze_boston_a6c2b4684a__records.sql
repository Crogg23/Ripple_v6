{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TOWNS_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TOWNS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_A6C2B4684A') }}

),

renamed as (

    select
        "OBJECTID_1" as objectid_1,
        "TOWNS_ID" as towns_id,
        "TOWN_ID" as town_id,
        "TOWN" as town,
        "FIPS_STCO" as fips_stco,
        "CCD_MCD" as ccd_mcd,
        "FIPS_PLACE" as fips_place,
        "POP1980" as pop1980,
        "POP1990" as pop1990,
        "POP2000" as pop2000,
        "POPCH80_90" as popch80_90,
        "POPCH90_00" as popch90_00,
        "FOURCOLOR" as fourcolor,
        "TYPE" as type,
        "ISLAND" as island,
        "FIPS_MCD" as fips_mcd,
        "FIPS_COUNT" as fips_count,
        "TEST" as test,
        "SHAPE_LENGTH" as shape_length,
        "SHAPE_AREA" as shape_area,
        "SHAPE_WKT" as shape_wkt,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/city-of-boston-outline-boundary-water-included' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by towns_id order by _loaded_at desc) = 1
