{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (TOWNS_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: TOWNS_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 21 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_A6C2B4684A') }}
    qualify row_number() over (partition by "TOWNS_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"OBJECTID_1"') }} as objectid_1,
        {{ null_junk('"TOWNS_ID"') }} as towns_id,
        {{ null_junk('"TOWN_ID"') }} as town_id,
        {{ null_junk('"TOWN"') }} as town,
        {{ null_junk('"FIPS_STCO"') }} as fips_stco,
        {{ null_junk('"CCD_MCD"') }} as ccd_mcd,
        {{ null_junk('"FIPS_PLACE"') }} as fips_place,
        {{ null_junk('"POP1980"') }} as pop1980,
        {{ null_junk('"POP1990"') }} as pop1990,
        {{ null_junk('"POP2000"') }} as pop2000,
        {{ null_junk('"POPCH80_90"') }} as popch80_90,
        {{ null_junk('"POPCH90_00"') }} as popch90_00,
        {{ null_junk('"FOURCOLOR"') }} as fourcolor,
        {{ null_junk('"TYPE"') }} as type,
        {{ null_junk('"ISLAND"') }} as island,
        {{ null_junk('"FIPS_MCD"') }} as fips_mcd,
        {{ null_junk('"FIPS_COUNT"') }} as fips_count,
        {{ null_junk('"TEST"') }} as test,
        {{ null_junk('"SHAPE_LENGTH"') }} as shape_length,
        {{ null_junk('"SHAPE_AREA"') }} as shape_area,
        {{ null_junk('"SHAPE_WKT"') }} as shape_wkt,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/city-of-boston-outline-boundary-water-included' as _source_url

    from source

)

select * from renamed
