version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_FORT_WORTH_OPEN_9F0879147A
        description: >
          Raw landing table for Building Footprints. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_fort_worth_open_9f0879147a__places
    description: "GRAIN: one row per place (GEOM is unique). SPINE_ENTITY: place."
    columns:
      - name: geometry
        data_tests:
          - unique
          - not_null
