{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_SAM_EXCLUSIONS') }}

),

renamed as (

    select
        UEI as uei,
        CAGE as cage,
        NPI as npi,
        ENTITY_NAME as entity_name,
        FIRST_NAME as first_name,
        MIDDLE_NAME as middle_name,
        LAST_NAME as last_name,
        PREFIX as prefix,
        SUFFIX as suffix,
        DNB_OPEN_DATA as dnb_open_data,
        CLASSIFICATION as classification,
        EXCLUSION_TYPE as exclusion_type,
        EXCLUSION_PROGRAM as exclusion_program,
        EXCLUDING_AGENCY as excluding_agency,
        ACTIVATION_DATE as activation_date,
        TERMINATION_DATE as termination_date,
        RECORD_STATUS as record_status,
        CITY as city,
        STATE as state,
        ZIP as zip,
        COUNTRY as country,
        _INGESTED_AT as _loaded_at,
        'https://sam.gov/content/exclusions' as _source_url

    from source

)

select * from renamed
