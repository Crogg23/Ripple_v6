version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_SAM_EXCLUSIONS
        description: >
          Raw landing table for SAM.gov Exclusions (Federal Debarment List). Grain not yet determined.

models:
  - name: stg_fed_sam_exclusions__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
