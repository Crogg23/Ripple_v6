{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (DATASET_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: DATASET_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'INTL_CL_DATOSGOB') }}

),

renamed as (

    select
        "DATASET_ID" as dataset_id,
        "TITLE" as title,
        "NAME" as name,
        "PUBLISHER_INSTITUTION" as publisher_institution,
        "DESCRIPTION" as description,
        "TAGS" as tags,
        "CATEGORY" as category,
        "LICENSE_ID" as license_id,
        "NUM_RESOURCES" as num_resources,
        "RESOURCE_URLS" as resource_urls,
        "RESOURCE_FORMATS" as resource_formats,
        "METADATA_CREATED" as metadata_created,
        "METADATA_MODIFIED" as metadata_modified,
        "STATE" as state,
        "URL" as url,
        _INGESTED_AT as _loaded_at,
        'https://datos.gob.cl/' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by dataset_id order by _loaded_at desc) = 1
