version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_UTAH_OPEN_DATA_P_589CC47A29
        description: >
          Raw landing table for DWQAssessedWaters_data. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_utah_open_data_p_589cc47a29__facilities
    description: "GRAIN: one row per facility (NPDES_ID is unique). SPINE_ENTITY: facility."
    columns:
      - name: npdes_id
        data_tests:
          - unique
          - not_null
