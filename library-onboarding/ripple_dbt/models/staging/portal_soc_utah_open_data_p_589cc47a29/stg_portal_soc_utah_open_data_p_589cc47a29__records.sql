{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (NPDES_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: NPDES_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_UTAH_OPEN_DATA_P_589CC47A29') }}

),

renamed as (

    select
        "THE_GEOM" as the_geom,
        "OBJECTID" as objectid,
        "REC" as rec,
        "NPDES_ID" as npdes_id,
        "PERMITTEE" as permittee,
        "PERMITTYPE" as permittype,
        "PERMITNAME" as permitname,
        "CONTACT" as contact,
        "PHONE" as phone,
        "LOC_ADDRES" as loc_addres,
        "CITY" as city,
        "STATE" as state,
        "ZIPCODE" as zipcode,
        "STATE_ZIP" as state_zip,
        "COUNTY" as county,
        "REC_WATERS" as rec_waters,
        "ISSUE_DATE" as issue_date,
        "EFFEC_DATE" as effec_date,
        "EXPIR_DATE" as expir_date,
        "LAT_DD" as lat_dd,
        "LONG_DD" as long_dd,
        "PERMIT_GRP" as permit_grp,
        "NAD83UTM_Y" as nad83utm_y,
        "NAD83UTM_X" as nad83utm_x,
        "MAPLABEL" as maplabel,
        "POINT_X" as point_x,
        "POINT_Y" as point_y,
        "ID" as id,
        "NAME" as name,
        "ADDRESS" as address,
        "TYPE" as type,
        "ENVIROAPPLABEL" as enviroapplabel,
        "ENVIROAPPSYMBOL" as enviroappsymbol,
        "SIC_CODE" as sic_code,
        "SUP_ADDRES" as sup_addres,
        "DESCRIPT" as descript,
        INGESTED_AT as _loaded_at,
        'https://opendata.utah.gov/d/sxmt-bpxq' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by npdes_id order by _loaded_at desc) = 1
