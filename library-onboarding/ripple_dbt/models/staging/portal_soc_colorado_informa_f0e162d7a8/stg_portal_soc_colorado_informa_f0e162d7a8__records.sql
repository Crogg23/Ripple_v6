{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 10 of 14 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   taxyr: integer
--   totalrevenueamt: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   grantsandsimilaramountspaidamt: integer
--   benefitspaidtoorformembersamt: integer
--   salariesothercompemplbnftamt: integer
--   feesandotherpymttoindcntrctamt: integer
--   occupancyrentutltsandmaintamt: integer
--   printingpublicationspostageamt: integer
--   otherexpensestotalamt: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   totalexpensesamt: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_COLORADO_INFORMA_F0E162D7A8') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ stg_int('"TAXYR"') }} as taxyr,
        {{ null_junk('"BUSINESSNAME1"') }} as businessname1,
        {{ null_junk('"EIN"') }} as ein,
        {{ stg_id_text('"TOTALREVENUEAMT"') }} as totalrevenueamt,
        {{ stg_int('"GRANTSANDSIMILARAMOUNTSPAIDAMT"') }} as grantsandsimilaramountspaidamt,
        {{ stg_int('"BENEFITSPAIDTOORFORMEMBERSAMT"') }} as benefitspaidtoorformembersamt,
        {{ stg_int('"SALARIESOTHERCOMPEMPLBNFTAMT"') }} as salariesothercompemplbnftamt,
        {{ stg_int('"FEESANDOTHERPYMTTOINDCNTRCTAMT"') }} as feesandotherpymttoindcntrctamt,
        {{ stg_int('"OCCUPANCYRENTUTLTSANDMAINTAMT"') }} as occupancyrentutltsandmaintamt,
        {{ stg_int('"PRINTINGPUBLICATIONSPOSTAGEAMT"') }} as printingpublicationspostageamt,
        {{ stg_id_text('"OTHEREXPENSESTOTALAMT"') }} as otherexpensestotalamt,
        {{ stg_id_text('"TOTALEXPENSESAMT"') }} as totalexpensesamt,
        {{ null_junk('"BUSINESSNAME2"') }} as businessname2,
        INGESTED_AT as _loaded_at,
        'https://data.colorado.gov/d/xb4j-dshc' as _source_url

    from source

)

select * from renamed
