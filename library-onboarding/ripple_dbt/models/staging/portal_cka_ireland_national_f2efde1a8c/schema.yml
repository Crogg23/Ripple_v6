version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_IRELAND_NATIONAL_F2EFDE1A8C
        description: >
          Raw landing table for Research Ireland Grant Commitments. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_ireland_national_f2efde1a8c__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PROJECT_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: project_id
        data_tests:
          - unique
          - not_null
