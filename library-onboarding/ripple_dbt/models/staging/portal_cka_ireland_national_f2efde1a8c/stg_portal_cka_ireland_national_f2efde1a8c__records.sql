{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (PROJECT_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: PROJECT_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 2 of 11 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   actual_start_date: timestamp
--   final_end_date: timestamp
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_IRELAND_NATIONAL_F2EFDE1A8C') }}
    qualify row_number() over (partition by "PROJECT_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"PROJECT_ID"') }} as project_id,
        {{ null_junk('"PROGRAMME_NAME"') }} as programme_name,
        {{ null_junk('"LEAD_APPLICANT"') }} as lead_applicant,
        {{ null_junk('"ORCID_ID"') }} as orcid_id,
        {{ null_junk('"RESEARCH_BODY"') }} as research_body,
        {{ null_junk('"FUNDER_NAME"') }} as funder_name,
        {{ null_junk('"FUNDER_ROR_ID"') }} as funder_ror_id,
        {{ null_junk('"PROJECT_TITLE"') }} as project_title,
        {{ stg_ts('"ACTUAL_START_DATE"') }} as actual_start_date,
        {{ stg_ts('"FINAL_END_DATE"') }} as final_end_date,
        {{ null_junk('"AWARD_AMOUNT"') }} as award_amount,
        INGESTED_AT as _loaded_at,
        'https://data.gov.ie/dataset/research-ireland-grant-commitments' as _source_url

    from source

)

select * from renamed
