{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'INTL_CH_OPENDATASWISS') }}

),

renamed as (

    select
        "ID" as id,
        "NAME" as name,
        "TITLE" as title,
        "DESCRIPTION" as description,
        "ISSUED" as issued,
        "METADATA_CREATED" as metadata_created,
        "METADATA_MODIFIED" as metadata_modified,
        "PUBLISHER" as publisher,
        "C_ORGANIZATION" as c_organization,
        "OWNER_ORG" as owner_org,
        "MAINTAINER" as maintainer,
        "MAINTAINER_EMAIL" as maintainer_email,
        "ACCRUAL_PERIODICITY" as accrual_periodicity,
        "KEYWORDS" as keywords,
        "GROUPS" as groups,
        "NUM_TAGS" as num_tags,
        "STATE" as state,
        "PRIVATE" as private,
        "TYPE" as type,
        "RESOURCES" as resources,
        "RELATIONS" as relations,
        "CONTACT_POINTS" as contact_points,
        "TEMPORALS" as temporals,
        "LANGUAGE" as language,
        _INGESTED_AT as _loaded_at,
        'https://handbook.opendata.swiss/de/content/nutzen/api-nutzen.html' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by id order by _loaded_at desc) = 1
