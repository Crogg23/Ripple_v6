{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 5 of 24 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   issued: timestamp
--   metadata_created: timestamp
--   metadata_modified: timestamp
--   num_tags: integer
--   private: boolean
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'INTL_CH_OPENDATASWISS') }}
    qualify row_number() over (partition by "ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"TITLE"') }} as title,
        {{ null_junk('"DESCRIPTION"') }} as description,
        {{ stg_ts('"ISSUED"') }} as issued,
        {{ stg_ts('"METADATA_CREATED"') }} as metadata_created,
        {{ stg_ts('"METADATA_MODIFIED"') }} as metadata_modified,
        {{ null_junk('"PUBLISHER"') }} as publisher,
        {{ null_junk('"C_ORGANIZATION"') }} as c_organization,
        {{ null_junk('"OWNER_ORG"') }} as owner_org,
        {{ null_junk('"MAINTAINER"') }} as maintainer,
        {{ null_junk('"MAINTAINER_EMAIL"') }} as maintainer_email,
        {{ null_junk('"ACCRUAL_PERIODICITY"') }} as accrual_periodicity,
        {{ null_junk('"KEYWORDS"') }} as keywords,
        {{ null_junk('"GROUPS"') }} as groups,
        {{ stg_int('"NUM_TAGS"') }} as num_tags,
        {{ null_junk('"STATE"') }} as state,
        {{ stg_bool('"PRIVATE"') }} as private,
        {{ null_junk('"TYPE"') }} as type,
        {{ null_junk('"RESOURCES"') }} as resources,
        {{ null_junk('"RELATIONS"') }} as relations,
        {{ null_junk('"CONTACT_POINTS"') }} as contact_points,
        {{ null_junk('"TEMPORALS"') }} as temporals,
        {{ null_junk('"LANGUAGE"') }} as language,
        _INGESTED_AT as _loaded_at,
        'https://handbook.opendata.swiss/de/content/nutzen/api-nutzen.html' as _source_url

    from source

)

select * from renamed
