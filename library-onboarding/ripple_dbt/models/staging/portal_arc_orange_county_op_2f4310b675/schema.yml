version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_ORANGE_COUNTY_OP_2F4310B675
        description: >
          Raw landing table for SAR Food Commercial Facilities. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_orange_county_op_2f4310b675__facilities
    description: "GRAIN: one row per facility (FACILITY_ID unique). SPINE_ENTITY: facility."
    columns:
      - name: facility_id
        data_tests:
          - unique
          - not_null
