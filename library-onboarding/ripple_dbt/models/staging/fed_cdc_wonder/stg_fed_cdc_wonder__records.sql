{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (DATABASE_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: DATABASE_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_CDC_WONDER') }}

),

renamed as (

    select
        "DATABASE_ID" as database_id,
        "GROUP_BY_1" as group_by_1,
        "GROUP_BY_1_CODE" as group_by_1_code,
        "GROUP_BY_2" as group_by_2,
        "GROUP_BY_2_CODE" as group_by_2_code,
        "DEATHS" as deaths,
        "POPULATION" as population,
        "CRUDE_RATE" as crude_rate,
        "AGE_ADJUSTED_RATE" as age_adjusted_rate,
        "AGE_ADJUSTED_RATE_SE" as age_adjusted_rate_se,
        "AGE_ADJUSTED_RATE_CI_LOWER" as age_adjusted_rate_ci_lower,
        "AGE_ADJUSTED_RATE_CI_UPPER" as age_adjusted_rate_ci_upper,
        "ROW_TYPE" as row_type,
        "CAUSE_OF_DEATH_FILTER" as cause_of_death_filter,
        "QUERY_TIMESTAMP" as query_timestamp,
        "RAW_XML_RESPONSE" as raw_xml_response,
        _INGESTED_AT as _loaded_at,
        'https://wonder.cdc.gov/wonder/help/wonder-api.html' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by database_id order by _loaded_at desc) = 1
