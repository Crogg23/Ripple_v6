version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_CDC_WONDER
        description: >
          Raw landing table for CDC WONDER. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_fed_cdc_wonder__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (DATABASE_ID is unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: database_id
        data_tests:
          - unique
          - not_null
