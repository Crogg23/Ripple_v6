{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_WKT)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_E1262D25C2') }}

),

renamed as (

    select
        "OBJECTID_1" as objectid_1,
        "BFD_ID" as bfd_id,
        "MAP_ID" as map_id,
        "MAPCODE" as mapcode,
        "LOCCODE" as loccode,
        "LOCDEPT" as locdept,
        "LOCNAME" as locname,
        "LOCCONTACT" as loccontact,
        "LOCPHONE" as locphone,
        "LOCSTNO" as locstno,
        "LOCADDR" as locaddr,
        "LOCOWNER" as locowner,
        "LOCWARD" as locward,
        "LOCPARCL" as locparcl,
        "LOCPRECT" as locprect,
        "LOCPLAN" as locplan,
        "STUDY" as study,
        "ABOVE" as above,
        "ABOVE_DESC" as above_desc,
        "SOURCE" as source,
        "GEOADDRESS" as geoaddress,
        "PD" as pd,
        "CT90" as ct90,
        "XCOORD" as xcoord,
        "YCOORD" as ycoord,
        "SHAPE_WKT" as shape_wkt,
        "POINT_X" as point_x,
        "POINT_Y" as point_y,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/fire-departments' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by shape_wkt order by _loaded_at desc) = 1
