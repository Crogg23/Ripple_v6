{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (GEOM is unique)
-- SPINE_ENTITY: place  (natural_key: SHAPE_WKT)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 8 of 28 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   bfd_id: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   mapcode: integer
--   locstno: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   above: integer
--   xcoord: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   ycoord: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   point_x: float
--   point_y: float
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_ANALYZE_BOSTON_E1262D25C2') }}
    qualify row_number() over (partition by "SHAPE_WKT" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"OBJECTID_1"') }} as objectid_1,
        {{ stg_id_text('"BFD_ID"') }} as bfd_id,
        {{ null_junk('"MAP_ID"') }} as map_id,
        {{ stg_int('"MAPCODE"') }} as mapcode,
        {{ null_junk('"LOCCODE"') }} as loccode,
        {{ null_junk('"LOCDEPT"') }} as locdept,
        {{ null_junk('"LOCNAME"') }} as locname,
        {{ null_junk('"LOCCONTACT"') }} as loccontact,
        {{ null_junk('"LOCPHONE"') }} as locphone,
        {{ stg_id_text('"LOCSTNO"') }} as locstno,
        {{ null_junk('"LOCADDR"') }} as locaddr,
        {{ null_junk('"LOCOWNER"') }} as locowner,
        {{ null_junk('"LOCWARD"') }} as locward,
        {{ null_junk('"LOCPARCL"') }} as locparcl,
        {{ null_junk('"LOCPRECT"') }} as locprect,
        {{ null_junk('"LOCPLAN"') }} as locplan,
        {{ null_junk('"STUDY"') }} as study,
        {{ stg_int('"ABOVE"') }} as above,
        {{ null_junk('"ABOVE_DESC"') }} as above_desc,
        {{ null_junk('"SOURCE"') }} as source,
        {{ null_junk('"GEOADDRESS"') }} as geoaddress,
        {{ null_junk('"PD"') }} as pd,
        {{ null_junk('"CT90"') }} as ct90,
        {{ stg_id_text('"XCOORD"') }} as xcoord,
        {{ stg_id_text('"YCOORD"') }} as ycoord,
        {{ null_junk('"SHAPE_WKT"') }} as shape_wkt,
        {{ stg_float('"POINT_X"') }} as point_x,
        {{ stg_float('"POINT_Y"') }} as point_y,
        INGESTED_AT as _loaded_at,
        'https://data.boston.gov/dataset/fire-departments' as _source_url

    from source

)

select * from renamed
