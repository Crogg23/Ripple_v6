version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_ARC_LA_COUNTY_OPEN_D_6DF63A4983
        description: >
          Raw landing table for ahn. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_arc_la_county_open_d_6df63a4983__providers
    description: "GRAIN: one row per provider (NPI is unique). SPINE_ENTITY: provider."
    columns:
      - name: npi
        data_tests:
          - unique
          - not_null
