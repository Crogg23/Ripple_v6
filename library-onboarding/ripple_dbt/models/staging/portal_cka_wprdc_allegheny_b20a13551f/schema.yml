version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_WPRDC_ALLEGHENY_B20A13551F
        description: >
          Raw landing table for Allegheny County WIC Vendor Locations. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_wprdc_allegheny_b20a13551f__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (REF_ID unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: ref_id
        data_tests:
          - unique
          - not_null
