{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (REF_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: REF_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WPRDC_ALLEGHENY_B20A13551F') }}

),

renamed as (

    select
        "ARC_CITY" as arc_city,
        "ARC_STATE" as arc_state,
        "ARC_STREET" as arc_street,
        "ARC_ZIP" as arc_zip,
        "ADDR_TYPE" as addr_type,
        "ADDRESS" as address,
        "LOC_NAME" as loc_name,
        "MATCH_ADDR" as match_addr,
        "MATCH_TYPE" as match_type,
        "MUNICIPA_1" as municipa_1,
        "MUNICIPALI" as municipali,
        "NAME" as name,
        "OBJECTID" as objectid,
        "REF_ID" as ref_id,
        "SCORE" as score,
        "SIDE" as side,
        "STATUS" as status,
        "USER_FLD" as user_fld,
        "X" as x,
        "Y" as y,
        "ZIPCODES" as zipcodes,
        "GEOMETRY" as geometry,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-wic-vendor-locations' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by ref_id order by _loaded_at desc) = 1
