{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ANC_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ANC_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 16 of 42 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   join_count: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   target_fid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   primary_number: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   phone_number: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   data_version: date, YYYYMMDD
--   raw_visit_counts: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   raw_visitor_counts: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits_by_day: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_12_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_13_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_14_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_15_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_16_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_17_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   visits4_18_2020: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_ARC_OPEN_DATA_DC_F37F5DDC3D') }}
    qualify row_number() over (partition by "ANC_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_id_text('"JOIN_COUNT"') }} as join_count,
        {{ stg_id_text('"TARGET_FID"') }} as target_fid,
        {{ null_junk('"GIS_ID"') }} as gis_id,
        {{ null_junk('"ANC_ID"') }} as anc_id,
        {{ null_junk('"WEB_URL"') }} as web_url,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"SAFEGRAPH_PLACE_ID"') }} as safegraph_place_id,
        {{ null_junk('"PARENT_SAFEGRAPH_PLACE_ID"') }} as parent_safegraph_place_id,
        {{ null_junk('"LOCATION_NAME"') }} as location_name,
        {{ null_junk('"BRANDS"') }} as brands,
        {{ null_junk('"TOP_CATEGORY"') }} as top_category,
        {{ null_junk('"SUB_CATEGORY"') }} as sub_category,
        {{ null_junk('"NAICS_CODE"') }} as naics_code,
        {{ null_junk('"STREET_ADDRESS"') }} as street_address,
        {{ stg_id_text('"PRIMARY_NUMBER"') }} as primary_number,
        {{ null_junk('"STREET_PREDIRECTION"') }} as street_predirection,
        {{ null_junk('"STREET_NAME"') }} as street_name,
        {{ null_junk('"STREET_SUFFIX"') }} as street_suffix,
        {{ null_junk('"STREET_POSTDIRECTION"') }} as street_postdirection,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"REGION"') }} as region,
        {{ null_junk('"POSTAL_CODE"') }} as postal_code,
        {{ null_junk('"ISO_COUNTRY_CODE"') }} as iso_country_code,
        {{ stg_id_text('"PHONE_NUMBER"') }} as phone_number,
        {{ null_junk('"CATEGORY_TAGS"') }} as category_tags,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ stg_date('"DATA_VERSION"', 'YYYYMMDD') }} as data_version,
        {{ stg_id_text('"RAW_VISIT_COUNTS"') }} as raw_visit_counts,
        {{ stg_id_text('"RAW_VISITOR_COUNTS"') }} as raw_visitor_counts,
        {{ stg_id_text('"VISITS_BY_DAY"') }} as visits_by_day,
        {{ stg_id_text('"VISITS4_12_2020"') }} as visits4_12_2020,
        {{ stg_id_text('"VISITS4_13_2020"') }} as visits4_13_2020,
        {{ stg_id_text('"VISITS4_14_2020"') }} as visits4_14_2020,
        {{ stg_id_text('"VISITS4_15_2020"') }} as visits4_15_2020,
        {{ stg_id_text('"VISITS4_16_2020"') }} as visits4_16_2020,
        {{ stg_id_text('"VISITS4_17_2020"') }} as visits4_17_2020,
        {{ stg_id_text('"VISITS4_18_2020"') }} as visits4_18_2020,
        {{ null_junk('"SHAPE__AREA"') }} as shape__area,
        {{ null_junk('"SHAPE__LENGTH"') }} as shape__length,
        {{ null_junk('"GEOMETRY"') }} as geometry,
        INGESTED_AT as _loaded_at,
        'https://opendata.dc.gov/datasets/4a42c826509d469e85348b1c6290fb8b_0' as _source_url

    from source

)

select * from renamed
