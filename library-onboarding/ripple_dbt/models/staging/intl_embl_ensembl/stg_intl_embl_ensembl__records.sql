{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 4 of 20 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   strand: integer
--   c_start: integer
--   end: integer
--   version: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'INTL_EMBL_ENSEMBL') }}
    qualify row_number() over (partition by "ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ null_junk('"SPECIES"') }} as species,
        {{ null_junk('"ASSEMBLY"') }} as assembly,
        {{ null_junk('"REGION"') }} as region,
        {{ null_junk('"FEATURE_TYPE"') }} as feature_type,
        {{ null_junk('"SYMBOL"') }} as symbol,
        {{ null_junk('"BIOTYPE"') }} as biotype,
        {{ stg_int('"STRAND"') }} as strand,
        {{ stg_int('"C_START"') }} as c_start,
        {{ stg_int('"END"') }} as end,
        {{ null_junk('"SEQ_REGION_NAME"') }} as seq_region_name,
        {{ stg_int('"VERSION"') }} as version,
        {{ null_junk('"DB_TYPE"') }} as db_type,
        {{ null_junk('"TAXON_ID"') }} as taxon_id,
        {{ null_junk('"CONSEQUENCE_TYPE"') }} as consequence_type,
        {{ null_junk('"ALLELE_STRING"') }} as allele_string,
        {{ null_junk('"POPULATION_NAME"') }} as population_name,
        {{ null_junk('"ONTOLOGY_TERM"') }} as ontology_term,
        {{ null_junk('"SOURCE_NAME"') }} as source_name,
        {{ null_junk('"PRIMARY_ACCESSION"') }} as primary_accession,
        _INGESTED_AT as _loaded_at,
        'https://rest.ensembl.org/' as _source_url

    from source

)

select * from renamed
