{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_USCIS_DATA') }}

),

renamed as (

    select
        FORM_NUMBER as form_number,
        FORM_TITLE as form_title,
        FISCAL_YEAR as fiscal_year,
        QUARTER as quarter,
        CATEGORY as category,
        COUNTRY as country,
        USCIS_OFFICE as uscis_office,
        RECEIPTS as receipts,
        APPROVALS as approvals,
        DENIALS as denials,
        PENDING as pending,
        AVG_PROCESSING_TIME_DAYS as avg_processing_time_days,
        RFE_COUNT as rfe_count,
        REPORT_TITLE as report_title,
        PUBLICATION_DATE as publication_date,
        SOURCE_FILE_URL as source_file_url,
        _INGESTED_AT as _loaded_at,
        'https://www.uscis.gov/tools/reports-and-studies/immigration-and-citizenship-data' as _source_url

    from source

)

select * from renamed
