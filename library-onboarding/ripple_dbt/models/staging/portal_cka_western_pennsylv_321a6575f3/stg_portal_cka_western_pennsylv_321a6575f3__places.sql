{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (ZIP is unique)
-- SPINE_ENTITY: place  (natural_key: ZIP)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: none of 13 columns. landing_column_profile_2026-09-19.tsv found no TEXT column where
-- every filled value parsed as a number, date or boolean.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_WESTERN_PENNSYLV_321A6575F3') }}
    qualify row_number() over (partition by "ZIP" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"OBJECTID_1"') }} as objectid_1,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"GLOBALID"') }} as globalid,
        {{ null_junk('"ADDRESS"') }} as address,
        {{ null_junk('"LASTUPDATE"') }} as lastupdate,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"ZIP"') }} as zip,
        {{ null_junk('"IMAGEURL"') }} as imageurl,
        {{ null_junk('"SHAPE_LENG"') }} as shape_leng,
        {{ null_junk('"OBJECTID"') }} as objectid,
        {{ null_junk('"NAME"') }} as name,
        {{ null_junk('"WEBURL"') }} as weburl,
        {{ null_junk('"DATASPATIAL_WKB"') }} as dataspatial_wkb,
        INGESTED_AT as _loaded_at,
        'https://data.wprdc.org/dataset/allegheny-county-parks-outlines' as _source_url

    from source

)

select * from renamed
