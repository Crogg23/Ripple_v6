{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_NCUA_CALL_REPORTS') }}

),

renamed as (

    select
        CU_NUMBER as cu_number,
        CYCLE_DATE as cycle_date,
        ACCT_CODE as acct_code,
        ACCT_VALUE as acct_value,
        CU_NAME as cu_name,
        STATE as state,
        CITY as city,
        ZIP_CODE as zip_code,
        EIN as ein,
        CHARTER_TYPE as charter_type,
        TABLE_NAME as table_name,
        ACCT_DESC as acct_desc,
        ACCT_STATUS as acct_status,
        _INGESTED_AT as _loaded_at,
        'https://ncua.gov/analysis/credit-union-corporate-call-report-data/quarterly-data' as _source_url

    from source

)

select * from renamed
