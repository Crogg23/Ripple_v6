version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_UTAH_OPEN_DATA_P_D5F7CA2621
        description: >
          Raw landing table for Utah Toxic Release Spills Inventory 2015-2018 EPA. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_utah_open_data_p_d5f7ca2621__facilities
    description: "GRAIN NOT PROVEN: registry natural_key is not unique within a load; whole-row dedupe (exact copies only). GRAIN: one row per facility (FRS_ID+C_1_YEAR+C_18_INDUSTRY_SECTOR_CODE+C_19_INDUSTRY_SECTOR+C_35_CAS_COMPOUND_ID+C_36_SRS_ID unique). SPINE_ENTITY: facility."
    columns:
      - name: c_3_frs_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: c_1_year
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: c_18_industry_sector_code
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: c_19_industry_sector
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: c_35_cas_compound_id
        data_tests:
          - not_null:
              config:
                severity: warn
      - name: c_36_srs_id
        data_tests:
          - not_null:
              config:
                severity: warn
