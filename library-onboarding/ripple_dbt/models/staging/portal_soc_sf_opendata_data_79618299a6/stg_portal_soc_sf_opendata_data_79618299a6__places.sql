{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ZIP+DBA_START_DATE+LOCATION_START_DATE unique)
-- SPINE_ENTITY: place  (natural_key: BUSINESS_ZIP, DBA_START_DATE, LOCATION_START_DATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_SF_OPENDATA_DATA_79618299A6') }}

),

renamed as (

    select
        "UNIQUEID" as uniqueid,
        "CERTIFICATE_NUMBER" as certificate_number,
        "TTXID" as ttxid,
        "OWNERSHIP_NAME" as ownership_name,
        "DBA_NAME" as dba_name,
        "FULL_BUSINESS_ADDRESS" as full_business_address,
        "CITY" as city,
        "DBA_START_DATE" as dba_start_date,
        "LOCATION_START_DATE" as location_start_date,
        "MAILING_ADDRESS_1" as mailing_address_1,
        "MAIL_CITY" as mail_city,
        "MAIL_STATE" as mail_state,
        "MAIL_ZIPCODE" as mail_zipcode,
        "PARKING_TAX" as parking_tax,
        "TRANSIENT_OCCUPANCY_TAX" as transient_occupancy_tax,
        "DATA_AS_OF" as data_as_of,
        "DATA_LOADED_AT" as data_loaded_at,
        "STATE" as state,
        "BUSINESS_ZIP" as business_zip,
        "DBA_END_DATE" as dba_end_date,
        "LOCATION_END_DATE" as location_end_date,
        "LOCATION" as location,
        "NAIC_CODE" as naic_code,
        "ADMINISTRATIVELY_CLOSED" as administratively_closed,
        "NAIC_CODE_DESCRIPTION" as naic_code_description,
        "NAICS_CODE_DESCRIPTIONS_LIST" as naics_code_descriptions_list,
        "NEIGHBORHOODS_ANALYSIS_BOUNDARIES" as neighborhoods_analysis_boundaries,
        "SUPERVISOR_DISTRICT" as supervisor_district,
        "COMPUTED_REGION_26CR_CADQ" as computed_region_26cr_cadq,
        "COMPUTED_REGION_AJP5_B2MD" as computed_region_ajp5_b2md,
        "COMPUTED_REGION_JWN9_IHCZ" as computed_region_jwn9_ihcz,
        "COMPUTED_REGION_6QBP_SG9Q" as computed_region_6qbp_sg9q,
        "COMPUTED_REGION_QGNN_B9VV" as computed_region_qgnn_b9vv,
        "COMMUNITY_BENEFIT_DISTRICT" as community_benefit_district,
        "LIC" as lic,
        "LIC_CODE_DESCRIPTION" as lic_code_description,
        "LIC_CODE_DESCRIPTIONS_LIST" as lic_code_descriptions_list,
        "BUSINESS_CORRIDOR" as business_corridor,
        INGESTED_AT as _loaded_at,
        'https://data.sfgov.org/d/g8m3-pdis' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by business_zip, dba_start_date, location_start_date order by _loaded_at desc) = 1
