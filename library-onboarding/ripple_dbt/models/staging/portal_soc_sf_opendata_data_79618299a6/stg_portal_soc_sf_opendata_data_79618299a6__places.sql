{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per business registration record (UNIQUEID unique)
-- SPINE_ENTITY: place  (natural_key: UNIQUEID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 14 of 36 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   dba_start_date: timestamp
--   location_start_date: timestamp
--   parking_tax: boolean
--   transient_occupancy_tax: boolean
--   data_as_of: timestamp
--   data_loaded_at: timestamp
--   dba_end_date: timestamp
--   location_end_date: timestamp
--   supervisor_district: integer
--   computed_region_26cr_cadq: integer
--   computed_region_ajp5_b2md: integer
--   computed_region_jwn9_ihcz: integer
--   computed_region_6qbp_sg9q: integer
--   computed_region_qgnn_b9vv: integer
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_SF_OPENDATA_DATA_79618299A6') }}
    qualify row_number() over (partition by "UNIQUEID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"UNIQUEID"') }} as uniqueid,
        {{ null_junk('"CERTIFICATE_NUMBER"') }} as certificate_number,
        {{ null_junk('"TTXID"') }} as ttxid,
        {{ null_junk('"OWNERSHIP_NAME"') }} as ownership_name,
        {{ null_junk('"DBA_NAME"') }} as dba_name,
        {{ null_junk('"FULL_BUSINESS_ADDRESS"') }} as full_business_address,
        {{ null_junk('"CITY"') }} as city,
        {{ null_junk('"STATE"') }} as state,
        {{ null_junk('"BUSINESS_ZIP"') }} as business_zip,
        {{ stg_ts('"DBA_START_DATE"') }} as dba_start_date,
        {{ stg_ts('"LOCATION_START_DATE"') }} as location_start_date,
        {{ null_junk('"MAILING_ADDRESS_1"') }} as mailing_address_1,
        {{ null_junk('"MAIL_CITY"') }} as mail_city,
        {{ null_junk('"MAIL_STATE"') }} as mail_state,
        {{ null_junk('"MAIL_ZIPCODE"') }} as mail_zipcode,
        {{ stg_bool('"PARKING_TAX"') }} as parking_tax,
        {{ stg_bool('"TRANSIENT_OCCUPANCY_TAX"') }} as transient_occupancy_tax,
        {{ stg_ts('"DATA_AS_OF"') }} as data_as_of,
        {{ stg_ts('"DATA_LOADED_AT"') }} as data_loaded_at,
        {{ stg_ts('"DBA_END_DATE"') }} as dba_end_date,
        {{ stg_ts('"LOCATION_END_DATE"') }} as location_end_date,
        {{ null_junk('"LOCATION"') }} as location,
        {{ null_junk('"ADMINISTRATIVELY_CLOSED"') }} as administratively_closed,
        {{ null_junk('"SELF_REPORTED_NAICS_CODE"') }} as self_reported_naics_code,
        {{ stg_int('"SUPERVISOR_DISTRICT"') }} as supervisor_district,
        {{ null_junk('"COMMUNITY_BENEFIT_DISTRICT"') }} as community_benefit_district,
        {{ null_junk('"NEIGHBORHOODS_ANALYSIS_BOUNDARIES"') }} as neighborhoods_analysis_boundaries,
        {{ stg_int('"COMPUTED_REGION_26CR_CADQ"') }} as computed_region_26cr_cadq,
        {{ stg_int('"COMPUTED_REGION_AJP5_B2MD"') }} as computed_region_ajp5_b2md,
        {{ stg_int('"COMPUTED_REGION_JWN9_IHCZ"') }} as computed_region_jwn9_ihcz,
        {{ stg_int('"COMPUTED_REGION_6QBP_SG9Q"') }} as computed_region_6qbp_sg9q,
        {{ stg_int('"COMPUTED_REGION_QGNN_B9VV"') }} as computed_region_qgnn_b9vv,
        {{ null_junk('"LIC"') }} as lic,
        {{ null_junk('"LIC_CODE_DESCRIPTION"') }} as lic_code_description,
        {{ null_junk('"LIC_CODE_DESCRIPTIONS_LIST"') }} as lic_code_descriptions_list,
        {{ null_junk('"BUSINESS_CORRIDOR"') }} as business_corridor,
        INGESTED_AT as _loaded_at,
        'https://data.sfgov.org/d/g8m3-pdis' as _source_url

    from source

)

select * from renamed
