version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_SOC_SF_OPENDATA_DATA_79618299A6
        description: >
          Raw landing table for Registered Business Locations - San Francisco. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_soc_sf_opendata_data_79618299a6__places
    description: "GRAIN: one row per business registration record (UNIQUEID unique). SPINE_ENTITY: place."
    columns:
      - name: uniqueid
        data_tests:
          - unique
          - not_null
