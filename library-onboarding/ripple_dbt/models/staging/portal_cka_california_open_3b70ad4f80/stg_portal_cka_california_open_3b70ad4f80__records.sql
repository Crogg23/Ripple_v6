{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (STN_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: STN_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (STN_ID) is NOT unique within a load as landed at generation time -- it would collapse 115 of 7,754 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_CALIFORNIA_OPEN_3B70AD4F80') }}

),

renamed as (

    select
        "PROGRAM_TYPE" as program_type,
        "STN_ID" as stn_id,
        "SITE_CODE" as site_code,
        "WELL_NAME" as well_name,
        "BASIN_NAME" as basin_name,
        "GSP_ALT_BASIN_NAME" as gsp_alt_basin_name,
        "AGENCY" as agency,
        "GSP_NAME" as gsp_name,
        "MONITORING_NETWORK_TYPE" as monitoring_network_type,
        "SUSTAINABILITY_INDICATORS" as sustainability_indicators,
        "PRINCIPAL_AQUIFER" as principal_aquifer,
        "SWN" as swn,
        "WCR_NO" as wcr_no,
        "LATITUDE" as latitude,
        "LONGITUDE" as longitude,
        "WELL_USE" as well_use,
        "WELL_TYPE" as well_type,
        "WELL_DEPTH" as well_depth,
        "TOP_PRF" as top_prf,
        "BOT_PRF" as bot_prf,
        "LAST_GSE" as last_gse,
        "LAST_RPE" as last_rpe,
        "SMC_START_DATE" as smc_start_date,
        "SMC_MT" as smc_mt,
        "SMC_IM_5_YR" as smc_im_5_yr,
        "SMC_IM_10_YR" as smc_im_10_yr,
        "SMC_IM_15_YR" as smc_im_15_yr,
        "SMC_MO" as smc_mo,
        "COMMENTS" as comments,
        "FIRST_MSMT_DATE" as first_msmt_date,
        "LAST_MSMT_DATE" as last_msmt_date,
        "LAST_NM_CODE" as last_nm_code,
        "MSMT_COUNT" as msmt_count,
        "LAST_NM_DATE" as last_nm_date,
        INGESTED_AT as _loaded_at,
        'https://data.ca.gov/dataset/gsp-monitoring-data' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by program_type, stn_id, site_code, well_name, basin_name, gsp_alt_basin_name, agency, gsp_name, monitoring_network_type, sustainability_indicators, principal_aquifer, swn, wcr_no, latitude, longitude, well_use, well_type, well_depth, top_prf, bot_prf, last_gse, last_rpe, smc_start_date, smc_mt, smc_im_5_yr, smc_im_10_yr, smc_im_15_yr, smc_mo, comments, first_msmt_date, last_msmt_date, last_nm_code, msmt_count, last_nm_date order by _loaded_at desc) = 1
