{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (STN_ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: STN_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 15 of 34 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   well_depth: integer
--   top_prf: float
--   bot_prf: float
--   last_gse: float
--   last_rpe: float
--   smc_start_date: timestamp
--   smc_mt: float
--   smc_im_5_yr: float
--   smc_im_10_yr: float
--   smc_im_15_yr: float
--   smc_mo: float
--   first_msmt_date: timestamp
--   last_msmt_date: timestamp
--   msmt_count: integer
--   last_nm_date: timestamp
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (STN_ID) is NOT unique within a load as landed at generation time -- it would collapse 115 of 7,754 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_CALIFORNIA_OPEN_3B70AD4F80') }}
    qualify row_number() over (partition by "PROGRAM_TYPE", "STN_ID", "SITE_CODE", "WELL_NAME", "BASIN_NAME", "GSP_ALT_BASIN_NAME", "AGENCY", "GSP_NAME", "MONITORING_NETWORK_TYPE", "SUSTAINABILITY_INDICATORS", "PRINCIPAL_AQUIFER", "SWN", "WCR_NO", "LATITUDE", "LONGITUDE", "WELL_USE", "WELL_TYPE", "WELL_DEPTH", "TOP_PRF", "BOT_PRF", "LAST_GSE", "LAST_RPE", "SMC_START_DATE", "SMC_MT", "SMC_IM_5_YR", "SMC_IM_10_YR", "SMC_IM_15_YR", "SMC_MO", "COMMENTS", "FIRST_MSMT_DATE", "LAST_MSMT_DATE", "LAST_NM_CODE", "MSMT_COUNT", "LAST_NM_DATE" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"PROGRAM_TYPE"') }} as program_type,
        {{ null_junk('"STN_ID"') }} as stn_id,
        {{ null_junk('"SITE_CODE"') }} as site_code,
        {{ null_junk('"WELL_NAME"') }} as well_name,
        {{ null_junk('"BASIN_NAME"') }} as basin_name,
        {{ null_junk('"GSP_ALT_BASIN_NAME"') }} as gsp_alt_basin_name,
        {{ null_junk('"AGENCY"') }} as agency,
        {{ null_junk('"GSP_NAME"') }} as gsp_name,
        {{ null_junk('"MONITORING_NETWORK_TYPE"') }} as monitoring_network_type,
        {{ null_junk('"SUSTAINABILITY_INDICATORS"') }} as sustainability_indicators,
        {{ null_junk('"PRINCIPAL_AQUIFER"') }} as principal_aquifer,
        {{ null_junk('"SWN"') }} as swn,
        {{ null_junk('"WCR_NO"') }} as wcr_no,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"WELL_USE"') }} as well_use,
        {{ null_junk('"WELL_TYPE"') }} as well_type,
        {{ stg_int('"WELL_DEPTH"') }} as well_depth,
        {{ stg_float('"TOP_PRF"') }} as top_prf,
        {{ stg_float('"BOT_PRF"') }} as bot_prf,
        {{ stg_float('"LAST_GSE"') }} as last_gse,
        {{ stg_float('"LAST_RPE"') }} as last_rpe,
        {{ stg_ts('"SMC_START_DATE"') }} as smc_start_date,
        {{ stg_float('"SMC_MT"') }} as smc_mt,
        {{ stg_float('"SMC_IM_5_YR"') }} as smc_im_5_yr,
        {{ stg_float('"SMC_IM_10_YR"') }} as smc_im_10_yr,
        {{ stg_float('"SMC_IM_15_YR"') }} as smc_im_15_yr,
        {{ stg_float('"SMC_MO"') }} as smc_mo,
        {{ null_junk('"COMMENTS"') }} as comments,
        {{ stg_ts('"FIRST_MSMT_DATE"') }} as first_msmt_date,
        {{ stg_ts('"LAST_MSMT_DATE"') }} as last_msmt_date,
        {{ null_junk('"LAST_NM_CODE"') }} as last_nm_code,
        {{ stg_int('"MSMT_COUNT"') }} as msmt_count,
        {{ stg_ts('"LAST_NM_DATE"') }} as last_nm_date,
        INGESTED_AT as _loaded_at,
        'https://data.ca.gov/dataset/gsp-monitoring-data' as _source_url

    from source

)

select * from renamed
