version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: PORTAL_CKA_SAN_JOSE_OPEN_DA_98849B65EE
        description: >
          Raw landing table for Park Condition Assessment. All columns arrive as TEXT and
          are cast in the staging layer.

models:
  - name: stg_portal_cka_san_jose_open_da_98849b65ee__records
    description: "GRAIN: one row per record (spine_entity not determined -- no registry hint available) (LATLON+REPORTDATE unique). SPINE_ENTITY: (not determined)."
    columns:
      - name: latitude
        data_tests:
          - not_null
      - name: reportdate
        data_tests:
          - not_null
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - latitude
            - reportdate
