{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per place (LATLON+REPORTDATE unique)
-- SPINE_ENTITY: place  (natural_key: LATITUDE, REPORTDATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 34 of 45 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   x: float
--   y: float
--   objectid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   facilityid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   intid: ID: whole numbers, over 90% distinct -- stays TEXT, '.0' tail removed
--   assessmentyear: integer
--   parkfacilityid: integer
--   councildistrict: integer
--   parkdistrict: integer
--   acreageall: float
--   acreagedev: float
--   courtscoretotal: float
--   fieldscoretotal: float
--   benchscoretotal: float
--   playgroundscoretotal: float
--   devlandscoretotal: float
--   dogparkscoretotal: float
--   drinkftnscoretotal: float
--   exerstascoretotal: float
--   hardscapescoretotal: float
--   turfscoretotal: float
--   plantscoretotal: float
--   pklotscoretotal: float
--   restroomscoretotal: float
--   skateparkscoretotal: float
--   picnicscoretotal: float
--   treesscoretotal: float
--   undevlandscoretotal: float
--   wasterecscoretotal: float
--   watermgmtscoretotal: integer
--   overallscore: float
--   createddate: timestamp
--   lastupdate: timestamp
--   hpipctile: float
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.
-- DEDUPE: registry natural_key (LATITUDE, REPORTDATE) is NOT unique within a load as landed at generation time -- it would collapse 1 of 1,616 rows. Deduping on the WHOLE ROW instead (exact copies only). A record that changes between loads shows once per version. Find the real key, fix SOURCE_REGISTRY, regenerate.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_98849B65EE') }}
    qualify row_number() over (partition by "X", "Y", "OBJECTID", "FACILITYID", "INTID", "REPORTDATE", "ASSESSMENTYEAR", "PARKNAME", "PARKFACILITYID", "LOCATIONID", "LONGITUDE", "LATITUDE", "COUNCILDISTRICT", "PARKDISTRICT", "ACREAGEALL", "ACREAGEDEV", "PARKTYPE", "ASSESSMENTCATEGORIES", "COURTSCORETOTAL", "FIELDSCORETOTAL", "BENCHSCORETOTAL", "BIKESCORETOTAL", "PLAYGROUNDSCORETOTAL", "DEVLANDSCORETOTAL", "DOGPARKSCORETOTAL", "DRINKFTNSCORETOTAL", "EXERSTASCORETOTAL", "HARDSCAPESCORETOTAL", "TURFSCORETOTAL", "PLANTSCORETOTAL", "PKLOTSCORETOTAL", "RESTROOMSCORETOTAL", "SKATEPARKSCORETOTAL", "PICNICSCORETOTAL", "TREESSCORETOTAL", "UNDEVLANDSCORETOTAL", "WASTERECSCORETOTAL", "WATERMGMTSCORETOTAL", "OVERALLSCORE", "CREATEDDATE", "LASTUPDATE", "ENTERPRISEID", "HPIPCTILE", "ASSESSMENTTYPE", "ASSESSMENTID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_float('"X"') }} as x,
        {{ stg_float('"Y"') }} as y,
        {{ stg_id_text('"OBJECTID"') }} as objectid,
        {{ stg_id_text('"FACILITYID"') }} as facilityid,
        {{ stg_id_text('"INTID"') }} as intid,
        {{ null_junk('"REPORTDATE"') }} as reportdate,
        {{ stg_int('"ASSESSMENTYEAR"') }} as assessmentyear,
        {{ null_junk('"PARKNAME"') }} as parkname,
        {{ stg_int('"PARKFACILITYID"') }} as parkfacilityid,
        {{ null_junk('"LOCATIONID"') }} as locationid,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ stg_int('"COUNCILDISTRICT"') }} as councildistrict,
        {{ stg_int('"PARKDISTRICT"') }} as parkdistrict,
        {{ stg_float('"ACREAGEALL"') }} as acreageall,
        {{ stg_float('"ACREAGEDEV"') }} as acreagedev,
        {{ null_junk('"PARKTYPE"') }} as parktype,
        {{ null_junk('"ASSESSMENTCATEGORIES"') }} as assessmentcategories,
        {{ stg_float('"COURTSCORETOTAL"') }} as courtscoretotal,
        {{ stg_float('"FIELDSCORETOTAL"') }} as fieldscoretotal,
        {{ stg_float('"BENCHSCORETOTAL"') }} as benchscoretotal,
        {{ null_junk('"BIKESCORETOTAL"') }} as bikescoretotal,
        {{ stg_float('"PLAYGROUNDSCORETOTAL"') }} as playgroundscoretotal,
        {{ stg_float('"DEVLANDSCORETOTAL"') }} as devlandscoretotal,
        {{ stg_float('"DOGPARKSCORETOTAL"') }} as dogparkscoretotal,
        {{ stg_float('"DRINKFTNSCORETOTAL"') }} as drinkftnscoretotal,
        {{ stg_float('"EXERSTASCORETOTAL"') }} as exerstascoretotal,
        {{ stg_float('"HARDSCAPESCORETOTAL"') }} as hardscapescoretotal,
        {{ stg_float('"TURFSCORETOTAL"') }} as turfscoretotal,
        {{ stg_float('"PLANTSCORETOTAL"') }} as plantscoretotal,
        {{ stg_float('"PKLOTSCORETOTAL"') }} as pklotscoretotal,
        {{ stg_float('"RESTROOMSCORETOTAL"') }} as restroomscoretotal,
        {{ stg_float('"SKATEPARKSCORETOTAL"') }} as skateparkscoretotal,
        {{ stg_float('"PICNICSCORETOTAL"') }} as picnicscoretotal,
        {{ stg_float('"TREESSCORETOTAL"') }} as treesscoretotal,
        {{ stg_float('"UNDEVLANDSCORETOTAL"') }} as undevlandscoretotal,
        {{ stg_float('"WASTERECSCORETOTAL"') }} as wasterecscoretotal,
        {{ stg_int('"WATERMGMTSCORETOTAL"') }} as watermgmtscoretotal,
        {{ stg_float('"OVERALLSCORE"') }} as overallscore,
        {{ stg_ts('"CREATEDDATE"') }} as createddate,
        {{ stg_ts('"LASTUPDATE"') }} as lastupdate,
        {{ null_junk('"ENTERPRISEID"') }} as enterpriseid,
        {{ stg_float('"HPIPCTILE"') }} as hpipctile,
        {{ null_junk('"ASSESSMENTTYPE"') }} as assessmenttype,
        {{ null_junk('"ASSESSMENTID"') }} as assessmentid,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/park-condition-assessment' as _source_url

    from source

)

select * from renamed
