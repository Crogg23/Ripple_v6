{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (LATLON+REPORTDATE unique)
-- SPINE_ENTITY: place  (natural_key: LATITUDE, REPORTDATE)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM. Casts kept as landed (TEXT) -- add explicit type casts by hand once each
-- column's real type is confirmed; this generator has no semantic type knowledge.
-- spine_entity 'place' has no connect/ resolver yet (see connect/spine_entity.py) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_CKA_SAN_JOSE_OPEN_DA_98849B65EE') }}

),

renamed as (

    select
        "X" as x,
        "Y" as y,
        "OBJECTID" as objectid,
        "FACILITYID" as facilityid,
        "INTID" as intid,
        "REPORTDATE" as reportdate,
        "ASSESSMENTYEAR" as assessmentyear,
        "PARKNAME" as parkname,
        "PARKFACILITYID" as parkfacilityid,
        "LOCATIONID" as locationid,
        "LONGITUDE" as longitude,
        "LATITUDE" as latitude,
        "COUNCILDISTRICT" as councildistrict,
        "PARKDISTRICT" as parkdistrict,
        "ACREAGEALL" as acreageall,
        "ACREAGEDEV" as acreagedev,
        "PARKTYPE" as parktype,
        "ASSESSMENTCATEGORIES" as assessmentcategories,
        "COURTSCORETOTAL" as courtscoretotal,
        "FIELDSCORETOTAL" as fieldscoretotal,
        "BENCHSCORETOTAL" as benchscoretotal,
        "BIKESCORETOTAL" as bikescoretotal,
        "PLAYGROUNDSCORETOTAL" as playgroundscoretotal,
        "DEVLANDSCORETOTAL" as devlandscoretotal,
        "DOGPARKSCORETOTAL" as dogparkscoretotal,
        "DRINKFTNSCORETOTAL" as drinkftnscoretotal,
        "EXERSTASCORETOTAL" as exerstascoretotal,
        "HARDSCAPESCORETOTAL" as hardscapescoretotal,
        "TURFSCORETOTAL" as turfscoretotal,
        "PLANTSCORETOTAL" as plantscoretotal,
        "PKLOTSCORETOTAL" as pklotscoretotal,
        "RESTROOMSCORETOTAL" as restroomscoretotal,
        "SKATEPARKSCORETOTAL" as skateparkscoretotal,
        "PICNICSCORETOTAL" as picnicscoretotal,
        "TREESSCORETOTAL" as treesscoretotal,
        "UNDEVLANDSCORETOTAL" as undevlandscoretotal,
        "WASTERECSCORETOTAL" as wasterecscoretotal,
        "WATERMGMTSCORETOTAL" as watermgmtscoretotal,
        "OVERALLSCORE" as overallscore,
        "CREATEDDATE" as createddate,
        "LASTUPDATE" as lastupdate,
        "ENTERPRISEID" as enterpriseid,
        "HPIPCTILE" as hpipctile,
        "ASSESSMENTTYPE" as assessmenttype,
        "ASSESSMENTID" as assessmentid,
        INGESTED_AT as _loaded_at,
        'https://data.sanjoseca.gov/dataset/park-condition-assessment' as _source_url

    from source

)

select * from renamed
qualify row_number() over (partition by latitude, reportdate order by _loaded_at desc) = 1
