{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 12 of 22 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   time: timestamp
--   depth: float
--   mag: float
--   nst: integer
--   gap: float
--   dmin: float
--   rms: float
--   updated: timestamp
--   horizontalerror: float
--   deptherror: float
--   magerror: float
--   magnst: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.

with source as (

    select * from {{ source('ripple_raw', 'FED_USGS_EARTHQUAKES') }}
    qualify row_number() over (partition by "ID" order by _INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ stg_ts('"TIME"') }} as time,
        {{ null_junk('"LATITUDE"') }} as latitude,
        {{ null_junk('"LONGITUDE"') }} as longitude,
        {{ stg_float('"DEPTH"') }} as depth,
        {{ stg_float('"MAG"') }} as mag,
        {{ null_junk('"MAGTYPE"') }} as magtype,
        {{ stg_int('"NST"') }} as nst,
        {{ stg_float('"GAP"') }} as gap,
        {{ stg_float('"DMIN"') }} as dmin,
        {{ stg_float('"RMS"') }} as rms,
        {{ null_junk('"NET"') }} as net,
        {{ null_junk('"ID"') }} as id,
        {{ stg_ts('"UPDATED"') }} as updated,
        {{ null_junk('"PLACE"') }} as place,
        {{ null_junk('"TYPE"') }} as type,
        {{ stg_float('"HORIZONTALERROR"') }} as horizontalerror,
        {{ stg_float('"DEPTHERROR"') }} as deptherror,
        {{ stg_float('"MAGERROR"') }} as magerror,
        {{ stg_int('"MAGNST"') }} as magnst,
        {{ null_junk('"STATUS"') }} as status,
        {{ null_junk('"LOCATIONSOURCE"') }} as locationsource,
        {{ null_junk('"MAGSOURCE"') }} as magsource,
        _INGESTED_AT as _loaded_at,
        'https://earthquake.usgs.gov/fdsnws/event/1/query' as _source_url

    from source

)

select * from renamed
