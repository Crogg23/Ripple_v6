{{ config(tags=['minimal_staging']) }}

-- GRAIN: NOT YET DETERMINED (profiler returned AMBIGUOUS -- needs manual review)
-- This is a passthrough staging view: snake_case rename only, no dedup.
-- Once grain is resolved, replace with a full staging model via generate_staging_models.py.
-- Generated by scripts/generate_minimal_staging.py

with source as (

    select * from {{ source('ripple_raw', 'FED_PBGC_DATA') }}

),

renamed as (

    select
        DATA_YEAR as data_year,
        PUBLICATION_DATE as publication_date,
        PROGRAM_TYPE as program_type,
        TABLE_NAME as table_name,
        METRIC_NAME as metric_name,
        METRIC_VALUE as metric_value,
        UNIT as unit,
        STATE as state,
        SOURCE_FILE as source_file,
        NOTES_FLAG as notes_flag,
        _INGESTED_AT as _loaded_at,
        'https://www.pbgc.gov/about/reports/pension-insurance-data' as _source_url

    from source

)

select * from renamed
