version: 2

sources:
  - name: ripple_raw
    database: LIBRARY_RAW
    schema: LANDING
    tables:
      - name: FED_DOJ_EPSTEIN_LIBRARY
        description: >
          Raw landing table for DOJ Epstein Library — File Manifest. Grain not yet determined.

models:
  - name: stg_fed_doj_epstein_library__records
    description: "Minimal passthrough staging (grain undetermined). Needs manual review."
