{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ADVERTISER_ID is unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ADVERTISER_ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 21 of 26 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   total_creatives: integer
--   spend_usd: integer
--   spend_eur: integer
--   spend_inr: integer
--   spend_bgn: integer
--   spend_czk: integer
--   spend_dkk: integer
--   spend_huf: integer
--   spend_pln: integer
--   spend_ron: integer
--   spend_sek: integer
--   spend_gbp: integer
--   spend_nzd: integer
--   spend_ils: integer
--   spend_aud: integer
--   spend_twd: integer
--   spend_brl: integer
--   spend_ars: integer
--   spend_zar: integer
--   spend_clp: integer
--   spend_mxn: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'FED_GOOGLE_POLADS_ADVERTISER_STATS') }}
    qualify row_number() over (partition by "ADVERTISER_ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ADVERTISER_ID"') }} as advertiser_id,
        {{ null_junk('"ADVERTISER_NAME"') }} as advertiser_name,
        {{ null_junk('"PUBLIC_IDS_LIST"') }} as public_ids_list,
        {{ null_junk('"REGIONS"') }} as regions,
        "ELECTIONS" as elections,
        {{ stg_int('"TOTAL_CREATIVES"') }} as total_creatives,
        {{ stg_int('"SPEND_USD"') }} as spend_usd,
        {{ stg_int('"SPEND_EUR"') }} as spend_eur,
        {{ stg_int('"SPEND_INR"') }} as spend_inr,
        {{ stg_int('"SPEND_BGN"') }} as spend_bgn,
        {{ stg_int('"SPEND_CZK"') }} as spend_czk,
        {{ stg_int('"SPEND_DKK"') }} as spend_dkk,
        {{ stg_int('"SPEND_HUF"') }} as spend_huf,
        {{ stg_int('"SPEND_PLN"') }} as spend_pln,
        {{ stg_int('"SPEND_RON"') }} as spend_ron,
        {{ stg_int('"SPEND_SEK"') }} as spend_sek,
        {{ stg_int('"SPEND_GBP"') }} as spend_gbp,
        {{ stg_int('"SPEND_NZD"') }} as spend_nzd,
        {{ stg_int('"SPEND_ILS"') }} as spend_ils,
        {{ stg_int('"SPEND_AUD"') }} as spend_aud,
        {{ stg_int('"SPEND_TWD"') }} as spend_twd,
        {{ stg_int('"SPEND_BRL"') }} as spend_brl,
        {{ stg_int('"SPEND_ARS"') }} as spend_ars,
        {{ stg_int('"SPEND_ZAR"') }} as spend_zar,
        {{ stg_int('"SPEND_CLP"') }} as spend_clp,
        {{ stg_int('"SPEND_MXN"') }} as spend_mxn,
        INGESTED_AT as _loaded_at,
        '' as _source_url

    from source

)

select * from renamed
