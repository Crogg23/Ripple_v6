{{ config(tags=['spine_generated']) }}

-- GRAIN: one row per record (spine_entity not determined -- no registry hint available) (ID unique)
-- SPINE_ENTITY: (not determined)  (natural_key: ID)
-- Generated by scripts/generate_staging_models.py from SOURCE_REGISTRY -- deterministic,
-- no LLM.
-- CASTS: 2 of 17 columns, from landing_column_profile_2026-09-19.tsv. A column is cast only
-- when every filled value in the sample parsed. try_ casts: a value that does not
-- parse reads NULL. The natural key and join-key columns stay TEXT.
--   taxyr: integer
--   assetsincludedamt: integer
-- spine_entity not determined (grain/natural_key proven, but no registry hint says what this row is ABOUT) -- no SPINE_ENTITY_ID emitted.
-- landing table's ingestion timestamp is named INGESTED_AT (no leading underscore) rather than the usual _INGESTED_AT -- confirmed via INFORMATION_SCHEMA, not assumed.

with source as (

    select * from {{ source('ripple_raw', 'PORTAL_SOC_COLORADO_INFORMA_D017CFCF7F') }}
    qualify row_number() over (partition by "ID" order by INGESTED_AT desc) = 1

),

renamed as (

    select
        {{ null_junk('"ID"') }} as id,
        {{ stg_int('"TAXYR"') }} as taxyr,
        {{ null_junk('"BUSINESSNAME1"') }} as businessname1,
        {{ null_junk('"EIN"') }} as ein,
        {{ null_junk('"COLLECTIONSOFARTIND"') }} as collectionsofartind,
        {{ null_junk('"COLLECTIONUSEDPUBEXHIBITIONIND"') }} as collectionusedpubexhibitionind,
        {{ null_junk('"COLLUSEDSCHOLARLYRSRCHIND"') }} as collusedscholarlyrsrchind,
        {{ null_junk('"COLLECTIONUSEDPRESERVATIONIND"') }} as collectionusedpreservationind,
        {{ null_junk('"COLLUSEDLOANOREXCHPROGIND"') }} as collusedloanorexchprogind,
        {{ null_junk('"COLLECTIONUSEDOTHERPURPOSESIND"') }} as collectionusedotherpurposesind,
        {{ null_junk('"SOLICITEDASSETSSALEIND"') }} as solicitedassetssaleind,
        {{ null_junk('"HELDWORKS_REVENUESINCLUDEDAMT"') }} as heldworks_revenuesincludedamt,
        {{ null_junk('"HELDWORKS_ASSETSINCLUDEDAMT"') }} as heldworks_assetsincludedamt,
        {{ null_junk('"BUSINESSNAME2"') }} as businessname2,
        {{ stg_int('"ASSETSINCLUDEDAMT"') }} as assetsincludedamt,
        {{ null_junk('"OTHERPURPOSESDESC"') }} as otherpurposesdesc,
        {{ null_junk('"REVENUESINCLUDEDAMT"') }} as revenuesincludedamt,
        INGESTED_AT as _loaded_at,
        'https://data.colorado.gov/d/n5sw-qzbs' as _source_url

    from source

)

select * from renamed
