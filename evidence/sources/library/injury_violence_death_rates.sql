-- County-level death rates from injury, overdose, and violence (including guns), by intent.
-- Auto-generated by scripts/gen_evidence_pages.py: 132,000 rows > 100k, so this is a BOUNDED pre-aggregate (parquet-safe by construction), not select *.
-- no typed date or known category column; single-row count only.
select count(*) as row_count from THE_LIBRARY.HEALTH.INJURY_VIOLENCE_DEATH_RATES
