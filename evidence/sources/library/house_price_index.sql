-- The official US house-price trend, free and keyless, down to the census tract -- the price side of affordability.
-- Auto-generated by scripts/gen_evidence_pages.py: 184,807 rows > 100k, so this is a BOUNDED pre-aggregate (parquet-safe by construction), not select *.
-- no typed date or known category column; single-row count only.
select count(*) as row_count from THE_LIBRARY.HOUSING.HOUSE_PRICE_INDEX
