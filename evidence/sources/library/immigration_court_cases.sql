-- 12.6M immigration court case rows -- but only the case-type column landed. Big row count, thin substance; needs a re-pour.
-- Auto-generated by scripts/gen_evidence_pages.py: 12,631,225 rows > 100k, so this is a BOUNDED pre-aggregate (parquet-safe by construction), not select *.
-- no typed date or known category column; single-row count only.
select count(*) as row_count from THE_LIBRARY.IMMIGRATION.IMMIGRATION_COURT_CASES
