-- Irish CRO company register -- every registered company, status, type, address, NACE code.
-- Auto-generated by scripts/gen_evidence_pages.py: 818,934 rows > 100k, so this is a BOUNDED pre-aggregate (parquet-safe by construction), not select *.
-- no typed date or known category column; single-row count only.
select count(*) as row_count from THE_LIBRARY.COMPANIES.IRELAND_COMPANIES
