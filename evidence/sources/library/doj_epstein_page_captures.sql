-- Every archived snapshot of the DOJ Epstein document pages -- proof, by digest, of when the page changed.
-- Auto-generated by scripts/gen_evidence_pages.py: 1,537,352 rows > 100k, so this is a BOUNDED pre-aggregate (parquet-safe by construction), not select *.
-- no typed date or known category column; single-row count only.
select count(*) as row_count from THE_LIBRARY.OPEN_DATA.DOJ_EPSTEIN_PAGE_CAPTURES
