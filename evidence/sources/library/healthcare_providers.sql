-- Every US healthcare provider with an NPI -- all 9.6M. The backbone of the health data.
-- Auto-generated by scripts/gen_evidence_pages.py: 9,606,683 rows > 100k, so this is a BOUNDED pre-aggregate (parquet-safe by construction), not select *.
-- no typed date or known category column; single-row count only.
select count(*) as row_count from THE_LIBRARY.HEALTH.HEALTHCARE_PROVIDERS
