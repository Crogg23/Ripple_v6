-- What every Medicare provider billed and got paid, 1.3M rows -- follow the money by NPI.
-- Auto-generated by scripts/gen_evidence_pages.py: 1,296,739 rows > 100k, so this is a BOUNDED pre-aggregate (parquet-safe by construction), not select *.
-- no typed date or known category column; single-row count only.
select count(*) as row_count from THE_LIBRARY.HEALTH.MEDICARE_PROVIDER_BILLING
