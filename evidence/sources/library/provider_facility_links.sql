-- Which doctors work at which facilities -- the NPI-to-CCN crosswalk, 2.2M links.
-- Auto-generated by scripts/gen_evidence_pages.py: 2,260,193 rows > 100k, so this is a BOUNDED pre-aggregate (parquet-safe by construction), not select *.
-- no typed date or known category column; single-row count only.
select count(*) as row_count from THE_LIBRARY.HEALTH.PROVIDER_FACILITY_LINKS
