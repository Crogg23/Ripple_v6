---
title: Doj Epstein Page Captures
---

```sql n
select 1537352 as row_count
```

Every archived snapshot of the DOJ Epstein document pages -- proof, by digest, of when the page changed.

Source: `THE_LIBRARY.OPEN_DATA.DOJ_EPSTEIN_PAGE_CAPTURES` (raw, 1,537,352 rows). This page reads a bounded pre-aggregate of that view, not every row.

<BigValue data={n} value=row_count title="Rows in the full source" fmt="#,##0" />
