# Dev/test deps for the connect entity layer (the repo's test suite).
# Runtime deps live in the top-level requirements.txt -- install that first:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt
pytest==8.4.2
rapidfuzz==3.14.5  # offline Jaro-Winkler mirror for the match-quality eval (test-only)
                  #   NOTE: 9.0.0 was a hallucinated pin (no such release) that kept
                  #   CI red for 14 straight runs — verify against PyPI before bumping.
