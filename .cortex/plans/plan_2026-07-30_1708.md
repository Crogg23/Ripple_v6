---
created: 2026-07-30T17:08:55.937Z
session: 30923ed1-a85a-484e-bcce-62dee49931da
working_directory: c:\Code\Ripple_v6
---

## Context

Read CLAUDE.md, `connect/cohort.py` (full, incl. the 3 guard comments: dup-denominator, plausibility gate, pooled-vs-median), `connect/leads.py`, `connect/leads_specs.py`, `connect/receipt.py`, `connect/safety.py`, `connect/store.py`, `connect/db.py`, `connect/__main__.py`, and `outputs/cohort_outliers_2024.csv`. Re-ran `python -m connect.cohort --year 2024` — reproduces the baseline exactly (149,305 ranked, 16,260 outliers, same top rows, same totals).

Key finding: the LEADS/receipt machinery (`LEADS` table schema, `receipt.sql_sha256`, `receipt.resolve_snapshots`, `receipt.assemble/render/_verify`, `leads._merge_leads`, `leads._expire_rule`, `safety.gate_rows`) is generic — it does **not** require the JobSpec left/right-join shape. `receipt.resolve_snapshots(conn, spec)` only needs `spec["left"]["table"]`/`spec["right"]["table"]` to know which source tables to pin; `leads._merge_leads`/`_ensure_leads_table`/`_expire_rule` only need a DataFrame with the standard LEADS columns. So this cohort finding can ride the *same* tables and *same* functions without forcing it through `compile_sql` (which assumes a hard-key intersection — wrong shape for an aggregation/cohort query) and without inventing a parallel format.

One small gap: `receipt.run()`'s spec lookup is `JOBS.get(rule_name)` — hardcoded to `leads_specs.JOBS`. I'll extend it to also check a small `COHORT_SPECS` registry, so `connect receipt --id --check` works for this rule too, without registering it in `leads_specs.JOBS` (which would break `leads.run()`'s generic loop, since it calls `compile_sql` on every registered job).

`receipt._verify()` re-runs `COMPILED_SQL` and checks a `LEFT_KEY_VALUE` column against the stored one — generic, but `cohort.build_sql`'s final SELECT doesn't currently expose that column. I'll add one column (`EST_KEY`, already computed but not selected) to `cohort.py`'s final SELECT so a deterministic composite key can be derived — additive only, doesn't touch any WHERE/HAVING/guard logic, so all three guards are unaffected (will re-verify anyway per the task's own instruction to re-check after any query change).

## Implementation steps

**Step 1 — Re-verify guards (read-only, via `python -c` scripts using `connect.db`/`connect.cohort`, same connection as the tool):**
- 1a. Plausibility gate: `SELECT COUNT(*) FROM (ranked_est) WHERE HOURS/EMPLOYEES NOT BETWEEN 800 AND 3500` on the *ranked* output → must be 0.
- 1b. Dup-denominator: `SELECT ein, employees, hours, COUNT(DISTINCT establishment) FROM ranked_est GROUP BY 1,2,3 HAVING COUNT(*)>=3` → must be 0 rows in the ranked set.
- 1c. Confirm every ranking `ORDER BY`/filter in `cohort.py` uses `fold_vs_pooled` (cohort_pooled_dart), not `fold_vs_median` — visual/grep confirmation (already true from the read).
- 1d. Spot-check current top 10 (`cohort_outliers_2024.csv` top rows) against raw `FED_OSHA_ITA_300A_SUMMARY_2024`: hours/employee plausibility, distinct-entity sanity (e.g. NY-Presbyterian Morgan Stanley Children's, Quality Behavioral Outcomes, BluSky Restoration, Wegmans, Amergis).
- If any check fails → fix `cohort.py`, re-run, repeat. (Expectation from research: they pass — the guards already active in the query match the CSV.)

**Step 2 — Entity-quality gaps (read-only):**
- 2a. `SELECT COUNT(*), COUNT(CASE WHEN raw ESTABLISHMENT_ID blank THEN 1 END) FROM raw` fraction falling back to address key; check for any obvious split/merge (same EIN + same normalized address appearing as >1 est_key due to punctuation) in the ranked set.
- 2b. Eyeball NAICS-4 descriptions for the top 25 (already visible in the CSV — nursing homes, staffing, hospitals, restoration contractors, groceries) — note as a documented limitation any cohort where NAICS-4 seems too coarse (e.g. generic codes with blank INDUSTRY_DESCRIPTION).
- Document results/counts either way (found-none is also a documented result, not silently skipped).

**Step 3 — Build the receipt (code changes):**
- 3a. `connect/cohort.py`: add `s.est_key AS EST_KEY` to the final SELECT list (grain-identifying column, additive only).
- 3b. New file `connect/cohort_leads.py`:
  - `COHORT_SPECS = {"osha_cohort_outlier_2024": {"left": {"table": OSHA_TABLES[2024]}, "right": {"table": OSHA_TABLES[2024]}}}` (single-source finding; left==right table so `receipt.source_tables` dedupes to one pinned source — reuses `receipt.resolve_snapshots`/`source_urls` unmodified).
  - `build_compiled_sql(...)`: wraps `cohort.build_sql(...)` with `WITH ranked_est AS (...) SELECT *, EIN || '|' || EST_KEY AS LEFT_KEY_VALUE FROM ranked_est WHERE dart_cases >= 5 AND fold_vs_pooled >= 2 ORDER BY fold_vs_pooled DESC, dart_cases DESC` — deterministic, no `CURRENT_DATE` dependency (matches receipt's "AS_OF freezes the clock" property already, since `cohort.build_sql` never references the wall clock).
  - `run(year=2024, write=False)`: computes `sha = receipt.sql_sha256(sql)`, `as_of = date.today().isoformat()`, `snapshots = receipt.resolve_snapshots(conn, COHORT_SPECS[rule])` (skipped on dry-run, same pattern as `leads.run_job`), builds one LEADS row per ranked establishment: `LEAD_ID = md5(rule|EIN|EST_KEY)`-style via a local `_lead_id` (same scheme as `leads._lead_id`), `TITLE` = a neutral, peer-relative sentence (e.g. `"{company} — {city}, {state}: DART rate {dart_rate} is {fold}x its NAICS-{naics} {size_band} peer cohort's pooled rate ({cases} DART cases, {deaths} death(s))"` — no negligence/wrongdoing language, matches the neutral-phrasing convention in `leads_specs.py`), `SCORE = fold_vs_pooled`, `EVIDENCE` = one-element JSON array carrying dart_rate/cohort_pooled_dart/fold/cases/deaths/naics/industry/size_band/cohort_n/hours/employees/city/state (readable standalone, per Step 3d), `EVIDENCE_COUNT = 1`, `COMPILED_SQL/SQL_SHA256/AS_OF_DATE/SOURCE_SNAPSHOTS` as above.
  - Reuses `leads._ensure_leads_table`, `leads._merge_leads`, `leads._expire_rule` (imported), `safety.suppressed`/`safety.gate_rows` — no duplicate persistence logic.
  - CLI: `python -m connect.cohort_leads --year 2024 --write` (mirrors how `cohort.py` itself is invoked directly, not via `__main__.py`'s subparser — consistent with existing convention). `--write` omitted = dry-run preview only (matches `leads`' default-preview convention).
- 3c. `connect/receipt.py`: minimal 2-line change — `spec = JOBS.get(rule) or cohort_leads.COHORT_SPECS.get(rule)` (lazy import to avoid a cycle), so `connect receipt --id <LEAD_xxx> --check` works for this rule via the existing, unmodified `_verify()`/`assemble()`/`render()`.

**Step 4 — Run it:**
- `python -m connect.cohort_leads --year 2024 --write` → persists rows into `LIBRARY_META.CONNECT.LEADS`.
- `python -m connect receipt --id <top-lead-id> --check` → confirms byte-identical SHA256 and reproduces the row read-only. Do this for at least 2-3 of the top outliers.

**Step 5 — Write the finding file + brief:**
- `outputs/FINDING_osha_cohort_2024.md`: full ranked table (or top 25 + pointer to the CSV/LEADS table), the separate death list, Step 1/2 guard results with counts, Step 2 limitations, explicit non-claims (peer-relative rate ≠ negligence), explicit "NOT published — pending human sign-off."
- Chat brief to Chris: 3-5 bullets, answer first, per constitution §2/§6.

**Explicit scope boundaries respected:** no new OSHA years, no new cohort dimensions, no new connectors/datasets, no GLEIF/FIPS/absence-detector/phoenix-key work.

## Verification
- Guard queries in Step 1 return the expected zero-violation counts (shown in the finding file).
- `python -m connect.cohort --year 2024` still reproduces identical numbers after the `EST_KEY` column addition (diff the console output / CSV row counts against the pre-change baseline already captured).
- `python -m connect.cohort_leads --year 2024` (dry-run) prints a preview without error.
- `python -m connect.cohort_leads --year 2024 --write` persists rows; a `SELECT COUNT(*) FROM LIBRARY_META.CONNECT.LEADS WHERE RULE_NAME='osha_cohort_outlier_2024'` matches the outlier count.
- `python -m connect receipt --id <lead> --check` prints `✓ reproduced` for spot-checked leads.

## Critical Files
- `connect/cohort.py` — one additive column (EST_KEY) to the final SELECT; re-verify guards after.
- `connect/cohort_leads.py` (new) — the receipt-compatible adapter; reuses `leads.py`'s persistence helpers rather than duplicating them.
- `connect/receipt.py` — 2-line spec-lookup fallback so `receipt --check` works for this rule.
- `connect/leads.py` — source of the reused `_ensure_leads_table`/`_merge_leads`/`_expire_rule`/`_lead_id` pattern (not modified, only imported from).
- `outputs/FINDING_osha_cohort_2024.md` (new) — the human-facing finding artifact.
