---
created: 2026-07-30T04:49:13.945Z
session: 213efbdd-b1c9-42ec-ac72-0d171745aff4
working_directory: c:\Code\Ripple_v6
---

## What I found (the handoff's numbers don't hold up)

**Nine corrections, all verified live this session:**

1. **It's 112 candidates, not 307.** 346 modeled − 42 wired = 304, but only 112 have any verified key. The handoff conflated "not in the spine" with "has a key ready to use."
2. **`fed_dea_arcos` (178.6M, the "fastest real win") is a ghost.** Its `LANDING_FQN` points at `FED_DEA_ARCOS` which doesn't exist; `LANDED_ROW_COUNT` is NULL; its row count is copied from `FED_DEA_ARCOS_FULL`, which is **already in the spine**. Nothing to wire.
3. **Row count is the wrong ruler.** GLEIF REPEX: 6.3M rows → **+2,288** new entities. USASPENDING assistance: → **+175,699**. The 21 CMS/NPI tables: **+0, +0, +0, +236, +19**. Ranking by rows points at the least valuable work.
4. **The spine reads `LIBRARY_RAW.LANDING`, not the marts.** Zero of 42 spine tables are marts; zero need to be. "Wire the marts" really means "wire the landing tables the marts sit on."
5. **The blocker nobody named: two key vocabularies have drifted.** The registry says `STEEL: FRS_ID`, but `connect/` *cannot join on it* — `KEY_TOKENS` (portal_recon) has never heard of `FRS_ID`, and `normalize_sql` **raises** on unmapped keys. `scripts/retier_join_keys.py` invented `FRS_ID/PWSID/MINE_ID/FEC_ID/CUSIP` in its own dict. No new axis opens until both sides agree.
6. **Entity-type mapping is duplicated across 4 files** that must stay in lockstep by hand (`spine.py`, `incremental.py`, `entity_index_specs.py`, `spine_entity.py`).
7. **35 sources are silently truncated at exactly 500,000 rows — at the loading dock, not the mart.** None flagged `IS_SAMPLE`. This is an honesty problem: once wired, "this provider isn't in dataset X" would read as a fact when we just never loaded X.
8. **`MATCH_PAIRS` grows ~4–5x** (22.2M → ~100M). Measured, not guessed: adding 5 CMS/NPI tables took the touched slice 13.1M → 22.6M pairs. Acceptable, but worth watching.
9. **All four new axes verify clean:** MINE_ID (9-char, 0 blank, 31,277 mines), PWSID (9-char, 0 blank), FRS_ID/REGISTRY_ID (12-char, 5.3M distinct), FEC CMTE_ID (9-char, 0 blank). And the `COUNT(col)` trap caught one: USASPENDING `recipient_uei` reads 100% non-null but is **61% empty strings** — real key, 7.8M joinable rows not 19.9M.

**Your calls, applied:** wire every key type; keep NPI breadth; correct the exclusion list. **My call (truth, not taste):** `ACCESSION_NUMBER`/`CUSIP` do *not* become entity types — a filing isn't an entity and a security isn't an org. They wire as links off the existing CIK axis in a later pass.

## Implementation

**Step 0 — reversibility.** Zero-copy clone `LIBRARY_META."CONNECT"` before touching anything.

**Step 1 — kill the vocabulary drift (the real unlock).**
- Add `FRS_ID, PWSID, MINE_ID, FEC_CMTE_ID, FEC_CAND_ID` to `KEY_TOKENS` as STEEL, with exclusion tokens for false friends.
- Add matching `NORM_RULES` in `connect/keys.py`: `FRS_ID ("fixed",12)`, `PWSID ("fixed",9)`, `MINE_ID ("pad",9)`, `FEC_CMTE_ID/FEC_CAND_ID ("alnum_upper",0)` — each width taken from the live check above, not assumed.
- **Split `FEC_ID` into committee and candidate.** `retier_join_keys.py` lumped them; a committee is not a candidate, and merging them would fuse a PAC with a human.
- Add a parity test asserting every STEEL/STRONG key in `KEY_TOKENS` has a `NORM_RULES` entry, so this drift can't recur silently.

**Step 2 — collapse the 4-site lockstep into one dict.** Make `ENTITY_TYPE_BY_KEY` in `entity_index_specs.py` the single source; generate the `CASE` SQL in `spine.py`/`incremental.py` from it. New: FRS_ID/PWSID/MINE_ID → `facility`, FEC_CMTE_ID → `organization`, FEC_CAND_ID → `person`.

**Step 3 — `scripts/gen_spine_specs.py`.** Per candidate: verify the key with `COUNT(*)` + `COUNT(DISTINCT normalized)` + blank% + length range + value sample (CLAUDE.md §7 discipline, never the tier label), pick name/address columns, assign authority. Emits a `DISPLAY_SPECS` block for git-diff review plus `outputs/spine_wiring_evidence.csv`. **Anything failing verification is rejected to a file, not wired.** Specs stay in Python — git history plus the existing config-fingerprint guard already give durability and force a rebuild on change; a control table would just be a second source of truth.

**Step 4 — wire in waves, verify between each.**
- **Wave 1 (new entities, mission-first):** USASPENDING assistance (UEI, +176K orgs), OSHA ITA ×6 (EIN, +102K employers — injury counts are literally a column), IRS Pub78 + CMS `*_ENROLLMENTS` (CCN).
- **Wave 2 (new axes):** MSHA first (smallest, proves the path), then EPA FRS_ID ×12, PWSID ×9, then FEC.
- **Wave 3 (breadth):** the ~21 CMS/NPI + 14 CIK sources.

**Step 5 — correct `discover.py`.** Drop `FED_USASPENDING_ASSISTANCE_FULL` from `EDGE_UNIVERSE_EXCLUDE_TABLES` with the evidence in the comment. Keep `CONTRACTS_FULL`/`BULK` excluded — those *are* duplicates, and `CONTRACTS_FULL` sitting at exactly 20,000,000 rows is its own truncation smell.

**Step 6 — honesty flag.** Mark the 35 landing-capped sources in the registry so spine absence never reads as a real negative. Non-optional before the spine gets used for "X isn't in Y" claims.

**Step 7 — full rebuild.** `python -m connect spine`, then `python -m connect.incremental seed` to re-pin the fingerprint.

## Verification
- `scripts/audit_dbt_sources.py` pre-flight before any change.
- **ENTITY_ID stability:** IDs are content-addressed, so a rebuild must renumber *no one*. Diff the pre/post ENTITY_ID set against the clone — any drift means Step 1 re-keyed something and stops the wave.
- Per-source evidence row: rows_with_key / distinct_keys / new_to_spine, recorded in the CSV.
- Assert `normalize_sql` raises for zero wired keys; parity test green.
- Before/after: source tables 42→N, entities, multi_source, MATCH_PAIRS size.
- Spot-check 3 entities end to end: an OSHA employer that's also a 990 filer, one mine, one water system.
- Re-confirm all 252 `THE_LIBRARY` views still resolve (they broke on a prior session's table churn).

## Critical files
- `connect/entity_index_specs.py` — `DISPLAY_SPECS` *is* the spine's actual scope; all wiring lands here
- `connect/keys.py` — `NORM_RULES`; a missing entry means `normalize_sql` raises, not silently mis-joins
- `portal_recon/tag_portal_index.py` — `KEY_TOKENS`, the other half of the drifted vocabulary
- `connect/spine.py` + `connect/incremental.py` — entity-type lockstep, rebuild and MERGE paths, config guard
- `scripts/retier_join_keys.py` — the registry-side vocabulary to reconcile against

## Open, flagging not deciding
`MATCH_PAIRS` is fully derivable from `ENTITY_MAP.MEMBER_TABLES`. At ~100M rows it may not be worth materializing — but that's an existing design choice, so I'll measure it and come back rather than change it mid-wiring.