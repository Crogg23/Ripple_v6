---
created: 2026-07-29T15:21:52.187Z
session: 01271d36-48bc-4afd-a88e-5b4a008f43b5
working_directory: c:\Code\Ripple_v6
---

## Plan: Build Marts for All 336 Unmodeled Sources

### Context
- **96 dbt mart model files** already exist in `library-onboarding/ripple_dbt/models/marts/`
- **96 mart tables** exist in LIBRARY_MARTS across various schemas
- However, many of those 96 files haven't been run (`dbt build`), and ~240 sources have no model written at all
- The pattern is well-established: `{{ config(materialized='table', schema='DOMAIN') }}` with typed columns, dedup, and a source reference

### Three-phase approach

**Phase 1: Run dbt build on the 62 existing models that aren't materialized yet (~5 min)**
- These already have SQL written but haven't been executed
- Includes: `health__fed_cms_nadac`, `economics__fed_sba_ppp`, `economics__fed_bls_qcew`, `economics__fed_sba_loans`, `economics__fed_foreignassistance`, `justice__xc_vera_incarceration_trends`, `land_and_territory__fed_bia_tribal_geo`, `criminal_justice__fed_bjs_data`, etc.
- Just run `dbt build` from the ripple_dbt directory

**Phase 2: Write new mart models for the ~128 tier A-D sources (1K+ rows, no model) (~bulk generation)**
- For each source: generate a `domain__source_id.sql` file following the established pattern
- Pattern: read columns from INFORMATION_SCHEMA → write typed SELECT with `try_to_date`, `try_to_number`, `try_to_double` casts → dedup with `qualify row_number()`
- I'll batch-generate these using a script that reads column metadata and writes the model files
- Positional-format sources (FEC C1..C15, NHTSA C1..C51) already have staging views that handle the column renaming — their mart just references `{{ ref('stg_...') }}`

**Phase 3: Write minimal "pass-through" mart models for the ~208 tier E sources (<1K rows)**
- These are mostly portal scrapes — the mart is basically `SELECT * FROM source` with metadata columns stripped
- Batch-generatable with the same script

### Implementation steps
1. Run `dbt build --full-refresh` in the ripple_dbt directory to materialize all 62 existing unmaterialized models
2. Write a Python script (`scripts/gen_mart_models.py`) that:
   - Queries LIBRARY_RAW.INFORMATION_SCHEMA.COLUMNS for each unmodeled source
   - Generates a mart SQL file using the established pattern (typed casts, dedup, domain schema)
   - Maps source domain_primary from catalog → schema subfolder
3. Run the generator for all ~240 sources missing models
4. Run `dbt build` again to materialize the new models
5. Verify with a catalog query that `_real_mart` count matches expectations

### Verification
- `SELECT lifecycle, COUNT(*) FROM LIBRARY_META.REGISTRY.CATALOG GROUP BY 1` should show most sources as 'modeled'
- Spot-check 5-10 new mart tables to verify row counts match landing
- Run `dbt test` for any schema tests that exist

### Critical files
- `c:\Code\Ripple_v6\library-onboarding\ripple_dbt\dbt_project.yml` — schema/materialization config
- `c:\Code\Ripple_v6\library-onboarding\ripple_dbt\profiles.yml` — connection config (needs env vars set)
- `c:\Code\Ripple_v6\library-onboarding\ripple_dbt\models\marts\health\health__fed_cms_nadac.sql` — pattern to follow for typed marts
- `c:\Code\Ripple_v6\library-onboarding\ripple_dbt\models\staging\fed_nhtsa_complaints\stg_fed_nhtsa_complaints__records.sql` — pattern for positional-format sources
- `c:\Code\Ripple_v6\library-onboarding\ripple_dbt\models\marts\corporate_registry\corporate_registry__intl_uk_companies_house.sql` — pattern for dotted-column sources