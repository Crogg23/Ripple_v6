---
created: 2026-07-29T19:13:43.041Z
session: 397ef9b7-b22a-4c41-8be9-b5002ec75b01
working_directory: c:\Code\Ripple_v6
---

## Context — what I verified (and what the handoff got wrong)

**The headline: your dbt project is completely dead. It cannot even parse.** Not "warnings." A hard stop. `dbt run`, `build`, and `test` are all impossible right now.

Two independent reasons:

**1. Duplicate source definitions — 962 of them.**
`dbt parse` aborts on the first one. GAP 3 called this "warnings only, not blocking" — that's wrong, it's fatal.
- 939 come from `_all_sources.yml` (untracked, created last session).
- **23 existed *before* that file.** I proved it by moving the catch-all aside — dbt still failed, on `FED_FOREIGNASSISTANCE` declared in both `_economics__sources.yml` and `_health__sources.yml`.
- The catch-all is 1,905 lines that provide exactly **one** definition nothing else has (`FED_SEC_13F_SUBMISSIONS`). It's 99.9% dead weight that broke the build.

**2. dbt has no way to log in.** You asked how you don't have a PAT when I've been querying all session. Here's the answer: `~/.snowflake/connections.toml` uses `authenticator = "OAUTH_AUTHORIZATION_CODE"` — browser OAuth. There is no PAT and no password in it. I query through that OAuth token; `profiles.yml` demands a `SNOWFLAKE_PAT` env var that does not exist on this machine. No private keys either.

**So the previous session's "ran dbt build, hit 28 errors" could not have happened as described.** dbt couldn't connect and couldn't parse. Those 28 "errors" came from somewhere else. Treat that whole claim as unverified.

**Other confirmed defects:**
- **Your counts are inflated.** 30 sources are double-counted — a lowercase row (`fed_fda_drug_enforcement`, modeled) *and* an UPPERCASE row (`FED_FDA_DRUG_ENFORCEMENT`, stale). "358 active / 95% connectable" is overstated. Real: 221 modeled, 137 landed, and `stale` is 5, not 34.
- **7 junk marts** — 4 are a single unparsed JSON column called `RECORD`; plus `RAW_TEXT`, `COL_1/COL_2`, and one with only `CASE_TYPE`.
- **8 orphan duplicate tables** the disabled models never cleaned up (`UNCATEGORIZED__INTL_GLEIF`, `ECONOMICS__INT_GLEIF_RR` twin of `..._INTL_GLEIF_RR`, three `UNCATEGORIZED__*` sitting inside the `FINANCE` schema).
- **50 mart models are missing `schema=`**, so dbt sends them to `LIBRARY_MARTS.DBT_CROGERS` instead of their domain schema. That's the 44 stray tables there.
- **Good news:** zero empty marts. Spine confirmed at 42 tables / 25.3M records. And GAP 10 is a false alarm — `FED_IRS_EO_PR` is a 2,587-row Puerto Rico subset; your real exempt-orgs file is `FED_IRS_BMF` at 1.97M rows. (Separately, `FED_IRS_990` has only 200 rows — that one looks broken.)

---

## Implementation steps

**Phase 1 — Make dbt run at all** *(gate: `dbt parse` passes)*
1. Add `FED_SEC_13F_SUBMISSIONS` to `models/marts/uncategorized/_uncategorized__sources.yml`, then delete `models/marts/_all_sources.yml`.
2. Fix the 23 pre-existing duplicates. Rule: **`staging/<source>/schema.yml` wins** (it's the per-source convention and carries the data tests); strip the duplicate table entry from the mart-level `_*__sources.yml`. Both declare identical `database`/`schema`, so this is lossless. Two are mart-vs-mart (`FED_DEA_ARCOS_FULL`, `FED_FOREIGNASSISTANCE`) — keep the domain that owns the model.
3. Switch `profiles.yml` to `authenticator: externalbrowser` and drop the dead `password` line. Matches how you actually log in — one browser popup, then it caches. No secret to store.
4. Extend `scripts/audit_dbt_sources.py` into a pre-flight check so a duplicate can never silently kill the build again.

**Phase 2 — Reconcile code to warehouse** *(gate: `dbt build` green)*
5. Add `schema='<DOMAIN>'` to the 50 models missing it, so they stop landing in `DBT_CROGERS`.
6. Run `dbt build`. This is the first real test of whether the 243 hand-built tables actually match their model files.
7. Drop the 8 orphan duplicate tables and the 3 misfiled `UNCATEGORIZED__*` in `FINANCE`.

**Phase 3 — Honest registry**
8. Collapse the 30 case-split duplicate rows, keeping the row with the real lifecycle. Make the `CATALOG` view's joins case-insensitive so this can't recur.
9. Re-baseline the source/connectable counts and hand you corrected numbers.

**Phase 4 — Parse the 7 junk marts**
10. Crack the `RECORD` JSON into real columns (same approach that worked for FDA Drug Enforcement), re-tier them in the registry, and back each one with a proper dbt model instead of direct SQL.

**Phase 5 — The 137 landed sources**
11. Generate marts for all 137. **One call I'm making for you:** the four giants (`fed_sec_13f_holdings` 101M, `fed_courtlistener_dockets` 71M, `fed_fda_faers_drug` 20M, `fed_usaspending_contracts_full` 20M) get **views, not tables.** A `select *` table on 101M rows just pays twice for the same bytes. Views cost nothing and read the same. The other 133 get normal tables. Say the word if you want them all as tables anyway.

---

## Verification
- `dbt parse` — must pass (offline, no creds needed). This is the hard gate on Phase 1.
- `dbt build` — real end-to-end proof, once `externalbrowser` auth is wired. Expect one browser popup.
- `dbt test` — runs the existing data tests that have never been able to execute.
- SQL spot-checks: no zero-row marts; every mart resolves to a domain schema (not `DBT_CROGERS`); registry has zero case-duplicate `SOURCE_ID`s; the 7 formerly-junk marts have real typed columns.
- Re-run `scripts/audit_dbt_sources.py` — must report 0 duplicates.

## Critical Files
- `library-onboarding/ripple_dbt/models/marts/_all_sources.yml` — the 1,905-line file to delete; primary cause of the parse failure
- `library-onboarding/ripple_dbt/profiles.yml` — auth is broken here; switch to `externalbrowser`
- `library-onboarding/ripple_dbt/macros/generate_schema_name.sql` — the domain-schema routing contract the 50 unconfigured models bypass
- `library-onboarding/ripple_dbt/models/staging/<source>/schema.yml` — canonical source definitions; the winner in every duplicate conflict
- `scripts/audit_dbt_sources.py` — duplicate detector I wrote this session; becomes the permanent pre-flight guard

## One thing to know before you say go
Phase 5 is real warehouse spend, and Phase 2's `dbt build` is the first honest test this project has ever had — **expect it to surface failures the previous session reported as fixed.** I'd rather find them now than have you trust a number that isn't real.