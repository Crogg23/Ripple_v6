# WHERE AM I — Full Repo Audit, read through the new Constitution

**Date:** 2026-07-14 · **Method:** 9 agents swept the whole repo and cross-checked every claim against a **live read-only query to Snowflake this session** (role `RIPPLE_READER`, 17:58 PT). Where a doc and the warehouse disagreed, the warehouse won. Yesterday's `STRATEGIC_REVIEW` / `PROJECT_SHAPE` / `build-state` were treated as claims to verify, not facts.

---

## The one paragraph

You've built a genuinely working, honestly-green build machine that **has never once closed its own core loop.** Scout → land → connect → detect all work and are real. Review → decide → publish has never run — **0 human decisions ever recorded, live-confirmed today.** And it's worse than "not started": the schema to record a decision doesn't exist yet, the one view documented as "the safe way to read published leads" points at a **dead, permanently-empty table**, and this checkout holds **no write-capable Snowflake credential at all.** Meanwhile the two most important new things in the repo — the Constitution that just redefined the mission, and the one finding that supposedly fulfills it — are both sitting **uncommitted in the working tree, one bad `git` command from vanishing.** The machine is ready. The loop is blocked by three concrete, fixable breaks. And underneath all of it sits a question only you can answer: is what you actually built the "census of power" the mission describes, or a healthcare-fraud detector you drifted into?

---

## Verified live numbers (2026-07-14, read-only)

| Metric | Value | Note |
|---|---|---|
| **Human decisions ever recorded** | **0** | the whole point, still zero |
| **Findings published anywhere** | **0** | the door has never opened |
| Active leads | 1,030 | ~1,009 are health-vertical (see §C) |
| Landed rows / tables | 284.8M / 1,784 | down 2.26M since Jul 12 |
| Real landed sources | 200 (170 landed + 30 modeled) | trust-unaudited (see gaps) |
| Connect edges / entities | 41,101 / 9.79M | ~4.3k trustworthy core |
| Open defects / blockers | 20 / **1** | blocker = leaked PATs |
| Pending actions (1 irreversible) | 2 | A00 Snowsight, A03 revoke |
| Last ingest run | **2026-07-11** | acquisition silent 3 days |

The needle that matters (`decisions.total`) hasn't moved off 0 since the machine was built. Everything upstream of "decide" is overbuilt relative to it.

---

## A. The three calls that are actually YOURS (RED — taste, not truth)

These aren't things I can fix. They're mission/scope, and the Constitution puts them squarely on you.

### 1. Your own steering doc now contradicts your own Constitution
- **STRATEGIC_REVIEW.md** (the doc currently driving the plan) says: *"pick the story… publish it,"* Definition of Done = *"one lead published = TRUE,"* freeze the whole library 30 days to get it.
- **The Constitution §1** says, in bold: *"'Pick one lead and publish it' is NOT the goal and never gets framed that way. THE MAP is the deliverable."*
- **My read: reconcilable in substance, opposed in framing.** The review's real argument is *"the back half has never run — exercise it once to prove it works."* The Constitution blesses exactly that: *"a single story is a pin dropped on the map to prove it's real."* Running one grain of sand through the loop = dropping a pin = allowed.
- **The reconciliation I'd propose (your call):** keep the review's insight — *prove the loop can drop ONE verified, human-signed-off pin* — but reframe the goal as **"prove the machine closes the loop,"** not *"publish a story."* And treat *"freeze the map for 30 days"* as a RED scope call you make on purpose, not a default the staff adopts.

### 2. Is the one finding you have a lead — or trivia?
- `SYSTEMIC_FINDING.md` (SBA 7(a) fee-cliff bunching) is **the best mechanism-first work in the repo** — pattern is the headline, case is the receipt, adversarially killed and it survived.
- But it says, in its own words: *"Nothing about this loan is improper — that is the point."* **No harmed human named.**
- **Constitution §1:** *"who gets hurt, and does the data show it? No human on the other end of the number → it's not a lead, it's trivia."*
- **Your call:** does an elegant, legal-but-systemic loan-sizing pattern clear the bar, or is it a beautiful piece of trivia? This decides whether your proof-of-mission actually proved the mission. (Caveat: this audit did **not** re-run its SQL — accepted its internal re-derivation on trust.)

### 3. "A census of all power" vs. what you actually built: a ~98% healthcare machine
- **Constitution §1:** *"Recon on power. We look at all of them, the same way… a census, not a subpoena."*
- **Reality (audit's aggregation of live facts):** ~97.9% of the 9.79M-entity spine is NPPES healthcare providers; **1,009 of 1,030 live leads are health-vertical**; the only non-health detector (`sec_filer_in_irs_bmf`) has **zero live rows.**
- Ripple's stated identity (even census of power) is contradicted by what it's become (a healthcare-fraud detector). **Is that drift to correct, or a beachhead to own on purpose?** Yours.

---

## B. The loop-blockers (GREEN fixes — the actual wall between you and a closed loop)

These are truth-questions with obvious answers. I can do them the moment you say go. All three were **verified broken live this session.**

1. **The publish surface is wired to a corpse.** `V_LEADS_PUBLISHED` — the view documented as "the safe way to read published leads" — still joins the OLD, retired, permanently-0-row `CONNECT.DECISIONS`. The write path moved to `REVIEW.DECISIONS` on 07-12. **Result: even if a human recorded a decision, it could never surface as published.** The one gate the whole safety architecture protects is structurally incapable of opening. *(Fix needs a write credential this checkout doesn't have — it's a Snowsight/your-PC job.)*

2. **The entity spine has been crashing on every run since June 29.** `connect/entity_index_specs.py` still uses double-underscore NPPES column names (`PROVIDER_LAST_NAME__LEGAL_NAME`) that stopped existing after the NPPES re-land. Reproduced the exact `invalid identifier` error live. The golden-record step dies, freezing the entity index, incremental sync, and lead backfill at **June 29.** The defect ledger marks this class "clear" — but only `resolve.py` was fixed; this sibling file is still live-broken. *(This one is a pure code edit — GREEN, I can fix now.)*

3. **The Constitution isn't committed.** Git HEAD still ships the OLD "Ripple Library Onboarding Agent" manual. `CLAUDE.md` shows `M`, all five new root docs + the SBA finding show `??`. *"Every session reads the new version on boot"* is **false** until committed — a fresh clone, GitHub, CI, and Fable all still read the stale onboarding manual. The single best evidence you followed your own "stop widening" call (the SBA hunt) exists nowhere durable. *(Say the word and I commit the Constitution + the working-tree docs.)*

---

## C. What yesterday's docs got WRONG (deltas the live check caught)

- **"Only write lane is an ACCOUNTADMIN token" → stale.** This checkout's default PAT is now the **read-only `RIPPLE_READER`** token; proven live it can't escalate and holds zero write privileges. The ACCOUNTADMIN-in-routine-use fire the review described isn't burning here. (New risk instead: writes now *fail* rather than *work dangerously* — see gaps.)
- **"evidence.dev is dark, dead token, swap undone" → stale.** `connection.yaml` was already swapped to the enforced `RIPPLE_READER`/`SERVE_WH` variant on 07-12 and the token authenticates live. Only the actual `npm` rebuild/data-pull remains unproven (it hung twice — needs a clean run).
- **"politics is plan-stage, P1–P5 unexecuted" → wrong on the day it was written.** politics/ is a **live, merged domain**: four box-score phases + itemized-donations mart + election-outcomes mart + a full judiciary sub-domain, reconciled to OpenFEC/GovTrack. The riskiest phase (P5 itcont firehose) is fully built. The review collapsed 3 real fronts into one wrong line.

---

## D. The honest gaps — what this audit could NOT see

Flagging these so "where am I" isn't falsely clean:

- **The entire write/run half is unassessed.** Everything ran as `RIPPLE_READER`. From this machine, `onboard.py` can't LOAD/REGISTER, dbt can't build marts, the REVIEW lane can't be provisioned. Writes happen from a vantage I can't see (your PC / a Snowsight ACCOUNTADMIN PAT / the Windows heartbeat). So "acquisition silent 3 days" and "LEAD_QUEUE mart missing" **may be structural (this box can't write), not choices.** Also unknown: whether the Anthropic API balance (0 credits as of 07-05 in memory) currently lets `onboard.py`'s LLM steps run at all.
- **"200 landed" is trust-unaudited.** Memory flags ~13 dead-scrape junk sources + poisoned tables (AIS reverse-causality snapshot, Open Payments year-split, LEIE date/NPI traps). The genuinely-usable count is **likely materially below 200.** The whole "survive a hostile skeptic" doctrine rests on this foundation, and it's unchecked.
- **Disaster recovery is thin.** One Snowflake account, one PAT you plan to rotate. The only backup is an **8-day-old partial META parquet** — it does NOT contain the 284M landing rows, the 9.8M spine, the 41k graph, or any marts. Lose the account and recovery is an aging partial snapshot.
- **Fable — a Constitution-designated worker — is unassessed.** §5 names it the recon/hunting worker; no slice found evidence it's operational, has a mission packet, or ever ran. Tellingly, the one real finding came from a Claude Code session, not Fable. 1 of 3 designated workers may not exist operationally.

---

## E. Housekeeping (low urgency, mostly GREEN)

- **Policy drift needs one ruling.** `foundation_before_detectives` (no new detector work) has been broken repeatedly — PR #48, the EIN detector, the SBA hunt. Under §0 the Constitution's mechanism-first mission probably **supersedes** it, but nobody reconciled it. One-line call: retire it, or re-affirm it. *(This is the policy your recent work keeps quietly violating — a policy you override silently isn't a policy.)*
- **Tests are honestly green** (442 pass / 2 skip), but **dbt CI never runs a live build** (no PAT secret) — which is exactly how the `LEAD_QUEUE` mart can be missing without anything turning red.
- **The launchd heartbeat was never installed on this Mac** — silent 9 days, all tiers overdue. Built, self-tests clean, just not wired up.
- **Branch cleanup:** 19 of 24 branches are merged/dead, 4 abandoned since June 21. Safe to prune.
- **The docs layer still tells the old story** — README, OVERVIEW, docs/, and ~7 outputs/ files still frame Ripple as the "six-checkpoint onboarding agent" with Chris as "foreman." The Constitution replaced that (CEO/staff, three lanes). Stale by §0.

---

## The single next thing

**Commit the Constitution and the working-tree docs**, so the regime change and your one finding actually survive. It's the one action that's near-free, purely protective, and blocking nothing — and right now your newest, most important work is the least durable state possible. Everything in §A is your ruling; everything in §B I can start on the moment you point.
