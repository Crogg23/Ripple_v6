[pytest]
testpaths = tests
markers =
    snowflake: requires a live Snowflake connection (PAT in library-onboarding/.env)
    network: requires reaching a live public website (skips itself if unreachable)
